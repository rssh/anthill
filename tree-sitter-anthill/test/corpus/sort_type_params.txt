=====================================
sort with higher-kinded type param
=====================================

sort M[F[T]]
end

---

(source_file
  (sort_with_body
    name: (name (identifier))
    (sort_type_param_list
      (sort_type_param
        name: (identifier)
        (sort_type_param_list
          (sort_type_param
            name: (identifier)))))))

=====================================
sort with simple type params
=====================================

sort Duo[A, B]
end

---

(source_file
  (sort_with_body
    name: (name (identifier))
    (sort_type_param_list
      (sort_type_param
        name: (identifier))
      (sort_type_param
        name: (identifier)))))

=====================================
WI-454 bare bracket binder
=====================================

sort [A]

---

(source_file
  (sort_bracket_binder
    name: (identifier)))

=====================================
WI-454 bare var-marker binder
=====================================

sort ?A

---

(source_file
  (sort_var_binder
    marker: (variable)))

=====================================
WI-454 structured bracket binder
=====================================

sort [F] {
  sort [T]
}

---

(source_file
  (sort_bracket_binder
    name: (identifier)
    (sort_binder_body
      (sort_bracket_binder
        name: (identifier)))))

=====================================
WI-454 structured var-marker binder
=====================================

sort ?F {
  sort ?T
}

---

(source_file
  (sort_var_binder
    marker: (variable)
    (sort_binder_body
      (sort_var_binder
        marker: (variable)))))
