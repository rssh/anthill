-- Logic.Minimal — kernel SLD-over-Horn-clauses, no extra axioms.
-- Empty by design; sorts that `requires Logic.Minimal` declare they
-- assume nothing beyond the resolver's native semantics.
namespace anthill.logic.Minimal
  export Minimal
  sort Minimal
  end
end
