-- Operation: effectful computation.
-- E is constrained via requires EffectSet[E].
sort anthill.prelude.Operation
  sort A = ?
  sort B = ?
  sort E = ?
  requires EffectSet[E]

  operation apply(op: Operation[A, B, E], x: A) -> B effects E
end
