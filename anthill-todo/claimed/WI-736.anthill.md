## Attributes

- id: WI-736
- created: 2026-07-17T06:16:25Z

- status: Claimed
- status_agent: user
- status_at: 2026-07-17T08:46:16Z

- acceptance: cargo-test

## Description

examples/classic-mini: a collection of small CLASSIC programs implemented in anthill, one per subdirectory (eight-queens, eight-philosophers, ...). MOTIVATION: the repo's two examples (github-todo, webots-modelling) are both DOMAIN MODELS — there is no small, self-contained showcase of anthill AS A LOGIC LANGUAGE (rules + SLD search + the relational face). Notably proposal 052 uses 'queens' / 'Queen.find' as its RUNNING EXAMPLE throughout (052:18, :71, :91-95, :310-327) and NO queens implementation exists anywhere in the repo — verified: 'queens' occurs only in proposals, design docs and Rust comments. So the canonical illustration of the language's headline feature has never actually been written or run. SCOPE: examples/classic-mini/<name>/ per example, each self-contained (.anthill sources + a README stating the problem, the encoding, and how to run it), plus a collection index README. FIRST: eight-queens — 052's own running example, and the natural END-TO-END validation of WI-714: a rule 'queens(?board)' cited BY NAME is a Relation[Board] (1-collapse), consumed through the inherited Stream API (splitFirst / takeN — NOT head/headOption, which are rule-backed and do not evaluate, WI-733) and composable via the algebra (where / project / fix). THEN: eight-philosophers (dining philosophers) — anthill has no concurrency (Branch is out of 052's scope; Suspension = WI-069, unimplemented), so the INTERESTING part is modelled declaratively: states, the deadlock condition as a rule, and safety as negation-as-failure (watch the WI-525 NAF allowedness / floundering rules and the WI-728 free-column hazard). Further candidates (the 'etc'): zebra puzzle, towers of hanoi, family/ancestor transitive closure, map colouring, send+more=money. Each example is a separate increment; the directory + index + eight-queens is the first. ACCEPTANCE: examples/classic-mini/eight-queens loads and RUNS, producing correct solutions (8-queens has 92; the first solution is checkable), driven from anthill source not a Rust harness; the collection index + a per-example README explain the encoding; the examples are EXERCISED BY A TEST so they cannot rot (the github_todo_test.rs precedent — note WI-719's load-ORDER hazard: stdlib must register before an examples/ dir that sorts lexicographically before it); cargo-test green via scripts/test.sh.

## Changes

### 2026-07-17T06:18:27Z — feedback — claude

HELD (user, 2026-07-17) until the rule-body soundness floor lands. eight-queens — the example proposal 052 keeps citing — needs BOTH WI-738 (its diagonal test is arithmetic: neq(sub(?x,?y), ?d)) and WI-739 (queens(?board) is queried with the answer FREE, the (out) mode that currently flounders). Writing it today would ship an example that 'runs' and prints garbage rather than failing loudly. Depends now on WI-738 + WI-739. NOTE for when this resumes: an arithmetic-free example (ancestor/transitive closure; map colouring) IS expressible today PROVIDED every guard is spelled not(eq(..)) rather than neq(..) — verified: not(eq(?x,?y)) enumerates 6 ground rows where neq(?x,?y) flounders to 1 unbound row. That workaround should DISAPPEAR once WI-739 lands, so do not bake it into the examples.

### 2026-07-17T06:20:25Z — feedback — claude

SCOPE CORRECTION (user, 2026-07-17): eight-queens is SPLIT OUT to its own ticket WI-740, which depends on the unimplemented engine work (WI-738 arithmetic-in-rule-body soundness, WI-739 the (out,out) generate-and-test flounder). The COLLECTION itself is NOT blocked by those — its dependencies on WI-738/WI-739 are removed. Only queens needs them. So classic-mini can proceed with arithmetic-free examples (ancestor/transitive closure; map colouring) whenever it is resumed; it is PreOpened now only because the user chose to do the soundness floor (WI-738) first, not because it is blocked.

### 2026-07-17T08:58:09Z — feedback — user

INCREMENT 1 DELIVERED (pending commit): the directory + collection index + the first example (map-colouring). HOLD RELEASED — this ticket was held on the rule-body soundness floor; WI-738 (7506bc38) and WI-739 (5c89bde7) are both delivered.

examples/classic-mini/{README.md, map-colouring/{colouring.anthill, README.md}} + rustland/anthill-core/tests/classic_mini_test.rs. Runs from anthill SOURCE, not a Rust harness: it provides anthill.cli.Main, so 'anthill run examples/classic-mini/map-colouring/' prints 6. The test pins the ANSWER (6 = 3! for the WA/NT/SA triangle, Q/NSW/V forced) and that EVERY row is DEFINITE — a count alone would have read the WI-739 flounder's single unbound row as '1 solution'. WI-719 load-order hazard handled: stdlib collected BEFORE the example dir (classic-mini sorts before both github-todo and stdlib).

MAP-COLOURING IS THE END-TO-END VALIDATION OF THE FLOOR, and this was VERIFIED not assumed: I reverted resolve.rs to 797659cb (pre-WI-739), rebuilt, and re-ran the example — it does NOT print 6, it raises 'error: relation_floundered'. That is WI-737 failing loudly on WI-739's bug, the two composing exactly as WI-738's notes predicted. Post-fix it prints 6. The guards are spelled neq(..), the natural way — this ticket's own feedback said the not(eq(..)) workaround 'should DISAPPEAR once WI-739 lands, so do not bake it into the examples', and it has, so it is not.

ANCESTOR WRITTEN, VERIFIED BLOCKED, WITHDRAWN. Transitive closure was to be the second example (this ticket's feedback named it as expressible today). It is NOT: a RECURSIVE rule cannot be cited by name as a Relation[T] at all. Bisected live — single-clause cited by name WORKS, multi-clause non-recursive WORKS, recursive fails to LOAD ('expected a common column type across relation clauses, got disjoint types for column e'). Mechanism confirmed by a discriminating probe: a column whose only typing source in a clause is the rule's OWN recursive self-reference gets no type, so the cross-clause LUB calls it disjoint; adding a non-recursive goal that independently types the column makes it load and run. WI-714's schema synthesis has no fixpoint. Filed as WI-714 feedback (it is WI-714 CORE piece (3), not a new ticket). NOT shipped with a workaround — per this ticket's own scope note, a workaround that reads as idiomatic is worse than a missing example; the collection README states the gap, why the example is absent, and that the collection stays incomplete until it can be written honestly.

ACCEPTANCE NOTE — this ticket's ACCEPTANCE text is STALE: it still says 'examples/classic-mini/eight-queens loads and RUNS', but queens was SPLIT OUT to WI-740 (user, 2026-07-17) and is not part of this ticket. Read the acceptance as: the collection + index + at least one example that loads, runs and is test-exercised. Queens (WI-740) is now UNBLOCKED — both 738 and 739 are delivered.

REMAINING for this ticket (each its own increment): ancestor (blocked on the WI-714 gap above), dining philosophers, and the further candidates (zebra, hanoi, send+more=money). cargo-test green 2942/0 via scripts/test.sh.

### 2026-07-17T09:45:48Z — feedback — user

INCREMENT 2 DELIVERED: ancestor — the example this ticket wrote, verified blocked, and WITHDRAWN. The blocker (WI-714 recursive schema synthesis) is FIXED, so it is restored and runs honestly, with no workaround.

examples/classic-mini/ancestor/{ancestor.anthill, README.md} + a classic_mini_test case. `anthill run examples/classic-mini/ancestor/` prints 12 / 3 / 1.

THE EXAMPLE: transitive closure as two clauses — an ancestor is a parent, or a parent's ancestor. No traversal, no visited set, no queue. It also shows what a rule-as-relation buys that a function does not: ONE rule answers three modes — `ancestor` (both columns free, the whole closure), `ancestor("bart")` (first column bound; the applied citation BINDS a column and narrows T per 052 §8.3 → Relation[String]), `ancestor("bart","orville")` (both bound → Relation[Unit] membership).

WHY 12: 5 parent facts close into 12 pairs (3+3+3+2+1 over bart/lisa/maggie/homer/abe). The base clause ALONE yields exactly the 5 parent edges, so the count pins that the recursive clause actually ran — a build that dropped it would print 5. The test asserts all three modes plus a fourth: the CONVERSE `ancestor("orville","bart")` is empty, so the relation is not accidentally symmetric. Rows are asserted DEFINITE (the WI-737/739 precedent map-colouring set — a count alone would read a floundered unbound row as an answer).

The test also REFACTORS map-colouring's hand-built goal onto a shared `query(kb, qn, args)` helper (both tests built the same Term::Fn + ResolveConfig by hand).

NOTE the ancestor test's real gate is the LOAD: the example cites `ancestor` BY NAME, so the typer synthesizes its schema while type-checking main's body, and `load_example` panics on failure. Recursive rules always RESOLVED fine as subgoals — the resolver queries would have passed even before the fix. Said so in the test doc so it is not mistaken for a resolver test. The relation VALUE face over a recursive rule is covered end-to-end in wi714_recursive_relation_test.

FOOTGUN worth knowing (hit twice, cost a bogus bisection): a namespace whose LAST SEGMENT equals the rule name SHADOWS it — `namespace classic.ancestor` + `rule ancestor` makes `ancestor.takeN` resolve the NAMESPACE and fail with a misleading `unknown functor` rather than the real error. Hence `namespace classic.ancestry` with `rule ancestor`. It fails loudly, so it is not a soundness issue, but it is undiscoverable from the message.

Collection README updated: ancestor moves from "Not yet here" to Runs; the withdrawal paragraph is replaced by one line of history (it was withheld, the gap is fixed, it is here). REMAINING for this ticket: dining philosophers + the further candidates (zebra, hanoi, send+more=money). Queens is WI-740.

