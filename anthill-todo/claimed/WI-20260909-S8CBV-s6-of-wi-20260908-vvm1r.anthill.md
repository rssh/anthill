## Attributes

- id: WI-20260909-S8CBV-s6-of-wi-20260908-vvm1r
- created: 2026-09-09T17:03:50Z

- status: Claimed
- status_agent: claude
- status_at: 2026-09-09T17:51:59Z

- acceptance: cargo-test

- depends_on: WI-20260909-96ZTM-s4-of-wi-20260908-vvm1r

- tags: vvm1r

## Description

S6 of WI-20260908-VVM1R (umbrella): ATTRIBUTION BY PROJECTION ROOT -- let the requirement channel name a path-dependent projection, and key which dictionary a require means on the projection ROOT rather than on the bound SORT. This REPLACES S4's (WI-20260909-96ZTM) sort-matching selector and lets its anchored gate be lifted; it is not built on top of them.

WHY THE SORT CANNOT BE THE KEY (user, 2026-09-09, and it is the whole ticket). Two head bindings of one sort are not one carrier: if Leaf itself has a parameter, x: Leaf and y: Leaf may be Leaf[A] and Leaf[B], so x.T and y.T differ. S4 keys attribution on the bound sort and therefore CANNOT separate them -- rule p(?x: Leaf, ?y: Leaf) is refused there, and S4's own note records that as not delivered. The identity that does separate them is the PROJECTION ROOT: different expressions (different = not delta-equal) have different roots; delta-equal expressions are compatible.

NOT typeof, and not a runtime operation. This is path-dependent typing, and the substrate is already designed AND implemented: docs/design/path-dependent-types.md sec 4 -- 'equality is definitional conversion' -- delta-ground both sides and, if both stay neutral, check sigma-equality; 'a neutral equals only an identical neutral' (kernel-language.md 4331). unify_types OWNS that comparison (delta at the sites, zeta in unify_types, per that doc's sec 4.1). THIS TICKET MUST DELEGATE TO IT AND NOT RE-IMPLEMENT IT: S4 hand-rolled a structural key for the same question and /code-review found it wrong twice (head-only + SHORT name made Box[E = Leaf] and Box[E = Other] compare EQUAL; and it never canonicalized, which views_structurally_equal's own doc tells callers to do first).

MEASURED 2026-09-09, six rows, and they scope the work exactly:
  A  rule r(p: Leaf) :- seed(p)                          LOADS   -- proposal 060 sec 2.1 is live (WI-742), verified not merely claimed
  B  bare body reference to p                            LOADS
  C  sigil-free typed head beside a require              LOADS
  D  rule ... :- ?d = require[Desc[T = p.E]]             REFUSED -- 'p.E names neither a sort nor one of Desc's own type parameters'
  E  operation k(x: Leaf) requires Desc[T = x.E]         REFUSED -- unresolved name 'x.E'
  F  operation h(x: Leaf, e: x.E)                        LOADS

SO THERE IS NO GRAMMAR PROBLEM. p.E PARSES in a require bracket. (An earlier probe hit a parse error on ?x.E -- the SIGIL form; sec 2.1's head variable is a bare name used bare in the body, exactly like an operation parameter, so the projection is the same p.E that already loads in signature positions.) The requirement channel is the ONLY type position that cannot resolve a projection: F loads and E does not, in the same signature.

TWO GATES, and they are different failures. (1) D is WI-20260909-51W18's DROP RULE, a load-time classification this umbrella added: it admits a sort or the spec's own parameter name and reports everything else, so it reads a projection as a typo. It must learn that a projection is a third kind. (2) E is name RESOLUTION in the operation requirement channel: signature return and parameter positions resolve x.E (F) and the requires bracket does not. Fix (2) first -- it is a real gap with no dependence on this umbrella, and it is the smaller half.

THEN ATTRIBUTION. A require whose carrier binding is a projection names its ROOT; two requires are the same dictionary exactly when their roots are delta/sigma-equal. Consequences, each of which REMOVES something S4 installed rather than adding to it: the sort-matching selector goes (it is what mis-handles an APPLIED bracket by matching its head and dropping its arguments, and what accepts an INTRODUCER bound by symbol collision); the >1-anchor refusal narrows to the genuinely unattributed case; and the ANCHORED gate can be LIFTED, because a projection root attributes on the WITNESS path too -- match the named root against the witness call's carrier argument, which is the thing scan order currently decides.

ACCEPTANCE. rule p(x: Leaf, y: Leaf, ?d1, ?d2) :- ?d1 = require[Desc[T = x.E]], ?d2 = require[Desc[T = y.E]] binds TWO dictionaries and they are DIFFERENT, asserted BY VALUE. THE FIXTURE DISCIPLINE IS NOT OPTIONAL and this umbrella has now been burned by it twice: assert the BOUND dictionaries handed across a rule boundary (the WI-860 supplied-vs-derived check answers, and the discriminating observation is a REFUSAL, which value dispatch cannot produce), and RUN EVERY ROW WITH THE REQUIRES DELETED before believing it -- S4's first headline answered 7 and 9 with both requires gone, and its second control answered 7 with the require AND the annotation gone. If a row asserts through a call, the spec op must be NULLARY.

CONTROLS: two roots that are DELTA-EQUAL (a let-alias, path-dependent-types.md sec 4.1's zeta arm) must share ONE dictionary -- that is the other half of the rule and nothing else in the corpus pins it; the single-require clause answers exactly as today (passes either way by design, say so at the site); and a projection whose root is not a head binding of this clause is REFUSED with its own message. Say which rows fail per back-out, run each back-out with a patch that ASSERTS it applied (a back-out that silently no-opes reports 'no failures', which is indistinguishable from a control that measures nothing -- that happened in S4). cargo-test green via scripts/test.sh.

## Changes

### 2026-09-09T19:41:04Z — feedback — user

GATE (2) DELIVERED — the OPERATION requirement channel. Gate (1) and the ATTRIBUTION are not; the ticket stays claimed and its acceptance stands verbatim. Commits f23bf8a4 + cc0dbf2b; 10 rows in wi_s8cbv_projection_requirement_test.rs; full workspace 6756 passed, 0 failed.

WHAT WORKS NOW. `operation pick(x: Box) requires Desc[T = x.E]` resolves AND DRIVES: one declaration answers 7 at a Box of Red and 9 at a Box of Blue. Forwarding works too — `operation outer(b: Box) requires Desc[T = b.E] = pick(b)` answers 7 — and a caller that declares NOTHING, or declares the requirement at a DIFFERENT receiver, is refused at LOAD. Surface rule: kernel-language.md sec 5.2. Mechanism and traps: requirement-channel.md sec 10 item 4 (now PARTLY DELIVERED) and 060-implementation sec 8.5.

THE FIXTURE DISCIPLINE THIS TICKET DEMANDED, applied. The spec op is NULLARY and BODY-LESS: with a default body every row answered the default and the file would have measured that instead of the channel. Two controls bound the acceptance — a CONSTANT bracket answers 7 and 7 where the projection answers 7 and 9, and with no `requires` both calls answer nothing. Seven back-outs, each patched by a script that ASSERTS it applied: rung 7, ExprCarried stop 6, bridge δ 3, call-site δ 3, caller_covers 2, exact comparison 1, bridge guard 2 (panics).

FOUR THINGS A NEXT ATTEMPT SHOULD NOT RE-DERIVE.

(1) THE SURFACE HALF IS IN THE LOADER, NOT THE TYPER. A requires bracket binding reaches the converter as ONE dotted symbol on the ordinary TERM walk. Loader::try_contract_projection adds the rung, asking the type ladder's own classifier (try_expr_carried_projection_segments) with the WI-428 rigid route DISABLED — that route mints terms and pushes load errors, so admitting it would break the rung's own additivity argument.

(2) WI-552's wrap_places_as_var_ref MUST NOT DESCEND INTO AN ExprCarried. A projection is a TYPE; rewriting its receiver to a var_ref gave one projection two spellings and the requires copy had no reader (the eliminator keys on a Ref). This cost hours to find and is invisible from the surface.

(3) δ RUNS BEFORE σ, at BOTH readers. `x` is a parameter, not a type variable, so substitute_spec_via_subst walks straight past the projection. The two readers are resolve_bridge_requirements (a rule body calling an operation) and build_op_scoped_dicts (a typed call site).

(4) TWO BRANCHES WERE BUILT AND REMOVED FOR THE SAME REASON — a back-out failing ZERO rows. The δ in build_op_scoped_dicts as a SUPPLIER (the bridge answers first on every reachable path; it survives only because the refusal needs its verdict), and WI-459's arg_syms re-key map (δ grounds before the neutral arm, because the argument's own type already carries the caller's projection). Do not re-add either without a row that moves.

WHAT REMAINS — the ticket's own acceptance, unchanged.

  GATE (1): the rule-body `?d = require[Desc[T = p.E]]` bracket is STILL REFUSED, by S1's drop rule ('p.E names neither a sort nor one of Desc's own type parameters'). Its owner is require_spec_binding_occurrence / report_dropped_spec_binding in load.rs — the drop rule must learn that a projection is a THIRD kind beside 'a sort' and 'the spec's own parameter name'. Row D of the ticket's own six is exactly this, and it still reproduces.

  THE ATTRIBUTION: keying which dictionary a require means on the projection ROOT, which REMOVES S4's sort-matching selector, narrows the >1-anchor refusal and lifts the ANCHORED gate. Untouched. Note the operation half needed no selector at all — a requires names one dictionary per clause — so nothing delivered here bears on it.

ONE MEASUREMENT THAT MAY HELP THE ATTRIBUTION: views_structurally_equal over the δ-eliminated spec is what separates two receivers at the operation level (b.E vs c.E), and it is the same comparison the root attribution needs. It is NOT unify_types' ζ arm — the receivers here are already caller-keyed by δ, so a structural compare suffices. Whether the rule-head case can rely on that too is the first thing to measure, not to assume.

