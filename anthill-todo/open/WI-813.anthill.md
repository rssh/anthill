```anthill
fact WorkItem(id: "WI-813", created: "2026-07-22T10:11:51Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Open)
```

## description

CLI anthill.geometry emit swallows real codegen errors via .ok()/if let Ok -- distinguish empty-namespace from real failure. run_codegen_cpp (anthill-cli/src/main.rs:985, 'if let Ok(geometry_header) = emit_namespace_header_with_profile(kb, anthill.geometry, profile)') and run_codegen_cpp_project (main.rs:1049, '.ok()') treat the OPTIONAL anthill.geometry header as best-effort and swallow ALL errors so an absent/empty geometry namespace is skipped. But emit_namespace_header_in OVERLOADS Err for TWO cases: (a) 'no entities/sums/traits/consts to emit under namespace' (the benign case .ok() intends) AND (b) a REAL codegen failure -- an unlowerable type, and post-WI-810 a bodied TypeMapping/IncludeMapping/NamingConvention refusal via CppCodegenError. The .ok() cannot distinguish, so a real geometry codegen error is silently dropped (missing anthill_geometry.hpp + exit 0), violating the repo 'loud error over silent skip' rule. PRE-EXISTING (already swallowed unlowerable-type errors); WI-810 WIDENS it by one kind (a bodied-rule refusal that used to be an assert-abort/panic -- unswallowable -- now a graceful Err). NARROW reachability: the main-namespace emit runs first and the blanket read_facts(Refuse) usually fails there first (IncludeMapping/TypeMapping read unconditionally), shadowing the geometry slip; the live window is a bodied NamingConvention reached ONLY via geometry body-synthesis. FIX: distinguish 'empty namespace' (benign skip) from 'real error' (surface loudly, exit 1) -- e.g. give emit_namespace_header an Ok(Option<String>) empty-vs-content signal, or a CppCodegenError kind/predicate the CLI matches; then the two geometry sites propagate real errors and swallow only the empty case. Found by WI-810 /code-review.

