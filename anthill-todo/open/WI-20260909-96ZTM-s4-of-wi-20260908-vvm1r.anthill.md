## Attributes

- id: WI-20260909-96ZTM-s4-of-wi-20260908-vvm1r
- created: 2026-09-09T05:59:56Z

- status: Open
- status_agent: user
- status_at: 2026-09-09T13:06:27Z

- acceptance: cargo-test, scaland-sbt-test

- depends_on: WI-20260909-QMFC5-s2-of-wi-20260908-vvm1r

- tags: vvm1r

## Description

S4 of WI-20260908-VVM1R (umbrella): TWO ANCHORS ARE TWO DICTIONARIES -- the implicit-parameter reading -- and the CARRIER-DIRECTED, ACCUMULATING weave that makes it sound. Design: docs/design/060-implementation.md sec 8.5.

THE MODEL (settled with the user 2026-09-09, and it REPLACES an earlier proposal to refuse this case). The annotated head variables ARE the implicit parameters: one dictionary per (spec, anchor), each bound to its own variable, threaded into the calls that need THAT carrier. This is requirement-channel.md's own sentence -- 'the call site drives it; require[X] is only the explicit form', which 'simply pre-binds one of those variables' -- with the anchor as the slot source. So a clause with two typed head vars both providing one spec is NOT refused (S2 refuses it as an interim); only attributing a WRITTEN require between them needs the retained bracket, which is why this depends on S1.

THE WEAVE IS SINGLE-DICTIONARY BY CONSTRUCTION, NOT BY DESIGN, AND THAT IS THE WORK. weave_covered_call (typing.rs:71084) emits requirements: vec![Rc::clone(out)] -- ONE element, REPLACING the list -- and finds its target by Rc::ptr_eq. Two dictionaries never collide today only because collect_covered_calls filters on spec_op_parent_sort == spec_canon, so a call node is covered by at most one spec, and same-spec duplicates are refused upstream by has_duplicate_spec (the 'at most one requires on spec X per rule' error, whose stale WI-613 citation S1 retires). Under this model both consequences bite: (a) the weave must be CARRIER-DIRECTED -- pair each covered call with the dictionary whose ANCHOR matches that call's carrier ARGUMENT, not weave every covered call with the goal's out; (b) a second weave over an already-rebuilt node cannot find its target by Rc::ptr_eq, so it silently does not weave and trips the debug_assert!(wove, ...) at typing.rs:71207. ONE ACCUMULATING PASS, not two replacing ones. apply_within itself needs nothing -- requirements is already a list.

WHAT GOES WRONG IF (a) IS SKIPPED, and it is the silent class: with two Desc dictionaries and an undirected weave, a Colour-carried call gets the Shape dictionary and answers through the wrong instance. A clean load, a definite answer, the wrong one.

ACCEPTANCE. A clause with TWO typed head vars whose bounds both provide one spec loads; each dictionary binds separately; each covered call dispatches through the one matching ITS OWN carrier, asserted BY VALUE with the two suppliers answering different numbers so neither can be produced by accident. A WRITTEN require[Spec[P = <one of the two bounds>]] pre-binds the matching one (this is the row that needs S1's retained bracket, and it is what says the attribution is READ and not positional). CONTROLS: the single-anchor clause answers exactly as S2 left it (passes either way by design -- say so at the site); with the carrier-direction backed out, name the row that answers the OTHER supplier's value rather than merely failing; with the accumulation backed out, name the row that trips the debug_assert. cargo-test green via scripts/test.sh.

## Changes

### 2026-09-09T10:55:00Z — feedback — claude

UNBLOCKED: S2 (QMFC5) is Delivered. What S2 measured that changes this ticket -- read before designing.

(1) THE SCOPE GREW BY ONE SHAPE, and it is the most ordinary one. S2's first review round found rule p(?x: Leaf, ?y: Leaf) refused as "two dictionaries" and I admitted it by COLLAPSING two anchors of one DATA sort -- nothing can widen to a constructor-declaring sort, so both carriers ARE that sort at run time. The second round drove three counterexamples and the collapse was REMOVED, so that plain spelling is refused again and this ticket owns admitting it. Do not re-derive the collapse: it is measured wrong, three ways.

(2) NO LOAD-TIME COLLAPSE IS SOUND, and this is the design constraint the ticket did not have. (a) A PARAMETERIZED data sort collapses on the head symbol alone -- sort_functor_of_view discards the arguments -- so ?x: Box[E = Leaf], ?y: Box[E = Other] merged, and the conditional provision's sub-dictionary differs; that shape SHIPS in pair, list and option. (b) Even two BARE, BYTE-IDENTICAL bounds diverge under a conditional provision (Wrap provides Sh[T = Wrap] :- Sh[A]): measured ?r = 7 one way and a residual the other, decided by nothing but which head variable was written FIRST. So comparing the full bound TERMS does not rescue a collapse either. The predicate a collapse needs is "the carriers are equal AT RUN TIME", which a load-time bound cannot answer for a conditionally-providing sort. ONE GOAL PER ANCHOR is not one option among several -- it is the only correct route, which is what this ticket already proposes.

(3) THE ARGUMENT FROM "NOTHING WIDENS TO A DATA SORT" IS TRUE ABOUT THE SORT HEAD AND SAYS NOTHING ABOUT ITS TYPE ARGUMENTS. It appears in this ticket's neighbourhood and in 060-implementation sec 8.4. It justified TWO wrong mechanisms in S2 (the collapse, and a load-time check tier). Treat it as covering the head only.

(4) THIS TICKET NOW OWNS A SECOND REFUSAL TOO. "at most one requires on spec X per rule" (record_find_dictionary_grounding's has_duplicate_spec) is the same lift for the same reason -- two requires on one spec are two dictionaries. Its diagnostic string cited "Tier B", a delivered ticket's tier; S2 changed both the comment and the string to name 96ZTM. So the acceptance has a second surface: two requires on one spec base, each attributed by its retained bracket.

(5) THE CHECK TIER IS NOT A LOAD-TIME VERDICT any more, which changes what "each covered call dispatches through the one matching ITS OWN carrier" has to cover: requires(X) emits the SAME goal as require[X] with out: absent, so the carrier-directed weave must be correct for a clause carrying both spellings. See 060-implementation sec 8.8.

(6) COLLECT_COVERED_CALLS IS CARRIER-BLIND TODAY and that is recorded at its site as an OPEN axis, not a settled one: it filters on the functor's parent spec, arity and classified_apply_target, never on which carrier the call names. /code-review could not drive it to a wrong VALUE under S2 (the anchor path is only reachable when no witness call exists, and the shapes left name no carrier to differ on), so it is stated rather than claimed. Under this ticket's model it becomes drivable -- that is consequence (a) in the description above, and the row that measures it is the one to write first.

(7) THE >1 REFUSAL'S OWN MESSAGE names this ticket, and wi_qmfc5...::two_head_variables_of_one_data_sort_are_still_two_dictionaries plus ...::a_parameterized_data_sort_at_two_instantiations_is_refused are the two rows that must FLIP when this lands. They are written as refusal assertions today; converting them to value assertions is part of the acceptance.

### 2026-09-09T13:05:00Z — feedback — claude

ATTEMPTED AND BACKED OUT. The full attempt is kept at scratchpad/s4-full-attempt.diff (1034 lines); nothing of it is in the tree. It reached green -- 6740 passed, 0 failed -- and /code-review then found 15 findings plus a 3-finding sweep, including TWO REGRESSIONS and an acceptance row that measures nothing. Reverted rather than patched: the defects are in the DESIGN, not in the edits, and two prerequisites have to land first.

WHAT WAS BUILT AND WORKED, so it is not re-derived: witness CLAIMING (each require takes a witness no earlier one took), a CARRIER-DIRECTED weave keyed on call_carrier_var, an ACCUMULATING single-pass weave_all_covered_calls, an equal-requires refusal, and the >1-anchor lift for concrete bounds. Measured 7 through one dictionary and 9 through the other in one clause, with four back-outs each failing a distinct row.

WHY IT CANNOT SHIP, each measured by /code-review and re-driven by me:

(1) THE MULTI-DICTIONARY CHANNEL HAS NO READER -- and this is the one that decides it. dictionary_dispatch_target (resolve.rs:8329) bails on `let [dict_occ] = requirements else { return None }`, and eval.rs:1539 hard-errors on len > 1 ('apply_within requirements channel has 2 entries; v0 Model 1 expects 0 or 1'). So the accumulating weave produces a shape nothing downstream can consume. My comment 'apply_within needs nothing for this -- requirements is already a list' is FALSE, and the row I wrote to pin the accumulation (a_nullary_call_takes_every_dictionary_of_its_spec) credits its residual to an 'honest ambiguity refusal' when it actually comes from that pre-existing single-element guard. THE READER SIDE IS A PREREQUISITE, not a follow-up.

(2) REGRESSION, CONFIRMED BY ME: claimed_witnesses is one list per RULE keyed on the node alone, not per (rule, spec). So `rule related(?x, ?y) :- requires(PartialEq[T]), requires(Eq[T]), eq(?x, ?y)` -- the canonical typeclass shape, since Eq requires PartialEq and eq is PartialEq's DIRECT witness and Eq's INHERITED one -- LOADS CLEAN at HEAD and is REFUSED with the attempt. The corpus has no two-require clause, which is why the suite stayed green over it.

(3) REGRESSION: weave_all_covered_calls returns at a matched target without recursing into its children, so a covered call NESTED inside another covered call is never woven. Single require, nothing to do with two dictionaries: at HEAD it answers 7, with the attempt it PANICS on the sibling assert (and in release silently folds the spec default). Grouping by target fixed the ancestor-rebuild half and not the early-return half, though my comment claims 'grouping by target first makes both disappear'.

(4) MY HEADLINE ACCEPTANCE ROW MEASURES NOTHING. two_requires_on_one_spec_bind_two_dictionaries answers 7 and 9 with BOTH requires DELETED -- the values come from ordinary value dispatch on leaf()/other(). Confirmed by me. This is the exact trap QMFC5's own ticket documents ('a bodied Desc.describe call is SIMULTANEOUSLY the witness and the covered call, so the obvious fixture passes entirely on the existing witness path'), and I walked into it one stage later. A row that drives attribution must bind ?d1/?d2 into the head and assert the DICTIONARY VALUES while varying the bracket and holding call order fixed.

(5) THE WRITTEN BRACKET IS NEVER READ ON THE WITNESS PATH, which is the ticket's own acceptance ('a written require[Spec[P = ...]] pre-binds the matching one ... the attribution is READ and not positional'). spec_arg is used for the spec BASE, for slot 0 of the emitted goal, and for anchor_grounding -- which runs only after every witness scan MISSES. Requires pair with witnesses by scan ITERATION ORDER, and the two scans walk in OPPOSITE directions (the direct scan is source order; scan_body_dfs is a LIFO stack.pop()). For an op with a DEFAULT BODY the defaulted (LIFO) scan grounds it, so the pairing comes out INVERTED: ?dA = require[Desc[T = A]] measurably holds Other's dictionary, a clean load and a definite wrong answer through the bound variable. Swapping only the two BRACKETS changes nothing; swapping only the two CALLS flips it.

THE TWO PREREQUISITES, and neither is in this ticket's scope as written:
  P1. A READER for a call carrying more than one dictionary -- dictionary_dispatch_target and the eval path both assume 0 or 1.
  P2. rule_type_bounds must keep the head-introduced type VARIABLE, not its bound. Without it there is no map from a bracket's `T = B` back to the head variable B bounds, so the bracket CANNOT attribute and order is all that is left. The require BRACKET half of this was built and worked (blast radius: one row) and is in the saved diff; the BOUND half is untouched.

OTHER FINDINGS WORTH KEEPING, all measured: spec_instances_equal is head-only and SHORT-name, so Box[E = Leaf] and Box[E = Other] compare EQUAL and a valid program is refused (views_structurally_equal is the codebase's route); two unequal-but-same-instance requires (require[Desc] plus require[Desc[T = A]]) are admitted and answer a silent residual where HEAD refused loudly; an UNDIRECTED require (a literal carrier -- the commonest witness spelling, so 'a nullary op is the only undirected shape' is wrong) is threaded into every call and breaks a working one; a covered call matching no dictionary is silently DROPPED from the weave, which NARROWS the single-require weave that previously threaded its one dictionary everywhere; call_carrier_var reads only the FIRST carrier param where simp_guard_holds_core loops over all of them (PartialEq.eq, Ord, Magma.op2 are all multi-carrier); an APPLIED tvar (require[Desc[T = A[Bogus = Int64]]]) loads clean with its arguments discarded, bypassing 51W18's check_sort_type_args; a CHECK-tier requires claims a witness though it threads nothing; and the >1-anchor lift silently drops the ZERO-match case, leaving a typo blamed on the anchor count.

STATUS: back to Open. The ticket text stands; this note is what a next attempt should read first.
