```anthill
fact WorkItem(id: "WI-817", created: "2026-07-22T14:50:20Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-821", "WI-822", "WI-823", "WI-648"]), generates: none, requires_capability: none, status: Open)

fact Feedback(workitem: "WI-817", author: "user", at: "2026-07-23T09:26:58Z")

fact Feedback(workitem: "WI-817", author: "user", at: "2026-07-23T09:27:34Z")

fact Feedback(workitem: "WI-817", author: "user", at: "2026-07-23T09:35:06Z")

fact Feedback(workitem: "WI-817", author: "user", at: "2026-07-23T09:55:03Z")

fact Feedback(workitem: "WI-817", author: "user", at: "2026-07-23T10:01:58Z")

fact Feedback(workitem: "WI-817", author: "user", at: "2026-07-23T10:07:49Z")

fact Feedback(workitem: "WI-817", author: "user", at: "2026-08-16T21:27:54Z")
```

## description

CONSTRUCT the lambda-application case whose requirement CANNOT be fixed at construction, record what actually happens, THEN define the rules for handling it. CONSTRUCT FIRST -- the design decision is downstream of a witness, not the other way round. CONTEXT (from WI-816 feedback). Requirement analysis is UNIFORM: for every operation the typer derives the set of used requires from the body, and a LAMBDA BODY IS NO DIFFERENT (`for_each_child` descends `Expr::Lambda`, so `req_insertion::collect_classified` sees nested spec-op calls; MEASURED -- an uncovered call inside a lambda raises MissingRequiresForSpecOp with the span on the NESTED call, two binders deep, and does not over-fire). The asymmetry is BINDING TIME: an OPERATION has its requirements supplied BY THE CALLER PER CALL (operation-call-model.md:805, `callee.frame.requirements = caller.apply_within.requirements`), while a LAMBDA has them supplied BY ITS CREATOR ONCE (`reduce_lambda`, eval/eval.rs, snapshots `frame.requirements` into `Closure.requirements`; the doc calls this "the only place the uniform rule is overridden"). THE PREDICTION THIS TICKET MUST TEST, stated as a prediction because IT HAS NOT BEEN MEASURED: mutual recursion in which a requirement CHANGES TO THE TYPE-ARGUMENT TYPE needs a DIFFERENT instance at each step; per-call resolution can deliver that and resolve-once cannot; therefore the case should be EXPRESSIBLE AS AN OPERATION and INEXPRESSIBLE AS A LAMBDA. Do not build the fix on this sentence -- build the program and see. DELIVERABLE 1, THE WITNESS. Construct the minimal program: two mutually recursive operations where a call passes a DIFFERENT type argument so the required instance differs per step, with one leg routed THROUGH A LAMBDA so the requirement must ride the closure. Record VERBATIM which of these occurs: (a) clean load and correct evaluation -- the prediction is FALSE and the ticket says so and stops; (b) a LOAD error, with the exact variant and the span it lands on; (c) an EVAL error, with the exact variant; (d) a SILENTLY WRONG answer, which is the worst case and must be called out as such. COMMIT THE OPERATION-ONLY CONTROL BESIDE IT -- the same mutual recursion with no lambda. The whole claim is that the operation form works where the lambda form does not; IF THE CONTROL ALSO FAILS THE DIAGNOSIS IS WRONG and the ticket must report that instead of proceeding. HARNESS TRAPS, both already paid for once: `anthill run` MUTES load errors (exit code is not the loader's verdict) -- call `load_all` directly as the existing tests do; and after ANY trapped call every later `interp.call` on the SAME interpreter returns a bogus Internal error -- use a FRESH `interp_for` per evaluated case. A POSITIVE CONTROL is required: deliberately break something and confirm the harness actually reports it. PRE-EXISTING HAZARD THAT MAY MASK THE RESULT, unresolved: an agent reported that a sort-level `requires` makes ANY of that sort's operations untrappable at eval (`DeferToRequirement: projection index 0 out of range (requirement '__req_eq' bundles 0 sub-requirement(s))`) with no lambda in the program; a hand reproduction on a differently-shaped program produced a DIFFERENT error (`UnknownOperation`). NEITHER is established. If the witness trips either, ESTABLISH WHICH IS THE REAL BEHAVIOUR OF THE `requires` EVAL PATH FIRST -- a broken requires-eval path would make the lambda result uninterpretable, and would itself be the more serious bug. DELIVERABLE 2, THE RULES. With the witness in hand, state what SHOULD happen, with the reason. Candidates, none assumed: (1) REJECT -- keep requirements fully compile-time-resolvable and diagnose the varying-instance recursion as unresolvable, naming the requirement that cannot be pinned (what a monomorphising compiler does); (2) DEFER -- accept `DeferToRequirement` as the general case, the closure carrying its DERIVED requirement set and the dictionary being supplied per invocation, which is exactly what `lambda_within` would be the IR of; (3) some third rule the witness suggests. The USER'S LEADING HYPOTHESIS IS (2), DICTIONARY PASSING -- recorded as the hypothesis to beat, not as the answer. NOTE for (2): today's snapshot is also IMPRECISE, copying `frame.requirements` WHOLESALE rather than the set the body uses -- harmless for execution (the body reads by name via `var_ref(__req_*)`) but it is why anthill-cpp-gen REJECTS the variant, having no static record of which dictionaries a closure needs. WHATEVER IS CHOSEN, LEXICAL CAPTURE ITSELF MUST STAY for the monomorphic case: the body is type-checked in the CREATION scope, so running it against a callee's dictionaries would execute code against dictionaries it was never checked against (MEASURED -- a closure built under one spec evaluates correctly when invoked by a callee that cannot even NAME its spec op). RELATION TO WI-816: that ticket asks delete-vs-implement for the `*Within` IR variants; THIS ticket supplies the witness that decides it. If the constructed case needs dictionary passing, WI-816 option (b) wins and `lambda_within` is its IR; if the case can be cleanly rejected, option (a) wins and the variants go. ACCEPTANCE: the witness program AND its operation-only control are committed as tests pinning whatever behaviour is observed today (a test that pins a CURRENT DEFECT is fine and is the point -- name it as such so it is not mistaken for desired behaviour); the observed outcome is recorded verbatim in the ticket; the `requires`-eval-path question above is settled or explicitly excluded with a reason; the rules are stated with their reason; if (2) is chosen the design says WHERE the derived set is computed, HOW it rides the closure, and HOW it is supplied at invocation, and says what becomes of `Closure.requirements`. Full workspace green via rustland/scripts/test.sh.

## Feedback

### 2026-07-23T09:26:58Z — user

DELIVERABLE 1, THE WITNESS — CONSTRUCTED, MEASURED, COMMITTED (tests/include/wi817_polyrec_requirement_test.rs, 10 tests, all pinning observed behaviour; positive controls prove both the load and eval harnesses report breakage). Program family: spec Desc { describe(x:T)->Int64 }, base instance fact Desc[T=Leaf] (describe=1), CONDITIONAL instance sort WrapDesc { sort E=?; requires Desc[T=E]; fact Desc[T=Wrap[A=E]] } (describe = 10*describe(inner)+2, so correct values are depth-coded: drive(n) = 1, 12, 122, 1222). Mutual recursion f<->g where g calls f at the CHANGED type Wrap[.]; control = direct call; witness = the g->f leg inside  passed to a requirement-free applier that cannot name Desc. VERDICT: THE PREDICTION IS NOT OBSERVABLE TODAY — THE OPERATION-ONLY CONTROL FAILS IN EVERY EXPRESSIBLE SPELLING, AND THE LAMBDA WITNESS NEVER FAILS DIFFERENTLY FROM ITS CONTROL. Measured matrix, outcomes verbatim: (ROW 1, op-scoped requires over the op's OWN [T] — proposal-042 explicit type args, requires Desc[FT]) outcome (b) LOAD ERROR for control AND witness at the SAME site (f's covered describe call, NOT the recursion): '31:24: type mismatch in wi817.control.Desc.describe.dispatch: expected matching impl for per-call bindings, got no impl matches'; both binding spellings Desc[PT] / Desc[T=PT] fail identically even with NO recursion — op_requires_covers_call does not see op-level params at all (spec §5.4 says they are legal in requires positions; GAP). (ROW 2, op-scoped over a SORT param, cross-sort mutual recursion, per-call binding via argument inference — wi418's channel) outcome (c) EVAL ERROR, control AND witness identical: drive(0)=Ok(Int(1)), drive(>=1)=Err(Internal("DeferToRequirement: requirement param `__req_desc` not bound in caller frame")); the MINIMAL repro needs NO recursion and NO lambda — ONE conditional level probe(wrap(leaf())) already dies the same way. (ROW 3, SORT-level requires) the 1-conditional-level case WORKS CORRECTLY — Ok(Int(12)) — but the recursion is outcome (d) SILENTLY WRONG, control AND witness identical: drive(0..2) = Ok(Int(1)) at EVERY depth (correct: 1, 12, 122); the caller's Desc[GT] dictionary is forwarded UNCHANGED into f's Desc[FT:=Wrap[GT]] slot, the conditional instance is NEVER consulted, and the Leaf impl runs on a wrapped value. MECHANISM (verified in source, kb/typing.rs build_dep_projection): Strategy 1's entries_cover is WILDCARD-TOLERANT by design (WI-419 comment: 'a caller Eq[A] covers a dep Eq[B] whenever either element is a type param') and the WI-419 sigma-class check only DISAMBIGUATES 2+ covering entries — a SOLE covering wildcard entry blindly forwards var_ref(__req_desc) without consulting the call-site substitution (FT:=Wrap[GT]); Strategy 3 (SLD construction of the Conditional tree via resolve_inner + emit_tree_as_projection — the machinery wi224 tests at the resolver layer and V8 proves correct END-TO-END) is SHADOWED by that early return. Row 2's loud twin: an op-scoped chain has NO call-site supply channel at all (ConcreteApplyWithin gates on the callee's PARENT SORT chain) and value-directed dispatch pushes the impl frame WITHOUT the impl's own requires, so the read is unbound instead of wrong. THE REQUIRES-EVAL-PATH HAZARD: SETTLED. Sort-level requires does NOT make ops untrappable — V8 runs a conditional dictionary end-to-end correctly; NEITHER reported error spelling ('projection index 0 out of range' / 'UnknownOperation') reproduced anywhere in the family; the real failure modes are the two supply defects above, each now pinned minimally. BONUS HAZARD (pinned as unconditioned_parametric_fact_mispins_abstract_call): removing the conditional's requires leaves fact Desc[T=Wrap[A=E]] with FREE E, and an ABSTRACT covered call is then silently MIS-PINNED to WrapDesc.describe at load (var-var unification; WI-325's protection guards NoCandidates/NoMatch, not a bogus Unique) and dies at eval on field 'inner' — this is also why the first-cut positive control ('remove the requires, expect a load error') was itself refuted by measurement.

### 2026-07-23T09:27:34Z — user

CORRECTION to the preceding feedback, which lost a phrase to shell expansion: the witness sentence should read "witness = the g->f leg inside (lambda w -> FHolder.f(sub(n,1), w)) passed to a requirement-free applier that cannot name Desc". Substance otherwise unchanged. DELIVERABLE 2, THE RULES, from the witness. RULE A (the rule the witness actually dictates, upstream of the lambda question): at a call site supplying a callee requirement, FORWARDING the caller dictionary is sound ONLY when the dep element sigma-resolves to the SAME unification class as the covering entry element; when it sigma-resolves to a COMPOUND OVER the caller element (FT := Wrap[GT]) the dictionary must be CONSTRUCTED per call as the Conditional tree with FromScope leaves — construct_requirement(WrapDesc, [var_ref(__req_desc)]) — machinery that already exists (resolve_inner + emit_tree_as_projection, wi224-tested at the resolver layer) and is proven correct end-to-end by the V8 pin. Concretely: extend the WI-419 sigma-class check in build_dep_projection from TIE-BREAKING (2+ covering entries) to GATING (reject a sole covering wildcard entry whose sigma-resolution disagrees, fall through to Strategy 3). With that, V9/V10 compute 12/122 — per-call dictionary passing serving the unbounded instance family, with each call site still emitting ONE finite compile-time construction; the unboundedness lives only in runtime tree depth. This is hypothesis (2) DEFER in the ticket taxonomy, and it needs NO new IR at the call site. RULE A-2 (op-scoped parity): the op-scoped channel needs (i) a call-site supply channel at all — ConcreteApplyWithin gates on the callee PARENT SORT chain today, so an op-scoped chain gets nothing and dies loud, (ii) impl-frame seeding on value-directed dispatch — the dispatcher must resolve and install the impl own requires at the value concrete type when pushing the impl frame (the V6 failing read), and (iii) op_requires_covers_call must see op-level type params (ROW 1 load rejection; spec par 5.4 declares them legal in requires positions). CANDIDATE (1) REJECT is REFUTED as the kernel rule: V8 proves per-call construction of a changed-type requirement is already wanted and working one level deep; rejecting varying-instance recursion would either also reject V8-shaped programs or draw an unprincipled line between them. A monomorphising BACKEND may still reject residual DeferToRequirement — a backend property, not the kernel rule (matches the anthill-cpp-gen stance recorded in WI-816). ON THE LAMBDA PREDICTION: the predicted operation-vs-lambda asymmetry DID NOT MATERIALIZE and cannot be measured until Rule A lands — both legs fail identically at the shared call-site defect, before any closure-snapshot semantics can bite. The witness also sharpens the prediction itself: the lambda that would NEED per-invocation instances (created ONCE, invoked at MANY types) is not WRITABLE — a closure arrow type is monomorphic, so every writable lambda is created at a FIXED instantiation, where creation-scope capture is semantically CORRECT; the honest lambda rendering (a FRESH closure per recursion step) failed only because its creation scope already held the wrong/missing dictionary — the control defect, not a closure defect. The closure channels themselves behaved correctly in every case that reached them (the V7 lambda leg executes and the failure surfaces later, in the impl frame). CONSEQUENCE FOR WI-816 (recorded as evidence, decision stays there): this witness produces NO case where lambda_within (per-invocation requirement supply to closures) is needed — Closure.requirements creation-scope capture plus a FIXED call-site supply plausibly serves the polymorphic recursion; REMEASURE the committed witness after Rule A lands (the d-row pins must flip to 12/122, the c-row pins to values), and only then settle delete-vs-implement; the one argument left for implement is codegen static visibility of the derived set, which this witness neither supports nor refutes. WHAT BECOMES OF Closure.requirements under Rule A: unchanged — it keeps snapshotting the creation frame; the fix makes the snapshot CORRECT by fixing what the creation frame holds. FOLLOW-UP WORK, recorded here per the no-ticket-spinoff feedback rule, for the user to file or fold: (F1) the Rule A sigma-gate in build_dep_projection Strategy 1; (F2) op-scoped supply channel + value-directed impl-frame seeding + op-param coverage, items i-iii above; (F3) the unconditioned-parametric-fact mis-pin (V5 pin) — an abstract covered call silently pinned via var-var unification, sound behaviour is a load rejection. ACCEPTANCE state: witness + control committed as 10 pinned tests (wi817_polyrec_requirement_test.rs) including positive controls for both harness verdicts; outcomes recorded verbatim; requires-eval-path question SETTLED (V8 correct end-to-end, neither reported spelling reproduces); rules stated with reasons above; full workspace suite run pending at the time of this note, result recorded in the delivery.

### 2026-07-23T09:35:06Z — user

ADDENDUM, MULTI-HOP MEASUREMENT (user question: does a lambda passed operation-to-operation, each hop holding a DIFFERENT operation-level requires dictionary, survive?). New pinned test lambda_relay_chain_closure_correct_but_hop_dicts_forwarded: closure created under Desc[Leaf] (describe=1), relayed through Relay and Invoker which each hold their own CONCRETE Desc[Pebble] (describe=5), invoked two frames from creation. ALL bindings concrete — isolates dictionary FLOW from the changed-type recursion. Value coding: correct = 551 (fn(0)=1 creation dict + 10*5 + 100*5 hop dicts); hop-dict leak INTO the closure would read 555. MEASURED TODAY = 111 = 1 + 10*1 + 100*1: the CLOSURE HALF IS CORRECT — fn(0)=1, creation-scope capture holds across two foreign frames — but each hop OWN describe read 1 not 5: the Maker Leaf dictionary was FORWARDED into Relay and Invoker frames OVER each call site own concrete Pebble resolution. This BROADENS the Rule A finding: the Strategy-1 sole-wildcard-entry blind forward is wrong even for ALL-CONCRETE deps whenever the caller holds a same-spec wildcard requires — not only for changed-type recursion. The V8 pin works only because its driver holds NO requires (no covering entry, Strategy 3 reached). wi419 measured the 2-covering-entries disambiguation; the SOLE-entry different-instantiation forward was unmeasured and is wrong. Consequence: across EVERY measurement in this ticket the lambda machinery has not failed once; the operation-side call-site supply has failed everywhere it was asked to change instantiation. Suite: 11 tests, all green.

### 2026-07-23T09:55:03Z — user

REOPENED behind WI-821 (user decision): the witness and its measurements stand delivered in substance (tests committed at 8d3ec77a, outcomes and rules recorded above), but the ticket's ORIGINAL question — is the case expressible as an operation and inexpressible as a lambda — is answerable only after the call-site supply defect is fixed. REMAINING DELIVERABLE, blocked on WI-821: REMEASURE the committed witness once the sigma-gate lands — the (d) pins flip to 1/12/122 (control AND lambda witness) and the relay pin to 551; then state the prediction's final verdict from the remeasured pair (if the lambda witness computes the same correct values as the control, the prediction is FALSE for every writable lambda and WI-816 option (a) gains its evidence; if the lambda leg diverges once the shared defect is gone, THAT divergence is the real witness). The op-scoped (c) and op-param (b) rows stay pinned and are NOT gated on WI-821 — they flip under their own future tickets.

### 2026-07-23T10:01:58Z — user

SHARPENED QUESTION (user framing, confirmed by a new measurement): the sort-based mechanism works by passing a dictionary AS IS; the searched cases are those where passing-as-is cannot be right because the OP-SCOPED requirement set CHANGES when passing to another operation. Accordingly the dependency chain now runs WI-821 (sigma-gate) -> WI-822 (op-scoped supply) -> WI-823 (op-param coverage) -> THIS ticket: the remeasure that answers the prediction runs on the OP-SCOPED channel, in both its sort-param and explicit op-param spellings; the sort-level remeasure alone is not sufficient. NEW MEASUREMENT completing the picture (pinned as op_scoped_relay_chain_correct_via_value_direction): the same relay chain that the SORT-level channel computes WRONG (111 — hops receive the maker dictionary as-is) computes the CORRECT 551 TODAY when its requirements are OP-SCOPED — not because op-scoped supply works (it supplies nothing) but because every describe resolves VALUE-DIRECTED: the runtime value itself selects Leaf/Pebble.describe and no dictionary is consulted, so the per-hop changed instantiation is served by the values. THE TWO CHANNELS FAIL ON COMPLEMENTARY SHAPES: dict-directed pass-as-is is wrong the moment the instantiation changes (sort-level 111, recursion 1/1/1); value-directed no-supply is right until a dictionary is semantically REQUIRED — a conditional impl whose body needs its own chain — where it dies unbound (op-scoped one-level and recursion pins). NEITHER channel serves the searched case (changed requirement AND dictionary genuinely needed), which is exactly what WI-821+WI-822 together must produce. Constraint recorded for WI-822: its supply channel must KEEP the value-directed 551 green — supplying real dictionaries may change the mechanism but not the value.

### 2026-07-23T10:07:49Z — user

TWO FURTHER MEASUREMENTS from user-directed probes, both pinned. (1) TWO DESCRIBERS FOR ONE CARRIER (the precondition for measuring provider SELECTION — with one describer the system just discovers the sole candidate and pins it, testing nothing): REJECTED AT LOAD — DispatchAmbiguous at BOTH describe sites (multiple impls match, coherence rule) plus the global check (ambiguous witness: 2 distinct witness sorts provide wi817.tds.Desc for carrier wi817.tds.Pebble, keep exactly one). This PINS A SPEC-VS-IMPLEMENTATION DIVERGENCE: kernel-language par. Instance-coherence specifies SCOPED selection (different scopes may resolve the same Spec[carrier] to different providers, the per-import choice); the implementation enforces GLOBAL one-provider-per-carrier. Consequence: the provider-selection dimension of this ticket's question (same carrier, requirement decides the impl) is UNCONSTRUCTIBLE today; dictionaries can vary only along the TYPE dimension (conditional instances — the committed recursion pins). The fork (implement scoped selection vs amend the spec to global coherence) is a language-design decision for the user; pinned as two_describers_for_one_carrier_rejected_globally, which flips to 75 if scoped selection is implemented. (2) DIFFERENT REQUIREMENT SETS PER OPERATION — the essence-of-bug measurement (user framing: all earlier fixtures gave every op the SAME singleton set, making as-is inheritance and correct per-callee supply indistinguishable; the essence is that the sets DIFFER and a hand-off must REBUILD the callee set): caller a requires (Desc[AT]) calls b requires (Desc[BT], Tagd[BT]) at concrete BT := Pebble while a holds AT := Leaf. MEASURED 31001 vs correct 35001, decomposing as: the OVERLAPPING dep is WRONG (a's Leaf dictionary wildcard-forwarded into b's Desc slot — describe(pebble) runs the Leaf impl) while the DISJOINT dep is CORRECT in the SAME call (no covering entry, Strategy 3 statically constructs the PebbleTag dictionary, tag = 3). The callee set is rebuilt exactly where the caller set does NOT overlap and inherited as-is exactly where it DOES — live proof that WI-821's fall-through construction already works and its gate only needs to stop the overlap-forward. Pinned as different_sets_overlapping_dep_forwarded_disjoint_dep_constructed; flips to 35001 under WI-821.

### 2026-08-16T21:27:54Z — user

CROSS-REFERENCE: see WI-1119, measured under WI-823 (2026-08-16). The DOT spelling and the NAMED spelling disagree on a `requires`-constrained TYPE-PARAMETER receiver — `x.describe()` is refused at load where `Desc.describe(x)` of the same program loads and evaluates. Recorded here first, then moved to its own ticket at the user's direction; WI-1119 carries the full measurement. Relevant to this ticket because it is the one spelling of the witness family that still does not load, but it is NOT a dependency: the two are independent, it is unchanged by backing WI-943 out, and it fails identically for all three `requires` spellings (op-param, op-scoped sort-param, sort-level), so it is neither WI-823's op-param gap nor the requirement-supply question this ticket tracks.

