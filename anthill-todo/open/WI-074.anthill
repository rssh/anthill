fact WorkItem(id: "WI-074", description: some(value: "Reflect operations parity test: ensure sorts/operations/rules in anthill-stl/src/reflect/bridge.rs return the same results as the equivalent execute(LogicalQuery) path (per proposal 026.1 §Q3). Add integration tests that build a LogicalQuery via pattern_query/conjunction/sort_query and compare results to the direct Rust impl. No rewrite needed - the Rust impls exist, only equivalence verification. See docs/status/wi-047.md."), context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Open)
