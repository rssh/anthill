## Attributes

- id: WI-20260908-NARC7-port-wi-20260822-j38je-to
- created: 2026-09-08T11:16:17Z

- status: Open
- status_agent: claude
- status_at: 2026-09-08T11:16:17Z

- acceptance: scaland-sbt-test

## Description

PORT WI-20260822-J38JE TO SCALAND — a boolean constant in GOAL position is a SEARCH.

J38JE is marked Delivered with acceptance `cargo-test, scaland-sbt-test`, and its closing
note says "ITEMS 1 AND 4 ARE DELIVERED; THE TICKET'S SCOPE IS CLOSED" with nothing said
about scaland. The scaland half was never started. Same shape WI-980 had before
WI-20260821-SBZ2A: rustland delivered, the port carrying the rows.

THE RULE (user decision 2026-08-22, kernel-language.md §5.3): `true` is the goal that
always SUCCEEDS and `false` the goal that always FAILS, at EVERY goal position — the
body's atoms AND the goal slots of the connectives above them, which is where §6.6
already puts the boolean OPERATORS. Item 4: every OTHER constant in goal position
(`:- 42`, `:- "hello"`, `:- 1.5`) is a LOCATED LOAD ERROR — it names no predicate, so the
clause can never fire.

IN THE RESOLVER, NOT THE LOADER, and rustland's note says why: 061's `:- true` strip walks
the body's TOP-LEVEL goal list, so by construction it cannot reach a goal nested under
`not` or `|`. rustland reads it in `SearchStream::step_init` (kb/resolve.rs, arm 3.7):
`true` drops the goal and continues, `false` pops the frame.

THE STRIP STAYS — BOTH READINGS (J38JE item 5, settled by the implementation). Only an
EMPTY body makes `fact H` and `rule H :- true` ONE clause, and `KnowledgeBase.isEquation`
reads body-emptiness. scaland's strip landed with WI-20260821-SBZ2A
(`Loader.isEmptyConjunctionGoal`) and is documented there as a body-SHAPE device, not the
meaning of `true`.

MEASURED IN SCALAND TODAY (RuleHeadDeclarationTest, "J38JE GAP: a boolean constant goal
has no reading below the top level" — a row written to FAIL the day this lands):
    :- true             1   correct, via the strip
    :- false            0   correct BY ACCIDENT — a constant names no name, so it resolves
                            to no clause and no builtin; it does not FAIL, it never
                            becomes a goal, and WI-1034's "names nothing" cannot reach it
    :- not(false)       0   <- logic says 1
    :- base(9) | true   0   <- logic says 1
    :- 42               0, LOADS CLEAN   <- rustland refuses it, located (item 4)

`:- not(true)` already answers 0 here, and that is NOT evidence the reading works — it is
the same accident as `false`. rustland's own delivery note records that no count of
`false` can separate the two readings; `not(true)` / `not(false)` is the pair that does.

ACCEPTANCE: drive every row of the table above through `SearchStream`, at the values LOGIC
gives, not the ones scaland gives today — and update the GAP row rather than deleting it,
so the divergence's disappearance is itself recorded. Item 4's refusal must be LOCATED and
must name the constant. Back-outs: state which rows fall when the resolver arm is removed
and which when the loader strip is (in rustland the strip fells ZERO once the arm exists —
if it fells any here, the arm is incomplete). Keep the `fact H` / `rule H :- true`
one-clause identity as a control. sbt test green.

