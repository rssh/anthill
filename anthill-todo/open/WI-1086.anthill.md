```anthill
fact WorkItem(id: "WI-1086", created: "2026-08-12T11:41:26Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Open)
```

## description

DOCUMENT THE BIDIRECTIONAL INFERENCE ALGORITHM — there is no statement of it anywhere, only three scattered INCREMENT records, two of which are stale.

ASKED BY THE USER (2026-08-12), after WI-1083/WI-1084: "are we have algorithm of bidirectional inference explained shortly? If not - maybe add one to path-dependent-types.md?" The answer is no, and the absence is not cosmetic — it is why two soundness holes stayed invisible.

WHAT EXISTS TODAY, all of it about DELTAS rather than the algorithm:
  * `expansion-during-unification.md` §"Bidirectional inference has two directions" — WI-379 (`argument → expected`, args-before-expected) and WI-427 (`expected → argument`), plus the variance sidestep. Nominates ITSELF as the home ("the bidirectional-inference design itself … lives in expansion-during-unification.md §8").
  * `path-dependent-types.md` §4.1 "Bidirectional flow — the checklist example" — the three-flow acceptance example, cross-referencing the above.
  * `type-parameter-scoping.md` §"directionality" — one line, "bidirectional synthesize / check (WI-379) is a *checking* …".

BOTH ARE STALE, VERIFIED: they say the `expected → argument` half is "not yet built, filed as WI-427" and name an "on-disk anchor `wi427_bidirectional_flow_test`, `#[ignore]`'d until WI-427 lands". WI-427 is DELIVERED; the file exists with three tests and ZERO `#[ignore]`s (`bidirectional_flow_conforms`, `bidirectional_flow_wrong_value_rejected_for_right_reason`, `bidirectional_flow_constructor_field_pins`). So a reader is told the language cannot do something it does, and pointed at a disabled test that is running.

THE ONE PROPERTY THAT MUST BE WRITTEN DOWN, and the reason this is worth a ticket rather than a tidy-up: `expected` IS A SEED, NEVER A CHECK. The channel is `TypeWorkOp::Visit { expected: Option<Value> }` (`push_visit` / `push_visit_no_hint`). Every arm CONSUMES it to decide something — `check_bare_ref` reads an arrow-shaped expectation to choose the eta reading over the zero-arg-call reading, the lambda arms type their binders from it, the Apply arm seeds still-free type params. NO arm verifies the type it produced against it. The Apply arm is explicit: `if let Some(exp) = expected { unify_types(kb, &mut subst, &proj_return_type, &exp); }` with the boolean discarded, and its own comment says "The synthesized `resolved_ret` is then checked against `expected` at the use site". The `Stamp` frame every node routes through afterwards only RECORDS the inferred type.

So "where is the check?" has THREE deferred answers and no single one: an operation-body return check, a `let`-annotation conformance, and — for arguments — `validate_arg_against_param` in the PARENT's Apply frame, which is itself deliberately partial (withholds on a non-ground pair, carves out callables). WI-1083 and WI-1084 are both instances: at `callit(idp)` the expected type IS present at the argument node (the eta branch is gated on it), a checker that checked would have refused there, and instead the comparison was deferred to a relation that could not see it. That is not visible from any one function.

SCOPE. Write a SHORT statement of the algorithm as implemented — the work-list driver and its two modes, where `expected` comes from and where it is `None`, the two directions (WI-379 arg→expected, WI-427 expected→arg) with the variance restriction that keeps the second admissible, the "seed not check" property above, and the enumerated list of sites that DO check. Then fix the two stale passages and re-point their cross-references.

WHERE IT GOES IS THE FIRST DECISION, with the evidence rather than by preference. The user suggested `path-dependent-types.md`; `expansion-during-unification.md` already nominates itself; and the repo's own rule is that the SPEC absorbs a rule while a design doc keeps its increment record (memory: "spec absorbs it, proposal KEEPS its text" — duplication deliberate, cross-link both ways, never delete to dedupe). That argues for the NORMATIVE statement in `docs/kernel-language.md` (§8.1 is where the typing rules already live) with both design docs keeping their increments and cross-linking to it. Decide and record the choice; do not silently move either doc's text.

ACCEPTANCE: a reader can learn, from ONE place, how a type is inferred for an expression and where the obligation is discharged, without reading typing.rs; the "seed, not check" property and the three checking sites are stated explicitly; both stale WI-427 passages are corrected (feature Delivered, anchor present and not ignored) and the anchor's three test names are cited; the increments in `expansion-during-unification.md` / `path-dependent-types.md` / `type-parameter-scoping.md` keep their text and gain cross-links; nothing claims a behaviour that is not driven by a named test. DOC-ONLY — no typer change, so cargo-test green is a non-regression check rather than the acceptance.

REFERENCE: WI-379 (arg→expected, delivered), WI-427 (expected→arg, delivered, with `wi427_bidirectional_flow_test`), WI-270 (the `expected` hint channel), WI-283/284/285 (the Visit/Build work-stack and the Stamp frame), WI-1083 and WI-1084 (the two holes this absence hid), WI-836/WI-385 (the groundness discipline that makes `validate_arg_against_param` partial), `docs/design/expansion-during-unification.md` §8 + §"Bidirectional inference has two directions", `docs/design/path-dependent-types.md` §4.1, `docs/design/type-parameter-scoping.md` §"directionality".

