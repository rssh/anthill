## Attributes

- id: WI-912
- created: 2026-07-31T23:54:11Z

- status: Open

- acceptance: cargo-test

## Description

THE SAME DEFECT CLASS WI-908 FIXED, still live in persistence: `persistence/term_ser.rs::resolve_entity_functor` (~289) reads a functor name from a PERSISTED file (human-authored, KB-external — structurally identical to an extent-owner name) with a hand-rolled ladder: absolute `by_qualified_name` FIRST, then a BARE SHORT-NAME GLOBAL FALLBACK via `name.rsplit('.')` — the WI-476 global scan the shared ladder deliberately deleted. No scope, no head-qualification rung, no visibility gate. Two more spellings in the same file: `resolve_variant_sym` (~553/561 and ~636/641) builds `format!("{}.{}", kb.qualified_name_of(sort), key)` — `dotted_by_head` re-spelled locally — then an absolute rung, then `intern`. That final `intern` is WI-894's hazard: two scopes' same-spelled names collapse onto one bare global. NOT DRIVEN: code-read, raised by /code-review's altitude pass on the WI-908 diff. FIRST STEP: drive it — persist a fact whose functor short name is ambiguous across two namespaces, reload, assert which symbol it lands on. ACCEPTANCE: each of the three reads `KnowledgeBase::resolve_name_in_global` (or states why a persisted name is a different question, at the site); the short-name fallback is gone or justified; cargo-test green via scripts/test.sh.

