## Attributes

- id: WI-207
- created: 2026-05-08T19:11:51Z

- status: Open

- acceptance: cargo-test

- depends_on: WI-205

## Description

Typer rule: acyclic_cell(V) discharged predicate on Cell.new + Cell[V] annotations. Per proposal 037 §Cell + design doc docs/design/cell-runtime.md §'The rule — acyclic_cell as a discharged predicate'. Architecture: Cell.new's signature carries an implicit discharge obligation acyclic_cell(V) on the payload type V (surface syntax — explicit require clause vs sort-level constraint vs hidden kernel check — is an orthogonal language-design decision). Two-step discharge at every call site: (1) fact lookup — try to resolve fact acyclic_cell[T = V]; if any fact holds via standard SLD resolution, accept. (2) Default static walk — depth-first walk over V's structure carrying topCells (Cell types in the descent chain). At Cell[U]: if U in topCells, REJECT (type-graph cycle); else recurse into U with topCells extended. At entity/tuple/list/map: recurse without changing topCells. At abstract sorts: unfold and continue; visiting set guards termination on non-Cell sort recursion. At Closure/Stream/opaque: REJECT conservatively. At type variable ?T: deferred — re-check at the binding site. Implementation hook: kb::cell_well_typed(ty, top_cells) — called from Cell.new typing arm and Cell[T] annotation resolution. Cache (Cell[T], topCells) decisions. Extensibility: domain sorts (DList, DAG, ObserverChain, age-ordered cells, refinement-typed cells) declare invariants via fact acyclic_cell[T = …]; the discharge story (paper proof, SMT under WI-201, age-ordered runtime discipline, domain-API invariant) lives outside the kernel — the kernel only checks the fact exists. Tests: accept Cell[Int], Cell[String], Cell[Cell[Int]], Cell[List[Cell[Int]]], Cell[wis(IndexedFileStore, Int)]; reject Cell[A] where A's fields contain Cell[A]; reject Cell[Closure], Cell[Stream]; verify a fact acyclic_cell[T = dll[E]] makes Cell[dll[E = Int]] accepted even though the default walk would reject. Out of scope: the discharge-by-runtime-discipline mechanisms themselves (age-ordered cells, etc.) — those are separate WIs that build on this one. Acceptance: cargo-test green; the runtime cell_arena from WI-205 is unchanged; failing programs surface a typer error 'Cell[T] requires acyclic_cell(T); discharge failed at <site>' with a path describing where the cycle would close.

