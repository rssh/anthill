## Attributes

- id: WI-403
- created: 2026-06-05T22:16:41Z

- status: Open

- acceptance: cargo-test

- depends_on: WI-376, WI-002, WI-429

- tags: typing

## Description

Codegen/realization: lower an UNGROUNDED (rigid) path-dependent generic to the host language (docs/design/path-dependent-types.md sec.5.2). An ungrounded s.K means the operation is POLYMORPHIC over the member (an intentional type parameter induced by an abstract receiver; 011 well-typed/intentional ?, maps to a host generic). RULE DECIDED 2026-06-09 (normative: docs/rust-forward-mapping.md sec.2.14): spec-sort members surviving Self-collapse lower to Rust ASSOCIATED TYPES; within-sort s.K -> Self::K; cross-sort ungrounded -> the receiver lifts to a named generic, fn check[S: KVStore](s: S, k: S::K) (the induced host generic IS the receiver, so 'receiver in scope => nameable' = the lifted S); partial manifest -> equality bound S: KVStore[K = String]; the LIFT form fn merge[K, A: KVStore[K = K], B: KVStore[K = K]] is reserved for constraining two receivers to SHARE a member; data sorts keep generics. REMAINING SCOPE: the emitter -- implement sec.2.14 inside the WI-002 KB-driven full mapper (anthill-rust-gen; NOT the frozen bootstrap parse-IR codegen, where a projection must fail loudly instead). Rust-only (codegen).

## Changes

### 2026-06-09T17:58:39Z — feedback — claude

REALIZATION RULE DECIDED 2026-06-09 (user-steered session): WI-403's target is the WI-002 KB-driven full mapper (proposal 029 anthill-rust-gen), NOT the bootstrap parse-IR codegen (frozen per 029; today it silently strips the receiver and emits s.K as bare K -- spec now declares a projection reaching bootstrap must fail loudly). Decision: spec-sort members lower to Rust ASSOCIATED TYPES (forced by path-dependence: delta projects THE manifest member off the receiver type = functional-member reading = type K; trait generics would be the multi-instantiation reading under which s.K is ambiguous). Lowering forms: within-sort s.K -> Self::K; cross-sort ungrounded -> receiver lifts to a named generic, fn check<S: KVStore>(s: &S, k: S::K); partial manifest -> equality bound S: KVStore<K = String>; member sharing -> the ticket's lift fn merge<K, A: KVStore<K = K>, B: KVStore<K = K>>; concrete receiver delta-grounds before codegen. Data sorts keep generics (enums have no associated types). provides/fact bindings become impl type K = String; items (2.13 'bindings ignored' no longer holds for spec-sort impls in the full mapper). NORMATIVE: docs/rust-forward-mapping.md sec.2.14 (+ sec.2.6 mapper split, summary table); design closure: docs/design/path-dependent-types.md sec.5.2 + seam-map row. Ticket RE-POINTED onto WI-002 and LEFT OPEN: closes when the WI-002 emitter implements sec.2.14.

