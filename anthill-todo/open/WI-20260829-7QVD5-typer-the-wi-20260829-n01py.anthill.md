## Attributes

- id: WI-20260829-7QVD5-typer-the-wi-20260829-n01py
- created: 2026-08-29T19:28:00Z

- status: Open
- status_agent: user
- status_at: 2026-08-29T19:28:00Z

- acceptance: cargo-test, scaland-sbt-test

## Description

TYPER: the WI-20260829-N01PY witness leg builds its goal from a RAW `TermId` where the only other producer CANONICALIZES — a deep-form actual answers `false` with no diagnostic.

Found by /code-review on the N01PY commit (a6dc7061). READ, NOT DRIVEN — I have not
produced a program that reaches the arm in the deep form. VERIFY THAT FIRST: if the deep
form provably cannot arrive here, the fix is a sentence at the site saying why, not code.

THE ASYMMETRY. `witness_provides_admissibly` (typing.rs ~53008) binds the goal's carrier to
the raw `actual` `TermId`. The ONLY other producer feeding `spec_resolves_at_bindings` --
`declared_type_goal_bindings` (~58713) -- runs its values through `canonicalize_goal_value`
FIRST, and does so deliberately: the resolver's candidate side uses a bare `Ref(S)`, while
some type terms still carry the deep `sort_ref(name: Ref(S))` / `parameterized(...)` shape
that `type_dispatch_name` explicitly still accepts.

THE CONSEQUENCE IF IT CAN ARRIVE: an actual in the deep form builds a goal that matches NO
candidate, so the leg answers `false` -- a SILENT SKIP where this repo's stated principle is
a loud error. It would present as 'a witness-provided carrier is refused at a spec-typed
parameter', i.e. exactly the defect N01PY was opened to fix, reappearing on one shape with
no message pointing anywhere near this code.

WHY IT IS PLAUSIBLE RATHER THAN THEORETICAL: `bare_sort_compatible`'s own caller set now
spans BOTH dispatchers (see that function's corrected inventory), and the view-carried arm
exists precisely because a non-hash-consed producer -- `nominal_heads_compatible` -- can
hand it a carrier the term path would never have produced.

ACCEPTANCE, in this order: (1) try to DRIVE it -- a fixture whose actual reaches
`witness_provides_admissibly` in the deep form; (2) if it drives, either canonicalize here
for symmetry with the other producer or make the mismatch LOUD, and pin it with a row that
fails when the fix is backed out; (3) if it does NOT drive, write at the site WHY the deep
form cannot reach this arm -- a structural argument, naming what normalizes it and where --
so the next reader does not re-open this. Do not leave it as 'probably fine'.

