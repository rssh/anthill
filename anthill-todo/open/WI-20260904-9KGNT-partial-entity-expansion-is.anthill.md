## Attributes

- id: WI-20260904-9KGNT-partial-entity-expansion-is
- created: 2026-09-04T21:48:13Z

- status: Open
- status_agent: user
- status_at: 2026-09-04T21:48:13Z

- acceptance: cargo-test, scaland-sbt-test

## Description

PARTIAL ENTITY EXPANSION IS SCOPED BY FUNCTOR, NOT BY POSITION, and that generalization was never decided -- it fell out of how the rule was phrased. NARROW IT to the MATCHING positions. TODAY: `fill_entity_named_args` (kb/load.rs:16762) fills every unwritten declared field of a registered entity with a fresh var, in every logical position. In an ASSERTING position that silently turns an under-written fact into a universal claim -- MEASURED: with `entity Widget(id: Int64, label: String)` and `fact Widget(id: 42)`, the file loads clean with no diagnostic, `query Widget(id: ?i, label: ?l)` answers `?i = 42, ?l = ?label` (the query variable bound to the loader's minted var, named after the field), and `query Widget(id: 42, label: "never-written")` answers TRUE, 1 solution. CONTROL: the same program with `fact Widget(id: 42, label: "written")` answers `no solutions` for the never-written label and `true` for the written one -- so the difference is caused entirely by the fill. THIS IS CURRENTLY SPECIFIED BEHAVIOUR, which is why this is a narrowing and not a bug report: docs/kernel-language.md:4195 'Partial entity patterns' plus the WI-20260902-CZJ2N paragraph name a fact head as one of five logical positions and call the result 'the universal claim'. THE HISTORY IS THE ARGUMENT. As introduced (79985bed, 2026-03-20) the paragraph read '...whenever the functor is a registered entity -- including the zero-argument case, WHERE PARENTHESES SIGNAL PATTERN-MATCHING INTENT (bare account without parens remains a reference to the entity/sort)'. It is titled 'Partial entity PATTERNS', its rationale is pattern-matching INTENT, it sat in section 8.x beside Termination and Constraint Enforcement, and it never mentions fact heads or value positions. The scope clause is a condition on the FUNCTOR with no condition on the POSITION -- which is how an asserting position was included: not by a decision, but by the phrasing. WI-20260902-CZJ2N later rewrote that parenthetical into the five-position list and named the fact-head outcome 'the universal claim', but read what it was fixing -- 'fact account asserted a phantom account/0 atom that :- account() could not see'. It repaired the BARE ZERO-ARG spelling and made bare agree with parenthesised; it never asked whether partial expansion belongs in an asserting position, it inherited that from the 2026-03 phrasing and described the result. So 'the universal claim' is a description of what the code does, reached through a clause written for matching. THE NARROWING: KEEP the expansion in the three MATCHING positions -- rule-body goal, query pattern, proof step -- where 'avoids requiring the user to explicitly list unneeded fields with ?' is exactly right and where the discrimination tree needs both sides normalized to descend in lockstep (`insert_walk_args`, kb/discrim.rs:338, walks positionals then one `DiscrimKey::NamedKey` edge per named arg in canonical order). REFUSE an unwritten REQUIRED field in the two ASSERTING positions -- fact head and rule head -- where the same syntax silently means 'for every value of that field'. The author writes `?` to mean it, which the language already has. THE TRIE IS UNAFFECTED: a fact that must name every required field already produces the slot path the fill was producing, and patterns keep being normalized, so insert and lookup stay in lockstep. CZJ2N's bare-vs-parenthesised unification is ORTHOGONAL and stays -- it fixed a real invisibility bug. THE OPTIONAL COLUMN IS ALREADY CORRECT and is untouched: WI-716 split it by position (`none()` when asserting, wildcard when matching, docs/kernel-language.md:3462) and that split is the precedent this ticket follows one column over. There is no `none()` to fill a required field with, so refusing is the only truthful option. BLAST RADIUS MEASURED: 7 of 178 facts in a standard stdlib load carry head vars at all (probe over `rule_arity` on empty-bodied rules); each needs reading, since some may be deliberate `?`-written facts rather than omissions. ACCEPTANCE: an unwritten REQUIRED field in a fact head or rule head is a LOAD ERROR naming the entity, the field and the span; the three matching positions are unchanged and their tests stay green; the WI-716 optional behaviour is unchanged in both positions; docs/kernel-language.md:4195's scope clause is rewritten to condition on POSITION rather than on the functor, and the 'universal claim' sentence is corrected or removed; the stdlib's 7 head-var facts are inspected and either completed or shown to be deliberate; a test DRIVES the refusal and a second test DRIVES the still-legal body-goal omission (`rule known_id(?i) :- Widget(id: ?i)` must keep working); cargo-test green via scripts/test.sh.

