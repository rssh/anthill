## Attributes

- id: WI-1041
- created: 2026-08-07T10:57:40Z

- status: Open

- acceptance: cargo-test

## Description

HARDENING (WI-1039/1038 family; /code-review 2026-08-07 findings 4/8/9/10/11 — each verified LATENT: no suite-reachable trigger today, which is why they are ONE ticket; each is a silent mislocation or panic seam whose first trigger arrives with a future edit while the suite stays green). (1) typing.rs:39992 — type_rule_bodies tags every dispatch error Some(n.span.source) unconditionally; an atom carrying the empty-span sentinel (SourceId(0), 0..0) renders <first-file>:1:1 — a WRONG file, where pre-change behaviour was an honest unlocated offset. SourceId(0) is NOT reserved, it is the first registered file. Trigger: term-built rule bodies (term_body_to_nodes -> materialize_from_handle, which has no span table) whose dispatch walk errors; assert_rule is pub API. FIX: keep None for the sentinel — the sibling push 50 lines up is already Option-correct. (2) typing.rs:39987 — the errors/sources parallelism is guarded only by a debug_assert plus the TRUNCATING sources.resize(errors.len()) spelling; every sibling site (38820/38845/39231/39579) uses the structurally non-truncating while-push. A future push into sources inside the walk silently shifts the pairing in release builds — the exact WI-745 defect the change fixed — with no test observing tag alignment. FIX: the sibling spelling, or Vec<(TypeError, Option<SourceId>)>. (3) node_occurrence.rs:442 — carry_typer_stamps_from holds the src borrow guard across the dst borrow_mut of the SAME RefCell, so self==src panics BorrowMutError; verified unreachable through all three current callers, but the method is pub and the precondition is stated nowhere; the panic fires only post-typer on typed corpora, so unit tests over unstamped nodes stay green. FIX: if std::ptr::eq(self, src) return — one line. (4) load.rs:12121 — parse_span_table walk rests on Term::subterms, which returns EMPTY for Term::ParseAux: everything beneath a ParseAux is silently pruned, an undocumented second miss class beside the doc WHAT IT CANNOT COVER paragraph, through the one variant the walk own goes-stale-in-SILENCE comment says it guards. FIX: state the miss class in that paragraph; descend the payload if cheap, else a loud debug assertion. (5) node_occurrence.rs:4601 — match-branch metadata hardcodes branch_span = empty_span() inside visit_fn with br_tid and the caller span table both IN SCOPE, contradicting the consulted-ahead-of-kb.term_span-for-every-node promise; companion latent defect noted by the verifier: the positional reflect encoding silently DROPS branches. FIX: use the in-scope values; record or fix the branch drop. ACCEPTANCE: each of the five gains a driving test (an embedder-shaped assert_rule fixture for 1; unit tests for 2/3/5) or a stated why-not at the site; each test says which case fails when its guard is backed out; until closed the five sites cite this ticket. REFERENCE: /code-review 2026-08-07; WI-1039; WI-745; WI-1026.

