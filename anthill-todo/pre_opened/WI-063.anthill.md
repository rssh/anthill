```anthill
fact WorkItem(id: "WI-063", created: "2026-04-20T06:54:22Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-062"]), generates: none, requires_capability: none, status: PreOpened)
```

## description

EvalConfig: add value_weight_cap. Sum per-Value weight (scalars=1, Str ~= len/16, Entity/Tuple = 2 + children) across frame locals + closure arena. Catches memory-blowup programs where stack stays shallow but values grow (e.g., recursive cons-ing of big strings). Not needed for typical CLI / test usage; useful when embedding anthill in a host (untrusted input) or for spec-level budget contracts. Tag as 'embedding prerequisite'.

