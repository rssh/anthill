```anthill
fact WorkItem(id: "WI-712", created: "2026-07-13T12:44:16Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: PreOpened)
```

## description

Query capabilities to make the list report a QUERY not a hand-rolled fold: aggregation in value-binding (producer) position. CONTEXT: follow-up to WI-711. Even after WI-711 folds the blocked classification into the rule, list (cmd_list_plain) stays a functional report because one thing cannot be expressed relationally today: the [unblocks N] count (number of transitive dependents), which stays as operations (build_dep_map+count_transitive, main.anthill:946/1084). CORRECTION to an earlier framing -- aggregation is NOT absent: the language already has cardinality quantifiers forall/some/one/lone/no AND aggregate keywords count/sum/min/max (grammar quantifier_keyword/aggregate_keyword; ir.rs:576,592). But they live in CONSTRAINT/boolean-test position: one/lone/no lower to guard queries (one_q/lone_q/no_q, ir.rs:580), enforced via the general guard path (WI-020=discrim optimization, WI-556=first example, both open); forall/some ... in xs ALREADY resolve as rule-body goals (some_in/forall_in, resolve.rs:1437) -- the existential/universal WI-711's classification uses; count/sum/min/max are PARSED-BUT-LOAD-REJECTED -- 'aggregation constraint is not yet enforced (the guard engine cannot evaluate count/sum/min/max)' (load.rs:548). THE GAP (narrow): list needs a COUNT bound to a VALUE for projection into a Row -- the PRODUCER dual of the existing count-in-constraint-position (count(...) op bound, a boolean test). Two ways forward: (a) promote count from unenforced constraint-position to an enforced value-binding rule-body goal, e.g. unblocks(?id,?n) with ?n=|transitive dependents|, dovetailing the open WI-023/020/556 aggregate/cardinality thread rather than being greenfield; (b) sidestep -- if the exact N in [unblocks N] is not load-bearing, degrade to a boolean [unblocks]=(some ?x: depends(?x,?id)), expressible TODAY with the existing bounded-quant, no new capability. With aggregation-as-producer (or (b)), plus the standing fact that ORDER BY/GROUP BY are always post-query folds (true even in SQL -- chrono_topo+status grouping stay a final step), the whole list report collapses to query(item(?id,?status,?desc,?deps,?blocked,?unblocks,?has_fb)) -> order -> group -> render: the 'everything is facts / typing-as-transform' thesis applied to the CLI's own read path. Overlaps WI-370 (custom unification/search at a discrim node) and WI-580 (body-derived relational views on demand). ACCEPTANCE: a design proposal in docs/proposals/ settling (i) aggregation surface in value-binding position (how ?n binds; relation to the WI-023 constraint-position count/sum/min/max), (ii) interaction with NAF + undecided/floundered honesty (WI-519/531), (iii) whether counts are cached/incremental, (iv) migration of cmd_list_plain onto a query, and the (a)-vs-(b) decision for [unblocks N]; NOT a shipped impl (that is a follow-on). Backlog / future direction.

