## Attributes

- id: WI-637
- created: 2026-07-05T07:57:48Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-10T04:17:13Z

- acceptance: scaland-sbt-test

- tags: scala

## Description

scaland mirror of WI-633: nonlinear rule-head match completeness. scaland's discrim-leaf resolveLeaf (core/.../discrim/PersistSubst.scala SmallSubst.resolveLeaf + SharedSubst) binds via Substitution.bind(vid, tid), whose rebind check is TermId-raw equality (structural identity, no unification) — so a repeated head var over doubly-concrete occurrences false-fails exactly as rust did pre-WI-633: query unbox0(box(v: some(?x)), some(42)) against head unbox0(box(v: ?v), ?v) drops the candidate (silent 0 solutions) instead of binding ?x=42. MIRROR the rust fix (249f9fd0): make the SLD-resolution leaf path UNIFY on rebind (occurs-checked structural unification, the &self no-reduce sibling of scaland's unify) while KEEPING one-directional matching (matchTerm/matchView) on structural identity — the LOAD-BEARING split rust encodes via a unify_rebind bool threaded through resolve_leaf (query_resolved_value=true SLD, query_resolved=false match). NB scaland has NO typer (project_scaland_no_typer), so the rust simp_rewrite::try_fire / apply_eq_rules consumers do NOT exist here — only scaland's own SLD head-selection needs unify, and its match consumers stay structural; there is no eq-rules/simp guard to port. Sibling of WI-626 (scaland mirror of WI-624). Port the rust regression tests: nonlinear_head_doubly_concrete_unifies + a matchTerm-nonlinear-is-matching-not-unification analogue. ACCEPTANCE: sbt test green.

