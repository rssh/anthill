```anthill
fact WorkItem(id: "WI-608", created: "2026-07-01T04:41:13Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-01T20:25:12Z"))

fact Tag(workitem: "WI-608", name: "finiteness")

fact Feedback(workitem: "WI-608", author: "claude", at: "2026-07-01T20:25:32Z")
```

## description

Typer: ground the element/effect of an abstract spec-op called on a receiver that is a MANIFEST application of the SPEC ITSELF. Reproducer: a thin finite combinator whose Iterable iterator body does mapped(iterator(src), fn) where src: FiniteCollection[C=SrcC, Element=Src, E=ES]. iterator(src) does NOT ground its T/E, so mapped(...) leaks MappedStream[T=??_, Src=??_, ES=??_]. ROOT (lead): carrier_param_receiver (typing.rs ~11101) calls transitive_provision_view, which finds nothing when carrier_sym == spec_sort (a spec doesn't provide itself). CANDIDATE FIX: when the receiver's carrier IS the spec, return an IDENTITY provision view (each spec param -> itself) + transitive=true (defer to eval) so bind_spec_params_from_carrier_param reads Element/E off the receiver's OWN written type-args. CAUTION: a first attempt at exactly this REGRESSED the stdlib (a92a70f6 fork) and was reverted -> re-derive deliberately, verify no regression via the test harness (which reads live stdlib). Prereq for the WI-599 thin stdlib rewrite (the combinator must provide Iterable since FiniteCollection requires it). ACCEPTANCE: iterator(src)/mapped(iterator(src),fn) grounds; focused typer test + full suite green (no regression).

## Feedback

### 2026-07-01T20:25:32Z — claude

DELIVERED (code dfba147, rebased onto WI-600). ROOT (confirmed by instrumentation, NOT the ticket's 'carrier==spec' guess): iterator(src) on src:FiniteCollection dispatches Iterable.iterator with spec_sort=Iterable, carrier_sym=FiniteCollection — related by REQUIRES (FiniteCollection requires Iterable), not provides. transitive_provision_view is provides-only so returned None → no bind, no deferral → Stream[Element,E] leaked ??_ → mapped(...) cascaded MappedStream[T=??_,Src=??_,ES=??_]. FIX: abstract_spec_required_view builds the provision view from the requires clause (direct_requires + unwrap_spec_view; SAME SortView shape as a provides fact), wired into carrier_param_receiver via .or_else gated on carrier_is_abstract_spec, marked transitive=true → bind_spec_params_from_carrier_param threads Element/E off recv's own type-args and the carrier_is_abstract_spec dispatch gate (6273) defers to eval. The carrier-param twin of WI-598/601. /code-review (1 independent reviewer, not fork per prior process note) found a REAL latent gap: the WI-444 override extraction (typing.rs:5936) lacked the abstract-spec guard, so my broader Some would mis-pin a qualified Iterable.map/filter on a FiniteCollection to FiniteCollection's OWN map/filter — added the carrier_is_abstract_spec guard mirroring 6273 (concrete carriers keep their pin). REPRO PATTERN LESSON: the sort-member combinator must thread element/effect via EXPLICIT op type params bound from the receiver (the FilteredStream.splitFirst pattern), NOT implicit sort rigids (the match destructure leaves field-type params unbound) — my first repro wrongly used implicit rigids. Full suite 2408/0. WI-609 (collect, the REFLEXIVE carrier==spec case + result-effect threading) is a DISTINCT ticket, correctly UNTOUCHED by this fix (abstract_spec_required_view matches requires entries only, so collect on FiniteCollection still returns None).

