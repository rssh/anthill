```anthill
fact WorkItem(id: "WI-1029", created: "2026-08-06T14:08:13Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-06T14:02:49Z"))

fact Tag(workitem: "WI-1029", name: "rustland")

fact Feedback(workitem: "WI-1029", author: "user", at: "2026-08-06T14:02:49Z")
```

## description

RUSTLAND: THE SCOPE PREDICATES READ THE CONSTRUCTOR FLAG THROUGH A TERM ROUND TRIP. `is_sort_scope(kb, term)` matches `Term::Fn{f,[],[]}` + `has_kind(f, Sort)`; `eponymous_sort_symbol` sits on top of it and returns `Option<Symbol>`. After WI-1028 every caller holds a `ScopeId` and derives the term to feed them (`is_sort_scope_id`, and `DefinePass::at_item`'s `scope_term`), so the composition is provable by reading `KnowledgeBase::alloc`: it rewrites a nullary `Fn` to `Ref` iff `is_constructor_symbol(functor)`, and the reader matches only `Fn`. Therefore `is_sort_scope_id(scope)` IS `!is_constructor_symbol(scope.owner()) && has_kind(scope.owner(), Sort)` — the term is an ENCODING of the constructor flag, and the readers pattern-match the encoding instead of naming the referent (the WI-1024 shape). WORK: make the symbol form primary; keep `is_sort_scope`/`eponymous_sort_symbol` only if something still needs the term spelling, and delete them otherwise. WHY THE PROHIBITION DOES NOT APPLY, because it will be quoted at whoever picks this up: WI-1028 says "DO NOT FOLD IN the term-level readers (WI-983's feedback rules those out)". CHECKED — it does not. WI-983's criterion is "each needs a term BECAUSE A TERM IS WHAT GOES IN THAT POSITION" and it names four sites, all of which PUT a term somewhere (`registerEntityOf`'s parent sort, the `entity_of` argument, `provides_clause`'s `sort_ref`, `Builtins.executeScope`'s answer). These two put no term anywhere — one returns `bool`, the other `Option<Symbol>`. The generalization entered at WI-984's DELIVERY RECORD ("the term-level readers ... WI-983's feedback rules them out of scope") and WI-1028 restated it as an instruction; neither re-checked it against WI-983's own criterion. THE TRAP, and it is the whole reason this needs care: THERE IS NO DISCRIMINATING TEST, BY CONSTRUCTION. The two readings are equivalent, so no input separates them — do not invent a test that appears to. MEASURED over the whole corpus during WI-1028: the term reading and `has_kind(Sort)` on the owner NEVER disagree, i.e. no scope owner reachable today is a marked constructor at all. So state the acceptance as a SHAPE, not as a behaviour: (1) no predicate about a scope takes a `TermId`; (2) `is_sort_scope_id` takes `&KnowledgeBase`, not `&mut` — today it must be `&mut` because `make_name_term_from_sym` routes through `TermStore::alloc`, which bumps a refcount even on a hash-cons HIT (`kb/term.rs`, and `TermStore::find` exists for exactly this caller but answers `None` for a never-allocated term, which is why the two questions are one ticket); (3) the WI-926 dependency is spelled `is_constructor_symbol` at its site rather than implied by a `Term::Fn` match. PIN THE EQUIVALENCE rather than a behaviour change: a test that builds both shapes — a scope whose owner is `mark_constructor_symbol`'d and one that is not — and asserts the answer, so a future change to the WI-511 canon breaks it loudly. Say at the site which test fails when the change is backed out, and say plainly that the corpus passes either way BY DESIGN. REFERENCE: WI-1028; WI-984; WI-983; WI-926; WI-511; WI-1024.

## Feedback

### 2026-08-06T14:02:49Z — user

IMPLEMENTED, same session as WI-1028, workspace green 4228/0.

`is_sort_scope` now reads `!kb.is_constructor_symbol(scope.owner()) && kb.has_kind(scope.owner(), Sort)` and takes a `ScopeId` and `&KnowledgeBase`. `eponymous_sort_symbol` takes a `ScopeId` and returns `scope.owner()`; its second `Term::Fn` match was dead (the `is_sort_scope` guard above it had already established `Fn`) and is gone. The `is_sort_scope_id` adapter WI-1028 introduced is gone with them, and with it the `&mut` on a predicate and the per-call `alloc` refcount bump. The `DefinePass::at_item` Entity arm no longer derives a term at all.

THE PROHIBITION DID NOT HOLD, and the ticket said why: WI-983's criterion is "a term is what goes in that position" and neither of these puts a term anywhere. Verified rather than repeated.

NO DISCRIMINATING TEST EXISTS, BY CONSTRUCTION — the two readings are equal. The acceptance was written as a shape and is met that way. `a_sort_scope_is_one_whose_owner_is_a_sort_and_not_a_constructor` pins the RULE on both shapes (constructor-owned and not) and carries the old term reading beside it as an oracle, asserting they agree on all three rows; it passes either way BY DESIGN and says so. A future change to the WI-511 canon breaks it loudly.

MY FIRST FIXTURE WAS WRONG and the test said so: I built the symbols with `intern`, but a kind lives on `SymbolDef::Resolved`, so every row read false for the same uninteresting reason. `define`d now, with the reason at the site.

DOC ROT FIXED IN THE SAME COMMIT: WI-926's comment said marking an eponymous constructor "would flip the sort's own nullary term through the WI-511 canon, which `is_sort_scope` matches only in its `Fn` spelling" — true of the old encoding, meaningless now. It states the dependency as the plain sentence it always was: a constructor-owned scope is not a sort body, so marking it would stop `sort P { entity P(…) }` reading as one from the inside. `rustland/CLAUDE.md`, the `ScopeId` doc and the `ScopePass` doc all cited `is_sort_scope` as the live example of the trap; each now says it WAS one until this ticket.

