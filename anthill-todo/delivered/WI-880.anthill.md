## Attributes

- id: WI-880
- created: 2026-07-30T20:11:34Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-27T07:35:52Z

- acceptance: cargo-test

- depends_on: WI-876

## Description

MIGRATE THE REMAINING SPEC-OP-KEYED HOST REGISTRATIONS to per-carrier `operation_map` bindings — the generalization WI-876 proved on the ordering family and deliberately did not attempt in one go. THE HOLE IS THE SAME AND IT IS LATENT EVERYWHERE: `eval/builtins.rs` registers `anthill.prelude.Numeric.add`/`sub`/`mul`/`neg`, `anthill.prelude.PartialEq.eq`/`neq` and friends by SPEC-OP qualified name, so ONE host implementation is the backing for every carrier that never wrote its own — including carriers whose values it cannot handle. `op_backed`'s `kb.is_builtin` leg then certifies the operation as backed for ALL of them, which is WI-876's defect A stated for a different family. It has not bitten yet only because no structural carrier provides `Numeric` — exactly the position `Ordered` was in until `anthill.prelude.Pair` became its first structural provider (WI-858), which is how the ordering defect was found. NOTE `PartialEq.eq` is the ONE that is already safe, and for an instructive reason: `builtin_eq` -> `semantic_equal` DISPATCHES to a carrier's own `eq` before deciding structurally. It should still migrate — a dispatching builtin is a workaround for the missing key, not a design — but it is not urgent, and unlike the others it cannot simply be re-keyed: equality fires from UNIFICATION, where there is no call site and no dictionary (058 §3.7), so the value-directed step has to stay. WORK, per family: declare the operations on each host carrier (`Int64.add` etc., body-less), add the `operation_map` entries to the binding blocks, add the host functions to `host_fn_by_key`'s closed registry, delete the spec-op `register_if_present` lines, and give the spec real default bodies wherever a derivation exists. SIZE CHECK BEFORE STARTING: `Numeric` is the big one — `add`/`sub`/`mul`/`neg` across `Int64`/`Float`/`BigInt`, with the overflow behaviour differing per carrier (`Int64.add` raises `Overflow`, `Float` saturates, `BigInt` cannot), which is itself an argument FOR per-carrier keying: today one function branches on the operand carrier to decide which arithmetic it is implementing. Watch the same two traps WI-876 hit: a carrier's own member must be EXECUTABLE for `carrier_override_op` (`op_is_executable` now covers host mappings, so this is already paid for), and a spec default body may only call operations the spec's own `requires` chain covers — otherwise an OP-SCOPED `requires` on the default is the only place it can live. ALSO EXPECT the WI-883 class of fallout: a spec-op call on a carrier that does not provide the spec stops resolving, which is correct and may look like a regression in tests that relied on the builtin ignoring provisions. ACCEPTANCE: no `register_if_present` in `eval/builtins.rs` names an operation of a spec that has per-carrier providers; a structural carrier providing `Numeric` gets a working surface from its own primitives with no per-carrier boilerplate beyond what the spec cannot derive; a test drives that carrier the way `wi876_operation_mapping_test` drives the ordering one; full workspace green via rustland/scripts/test.sh. REFERENCE: WI-876 and docs/design/058-implementation.md §10 (the mechanism and its two surprises); CLAUDE.md's 'A HOST IMPLEMENTATION IS KEYED PER CARRIER' entry.

## Changes

### 2026-07-30T18:40:26Z — feedback — user

TWO ADDITIONS FROM WI-876's CODE REVIEW, both about readers that did NOT move when the mechanism did. (a) THE LOAD CHECK'S EXEMPTION IS COARSER THAN THE MAPPING, and it is the per-carrier version of the very bug WI-876 fixed per-spec-op. `check_provider_operations` skips ANY carrier that is an `Implementation.target` WHOLESALE — i.e. 'it is a host carrier, so assume every operation is backed' — and since `emit_operation_mapping_facts` only ever runs alongside `emit_implementation_fact`, every carrier that CAN carry an `operation_map` is already skipped before the op walk. CONSEQUENCE, measured by the reviewer: a `provides Widget3 language rust` carrier with body-less, UNMAPPED, unimplemented `compare`/`eq` plus `fact Ordered[T = Widget3]` LOADS CLEAN. So `op_backed`'s new `op_is_executable` host-mapping leg is correct-by-construction but UNREACHED today, and `docs/kernel-language.md`'s sentence about it has been softened to say so rather than describe a path nothing takes. Now that backing is knowable PER OPERATION, the skip is the thing to retire: keep it only for a carrier with an `Implementation` and NO `operation_map`, or drop it once the migration is complete. (b) THREE SIBLING READERS STILL ASK THE NARROW QUESTION, and two of them degrade SILENTLY once `eq` is host-mapped — which this ticket plans. `resolve.rs`'s `sem_eq_dispatch` and `eval/builtins.rs`'s `semantic_equal` branch on `op_has_runnable_body` to choose the eval bridge over `prove_rule_predicate`; a host-mapped `eq` would take the SLD branch, find no clauses, and return Refuted — EQUAL VALUES REPORTED UNEQUAL, precisely what both functions' own comments say they exist to prevent. And the typer's three static-pin gates (`typing.rs`, the WI-237/WI-444 sites) gate on a runnable body too, so a host-mapped member is never statically pinned and always takes eval's value-directed path — harmless today because WI-876 widened `carrier_override_op`, but the static and dynamic paths now DISAGREE about what 'runnable impl' means, and one of those comments still justifies itself with 'lets the runtime resolve it via its registered builtin', a rationale WI-876 removed for this family. THE DEEPER FIX, and it should land with or before the `eq` migration: name the two questions apart — `op_is_executable` (can the interpreter INVOKE it) versus an `op_has_anthill_body` (is there a BODY to inline / unfold / eta-lift) — and make each of the ~10 readers pick one EXPLICITLY. `body_specialize.rs`'s four sites and `eq_override_backed` are correctly the body question and their comments already say so; the five above are annotated neither way.

### 2026-07-30T22:05:47Z — feedback — user

PERF NOTE FROM WI-881'S REVIEW, on the exact registration path this ticket reworks — worth folding in rather than filing separately. `register_operation_mappings` recomputes a KB-STATIC result for EVERY fresh interpreter, including the short-lived one `run_in_bridge_interp` builds per bridged evaluation (`bridge_op_to_eval` once per SLD goal whose op must be evaluated, `try_expand_macro` once per macro fire) — where `register_standard_builtins` IS the entire startup cost, since the KB is `mem::take`n and there is no parse or load. Per crossing it does, per mapping: 3 `String` clones in the `.cloned()` snapshot (`op_qn`/`host_fn`/`lang`, of which `op_qn` and `host_fn` are read ONLY inside the two `format!` error arms), a `host_fn_by_key` str match, an arity lookup, a `canonical_sym` qualified-name hash, and 1-2 `Arc::new` in `register_builtin_sym`. WI-881 DOUBLED THE MULTIPLIER: the stdlib's mapping count went 25 -> 50 (float alone 4 -> 29), so this is now ~150 String allocations and ~50 symbol+arity lookups per bridged evaluation. WI-881 already took the cheapest piece — the arity read no longer builds a whole `OpInfoRecord` (which cloned five per-field `Vec`s to be dropped after one `.len()`); it reads the cached WI-656 signature, with the fact-scan kept as the fallback. THE REST IS STRUCTURAL AND BELONGS WITH THIS TICKET, because migrating `Numeric`/`String`/`Bool` will double the count again: memoize the VALIDATED `Vec<(Symbol, fn(&mut Interpreter, &[Value]) -> ...)>` once (a `OnceCell` beside `host_op_mappings`, invalidated by `set_host_op_mappings`), leaving per-interpreter work as N hashmap inserts. That collapses the snapshot clones, the key match and the arity check in one move. CALIBRATION, so it is not overstated: for the TEST harness (`common::interp_for`) this is noise — measured 0.46s per `interp_for`, entirely stdlib parse+load. The exposure is the bridge interpreter and it scales with bridged-call volume.

### 2026-07-30T22:57:23Z — feedback — user

WI-884: YOUR ACCEPTANCE IS WORDED FOR SPEC OPS AND SO CLAIMS NEITHER HALF OF THE SPLIT THIS TICKET WIDENED. It says no `register_if_present` may name 'an operation of a SPEC that has per-carrier providers' — but `anthill.prelude.String.concat` and `anthill.prelude.Int64.abs` are CARRIER-OWNED operations of a sort, so the hardcoded qualified name already keys them per carrier and they answer correctly. No open ticket claims them, and after WI-884 one carrier's host surface is enumerated at TWO altitudes: `String` has 8 operations registered by hardcoded name (`concat`/`length`/`startsWith`/`endsWith`/`substring`/`toUpper`/`toLower`/`repeat`) and 5 through `operation_map` (`contains`/`indexOf`/`replace`/`trim`/`split`); `Int64` has 9 and 2. THE TWO HALVES ARE NOT EQUIVALENT TO THEIR READERS, which is the cost: (i) `op_is_interpretable` (kb/typing.rs) counts a host MAPPING and not a hardcoded registration, so `String.contains` reads as backed and `String.concat` does not, though one interpreter runs both — WI-876's own defect shape (a host-backed member reading as ABSENT to `carrier_override_op`), unreached today only because no spec declares `concat`, which is exactly the coincidence WI-876's review refused to rely on; (ii) `kb.host_op_mappings()` — the reader WI-886 wants a second backend to consume — sees 5 of String's 13 host operations, so 'write the binding block, get the backend' is true for the new five and false for the old eight; (iii) only the mapped half has its arity checked against the anthill declaration at registration. WI-884 deliberately did not migrate the other seventeen (out of its scope, and it would have doubled the mapping list this ticket's own feedback already asks to memoize), and left a pointer comment at the `register_if_present` block naming this ticket. SUGGESTED: widen the acceptance with one clause covering CARRIER-OWNED registrations, so the split has an owner. Also note WI-884 added 7 mappings (57 total across the four carriers), so the per-interpreter cost this ticket's feedback flags has grown again.

### 2026-07-31T05:49:07Z — feedback — user

WI-886 FOUND A SECOND, WIDER FACE OF THE HOST-CARRIER EXEMPTION, and shipping cpp binding blocks is what made it bite: `check_provider_operations` (kb/typing.rs) builds its skip set from `anthill.realization.Implementation` facts with NO LANGUAGE FILTER, while `emit_implementation_fact` (kb/load.rs) emits one for EVERY `provides X language <L>` block. So a `provides Box language cpp` block alone puts `Box` in `host_targets` and exempts its provisions from the backing check — for the RUST build too.

Before WI-886 this was invisible because every binding block in the tree said `language rust`, so 'the carrier is host-realized' and 'THIS host realizes it' were the same sentence. They are not any more, and the ticket's own test program (`wi886_cpp_mapping_language_test.rs`, `CPP_ONLY_MEMBER`) is the first thing in the tree shaped to notice: it declares a body-less `Box.max` realized ONLY in C++ and loads clean — because of this skip, NOT because `op_is_executable`'s language-agnostic leg counted the cpp mapping. That distinction was asserted wrongly in the test's first draft and is now stated correctly there.

CONSEQUENCE FOR THIS TICKET'S SHAPE: narrowing the exemption 'now that backing is knowable per operation' is not only about per-operation granularity — it must also decide the LANGUAGE question. A cpp mapping is a real implementation of the operation (which is why `is_host_mapped_op` is language-agnostic and `op_is_executable` reads it) and is NOT one the rust build can run (which is why WI-886 split `is_interpreter_mapped_op` out for eval, `kb/mod.rs`). Whichever way `op_backed` should answer, `check_provider_operations`' `host_targets` must agree with it, or the two legs of one check disagree about what 'host-realized' means.

ALSO RECORDED HERE because this ticket's acceptance is spec-op-worded and does not currently claim them: WI-886 added `anthill_core::kb::op_info::declared_arity` (pub, two tiers, the `operation_is_declared` shape) so a backend crate can ask an operation's arity without building a whole `OpInfoRecord` — `op_record` is `pub(crate)`, which is why the rust registry's documented-cheap path was unreachable from cpp-gen. Any further host-surface migration should use it.

### 2026-08-27T06:22:35Z — feedback — user

THE `anthill.reflect` FAMILY IS 29 MORE HARDCODED REGISTRATIONS, and it is the one whose
consequence is not "a host op is awkward at one position" but "A RULE CANNOT READ A TERM
AT ALL". Measured 2026-08-27 on the tree at VPEWK, filed against this ticket's own WI-884
feedback ("SUGGESTED: widen the acceptance with one clause covering CARRIER-OWNED
registrations, so the split has an owner") — reflect is that clause's largest population
and it is named nowhere yet.

  29  registered by HARDCODED NAME in eval/builtins.rs: `extract`, `term_field`,
      `term_functor_name`, `term_list_items`, `term_as_entity`, `term_as_int`,
      `term_as_string`, `term_to_string`, `as_term`, `make_fn`, `make_apply`,
      `replace_named_arg`, `field_access`, `fresh_var`, `unify`, `occurrence_term`,
      `KB.{kb,loaded,execute,facts_of}`, `Substitution.lookup`, …
   3  DECLARED WITH NO IMPLEMENTATION AT ALL: `sort_as_term`, `term_as_sort`,
      `can_be_sort` — these run nowhere, not even from an operation body, and are a
      separate (worse) case from the split this ticket owns.

MEASURED CONSEQUENCE, on the guardians KB:
  rule ext(?v, ?e) :- SortProvidesInfo(sort_ref: ?c, spec: ?v), extract(?v, ?e)
      -> no solutions, against hundreds of provisions.
  rule s(?t) :- sort_as_term(Int64, ?t)   -> no solutions (the tier-2 case).
CONTROL that VPEWK is live in the same tree: `:- Bool.and(true, true) = true` answers 1,
the false case 0.

WHY IT MATTERS BEYOND TIDINESS. `TypeExtractor` is a COMPLETE structural view of a
`Type` (`SortRef` / `Parameterized(base, bindings)` / `Arrow` / `Denoted` / `NamedTuple`
/ `EffectsRows` / `TypeVar` / `Nothing`) and `extract` IS implemented — so the library
already has everything a rule needs to decompose a reflection row's type-valued field,
and the only thing between them is this registration split. kernel-language.md §5.2 says
the hardcoded half is DECIDED FALSE rather than suspended, so `not(...)` over one answers
1: for the reflect family that means a rule can conclude a POSITIVE fact from a term it
never read.

A SEPARATE FINDING, NOT THIS TICKET'S, recorded so it is not re-derived: the resolver
`BuiltinTag` `extract_sort_ref(?inst, ?result)` — tier 3, so the hardcoded decline does
not explain it — SUCCEEDS WITHOUT BINDING `?result` in a rule body. `builtin_extract_sort`
looks correct (it special-cases `SortView(name, …)`, falls through to the functor for a
bare `Ref`, ends in `finish_result`). Suspected: WI-938's relational view of the arity-1
declaration `operation extract_sort_ref(inst: Term) -> Symbol` shadows the arity-2 tag.
Genuine solutions, no residual; chaining `short_name(?result, ?n)` then answers nothing.

WRITTEN UP as library proposal 008 (docs/proposals/library/008-term-view-and-operations.md),
which DEPENDS on this ticket for its phase 1. Its first consumer is
examples/guardians/lib/safety.anthill, whose tier 1 is currently unwritable for exactly
this reason.
### 2026-08-27T07:35:38Z — feedback — user

DELIVERED. THE ARITHMETIC FAMILIES ARE KEYED PER CARRIER, the carrier-owned half went with them, and BACKING IS NOW A PER-OPERATION QUESTION. rustland 5587 passed / 0 failed; scaland 518/518.

PRE-STATE MEASURED FIRST, on a structural `Money(cents: Int64)` carrier providing `Numeric` with its own add/neg/zero/mul/one and NO `sub`: `Additive.sub(cents(700), cents(25))` died 'expected matching Int, BigInt, or Float, got Entity' on a program that LOADED CLEAN (op_backed reads `sub`'s resolver tag on the spec op). It answers 675 now. `add`/`mul`/`neg` answered EITHER WAY through Money's own members — stated so the fix is not credited with more than the one row it moved.

WORK: (1) `Int64`/`Float`/`BigInt` DECLARE add/sub/mul (and BigInt a `neg` it never had), each naming its own host function in its binding block — int_add/float_add/bigint_add etc. The OVERFLOW COLUMN is the argument made concrete and is driven: Int64.add RAISES, Float.add SATURATES to an infinity, BigInt.add cannot overflow. One function used to be all three and told them apart by TESTING ITS OPERANDS. (2) `Additive.sub` gains the default body `add(a, neg(b))` — the `sub_def` law, which arithmetic.anthill recorded as unwritable while the spec-op builtin shadowed it. `neg` keeps none: its derivation reads `sub`, so both bodies would bottom out in each other. (3) The four `register_if_present` lines are gone.

WI-884'S CLAUSE TAKEN TOO — the CARRIER-OWNED half: String's eight, Int64's abs/mod/rem/div/divExact/sign/to_float/to_string, Float's div + three IEEE predicates, BigInt's three conversions. Never the spec-op defect (the qualified name already keyed them per carrier and they answered correctly), but the two halves were not equivalent to their READERS, and kernel-language.md §8.7 recorded the sharpest as a SOUNDNESS gap: `is_unreduced_op_call` could not see a hardcoded registration, so `:- String.concat("a","b") = "ab"` was DECIDED FALSE rather than suspended and `not(...)` over it answered 1 DEFINITE. wi_vpewk_host_op_operand_test's pin rows were written 'to FAIL when either lands'; they now carry the flipped values (cat 0->1, ncat 1->0, len 0->1) and the test is renamed.

THE LOAD CHECK, feedback (a)+(WI-886's language question): `check_provider_operations` skipped any `Implementation.target` WHOLESALE — a claim about the CARRIER answering a question asked about an OPERATION. Being host-realized now buys exactly ONE thing the general rule withholds: a mapping on the SPEC's own member counts, which is the carrier dimension the flat index lacks and what a genuinely polymorphic implementation needs (the three filesystem stores lose retract/update/retrieve without it — MEASURED, 7 errors). WI-931's ZzNotAStore stays refused. NO LANGUAGE FILTER, deliberately: a LOAD check asks whether the PROGRAM declares an implementation, which a cpp mapping answers, while eval asks whether THIS runtime can call it (is_interpreter_mapped_op). That makes wi886_cpp_mapping_language_test load for the reason its header states instead of by accident, and its header is corrected.

PERF, per its feedback: the VALIDATED `(Symbol, HostFn)` list is derived ONCE per KB (a OnceCell beside host_op_mappings, invalidated by set_host_op_mappings) instead of once per interpreter — run_in_bridge_interp builds one per bridged evaluation, and the per-crossing cost was 3 String clones + a HOST_FNS key scan + an arity lookup + a canonical_sym hash PER MAPPING, over a list this ticket grew past 60.

FOUR THINGS THE TICKET DID NOT PREDICT, each measured. (1) A DECLARATION CAPTURES THE NAME IT WAS GOING TO SHADOW: declaring `Float.mul` made `import Multiplicative.{mul}` a 059 R4 no-override capture and refused 367 tests at the stdlib load. Repair is WI-876's own (delete the shadowed import — the two laws that write a bare `mul` have Float operands); int64 and bigint lost their `Additive.{add, sub}` imports the same way. (2) A DEFAULT BODY MOVES A RULE-BODY CALL INTO A DIFFERENT TYPE-CHECK SHAPE: `call_dispatch_shape` routes a DEFAULTED spec op to CallDispatch::Checked, and only the BodyLessSpecOp arm applied `undecidable_by_this_typer` LEAF BY LEAF — the Checked arm asked it of the whole error, which matches a bare exempt error and not a Multiple. So the WI-282 unresolved-dot-receiver exemption held for ONE exempt argument and lapsed for two. Giving `Additive.sub` a body moved `?dx = ?p1.position.x - ?p2.position.x` (the very site the exemption's doc names) onto that arm: 15 tests, the corpus among them. The arm now filters leaves and re-aggregates. (3) `stdlib/` ALONE IS NOT A PROGRAM: the binding blocks live in rustland/anthill-stl/anthill/, so a stdlib-only KB has no operation_map to register from. Invisible while the arithmetic sat on its spec ops. wi483_rule_body_method_eval_test was loading stdlib alone and evaluating `?b.value + 1` through the SLD->eval bridge — MEASURED holding the fixture fixed and varying only the directories: stdlib alone 1 solution, stdlib+bindings 0. It loads the full closure now, as WI-1103 already did for incremental_load_test. (4) A DELIBERATE ABSENCE NEEDED A NAME: cpp `String` omits toUpper/toLower on purpose (no C++17 Unicode case mapping; ASCII-only would be the silent divergence WI-884 refused) and the anti-drift check had no way to say so — DELIBERATELY_UNREALIZED_IN_CPP, each entry verified to exist on both sides so the list cannot rot. cpp Int64/Float gained add/sub/mul, which cpp-gen's operator table predicted ('when WI-880 moves the spec-op families per carrier, these follow').

PartialEq.eq/neq STAY on the spec op, per the ticket's own note, and the reason is now written at the site. builtin_eq is not the defect shape: it DISPATCHES through semantic_equal to the head carrier's own eq before deciding structurally — which is exactly why eq worked on a structural carrier when compare did not. And it cannot simply be re-keyed: equality fires from UNIFICATION as well as from a call (058 §3.7), where there is no call site and no dictionary, so a value-directed step has to exist whatever the registration says; keying it per carrier would ADD a channel, and would cost each scalar carrier a declared `eq` that 059 R4 then refuses as a capture of the PartialEq.{eq} import its own laws use (the Float.mul measurement above is that shape). NOT DONE and stated as such.

FEEDBACK (b) — THE PREDICATE SPLIT: not renamed. `op_has_runnable_body` already IS the body question by name and `op_is_executable`/`op_is_interpretable` are the invoke questions; what was missing is that the readers do not SAY which they mean. The two that would degrade SILENTLY — sem_eq_dispatch (resolve.rs) and semantic_equal (eval/builtins.rs) — are now annotated with the exact boundary: no carrier's `eq` is operation_mapped anywhere in the tree BECAUSE PartialEq.eq is the one registration this ticket kept, and whoever maps an `eq` owns those two lines. The typer's three static-pin gates are unaffected and measured so: Int64.add is body-less host-mapped now and `40 + 2` answers 42 through eval's value-directed path, which is Int64.gt's existing route.

CODE-REVIEW (high, 5 findings, each VERIFIED before fixing). ONE REAL BUG, and it is the memoization: host_op_registrations is a memo of host_op_mappings, and that field IS in kb_scoped_fields! while the memo was not — so a discarded layer's derivation survived the rollback. Worst case is a POISONED BASE, not a stale entry: an unknown host_fn is not a load error (only the runtime can judge another language's function set), the refusal is cached by design, and after the discard EVERY interpreter over the base KB fails permanently for a mapping the base never had — defeating kb_loaded's own contract. Driven by wi880_a_discarded_layers_operation_map_does_not_poison_the_base, and the back-out separates cleanly: dropping the field from kb_scoped_fields! fails that test's LAST assertion and only it. The per-interpreter re-derivation this ticket replaced was self-healing, which is why nothing noticed — an invariant safe by construction dying when the construction is removed. THE OTHER FOUR: the wrappers delegated to the shared arithmetic with NO LABEL, so an Int64 overflow reported op: 'Numeric.add' — a spec operation this ticket had just stopped implementing, in exactly the diagnostic the split exists to sharpen, and my own test asserted the VARIANT only so it could not see it (both arms now assert the name; eval_test::m3_int_add_overflow_errors is the independent confirmation, and it had pinned the old label); kernel-language.md §5.3 still described the soundness gap as live and named an empty migration population; the cpp test's new doc said String has no cpp binding block when it has one with two deliberate omissions; arithmetic.anthill's rule block still said sub_def 'cannot yet be a body' fifteen lines under the note saying it is one.

ALSO ADDED after the fact, because the first cut could not measure it: a_carriers_own_sub_beats_the_spec_derivation. add(a, neg(b)) and an ordinary sub AGREE AT EVERY VALUE, so a carrier writing a normal sub separates nothing and the test is green either way — the saturating carrier's member clamps at zero, so the row answers 0 if the member ran and -7 if the default body did. It matters beyond the fixture: if the default shadowed a carrier member, every `a - b` would cost an interpreter frame plus a neg where the host answers in one call, the regression WI-876 avoided by mapping Ord.max/min per carrier.

DOCS: kernel-language.md §10.2 (the softened 'nothing exercises that today' sentence is replaced by the live rule, the spec-mapping leg and the language decision) and §5.3; docs/design/058-implementation.md gains §10a. NOT DONE: the SLD-side registry is still hardcoded and still carries spec-op entries beside the carrier ones (WI-879), and this ticket ADDED {Int64,BigInt,Float}.{add,sub,mul} to it for the reason WI-876 added the comparisons — a bare `add(?n, 1)` in a rule inside sort Int64 now means Int64.add and would otherwise stop computing in a query.

