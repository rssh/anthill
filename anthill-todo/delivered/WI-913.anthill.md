```anthill
fact WorkItem(id: "WI-913", created: "2026-07-31T23:54:11Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-16T15:15:03Z"))

fact Feedback(workitem: "WI-913", author: "user", at: "2026-08-16T15:15:34Z")
```

## description

FOUR MORE host-supplied-name positions resolve absolute-only, each a WI-908 twin (code-read, NOT driven; from /code-review's altitude pass on the WI-908 diff). (1) `anthill-stl/src/reflect/builtins.rs::lookup_symbol_op` (~750): the anthill-visible `lookup_symbol("Foo")` errors with "'{name}' not in scope" while `try_resolve_symbol` consults NO scope — MESSAGE AND CODE DISAGREE (the WI-886 shape). It has `kb_mut`, so routing to `resolve_name_in_global` is a one-liner. (2)+(3) `eval/builtins.rs` `make_fn` (~3302) / `make_apply` (~3347): a functor name written as a `Value::Str` INSIDE an anthill program, resolved absolute-only — and `make_fn`'s own doc (~3290) says these are rule-head functors NOT scanned as definitions, i.e. exactly the names that will have no `by_qualified_name` entry under the spelling the program used. (4) `eval/builtins.rs::persistence_monotonicity` (~4252): 'a functor passed as its raw name string', absolute-only. Also `kb/mod.rs::make_sort_ref_by_name` (~5529) is absolute-or-`intern`, whose bare-intern branch is WI-894's collapse hazard. FIRST STEP: drive (1) — its message already claims the behaviour the code lacks, so a two-namespace fixture settles it. ACCEPTANCE: each position either reads the shared ladder or carries a stated reason at the site for asking a different question; cargo-test green via scripts/test.sh.

## Feedback

### 2026-08-16T15:15:34Z — user

DELIVERED 2026-08-16 (commit 3241b735, main). Full workspace 4936/0 green via rustland/scripts/test.sh.

THE TICKET WAS RIGHT ABOUT FOUR POSITIONS AND SHORT BY ONE. `anthill.reflect.lookup_symbol` has TWO backings: the eval-side `lookup_symbol_op` the ticket names, and the SLD-side `KnowledgeBase::builtin_lookup_symbol` (resolve.rs), registered for the identical qualified name at kb/mod.rs. Both were absolute-only. Fixing one would have left ONE declared operation answering two ways depending on whether it is called from an expression or as a goal — the WI-984 defect, documented 20 lines BELOW the very site the ticket named. Their failure shapes already differed (eval errors, SLD fails silently); that asymmetry is kept and stated, because a resolver builtin has no diagnostic channel.

THE PROPOSED FIX WAS NOT A WIDENING, and the ticket does not say so. Measured before implementing: the ladder ADDS the implicit tier (cons, nil, some, none, and, not, SortInfo, SortView, OperationInfo — bare names that denoted NOTHING absolutely, and exactly what a make_fn caller writes) and takes away the unmarked reading of the qualified-only kernel names.

I FIRST RECORDED THAT SECOND HALF AS A DROPPED CAPABILITY, repeating WI-908's framing, AND IT WAS WRONG. `..Member` and `..meta` DO resolve: since WI-1075 an unmarked dotted path is the RELATIVE reading and the root has its own spelling. Nothing became unreachable — a spelling moved. Corrected in the test, three doc comments and the spec sentence. The lesson is the one this repo keeps re-learning: an inherited justification is not a measurement, and WI-908's was five tickets stale.

ONE CLASS GENUINELY STOPS RESOLVING and I found it by CENSUS, not by fixture: a FIELD named by path. Over stdlib + anthill-stl, 2638 dotted qualified names, 441 unreachable through the ladder — 436 fields (refused in every reading as a category error, WI-751) and 5 internal. ZERO unexplained, zero bound to a DIFFERENT symbol. Kept as a test (`kb::wi913_host_name_ladder_tests`), because no fixture names more than a handful and a green suite could never have said it.

FROM /code-review, THE BEST CATCH, and it is my own recorded failure mode: the WI-714 LogicalQuery lowering guarded `make_sort_ref_by_name` with a `try_resolve_symbol` PRE-CHECK. Total only while both spellings asked one question — and routing the mint through the ladder made them differ, the ladder being strictly narrower. So the guard could admit a name the mint then interned as the phantom sort the guard exists to stop, and the Expr::Spliced typer arm reads inferred_type OVER expected, so the phantom would have overridden the runner's real hint SILENTLY. Fixed by deleting the second question rather than re-aligning it: `try_make_sort_ref_by_name` returns the resolution's own verdict, so the guard IS the mint. The divergence is now unrepresentable and therefore not drivable; the verdict it rests on is driven.

ALSO: `monotonicity` has TWO lookups and only one is host text — the second reads a Rust constant and rightly stays absolute (a distinction the ticket does not make). Its Str arm no longer folds a name error into `TypeMismatch{expected:"Symbol (functor)"}`, which used to report a misspelled functor as a wrong-KIND value. `make_sort_ref_by_name`'s intern fallback is narrowed, not deleted (no failure channel in `-> TermId`); it was firing on Modify and Error, minting bare twins beside the prelude sorts — WI-894's collapse from the other side. Two builtin_lookup_symbol unit tests went red and were FIXTURE ARTIFACTS: they registered `ns.Qux` with no `ns` symbol, a shape no load produces.

EFFICIENCY, measured because it looked hot and was not: the ladder is 4-8x the old hash lookup per call (41->190ns; 53->461ns dotted, paired in-process min-of-7), and `make_sort_ref_by_name` runs 83 TIMES in a full stdlib+stl load. ~11 microseconds. The typer does name every literal's sort through it, which is why I counted instead of reasoning.

CONTROLS: measured by restoring the four source files, NOT predicted — my first count was wrong in both directions. 9 of the 14 anthill-core rows fail pre-fix, 4 pass either way by design, 1 cannot run pre-fix (it names the review fix's new method). The anthill-stl row fails pre-fix too, measured separately by backing out only lookup_symbol_op's body so the test survived.

NO FOLLOW-UPS FILED. I censused all 393 `try_resolve_symbol` sites: every remaining non-literal argument is a symbol ROUND-TRIP (kb.qualified_name_of(sym) — which must stay absolute, since a round-trip through the ladder could bind a different symbol) or a Rust constant, EXCEPT persistence/term_ser.rs, which is WI-912 and already filed. WI-913's scope is closed.

