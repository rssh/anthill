```anthill
fact WorkItem(id: "WI-466", created: "2026-06-14T23:51:44Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-15T16:48:15Z"))

fact Tag(workitem: "WI-466", name: "typing")
```

## description

Typer GAP (WI-405 follow-up, same root cause): the (parameterized, sort_ref) arm of types_compatible calls sort_sym_compatible with SWAPPED args -- (expected_spec, actual_base) instead of (actual, expected) as the working (sort_ref, parameterized) sibling arm does (actual-side gets extract_sort_ref_sym(expected), parameterized_base_sym(actual), then sort_sym_compatible(kb, e, ab)). WI-405 FACET A added a CORRECTLY-directed sort_provides_admissibly(kb, ab, e) right beside this swapped nominal call, exposing the inconsistency. TWO latent defects: (1) FALSE-REJECT -- a parameterized actual whose base REFINES / is-entity-of a bare expected does not conform. Verified probe: sort Base; sort Refined { requires Base; sort T=?; entity makeRefined(x:T) }; operation f(p: Refined[T=Int64]) -> Base = p  => 'type mismatch in f.return: expected Base, got Refined[T = Int64]'. (2) FALSE-ACCEPT (soundness) -- the reverse direction: a Base[..] would wrongly conform to an expected spec that refines Base. FIX: in BOTH types_compatible_term_dispatch and its carrier peer types_compatible_view_structural, change the (parameterized, sort_ref) nominal check to sort_sym_compatible(kb, ab, e) / (ab, ev) (actual-base, expected) -- matching the sibling arm and the FACET A provider call. SOUNDNESS-affecting semantic change (removes a false accept) -> own focused change + a both-directions regression test + full-suite verify; NOT folded silently into WI-405. Rust-only (typer).

