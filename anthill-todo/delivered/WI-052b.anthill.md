## Attributes

- id: WI-052b
- created: 2026-05-03T17:09:44Z

- status: Delivered
- status_agent: claude
- status_at: 2026-05-03T13:55:00Z

- acceptance: cargo-test

- depends_on: WI-052

## Description

026.1 Q4 Stage B (delivered) — routing-side wiring for external-source backends. Adds the resolver path that turns a goal whose head functor has a registered route handler into ExternalRow candidates pulled from a Box<dyn ExternalStream>. Concrete pieces: (1) RouteHandler trait + RouteRegistry on KB (kb/route.rs); (2) Candidate::ExternalRow variant in kb/resolve.rs; (3) step_init eager-drain of the registered handler's stream after the discrim-tree query, with match_view + bind_value preserving Value::Entity / Value::Str lineage; (4) is_duplicate_projection skipped when σ has any non-Term Value binding (reify can't see those, so dedup would collapse genuinely distinct external rows); (5) public KB API: register_route_handler / route_handler_for. Acceptance: tests/route_dispatch_test.rs::stage_b_routed_goal_yields_external_rows — register a 3-row WorkItemBackend, query WorkItem(id: ?, description: ?), assert 3 distinct solutions surface as Value::Str bindings, and KB.term_store_len() unchanged from baseline. All workspace tests pass. Follow-ups (separate WIs): (a) lazy per-iteration stream pumping (current path is eager-drain — correct, but materializes a Candidate per matching row); (b) anthill-level retrieve(store, pattern) builtin matching the stdlib operation signature in stdlib/anthill/persistence/store.anthill (currently List[Term]; needs to evolve to Stream[Term] to align with Q4 — design decision pending); (c) consult anthill-level route(GoalSort(?))=Store rules at resolution time instead of the direct functor→handler registry (concrete-backend-driven).

