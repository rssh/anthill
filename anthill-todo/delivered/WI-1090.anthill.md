```anthill
fact WorkItem(id: "WI-1090", created: "2026-08-13T06:55:01Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-13T07:59:25Z"))

fact Feedback(workitem: "WI-1090", author: "user", at: "2026-08-13T07:31:50Z")
```

## description

A `===`-DEFINED FUNCTION IS DEAD ON ARRIVAL: `struct_eq` IS AN EQUATION CONNECTIVE ON THE PARSE SIDE AND NOT ON THE KB SIDE. `pratt::is_equation_functor` names three connectives (eq / unify / struct_eq, the desugars of `=` / `<=>` / `===`), and the parse-layer readers honour all three: `load::parse_equation_lhs` reads a `===` head's subject off its LHS and `scan_rule_goal` stamps that subject `SymbolKind::EquationFunctor`. Every KB-layer owner of the same question knows only TWO. `KnowledgeBase::is_equality_connective_functor` (kb/mod.rs) compares against `eq_connective_sym` / `unify_connective_sym` only — `cache_connective_syms` resolves `anthill.prelude.PartialEq.eq` and `anthill.kernel.unify` and nothing else — so `is_equational_head`, `is_equation` and `simp_equation_rids` all answer NO for a `===` head. MEASURED (WI-948, throwaway probe on a loaded stdlib KB): `sort S  rule { g948(?x) === ?x [simp] }  end` loads clean; `S.g948` is stamped `EquationFunctor` with ZERO clauses under it; the clause sits in the `anthill.kernel.struct_eq` bucket. Three consequences, all silent: (1) `simp_equation_rids` scans only the eq+unify buckets, so the `[simp]` tag can NEVER fire and the function has no meaning; (2) `is_equational_head` being false means WI-139's `unindex_functor` never runs, so the law STAYS indexed and drives automatic SLD rewriting — a `f(?a, ?b) === f(?b, ?a)` law loops, which is the exact hazard WI-139 exists to prevent, and the `=`/`<=>` spellings of the same law are protected from it; (3) `cites_a_relation` sees an EquationFunctor with no clauses, so citing `g948` draws WI-898's 'defined by equations … no defining equation for it can be found' — a message that is true and useless, since the equation is right there. NOT INTRODUCED BY WI-948, which only added the `is_minted` provenance guard; the split predates it and WI-948's review is where it was measured. WHAT THIS TICKET MUST DECIDE, because the two halves disagree on a DESIGN question and not on a constant: is `===` a DEFINING connective (then `cache_connective_syms` gains `anthill.kernel.struct_eq`, `===` laws become cite-required like their `=` twins, and `[simp]` fires them) or is it purely the TOTAL, carrier-agnostic STRUCTURAL TEST the spec calls it (§'`===` is a resolver builtin … never dispatches', eval/builtins.rs `builtin_struct_eq`) — in which case `is_equation_functor` must DROP it and a `===` rule head is refused rather than stamped. Whichever way it lands, the two sides must be read off ONE owner so a fourth spelling cannot re-open the gap. ACCEPTANCE: a driven test per consequence above (the [simp] fires or the head is refused; the law does or does not drive SLD; the citation diagnostic matches reality), the decision written into docs/kernel-language.md beside the equational-rules paragraph, and full workspace green.

## Feedback

### 2026-08-13T07:31:50Z — user

CORRECTION, measured during the fix: consequence (2) as filed is FALSE. The ticket claimed the un-unindexed law 'STAYS indexed and drives automatic SLD rewriting — a f(?a,?b) === f(?b,?a) law loops'. Driven on a GROUND goal the symmetry law makes true and structural identity makes false: 0 solutions WITH the law and 0 WITHOUT it. The resolver builtin answers a === goal before any clause is consulted (WI-899's stated behaviour), so the clause is INERT, not dangerous — consequence (2) is a wasted clause, nothing more. That claim came from the WI-948 review's reasoning and I filed it without driving it; it is pinned as wi1090_struct_eq_is_a_test_test::a_clause_on_the_struct_eq_builtin_never_decides_a_goal. Consequences (1) and (3) held as filed.

