```anthill
fact WorkItem(id: "WI-797", created: "2026-07-21T11:17:55Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-796"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-21T18:45:30Z"))

fact Feedback(workitem: "WI-797", author: "user", at: "2026-07-21T18:45:24Z")

fact Tag(workitem: "WI-797", name: "extent-seam")
```

## description

Wire the ExtentSource mount into resolution and load, per proposal 057 (docs/proposals/057-extent-read-seam.md) decomposition item 1 (the resolver/loader half; foundation = WI-796). BUILD: (1) DISCRIM MOUNT -- a store-owned functor (registered in kb.extents, WI-796) is mounted at its discrim functor node; resolver candidate-gathering (kb/resolve.rs step_init and the kb/mod.rs query_view path) reaching the mount delegates to the owning source query with the goal ground fields pushed down as a QueryPattern, yielding TAGGED candidates: Resident(RuleId) opened exactly as today, or Row(Value) entering the substitution via bind_value with no TermStore allocation. ONE candidate path -- no consumer branches on ownership (the WI-770 divergence lesson applied to retrieval). Lazy per-pump cursor advancement, NOT an eager drain. (2) RETIRE the two read-beside-discrim surfaces INTO query (057 stage R2): delete RouteHandler (kb/route.rs) and the eager Store::retrieve -> Vec<TermId>, both in-tree-only with no declared twin; the read registries fully merge into kb.extents. (3) LOADER read-side refusal -- a source-file fact (or a same-head bodied rule) whose functor is owned by a mounted source is a loud LoadError (owned by store X, seed through its own channel), the single-owner decision. ACCEPTANCE: a functor owned by a mounted, seeded InMemoryExtentSource RESOLVES end-to-end through SLD (a goal over it yields the seeded rows), interleaved correctly with resident rules for other functors; RouteHandler and the eager Store::retrieve are deleted with no caller remaining; a source-file fact for an owned functor is refused at load; cargo-test green via scripts/test.sh. DEPENDS WI-796.

## Feedback

### 2026-07-21T18:45:24Z — user

Scope decision (session 2026-07-21): criterion-2's 'eager Store::retrieve deleted' clause is DEFERRED to WI-780 (dep edge added). Store::retrieve (persistence/mod.rs:161) backs the LIVE persistence_retrieve builtin realizing the still-declared QueryableStore.retrieve op (stdlib store.anthill:85) -- NOT the claimed 'no declared twin'. Clean reroute needs the store_registry->kb.extents move = write-seam. WI-797 delivered: resolver discrim mount (QueryPattern pushdown, lazy per-pump match), FULL RouteHandler deletion, loader FunctorOwnedByExtent + register_extent_owner ResidentCollision refusals.

