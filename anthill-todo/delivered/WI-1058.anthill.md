```anthill
fact WorkItem(id: "WI-1058", created: "2026-08-08T14:24:23Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-09T09:41:19Z"))
```

## description

DELIVERED (2026-08-09). The general `Expr::Apply` in a rule body is decided, and what decides it is the POSITION it sits in — one distinction, not the two readings this ticket predicted.

THE TICKET'S MEASUREMENT DID NOT REPRODUCE, and the correction is the finding. Retaken by widening `call_dispatch_shape`'s Apply arm with no position awareness (stdlib + host bindings): 323 errors, 154 distinct — not 468/173. The split is inverted: 271 `expected List[T = Term]` + 21 `tuple` + 3 others, and only 28 rule-name subgoals (the ticket said 220). And 295 of the 323 come from ONE producer, the loader's synthesized `<Sort>.induction` (`emit_induction_rule`), whose body is `ho_apply(?P, ctor(…))` and `forall_impl(tuple(binders), tuple(ihs), tuple(goal))` by construction. There was no scattered 'reflect-argument' population.

DELIVERED: `dispatch_calls_in_occ` threads a `BodyPos` (Goal / GoalTuple / Value / Untyped) derived from the ONE slot table, `KnowledgeBase::goal_slot_readings` — which gained the two readings it never carried, a discharge's ANTECEDENTS (hand-read in `assumed_body_functors` until now) and its BINDERS (written down nowhere, which is why `tuple(?f1, …)` read as a call). At GOAL position the ladder is: connective → not a call; spec op → unchanged (WI-1026/1043/1056); resolver BUILTIN → read by the builtin (this is `ho_apply`: `BuiltinTag::HoApply` in a goal, the reflect IR ENTITY as a value — one name, two questions); entity → fact pattern, field-schema checked; SORT → a type / instance claim; else SUBGOAL, checked against the clauses its functor heads (`subgoal_shape_error`).

THE VALUE POSITION IS **NOT** TYPE-CHECKED, and that is measured, not cautious. Typing a data slot is wrong three ways, each found by driving the suite: it loses the EXPECTATION (27 failures across wi300/625/642/710/839/927/1040/1045), loses the binder SCOPE (wi620), and REWRITES the body (wi884: `holds(ite(true,10,20))` became `holds(10)` at load, so the rule answered under `simplify: false`). A data slot gets the one context-free check it can have — does its functor NAME anything (`data_functor_error`, sharing `undefined_functor` with WI-1034). That closes WI-895's remaining half; its pin and two WI-1034 rows were updated as their docs instructed.

ALSO: the ARITY half of the inherited `UnreducedEquationFunctor` exemption is closed — `?r = ite(?x)` against a 3-argument functor is refused through the same `unmatchable_shape_error` the subgoal check uses. The exemption itself STANDS: deriving a redex's TYPE from its `[simp]` clauses is the larger half and no ticket owns it. The WI-282 unresolved-dot-receiver exemption stands unchanged (it needs rule-head variable types).

/code-review (high) found four defects a green suite could not show, all fixed and each now a driven row: the type-term test was written for the earlier type-checking design and answered `true` for every `has_kind(Sort)` functor, so a free-standing or eponymous entity in a data slot switched the walk off for its whole subtree (a REGRESSION — a dot inside one stopped dispatching); the subgoal check lacked the hypothesis and tolerated-branch exemptions `undefined_rule_body_goals` holds, refusing programs §5.3 legislates (now `GoalCommit`); the refusal named the callee instead of the enclosing rule; and `Call` still stored `type_check_node`'s rewritten node.

COST at the acting arm: 242 newly decided sites on stdlib + host bindings (194 data-term, 31 subgoal, 17 fact-pattern), 416 on the whole corpus — ZERO reports at every tier (stdlib, host bindings, examples/, anthill-todo/, anthill-testcases/). Full workspace 4433 passed / 0 failed. Coverage + the six-column back-out matrix (driven, one revert each; the predicted table was wrong in seven cells): `wi1058_rule_body_position_test`. `docs/kernel-language.md` §5.3 states both the subgoal check and why a data slot is not typed.

