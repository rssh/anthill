## Attributes

- id: WI-603
- created: 2026-06-29T18:29:43Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-02T23:14:41Z

- acceptance: cargo-test

## Description

Type the rule body: stamp inferred_type on rule-body atoms from the op/field signatures (complete the untyped->typed transform for rules). TODAY rule-body typing is SELECTIVE: only dot subtrees (dispatch_rule_body_dots) get inferred_type; a plain f(?x,?y) atom is walked by collect_rule_var_types (which reads f's signature) but STAMPED BY NOBODY, so f(?x,?y) carries no inferred_type. PRINCIPLE (user): the op signature f(x:Int,y:Int) is the SINGLE source of truth for arg types -- do NOT add a separate per-rule var_types store (redundant); instead stamp the derived types onto the rule's already-stored body_nodes (NodeOccurrence.inferred_type is interior-mutable and persisted on RuleEntry), sourced from OperationInfo / entity_field_types, with the cross-occurrence UNIFICATION (the one genuinely new fact -- a var shared across f(?a,..) and g(?a,..)) living on the occurrence. Then EVERY consumer reads inferred_type off the occurrence instead of re-walking signatures: check_rule_typing (contradiction), dispatch_rule_body_dots (dot env), the resolver firing guard typed_pattern_bounds_hold, and the WI-602 rule-body precondition check. Subsumes the <=2x collect_rule_var_types recompute (called in check_rule_typing + dispatch_rule_body_dots, discarded both). UNBLOCKS WI-602 (the refutation-aware rule-body precondition check needs the call-site arg types). A concrete slice of WI-079 (full relational rule typing). CHEAP INTERIM (no typer change, no new storage): call collect_rule_var_types ONCE at load and share between the two passes. ACCEPTANCE: f(?x,?y) in a rule body carries inferred_type on its arg occurrences sourced from f's signature; the two collect_rule_var_types call sites read it instead of recomputing; cargo-test green.

