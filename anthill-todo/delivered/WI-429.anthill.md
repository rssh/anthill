```anthill
fact WorkItem(id: "WI-429", created: "2026-06-10T20:40:18Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-12T13:56:08Z"))

fact Tag(workitem: "WI-429", name: "typing")

fact Feedback(workitem: "WI-429", author: "user", at: "2026-06-10T22:01:39Z")

fact Feedback(workitem: "WI-429", author: "claude", at: "2026-06-12T13:56:08Z")
```

## description

Loader FIX (against delivered WI-376 classifier): an unresolvable Capitalized dotted name in TYPE position must be a LOUD load error, not a warning + degenerate nominal sort. try_expr_carried_projection (load.rs ~6108) gates on a VALUE head; a type-receiver head (MemStore.Key / Storage.Key / P.Key) falls through to remap_name, which emits only an 'unresolved name' WARNING and mints an opaque nominal sort literally named 'Storage.Key'. PROBED 2026-06-10 (/tmp/tm-probe, anthill check): FALSE REJECT -- probeConcrete(k: MemStore.Key) -> String fails 'expected String, got MemStore.Key' though MemStore provides Storage[Key = String] (semantically it IS String); FALSE ACCEPT -- probeBareSpec(s: Storage, k: Storage.Key) -> Storage.Key = k TYPECHECKS, both positions conflated to one meaningless global nominal (Scala-2 T#K carrier-conflation arriving by accident; two Storage params in one signature would wrongly share it). Violates loud-error-over-silent-skip in BOTH directions. FIX: in type position, a multi-segment name that classifies as neither a projection (value head; type head once WI-428 lands) nor a resolvable qualified sort ref is a HARD error naming the supported spellings (s.Key value path; P.Key after WI-428). Independent of WI-428 -- deliverable first; the diagnostic text just improves when WI-428 lands. Sweep existing stdlib/examples/todo .anthill files for any spelling that silently rode the nominal path (none expected; the form was unwritable).

## Feedback

### 2026-06-10T22:01:39Z — user

SCOPE ADDITION from WI-428's review round (2026-06-10): WI-428's concrete-head classifier converts an unresolvable Sort.Member dotted TYPE name into a stored RigidTypeProjection term; positions the typer ELIMINATES (op signatures, call sites, let annotations) now get a LOUD factually-correct error -- but a typo in a position the typer never eliminates (entity FIELD types, fact/constraint/rule type slots) loads SILENTLY (previously at least an UnresolvedName warning + degenerate nominal). WI-429's hard-error must therefore cover BOTH the remaining warning+nominal fall-throughs (non-Sort-headed dotted names) AND a post-load validation sweep of STORED RigidTypeProjection terms in non-eliminated positions (or run formation validation eagerly at end-of-load, when requires/provides info is complete).

### 2026-06-12T13:56:08Z — claude

DELIVERED 2026-06-12 (0174f5d + review commit; full workspace 53 binaries green twice). TWO LAYERS as the WI-428-feedback scope demanded. (1) remap_name's NotFound arm: new load-BLOCKING LoadError::UnresolvedTypeName for a dotted Capitalized-MEMBER name in type position — gated by a new Loader.in_type_position flag set by a type_expr_to_child wrapper (the WI-440 in_effect_absence pattern), so every type slot that flows through the TypeChild lowering (op sigs, fields, aliases, requires/provides, bindings) is covered; lowercase-member dotted names (value places, Modify[result.a]) keep the advisory path. Verified the severity split is REAL: is_load_blocking gates the warning-vs-error filter in anthill-cli run/main + anthill-todo main (the ticket's 'anthill check shows only a warning' probe), while load_all itself fails on any error — the CLI flows are where the nominal silently rode. (2) End-of-load formation sweep validate_rigid_projection_formations (typing.rs): the loader records every formed RigidTypeProjection + source span on kb.rigid_projection_formations (both make sites in try_rigid_type_projection); the sweep drains+dedups (hash-consed TermId) and re-runs the eliminator's own resolve_rigid_projection once requires/provides are complete (after resolve_instantiations, before type_check_sorts) — a malformed projection in a NEVER-ELIMINATED position (entity field, fact/rule type slot) now fails the load: typo'd member (MemStore.Kye) and bare-spec subject (Storage.Key, 'conflate distinct carriers') both rejected in field position; valid manifest (MemStore.Key->String) and rigid-param (P.Key under requires Storage[C=P]) field projections load CLEAN. SURFACED + FIXED in the process: field_is_recursive resolved every Simple field type through the ERROR-PUSHING remap_name — a duplicate resolution that pushed a spurious advisory UnresolvedName for ANY projection-typed field (P.Key/MemStore.Key failed even the load_all flow pre-WI-429!); now a side-effect-free resolve_in_scope probe (+ preserved by_qualified_name fallback for fully-qualified self-references, review fix). Sweep of stdlib/examples/todo .anthill files = the corpus-wide green suite (nothing rode the nominal path, as the ticket expected). Tests: wi429_unresolved_type_name_test (6). Diagnostic text improvement when projecting off ops etc. remains naturally tied to future classifier growth. NOTE: sweep errors render via TypeError::Other->LoadError::TypeMismatch as 'type mismatch in <Sort>.<Member>: expected a well-formed type projection, got <reason>' — blocking, span-bearing.

