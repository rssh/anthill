## Attributes

- id: WI-292
- created: 2026-05-23T09:20:30Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-29T09:26:24Z

- acceptance: cargo-test

- depends_on: WI-283, WI-572, WI-578, WI-584

- tags: wi502

## Description

Type-directed [simp] firing (CONSUMER of the WI-502 typed-term substrate). A requires-guarded [simp] rule (add(?x,0)=?x scoped to Numeric) fires by READING the carried type of the redex rather than being skipped (today: the typer fires via simp_fire_guard_holds over op bodies; the resolver equation_is_requires_guarded-skips). With typed values (WI-502) the matcher reads the redex carried type and checks subsort(type, Numeric) as an SLD query over sort/instance facts: a concrete carrier holds via a provides fact, an abstract carrier via the in-scope requires in the constraint store; fire / do-not-fire / SUSPEND as residual C when under-determined (never NAF-decide, WI-067 hazard). NO min_sort builtin (min_sort is REMOVED per WI-502, replaced by typed(value,env)); read the carried type. Depends on WI-502. See docs/design/constrained-term-substrate.md. Acceptance: cargo-test green.

## Changes

### 2026-06-26T13:21:47Z — feedback — user

This ticket IS WI-502 Step 5 (Shape B — type side-condition guard firing in the resolver). Stands on the Step-3 substrate (WI-572: static type on carrier + read API); now depends on it. Tagged wi502. All soundness lives in the suspend rule: min_sort(v) under-determined ⇒ suspend as residual C, never NAF-decide (the WI-067 var_ref-non-ground hazard, one level up). See docs/design/constrained-term-substrate.md Step 5.

### 2026-06-26T20:11:21Z — feedback — user

STEP-3 HANDOFF (WI-572 delivered 9119c41): Step 3 landed the carry (inferred_type survives open/subst via NodeOccurrence::rebuilt_expr) + the value-level read API min_sort_of_value(kb,σ,value). As WI-502 Step 5 you now OWN: (C) the compute-once entry — re-type the untyped prove/simplify entry: apply_eq_rules(term:TermId)/simplify take a bare TermId redex with NO inferred_type stamp; equation_is_requires_guarded (resolve.rs:2258) blanket-SKIPs requires-guarded [simp] rules — replace that skip by reading min_sort_of_value over the redex and fire-or-suspend. (E) a bare Value::Term redex has no carrier cache → min_sort_of_value returns None; add a NARROW external (type,inner) pair at the boundary (NOT a Value::Typed variant — 1844 Value:: arms + WI-538 silent-wildcard trap). store_sort_bound (typing.rs) currently returns a Type-constraint payload-head as the var sort (sound upper-bound read, but pin-vs-bound + the WI-067 flounder-don-t-decide suspend rule is yours to finalize). ALSO (from WI-571 Step 2): the Value::Term fact-match bind path goes via bind_compressed (loud-asserts on a constrained var) — route constrained-var fact binds through bind_waking or add the per-kind check, else a constrained query var binding a concrete fact term PANICS instead of suspending.

### 2026-06-27T20:29:00Z — feedback — claude

RE-SCOPE CLEANUP (post-2026-06-27 session): the min_sort removal + the typed-VALUE (type,TermId) carrier (this ticket's old deferred (E)) moved to WI-578; WI-292 now depends on WI-578 and is purely the [simp]-firing consumer that READS the carried type (fire / suspend / never NAF-decide). AUTHORITATIVE framing = the 2026-06-27 CORRECTION (typer-side firing, read the carried type, NO min_sort builtin); the earlier 2026-06-26 'Shape B guard firing in the RESOLVER / min_sort under-determined' feedback is SUPERSEDED. Circular dep on the WI-502 umbrella removed (the umbrella completes WHEN this consumer lands).

### 2026-06-28T06:18:04Z — feedback — claude

WORKED ACCEPTANCE CASES (dormant stdlib rules to activate), found 2026-06-28.

CORE (type-directed -- the WI-292 case): UNCONDITIONAL equational rules implicitly guarded by a SORT-LEVEL `requires`, so the resolver skips them via equation_is_requires_guarded (they fire in the typer via simp_fire_guard_holds, not the resolver):
- Set -- set.anthill:6 declares `requires Eq[T]`, so ALL its equational rules (set.anthill:17-23) are dormant in the resolver: member(?x, insert(?s,?x)) <=> true; insert(insert(?s,?x),?x) <=> insert(?s,?x); insert(insert(?s,?x),?y) <=> insert(insert(?s,?y),?x); member(?, empty()) <=> false; subset(empty(),?) <=> true; union(empty(),?s) <=> ?s; union(?s,empty()) <=> ?s.
- Map -- map.anthill:7 declares `requires Eq[T = K]`, so ALL its equational rules (map.anthill:46-56) are dormant: get(empty,?) <=> none; get(put(?m,?k,?v),?k) <=> some(?v); get(put(?m,?k2,?v),?k) = get(?m,?k); contains(?m,?k) <=> neq(get(?m,?k),none); remove(empty,?) <=> empty; get(remove(?m,?k),?k) <=> none; size(empty) <=> 0.
~14 real stdlib rules. FIX: the resolver reads the carried type (WI-578 substrate) and HONORS the sort `requires` -- replacing the blanket equation_is_requires_guarded skip (resolve.rs). NOTE: Set/Map keep the requires at SORT level deliberately (a set needs element-Eq, a map needs key-Eq), unlike List which op-scoped its Eq (WI-562) -- so HONOR the sort requires, do not op-scope it.

SIBLING GAP (NOT type-directed -- a separate firing concern): guarded equations with an EXPLICIT `:- guard` (mostly VALUE guards) are not even indexed, because is_equation requires an EMPTY body (043 indexing note). See docs/design/constrained-term-substrate.md "Conditional rewrite rules". Examples:
- stream.anthill:127-134 -- head/tail/isEmpty/headOption guarded by splitFirst(?s) = some/none (file comment L40 confirms head is "NOT yet wired").
- field.anthill:25-27 -- mul(div(?a,?b),?b) = ?a ; div(?a,?b) = mul(?a,recip(?b)) ; mul(recip(?a),?a) = 1, each guarded by neq(?,0).
- list.anthill:139 -- nth(cons(?_,?t),?i) = nth(?t, sub(?i,1)) :- gt(?i,0).
FIX for the sibling: (a) index guarded equations (drop the is_equation empty-body requirement); (b) evaluate the guard post-match -- value guard by resolution, type guard via the carried type. Likely its own WI; recorded here because it shares the firing machinery.

### 2026-06-29T04:05:11Z — feedback — claude

WI-578 HANDOFF (substrate DELIVERED + closed 2026-06-29): the typed-VALUE substrate is COMPLETE -- typed(value,env) (total, carrier-agnostic, correct incl seq/tuple literals), the ty carrier on Value::Term/Entity/Tuple + inferred_type()/ty() readers, value_type_term, Value::typed_term ctor, min_sort REMOVED. Per the 2026-06-29 boundary decision (close WI-578 as substrate-complete), WI-292 now ALSO OWNS the prove/simplify-entry wiring + the 4 carry-sites that were WI-578's deferred consumer-coupled tail (inseparable from this consumer, testable only here):
(i) ENTRY-WIRING (the (C) compute-once entry, already nominally yours): run typed(kb, subst, &Value::term(redex)) at the apply_eq_rules/simplify entry so the redex carries its type, then in equation_is_requires_guarded (resolve.rs ~2258) READ the carried type (value.ty() / value_type_term) instead of blanket-SKIPping requires-guarded [simp] rules -- discharge subsort(typeof(redex), Bound) by SLD over sort/instance facts -> FIRE / DONT-FIRE / SUSPEND as residual C (never NAF-decide, WI-067 hazard).
(ii) CARRY-SITES (carry ty, never drop -- value-level analog of WI-572 rebuilt_expr; M6): once (i) stamps ty, the 4 reconstruction sites that currently write/ignore ty must carry+sigma-refine it or the WI-502 drop-bug returns: reify_value (kb/mod.rs ~2415; reify CHANGES shape -> re-type via typed(), not verbatim copy), close_value_head_debruijn (kb/mod.rs ~2533; DeBruijn-close ty too), rewrite_ref_value (node_occurrence.rs ~3405; rekey ty refs too), walk_value_to_resolved (typing.rs ~14849; sigma-refine ty). INERT/untestable until (i), which is why they ship here.
ALSO available: broader Value::with_ty / typed Entity-Tuple builders (companion to Value::typed_term) -- add WITH the carry-sites. Acceptance cases (~14 dormant Set/Map sort-requires rules + explicit-:- guard siblings) recorded above. Design: docs/design/constrained-term-substrate.md.

### 2026-06-29T05:12:01Z — feedback — claude

PHASE-1 DESIGN (investigated 2026-06-29, code reverted pending a prerequisite). The type-directed guard check is BUILT + SOUND but BLOCKED on a GENERAL equational-rewriter gap (filed as the new prerequisite, this ticket now depends on it): the resolver apply_eq_rules cannot instantiate VARIABLE-RHS [simp] rules -- op2(?a,?b)=?a makes simplify(op2(5,7)) yield Var(DeBruijn(1)) not 5. PROBE CONFIRMED the gap is general (reproduces with a NON-requires-guarded sort pick(?a,?b)=?a), so it is pre-existing rewriter debt, not WI-292 scope.

THE GUARD-CHECK DESIGN (re-apply once the prerequisite lands; mirror the typer simp_fire_guard_holds via a SHARED CORE): (1) extract simp_guard_holds_core(kb:&KB, functor:Symbol, arg_carrier_sort: impl Fn(usize)->Option<Symbol>) -> bool from simp_fire_guard_holds (typing.rs ~20684) = the lookup_spec_op_dispatch + carrier-param + sort_provides decision, parameterized over how each arg carrier sort is read. (2) typer simp_fire_guard_holds passes |i| pos_args.get(i).and_then(|a| a.inferred_type()).and_then(|t| sort_functor_of_view(kb,&t)). (3) NEW pub(crate) fn simp_requires_guard_holds(kb:&mut KB, redex:TermId, subst:&Substitution)->bool: match get_term(redex) Fn{functor,pos_args}(non-Fn=>true); precompute arg_sorts:Vec<Option<Symbol>> = pos_args.map(|tid| sort_functor_of_view(kb,&value_type_term(kb,subst,&Value::term(tid)))) (needs &mut kb), then simp_guard_holds_core(kb,functor,|i| arg_sorts.get(i).copied().flatten()). (4) resolve.rs:2258 replace `if equation_is_requires_guarded(rid){continue}` with `if equation_is_requires_guarded(rid) && !super::typing::simp_requires_guard_holds(self,current,&Substitution::new()){continue}`. SOUNDNESS: value_type_term is TOTAL -> under-determined arg -> ?_ -> no sort head -> None -> dont fire (sound suspend, never NAF-decide, WI-067). The NEGATIVE case PASSED in testing (op2 over Bool -> not fired). TEST: flip wi283 resolver_skips_requires_guarded_equation -> resolver_fires_..._when_carrier_provides_spec (op2(5,7) -> VALUE 5; compare by VALUE not TermId, the fired RHS neednt equal the test five TermId) + add ..._does_not_fire_..._when_carrier_lacks_spec (op2(true,false) stays).

PHASING (Magma SRC in wi283_type_directed_guard_test.rs = phase-1 acceptance): PHASE 1 = prerequisite (var-RHS instantiation) + the guard check above -> fires SPEC-SORT-dispatched rules (Magma.op2, Numeric.add; lookup_spec_op_dispatch succeeds). PHASE 2 = CONTAINER-SORT-requires (Set/Map cases: member/insert/union/get are NOT spec ops -> lookup_spec_op_dispatch=None -> simp_fire_guard_holds returns guard-free -> typer fires them unconditionally); they need a DIFFERENT check (the sorts type-param element provides the sorts requires spec Eq), a distinct mechanism from spec-op dispatch -- design separately. Resolver-stricter is fine: a well-typed Set has Eq satisfied; only under-determined queries differ -> dont fire = sound. PHASE 3 = WI-578 deferred tail (4 carry-sites + compute-once typing at the entry) = EFFICIENCY only; the phase-1 on-demand value_type_term is correct without it.

