## Attributes

- id: WI-882
- created: 2026-07-30T20:11:34Z

- status: Delivered
- status_agent: claude
- status_at: 2026-09-19T09:18:07Z

- acceptance: cargo-test

## Description

THE STDLIB'S 11 DENIAL CONSTRAINTS ARE INERT, AND `kernel-language.md` §8.4 PROMISES THEY ARE NOT — decide which way the divergence resolves, then make the stdlib say it once in a channel that fires. FOUND by a user question: 'I see constraint statement in Float. Where is constraint described in kernel-language.md?', followed by 'we have precondition in operations and can use preconditions directly. Or error effect if x < 0 (as div)'. Both halves of that turned out to be exactly right. WHAT THE SPEC SAYS. `constraint` is documented in three places and they agree with each other: §5.3 (`constraint I :- G` = integrity constraint, invariant I must hold when guard G holds — listed under Rule because it is sugar over one), §6.2 with the grammar and the desugaring (`constraint non_negative: gte(balance(?a), 0) :- balance(?a, ?b)` -> `rule non_negative: ⊥ :- balance(?a, ?b), lt(balance(?a), 0)`), and §8.4 'Constraint Enforcement': 'Constraints (denials) are checked whenever a new fact is asserted. … If the body is fully satisfied, the assertion is REJECTED.' WHAT THE KERNEL DOES. Nothing. `load_constraint` (kb/load.rs) says so in its own comment: 'Historical behavior: store an inert Constraint(head:, guard:) fact. Denial/invariant constraints are NOT registered as guards (WI-023 wires only the quantified forms; enforcing the existing denial constraints is a separate concern with its own regression surface — THE STDLIB RELIES ON THEM BEING INERT TODAY).' Only the QUANTIFIED forms (no/lone/one/some/forall + aggregations) reach `check_all_guards`. MEASURED, three ways, each loading CLEAN: (a) `constraint always_false: gt(0, 1)` — guardless and plainly false; (b) a fact-guard denial in §6.2's own documented shape with a VIOLATING fact (`constraint non_neg: gte(?v, 0) :- bal(?v)` + `fact bal(v: -5)`); (c) the stdlib's own `constraint mod_positive: gt(?b, 0) :- mod(?_, ?b)` — `mod(5, -3)` evaluates to 2 with no complaint. THE 11: int64 (in_bounds, div_nonzero_primary, div_nonzero, rem_nonzero, mod_positive), float (recip_nonzero, div_float_nonzero, sqrt_nonneg, log_positive, log10_positive, log2_positive). Every one of them is a PRECONDITION written in a channel that does not fire. THE TWO LIVE CHANNELS ALREADY EXIST and are enforced at LOAD — MEASURED: an operation-level `requires` (`operation root(a: Int64) -> Int64 requires gte(a, 0)`) refuses the call site: 'expected precondition gte provable at the call site, got unsatisfied precondition'; and a GUARDED ERROR EFFECT (`div(a, b) -> Int64 effects { Error[DivisionByZero] :- eq(b, 0) }`) propagates to the caller: `div(1, 0)` refuses with 'undeclared effect: Error[T = DivisionByZero]'. NOTE `Int64.div`/`rem`/`mod` ALREADY DECLARE BOTH — the live effect row AND the dead constraint — so the stdlib states the same precondition twice, once in a channel that works and once in one that does not, which is precisely how a reader is misled into writing the dead one (as `float.anthill` did: its six constraints have NO effect-row twin, so `sqrt(-1.0)` is unguarded by anything). THE DECISION THIS TICKET OWNS, and it is a decision, not a cleanup: EITHER (1) implement §8.4 — register denial constraints as guards and enforce them, which the loader comment warns has 'its own regression surface' and would need the 11 re-checked (`in_bounds` over every Int64 is not obviously a decidable post-load check); OR (2) accept that a denial constraint over an OPERATION APPLICATION is not the mechanism, delete the 11, express each as a `requires` precondition or a guarded `Error` effect (whichever the operation's totality calls for — `div` already shows the pattern), and CORRECT §8.4, which currently describes behaviour the kernel does not have. Option 2 is the smaller change and matches what the working code already does; option 1 is what the spec says. Either way §8.4 and the loader must stop disagreeing. NOTE a THIRD reading the spec does not document at all, and which the 11 rely on: §6.2's guard is a FACT PATTERN ('when this fact exists'), while these guards are OPERATION APPLICATIONS ('wherever this call is formed'). If option 1 is taken, that reading has to be specified — it is a different quantification over a different domain. ACCEPTANCE: the 11 are each resolved explicitly (enforced, or replaced by requires/effects and deleted); `sqrt(-1.0)`, `log(0.0)` and `recip(0.0)` are guarded by something that FIRES; §8.4 matches the implementation; a test drives one guarded operation per mechanism; full workspace green via rustland/scripts/test.sh. REFERENCE: kb/load.rs `load_constraint`'s ConstraintBody::Denial arm and its comment; WI-023 (which wired the quantified forms only); WI-881 (`Float`'s unbacked operations — the same file, the same 'declared but not real' shape).

## Changes

### 2026-07-30T20:10:58Z — feedback — user

DECIDED BY THE USER: option 2 — DELETE. 'solution is delete - we don't want duplication.' So this ticket is no longer a choice between implementing §8.4 and removing the constraints; it removes them, and §8.4 is corrected to match the kernel rather than the kernel changed to match §8.4. TWO GROUPS, and they are not the same job. (a) THE FIVE IN `int64.anthill` ARE PURE DUPLICATION and delete outright: `div_nonzero_primary`, `div_nonzero`, `rem_nonzero` and `mod_positive` each restate a precondition the operation ALREADY declares as a live guarded `Error` effect (`div`/`rem`/`mod`/`divExact` all carry `effects { Error[DivisionByZero] :- eq(b, 0) }`), so deleting them loses nothing — the working channel stays. `in_bounds` (`gte(?n, minValue), lte(?n, maxValue)`) is the odd one: it is guardless, it is not a precondition of any operation, and it is not obviously checkable at all (it quantifies over every Int64 value); it should go too, but say WHY in the commit rather than lumping it with the four. (b) THE SIX IN `float.anthill` ARE NOT DUPLICATES — they are the ONLY guard those operations have, and deleting them bare would leave `sqrt(-1.0)`, `log(0.0)`, `log10`, `log2`, `recip(0.0)` and float `div` guarded by nothing. Each needs its live replacement written FIRST, in whichever channel fits: a guarded `Error` effect for the partial ones, following `Int64.div`'s exact pattern, is the closer match to what the constraint was trying to say ('this call is an error when the guard holds') than an operation-level `requires` ('the caller must prove this'), and it is what the user's own message named ('Or error effect if x < 0 (as div)'). NOTE the ordering dependency with WI-881: five of those six operations (`sqrt`, `log`, `log10`, `log2`, `recip`) have NO IMPLEMENTATION AT ALL today, so 'guard them' and 'back them' are one piece of work on the same lines of the same file — do them together or WI-881 will re-touch every one. `div` is the exception: it IS backed, so its float constraint can be replaced with an effect row immediately. AND the doc half is not optional: §8.4 currently tells a reader that denial constraints are enforced on every fact assertion. Once the stdlib stops using them, that paragraph is the only thing still claiming they work — it must say what `load_constraint` says, namely that denial/invariant constraints are stored inert and only the QUANTIFIED forms (`no`/`lone`/`one`/`some`/`forall` + aggregations) are enforced by `check_all_guards`. Whether the surface should keep admitting a form that does nothing is a separate question this ticket may raise but need not answer.

### 2026-09-16T18:38:43Z — feedback — user

CORRECTION TO THIS TICKET'S OWN FEEDBACK, from the closure of WI-874 (deleted 2026-09-16): `mod_positive` IS NOT A DUPLICATE, and deleting it on the "pure duplication" reading silently drops a claim. The feedback above groups the five int64 denials as "PURE DUPLICATION — each restates a precondition the operation ALREADY declares as a live guarded `Error` effect". That holds for `div_nonzero_primary`, `div_nonzero` and `rem_nonzero` (all three restate `eq(b, 0)`). It does NOT hold for `mod_positive`: `constraint mod_positive: gt(?b, 0) :- mod(?_, ?b)` (int64.anthill:106) forbids EVERY NEGATIVE DIVISOR, while `mod(a, b) ... effects { Error[DivisionByZero] :- eq(b, 0) }` (int64.anthill:87) forbids only ZERO. The two are different statements about different domains, so the effect row is not "the working channel that stays" for `mod` — it is a WEAKER channel, and the delete loses the negative-divisor claim outright. THIS TICKET ALREADY MEASURED THE CONSEQUENCE without connecting it to this line: its own body records `mod(5, -3)` evaluating to 2 with no complaint, which is exactly `mod_positive` being inert. DECISION OWED BEFORE THE DELETE: is a negative divisor legal for `Int64.mod`? The implementations already disagree and neither matches `gt(b, 0)` — eval `int_mod` (eval/builtins.rs:1217) guards ZERO only and overflow-panics on `i64::MIN mod -1`, while the resolver`s `BuiltinTag::Mod` (kb/resolve.rs:5768) is `checked_rem_euclid`, safe on that overflow but dropping its answer as `no solution` (see WI-875). If negative divisors ARE legal (Euclidean `mod` is well-defined for them, which is what both implementations actually compute), `mod_positive` is WRONG rather than redundant and the commit should say so; if they are NOT, the replacement is a second guarded element or a `requires`, not silence. Either way the two implementations should be made to agree in the same change. PROVENANCE: WI-874 proposed extracting a partial op's runtime guard from its declared conditional effect. Closed unimplemented: measured (five `anthill load` fixtures, 2026-09-16) that a declared guard is an UPPER BOUND, not a truth — a `provides` override may DROP a guarded `Error` silently (loads clean, no warning) while ADDING one is refused ("effects must not widen"), and a `requires`-shadow is an unrelated operation whose precondition may be STRONGER (loads with only a shadow warning; strengthening on a real override is refused). So a call-site guard read off the spec over-raises for a carrier that declared the operation total. The declaration coincides with the implementation only for a CARRIER-OWN operation, which necessarily already has one (WI-880/881 make an unbacked body-less operation a load error) — leaving nothing to extract. Also §5.5 (kernel-language.md:2990) states outright that guarded effects are "optional precision, NOT preconditions or proof debts", so the mechanism would have needed a spec change to be admissible at all. The language's precondition channel is the operation-level value precondition (§5.4), which is what this ticket's option 2 should reach for wherever an effect row is the weaker statement.

### 2026-09-19T09:17:56Z — feedback — user

DELIVERED in 5543ae2d, and TWO CLAUSES OF THIS TICKET'S OWN ACCEPTANCE WERE UNSATISFIABLE AS WRITTEN — recorded here because the reason is a dependency that landed after the text was, not a judgement call.

(1) 'sqrt(-1.0), log(0.0) and recip(0.0) are guarded by something that FIRES' IS DROPPED, and nothing replaces it. That clause, and the 2026-07-30 feedback's 'the six float constraints are NOT duplicates — they are the ONLY guard those operations have', were both TRUE WHEN WRITTEN: the six operations were backed by NOTHING (this is the 24-operation hole WI-881 found). WI-881 then backed them with the IEEE intrinsics — float_sqrt IS f64::sqrt, float_log IS f64::ln, float_div IS x / y, recip inlines to div(1.0, ?a) — and IEEE is TOTAL. MEASURED end to end via anthill run: sqrt(-1.0) is NaN, log(0.0) is -inf, recip(0.0) and div(1.0, 0.0) are +inf. None of them raises. So the six were not under-enforced preconditions, they were FALSE claims about a total operation, and they deleted BARE. Guarding them would ADD a partiality the carrier does not have and refuse programs IEEE defines; a 'requires gte(a, 0.0)' on sqrt would be the same mistake spelled in a channel that works, which is worse than one that is merely inert.

(2) 'a test drives one guarded operation per mechanism' IS ALREADY SATISFIED ELSEWHERE, not by new work. After the delete the only surviving mechanism in int64/float is the Error[DivisionByZero] effect row, which wi066_division_effect_test has always driven. What the change actually owed was the CONTROL — a pin on the behaviour the deleted rows contradicted, so the same claims cannot creep back as prose. That is wi882_deleted_denials_test.

THE COUNT WAS 12, NOT 11. This ticket inventoried int64 (5) and float (6) before division.anthill's 'constraint mod_nonneg: gt(?b, zero) :- mod(?_, ?b)' was written. That row is the generic twin of mod_positive — division.anthill's own comment said the two 'agree deliberately' — so it is false for the same Euclidean reason and went in the same change.

THE 2026-09-16 CORRECTION WAS RIGHT ABOUT mod_positive AND STALE ABOUT THE ENGINES. It is right that mod_positive is not a duplicate: gt(?b, 0) forbids every negative divisor where the effect row forbids only zero, so deleting it on the 'pure duplication' reading would have dropped a claim silently. Its premise that 'the implementations already disagree' is no longer true — WI-875 made eval int_mod and the resolver's BuiltinTag::Mod both rem_euclid with the same explicit -1 arm. So the decision it demanded has ONE answer, not two: a negative divisor IS legal, and mod_positive was WRONG rather than redundant. MEASURED in both engines: mod(5, -3) is 2.

AND THE REVERSAL IS REFUSED BY THE LOADER, which is a stronger argument than anything this ticket made and was found by BUILDING it rather than reasoning about it. Restating mod_positive as a live 'requires gt(b, 0)' on Int64.mod produces, besides the three expected call-site refusals, a fourth load error nobody wrote a call site for: "'anthill.prelude.Int64' overrides 'anthill.prelude.EuclideanDomain.mod' but does not refine it: it strengthens the precondition". An override may only WEAKEN a precondition and EuclideanDomain.mod declares none, so gt(b, 0) was never admissible on this carrier in a live channel AT ALL. The denial channel accepted it only because it was inert — being unenforced is also being unchecked, which is how a claim no live channel will admit survives indefinitely.

KEPT, and the ticket did not ask about it: WeakOrd.compare_congruent, now the only plain denial in the library. It is a LAW about what an implementor may SUPPLY, not a precondition on what a call site may PASS, so neither live channel can address it — requires is a call-site obligation, an effect row a per-call guard. It waits on the verifier layer.

THE DOC HALF WAS ALREADY DONE before this change started: §8.4 and §6.2 already said denials are stored inert and only the quantified forms are enforced. What they still did was point at WI-882 as pending; they now record what each of the twelve turned out to be. 048's open question C is marked settled — its 'keep the constraint as an integrity guard' lean was never available, because a plain denial is not one.

NOT DONE, DELIBERATELY, and stated at the site so it does not read as an oversight: Float.floor/ceil/round are the genuinely partial operations on that sort, they still have no effect row, and they DO raise (MEASURED: floor(div(1.0, 0.0)) is 'error: Float.floor: integer overflow'). WI-881's comment called that 'WI-882's shape'; it is not. Overflow is not an anthill effect at all — unlike DivisionByZero, which routes to a real Error[DivisionByZero], it is a raw EvalError with no payload sort in effects.anthill and no routing — and the identical gap covers every checked Int64 operation (add/sub/mul/neg/abs). Nor is the guard writable today: '|a| >= 2^63' needs an exponent float literal and '1e300' is a syntax error. That is a separate piece of work whose real subject is 'Overflow is not an effect', not 'floor lacks a row'.

Green: rustland 7136 passed / 0 failed across 36 binaries (rustland/scripts/test.sh); scaland 575 / 0 (sbt testFull). /code-review at high effort: no findings.

