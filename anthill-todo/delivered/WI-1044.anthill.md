```anthill
fact WorkItem(id: "WI-1044", created: "2026-08-07T10:57:40Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-1037", "WI-1042"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-09T21:27:22Z"))

fact Feedback(workitem: "WI-1044", author: "user", at: "2026-08-09T21:27:55Z")
```

## description

BUG (058; /code-review 2026-08-07 finding 1, CONFIRMED BY DRIVING): a TOP-LEVEL QUERY on a defaulted spec op folds the spec DEFAULT while the identical goal as a rule body dispatches the supplied implementation — WI-1026 created a rule-body/query divergence for one goal text, and with two suppliers the load refusal is silently BYPASSED on the query path. DRIVEN on the WI-1026 one-supplier fixture: Desc.describe(leaf(), ?r) as a rule body answers Int(7) (the supplied impl); as a top-level query via kb.resolve / the CLI it answers Int(1) (the spec default) — both definite, one goal text, two answers. MECHANISM: query goals and runtime-asserted rules never pass type_rule_bodies, so their occurrences carry NO CallClass; the WI-938 hook materializes an unstamped occurrence (resolve.rs:1293-1320), and reduce_op_value computes op = classified_apply_target().unwrap_or(functor) (resolve.rs:6143) — unstamped falls to the spelled functor and folds op_body_node(spec_op), the default. OWNERSHIP MAP (so this is not re-diagnosed): WI-1034 owns the dangling-functor population, WI-1036 the builtin-clause blast radius, WI-1037 the ConcreteApplyWithin fold — NONE owns the query population; this ticket does. WORK: give the query/materialized population the same classification the typer gives rule bodies — stamp at the WI-938 materialization, or classify on demand in reduce_op_value when no stamp exists, through WI-1042 shared gate helper so the condition is written once — and the same TIE behaviour: a two-supplier carrier REFUSES at the query (loud, naming both candidates by supply route; a query has no load moment, so the call is its load moment). DEPENDS on WI-1037: the stamp CREATES a ConcreteApplyWithin population on the query path (a supplied impl whose parent sort declares requires), which must decline the structural fold and reach the bridge — without WI-1037 the stamp would route those calls into a fold that runs a body with no dictionary. DEPENDS on WI-1042 for the shared gate. ACCEPTANCE (each test says which case fails when backed out): the one-supplier fixture answers Int(7) via top-level query (control: backed out, Int(1)); the two-supplier fixture refuses at the query naming both candidates (control: backed out, silently answers the default); rule-body behaviour unchanged (wi1026 suite green); the WI-938 hook path is the one exercised (assert the goal reached materialization, not a pre-typed rule). REFERENCE: /code-review 2026-08-07 finding 1; WI-1026 sec 20; WI-938; resolve.rs:6143.

## Feedback

### 2026-08-09T21:27:55Z — user

DELIVERED, with the ticket's own scope claim REFUTED BY DRIVING and one delivered ticket's controls consumed.

(1) THE POPULATION IS WIDER THAN 'never passes type_rule_bodies'. A STORED rule body passes that walk and still comes out Unclassified whenever the typer could not pin the carrier (rule via(?x,?r) :- Desc.describe(?x,?r), receiver bound only by the caller — 058 §3.3). Classifying on demand fires there too, and MEASURED it moved two delivered wi1040 assertions that pinned 1: the_same_clause_without_require_runs_the_spec_default and the_check_only_spelling_is_unchanged. KEPT, because 1 was the same defect one spelling over — eval's step 3 (resolve_carrier_override_by_value) has ALWAYS dispatched that call by value, so an operation body answered 7 where the identical unpinnable rule-body call answered 1. Narrowing to 'only what the WI-938 hook built' needs a flag threaded from one caller, i.e. a gate keyed on WHERE the call is written. Both wi1040 tests re-aimed at their sites; the discrimination its headline lost moves to a_two_supplier_carrier_dispatches_silently_through_the_dictionary, which was documented 'PASSES EITHER WAY by design' and is now that file's live control, with its un-woven twin asserted at the site rather than predicted in the header.

(2) THE TICKET NAMED TWO PIECES; THERE WERE THREE. runtime_carrier_sort excluded Value::Node while its own callee value_functor answers for one ('an occurrence lowers too'). The resolver hands its arguments in the occurrence carrier, so without that arm the whole fix is INERT — WI-1016's both-carriers-key-alike, unapplied. Listed as its own back-out column because it is an independent edit in another file.

(3) THE REFUSAL COULD NOT GO WHERE THE ANSWER WENT. The resolver has no diagnostic channel (BuiltinResult is Success/Bindings/Delay/Failure; bridge_op_to_eval residualizes AmbiguousSpecOpDispatch to None BY DESIGN, WI-483). So the tie is refused at the query's own load moment — KnowledgeBase::ambiguous_query_dispatch, read by the CLI ahead of the RESOLVE run, beside ambiguous_query_names and for its stated reason. RESIDUE, stated at the site: a tie whose carrier is bound only mid-search is silent (the resolver declines, so never the default — but no moment is left to be loud at). Same open half WI-938's feedback records.

(4) FIRST sigma-DEPENDENT WRITE to occurrence state. classify_pin_or_apply_within writes a RefCell on a shared Rc<NodeOccurrence>; every earlier writer was the typer, whose verdict is sigma-independent. 'One occurrence is never reduced under two sigma' becomes load-bearing here for the first time. Holds because each activation gets its own occurrence (WI-938 rebuilt_expr per attempt; with_fresh_vars/body_rename substitute rather than share) — DRIVEN, not argued: a_backtracked_receiver_reclassifies_per_carrier, two carriers answering 7/5, goal shape and operand fold, both clause orders.

(5) /code-review high found FIVE, all fixed inline: runtime_carrier_sort matched the RAW value so the carrier algebra was never cancelled (a spliced Map named nothing where a raw one names Map); 'only a CONSTRUCTOR now answers' was too strong — occ_head routes reflect-WRAPPED forms through wrapped_expr_head, so a node-carried lambda heads as a real anthill.reflect.Expr constructor and names THAT carrier where the same lambda as Value::Closure names Function (latent: reflect.Expr provides no spec; recorded at the arm rather than excluded by name); two stale invariant paragraphs this change falsified (value_functor's 'runtime_carrier_sort gives Node a fixed answer BEFORE it calls this' and that function's own 'raw node maps to None'); the sigma invariant above; and the refusal refusing a --match STRUCTURAL BROWSE, which dispatches nothing — moved to the resolve path, pinned by a_two_supplier_browse_is_not_refused.

(6) TWO PREDICTED BACK-OUT CELLS WERE WRONG and are corrected by measurement, not by reasoning: the two refusal rows SURVIVE the runtime_carrier_sort revert (the query walk reads TERM-carried args and never touches that arm — so the two faces of this fix do not share that dependency), and a_backtracked_receiver_reclassifies_per_carrier does NOT pass either way (it answers [1,1], the default twice).

(7) A STATED MISS, measured: a label written in a QUERY PATTERN does not intern to the operation's arg-place symbol, so the named-arg leg of the query walk does not fire for Desc.describe(x: leaf(), ?r). A miss, never a wrong verdict, and that goal cannot answer under any verdict (WI-938 requires named_arity 0). Left because closing it means comparing labels by NAME, which §8.6 refuses; TermView::named_arg compares by symbol identically, so it is the tree's existing rule and not a gap introduced here.

Docs: 058-implementation.md §25; two spec sentences in kernel-language.md (the ambiguity-raising section and the override section). Full workspace green: 29 binaries, 4458 passed, 0 failed.

