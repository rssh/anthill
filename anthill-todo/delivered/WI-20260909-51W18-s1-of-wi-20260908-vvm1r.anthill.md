## Attributes

- id: WI-20260909-51W18-s1-of-wi-20260908-vvm1r
- created: 2026-09-09T05:58:25Z

- status: Delivered
- status_agent: user
- status_at: 2026-09-09T07:37:15Z

- acceptance: cargo-test, scaland-sbt-test

- depends_on: WI-20260908-VVM1R-design-feature-proposal-060

- tags: vvm1r

## Description

S1 of WI-20260908-VVM1R (umbrella): UN-STRIP the spec's type arguments at the guard tier -- requirement-channel.md sec 10 item 1, whose answer is written at 060-implementation.md sec 8.6. Both producers (lower_require, rewrite_requires_goal, convert.rs:3478/3657) call strip_spec_type_args (convert.rs:3672), which rebuilds the spec instance as a bare nullary Fn; measured, require[Desc] and require[Desc[T = Leaf]] are BYTE-IDENTICAL after convert, which is why two requires on one spec base are refused as unattributable and why writing require[FiniteCollection[C = List[T = String], Element = String]] says nothing the witness did not.

THE CHANNEL IS NOT NEW. sec 10 item 1 offered two candidates (the application type-args channel WI-272/383, vs a type-kind occurrence child). Neither: it is the EXISTING type-position ladder, type_expr_to_child's TypeExpr::Simple arm (load.rs:26251), which already resolves an arrow binder, then a type param of the current scope to a ground logic Var (WI-849 exactly), then a value place, then a sort ref. Un-stripping is routing the goal's spec argument through THAT walk instead of the value-argument walk. TWO RUNGS ARE MISSING: (1) a HEAD-INTRODUCED tvar (rule_tvar_bounds) -- rule_head_bound_alias is gated on in_rule_head_bound, which WI-20260908-PW9A0 narrowed ON PURPOSE ('in the SAME rule, A written in a body goal or a data slot still means what it always meant'), and this body position has to join that set; that is the largest blast radius in this ticket and its own back-out. (2) a name resolving to NOTHING => WILDCARD, the spec's own parameter reference.

WILDCARD, NOT REFUSED, and this is settled rather than open: it is precisely what WI-20260830-X9PB4 already synthesizes for an element no witness names, and requires(Eq[T]) -- the canonical typeclass spelling, whose whole point is that T is UNCONSTRAINED -- must stay writable. That makes the change STRICTLY ADDITIVE.

MEASURED 2026-09-09 by making strip_spec_type_args the identity, tree restored: a CONCRETE bound already survives un-stripping in BOTH spellings (require[Desc[T = Leaf]] and require[Desc[Leaf]] load and answer 7). THE SPLIT IS CONCRETE-vs-VARIABLE, NOT NAMED-vs-POSITIONAL -- the named form require[Desc[T = T]] refuses too, so the type_args ParseAux channel is NOT already carrying it; that guess was measured false. Only a free type-variable name refuses (unresolved name 'T'), in all three spellings including the check tier.

BLAST RADIUS: 33 of 4338 wi_tests, ALL ONE CAUSE, and ZERO shipped programs -- grep -rn 'requires(\|require\[' over stdlib + examples + anthill-todo returns exactly one hit, a rule NAMED sort_requires. The 33 report either the raw unresolved-name or the fixture's own expectation string (wi642:116 requires(Relatable[T]); wi625:324 and wi625:648 requires(Eq[T])).

ACCEPTANCE. All 33 named tests pass UNCHANGED (no fixture rewritten) -- that is what says wildcard preserved today's meaning, and it must be asserted by running them, not by reasoning. A written CONCRETE binding is READABLE downstream: assert by VALUE that require[Desc[T = Leaf]] and require[Desc] now produce different stored goals (a load-clean assertion measures nothing here, since both loaded clean before). A head-introduced tvar in the bracket (require[Desc[T = A]] under rule p[A](?x: A) :- Desc[A]) resolves rather than reporting unresolved name 'A'. CONTROLS, stated at each site: which rows fail when the wildcard rung is backed out (the 33), and which fail when the head-tvar rung is backed out (the bracket-with-A rows only) -- two rungs, two separate back-outs, neither standing in for the other. cargo-test green via scripts/test.sh.

## Changes

### 2026-09-09T07:37:10Z — feedback — user

DELIVERED. The spec's type arguments survive convert, the loader walk and the typer rewrite, and are readable on the stored goal. Driven by anthill-core/tests/include/wi_51w18_require_spec_type_args_test.rs (12 tests). Full workspace green via scripts/test.sh: 36 result lines, all ok, 0 failed (wi_tests 4349 passed). requirement-channel.md sec 10 item 1 marked delivered; 060-implementation.md sec 8.6 rewritten as-built.

WHAT LANDED. convert.rs: both producers stop stripping; strip_spec_type_args deleted. load.rs: build_require_spec_occurrence lowers the spec instance as a TYPE, resolving each binding through parse_arg_sort_symbol -- the one owner of "does this name denote a sort", shared with the sec 2.1 parameter form's bound. typing.rs: make_witness carries the instance whole instead of re-minting Expr::Ref(spec_base).

FOUR PREDICTIONS IN THE TICKET WERE WRONG, corrected in the docs rather than dropped.
(1) The head-introduced-tvar rung needed NO gate widening. The ticket called it "the largest blast radius in this ticket" because rule_head_bound_alias is gated on in_rule_head_bound, which PW9A0 narrowed on purpose. parse_arg_sort_symbol asks rule_head_tvar UNCONDITIONALLY ("must answer before any conversion happens"), so the rung came free; in_rule_head_bound is untouched.
(2) The channel is NOT type_expr_to_child's TypeExpr::Simple arm, which the design doc named: that arm takes a TypeExpr, and a require bracket is a parse Term::Fn whose binding values are plain Term::Ref names (measured: Desc[T = Leaf] -> Fn(Desc, named:[(T, Ref(Leaf))])).
(3) ONE WALK, NOT TWO. A first cut also wrote the term-side lowering on WI-742 sec 2.1's precedent. NOTHING DRIVES IT -- the whole binary passes with that arm disabled, because a rule BODY is stored as occurrences (WI-246: "the term body is gone") and require[...] is legal only as a body goal. Deleted rather than kept undrivable; build_require_spec_occurrence's doc records what a future term-path user would get instead.
(4) Retention was being ERASED one phase later: make_witness re-minted slot 0 bare, so the two spellings would still have produced identical stored goals and the un-strip would have bought nothing. Found only by trying to write the assertion.

/code-review (high) FOUND SEVEN, ALL DRIVEN AND ALL FIXED HERE rather than deferred. The serious one: a DROPPED POSITIONAL silently RE-INDEXED the ones after it -- requires(Desc[Zork, Leaf]) stored Leaf, written SECOND, at slot #0, attributing it to the FIRST type parameter, on a clean load with no diagnostic, in the exact slot S2 is specified to read. Fixed by pairing positionals with the spec's declared params by index (canonicalize_fact_binding_value's rule, applied not re-derived) so every retained binding is NAMED and cannot shift; pinned by a_dropped_positional_does_not_re_index_the_others with a two-sort control so the SHIFT is what moves, not the count. Also fixed: a nested application whose head is a head-introduced tvar reported unresolved name (its base fell to remap_symbol_strict -- two resolvers answering one question, the same split the doc claimed could not happen); a written effect row Spec[E = {}] vanished silently (a ParseAux carrier, not a name, so the drop rule never covered it); this slot bypassed the check_sort_type_args its sibling walk runs, so Desc[Bogus = Leaf] and an over-application loaded clean and stored the bogus binding; a DOTTED spec base was read as a field_access application (pre-existing, confirmed by the reviewer's own control, fixed here since this function now owns the shape); and three stale docs -- including typing.rs's own "the guard tier strips the spec type-args" comment, which 060 sec 8 had explicitly told the next reader to distrust.

CONTROLS, measured by mutating each site and re-running, not predicted. RETENTION back-out: 3 rows. THE DROP back-out: 3 rows here plus 33 ELSEWHERE -- wi1040 9, wi625 7, wi_x9pb4 6, wi1045 5, kernel_mint_address 3, wi1098 2, wi642 1 -- which is the population that rung protects and which passes UNCHANGED as delivered (no fixture rewritten). THE WHOLE ARM back-out: 4 rows, and notably NOT the retention rows, because once the converter stops stripping the ordinary walk lowers a CONCRETE binding correctly on its own; the arm's job is exactly the names that do not resolve as values. the_bare_spelling_is_unchanged passes under all three BY DESIGN and is the yardstick.

ONE ROW MEASURED NOTHING UNTIL THE BACK-OUT RAN: the_positional_spelling_binds_the_same_way asserted [] -- identical to the bare spelling -- because stored_bindings listed only named args. Reporting positionals as #0 made it discriminate, and it then moved again when the re-indexing fix landed, which is what the assertion was for.

TWO NEW REFUSALS (the bogus-param-name / over-application check) cut against the "strictly additive" claim. Measured: they break nothing -- the corpus writes no rule-body requires(...) / require[...] at all, and no fixture writes either shape.

FOR S2 (QMFC5): the bracket is now readable on the stored goal as NAMED bindings on find_dictionary's slot 0, and the duplicate-spec-base refusal in record_find_dictionary_grounding is now a boundary waiting on its reader rather than an impossibility -- its comment says so and names QMFC5.

NOT COMMITTED -- left in the working tree for review.

