```anthill
fact WorkItem(id: "WI-907", created: "2026-07-31T21:41:42Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-08-01T00:54:51Z"))

fact Feedback(workitem: "WI-907", author: "user", at: "2026-08-01T00:55:18Z")
```

## description

`resolve_name_in_kb_opt` (rustland/anthill-core/src/kb/load.rs) SILENTLY RESOLVES PAST AN AMBIGUITY, and it is the third of three policies for one question. Its match is `Found(sym) => Some(sym), _ => resolve_dotted_in_kb(..).or_else(resolve_implicit)` — the `_` arm covers Ambiguous, so an ambiguous short name falls through to the lower rungs and the QUERY-PATTERN position binds it to the implicit-prelude target with NO diagnostic. The same name in a program is refused (`remap_name_str_inner` pushes AmbiguousSymbol, which blocks the load), and since WI-900 the rule-head mint guard counts it as denoting (`name_denotes_for_rule_head`, which cites `Loader::qualified_name_resolves`'s stated reason: an ambiguity is a real finding with a real error to emit). So three sites answer 'what does this ambiguous name mean here?' three ways, and the query one is the silent one — exactly the silent-skip the repo forbids. NOT DRIVEN: raised by /code-review's altitude pass on the WI-900 diff, code-read only. FIRST STEP: drive it — an `anthill query` whose pattern names a short name that is ambiguous at the query scope, and assert what it binds today. Then decide: report (and return None), or state why a query pattern is the one position that may guess. Note the risk named at filing: a query that answers today would start erroring. ACCEPTANCE: the Ambiguous arm of `resolve_name_in_kb_opt` is a stated decision with a test, not a wildcard; the three sites' policies are each documented at their site; cargo-test green via scripts/test.sh.

## Feedback

### 2026-08-01T00:55:18Z — user

DELIVERED (commit 2e18efc6). DRIVEN FIRST, as the ticket asked, and the ticket's own risk note was RIGHT: a query that answered today does start erroring. Fixture — two wildcard-imported user sorts named SortInfo at _global. anthill query 'SortInfo(name: ?n)' answered FOUR rows out of anthill.reflect.SortInfo; with ONE import it was correctly empty; with NONE the tier answers legitimately. So the second import — which makes the name strictly MORE contested — is what brought back the reading the user's own declarations had shadowed. The three-way control (NotFound / Found / Ambiguous) is what proved the Ambiguous arm was doing it rather than a plain miss.

THE TICKET'S FRAMING ('three policies for one question') RESOLVED TO TWO, not to three documented sites: name_denotes_for_rule_head is now literally !matches!(resolve_name_in_kb(..), NotFound), so the mint guard and the ladder cannot disagree, and its WI-900 debug_assert about the omitted dotted rung went with it (the rung is no longer omitted, so the coupling dissolved rather than needing narration). What remains is the ladder (no channel: Ambiguous is returned, each caller states its arm) and remap_name_str_inner (has a channel: reports and refuses). Applying WI-900's own lesson one level up — the fix for N agreeing comments is one function.

SCOPE THAT GREW BY NECESSITY: returning ResolveResult forced resolve_name_in_global's callers to decide, so the MOUNT got a typed ExtentRegError::AmbiguousName. Reporting an ambiguous host name as UnresolvableName would have been loud-and-wrong, and it is my change that created that arm.

FOUR FALSE OR ROTTEN CLAIMS OF MINE, all caught by /code-review, two of them prose I wrote to explain my own fix (the recurring failure): (1) 'None sends the whole target to remap_name, whose own AMBIGUITY REPORT refuses the load' — DRIVEN: it refuses with 'unresolved name f.requires', never an ambiguity, because remap_name re-asks about the whole dotted string. (2) The spec paragraph I added said the ambiguity is 'reported wherever there is a channel' — DRIVEN FALSE: 'rule r(?x) :- Widget907.w907a(v: ?x)' with two ambiguous Widget907s LOADS CLEAN. (3) 'lifted at the THIRD copy' — wi752's copy was already DEAD (the lift is what surfaced it), so it was the second LIVE copy. (4) The rename left six 'resolve_name_in_kb_opt' references, one of them INSIDE the very doc comment that lists resolve_implicit's consumers — that list rotted in the same commit that wrote it, which is the argument for deleting such lists rather than maintaining them.

REVIEW'S BIGGEST WIN WAS DELETING PROSE, for the third ticket running: the SortInfo measurement had been retold at six sites. It survives in ONE place (the core test header, which is what it justifies) plus here.

EFFICIENCY: neutral, measured — resolve_in_scope already returned ResolveResult by value, the guard still makes exactly one of those calls, and the new dotted rung is one split_once('.') that returns immediately for a short name. In debug builds the guard now scans LESS, since the deleted debug_assert ran a full contains() on every call.

FOLLOW-UPS: WI-917 (the two gaps, both driven — an ambiguous dotted HEAD is reported nowhere and reported FALSELY at a pattern; and the WI-863-tolerated query positions). WI-916 got feedback: this diff added one more instance of the shape it owns, deliberately, and external_owner_functor is the single point its proposal would delete.

