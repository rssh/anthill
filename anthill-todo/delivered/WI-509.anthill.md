## Attributes

- id: WI-509
- created: 2026-06-18T07:31:17Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-18T10:36:25Z

- acceptance: cargo-test

## Description

A field-PROJECTION operation body in the LOADED stdlib (e.g. a mutable carrier using 'Cell.set(s.rep, ..)' / 'Cell.get(s.rep)' instead of a 'match s case wrap(r)' pattern-match) triggers spurious 'TypeMismatch { entity_name: field_access, field_name: field, expected_type: Symbol, actual_type: String }' errors in OTHER code's field accesses -- observed in parse_tests::typing_test fixtures (test.* snippets) but NOT in wi_tests (e.g. wi364 loads the same carrier clean). Discovered in WI-506: after the typer fix made field-projection mutating ops typecheck, simplifying stdlib/anthill/prelude/mutable_stack.anthill from pattern-match to 's.rep' projection bodies made parse_tests fail ~30 typing_test cases with the above field_access error, while wi364 (wi_tests) stayed green. The WI-506 typer fix itself is INNOCENT (reverting only typing.rs left the failures; reverting only mutable_stack.anthill to pattern-match cleared them) -- the trigger is the field-projection op body in the loaded set. SMELLS like a field-access field-name representation issue (Symbol vs Const(String); cf. WI-490/WI-280 field_name_from_node accepting Const(String) as the canonical post-typer field name) crossed with parse_tests::typing_test global/kb state shared across tests (a load leaves a field name interned as a String that later field_access typing rejects). CONSEQUENCE: mutable carriers must keep the pattern-match idiom (bind the inner cell via 'match', WI-219) rather than field-projection, even though WI-506 now makes field-projection typecheck in isolation -- so the MutableStack carrier was kept pattern-match. REPRO: edit mutable_stack.anthill bodies to project s.rep (drop the 'match s case mutableStack(r)'), build, run parse_tests -- ~30 typing_test failures with field_access Symbol-vs-String. INVESTIGATE: the field_access typer node's field-name carrier (Symbol vs String) + parse_tests::typing_test test isolation / whether a prior stdlib load mutates shared state. ACCEPTANCE: field-projection mutating op bodies can live in stdlib without perturbing unrelated field_access typing; mutable_stack.anthill (or a probe carrier) can use s.rep bodies; parse_tests + wi_tests + full anthill-core green. REF: WI-506; rustland/anthill-core/tests/include/typing_test.rs (via parse_tests + wi_tests); field_access TypeMismatch in kb/typing.rs; WI-490/WI-280 field-name carrier. Rust-only.

## Changes

### 2026-06-18T10:36:21Z — feedback — user

DELIVERED. Root cause: NOT shared/global state and NOT load order. The typer's ?x.field -> field_access(?x, "field") rewrite (DotApply INC-1b) produces a node that does NOT round-trip a RE-type-check: re-typing the stored field_access Apply (a) types it as the generic reflect op field_access(object,field:Symbol)->Term (losing the field type; and the Const(String) name fails field:Symbol), and (b) the WI-506 effect re-key loses the projection head (stable_receiver_path only handled DotApply, not the rewritten field_access Apply). The asymmetry parse_tests-vs-wi_tests: wi_tests does ONE load pass (synthesis path, never re-typed); parse_tests typing_test fixtures re-type-check (type_check_stdlib_no_spurious_errors does an explicit 2nd type_check_sorts over all stdlib sorts; type_check_correct_fact_no_errors's snippet sort_terms exclude stdlib sorts -> mutable_stack.clear falls into the free-op sweep). FIX (kb/typing.rs): make the rewrite IDEMPOTENT under re-type. (1) field_access_projection_type: when check_apply_iter sees field_access(receiver, Const(String)) with a resolvable receiver sort+field, return the FIELD's type (mirrors INC-1b) instead of the generic op; guarded on named_args.is_empty() (review). (2) stable_receiver_path: handle the field_access Apply form (recurse receiver + append interned field name) so the WI-506 effect re-key keeps the head after re-type; signature gained &mut kb for interning. mutable_stack.anthill converted FULLY to field-projection (push/pop/top/iterator/clear) — the acceptance demonstration (carriers can now use s.rep). Regression test wi509_field_projection_body_roundtrips_retype (field-projection carrier + 2nd type_check_sorts(&[]) free-op re-check; verified FAILS without the fix). /code-review (3 agents): all risks REFUTED except the named_args guard (both agents, fixed) + a stale wi364 comment (fixed) + gratuitous clones (removed). Bonus: WI-509 incidentally fixes a previously-broken field-path let-alias. Full workspace green. Lossy TypeError->LoadError remapping (to_load_error flattens context+Value to strings) noted as a separate debuggability cleanup.

