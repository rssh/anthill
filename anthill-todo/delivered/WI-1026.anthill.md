```anthill
fact WorkItem(id: "WI-1026", created: "2026-08-06T09:46:31Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-07T03:09:29Z"))

fact Feedback(workitem: "WI-1026", author: "user", at: "2026-08-06T09:35:33Z")
```

## description

The SLD goal path consults none of the spec-op supply routes, so a rule body that names a defaulted spec op DIRECTLY honours neither WI-1010 nor WI-1012. MEASURED during WI-1012 on its own fixture, three spellings x two supply shapes, all silent, none refused: with ONE supplier (the fact route, 'fact Desc[T = Leaf, describe = leafDescribe]', which WI-1010 says must beat the default) 'rule answer(?r) :- Desc.describe(leaf(), ?r)' answers 1 (the DEFAULT - the written binding is invisible), 'describe(leaf(), ?r)' answers [] (nothing at all), 'leaf().describe(?r)' answers 1; with TWO suppliers (own member 7 + fact binding 9) the same three answer 1, [] and 7 (route 1, first match) - three different silent answers for one ambiguous program, where the operation-body path is refused at LOAD. CONTROL, same program, tie reached through an operation body: 7 with one supplier and a load refusal with two, so the defect is the PATH and not the fixture. WHY IT IS NOT WI-1012's: both WI-444 sites (the typer's 'check_apply_iter' block and eval's 'resolve_carrier_override_by_value') sit on the CALL path; 'type_rule_bodies' reaches 'check_apply_iter' only for a DOT ('dispatch_dots_in_occ'), and the dot case resolves the receiver's own member directly, never consulting 'carrier_override_suppliers'. So this is pre-existing and predates both tickets. WORK: decide per spelling. The bare-goal '[]' is likely a separate resolution failure and should be diagnosed first (it answers nothing even with a single unambiguous supplier). For the qualified and dot spellings, the question is whether the SLD goal path should consult 'carrier_override_suppliers' at all, or whether an operation call in a rule body should route through the same reader the eval bridge uses - measure which sites resolve these goals before choosing. BLAST RADIUS IS REQUIRED FIRST: changing what a rule-body goal dispatches to changes program answers, so instrument old-vs-new over the corpus the way WI-1010 did before any arm is written. ACCEPTANCE: the table above re-measured with every row either answering 7 (the supplied impl) or refusing, and a stated reason for any row deliberately left as is. REFERENCE: WI-1012; WI-1010; WI-444; WI-431; docs/design/058-implementation.md S13; the measured table is in the header of wi1012_static_supplier_tie_test.rs.

## Feedback

### 2026-08-06T09:35:33Z — user

CHECKED AGAINST proposal 058 - no contradiction; the proposal REQUIRES this and does not currently reach it.

S3.7 states the general rule verbatim: 'a read that asks EXISTENCE of a provider stays boolean; a read that SELECTS one goes loud on the second candidate - NEVER FIRST-MATCH.' The measured 'rule answer(?r) :- leaf().describe(?r)' = 7 with two suppliers IS first-match (route 1 wins), so today's rule-body path violates S3.7 directly.

S3.1 states the model for the other rows: concrete providers 'pay in VALUES: their values carry their own sort, DISPATCH ANSWERS BY THE VALUE'. With a single fact-route supplier the rule body answers 1 (the spec DEFAULT), i.e. does not answer by the value. So the acceptance ('every row answers 7 or refuses') is S3.1 + S3.7 restated, not a new policy.

WHAT 058 DOES NOT SAY, worth knowing before designing: it never legislates rule bodies. S2 models instance resolution AS the SLD goal 'provides(?W, Spec[..])' - but immediately adds that the checker implements it 'inside the typer rather than by literally querying the reflect KB', and S3.9 says every dictionary in flight 'was built at a site where the TYPER resolved the witness'. A rule body has no typer-resolved witness, so this is a GAP in the proposal, not a rule it grants an exemption to. Do not read S3.9 as licensing the present silence; if a decision is taken to leave a spelling alone, it needs writing INTO 058, not inferring from it.

ORDERING NOTE: the bare-goal '[]' row answers nothing even with ONE unambiguous supplier, so it is a resolution failure prior to any 058 question and should be diagnosed before the dispatch policy is touched.

