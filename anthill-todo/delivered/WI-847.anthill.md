```anthill
fact WorkItem(id: "WI-847", created: "2026-07-28T11:03:40Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-29T03:37:42Z"))
```

## description

cpp-gen resolve_type_mapping: the QUALIFIED-over-SHORT anthill_type priority was lost in the WI-833 migration, turning a working override into an ambiguity ERROR — and the function's own doc still promises the priority it no longer honors. CODE-READ at HEAD 2cdfde3e (rustland/anthill-cpp-gen/src/lib.rs, resolve_type_mapping): the loop MERGES the hits of every candidate spelling into ONE hits vec (hits.extend(query_type_mappings(kb, "cpp", name)?)) and then hands the merged list to select_keyed_unique, whose per-key aggregation is unique_or_ambiguous — 0 distinct falls through, 1 selects, >1 is a LOUD CppCodegenError. So a stdlib mapping written under the SHORT name and a project override written under the QUALIFIED name, both at the same key, are now two competing mappings at one key and codegen ABORTS where it used to take the override. VERIFIED against the parent: 0f979eac~1:lib.rs:3861-3872's select_keyed selected by hits.iter().position(|h| key_of(h) == ak) — FIRST match in list order — and resolve_type_mapping pushes the qualified spelling's hits FIRST, so the qualified entry won. That first-wins was deliberate at the time; select_keyed_unique's own doc now says the ambiguity is "never the silent first-wins the WI-089 select_keyed did", i.e. the change was intended for a SAME-SPELLING conflict, but the CROSS-SPELLING case it also swept in is not discussed anywhere. THE DOC IS NOW FALSE either way: resolve_type_mapping still reads that its `names` are the candidate `anthill_type` spellings "tried in order and merged (a sort's qualified name first, then its short name, so a fact written either way resolves)" — "tried in order" does no work once the merged list is deduped-or-refused, so the text describes a priority the code cannot express. NOT YET DRIVEN, and the reason is the second half of the finding: the two-name call is reached from exactly ONE site, marshal_for_type (lib.rs:1261, passing &[&qualified, short]) — the value-level lift/lower reader at a carrier-dispatch boundary. Every other resolve_type_mapping caller passes a SINGLE name (lib.rs:1164, 1172, 3700, 3790, 4074), and every WI-833 TypeMapping test goes through money_base -> cpp_base_host_type, one spelling. So the merge path has NO coverage at all, which is why the behaviour change is invisible to the suite. DECIDE, do not guess: either (a) restore qualified-over-short as an EXPLICIT priority — select per spelling in order and take the first spelling that yields a selection, so a cross-spelling pair is an OVERRIDE and only a same-spelling pair is ambiguous; or (b) keep the merge and rule that a cross-spelling pair IS a conflict — in which case say so in the doc, and make the diagnostic name BOTH spellings so the author can see why two facts they believe unrelated collide. (a) preserves a working override idiom; (b) is defensible but is a breaking change to how a project overrides a stdlib mapping, and nothing records that decision today. ACCEPTANCE: (1) a driven test through marshal_for_type (emit with a carrier binding + lift/lower mappings) exercising a qualified/short pair at one key — the coverage that is missing whichever way the decision goes, and it must be shown to FAIL on the current behaviour if (a) is chosen; (2) the decided behaviour implemented and resolve_type_mapping's doc made true; (3) the existing WI-833 same-spelling ambiguity tests pass unchanged; (4) full workspace green via rustland/scripts/test.sh. SOURCE: /code-review of the WI-833 merge, 2026-07-28. SCOPE: rustland/anthill-cpp-gen.

