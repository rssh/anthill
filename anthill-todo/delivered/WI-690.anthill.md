```anthill
fact WorkItem(id: "WI-690", created: "2026-07-11T17:31:32Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-691"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-11T21:19:44Z"))

fact Tag(workitem: "WI-690", name: "carrier-neutral")

fact Feedback(workitem: "WI-690", author: "user", at: "2026-07-11T21:19:41Z")
```

## description

Make WI-580's `unfold_eq_operand` goal builders OCCURRENCE-NATIVE, eliminating all four Term<->occurrence conversions in the unfold cluster (resolve.rs `unfold_eq_operand` + `fresh_pattern_term` + `anf_flatten`). THE FOUR SITES: (1) ~L5748 occurrence_to_term(scrutinee_occ) occ->Term, to embed the case-split scrutinee into a Term::Fn `unify` goal; (2) ~L5756 occurrence_to_term(other) [Node case] occ->Term, to embed OTHER into the result `unify` goal; (3) ~L5880 occurrence_to_term, anf_flatten's term-leaf fallback; (4) ~L5798 materialize_from_handle(h) Term->occ -- the ROUND-TRIP: anf_flatten builds each hoisted `eq` goal as a Term::Fn (~L5937), pushes the TermId, then this immediately re-walks it BACK to a Node so WI-668's recursion re-enters op_call_as_occ's Node arm. WHY THIS IS THE REDUNDANCY: L5798 is a build-then-re-walk (the lossy term->occurrence path build_expr_leaf warns about). The two `unify` goals are built as Term::Fn ONLY because fresh_pattern_term/anf_flatten are Term-native builders (alloc fresh Term::Var, canonical named+sorted entity Term::Fn); since scrutinee/other arrive as occurrences and pattern/result are freshly Term-built, the occurrence operands MUST be lowered to co-reside -- occurrence_to_term is the minimal forward bridge GIVEN a Term goal. Switching just the leaf unify goals to Node is a wash-to-worse (trades occurrence_to_term for materialize_from_handle on the pattern); the ONLY net win is making the WHOLE unfold occurrence-native, which drops all four. NOTE these two unify goals are LEAF goals that do NOT re-trigger the unfold, so WI-668 deliberately left them as Value::term -- this WI revisits that only as a side effect of removing the builders' Term-nativeness, not for the leaf goals' own sake. DESIGN: fresh_pattern_term -> return Rc<NodeOccurrence> (Expr::Var(Var::Global) for binders/wildcards via fresh_var, Expr::Const for literals, Expr::Constructor{name, named+sorted} for constructors), same rename side-table; anf_flatten -> return Rc<NodeOccurrence>, hoisting each nested op-call as fresh var + a Node-carried `eq` goal DIRECTLY (no Term::Fn intermediate, no L5798 materialize); build both `unify` goals as Value::Node occurrences Apply{unify,[scrutinee_occ,pattern_occ]} / Apply{unify,[result_occ,other_occ]}; OTHER's scalar/entity/var case (currently alloc_from_value ~L5757) wraps value->occurrence (a leaf Node) instead of Term; re-express term_has_bodied_op_call(other) (~L5759) over the Value/occurrence via TermView. CONSTRAINTS/SOUNDNESS: (1) CANONICAL ENTITY FORM is the highest risk -- fresh_pattern_term's constructor case builds a named+sorted canonical entity so the bound value matches how entities are represented everywhere; the Expr::Constructor occurrence MUST reproduce the byte-equivalent shape (cf. WI-449 fact-path canonicalization discipline); (2) fresh vars Expr::Var(Var::Global(vid)) MUST walk identically to Term::Var(Var::Global(vid)) under the resolver (memory node-carrier-mirrors-term-structure); (3) the two goals stay UNIFY not SemEq -- needed narrowing must BIND spine vars (preserve the ~L5774-5785 rationale); (4) hoisted goals must still be recognized by op_call_as_occ's Node arm on re-entry (WI-668) and the two unify goals must NOT re-trigger the unfold; (5) preserve occurrence_to_term's asserting posture for a non-goal scrutinee -- a control-flow scrutinee is a bug, stays a LOUD error not a silent skip (dev principle: loud error over silent skip); (6) op_call_as_occ's Term arm (a Term-carried eq operand still reaches the unfold, WI-668 regression guard wi668_term_carried_opcall_eq_case_splits) is untouched -- builders going occurrence-native does not change that ENTRY. INCREMENTS: inc1 = anf_flatten half only (kill the L5798 round-trip + L5880) as the concrete-payoff first step; inc2 = fresh_pattern_term + the two Value::Node unify goals + OTHER value->occurrence + term_has_bodied_op_call over TermView. ACCEPTANCE: all four conversions removed from the unfold cluster; fresh_pattern_term/anf_flatten return Rc<NodeOccurrence>; WI-580/WI-668 suites green (relational append/member unfold, wi668_term_carried_opcall_eq_case_splits, the eq case-split recursion tests); anthill-core + full workspace green; cargo-test green. Rust-only (resolver). Refs: WI-580 (the unfold), WI-668 (Continuation Vec<Value> + hoisted-goal Node materialization -- the round-trip this removes), WI-348 (carrier-neutral direction), WI-449 (canonicalization discipline for the entity-form risk).

## Feedback

### 2026-07-11T21:19:41Z — user

DELIVERED inc1+inc2 (integrated onto origin/main after f4524322). inc1: occurrence-native anf_flatten -- hoisted op-call eq goals built as Value::Node DIRECTLY, retired the L5798 materialize_from_handle round-trip + the L5880 leaf occurrence_to_term. inc2: both unify goals ride as Value::Node (fresh_pattern_occ returns Rc<NodeOccurrence>, canonical named+sorted / nullary->Ref), retiring the L5748 scrutinee + L5756 other -- ALL 4 cluster conversions gone. inc2 REQUIRED WI-691: binding the query var to a Node pattern makes the SOLVED-VAR ANSWER Node-carried, and reify()'s non-Term arm was returning a bound Node RAW (inner vars unresolved even though sigma determined them); WI-691 routes it through reify_value (carrier PRESERVED not promoted, so the WI-348 denoted-node identity stays intact -- promoting ground Nodes to Terms was tried and rejected). value_has_bodied_op_call replaces term_has_bodied_op_call (carrier-neutral TermView walk; an un-decomposable Opaque OTHER -> conservative decline, loud-over-silent). rename carries a VarId (dropped the redundant per-binder Term::Var). 3 unfold tests updated to lower via node_occurrence::value_to_term (answers now Node-carried). USER'S ORIGINAL Q ANSWERED: removing occurrence_to_term at L5748 IS possible, but it forces Node-carried answers -- L5748 was load-bearing for a Term-carried answer. /code-review high (8 finder angles): 0 confirmed correctness bugs (the tuple-OTHER candidate is declined by alloc_from_value; hardened the Opaque case anyway). anthill-core + full workspace green; reify unit test reify_sigma_applies_a_node_binding_wi691 adversarially verified (revert -> fails).

