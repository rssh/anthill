## Attributes

- id: WI-806
- created: 2026-07-21T23:45:15Z

- status: Delivered
- status_agent: user
- status_at: 2026-07-21T23:44:48Z

- acceptance: cargo-test

- depends_on: WI-773

## Description

Migrate anthill-cli realization/reflect facts-only readers onto the WI-773 read_facts accessor, fixing a latent value-fact-head panic. Five check.rs/prove.rs sites read a reflect/realization fact head via kb.rule_head(rid), which PANICS on a Value::Node/Entity head (the class WI-366/WI-663 hardened elsewhere), behind a local is_fact skip (one of the three divergent bodied-rule policies WI-773 centralizes): check.rs:90 run_check_with (ProofRecord, Result<_,i32> channel), check.rs:479 proof_record_exists (ProofRecord, bool), check.rs:568 check_scope_axiom_witness induction branch (SortInfo, CheckStatus), prove.rs:1449 cite_status (ProofRecord, CiteStatus enum), prove.rs:1585 implicit_cites_for (ProofRecord, bare Vec). Migrate each to read_facts(functor, empty selection, Refuse): rows are carrier-neutral Values (narrow Value::Term to the head TermId the existing extraction consumes), and a bodied rule is refused through one channel (Err(1) for run_check_with -- a checker must not skip a record it cannot read into a green result; CheckStatus::Failed for the axiom witness; conservative degrade false/NotFound/empty for the bool/status/Vec returns). The already-carrier-safe fact_head_named_args sites (check.rs:601 requires-branch, run.rs:66 find_main_providers) STAY: they read SortRequiresInfo which GENUINELY carries value heads (WI-366), so read_facts Value::Term extraction would warn spuriously on every denoted-spec load. OUT OF SCOPE (see WI-807): the prove.rs discharge-path readers, and the core op_info/eq_derive/typing.rs readers (already carrier-safe, compiler-internal tables, no bodied rules, Result-friction on hot paths for ~zero value; two typing.rs index-builders store the RuleId as payload). ACCEPTANCE: the five sites read through read_facts; a bodied ProofRecord/SortInfo rule is refused (not silently skipped or panicked); cargo-test green via scripts/test.sh.

