```anthill
fact WorkItem(id: "WI-110", created: "2026-04-28T11:02:53Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-17T09:54:55Z"))

fact Feedback(workitem: "WI-110", author: "claude", at: "2026-06-17T09:54:48Z")
```

## description

Audit anthill-core tests that may rely on tree-sitter's permissive error recovery. Background: ho_bigint_induction_parses (typing_test.rs) used a malformed source — '-:' inside a fn-term arg with no grammar rule for that position — and was passing only because tree-sitter embedded the ERROR node inside an fn_term and the Rust parse::parse only flags top-level ERROR nodes. WI-C1's '-:' rule-conclusion clause exposed the bug by promoting the ERROR to top-level. Sweep: grep for tests that call parse::parse with .is_ok() asserts but never check the resulting tree for embedded ERROR nodes; tighten parse::parse to surface embedded errors OR fix test sources to use grammar-supported shapes. Acceptance: identify and either fix or document each test that depends on permissive recovery.

## Feedback

### 2026-06-17T09:54:48Z — claude

AUDIT COMPLETE — no test currently depends on tree-sitter permissive recovery; the root concern was already closed by WI-303 and the lone straggler (ho_bigint_induction_parses) is already fixed. METHODOLOGY + FINDINGS: (1) parse::parse (src/parse/mod.rs) is the SOLE tree-sitter entry point in anthill-core — grep-confirmed no other tree_sitter::Parser / Converter use in src or tests; the test common helpers (tests/common/mod.rs:95,98) and every per-test loader route through it, so there is NO bypass path. (2) It ALWAYS runs collect_syntax_errors, which descends the WHOLE CST (prunes clean subtrees via has_error(), reports each ERROR via is_error() and each MISSING via is_missing()) — NOT just top-level as the WI background described the OLD behavior. WI-303 (9d73fdc) made this change and swept every then-existing fixture. (3) PROBE-CONFIRMED the fix is robust for the exact WI-110 shape: feeding the old unparenthesised '-:' rule-body source (ERROR buried deep in the body) now returns Err('syntax error near ...'); '-:' inside a fn-term arg likewise Err. So any .is_ok()/.unwrap() parse test that PASSES is by construction free of embedded ERROR/MISSING nodes — a green suite IS the audit proof. (4) Full anthill-core suite green: 1706 passed, 0 failed, 3 ignored (wi237_diag dump by design; wi374 '?'-binding-slot feature gate; wi402 WI-391 lowering pending) — none are permissive-recovery false-greens. (5) WI-303's one #[ignore]d false-green, typing_pass_spec_parses_and_loads, is now a LIVE genuinely-passing #[test] (WI-302/WI-306 landed; it parses clean). DELIVERABLE: added regression guard parse_fires_on_embedded_syntax_error (parse_test.rs) using the exact historical '-:'-in-rule-body shape, asserting parse() surfaces the DEEPLY-EMBEDDED ERROR — locks the contract so a future 'only check the root' regression can't silently reopen the gap. RESIDUAL (out of WI-110 scope): tree-sitter recovery that emits NO error node (a silent alternative parse) is undetectable by collect_syntax_errors — a semantic concern, distinct from this ticket's ERROR-node recovery (the ho_bigint case was an ERROR-node case, probe-confirmed).

