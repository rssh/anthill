## Attributes

- id: WI-843
- created: 2026-07-28T09:43:49Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-29T23:16:16Z

- acceptance: cargo-test

- depends_on: WI-841, WI-842, WI-838

- tags: modinst

## Description

Proposal 058 (WI-648) PHASE 3b — COHERENCE: move the two-provider refusal from LOAD TIME to the UNSELECTED USE SITE (§4.1 tier 3). Admissible ONLY on top of phase 3a. TODAY two independent refusals fire on the §1 program: the per-call-site one (typing.rs:10731 — "expected unique impl for per-call bindings, got multiple impls match (coherence rule)") and the LOAD-TIME global one (`AmbiguousWitness`, typing.rs:15995; the instance-fact flavor `AmbiguousInstanceFact`, load.rs:283) — and the second does not depend on any call at all: delete the calling sort from the two-monoid program and the load still fails with "keep exactly one". What Anthill rejects today is the pair of DECLARATIONS; the question of which monoid a call wants is never reached. AFTER: two providers may COEXIST, and what is refused is an UNSELECTED dispatch against them — at that use site, naming the candidates and the syntax that picks one. NOTHING may become silently dispatched: an ambiguous site that says nothing is still an error, with a better message. The criterion is unchanged from the one already shipping (§1.1): the concrete-provider exemption (typing.rs:15977) asks whether the CALL has one answer — with a concrete carrier the VALUE answers, so two providers are no ambiguity — and tier 3 supplies an answer for the case where the value is silent. GATE — COEXISTENCE REQUIRES EVERY CANDIDATE TO BE NAMEABLE (§4.1, §4.3): an instance fact has no name, so two instance facts stay refused at load exactly as today, AND SO DOES A MIXED instance-fact + witness pair. WI-838 is the trap here: measured 2026-07-28, that mixed pair LOADS CLEAN TODAY because the two ambiguity checks are each grouped by provider KIND and neither sees the cross-kind pair — an implementation that merely DELETES the witness check inherits that hole as its coexistence rule. A tier-3 diagnostic listing a candidate the author cannot spell is a dead end, not a fix. ACCEPTANCE (§9 phase 3b, as amended): the §1 two-monoid program LOADS, and `fold[Monoid = AddM]([2,3,4])` / `fold[Monoid = MulM]([2,3,4])` compute 9 / 24 (the value test deferred here from phase 2, since it needs coexistence); the unselected `Use.go` becomes an error naming AddM/MulM and the syntax that picks one — the diagnostic MOVES from the declarations to the one call that is actually ambiguous. `two_describers_for_one_carrier_rejected_globally` (rustland/anthill-core/tests/include/wi817_polyrec_requirement_test.rs:662) flips to 75 with one selection written per site (`Desc.describe[Desc = LoudDesc](w)` in the loud scope, `[Desc = QuietDesc]` in the quiet one) — 55 or 77 is a FAILURE, not a variant: either betrays a pin ignored or cross-wired. TWO CHORES THAT GO WITH THE FLIP, or the tree keeps a false claim: (a) that fixture's doc comment currently reads "lambda keeps its creation-scope provider (5), the quiet hop uses its own (7)" — a SCOPE-CAPTURE reading the pinned program no longer demonstrates (a pin is a per-occurrence static constant emitted at classification; the flipped fixture measures PIN SURVIVAL through a closure hop, which is the right acceptance for this proposal since implicit scoped selection is deferred). Rewrite it; the VALUE 75 is unchanged, which is exactly why a stale comment here would go unnoticed. (b) KEEP AN UNPINNED TWIN as the tier-3 control — the current test's own name is that control, and it must keep failing, now at the USE SITE rather than at the declarations. Editing the only fixture into the pinned form would leave tier 3's refusal untested. ALSO: a mixed witness + instance-fact pair stays a LOAD error; every current single-provider program is unchanged — FULL SUITE GREEN via rustland/scripts/test.sh is the control. REFERENCE: docs/proposals/058-modular-instances.md §4.1, §4.3, §5.1, §5.2, §9 phase 3b.

## Changes

### 2026-07-29T23:16:34Z — feedback — user

WIDENING FOUND BY REVIEW, PINNED NOT FIXED: a specificity-ordered pair never reaches tier 3. Two providers in ONE (spec, carrier) group whose heads differ in groundness are not a tie — pick_most_specific takes the more specific one and the call runs with NO diagnostic. MEASURED: ground `Monoid[T = List[T = Int64]]` beside parametric `Monoid[T = List[T = E]]` loads and answers 111 (the ground one); control — the mixed-kind twin is still refused 'for carrier anthill.prelude.List', proving both provisions are one group and so were refused before this ticket. So the ticket's 'NOTHING may become silently dispatched' is FALSE as written; what holds is 'nothing became silently FIRST-MATCHED' (WI-842's readers). Not fixed here because tier 2's specificity rule is '058 §4.1 today's rule, unchanged' and WI-841's committed acceptance (selection_overrides_the_search_and_the_value_shows_it) DEPENDS on the search taking the ground provider silently; making same-group pairs loud is a new coherence rule needing its own measurement. Pinned by a_specificity_ordered_pair_silently_takes_the_more_specific and recorded in 058 §4.1.

