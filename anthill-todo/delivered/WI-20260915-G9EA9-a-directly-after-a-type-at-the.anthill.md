## Attributes

- id: WI-20260915-G9EA9-a-directly-after-a-type-at-the
- created: 2026-09-15T12:37:14Z

- status: Delivered
- status_agent: claude
- status_at: 2026-09-15T21:51:01Z

- acceptance: cargo-test, scaland-sbt-test

- depends_on: WI-20260914-DV7DP-a-meta-block-on-a-sort-entity

## Description

META BLOCKS ARE WRITTEN `@[…]` — ONE TOKEN, IN EVERY POSITION (declarations, rules, facts), replacing the bare `[…]` and the operation `meta [..]` clause. A bare `[` competes with type arguments and with collection literals, and those competitions have lost blocks silently.

THE PROBLEM, first measured at declaration ends. Four productions in tree-sitter-anthill/grammar.js end `field(_type) optional(meta_block)` with nothing between them: `abstract_sort` (`sort X = T [..]`), `effects_sort_item` (`effects E = T [..]`), a body-less, clause-less `operation_declaration` (`-> T [..]`) and a body-less `const_declaration` (`const K: T [..]`). After a type NAME, `Name [e1, …]` is either `application` (`Name[sort_binding, …]`, whitespace-insensitive) or `simple_type(Name)` followed by `meta_block`; the entry grammars overlap on a bare name, and `application`'s prec(1) always picks the type. MEASURED 2026-09-15 (during WI-20260914-DV7DP; sort and const driven, effects and operation read from the grammar): `sort Ids = List [Int64]` and `const Xs: List [Int64]` LOAD as `List[Int64]`; `sort Id = Int64 [Marker]` is refused as over-application, `List [Marker]` as an unresolved name, `const K: Int64 [Marker, Key: 7]` as a syntax error. The refusals are accidents of the entries failing to type — an attribute spelled like a sort in scope (`[Serializable]`) is lost with nothing said.

THE SAME BRACKET ELSEWHERE. (1) WI-893: after a rule entry's heads, `[` is this entry's `meta_block` or the NEXT entry's collection-literal head; a preceding comment once tipped it, `[simp]` became a junk rule and the equation went inert with no diagnostic — held today by `prec.dynamic(1, $.meta_block)` and the `[$.rule_entry]` conflict. (2) WI-20260829-BAD3V: `?x.m [simp]` is the meta block or a dot-callee bracket, kept apart only because GLR lets the second reading die at the missing `(`. (3) An operation with a `meta [A]` clause AND a trailing `[B]` keeps only `A`: `convert.rs`'s operation conversion prefers the accumulated clauses and reads the trailing block only when there are none (read from the code, not driven).

DECIDED with the user (2026-09-15):
- `@[` is a single lexical token (tree-sitter `token('@[')`, a literal in scaland's fastparse). `@ [` with a space is a syntax error. `@` keeps its effect meaning: `-> B @ E` / `-> B @ {E1, E2}`. CHECKED FREE: no effect form begins with `[` (`_effect_set` / `_effect_type`: a spec instantiation, `+E`, `-E`, `merge(…)`, guarded forms, `{…}`), and the term-level infix `@` followed by a collection literal has zero uses — grep over `.anthill`, `.rs`, `.scala` on 2026-09-15 found only three Rust comments in an old effect notation.
- EVERY meta block moves in this one change — sort / enum / abstract sort / effects / entity / const / constraint / operation / operation entry, rule / rule entry, fact, proof step. The operation `meta [..]` clause is removed.
- A bare `[…]` in those positions is no longer a block. Where it still parses as something else, the author must hear that they wrote a block the old way (see acceptance 3), not get a junk rule or a silent type application.
- Placement: TRAILING, where the block sits today (`entity Point(x: Int64) @[M]`, `end @[M]`, `rule r(?x) :- body @[simp]`) — confirmed by the user. Prefix placement (Lean's `@[simp] theorem …`) was rejected: its migration moves text rather than inserting a character, and a sort's prefix block would name members declared after it.
- DOCUMENTATION moves to the new format in the same change: every example and prose mention of a bare block becomes `@[…]`. That includes docs/kernel-language.md's §7 "Syntactic sugar" paragraph, whose `fact … [trust: axiom]` "desugars to" `rule … meta: Meta(trust: axiom, …)` — the grammar has no `meta:` form (only the `meta [..]` clause being removed), so the expansion is rewritten as plainly conceptual or removed. Work items that are open or claimed move too (their examples are what gets implemented, e.g. WI-20260914-Z73FX's `[internal]`); delivered, verified and rejected items are historical records and keep the syntax they were written in.

GRAMMAR TO RE-EXAMINE once the bare block is gone: `application`'s prec(1) meta-block rationale, the `[$.rule_entry]` conflict and its `prec.dynamic`, and the meta-block half of the `[$._non_name_atom_term, $.dot_application]` conflict (its collection-literal half may remain).

MIGRATION SIZE (measured 2026-09-15): `.anthill` files — 27 blocks, all `[simp]` on rules (tree-sitter query over all 246 files; exact, so rewritable from capture positions). Inline Anthill in Rust sources (grep, approximate) — ~1020 `[simp]` / `[unfold]` / `[hint]` tags, ~12 `meta [..]` clauses, ~8 `end [..]` sort blocks, 2 entity blocks, 3 fact `[trust: …]` blocks. Markdown (regex over the common keys `simp` / `unfold` / `hint` / `trust:` / `agent:` / `Marker` / `internal`, approximate) — `docs/` ~340 in 37 files (kernel-language.md 33, design/simp-rewrite-brainstorm.md 61, proposals/043-simp-rewrite.md 29, …), `rustland/**/*.md` 10 in 2, the root CLAUDE.md `[simp]` bullet, and the open/claimed share of `anthill-todo/`'s ~570 in 139 files. The spec's §5.3 / §5.8 / 'Declaration metadata' paragraph (whose bracket sentence describes the behavior this ticket removes). Also scaland's parser and tests, the tree-sitter corpus, `testdata/parser-parity`.

ACCEPTANCE: (1) `@[Marker, Key: 7]` is recorded at every position listed above, each driven by its own test: a declaration's block read back from `anthill.reflect.DeclarationMeta` (WI-20260914-DV7DP), an operation's from `OperationInfo.meta`, a rule's `@[simp]` FIRING (an equation rewrites), a fact's clause metadata read back; (2) `sort Ids = List @[Serializable]` records `Serializable`, and `sort Ids = List [Int64]` is `List[Int64]` by design — likewise at the other three type-ending productions; (3) a bare `[simp]` after a rule or fact head, and `@ [simp]` with a space, are refused with a message naming `@[…]` — CONTROL: before the change the first loads (as a tag), so the test fails on the old grammar; (4) the operation `meta [..]` clause is refused, naming `@[…]`; (5) no bare meta block remains in the repo (the tree-sitter query that measured the 27 returns none, and the inline-Rust grep is reviewed to zero); (6) the documentation speaks only `@[…]` — docs/, CLAUDE.md, rustland Markdown and open/claimed work items, by the same key regex reviewed to zero outside historical items, with the §7 "Syntactic sugar" expansion no longer showing a `meta:` form the grammar lacks; tree-sitter corpus, `testdata/parser-parity`, scaland sbt and the full workspace via rustland/scripts/test.sh green.

### Implementation (2026-09-15)

GRAMMAR. `meta_block` opens with the token `@[`; `meta_clause` is deleted from `operation_clause`. With the bare-bracket rivalry gone, three tie-breakers were MEASURED unnecessary and removed — `generate` reports no conflict without them: the `[$.rule_entry]` conflict and its `prec.dynamic(1, $.meta_block)` (WI-893), and `application`'s `prec(1)`. The BAD3V conflict `[$._non_name_atom_term, $.dot_application]` was measured STILL required (removing it fails generation at `'rule' '{' field_access • '['`): its collection-literal half remains. scaland's fastparse `metaBlock` takes `"@["` and its `meta` clause alternative is gone. Corpus 226/226.

CONVERTER. The operation's `meta` clause merge is deleted — one trailing block. `parse::legacy_meta_block_hints` adds, to a FAILED parse only, one error per old-shaped bracket on an error's line or inside its span, naming the fix: `[Key…]` after a term and whitespace ("is a meta block in the retired spelling: write `@[…]`"), `meta [..]` ("clause was removed"), `@ [..]` ("remove the space"). The whitespace requirement separates a retired block from a type application on the same broken line (`Eq[T]`); all 298 blocks the old grammar found had it.

MIGRATION, tool-driven. (1) With the OLD grammar, every `.anthill` file, Markdown fence, Rust/Scala string literal and corpus input was parsed and every `meta_block` start rewritten exactly: 298 edits in 81 files. (2) With the NEW grammar, segments the old one could not parse were re-read; old-shaped brackets on ERROR lines or in a collection-literal rule head were reviewed: 30 applied, 8 rejected as not blocks (`sort [F]` binders, `[Elem]` in prose, WI-893's deliberate literal conclusions). (3) The canonical tags `[simp]` / `[unfold]` / `[hint]` in comments, prose and diagnostics: 1312 in 178 files, keeping the hint's own doc examples. (4) Declaration and clause keys in Markdown (`trust:`, `supersedes:`, `infix:`, `feeds:` …): 59. (5) By hand: the 12 `meta [..]` clauses (moved to trailing), the spec's §5.8 / §7 / EBNF / "Syntactic sugar" / DV7DP paragraphs, Z73FX's `[internal]` / `[public]`, and the comments whose subject WAS the bare-bracket ambiguity (grammar.js BAD3V, scaland `fieldSeg`, reflect.anthill's "nullary RHS needs parens", wi893's header). Historical items (delivered / verified / rejected / stale) and the gitignored `docs/local/` keep their text; REPL-output and EBNF brackets (`[store: …]`, `[MetaBlock]`) are not blocks.

A TRAP REMOVED, driven: §5.3 said a bare-identifier RHS before the attribute read as an instantiation (`<=> none [simp]` as `none[simp]`); `<=> constant @[simp]` now fires identically to `constant()` (`a_bare_name_before_the_block_is_not_an_instantiation`).

TESTS. `wi_g9ea9_meta_block_token_test`: rule and rule-entry firing (untagged sibling as control), fact clause metadata, operation declaration and operation-block entry, proof step (parse IR), the four type-ending productions with `[Int64]` type-application controls, over-application refusal, the retired spellings refused naming `@[…]`, and no hint for `Eq[T]` on a broken line. `wi087_operation_meta_test`: a second block is a syntax error; the `meta [..]` clause is refused naming `@[…]`.

### Code review (2026-09-15)

FIXED. (1) The legacy-block hint re-lexed raw bytes: beside a namespace-wide ERROR it named legal brackets — `sort Xs = List [Int64]`, `in [a, b]`, a `[ascending]` inside description prose — as retired blocks, and it was quadratic. A pure CST walk was tried and MEASURED to miss the retired blocks themselves, because error recovery gives them no stable place (a rule body, a loose ERROR leaf, a `sort_type_param_list`, an `application` with an ERROR inside). Shipped: candidates from the text, exclusions from the tree (comment / string / description-block tokens, a bracket an error-free `application` owns, a keyword before the bracket), binary-searched skip regions and `LineIndex`. Dotted keys and string values with punctuation are now named. (2) `?a -> ?b @ [simp]` parsed CLEAN (`@` is the arrow-effect infix), silently losing the tag: an `@` whose operand is a collection literal is refused on every parse, and the hint defers to that message. (3) scaland had no bare-literal conclusion refusal, so a retired `[simp]` in `rule { … }` loaded silently as an untagged law beside a junk rule; it is refused at LOAD (rustland's converter layer — scaland's parser tests write literal facts on purpose), naming `@[…]` for a list of bare names. (4) Stale prose the mechanical tag pass produced: the `none[simp]` trap notes in map.anthill / float.anthill / proposal 053, the BAD3V control claims (the `@[simp]` pin no longer separates the designs; its neighbours do), WI-893's history (the junk head WAS the list `[simp]`), the corpus row name, the flag-form doc. (5) The body-less `const K: Int64 [Marker, Key: 7]` refusal lost with the moved DV7DP test is back, with its hint.

