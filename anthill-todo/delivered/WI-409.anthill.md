## Attributes

- id: WI-409
- created: 2026-06-06T16:03:37Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-07T00:10:17Z

- acceptance: cargo-test

- depends_on: WI-275, WI-064

- tags: first-real-map

## Description

Stdlib+cli: implement map (+ the find/fold combinators ParseArgs uses) on Stream/Iterable over the WI-380 splitFirst substrate, and REWRITE stdlib/anthill/cli/parse.anthill ParseArgs (parse_argv/parse_args/find_spec/find_param/next_positional/check_required) to use them INSTEAD of hand-rolled match-nil/cons recursion. THIS IS THE ORIGINAL DRIVER behind the whole type-threading effort (WI-379->380->393->...): the element threading map needs (Stream[T=A]->Stream[T=B]) is DELIVERED (WI-380/393, explicit [Elem,Eff]); the remaining blocker is HOF (WI-275 -- probe-confirmed: applying a function-typed param / passing a lambda does NOT type-check today). The PROJECTION / path-dependent cluster (WI-376/397/398/399/400) is the write-shorter FLUENCY layer (bare s.T instead of explicit [Elem,Eff]) and is NOT required for this goal. Acceptance: parse.anthill's recursive list ops expressed via map/find/fold; cargo-test green (anthill-todo CLI parse tests stay green).

## Changes

### 2026-06-07T00:10:17Z — feedback — user

DELIVERED 2026-06-06 (04f1ba4): the 5 recursive list SEARCHES in stdlib/anthill/cli/parse.anthill (find_spec/find_param/has_binding/next_positional/check_required) rewritten via Stream.find (WI-064) — a List is the receiver via List-provides-Stream. Each predicate closes over a runtime value (target name / current acc), so it is a CURRIED builder op returning a closure typed against its OWN concrete Function[..] return (the lambda checked at a concrete element type — the workaround for WI-275's inline-lambda-on-generic-combinator gap; a bare op can't capture, an inline lambda on a generic combinator can't type). The argv walk parse_args stays direct recursion (lookahead state machine — a flag consumes the NEXT argv element — not a per-element fold). map/fold not used: parse has no list-transform/reduce-shaped op, only searches → find is the fit. Behavior IDENTICAL (head-first first-match, same predicates/error construction): cli_parse tests + full anthill-todo suite green (1538 workspace). /code-review: semantic-equivalence agent found NO behavioral diff; net +5 builder ops is the intended combinator demonstration (the whole point of WI-409). FOLLOW-UP: WI-412 (anthill-todo embedded-stdlib hand-list drifts — 7 files missing; migrate to build.rs auto-collect like anthill-cli; combinators.anthill added by hand here as interim). first-real-map GOAL REACHED: the type-threading effort (WI-379->380->393->275->064->409) now demonstrably powers real CLI code via stdlib combinators.

### 2026-06-07T12:42:25Z — feedback — user

CORRECTION (commit 62bc6e7): the curried-builder workaround was UNNECESSARY for parse.anthill. All 5 builder ops (spec_name_is/param_name_is/binding_name_is/unbound_positional/required_missing) were removed and the predicates inlined as lambdas passed directly to find. Inline lambdas DO type on find: the element type is pinned by the concrete List receiver and the result is Bool, so WI-275's bidirectional HOF-arg inference (delivered b0c01a9) types both the lambda param and body. The delivered comment's claim 'an inline lambda cannot type on a generic combinator (element type unpinned)' over-applied the WI-064 FOLD finding to FIND. That gap is real ONLY when the element/accumulator type is unpinned at the call site, i.e. fold: VERIFIED that generic fold_left(xs, 0, lambda (acc,x)->acc+x) still fails 'cannot prove Numeric[Acc]' (the genuine remaining gap = WI-275's deferred two-phase remainder). Rewrite is behavior-identical: anthill check 66/66, cli_parse eval 4/4, full workspace suite green, /code-review (max-effort, 2 finders) clean — no external refs to the removed ops, no export, embedded copies include_str! the live path.

