## Attributes

- id: WI-959
- created: 2026-08-03T12:00:18Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-03T14:50:13Z

- acceptance: cargo-test

## Description

Two typing.rs test modules still hand-roll the stdlib-loading fixture, and their walk is the one WI-747 fixed.

WI-955 lifted `#[cfg(test)] mod stdlib_fixture { load_stdlib(extra: Option<&str>) }` in rustland/anthill-core/src/kb/typing.rs at the second caller, and routed wi802_function_spec_owner_tests plus the new wi955_one_alias_keying_tests through it. Two older copies remain, each with its own directory walk:
  wi341_alpha_tests::load_ops    typing.rs ~40000
  wi464_variance_join_meet_tests::load_kb  typing.rs ~40115

Both hand-roll a recursive `collect()` instead of crate::fs_util::collect_files. That is the exact pattern WI-747 replaced elsewhere, and the reason is written on the lifted fixture: a hand-rolled walk returns an EMPTY list on an unreadable/missing dir, so the module's headline test then fails as 'the stdlib declares no <X>' - blaming the subject for a bad path. The lifted copy fails LOUD with the path instead.

SCOPE: route both through stdlib_fixture::load_stdlib and delete the local walks. Check the shapes first - load_ops may want something load_stdlib does not return (it is named for ops, not a KB), so the fixture may need one more accessor rather than a swap.

ACCEPTANCE: no directory walk left in typing.rs's #[cfg(test)] modules outside stdlib_fixture (grep is the check). cargo-test green.

SOURCE: WI-955 /simplify reuse review.

## Changes

### 2026-08-03T14:50:14Z — feedback — user

DELIVERED, with scope expanded on user direction mid-session.

WHAT THE TICKET ASKED: route wi341_alpha_tests::load_ops and wi464_variance_join_meet_tests::load_kb through typing.rs's stdlib_fixture and delete their hand-rolled walks. Done, then the USER asked 'why it in typing, not in test?!' and directed the full lift.

THE ANSWER TO THAT QUESTION, both halves. It CANNOT live in anthill-core/tests/common: that is a separate crate and sees only pub items, while these tests drive crate-private ones (typing::join_types and ::meet_types are bare fn, ::is_function_spec likewise, and region is a pub(crate) mod). But 'in src/' is not 'in typing.rs' - THREE kb/ modules hand-rolled the same sequence. New crate-internal kb/test_support.rs (#[cfg(test)] pub(crate) mod) with load_stdlib / load_stdlib_and_stl; typing.rs's mod stdlib_fixture deleted (61 lines, 5 modules repointed), region.rs and op_requirements.rs converted. op_requirements' workspace_root() cwd-walk replaced by CARGO_MANIFEST_DIR - a compile-time constant, cwd-independent.

THE REAL FINDING, and it is bigger than the ticket's. The ticket framed the hand-rolled walks as a MISDIAGNOSIS risk (empty list on a bad dir -> 'the stdlib declares no X'). Driving it found something worse: load_ops also ran `let _ = load::load_all(..)`. MEASURED, same broken fixture (an unresolved import), two code versions - old: the test PASSES; new: fails with '3:39: unresolved import'. Not a worse message - NO GUARD AT ALL. wi341's two tests would have stayed green over a half-loaded KB.

USER'S THREE QUESTIONS, each found a defect I had shipped or was about to:
1. 'Why we receive and silently throw-out Err?' -> the survey. 30 matches, of which one legitimate use (a fixture DELIBERATELY carrying an unresolvable shape, documented at anthill-smt-gen/tests/common/mod.rs:34, handled there by a two-function split), and a NAME TRAP: common::load_kb_with PANICS in anthill-core/tests and DISCARDS in anthill-smt-gen/tests. -> WI-966.
2. 'Why we need a load a broken stdlib?!' -> I had added a should_panic test that loaded the entire stdlib to assert a panic about a FIXTURE. Removed. It also exposed that my panic message said 'stdlib load errors' when the fixture is the likely culprit - a false diagnostic, the exact misdiagnosis the WI-747 comment three lines above claims to prevent. Now 'load errors (library + fixture)', with the reason at the site.
3. 'Why we use invalid fixtures at all?!' -> the decisive one. Flipping BOTH remaining `let _` to strict: op_requirements' fixture loads CLEAN (6 passed - the discard bought nothing). region.rs's failed on ONE thing: its own fixture spelled the unit value `unit`.

THE unit SPELLING DOES NOT EXIST. stdlib/anthill/prelude/unit.anthill said '`unit` is a built-in literal constant' and 'Requires: grammar keyword `unit`, Rust `Literal::Unit` variant'. VERIFIED: grammar.js has no unit keyword (the only spelling is `()`, an empty tuple_literal, line 1321) and there is no Literal enum in parse/ir.rs at all. The SPEC already settles it - kernel-language.md section 4.5, `TupleLiteral ::= '(' ')'  -- unit value`. So this was not unimplemented work, it was a comment CONTRADICTING the spec, and region's fixture was written against it. Fixture fixed (`unit` -> `()`), comment corrected to state the `()` spelling and that no bare keyword exists. BLAST RADIUS CHECKED: no other .anthill or inline .rs fixture uses it (the `unit(42)` hits in wi453_hk_concrete_fill_test are calls to the monadic unit OPERATION, unrelated).

CONSEQUENCE: there is NO lenient entry point in the new module, because measurement showed nothing needs one. `let _ = .. load_all(..)` is now absent from all of anthill-core/src/ (verified 0).

/simplify (four angles run inline, not via agents):
- EFFICIENCY, fixed: 37 fixture calls each re-walked and re-parsed the whole stdlib. Now LazyLock<Vec<ParsedFile>> per library, the shape anthill-smt-gen/tests/common:22 already uses. MEASURED over the 29 tests in the fixture-bearing modules: 22.0s -> 19.1s user CPU (~13%). Smaller than expected - the KB build dominates, not the parse - and the doc comment SAYS SO rather than overclaiming (my first draft claimed ~0.75s/test, which the measurement refuted).
- REUSE, none available: verified no stdlib-path helper exists in src/, and tests/common is unreachable. Stated in the module header so the next reader does not re-derive it.
- SIMPLIFICATION / ALTITUDE: clean.
- SKIPPED, filed as WI-967: every fixture calls register_prelude + register_standard_builtins, and load_all calls register_prelude itself (4196), which calls register_standard_builtins itself (3547) - both done twice, at every fixture in the workspace. Changing one fixture while the rest keep the pattern is how the next reader concludes the fixture is the odd one out.

TESTS: no #[test] added or removed (verified on the diff), so the count is the same set as HEAD. Full workspace 4074 passed, 0 failed. Net -173 lines across 4 files, +97 in the new module.

CONTROL, stated honestly: this is a refactor, so the existing tests pass both before and after BY DESIGN - they are not the evidence. The evidence is the two flip-to-strict measurements above, each of which changed a result: wi341 green->red on a broken fixture, and region 4-passed->4-failed revealing the `unit` defect.

NOT COMMITTED and /code-review NOT RUN (it is user-triggered; CLAUDE.md gates commit on it).

FOLLOW-UPS FILED: WI-966 (24 remaining `let _ = load_all` sites + the load_kb_with name collision), WI-967 (double registration of prelude/builtins).

NOTE ON NUMBERING: filed first as WI-964/965, which COLLIDED with two scaland tickets pushed from WI-961's session while this work was uncommitted. Re-filed as WI-966/967 against the updated tracker; if any text elsewhere cites WI-964/965 for these, it means this.

