## Attributes

- id: WI-759
- created: 2026-07-18T14:45:06Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-18T16:11:05Z

- acceptance: cargo-test

- depends_on: WI-734

## Description

Retire the LAST two op-name-keyed typer hatches (field_access_projection_type, field_access_owning_ctor) and the INC-1b/mode-3 STAMP by making field_access's return type SELF-CORRECTING via a reducible type constructor -- and supply the enabling primitive (a compile-time NAME in TYPE-ARGUMENT position) that WI-732's Project[T, keep] needs too. DISCIPLINE: the repo has an explicit "no op-name-keyed typer code" rule -- stdlib/anthill/prelude/relation.anthill:256-262 ("NOTHING in the typer keyed on fix's identity ... the alternative was an op_sym == Relation.fix recognizer, which that discipline forbids") and typing.rs:1741-1743 ("keyed on the constructor's sort symbol ... never keyed on a domain operation's identity"). check_apply_iter's ladder has exactly THREE name-keyed early returns: :7290 field_access_owning_ctor, :7298 field_access_projection_type, :7311 relation_projection_type -- verified EXHAUSTIVE (the only fn_sym ==/!= guards in typing.rs are :3737, :7019, :7081). relation_projection_type carries a self-disclaimer (:3719-3728, "a guard, not a hot path"); the two field_access hatches carry none. DIAGNOSIS: the desugared node is ill-typed against its OWN declared signature field_access(object: Term, field: Symbol) -> Term (reflect.anthill:158) in ALL THREE positions -- the receiver is an entity/named-tuple (and Term is NOT a top type), the selector is Const(Literal::String) not Symbol, the result is the FIELD's type not Term. It survives only because :7298 returns before ordinary op typing. That this is a genuine contradiction and not a harmless convention is demonstrated by a COMPETING implementation: anthill-stl/src/reflect/builtins.rs:883-905 registers anthill.reflect.field_access under the SAME QN implementing the DECLARED signature (expect_term + expect_symbol) -- it would reject every typer-synthesized node. Harmless today ONLY because register_reflect_builtins is called exclusively from its own unit test (:1102) while production (anthill-stl/src/runner.rs:43) registers only standard builtins; register_builtin is a plain HashMap insert (eval/mod.rs:408-417), LAST WINS, so wiring the reflect builtins into any real driver after the standard ones would silently shadow the working impl. That landmine should be closed (or pinned by an assertion) regardless of the rest. CRUX -- the field NAME is a VALUE that must reach TYPE position: there are NO singleton/literal types, a String const in value position types as plain String (typing.rs:5071-5073), so FieldOf[R, F] with F bound from the VALUE argument receives String and has LOST the name. The only existing route is the DENOTED type-arg channel: the grammar admits a literal in type position (grammar.js:156-166), parser -> TypeExpr::Denoted (convert.rs:558-562), loader -> make_denoted_occ (load.rs:10785-10796), typer TypeExtractor::Denoted (:25428) + unify_denoted_view (:19896). Exercised today ONLY for INTEGER denoteds (wi404_denoted_self_conformance_test.rs:61-72, Vec[T = Int64, N = 3]); there are ZERO string-valued type arguments anywhere in the repo, so this would be the FIRST. WI-708 (Delivered) made the eval type-arg channel actually carry its bindings, so a consumer that READS type args is now viable -- its own text flags it was "a live trap for the next one that does". TWO ENCODINGS, weigh before building: (a) CHEAP -- keep pos_args[1] = Const(String) EXACTLY as today (eval/resolve/codegen UNTOUCHED) and have the forward synthesis populate the node's RESOLVED TYPE ARGS (occ.set_resolved_type_args, the WI-708 channel) with name = denoted "f"; signature field_access[R, name](object: R, field: String) -> FieldOf[R, name]; the re-type reads the type args off the STORED node and reduces generically. Blast radius = typer only. (b) FULL -- move the selector INTO the type-arg channel: blast radius crosses 5 crates (eval/builtins.rs:213-291 accepts Value::Str ONLY and hard-errors otherwise; resolve.rs:5265-5271/:5288-5295/:5340-5378; body_specialize.rs:666-696; anthill-cpp-gen/src/lib.rs:2186-2199/:2458-2485; anthill-smt-gen/src/lib.rs:1452-1476) and would add a THIRD selector encoding to a contract that already carries TWO (P1 parse/loader form, Term::Ident at pos_args[1], from convert.rs; P2 typer form, Const(String)), whose lockstep is documented at body_specialize.rs:672-679. PREFER (a) unless it fails -- it leaves the P1/P2 contract alone. FieldOf REDUCTION must cover THREE receiver cases from ONE lookup SHARED with the forward direction (that sharing is the whole point -- it makes forward/reverse drift structurally impossible, which is the WI-758 defect): ENTITY/sort (field_constructors_of_sort + entity_field_types + resolve_field_type, what :7031-7039 does), NAMED TUPLE (TypeExtractor::NamedTuple(Vec<(Symbol, Value)>) at :25445, by-name lookup as at :6182-6194), and Term (the genuine reflect metaprogramming call -> Term). The same lookup must ALSO yield the OWNING CTOR so WI-369's internal-visibility check (internal_field_hidden_from, :7054-7071) rides the same resolution instead of a second traversal -- owner is None for a named tuple (components have no owning ctor and no internal marker), which is the CORRECT answer there, NOT a missing arm. SETTLE an inconsistency while here: :6182-6194 matches components by SHORT name, without_named_tuple_types (:1877) by full symbol identity -- pick one for a name-keyed ctor. MACHINERY: BinaryTypeCtor (typing.rs:1713) + reduce_binary_type_ctor (:1750), reduced at the ONE return-type normalization boundary in check_apply_iter (:7913-7928); registration = inert sort in stdlib/anthill/prelude/sort.anthill (Concat :60, Without :88) + reducer + const + widen return_reducible_ctors (:1672, TODAY A HARDCODED 2-TUPLE) + the if-chain at :7916-7925 + the signature. A third ctor forces widening both, so generalize the table to a slice while here rather than growing the if-chain again. DEPENDS ON WI-734 (do NOT decide it here): the family's ABSTRACT-OPERAND rule. Today an unresolved operand does NOT stay symbolic -- it falls to the _ arm (:1802), reaches the reducer, and errors "must be a named-tuple type" -> TypeError::Other -> LOAD-BLOCKING (:1618-1623), so a reducible ctor CANNOT sit in a signature checked before the receiver type is known. field_access forces this harder than Concat does, because the P1 user-written field_access(?t, f) in a rule body has NO static receiver at all. WI-734 says settle it ONCE for the family (Concat/Without/Project) -- FieldOf joins that family. Contrast precedent for the lazy reading: s.T DOES survive as a neutral residual (ExprCarried, :25429-25434). UNBLOCKS WI-732 (cross-reference, do NOT merge): its stated reason for deferring Project[T, keep] is that the current shape "is legitimized by the field_access INC-1b stamp+re-derive precedent, and encoding a keep-spec in TYPE position is awkward" -- this WI removes that precedent AND supplies the name-in-type-position primitive the keep-spec needs. NOT a constraint (checked): op/rule bodies are NEVER printed back to .anthill text -- persistence writes FACTS only (print_fact takes a TermId, print.rs:1095-1106; persistence/** has zero field_access hits) and print_occurrence's only non-test callers are CLI display (anthill-cli/src/main.rs:1524, :1561). So there is no text round-trip to preserve; the real idempotency constraint is the IN-MEMORY re-type (:7290, :7298) plus the WI-506 effect re-key in stable_receiver_path (:17747-17759), which must keep working -- it is what preserves the Modify[c] -> Modify[arg] head, and stdlib/anthill/prelude/mutable_stack.anthill:16 documents that as load-bearing. ACCEPTANCE: field_access's declared signature is self-correcting (its return type reduces to the field's type at the same boundary Concat/Without reduce at); field_access_projection_type and field_access_owning_ctor are GONE and check_apply_iter's ladder has at most ONE name-keyed early return left (relation_projection_type, WI-732's); the INC-1b and WI-638 mode-3 set_inferred_type stamps are GONE, both paths synthesizing and push_visit-ing like the INC-1a method fallback already does (:6077-6081), so forward and reverse are ONE decision procedure; the WI-506 effect re-key still preserves Modify heads; a named-tuple field access survives a re-type round-trip (WI-758's acceptance, satisfied structurally rather than by a second arm); the anthill-stl duplicate field_access is retired or pinned; existing wi638 / wi509 / wi364 / wi714 / wi727 tests green; cargo-test green via scripts/test.sh.

## Changes

### 2026-07-18T16:11:45Z — feedback — user

DELIVERED. FieldOf[T, Name] joins the Concat/Without binary-type-ctor family; field_access is now field_access[R, Name](object: R, field: String) -> FieldOf[T = R, Name = Name] and both name-keyed hatches are GONE. check_apply_iter's ladder has exactly ONE fn_sym guard left (typing.rs:4029, relation_projection_type / WI-732) -- verified by grep for fn_sym ==/!=. ENCODING: took the ticket's option (a), the CHEAP one -- pos_args[1] stays the String const, so eval / SLD / cpp-gen / smt-gen are untouched; the name ALSO rides the type-arg channel as a denoted. But NOT via set_resolved_type_args as the ticket proposed: that is the eval OUTPUT channel, rewritten by check_apply_iter itself at :8452, so a re-type would clobber it. The right channel is Expr::Apply.type_args, the INPUT seed_op_type_args reads -- the same channel a user's [Name = "f"] writes, which is what makes the node self-describing. MEASURED, not reasoned: the denoted must be GROUND (make_denoted over a hash-consed Const), not the occurrence-form make_denoted_occ. The return type is term-backed, so the type-param binding resolves through the TermId deep sigma-walk, which STOPS at a non-Term binding (WI-394) -- with a Value::Node denoted every projection reduced to a residual and 34 tests failed with 'FieldOf.<col>: no such member'. A field name is closed ground data anyway, exactly what hash-consing is for. SHARED LOOKUP: resolve_projected_member is the one resolution both directions use -- that sharing is the deliverable, not a detail: it is what makes WI-758's drift structurally impossible rather than fixed. WI-369 rides it via hidden_field_owner. The reduce contract gained a CtorReduceSite (sp, span, enclosing scope) because FieldOf is scope-SENSITIVE while Concat/Without are purely structural. THREE REVIEW FINDINGS FIXED (all real, two probed live): (1) putting the Term arm in the SHARED lookup silently widened FORWARD dot dispatch -- 't.definitely_not_a_field' on a Term receiver LOADED CLEAN (probe-verified in both states). A Term declares no members; 'any name projects' is right for the REDUCTION only. Moved into field_of_type; regression test pins it. (2) owner was picked by find_map over field_constructors_of_sort, whose order is HashMap iteration order -- load.rs:10154 already documents that hazard and its remedy. A multi-variant sort with one internal variant sharing a field name got a RUN-DEPENDENT visibility verdict. Now collects EVERY declaring ctor; verdict is any-hidden (deterministic + encapsulation-safe). Pre-existing in both retired hatches; consolidation made it a one-place fix. (3) the forward probe built and discarded a format! diagnostic on every miss -- i.e. on every relation column projection r.col. Split into MemberMiss::NoSuchMember (no message; the forward direction's legitimate fall-through) vs Unresolvable (a real diagnostic, now LOUD in both directions via a new TypeErrorContext::DotProjection -- previously the old INC-1b swallowed it with .ok()?). ACCEPTED COSTS, not oversights: field_access having type params turns on the previously-skipped set_resolved_type_args block per projection, and synthesize_field_access does one extra lookup_operation_info_full (an O(1) map hit that Path 1 repeats immediately after) to read the Name param symbol off the DECLARATION rather than kb.intern("Name") -- WI-708's lesson that the two keyings differ. Suppressing either would need op-identity-keyed code, which is the thing this WI removes. A positional type arg would be free but binds by declaration ORDER, silently wrong on a reorder; a loud lookup beats a quiet mis-bind. STL LANDMINE CLOSED: the duplicate anthill.reflect.field_access in anthill-stl/src/reflect/builtins.rs is DELETED (it implemented the never-live expect_term+expect_symbol shape and would have shadowed the working core impl, register_builtin being last-wins). Its unit test is replaced by one that installs BOTH sets in the shadowing order and asserts the PRODUCTION contract still answers. Codegen fallout: reflect.anthill's new 'import anthill.prelude.{FieldOf}' emits a use line, so prelude/mod.rs gains an opaque FieldOf stub -- the same treatment TypeExtractor already has for the excluded free op 'extract'. SETTLED (the ticket's inconsistency item): a NAME-keyed ctor matches by SHORT name (the convention of named_tuple_compatible / thread_expected_tuple_field / forward dot dispatch, and forced anyway -- there is no symbol to compare); without_named_tuple_types keeps symbol identity, correctly, because both ITS operands are type-level tuples from one producer. Documented on resolve_projected_member. TESTS: 10 in wi759_field_of_type_test.rs. A/B-VERIFIED rather than asserted: wi759_named_tuple_projection_survives_retype FAILS on pre-change code with the ticket's verbatim 'type mismatch in field_access.field (op-arg): expected Symbol, got String' -- that is WI-758's acceptance, met structurally. Its entity sibling PASSES pre-change (the old hatch covered that arm) and is labelled a preservation control, not a second regression test. Full workspace: 3048 passed, 0 failed (baseline 3038). NOT DONE / follow-on: WI-758 is satisfied by this and should be closed rather than worked. WI-732 is now unblocked -- its stated blocker (the field_access stamp precedent, plus 'encoding a keep-spec in TYPE position is awkward') is removed, and Project[T, keep] can use the string-in-type-position primitive this adds; it is also the last name-keyed early return in the ladder.

