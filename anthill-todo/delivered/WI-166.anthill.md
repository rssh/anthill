```anthill
fact WorkItem(id: "WI-166", created: "2026-05-03T08:19:07Z", context: none, acceptance: [ToolPasses(tool: "scaland-sbt-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-05-03T08:18:51Z"))
```

## description

scaland — make match expression `end` keyword optional. Today scaland's matchExpr requires an explicit `end` keyword to terminate the arm list (`match scrut case ... case ... end`). Stdlib cli/help.anthill and cli/parse.anthill use the indentation-delimited form without trailing `end`. Both files fail to parse mid-format_help / parse_argv operation because matchBranch.rep(1) stops at the next non-case token, then matchExpr requires `end` and fails when it sees `operation` instead. Fix: make `keyword("end")` an optional suffix (`.?`) — matchBranch.rep(1) naturally terminates at the first non-case token. Mirror reference: rustland's tree-sitter grammar accepts both forms. Files: scaland/core/src/main/scala/anthill/parse/AnthillParser.scala (one-line change to matchExpr). Acceptance: sbt test green; cli/help.anthill and cli/parse.anthill parse cleanly; new ParseTest case for indentation-delimited match.

