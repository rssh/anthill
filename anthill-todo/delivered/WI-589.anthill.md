## Attributes

- id: WI-589
- created: 2026-06-29T06:20:31Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-30T14:55:29Z

- acceptance: cargo-test

- depends_on: WI-588, WI-598

- tags: finiteness

## Description

Finiteness Phase C (proposal library/003): remove the unsound homes. Delete count/foldLeft/foldRight/collect from stream.anthill and size/foldLeft/foldRight from iterable.anthill. Regenerate the WI-553 Stream trait (anthill-stl/build.rs -> OUT_DIR/stream.rs drops them) and remove the matching SearchStreamAdapter impls in anthill-stl/src/reflect/bridge.rs (count, fold_left, fold_right, collect; retire fold_solutions if unused). Rewire eval_test wi064 + wi424_iterable_members_test to the FiniteCollection ops. IMPORTANT: removal must be in BOTH spec AND Rust host. Suite stays green because Phase B already rewired the consumers. Depends on Phase B.

## Changes

### 2026-06-29T14:32:17Z — feedback — user

Phase B did NOT rewire the consumers (the WI premise is wrong): removing the eager consumers from Stream/Iterable breaks 46 tests across 16 files. ~half are mechanical import swaps (Iterable.{size,fold}/Stream.{collect,fold} -> FiniteCollection); the other half call collect on an abstract map/filter/iterator/rewrap result and hit WI-598 (collect on an abstract FiniteStream: 'no impl matches per-call bindings'). Several of those (wi368/wi411/wi424-bump/wi439/wi461) test collect-returns-List element-precision, which can't be faithfully kept without WI-598. Also wi461/wi411 helpers return a BARE Stream (would need to become FiniteStream). Decision (user, 2026-06-29): deliver WI-598 first, so added depends_on WI-598; then Phase C is a clean mechanical rewire. The spec+bridge removal itself is straightforward and verified to compile (regenerated stream.rs drops the ops, bridge SearchStreamAdapter drops count/fold_left/fold_right/collect + retires fold_solutions). Per-file rewiring notes for when WI-598 lands: wi345 = drop count to 2 (map/filter only, its own comment predicts this); wi495 IntBag must 'provides FiniteCollection' (collect=items, finiteIterator=items) mirroring Map; wi492 iterable_size_on_lazy_mapped_stream tests a REMOVED capability (sizing a lazy mapped stream) -> repurpose to find/isEmpty; wi374 Stream.count(s) on a bare Stream -> length(takeN(s,1)) or similar; wi365 collect_wrong_element_return currently passes only because the import fails (false positive) -> rewire to FiniteCollection.collect.

