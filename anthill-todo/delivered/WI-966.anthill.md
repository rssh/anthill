## Attributes

- id: WI-966
- created: 2026-08-03T14:50:43Z

- status: Delivered
- status_agent: user
- status_at: 2026-08-03T19:06:49Z

- acceptance: cargo-test

## Description

Twenty-four test fixtures still discard the loader's Err, and the same helper NAME means opposite things in two crates.

WI-959 removed the last `let _ = load::load_all(..)` from anthill-core/src/ (see kb/test_support.rs). 24 code sites remain, all under tests/: 19 anthill-core/tests/include/*, 2 anthill-smt-gen/tests, 1 anthill-cpp-gen/tests/common.

WHY THIS MATTERS - MEASURED, not theoretical. With `let _`, region.rs's wi353 fixture carried an unresolved name for its whole life and all 4 of its tests passed GREEN over a half-loaded KB. Flipping it to strict was the only thing that surfaced it. WI-887 recorded the same failure mode in anthill-smt-gen (wi680_ite_lowering_test). A discarded Err does not merely degrade a diagnostic - it lets a test assert over a KB that never finished loading.

THERE IS ONE LEGITIMATE USE, already documented at anthill-smt-gen/tests/common/mod.rs:34: a fixture that DELIBERATELY carries an unresolvable shape. That crate handles it correctly with a two-function split. The 24 sites are not that - they are copy-propagation of a house pattern (the one WI-959 removed arrived with commit 892a5ddd as a copy, and its fixture loaded clean).

THE NAME TRAP, verify before touching anything:
  anthill-core/tests/common::load_kb_with   PANICS on load errors (try_load_kb_with is the Result twin)
  anthill-smt-gen/tests/common::load_kb_with  DISCARDS them (load_kb_strict is the panicking twin)
Same name, opposite guarantee, same workspace. Renaming one is probably the real fix; decide before converting call sites.

SCOPE: for each of the 24, determine by MEASUREMENT (flip to strict, run) whether its fixture loads clean. Convert those that do. For any that genuinely need leniency, route through a NAMED lenient entry point, never a bare `let _`. Resolve the load_kb_with name collision.

ACCEPTANCE: no bare `let _ = .. load_all(..)` outside a named lenient helper; the two load_kb_with spellings no longer disagree; cargo-test green.

SOURCE: WI-959.

## Changes

### 2026-08-03T19:06:46Z — feedback — user

DELIVERED. Acceptance met on all three counts; NOT COMMITTED.

THE TICKET'S COUNTS WERE BOTH OFF, and the difference mattered. "24 code sites" is 22 FILES; the site count is 28 (25 in anthill-core/tests/include across 19 files - not all `load_all`: 3 are `load_incremental`, 1 is `load` - plus 2 smt-gen, 1 cpp-gen). Two more `let _ = load::scan_definitions(..)` sites (a different return type, `Vec<LoadError>`, so no grep for `load_all` finds them) were also discarding; MEASURED empty at both, now asserted.

MEASUREMENT FIRST, exactly as the ticket asked: all 28 flipped to panic at once, full workspace run, triage the failures. 22 of 28 loaded clean - the discard bought nothing. The 6 that failed named THREE genuinely broken fixtures plus one deliberate one, all silent for as long as the discard was there:
- toml_ser_test: `17:72: unresolved name 'List'` carried through all 18 of its tests.
- specialization_witness_test (2 tests): `B provides A, which requires Eq, but B does not provide Eq`.
- wi224_sld_resolution_test (2 tests): the same coherence complaint - and here it is DELIBERATE (the file's own comment says so: an unsatisfiable `PartialEq` leg is the subject, recorded as `Unavailable`).

THE TICKET'S "ONE LEGITIMATE USE" WAS IN THE WRONG CRATE. It pointed at anthill-smt-gen/tests/common/mod.rs:34, whose comment states the discard is "load-bearing for the fixtures that deliberately carry an unresolvable shape". REFUTED by flipping it: all 100 smt-gen tests pass strict. No such fixture exists there. So the name trap DISSOLVED instead of being renamed around - the discarding `load_kb_with` is deleted and `load_kb_strict` takes the name. `load_kb_with` now means panic-on-load-error in all three test crates, and `load_kb_strict` no longer exists to choose wrongly between. (WI-959's memory recorded the same shape: a comment justifying a silence is evidence about the author, not the code.)

THE REAL LEGITIMATE USE IS cpp-gen's `load_kb_with_lenient`, which the ticket listed as a site to convert. It is already a NAMED lenient helper, so it meets acceptance as-is - but its 57 callers had never been measured. Flipping the whole crate strict: 53 load clean and now go through `load_kb_with`; 4 stay, each with the measured error at the call site (three `unsupported_test` degrade-to-`static_assert` fixtures and `higher_kinded_arrow_test`'s applied first-order param - all write the error they test). Of the 6 original failures, only 4 were real: TWO WERE PLAIN FIXTURE BUGS whose call-site comments blamed the typer.
- expr_body_f: comment said "the typer doesn't yet model raise's `Nothing` return as compatible with `Int64`". The loader was complaining about something else entirely - `expected declared: [Error], got undeclared effect: Error[T = String]`. A missing type argument.
- expr_body_c: `operation lam(n: Int64) -> Int64 = lambda x -> add(x, n)`. Declaring the arrow return type it actually has loads clean and lowers identically.

A FOURTH DISCARD CHANNEL THE TICKET DID NOT LIST: cpp-gen's strict `load_kb_with` skipped its own panic when `ANTHILL_TEST_IGNORE_LOAD_ERRORS` was set - an env var nothing in the workspace sets, that would have silently downgraded EVERY test in the crate. Removed; the opt-in is the named helper.

toml_ser's RESOLVER WAS FICTION TOO. Fixing the missing import surfaced `List` having no type parameter `T`. `FileSourceResolver` maps `anthill.prelude.List` to `anthill/prelude/List.anthill` and the file is `list.anthill`, so every lookup missed and `List` was only ever the bare name `register_prelude` registers. CONTROL: swapping that resolver for `NullResolver` changed nothing - 20 passed / 1 failed either way, same error. Both helpers now load the real stdlib via `common::load_kb_with`, which is what their names always claimed. (The case convention is a live resolver question, not touched here - no ticket filed, since no caller in the workspace depends on it and one would be inventing a requirement.)

WHAT LANDED
- `common::expect_loaded` - one owner of "a load error fails the test", for the suites that build their own file set (custom resolver, incremental second phase, stdlib-only) and so cannot go through `load_kb_with`. All 25 core sites route through it.
- `common::expect_load_errors` - the deliberate-dirty twin. wi224's two fixtures now PIN their coherence complaint instead of discarding it; a fixture that starts loading CLEAN fails just as loudly as one acquiring a second error.
- specialization_witness's two fixtures made coherent (they never meant to be incoherent - nothing in the file measures that).
- smt-gen: one loader. cpp-gen: `load_kb_with_lenient` documents its 4 survivors and why.

CONTROL, stated honestly. This is a strictness change, so the ~4080 existing tests pass with the discards restored BY DESIGN - that property is exactly what let three broken fixtures live this long, and it is why the delivery includes `wi966_loader_verdict_test`. MEASURED: restoring ONE discard (logic_sorts_test) makes it fail and NAME the file and line, while logic_sorts_test's own two tests keep passing. It carries its own recogniser control, since a typo in the matcher would make it pass vacuously over a tree full of discards, plus a floor on the file count for the same reason. The other evidence is the flip-to-strict run itself: 28 sites, 6 result changes, 3 broken fixtures.

/simplify (four angles inline, no agents):
- EFFICIENCY, fixed: `try_load_kb_with_files` has 683 call sites in anthill-core's suites and re-walked, re-read and re-parsed all 76 stdlib files at every one. Now `LazyLock<Vec<ParsedFile>>`, the shape smt-gen/cpp-gen already use and WI-959 put on the src/ side. MEASURED on wi_tests: 86.1s -> 79.3s wall, 32m33s -> 29m53s user CPU (~8%). Modest, and the doc comment says so rather than overclaiming - the KB build dominates, not the parse, which is the same thing WI-959 measured.
- REUSE, fixed: `load_kb_with` had its own `unwrap_or_else` spelling of the panic policy `expect_loaded` owns. `expect_loaded`/`expect_load_errors` are generic over `E: Display`, so the `Vec<String>` twin shares them.
- SIMPLIFICATION, fixed: toml_ser's `load_kb_with_stdlib` had become a one-line alias for `common::load_kb_with` while `build_test_kb` in the same file called the shared helper directly - two names for one thing. Deleted.
- ALTITUDE: clean. The one judgement recorded: `wi966_loader_verdict_test`'s ALLOWED list is keyed by PATH, which a file move breaks - accepted, because the failure mode is a loud test failure and the alternative (allow any file containing a `_lenient` fn) would let a real discard hide beside a legitimate one.

TESTS: 4080 -> 4082 (the two new guard tests; verified on the diff that no other #[test] was added or removed). Full workspace green, 0 failed. Full-run wall time 245-252s -> 234s.

NOT COMMITTED, and /code-review NOT RUN - it is user-triggered and CLAUDE.md gates commit on it.

NO FOLLOW-UP TICKETS FILED. The two candidates were considered and rejected: the `FileSourceResolver` case convention (no caller depends on it; a ticket would be inventing a requirement) and the 19 remaining hand-rolled stdlib walks in tests/include (each keeps its own file set and resolver deliberately, so consolidating them would change what fixtures load - the copy-propagated PANIC POLICY, which was the actual defect, now has one owner).

