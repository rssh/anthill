```anthill
fact WorkItem(id: "WI-1016", created: "2026-08-05T12:48:59Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-05T21:41:26Z"))

fact Feedback(workitem: "WI-1016", author: "user", at: "2026-08-05T21:41:25Z")
```

## description

Mint Value::SymbolRef in production: complete the workspace carrier audit, then flip `symbol_value`. WI-1015 added the Value::SymbolRef carrier and made every path it already audited correct, but deliberately left `eval/builtins.rs::symbol_value` minting the interned Value::term(Term::Ref) carrier. Flipping that one line makes the variant live across the whole reflect surface at once (Dictionary.impl, OpRef.op), and /code-review found the audit it requires is WORKSPACE-wide while WI-1015's grep covered only anthill-core/src. Sites with a FORCED answer (mirror the Term::Ref twin) are already fixed under WI-1015: scalar_eq (two identical symbols compared UNEQUAL), scalar_value_expr (subst_var_leaf panicked; simp_rewrite silently rewrote a redex to bottom), detect_cycle (Modify.set's self-reference guard reported no cycle), constructor_sub_values (MatchDispatch's pre-filter promised an arm the destructure declined), anthill-cli render_value (a symbol answer binding printed the Rust Debug form), op_info list_items_strict. REMAINING, each needing a REPRESENTATION DECISION rather than an arm: (1) anthill-stl expect_symbol reads by carrier and backs qualified_name / short_name / scope / kind / resolve_sort_instantiation_param — the eval twins of ops whose SLD twins WI-1015 widened, so one op would answer two ways; KnowledgeBase::value_symbol is pub for exactly this. (2) MapKey has no Ref variant and try_from_value has no &mut kb to intern one, so a Symbol-keyed Map hard-errors — a representation gap, not a missing arm. (3) fn_value's all-Term test would push a SymbolRef-bearing head onto the Value::Entity path and into the DISJOINT value_fact_dedup key space (WI-815), storing two facts for one logical fact. (4) is_duplicate_projection's negative carrier test drops a SymbolRef binding into the opaque bucket and disables answer dedup (fail-open, extra answers). Deliver with a test that FAILS when symbol_value is reverted — WI-1015's did not, because sym_qn reads by content and accepts both carriers.

## Feedback

### 2026-08-05T21:41:25Z — user

DELIVERED. The flip plus the four representation decisions, each with a control MEASURED by backing it out (and RE-MEASURED after the /code-review refactors, because restructuring can silently break a control). (1) anthill-stl expect_symbol reads through KnowledgeBase::value_symbol. (2) MapKey gained Ref(Symbol); try_from_value takes &KnowledgeBase and canonicalizes the Term::Ref twin into it, so one symbol is one map slot -- narrower than value_symbol ON PURPOSE, since for a store key a widening MERGES keys and one put would overwrite another. (3) fn_value test became "is a leaf" rather than "is a Value::Term". (4) is_duplicate_projection admits SymbolRef. CONTROLS: revert symbol_value -> 3 tests fail on the carrier premise; revert expect_symbol -> all five host symbol ops TypeMismatch while their SLD twins keep answering; drop MapKey::Ref -> Map.put hard-errors; drop its Term::Ref arm -> cross-carrier Map.get answers none; revert fn_value -> RuleId(0) vs RuleId(1) for ONE logical fact; revert is_duplicate_projection -> 2 solutions where 1 is right. 4170 passed. THE FLIP IS DELIBERATELY PARTIAL: lookup_symbol / scope / builtin_lookup_symbol still mint Term::Ref, so BOTH carriers stay in circulation. Flipping them all would have made the cross-carrier seams untestable by removing the second carrier -- the MapKey hole is only observable because both spellings exist. TWO SITES THE TICKET OWN LIST MISSED, both found by review, both fixed here: anthill-stl ref_repr_symbol (the same by-carrier match as expect_symbol, 60 lines up in the same file, reading a TermRepr name field -- DECLARED Symbol, so RefRepr(name: Dictionary.impl(d)) is well-typed and was rejected); and op_info head_name_ref, whose None means "skip this fact", so an OperationInfo would have gone silently invisible. THREE OF MY OWN CLAIMS WERE WRONG AND ARE CORRECTED AT THEIR SITES. (a) The dedup test first shape measured nothing: I put the multiplicity in the rule BODY, so the nearest ancestor ChoicePoint was the body disjunction, whose two answers genuinely differ -- the two routes must be alternatives for ONE goal. (b) Its premise assert read resolve_as_value, which stops at Term::Var; the rule route binds ?q -> Var(?v) with ?v -> SymbolRef, so it needs a full reify walk. (c) MapKey doc said "a widening MERGES KEYS" of all three carriers value_symbol adds -- true only of Term::Ident; declining a non-canonicalized nullary Fn{c,[],[]} accepts a key SPLIT, and Value::Node is a REFUSAL. FILED WI-1023 (renumbered from a local WI-1020 that collided with the scaland Bootstrap ticket pushed to main meanwhile) rather than fixed inline, because all three parts move in the answer-DROPPING direction and need their own controls: is_duplicate_projection allow-list was ALREADY stale before this ticket (Requirement and OpRef became structural in WI-1019, the commit immediately before; Value::Var fingerprints as a Var token -- all three still switch dedup off); fn_value leaf set is narrower than what alloc_from_value lowers, so a SCALAR child has the same disjoint-key-space split (measured unreachable: all four assert_fact_carrier callers pass Value::term); and MapKey nullary-Fn split. Each site now carries the TICKET NUMBER, not a justification. Also from review: leaf_term_of restated alloc_from_value two arms verbatim -- deleted, since that would be a second untied statement of the variant whole contract; symbol_value dropped its unused &mut KnowledgeBase (a function demanding a store write advertises the write this change removes); the four Map builtins share one map_key helper; the two symbol-ops tests now drive ONE assert_symbol_ops block, so "both carriers answer alike" is the assertion rather than two hand-copied lists; and anthill-stl load_stdlib_and_source got the LazyLock STDLIB_PARSED that anthill-core/tests/common already had (23 callers were each re-parsing 76 files). Docs: proposal 035 MapKey domain and the value-facts design doc split-key-space section now state the canonicalization rule.

