## Attributes

- id: WI-535
- created: 2026-06-21T08:22:33Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-21T14:49:17Z

- acceptance: cargo-test

## Description

Migrate the host reflect bridge (anthill-stl/src/reflect/mod.rs + bridge.rs) and the anthill-rust-gen contracts from TermId to TermView (carrier-faithful). The bridge + rust-gen were written BEFORE the TermView/carrier-faithful migration (WI-348/470/477), so 'pub type Term = anthill_core::kb::term::TermId' (reflect/mod.rs:10) and the whole bridge reifies every term to a bare TermId — it is NOT carrier-faithful at ANY point (a Value::Node loses its occurrence identity/span). SCOPE: the host Term representation across the KB trait (reify/reflect/nonvar/ground/apply_core_subst), TermRepr, SortInfo/OperationInfo/FieldInfo, Substitution::apply + SubstBridge, AND the anthill-rust-gen codegen that emits/consumes these (generated-code contract changes). TermView is a TRAIT (impl for TermId/Value/Rc<NodeOccurrence>/ViewItem/ReflectedExpr, term_view.rs:902/953/1078/1108), so pick a concrete carrier for the host Term (Box<dyn TermView> or a view enum). Stage if possible: bridge-internal first, then the generated-code surface. Rust-only (anthill-stl + anthill-rust-gen). This is the FOUNDATION WI-534 needs: streaming a Solution with a carrier-faithful residual (Value/Node-preserving) is incoherent while the rest of the bridge speaks bare TermId and would force a lossy reify. Found during WI-534 — the bridge is pre-migration legacy.

