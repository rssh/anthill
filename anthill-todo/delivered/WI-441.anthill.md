```anthill
fact WorkItem(id: "WI-441", created: "2026-06-11T14:52:52Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-439", "WI-440"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-11T16:00:14Z"))

fact Feedback(workitem: "WI-441", author: "user", at: "2026-06-11T15:04:26Z")

fact Feedback(workitem: "WI-441", author: "user", at: "2026-06-11T16:00:13Z")

fact Feedback(workitem: "WI-441", author: "user", at: "2026-06-11T16:05:25Z")

fact Feedback(workitem: "WI-441", author: "user", at: "2026-06-11T16:25:59Z")

fact Feedback(workitem: "WI-441", author: "user", at: "2026-06-11T16:56:31Z")

fact Feedback(workitem: "WI-441", author: "user", at: "2026-06-11T17:23:17Z")
```

## description

Iterable.filter arrow-typed pred with dependent absence: 'operation filter(c: C, pred: (x: Element) -> Bool @ -Modify[x]) -> Stream[Element, ?row]' — the user-proposed form (2026-06-11 design discussion). Two payoffs over the Function[Element, Bool, E] pattern: (1) MUTATION SAFETY — pred cannot modify the element under test (lazy FilteredStream re-walks; a mutating pred makes the filtered view inconsistent), enforced by the WI-440 checking direction; (2) ROW DECOUPLING — pred's effect row is its own (open row minus Modify[x]) instead of being TIED to the collection's access row E, which today forces a PURE pred on List (E = {}). SCOPE: (a) arrow-typed HOF param interop — probe p1 2026-06-11 shows '(x: Int64) -> Bool' param + f(5) application LOADS; verify typecheck+eval with a real lambda/Function value at the call site (arrow vs Function[A,B,E] structural-variance bridge, proposal 035 / WI-293 lineage; WI-275 HOF was Function-sort apply); (b) RESULT ROW — the produced lazy stream pays pred's effects on consumption, so the result row must be merge(E, pred_row); decide whether row-merge of two open tails is expressible (045 merge normal form) or v1 keeps pred_row tied to E with only the -Modify[x] absence added (still gains mutation safety, defers decoupling); (c) NAMING — avoid 'c' for the arrow binder (shadows the receiver 'c: C'; both -Modify[element] and -Modify[collection] are meaningful constraints — consider BOTH: pred must not mutate the element NOR the collection being iterated); (d) evaluate retrofitting find (and map's transform) with the same form. ACCEPTANCE: filter with an effectful-but-element-pure pred typechecks on List; a pred writing through x rejected; result row covers pred effects; cargo-test green.

## Feedback

### 2026-06-11T15:04:26Z — user

user 2026-06-11: Iterable.find MUST get the same -Modify[x] pred form — scope item (d) is not 'evaluate retrofitting' but a requirement; find's pred is '(x: Element) -> Bool @ -Modify[x]' alongside filter's. (map's transform remains 'evaluate'.)

### 2026-06-11T16:00:13Z — user

Delivered 3883e13 (v1). All three members converted: find/filter pred '(x: Element) -> Bool @ {E, -Modify[x]}', map's f '(x: Element) -> Dst @ {E, -Modify[x]}' (the 'evaluate' item resolved YES — worked for free). SCOPE OUTCOMES: (a) arrow~Function interop VERIFIED end-to-end — the arrow-typed callback forwards into the Function-typed Stream.find/mapped/filtered engines with zero engine changes (wi424/wi439 suites unchanged); (b) RESULT ROW: v1 keeps the row TIE ({E | -Modify[x]}; result rows unchanged Stream[.., E]) — acceptance item 'effectful-but-element-pure pred typechecks on List' is CONSCIOUSLY NOT met in v1 (E := {} closes the row; same pure-pred-on-List semantics as the old Function form, now with a LOUD row message — pinned by test). The decoupled independent pred row + merge(E, pred_row) result is the remaining half, blocked on the WI-440-recorded boundary gaps (arrow-Eff vs effects-clause VarId split; row-valued boundary instantiation not decomposed) — keep ticket open or re-file per user preference; (c) binder named 'x' (no receiver shadowing); -Modify[collection] (pred must not mutate the COLLECTION mid-iteration) NOT expressible yet — the receiver 'c' is an outer op param, referencing it from the callback's annotation is the WI-353-era outer-binder case, untested here. (d) find mandated + done.

### 2026-06-11T16:05:25Z — user

user 2026-06-11 (post-v1 direction): in find, pred should NOT carry the iterable's E — pred gets its OWN row (EffPred), and the op's effect = MERGE of EffPred and E ('effects {E, EffPred}'). This activates the deferred decoupled half for the EAGER member: requires fixing the two boundary gaps (arrow-annotation row var vs effects-clause var must be ONE VarId; boundary declared-vs-incurred must decompose row values). Stream.find engine must decouple too (its single Eff currently ties pred row to stream row). Lazy map/filter stay tied for now (their decoupling needs merge-rows in result TYPE position — the two-open-tails row).

### 2026-06-11T16:25:59Z — user

Decoupled rows DELIVERED f3894fb (the post-v1 user direction): Iterable.find[EffP] + Stream.find[Elem, Eff, EffP] — pred row EffP independent of the access row, op effect = merge {E, EffP} (op-level effects are an ATOM LIST, so the eager member needs no merge-row TYPE). PAYOFF VERIFIED: effectful-but-element-pure pred typechecks on List AND its effects thread to the caller's boundary (undeclared -> loud). THE THREE ENGINE FIXES (were the WI-440-recorded gaps): (1) rewrite_type_occ_deep — the rigidify/canon walk now penetrates Value::Node carriers (the 'Nodes carry Refs, not type-param vars' assumption was false for callback arrow row tails; this was the REAL root of the 'VarId split' — one var, un-rigidified on the Node side); (2) boundary declared-vs-incurred is row-aware: row-shaped incurred effects EXPLODE into present labels + tail vars (absences dropped), canon-walked through the op's rigidify subst stored on OpInfo; (3) rigid tail-to-tail ALIASING in unify/subtype both-open arms (forwarding sites pair a flexible callee row var with a rigidified outer tail; flexible now aliases the rigid via open(rigid) continuation instead of the failing symmetric fresh-tail step). KNOWN CONSERVATISM: lacks on the flexible side are NOT propagated onto a rigid continuation (enforced at the rigid's own instantiation site). REMAINING (lazy half): map/filter stay on the tied {E, -Modify[x]} form — decoupling them needs merge-rows in the result TYPE position (Stream[Element, merge(E, EffP)]), plus FilteredStream/MappedStream carrier row threading. Also fold_left/fold_right still Function-form.

### 2026-06-11T16:56:31Z — user

user 2026-06-11 (third direction): map and filter callbacks also get their OWN operation-effects row (EffP), distinct from the Iterable E — the lazy half. Requires: multi-tail rows ({E, EffP} in the result TYPE position — decompose_effect_row's WI-339 F13 two-tail reject must become Vec<tails> with conservative unify arms), two-row MappedStream/FilteredStream carriers (source row ES + callback row EF, provides Stream[T, {ES, EF}], splitFirst effects {ES, EF}), and Iterable.map/filter[..., EffP] -> Stream[.., {E, EffP}].

### 2026-06-11T17:23:17Z — user

Lazy half DELIVERED 271e80b: map/filter callbacks carry their OWN row end-to-end. Iterable.map[Dst, EffP]/filter[EffP] -> Stream[.., {E, EffP}]; two-row MappedStream/FilteredStream (ES + EF, provides Stream[T, {ES, EF}], splitFirst effects {ES, EF}); the callback's effects ride the produced stream, paid on CONSUMPTION (pinned: collect(filter(xs, noisy)) caller must declare Beep, undeclared is loud). ENGINE: multi-tail row UNIONS — decompose Vec<tails> (supersedes WI-339 F13), multi_tail_rows_compat (equal tail sets / bare-flexible wholesale absorb, occurs+lacks guarded), FULL row algorithm in both (effects_rows, effects_rows) unify arms (the WI-320 structural inner-unify was merge-ORDER-sensitive), row-shaped routing in all four dispatch fallbacks (?rho vs open(?rho)), build_canonical folds ALL row vars as opens, kb.row_tail_var_of resolves SORT-level row params' Ref(S.E) lowering to the alias var, provider-view canon vars instantiated through the actual instance's bindings in the cross-sort arm. CONSERVATISMS: multi-tail pairing only via set-equality or wholesale absorb (general two-tail-vs-two-tail pairing rejected — no principal unifier); entity-FIELD arrows carry the plain {EF} row (no field binder places; lacks enforced at the public signatures). REMAINING: fold_left/fold_right still Function-form (same decoupling applies if wanted); -Modify[collection] outer-binder still untested. WI-441 scope now FULLY delivered per the three user directions.

