## Attributes

- id: WI-774
- created: 2026-07-19T10:30:00Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-22T15:04:20Z

- acceptance: cargo-test

## Description

Guarded realization selection: resolve seam vs WI-770's blanket refusal (design follow-up). WI-770's refusal is deliberately blanket, and the review recorded two over-refusal consequences to weigh, both latent (no bodied realization entries exist in-tree). (a) ENUMERATION: supported_effects' candidate gathering (rustland/anthill-cpp-gen/src/lib.rs ~4100; flat EffectMapping query with ground=None, plus the LanguageMapping nested walk) inherits the refusal, but its own doc says candidates are deliberately over-broad and the realizes_effect RESOLUTION filter makes the answer exact -- and the stdlib realizes_effect rules genuinely SLD-resolve EffectMapping subgoals with guards honored (stdlib/anthill/realization/realization.anthill:354-380), so a bodied EffectMapping is correct on the DECIDING path yet refused by the candidate path; worse, the abort fires inside describe_supported_effects (lib.rs ~4136) while RENDERING the WI-576 capability-gate error (call site ~1464), turning an intended diagnostic into a panic. WI-770's message therefore tells EffectMapping authors 'guarded selection is honored only where codegen RESOLVES' -- true, but the refusal itself is what stops them reaching that path via guarded DATA; the sanctioned shape is a fact plus a realizes_effect overlay arm. (b) POST-FILTER BLINDNESS: query_type_mappings grounds only anthill_type and post-filters lang AFTER the query (lib.rs ~3870), so a bodied TypeMapping for ANOTHER language panics every cpp codegen touching that anthill_type even though the lang filter would have discarded it. OPTIONS TO WEIGH: make enumeration resolve (supported_effects already holds &mut kb -- resolve EffectMapping(effect: ?e, ...) so guards gate candidate names; the WI-767 'resolve by default' shape); ground `lang` structurally in the pattern (option_value already builds some(...) entities for goals) so other-language candidates never surface; give TypeMapping/IncludeMapping/NamingConvention a real resolve seam (WI-089's addendum lower_type predicate, which still does not exist -- cpp-gen's sole kb.resolve consumer is realizes_effect at lib.rs:4011); or explicitly re-affirm blanket refusal and pin the enumeration abort as intended. Coordinate with WI-773's accessor if both land. ACCEPTANCE: a bodied EffectMapping that realizes_effect resolves correctly no longer aborts error-message rendering (resolved at enumeration or narrowed out), OR a design note + test pin blanket refusal as the accepted contract; a bodied other-language TypeMapping no longer panics cpp codegen, or same explicit pin; cargo-test green via scripts/test.sh.

## Changes

### 2026-07-19T12:10:31Z — feedback — user

2026-07-19 design discussion: candidate decided-policy for graduating a table functor to resolution: resolve (guards honored) + SEMANTIC solution ordering (most-specific-first via discrim specificity, the simp precedent -- NOT source/positional order, which the WI-760 landmine already rejected) + loud refusal on incomparable-specificity ties + WI-767-style loud depth-cap discipline for the search. The loud channel never disappears for single-valued reads -- shape refusal (WI-770/772) relocates to ambiguity refusal. Enumeration-shaped reads stay walkable; write-side reads need fact identity and stay store-addressed (WI-780). Engineering note: resolve needs &mut KnowledgeBase (WI-760 threading precedent).

