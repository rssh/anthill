## Attributes

- id: WI-971
- created: 2026-08-04T02:26:58Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-04T03:34:02Z

- acceptance: scaland-sbt-test

- tags: scaland

## Description

SCALAND: ~20 declaration productions still hand-write the span capture WI-970 just repaired at eight sites, and the strongest fix would make the class unrepresentable.

WI-970 fixed eight span-end defects and routed six productions through the `located` combinator. What remains: ~26 productions spell `Index ... ~~ Index` by hand, ~20 of which are MECHANICALLY convertible. That is verified, not assumed - four of them (name, importPath, distributiveProjectionSeg, proofStrategy) were converted during WI-970 review with the suite green, and those four are now delivered as part of it.

SIX genuinely cannot, and the reason is structural rather than stylistic - their start does not come from an `Index` at their own entry:
  the four sortDecl branches take theirs handed DOWN as an Int (the WI-947 dispatcher rule documented at namespaceDecl)
  sortTypeParam reads its back off nameSpan.start
  proofStep takes two different ends off one start

THE STRONGEST FIX IS REACHABLE BUT NOT FREE: shadow `Index` inside AnthillParserImpl so a hand-written capture is a COMPILE error - the same trick this file already plays on fastparse.P for WI-950 refusal scoping - with a greppable escape hatch (rawIndex) for the six. That cuts the hand-written surface 26 -> 6 and makes the WI-970 defect class unrepresentable rather than merely fixed at the sites someone happened to grep. COST: a ~20-production mechanical diff, each site gaining one nesting level in its destructuring (case ((vis, n, fields, meta), span)).

REJECTED ALTERNATIVE, recorded so it is not re-invented: the parser owns its whitespace function (given ws) and ParsingRun exposes a misc map, so the skipper could record the pre-trivia index and a tightIndex could hand it back whenever ctx.index equals the post-skip index - making plain and no-whitespace captures agree everywhere including the six. It works, but swaps a visible local syntax rule for hidden parser state, and would silently pull a START index back over leading trivia if one were ever placed after a plain sequence.

WATCH FOR - THE GREP DOES NOT FIND EVERYTHING: WI-970 found that ruleArrowChoice leaked trivia into EVERY `:-` rule span through fastparse flatMap (which runs the whitespace skipper before the continuation, and the continuation there is Pass), not through a trailing Index at all. Three greps missed it; the declaration-end assertion added to DeclarationSpanTest is what caught it. An Index-shadowing lint would not have caught it either. Exactly one flatMap exists in the parser today - a new one is a new instance of the same class.

ACCEPTANCE: the hand-written Index captures are gone except the named six, or the WI records why shadowing was rejected AFTER trying it; SpanEndTest plus the DeclarationSpanTest end assertion stay green; scaland-sbt-test green.

SOURCE: WI-970 /simplify altitude + reuse review.

