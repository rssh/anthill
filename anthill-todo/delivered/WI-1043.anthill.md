```anthill
fact WorkItem(id: "WI-1043", created: "2026-08-07T10:57:40Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-1042"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-08T12:41:54Z"))

fact Feedback(workitem: "WI-1043", author: "user", at: "2026-08-08T12:41:57Z")
```

## description

BUG (058; /code-review 2026-08-07 finding 2, CONFIRMED BY DRIVING) + SPEC: a rule-body call NAMING a body-less spec op is never typed — the program loads CLEAN and answers [] silently, while the spec claimed REFUSED. DRIVEN: a body-less spec op with TWO suppliers and only rule answer(?r) :- Desc.describe(leaf(), ?r) loads clean and answers []; the refusal the spec cell reported came from the wi1027 fixture probe OPERATION (the load refusal locates at the probe operation line, 19:32) — the exact fixture-contamination class 058-implementation sec 20 itself diagnosed for its own earlier numbers. MECHANISM: expr_needs_call_dispatch requires op_has_runnable_body (typing.rs:41506), so a body-less named call in a rule body never routes to check_apply_iter, and no other refusal path exists; the typing.rs:41398 residue note cites WI-1027, which is DELIVERED — nothing owned the hole until this ticket. THE SPEC CELLS ARE CORRECTED in the same commit that fires this ticket, each citing this number: 058-implementation sec 21 row body-less/instance-fact/rule-body; kernel-language sec Instance coherence and sec Operation override; the wi1035_dot_member_supplier_tie_test.rs header table. WORK: give the body-less rule-body NAMED call the load face WI-1027 gave the operation body — widen the WI-1026 arm (expr_needs_call_dispatch + check_apply_iter) to the body-less half, so ONE supplier pins and dispatches at the call and TWO suppliers refuse at load naming both; or record at the arm, with this number, why it may not. ALSO record the GENERAL-Apply narrowing at the dispatch_calls_in_occ site (rule-body calls that are neither dot, defaulted, nor body-less spec ops stay untyped) as this ticket residue or its own spun-off number. DEPENDS on WI-1042: the arm this widens is the one WI-1042 deduplicates into the shared gate helper — land the helper first so the body-less clause is written once, not a fourth spelling. ACCEPTANCE (each test says which case fails when backed out): the driven two-supplier fixture above REFUSES at load naming both suppliers by supply route (control: backed out, it loads clean answering []); the ONE-supplier body-less fixture answers the supplied impl value as a rule body (control: today it answers [], since the goal is never typed and the abstract-op early return at resolve.rs:6136 returns before the bridge); wi1026/wi1035 suites stay green; the corrected spec cells flip from []-silent back to REFUSED in the closing commit. REFERENCE: /code-review 2026-08-07 finding 2; WI-1027 (Delivered — the operation-body half and the declining-arms record); WI-1035; WI-1034 (the dangling-functor residue, a DIFFERENT population); WI-496.

## Feedback

### 2026-08-08T12:41:57Z — user

DELIVERED, with the ticket's WORK done in THREE pieces (it named one) and one of its own numbers corrected. (1) THE LOAD FACE, as asked: the walk gate is spec_op_call_parent — the union of the two halves, one clause in defaulted_spec_op_parent's neighbourhood — so a body-less rule-body call reaches check_apply_iter's WI-210 block and WI-1027's tie guard. TWO suppliers REFUSE at load naming both by route, ONE supplier pins. (2) THE GOAL SHAPE, which the ticket's acceptance predicted only obliquely: the pin alone does not answer. functional_relation_arity asks 'has a runnable body?' of the SPELLED functor, so the goal f(a…, ?r) never became the call reduce_op_value folds — the pin was written, carried and never consulted. dispatched_relation_arity reads it; the rule-less clause stays on the spelled functor (Ord.lt's clauses must still win). (3) A PRE-EXISTING REFUSAL OF A LEGAL PROGRAM had to be fixed FIRST: the rule-body requirement check asks sort_provides, which walks the CARRIER's own out-edges, so a WITNESS-supplied carrier (sort Rival provides Desc[T = Leaf], WI-450) read as having no instance and was REFUSED with 'missing requires' — measured on main through the DOT spelling, whose atoms have been typed since WI-282, while the operation-body twin answers 9. carrier_provided_by_witness declines that question; without it the widening would have extended the refusal to the qualified spelling. THE SPEC CELLS ARE FLIPPED BACK TO REFUSED (058 sec 21 row + a new sec 23; kernel-language 'Instance coherence' and 'Operation override'; the wi1035 header table). THE NARROWING THAT LET IT SHIP, and it is the ticket's real cost: an = goal loads as a call on the body-less PartialEq.eq, so this shape is most of what a rule body is made of — 133 newly decided call sites per whole-corpus load, whose failures are 19 leaf errors (15 DotDispatchNoMatch, 2 TypeMismatch, 2 UnknownApplyFunctor) and ZERO dispatch verdicts, rendering as 7 load errors in 4 files, every one a program that loads and runs today. The frame reports only the two TIES, and only for the call it is deciding; the backlog is WI-1056 (which also takes the general-Expr::Apply narrowing the ticket asked to be recorded). TWO DEFECTS OF MY OWN, both found by measurement and not by reasoning: the first cut handed the whole atom to type_check_node and SWALLOWED two delivered rule-body diagnostics (WI-282's member-not-found, WI-602's definite precondition violation) — found as two full-suite failures, fixed by recursing INTO a body-less atom as well as deciding it; and the tie of a call NESTED in an = goal was then reported TWICE (a regression against pre-WI-1043 behaviour for the dot spelling) — found by /code-review, fixed by the (op, span) identity in own_call_tie, driven by a_tie_nested_in_an_eq_goal_is_reported_once. RESIDUE: WI-1057 — an implementation supplied ONLY by an instance fact stays unreachable from a rule body (both spellings answer [], the operation body answers 9), pinned by a test that FAILS when it is closed; the naive fix was driven and is WORSE (it bound ?r to the un-reduced call and reported it DEFINITE). Back-out matrix driven per piece, four reverts. Full workspace green: 29 binaries, 4385 passed, 0 failed.

