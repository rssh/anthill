## Attributes

- id: WI-20260822-1MAGR-a-provision-s-member-signature
- created: 2026-08-22T22:11:19Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-23T03:33:14Z

- acceptance: cargo-test, scaland-sbt-test

## Description

A PROVISION'S MEMBER SIGNATURE IS STILL UNCOMPARED — arity, parameter order, and the return type wherever no contract clause reads `result`.

STATE, and it is a deliberate one. `op_backed` matches a declared member by SHORT NAME ONLY. kernel-language.md's "Backing conformance is NOT checked beyond the name (WI-935, measured)" states it: `fact VectorSpace[BadVec, Float]` loads clean when `vec_add` takes one argument, when `vec_sub` returns `Float`, or when `vec_scale`'s parameters are swapped. Each then mis-dispatches or dies at the call.

WHAT 59CDQ CHANGED, AND WHAT IT DELIBERATELY DID NOT. `check_override_refinement` now compares the RETURN TYPE in exactly one place: where a `requires`/`ensures` clause MENTIONS `result`. That is not a general conformance check — it is the soundness condition of the result-binder alignment, which is what makes such a clause compare equal at all, and discharging `ensures P(result)` across two different return types promises P of a value of the wrong type. Everything else is untouched, and the two scope-control tests in wi347_override_refinement_test pin the boundary: a return-type mismatch no compared clause reads must still LOAD.

THE MACHINERY ALREADY EXISTS, AND ITS DOC SAYS WHY IT WAS NOT REUSED. `check_instance_fact_op_signatures` (rustland/anthill-core/src/kb/typing.rs) already does same param ARITY, contravariant PARAM types, covariant RETURN type, ground-gated with σ applied — for INSTANCE FACTS. Its own doc comment states the reason it is a separate pass: "A dedicated pass (not folded into check_override_refinement) so the carrier-own override path is untouched; instance facts have no stdlib presence yet, so this can be strict without regressing existing providers." So the obstacle is not the check; it is the unmeasured blast radius on the `provides` population. REGRESS MEANS REFUSE AT LOAD, not run slower: this pass emits `LoadError`s, so a provision that loads clean today and does not conform would stop the file loading at all. Instance facts could be checked strictly because nothing in the tree writes one — there was no population to break. The carrier-own path has the stdlib's provisions and every example behind it, and how many of those declare a member whose arity, parameter order or return type does not match the spec's is simply not known.

THE WORK IS THE MEASUREMENT FIRST. Run the instance-fact pass's per-op comparison over the CARRIER-OWN override path in report-only mode and count the population across stdlib, examples/, docs/measurements/ and the fixture corpus, BROKEN DOWN per failure kind (arity / parameter order / return type) and per carrier. A single count is not the answer: WI-935's own history is that one apparently-safe tightening moved an unrelated imported short name from 1 solution to 0 with no diagnostic. Only after that census is it decidable whether to enforce all three legs, enforce arity alone, or enforce with a documented exemption list.

DO NOT ASSUME THE THREE LEGS SHARE A VERDICT. Arity is always decidable and is the cheapest to enforce; parameter ORDER is decidable only when the types differ (two `Int64` params in either order are indistinguishable, so the check is a partial one and must say so); the RETURN type needs σ and fails open where the provision binds nothing to the spec's parameter — which 59CDQ measured as a live case, not a hypothetical.

ACCEPTANCE: the three WI-935 repros (`vec_add` at one argument, `vec_sub` returning `Float`, `vec_scale` with swapped parameters) are each refused naming the spec's declared shape and the member's, or each is documented as still admitted with the reason at the enforcement site; the census above is recorded in the ticket with its per-kind numbers; every stdlib/example provision either still loads or is named individually with its own reason; kernel-language.md's "Backing conformance is NOT checked beyond the name" paragraph is rewritten to what then actually holds, and the 59CDQ sentence appended to it is folded in rather than left beside a contradicting one; cargo-test green via rustland/scripts/test.sh.

SOURCE: WI-20260822-59CDQ, which asked whether the two are one ticket or two and answers TWO — its own change is the contract-discharge slice and stops there. WI-935 is Delivered and its statement is a measurement, not an open obligation, so nothing owned this until now.

## Changes

### 2026-08-23T03:33:08Z — feedback — claude

DELIVERED. The comparison ships, with two structural exemptions and one gate, and the census that decided all three is below.

THE CENSUS FIRST, because the ticket said it had to be. Report-only run of the instance-fact pass's three legs over `check_override_refinement`'s own population — the provision's declaring sort's own member of the spec operation's short name — across stdlib + `rustland/anthill-stl` + every `examples/` project + `docs/` + `testdata/` + `anthill-testcases/` + `anthill-todo/` + the whole fixture suite (a temporary corpus-loading test carried the files no suite loads; deleted before delivery).

  population              1152 distinct (carrier, spec, operation) pairs, 999 carriers
  raw findings                67   arity 3 · parameter 44 · return 20
  EXEMPT: self-receiver       42   all 42 of them parameter findings, 32 carriers
  EXEMPT: `s.T` projection    14   all 14 of them return findings
  residue                     11

EVERY ONE OF THE 67 RAW FINDINGS WAS A LEGITIMATE PROGRAM. That is the answer to "count the population per failure kind": there was no pre-existing non-conformance to grandfather anywhere in the tree. Both exemptions are about DECIDABILITY, not about excusing a population:

  * SELF-RECEIVER — a spec that types a parameter as ITSELF (`splitFirst(s: Stream)`) is naming the dispatch receiver, and an override narrows it to the carrier (`splitFirst(xs: List)`). Contravariance refuses every one of them; dispatch is what makes the narrowing sound. This is not a corner: `Stream`/`FiniteStream`/`List`/`LogicalStream`/`MappedStream`/`Relation` plus 26 fixture carriers, i.e. the ordinary way a stdlib member is written. Recognised structurally, never by position — `test.wi596.Bag.holds(x: T, b: Bag)` puts it SECOND, which is what a position-keyed exemption would have got wrong.
  * `s.T` PROJECTION (WI-376) — σ binds the spec's TYPE PARAMETERS and cannot ground a projection off the operation's own parameter. The two operations' receivers are different parameters, so `Stream.splitFirst`'s `s.T` and `List.splitFirst`'s `xs.T` are two distinct neutrals that `types_compatible` refuses by `expr_carried_zeta`'s own rule. `contains_type_param` does NOT catch these — they are not vars and not sort-param symbols — so the inherited ground gate let all 14 through and the projection gate is a second, separate one.

THE RESIDUE OF 11 SPLITS EXACTLY ON ONE PREDICATE, and that is what decided the shape. The predicate is whether the SPEC OPERATION has an executable implementation of its own (`op_is_executable`: default body │ resolver builtin │ host mapping):

  * 3 with one — and all three are programs the language has ALREADY DECIDED must load:
      `wi1125.nullary.Color` / `PartialEq.neq`, arity 2 vs 0. WI-1125: "a 0-ary member is not `PartialEq.neq` under any reading, so it supplies nothing and the program is fine."
      `wi1125.witnessunrelated.ColorEq` / `PartialEq.neq`, `(T,T)`→`(Color,Color)` vs `(Int64,Int64)`. WI-1125 finding 2, whose whole point is that the message must not tell the author to delete a member with nothing to do with equality.
      `wi1042.nonparametric.Leaf` / `Plain.describe`, `Int64` vs `Leaf`. WI-1042: `Plain` declares no `sort X = ?` so it is not a spec, there is no spec-op dispatch to decide, and its test asserts the ANSWER is 7.
  * 8 without one — and all eight are DELIBERATE MISMATCH FIXTURES. Six in `wi347_override_refinement_test` (three already refused by 59CDQ's result-binder guard, three its scope controls). Two are `docs/measurements/guardians/p3_spec_wrong_sig.anthill` and `p7_sig_and_row.anthill` — the probes that RECORDED this gap, written up as C1 of `examples/guardians/docs/design/measured.md`: "Signature conformance is not checked · the gap".

SO THE RULE IS: the member must fit exactly where it is the SOLE BACKING. Where the spec supplies nothing, the member is the only thing that can back the operation — there is nothing for it to be distinct FROM — so a mismatch makes the provision's claim false and no other pass says so. Where the spec supplies its own implementation, a same-named member of a different signature is a DISTINCT OPERATION and the default is what backs the provision; that is the reading §8.7 already gives the `requires` direction (WI-1048), under which a call expecting the spec's shape is already a loud type error naming both types.

THE ALTERNATIVE I REJECTED, and why, because the ticket asked for the verdict per leg. "Enforce all three legs unconditionally" collides head-on with WI-1125's two rows and WI-1042's one. Every narrower escape I could construct for them keyed on the operation NAME (`neq` is derived, §8.3) or on a hand-written scope rule ("the provided sort must be a spec"), i.e. on a proxy. The sole-backing predicate is the EFFECT itself, it keys on nothing by name, and the corpus separates on it with no exceptions — 3 on one side, 8 on the other, no crossings. "Arity alone" would have caught `vec_add` and neither of the other two repros, and would still have refused `wi1125.nullary`.

DO NOT ASSUME THE THREE LEGS SHARE A VERDICT — they do, under the gate, and here is each one's own answer:
  ARITY always decidable, enforced, short-circuits (a per-position comparison across two arities pairs unrelated parameters).
  PARAMETER ORDER decidable only where the TYPES differ, and the message says so in as many words: two `Int64` parameters in either order are the same signature. Where the types do differ, a permutation of the MISMATCHED positions that would fit is reported as an order mistake rather than as N unrelated type errors, because that is the repair. Searching the mismatched positions only, not all n — the exempted ones never fit positionally, so a whole-list search would answer "not a permutation" for any signature with a self-receiver and report a genuine swap as two unrelated mismatches.
  RETURN TYPE covariant, σ-substituted, ground-gated AND projection-gated. This is the leg 59CDQ could not reach: no clause in the `vec_sub` repro mentions `result`.

MEASURED, THE ENFORCEMENT DIRECTION: full workspace with the check on and nothing else changed — 5562 passed, 3 failed, and the 3 are exactly the wi347 scope controls the ticket predicted would move. Nothing in stdlib, nothing in any example, nothing in 3350 anthill-core fixtures.

MEASURED, THE BACK-OUT DIRECTION: by MUTATION, not deletion — `check_member_signature` returns immediately, so the pass still runs and only the verdict changes — over all 3353 `anthill-core` tests. EXACTLY SIX fail: the four arms in `wi1magr_member_signature_test`, and two of the three re-pinned wi347 rows. The SEVENTH I predicted is not there, and saying so is half the measurement: `a_weakening_is_reported_as_one_even_when_the_return_types_also_differ` passes EITHER WAY, because after the re-pin both its assertions are about the DISCHARGE rule and neither depends on this one — it moved its spelling, not its subject. That the back-out reaches nothing else is the other half: no stdlib carrier, no example and no other fixture declares a member that does not fit.

THE THREE SCOPE CONTROLS ARE RE-PINNED, NOT DELETED. Each asserted a clean load because nothing else looked; each now asserts WHICH rule refuses it — `discharge_refusals` empty, `signature_refusals` one — so 59CDQ's claim (the discharge guard must not widen into the general signature question) is still the thing measured. `a_weakening_is_reported_as_one_even_when_the_return_types_also_differ` needed its second assertion re-spelled: it matched on ``returns `Bool` ``, which BOTH messages now contain, so it would have passed for the wrong reason.

DOCS: kernel-language.md §8.7's "Backing conformance is NOT checked beyond the name" is rewritten to what now holds, with 59CDQ's sentence folded IN rather than left beside it — the two return-type rules are stated as two questions that neither subsume nor duplicate each other (59CDQ's applies where a clause is discharged, including beside a spec default, which this gate excludes; this one applies with no clause in sight). The *Operation override* paragraph's two sentences that pointed at the gap are corrected. `examples/guardians/docs/design/measured.md` C1 flips from "the gap" to closed, with the probes RE-RUN (p3 refused, p7 refused twice, and `p1_spec_good` still clean as the control) and the original 2026-08-22 verdict kept beside it, because a measurement record that quietly drops what it measured is not one; `high-level-api.md`'s verdict table and `two-flows.md`'s "the one gap" section follow. The stale doc sentences at `check_instance_fact_op_signatures` ("instance facts have no stdlib presence yet, so this can be strict without measuring") and at `op_backed` are replaced with what actually divides the two passes.

WHAT IS STILL NOT COMPARED, stated so nobody re-derives it: a member beside a spec operation that HAS a default (a distinct operation by the rule above); a swap of two parameters of the same type; a non-ground or projection-carrying pair; and a member `op_backed` reaches only by qualified name rather than through the sort's own-op list.


DRIVEN THROUGH THE CLI TOO, because the guardians doc now claims this is caught "at check time" and a generate-and-check loop reads an exit code, not prose: `anthill check docs/measurements/guardians/p3_spec_wrong_sig.anthill` prints the refusal and exits 1; `anthill load` on the same file exits 1; `anthill load` on `p1_spec_good.anthill` (B2's control) prints `loaded: 2676 facts, 181 rules` and exits 0. (First measured wrong — `$?` after a pipe to `tail` is tail's status, not the CLI's.)

THE GATE'S PREDICATE IS `op_backed`'S OWN, NOT `op_is_executable`, and getting that wrong was my first cut. `op_is_executable` counts a host `operation_map` entry, and `is_host_mapped_op` is a FLAT SET with no carrier dimension — so a mapping naming the SPEC's member says an implementation exists somewhere and never that THIS carrier is realized. That is WI-876 defect A, and it is exactly why `op_backed` drops the leg for its spec-op candidate. The wide predicate would have gated the comparison off in the one case where the member really is the only backing: `op_backed` refuses to count the host mapping for the carrier, and this pass would have excused the member on the strength of it. Narrowed to `is_builtin │ op_has_runnable_body`. Narrowing can only WIDEN what is compared, so it was re-measured over the full workspace: nothing moved (no corpus provision has a member beside a host-mapped-only spec operation), and `a_host_mapping_on_the_spec_member_does_not_excuse_the_carriers` now drives the case with `anthill.persistence.Store.persist`. The census numbers above were taken with the wide gate; the split they report is unaffected, since all three `sole=false` findings sit beside a BUILTIN or a body.

/code-review FOUND TWO, both mine, both fixed with a driven row each and each row's back-out MEASURED:
  1. THE MESSAGE AND THE SPEC STILL SAID "no host mapping" after I narrowed the gate. Driven by the reviewer: a carrier of `Store` with a mis-typed `persist` was refused with "… has no default body, builtin or host implementation", which is FALSE — `persistence.anthill` maps `persist: "store_persist"`. The refusal is right; its reason was not. Message and all four doc sites now say "no default body and no builtin … a host `operation_map` on the spec's own member does not count: it names no carrier".
  2. THE CONTRACT LEGS RAN ON A MIS-ZIPPED PARAMETER LIST after an arity refusal, and my call-site comment claimed the ordering prevented exactly that. Every leg below the signature check compares in the spec's vocabulary through a positional `zip`; across two arities it pairs unrelated parameters, so a precondition the member restates VERBATIM stops matching. Driven: spec `describe(a: T, b: Int64) requires posi(b)` against member `describe(b: Int64) requires posi(b)` reported the signature AND "it strengthens the precondition", which the member does not. An ARITY mismatch now ends the pair; an equal-arity refusal does NOT, because the alignment is sound whatever the types are and 59CDQ's "report beside, not instead" still applies there.

TESTS: 10 new rows in `wi1magr_member_signature_test` (4 arms, 4 controls, 3 of which DRIVE a value through the interpreter rather than assert a clean load), 3 re-pinned in `wi347_override_refinement_test` (2 renamed off `…_is_not_this_passs_business` — the PASS does refuse them now, by its other rule). Full workspace via `rustland/scripts/test.sh`: 5566 passed, 0 failed, exit 0 (30 binaries). scaland `sbt test`: 514 passed, 0 failed (it has no provider checks, so this change cannot reach it; run because acceptance names it).

