```anthill
fact WorkItem(id: "WI-737", created: "2026-07-17T06:16:25Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-714"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-17T07:36:52Z"))
```

## description

WI-714 BUG: the Relation drain presents a FLOUNDERED (residual) solution as a definite row. RAISE instead (user decision 2026-07-17). SYMPTOM, probed empirically: a rule whose body cannot be decided yields, through the Relation face, ONE ROW WHOSE COLUMNS ARE UNBOUND LOGIC VARIABLES. Concretely, rule distinct_pair(?x,?y) :- num(v:?x), num(v:?y), neq(?x,?y) is typed Relation[(x: Int64, y: Int64)] and materializes a row (x: Var(Global(1231)), y: Var(Global(1232))) — the schema promises Int64 and the value IS a logic variable, a type-level lie in the very face (proposal 052) whose purpose is to replace raw Substitution walking with TYPED rows. Second symptom, same root: an applied membership relation over an undecidable body reports NON-EMPTY, so a floundered residual reads as a positive membership answer. ROOT CAUSE: Interpreter::materialize_solution (eval/mod.rs:1183) never inspects sol.is_definite() — it reads each column through resolve_as_value and returns whatever it finds, bound or not. THE REST OF THE CODEBASE GETS THIS RIGHT: (a) make_solution_value (eval/mod.rs:1126-1140) reifies the reflect Solution as definite(subst) vs undecided(subst, residual) precisely so consumers can inspect which goals stayed pending; (b) relation_negate (eval/builtins.rs:1086-1093) rejects a free-column operand LOUDLY, reasoning 'would flounder under NAF ... reading a floundered residual as a spurious solution ... rather than return a silently-wrong result' — the SAME hazard, guarded only for negate; (c) anthill-todo's hand-written walk (collect_id_set, main.anthill:2691-2709) explicitly skips 'case undecided(_, _)'. So the typed face built to RETIRE that boilerplate silently lost the residual honesty (WI-519) the boilerplate had. Missed by the existing wi714 tests because they all query ground facts, which only ever yield definite solutions. FIX (user decision): RAISE. A Relation[T] promises rows of T and T has no room for a third outcome, so a floundered solution must surface on the E channel — E is ALREADY >= {Error} on every relation, so the raise is already in the type and no signature changes. Follows relation_negate's precedent and the 'loud error over silent skip' principle. REJECTED: skipping residuals via ResolveConfig.definite_only — trades one silent lie for another, since isEmpty would then report empty for a goal the search never decided (undecidedness masquerading as refutation). NOTE the split this settles: the RAW reflect/LogicalStream face keeps undecidedness as DATA (mod.rs:1132 says it is never raised on execute's E channel, for WI-010's self-hosted resolver); the TYPED Relation face raises. Document the contrast where both live. SCOPE: gate materialize_solution on sol.is_definite(); raise an Error naming the undischarged residual goals (already carried; make_solution_value already renders them as List[Term]); decide whether the WI-628 truncated flag deserves a distinct message (search truncated at max_depth vs goal floundered). ACCEPTANCE: a relation over an undecidable body RAISES on drain instead of yielding a var-bearing row or a spurious membership; the message names the residual goals; definite relations unaffected (all wi714/wi727 suites green); a regression test pins BOTH symptoms; cargo-test green via scripts/test.sh.

