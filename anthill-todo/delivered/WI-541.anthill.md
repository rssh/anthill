```anthill
fact WorkItem(id: "WI-541", created: "2026-06-21T14:49:35Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-540", "WI-542", "WI-543", "WI-544", "WI-545", "WI-546"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-24T04:29:03Z"))

fact Feedback(workitem: "WI-541", author: "user", at: "2026-06-24T04:28:59Z")
```

## description

WI-540 Foundation follow-ups: complete the generated-and-used reflect KB bridge. Each gap is loud-documented in anthill-stl/src/reflect/bridge.rs. (1) Substitution.compose is PARTIAL through a trait object — a &dyn Substitution cannot enumerate s2's bindings, so it applies s2 to self's range but cannot merge s2's standalone bindings; needs a binding-enumeration op on the spec's Substitution trait, then a full union impl. (2) OperationInfo.requires/ensures are produced EMPTY: the host NodeOccurrence is currently an empty opaque unit struct (uncarrier-bound), so carrier-bind it to a ReflectNodeOccurrence(Rc<NodeOccurrence>) opaque newtype (same pattern as ReflectTerm) and have the bridge wrap the stored clause Values. (3) Implement the loud-panic stubs kb()/facts_of/assert/add_guard. (4) Add a first-class BigIntLiteral to reflect.anthill LiteralRepr (currently BigInt lowers to its decimal StringLiteral). (5) Optional: generate the Stream trait + consolidate the interpreter (builtins.rs) and host (KbBridge) reflect realizations. Non-blocking (bridge compiles, suite green). See [[project_wi536_generated_bridge]].

## Feedback

### 2026-06-24T04:28:59Z — user

DELIVERED (substantially-complete, tracking-only flip — no new code in this WI). All NON-optional gaps done via the split-out per-gap WIs: gap 1 (compose full union) = WI-544 + WI-547 (chase bare Value::Var); gap 2 (OperationInfo.requires/ensures carrier) = WI-545 (ReflectNodeOccurrence) + WI-548 (interpreter kb_operations parity); gap 3 (loud-panic stubs kb()/facts_of/assert/add_guard) = WI-542 (facts_of) + WI-546 (assert+kb()) + WI-549 (add_guard via LogicalQuery->guard-Value reifier); gap 4 (BigIntLiteral) = WI-543. Acceptance (cargo-test green) holds — full workspace suite exit 0. Gap 5 (OPTIONAL, non-blocking: generate Stream trait + consolidate the two parallel reflect realizations builtins.rs<->KbBridge) was never in WI-541's depends_on and is now filed as standalone WI-551. NOTE: WI-541 depends_on listed only 540/542-546; the actual delivery also required 547/548/549 (split out later from 544/545/546).

