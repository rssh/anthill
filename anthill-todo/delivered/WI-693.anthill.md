```anthill
fact WorkItem(id: "WI-693", created: "2026-07-12T08:52:11Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-12T08:52:11Z"))

fact Tag(workitem: "WI-693", name: "carrier-neutral")

fact Feedback(workitem: "WI-693", author: "user", at: "2026-07-12T08:52:11Z")
```

## description

resolve_cut_barrier carrier-neutral (WI-348 direction; sibling of WI-692). resolve_cut_barrier (resolve.rs) accepted a reified goal TermId and read pos_args off a Term::Fn, so the Cut step handler (step_init) had to lower a Value::Node cut goal via reify_goal_value just to read the baked barrier. Make it read the goal through TermView: head() gates the shape (Functor{pos_arity:0,named_arity:0} = unbaked nullary top-level cut -> None; Functor{pos_arity:1,named_arity:0} = baked cut(IntLit(B))), and pos_arg(0).head() reads the Int barrier directly (the barrier is a concrete IntLit baked at rule-open, never a var, so no walk -- faithful to the old direct terms.get). Drop the reify_goal_value fallback in the Cut branch, pass &goal_val. reify_goal_value + goal_t stay live (other builtin branch @ ~1254). A reported-but-unresolvable arg-0 after the arity-1 gate is a loud debug_assert (carrier/view desync), honoring loud-over-silent. cut is a builtin (not a constructor) so the 0-ary Functor head never canonicalizes to Ref. Acceptance: full cargo-test green (esp. cut_test: nullary + baked, cut x or, cut under NAF, delay/residualization).

## Feedback

### 2026-07-12T08:52:11Z — user

DELIVERED 2026-07-12 (a66fcee3). resolve_cut_barrier: goal: TermId -> &Value, body reads goal.head(kb): Functor{pos_arity:0,named_arity:0} => None (unbaked nullary top-level cut); Functor{pos_arity:1,named_arity:0} => match goal.pos_arg(kb,0) { Some(arg) => arg.head(kb) ViewHead::Const(Literal::Int(n)) => Some(n) else loud debug_assert; None(arg-0 unresolvable after arity-1 gate) => loud debug_assert (carrier/view desync) }; _ => loud debug_assert unexpected shape. Cut branch (step_init): dropped 'let goal = goal_t.unwrap_or_else(|| reify_goal_value(kb,&goal_val))', passes &goal_val. No walk on the arg -- the baked barrier is a concrete IntLit (bake_cut_term allocs Term::Const(Int)), never a var, so head-read is faithful to the old terms.get(pos_args[0]). cut is a builtin (not a constructor) so head() never canonicalizes the nullary form to Ref -- the 0-ary Functor arm is exact. reify_goal_value + goal_t stay live (used @ ~1254, another builtin branch), no dead code. Full workspace green: 73 binaries, 2735 passed, 0 failed; cut_test 7/7 (commits-to-first-clause, prunes-inner-choice, outer-cut-prunes-inner-or, inner-cut-opaque-to-outer-or, cut-under-NAF-scoped, ground-guard-definite, delay+residualization) + push_choice 14/14.

