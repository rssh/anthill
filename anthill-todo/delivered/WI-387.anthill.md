## Attributes

- id: WI-387
- created: 2026-06-04T17:29:34Z

- status: Delivered
- status_agent: user
- status_at: 2026-06-04T18:45:42Z

- acceptance: cargo-test

- depends_on: WI-386

- tags: typing

## Description

IMPLEMENT written effect rows on cross-sort carriers per docs/design/effect-rows-on-cross-sort-carriers.md (the WI-386 design). THREE parts: (1) re-apply FIX 2 -- parameterized_compatible_view cross-sort provider-view (typing.rs ~9449 None-arm: consult provider_spec_view_bindings for an expected param the cross-sort actual lacks; isolated-innocent, wi357/wi210 pass with it). (2) FIX 3 (the blocker) -- the WI-325/356 abstract/requires-coverage check must treat a spec param the carrier's SortProvidesInfo binds to a GROUND value (E={}) as COVERED, not as an uncovered abstract param demanding 'requires Stream[E=...]'; without it, writing E={} on List's Stream provision regresses delivered wi357 (element threading) + wi210 (dispatch). (3) stdlib: switch List to 'provides Stream[T=T, E={}]' (the fact-head {} loader gap is routed around -- a provides clause lowers the row via sort_inst_to_value; a fact head's term path makes Ref('{}')->unresolved) + write 'iterator[Elem](l: List[T=Elem]) -> Stream[T=Elem, E={}]'; wire + un-#[ignore] wi368_iterator_threading_test (on disk, dormant). ACCEPTANCE: WI-368 -- 'length(collect(List.iterator(xs)))' pure + element List[Int] (wi368_iterator_threading_test both cases); wi357 + wi210 STAY green; full anthill-core green. PROVEN achievable in isolation (design doc sec.Acceptance). Then UNBLOCKS WI-380 (remaining producer rewrites). depends WI-386. blocks WI-380. Rust (typer+loader+stdlib).

