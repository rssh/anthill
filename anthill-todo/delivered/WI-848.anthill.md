## Attributes

- id: WI-848
- created: 2026-07-28T11:07:58Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-28T19:05:59Z

- acceptance: cargo-test

## Description

cpp-gen row_named_term: the `Value::Var` ("field is unbound") arm has an UNMEASURED reachability claim in its own doc comment, and FIVE natural spellings of that claim do not reach it — so either the arm is dead, or its real trigger is something nobody has identified. Decide which, and pin it.

WHAT THE CODE CLAIMS (rustland/anthill-cpp-gen/src/lib.rs, row_named_term, added by WI-833 0f979eac). The doc names two triggers verbatim: "the resolved fact/rule OMITTED a required field (the loader var-fills a missing required field; an omitted `Option` field is `none`-filled instead, so it stays a `Term`) or a rule head left it unconstrained". On that reading it returns a loud CppCodegenError, which is the RIGHT direction (CLAUDE.md "loud error over silent skip") — this ticket does NOT propose making it silent again.

WHAT I MEASURED (HEAD 2cdfde3e, driven through emit_namespace_header via the wi833 `emit_inc_header` harness, IncludeMapping over `int64_t`, 2026-07-28). All FIVE emit CLEANLY — no error, header produced, the malformed mapping simply contributing no probe:
  (a) `rule IncludeMapping(lang: "cpp", host_type: "int64_t") :- Toggle(on: true)` — omitted required `include`;
  (b) the same with `include: ?anything` in the head;
  (c) `include: ?i` where `?i` is bound by a body goal that resolves to nothing (`Src(v: ?i)` with no Src facts);
  (d) `include: ?i` where `?i` appears NOWHERE in the body;
  (e) `include: ?` (anonymous head var);
  plus (f) `fact IncludeMapping(lang: "cpp", host_type: "int64_t")` — a FACT omitting the required field.
So the doc's first trigger ("a realization fact is missing this required field") and its second ("a rule head left it unconstrained") are BOTH refuted as written, for this functor. This also refutes the /code-review finding that motivated the ticket, which predicted (a) would "kill every namespace-header emit in the project" — it does not.

WHY THIS IS STILL WORTH A TICKET, i.e. what is actually wrong. (1) An arm whose stated trigger cannot be produced is either DEAD CODE carrying a false explanation, or LIVE with an unknown trigger — and both readings are bad in the same way: the next person to touch resolve_realization_rows will trust the comment. (2) If it IS live, the diagnostic is unusable: the message is a bare format! carrying only the field name — no rule identity, no span — so an author told "field `include` is unbound" has no way to find which of their rules is at fault. Contrast the ExtentReadError::BodiedRule refusal this reader replaced, which rendered `head :- body`. (3) The blast radius is the whole read: row_named_term's Err propagates out of Includes::from_kb / resolve_realization_rows and aborts the entire emit, so ONE malformed row takes down every header in the project — acceptable for a loud error only if the message can be acted on, which is (2).

WORK: (i) Determine the arm's real reachability — inspect what read_facts_resolved actually hands back for an under-determined row (does the enumeration goal's var-fill survive to the reified Value, or does the solution flounder / get dropped first?), across BOTH a fact and a bodied rule, and for a field the functor declares REQUIRED vs one declared `Option`. Try functors other than IncludeMapping (TypeMapping, NamingConvention) before concluding — the measurement above is IncludeMapping-only. (ii) If reachable: a test that reaches it, and a diagnostic that names the offending rule (or at minimum its span / rendered head), so the error is actionable. (iii) If NOT reachable: say so — delete the arm, or keep it as an explicit unreachable/defensive case whose comment states it is defensive and NOT a described user-facing behaviour. Do not leave a comment asserting a trigger that has been measured absent. (iv) Whatever the verdict, record it in the doc comment, since the current text is the artifact that misled the review.

ACCEPTANCE: the arm's status is DECIDED and stated in code — either a driven test that reaches it plus a diagnostic naming the offending rule, or its removal/redesignation as defensive with the reachability finding written down; the six probe spellings above keep emitting cleanly (they are correct programs — a mapping missing a required field contributes no mapping); full workspace green via rustland/scripts/test.sh.

SOURCE: /code-review of the WI-833 merge, 2026-07-28 — finding recorded as "aborts the ENTIRE realization read for one malformed row"; its repro was driven and REFUTED, and what survives is the reachability/diagnostic question above. SCOPE: rustland/anthill-cpp-gen (and kb/extent.rs read_facts_resolved for the reachability determination only).

