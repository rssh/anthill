```anthill
fact WorkItem(id: "WI-868", created: "2026-07-30T14:47:57Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-865"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-19T20:57:11Z"))

fact Feedback(workitem: "WI-868", author: "claude", at: "2026-08-19T20:57:06Z")
```

## description

WI-857 FOLLOW-UP (design question, depends on the runtime marker payload) — TWO coexisting representations of 'this slot has no evidence', and the refusal ordering is what keeps them apart. WI-857 left two: (a) ResolvedRequiresNode::Unavailable -> an empty bundle over the anthill.reflect.NoProvider marker, which resolve_op_target_checked REFUSES to dispatch through; (b) stand_in_requirement (eval/mod.rs), the host-entry stand-in, whose sub-slots are markers but whose OWN functor is a claim that e.g. Main provides WorkItemStore — a claim call_with_requirements' own doc already calls 'wrong impl, runtime mis-dispatch'. They cannot be unified TODAY because the marker refusal sits at resolve_op_target_checked, i.e. BEFORE the WI-350/WI-822 value-directed rescue that is exactly what makes the stand-in work: a stand-in dispatches onto the spec's own op, which for a bodyless spec op resolves the impl from the RUNTIME VALUE and gets the right answer (pinned by wi818_executable_backing_test::variant_b_carrier_impl_loads_and_evaluates, which MEASURED this — it failed when the stand-in was not layout-valid). If the refusal moved to AFTER the rescue fails, ONE representation would serve both: a marker functor would mean 'this dictionary pins nothing; let the value decide, and be loud if it cannot'. THE OPEN QUESTION is whether that is sound: the rescue is sound when it finds a carrier member, but when the spec op is a BUILTIN (PartialEq.eq, Ordered.compare) falling through runs the HOST default and decides structurally — which is the silent-wrong-answer case the refusal exists to prevent. So the move probably needs to distinguish 'the spec op is builtin-backed' from 'the spec op is bodyless and value-directable', and that distinction should be measured, not assumed. DEPENDS on carrying the marker's payload to runtime (the arena-slot variant), since a merged representation must still say WHY it pinned nothing. ACCEPTANCE: either one representation with a measured account of the builtin case, or a written decision that the two stay separate, recorded where stand_in_requirement is defined.

## Feedback

### 2026-08-19T20:57:06Z — claude

DELIVERED as a DECISION: the two representations stay separate, recorded at `stand_in_requirement` where the ticket asks. The ticket's own hypothesis is MEASURED FALSE, and the obstacle is not the one it names.

THE HYPOTHESIS: "they cannot be unified TODAY because the marker refusal sits at resolve_op_target_checked, BEFORE the value-directed rescue. If the refusal moved to AFTER the rescue fails, ONE representation would serve both." Three experiments, each over the whole anthill-core suite (4534 tests):

E1 — MERGE ALONE (stand_in_requirement mints the marker as its OWN functor; refusal untouched). 2 failures. `wi818 variant_b_carrier_impl_loads_and_evaluates` — a WORKING program — is refused; and `wi818 requires_path_reports_missing_body_like_direct_call` reports a MISSING BODY as an UNPINNED REQUIREMENT. The ticket predicted the first and named it; the second it did not, and it is the sharper one: one representation collapses two outcomes into one message, so the reader is sent to declare a provider for an operation whose actual defect is that nothing runs it.

E2 — MERGE + MOVE THE REFUSAL after the rescue (the ticket's proposal). THE TWO DO NOT RECOVER. They become `Internal("dispatching dict for wi818.b.Nameable.tag has arity 1 but its requires chain wants 0 slot(s) — 0 for spec `Nameable`'s requires chain then 0 for provider `anthill.reflect.NoProvider`'s")`. THAT IS THE REAL OBSTACLE, and it is not ordering: a dictionary's LAYOUT IS COMPUTED FROM ITS FUNCTOR, so a marker functor erases which provider's chain the dictionary stands in for. The merged form is layout-incoherent before any question of when the refusal runs. Nine further rows fail with it (wi857 x1, wi865 x7, wi869 x1) — each a refusal that used to name its cause, now an arity mismatch or, in `a_spec_half_with_no_provider_names_the_spec_it_is_missing`, "operation has no body: wi865.tie.Base.b", a repair pointing at the wrong file.

E3 — THE BUILTIN CASE, which the ticket asked to be measured rather than assumed. With the refusal off the dispatch path, a body reading a marker slot to call the BUILTIN-backed `PartialEq.eq` gets the host's structural verdict for a requirement NOTHING provides: `Driver.same -> Ok(Int(1))`, `Driver.diff -> Ok(Int(0))`. BOTH POLARITIES on purpose — `eq(x, x)` alone proves little, since reflexivity can be answered before dispatch, so `diff` (two distinct values, answered 0) is the row that shows the host's `eq` actually compared and decided. Under the refusal that ships, both refuse, naming the spec and the repair. Those two numbers and their control are `wi868_a_builtin_read_through_a_marker_slot_is_refused`, kept as the tripwire for a future merge attempt.

REACHING E3 AT ALL IS PART OF THE ACCOUNT. Two independent LOAD-TIME gates refuse the obvious spellings, measured: WI-1102's use-site discharge for a concrete carrier ("`Wrap` provides no `anthill.prelude.PartialEq` — declare `provides …`"), and `check_provider_requires` (WI-343/356) for a provider that does not cover its spec's chain ("'WTop' provides 'Top', which requires 'PartialEq', but 'WTop' does not provide 'PartialEq'"). The fixture needs the WI-865 template's RED HERRING — a provision at a binding the call never uses — to mint a marker at all. The case is narrow; where it exists the refusal is the only thing between the program and a host answer.

THE DECISION, recorded at `stand_in_requirement`: two representations, because they answer two questions. A marker says "nothing pins this slot, and here is why" — a verdict, refused at every dispatch. A stand-in says "this frame was entered from a host that named no dictionary; the receiver VALUE may still say which impl" — an invitation to the rescue, which is the only reason `interp.call` works at all on a sort with a `requires`. RE-OPEN CONDITION, stated: a stand-in that keeps the PROVIDER's identity while carrying an absence — the layout question and the evidence question answered by different FIELDS rather than by one functor. That is a change to the dictionary VALUE, not a re-ordering of the refusal, and E2 is the measurement that says so.

/code-review high: no correctness bugs; two low findings, both acted on. (1) `expand_dispatching_dict`'s no-owner arm still said "a namespace-level target" — the exact misattribution WI-866 removed 30 lines above, and false for the same measured reason (a namespace RESOLVES). Corrected, naming what actually reaches it and where the namespace case is really handled. (2) the always-on panic in `check_against_prediction` is the opposite answer to the one its two neighbours give; the doc now states the RULE that decides it — whose mistake can this be — with all three sites listed and the condition under which the answer moves.

Full workspace green: 35 binaries, 5275 tests, 0 failures. No behaviour change ships with this ticket: it is a decision, a doc, and the fixture that pins what the decision rests on.

