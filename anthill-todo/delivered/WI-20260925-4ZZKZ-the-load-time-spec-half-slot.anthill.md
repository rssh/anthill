## Attributes

- id: WI-20260925-4ZZKZ-the-load-time-spec-half-slot
- created: 2026-09-25T04:50:17Z

- status: Delivered
- status_agent: claude
- status_at: 2026-09-26T02:01:32Z

- acceptance: cargo-test

- depends_on: WI-20260925-P5G39-provision-checks-resolve-under

- tags: typing

## Description

THE LOAD-TIME SPEC-HALF SLOT STOPS BEING SILENT. WI-857 records a spec-half sub-goal that does not resolve as `ResolvedRequiresNode::Unavailable` (synth.rs, the `i < provider_half_start` arm), and only a READ of the slot at eval is loud. Its own justification is the resolver's false 'no': 'the spec's own requires is often satisfied only LOOSELY' (`FiniteCollection requires Iterable[C = C]` at `List`, reached only via `List provides Stream provides Iterable`), and refusing broke 33 tests at the time. The absence therefore reaches run time, which the repo principle forbids, and it hides resolver bugs: WI-20260925-P5G39's 10 stdlib slots came from exactly this.

MEASURED 2026-09-25: a panic at that push fails EVERY test (772 at the first binary), because the bare stdlib itself produces 10 such slots, all from WI-20260925-P5G39's check. Once that lands, the stdlib no longer masks the population and the probe tells which programs actually depend on the silent slot.

WORK: re-run the probe (panic, tagged by site) after P5G39 and decide per surviving shape. Either fix the resolver's precision (transitive provision at the binding level; effect-row matching, cf. wi508's `FiniteCollection[C = MutableStack, E = {}]` NoMatch that runs fine) or turn the verdict into three answers (built / undecided-because-open / definitely absent), refusing 'definitely absent' at load and forwarding or deferring 'undecided' explicitly. The three RUN-TIME producers in bridge.rs (`UnderDetermined` at an entry with no caller dictionary, `NamedSlotNotCarried`, `ParamSlotNotCarried`) are listed separately: each has its own documented reason ('the body may never read the slot'), and making them loud moves the failure to the ENTRY, per 3G1YT's 'a declared requires is owed because it is declared'. Decide them explicitly, one by one.

ACCEPTANCE: no load-time `Unavailable` is produced (the synth.rs arm refuses, or is unreachable by construction); every test that depended on it is re-read and either updated with a reason or shown to rely on a resolver bug that is fixed here; the run-time producers each carry a recorded decision; full workspace green via rustland/scripts/test.sh. SOURCE: WI-883 follow-up discussion.

## Changes

### 2026-09-25T18:51:15Z — feedback — user

PROBE RE-RUN AFTER P5G39 (2026-09-25; log-only probe at the synth.rs spec-half Unavailable push, tagged by test thread + call path). Bare stdlib: 0. Full workspace 7610/0/13 with the probe; 720 pushes in 72 tests, all anthill-core. SHAPES: (C1, ~500) a SIGMA-LESS resolution searches a goal whose element is an OPEN parameter (Eq[T = Coll.T], Eq[T = ?]): candidates.rs arm (2)'s sigma-less leniency (is_type_param_value && sigma.is_none()) admits it against the one STRUCTURED Eq head in the stdlib, SortedSet's, so SortedSet is 'chosen' and its spec half fails. Producers: build_dispatching_dict_direct (req-insertion DIAGNOSTIC dict, never read by eval) and scope_contract_covers_dep's provision leg (sigma None). (C2, ~70) a witness's named-slot pin (LexFst[OA = ...], ByInner[OI = ...]) does not reach the SPEC half: locality picks the same witness for PartialOrd and its slot ties (wi870, wi456 repair/deep rows — working programs). (C3) wi857/865/868 fixtures: provisions that are genuinely unsound (WTop provides Top[T = Wrap[E = E]] with no Base[Wrap[E]]) and load only through the checker's non-ground base-level fallback. (C4) check_provider_requires: the chosen provider's spec half fails because that provider's own provision is refused anyway (wi1109/1110/1111/wi343/wi869/p5g39 rows). (C6) effect-row matching in a spec half: E = PersistentCollection.Effect left unsubstituted, and E = EffectsRows[empty_row] vs a provision's E = {} (wi_wn9p8, wi_tx0g6, wi456_no_scope). (C5) a handful already refused at load for another reason (wi281, wi869 float pair, wi456 erased). Plan: fix C2/C1/C6 precision, make the checker precise for non-ground goals (C3), then refuse at the arm.

### 2026-09-26T02:01:17Z — feedback — user

DELIVERED (2026-09-26). No load-time Unavailable is produced, and none is representable: resolve_inner fails a spec-half slot exactly as a provider-half one (provider half resolved FIRST, so a refusal still names the provision's written condition — wi869's float-pair row), and ResolvedRequiresNode has no absence node any more. The run-time absences ride their own slot type, BridgeSlot::Absent (bridge.rs), ported by dictionary_of_absence; UnavailableWhy keeps only UnderDetermined / NamedSlotNotCarried / ParamSlotNotCarried, AbsenceRecord::Slot lost `below`, unavailable_why_of and relation.rs's tree_has_unavailable are deleted.

PER SHAPE (probe after P5G39: 720 slots in 72 tests, all anthill-core; bare stdlib 0):
 (C1, ~500) sigma-less open-element search: candidates.rs arm (2)'s sigma-less leniency (a bare type-param element matching a STRUCTURED head) removed — the stdlib's one structured Eq head, SortedSet's, 'answered' every Eq[T = X.T] / Eq[T = ?]. The req-insertion diagnostic rewrite (record_apply_within_concrete) now records the call site's own dispatch_dict instead of re-deriving it sigma-less in the callee's parameter space.
 (C2, ~70) witness slot bindings not reaching the spec half: LocalProvider carries the enclosing witness INSTANCE's written slots into a sub-goal locality answers with the same instance (same impl_subst) — LexFst[OA = Rev]'s PartialOrd floor no longer re-asks OA and ties.
 (C6) effect-row matching: provider_requires_subgoals omits an effect-row binding left at the spec's own parameter (the loader's spelled omission, E = PersistentCollection.Effect), so WI-387's omitted-row rule applies. A first cut that made ANY variable row non-discriminating broke wi608 (see NOT FIXED).
 (C3) check_provider_requires' base-level non-ground fallback DELETED: every goal resolves through P5G39's route, witness-locally (resolve_within_provider, 058 §3.8 — ByFst/BySnd bundles tied without it). Probe measured on the bare stdlib: precise == loose on every non-ground goal, so neither 'stdlib reality' in the old doc survives. The TooWeak classifier ranks witness-locally too.
 (C4) checker spec halves of refused providers: now a second, true refusal. LoadError::UnsatisfiedProviderRequires carries failure: RequirementFailure — NoProvider (the old 'on the carrier' sentence only when the goal is about the carrier, else 'nothing provides <goal>'), Unwritten (the goal names a parameter of the provided spec the provision left unwritten), Ambiguous naming candidates, Cyclic, Refused (the chosen provider cannot answer it: the resolver's named-slot hint) and Below (naming the provider and the failing goal) — classified from WHERE the top-level resolution failed (resolve_within_provider returns a TopFailure: provider chosen + provider-half slot / spec-half slot / refusal / alternatives), so WI-865's distinctions survive the move to load. P5G39's unentailed_condition_of_own_provision, which re-derived the failing condition by re-ranking and re-resolving without locality, pins or the spec half, is deleted; TooWeak is now read off that report.
 (C5) programs already refused for another reason: unchanged verdicts.

USE-SITE SCOPE TO ANY DEPTH (found by /code-review): a spec half is no longer tolerated, so the sub-goal lookup through a scope entry's chain (CKD4J's one hop) now follows it down (scope_chain_cover: build_requires_tree's order, composition and per-path cycle rule, breadth-first, pruned to entries that can reach the goal's spec, deeper than one hop only under sigma) — `requires Ord[A]` answering `WeakOrd.compare` at Pair[A, A] needs Eq[A] two hops down, which the load check (closed assumptions) admitted and the use site refused.

TESTS RE-READ AND UPDATED, each with its reason at the site: wi857 (two rows: refused at load; the resolver FAILS Top[Wrap[Int64]] naming Base), wi865 (the seven-row matrix now pins the LOAD refusal's wording per kind; host-entry row unchanged), wi868 (the gap fixture refused at load; the builtin tripwire kept through the host-entry stand-in on a sound twin), wi_ee0ep (Flat's WeakOrd[Pair[A, B]] was unsound at generic A/B — now :- WeakOrd[A], WeakOrd[B]), wi224 (match arm), and new wi_4zzkz_spec_half_refusal_test.rs (two-hop cover, own-provision spec half is not TooWeak, a refusing provider is named with its reason, an unwritten parameter is named). CONTROLS MEASURED: restoring the checker fallback fails exactly the 9 load-refusal rows; rendering every failure as NoProvider fails exactly wi865's 6 kind rows; each wi_4zzkz row fails under its own back-out; wi868's host-entry row answers Bool(true) with the marker refusal removed.

RUN-TIME PRODUCERS — each KEPT, loud at the read, decision recorded at its producer in bridge.rs, population measured by a log-only probe over the full workspace: UnderDetermined 73 slots / 25 tests (71 value-directed dispatch into MappedStream/FilteredStream.splitFirst and Mapped/FilteredStreamFinite.collect, whose sort-level requires Iterable[C = Source] names a parameter no value carries; 2 SLD-bridge size), none read — refusing at entry would refuse every map/filter pipeline evaluated by value; NamedSlotNotCarried 1 (R10KC's existential return — no route could build it); ParamSlotNotCarried 4 (host entries, repair call_with_witnesses named). The typed call sites of the same operations build these slots with sigma and are checked at load.

NOT FIXED, recorded: build_dep_projection's Strategy 2 still projects one hop for a DIRECT dependency (a completeness gap that refuses loudly, pre-existing); a provision refused for an unpublished dotted parameter (WI-954) now also when its goals were all non-ground (HEAD refuses the same shape for a carrier with any checked goal); load cost of resolving every goal measured at ~4% of a debug stdlib load (2.42 s vs 2.32 s, medians of 6, not a quiet machine). an OPEN element in a sigma-present PROVIDER-half goal still resolves by a lucky unique bare-head match (FilteredStream.splitFirst's Iterable[C = TypeVar] picks Stream; a second bare-headed Iterable provider would tie it — wi608 showed it when rows were made non-discriminating). Pre-existing, provider half, not an Unavailable producer. kernel-language.md's NX4FD paragraph still says the compile-time builder places a marker 'since WI-857' — spec wording proposed to the user, not changed.

Two /code-review passes (9 + 14 findings; the ones not fixed are listed above). Full workspace 7615 passed / 0 failed / 13 ignored (30 binaries, 36 results) via rustland/scripts/test.sh.

