```anthill
fact WorkItem(id: "WI-360", created: "2026-06-01T05:03:44Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-341", "WI-342"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-01T06:02:41Z"))

fact Feedback(workitem: "WI-360", author: "user", at: "2026-06-01T06:02:41Z")
```

## description

Emit a callback's latent Modify[<arrow param>] keyed to its CallbackParam place so the WI-353 op-boundary classifier fires END-TO-END from source (the propagation direction of proposal 046 effect_derive; docs/design/modify-effect-derive.md §2/§3). GAP (established by the WI-353 probe): a callback param f: (a: T) -> R @ Modify[a] does NOT load today -- the 'a' in Modify[a] resolves to nothing (UnresolvedName 'a'); the place is registered f.a (CallbackParam, WI-351/352) but nothing connects the bare arrow binder 'a' to it, so the body effect row never carries Modify[f.a] and WI-353's CallbackParam arm (kb/region.rs op_boundary_effects) is never exercised from source -- WI-353 tested it only with a SYNTHETIC Modify[f.a] input row vs real flow facts. NEEDED: (1) resolve/key the arrow-effect binder to its CallbackParam place -- at load via loader binder context (WI-341 mechanism A: resolve 'a' -> place f.a) or as a Var opened to the place (mechanism B: binder-as-value-var, which the doc prefers because it ALSO serves the find-style -Modify(a) CHECKING direction); WI-341/WI-342 establish the binder representation. (2) typing.rs check_apply_iter Path 2 (variable-with-arrow-type, ~typing.rs:3390) currently returns an applied callback's arrow effects RAW with no substitution (Path 1 / known-op does substitute_ref_syms; Path 2 does not) -- confirm that with the binder keyed to f.a this yields Modify[f.a] in the row, or add the open/instantiate step there. (3) when the path goes live, replace WI-353's documented LIMITATION -- the latent UNSOUND-DROP when flow_derive under-approximates a feed (callback arg is a library-accessor result, or a nested-callback application it does not model -> no keep_modify solution -> currently dropped) -- with the doc's sound coarse fallback: all-to-all keep (046 §'Role of kind', flow-presence conservative axis). DEPENDS on WI-341 (binder-as-value-var / loader binder context) + WI-342 (entity-representation migration; subsumes WI-341 step 2). ACCEPTANCE: an op with a @ Modify[a] callback applied over a list element surfaces Modify[l] at its boundary FROM SOURCE (full pipeline, not a synthetic row) -- the WI-353 region-test scenarios (each->Modify[l]; foldLeft accumulator->{Modify[z],Modify[result]}; Int-result masking) reproduced end-to-end; cargo-test green.

## Feedback

### 2026-06-01T06:02:41Z — user

DELIVERED 2026-06-01 by the WI-341 loader binder context (same change/commit). Acceptance MET: callback_element_modify_surfaces_on_list_from_source (wi341_callback_modify_test) proves the full pipeline FROM SOURCE -- binder a->place f.a -> body row Modify[f.a] (Path 2 returns the applied callback's arrow effects raw, which now CONTAIN Modify[f.a], no substitution needed) -> WI-352 flow element_of(l,f.a) -> WI-353 op_boundary keep_modify re-key -> Modify[l] (reported undeclared without the decl; clean with 'effects Modify[l]'). NEEDED items (1) binder->place and (2) Path-2-raw-yields-Modify[f.a]: CONFIRMED done. NOTE a key enabler beyond the two NEEDED items: flow_derive::run had to move BEFORE op-body type-checking (it ran after, so keep_modify had no facts during the typer) -- done in the WI-341 change. Item (3) -- replace WI-353's latent unsound-drop (flow_derive under-approximates a feed -> no keep_modify solution -> dropped) with the doc's all-to-all conservative keep -- is NOT done; remains a documented limitation (region.rs comment), reachable only for body constructs flow_derive doesn't model (library-accessor arg / nested-callback application), NOT the foreach/foldLeft acceptance. Optional hardening; file separately if wanted.

