## Attributes

- id: WI-526
- created: 2026-06-20T09:39:58Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-20T13:43:37Z

- acceptance: cargo-test

- depends_on: WI-523, WI-530

## Description

Proposal 049 build step 6 (radius-3 migration). Migrate every is_equation rule head from `=` to `<=>` across prelude/stdlib (classification-driven: oriented rewrites + [simp]/[unfold] equations migrate; contracts (ensures eq), constraints, body guard tests STAY `=`). Needs the engine to recognize `<=>`-headed equations (WI-523). Resolver-side type-GUARDED firing of requires-guarded rules is OUT OF SCOPE (WI-292).

## Changes

### 2026-06-20T12:02:07Z — feedback — user

HANDOFF from WI-523: recognition machinery is ready (is_equation/apply_eq_rules/try_fire accept unify; selection queries both eq+unify functors). BUT a source-level `<=>`-headed equation only indexes under anthill.kernel.unify if its head functor RESOLVES there. scan_rule_goal mints a fresh <ns>.unify Goal shadow for an unimported `unify` head (resolve_in_scope NotFound), hiding the equation. So migrated equations must reach canonical unify in scope — author them where `unify` is imported/reachable (the same way stdlib `=` equations sit in scopes where `eq` is reachable). DO NOT fix via a global scan_rule_goal skip for reserved names: I tried (skip fresh-Goal when implicit_qualified name exists) and it was (a) a regression — user predicate rules with reserved-name heads silently route to the kernel/prelude symbol (builtin shadows the user body; `or` merges with kernel disjunction), and (b) FLAKY — the by_qualified_name existence check at scan time gave non-deterministic results in the full test binary (passed isolation, failed full-binary intermittently; root cause undiagnosed). Prefer explicit import, or a load-time LOUD diagnostic on reserved-name head collision. Also note: apply_eq_rules (resolver path) does NOT open a rule's DeBruijn vars, so a variable-RHS equation leaves vars unthreaded there — the typer try_fire path opens them correctly (pre-existing, shared with `=`).

### 2026-06-20T13:43:34Z — feedback — user

DELIVERED 2026-06-20 (uncommitted), depends on WI-530 (delivered). Radius-3 migration: 40 empty-body equational rule heads =->><=> across 8 prelude files (eq, pair, set, string, map, int64, float, list), IMPORT-FREE (WI-530 makes <=> heads resolve to canonical anthill.kernel.unify). Classification-driven: guarded equations (field.anthill, list:127, map:48, logical_stream, stream head/tail/isEmpty), body-position = goals, contracts (ensures eq), and constraints all KEPT =. is_equational_head (load.rs ~4180) extended to recognize 'unify' so a migrated <=> equation stays cite-required/unindexed like its = predecessor (WI-139). Out of scope (per ticket): type-GUARDED resolver firing of requires-guarded rules = WI-292. wi526 diagnostic test (no shadow minted). Full anthill-core 1805/0/3 green; independent review verified migration complete+correct both directions and no non-termination (activated Pair/String/Int64/Float equations are orienting; Set commutativity is requires-guarded -> apply_eq_rules skips).

