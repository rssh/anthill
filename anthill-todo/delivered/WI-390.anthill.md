```anthill
fact WorkItem(id: "WI-390", created: "2026-06-05T05:59:22Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-05T09:34:56Z"))

fact Feedback(workitem: "WI-390", author: "user", at: "2026-06-05T06:05:42Z")

fact Feedback(workitem: "WI-390", author: "user", at: "2026-06-05T06:08:22Z")

fact Feedback(workitem: "WI-390", author: "user", at: "2026-06-05T06:13:35Z")

fact Feedback(workitem: "WI-390", author: "user", at: "2026-06-05T06:15:14Z")

fact Feedback(workitem: "WI-390", author: "user", at: "2026-06-05T06:17:46Z")
```

## description

Represent denoted faithfully in the Term world: make occurrence<->term LOSSLESS (admit temporary/scoped items in the hash-consed term-store). PRINCIPLE (user): prefer a SINGLE representation (no permanent Term-vs-Value::Node carrier split for denoted); but WHERE a second representation is transitionally needed, it MUST be CORRECT (lossless), never a lossy projection -- 'from an evolution point of view both are right: better to have no double representation, but if needed it should be correct'. PROBLEM: value-in-type denoted currently rides ONLY as Value::Node (WI-342/348/366 retired the term-based make_denoted), so the Term world is LOSSY for denoted -- which breeds a whole CLASS OF BUGS: 'term loses fact' (reify/materialize drop the denoted; value-fact heads silently skipped via 'fact_head_named_args(rid) else continue'), Value-needs-Hash/Eq-for-keys (the resolve_cache + RequiresEntry.spec blocker that surfaced this), and dual carrier-agnostic TermView paths threaded everywhere. KEY ENABLER (user): 'we can represent occurrence as term' -- make occurrence_to_term (node_occurrence.rs:2173) + the denoted lowering FAITHFUL so a Value::Node round-trips to a Term without loss. MECHANISM: a faithful denoted Term form (un-retire make_denoted in a COMPLETE form that the old ground-only one couldn't be); the hash-consed term-store ADMITS TEMPORARY/scoped items -- the PRICE: a binder-bearing denoted (an arrow type's bound effect/row var) doesn't dedup by structure, so it rides as a refcounted/transient store entry, not a persistent shared one (needs a lifecycle: refcount/GC + binder alpha-equivalence handling). PAYOFF: the Term world becomes faithful -> RequiresEntry.spec stays a faithful TermId, resolve_cache stays TermId-keyed (no Value:Hash+Eq needed), the carrier-split readers for denoted collapse back to term reads, and the bug class dies; evolves toward a single (term) representation while Value::Node stays correct transitionally. RE-SCOPES WI-366 B2: RequiresEntry.spec->Value and the requires/ensures Value migration become unnecessary (denoted requires carried faithfully via TermId) -- B2b is superseded by this. CAUTION: foundational pivot touching unification (denoted-term special-casing + preserving the value-in-type-vs-regular-term marker), the term-store (temporary-item lifecycle + binder alpha-equivalence correctness), and the carrier-agnostic code that existed ONLY for denoted. Acceptance: occurrence_to_term lossless for denoted (Value::Node round-trips byte-faithful); 'requires Foo[Int, 3]' rides as a faithful TermId RequiresEntry.spec (no silent skip, no Value); resolve_cache keys on it correctly; suite green.

## Feedback

### 2026-06-05T06:05:42Z — user

SUPPORTING RATIONALE (the sharpest 'why', from the 'why does Value have no Hash?' thread): the ABSENCE of Value: Hash/Eq is itself evidence that type-structure does not belong in the runtime union -- the SAME signal as the resolve_cache blocker. Value is a UNION of two worlds: (1) KB-structural data (scalars, Tuple, Entity, Term(TermId), Var, Node) which has a structural identity, and (2) INTERPRETER runtime handles (Closure, Stream, Lazy, Substitution, Map, Cell, Requirement) which are arena-refcounted u32 handles with NO lawful Eq/Hash -- a Cell is MUTABLE (identity = slot index, value mutated in place -> any structural hash is unstable), Closure/Stream/Lazy are suspended computations. That is why structural_eq is a METHOD, not impl PartialEq: it treats opaque handles as 'conservatively unequal' (structural_eq(closure,closure)==false) -> non-reflexive -> not a lawful Eq -> no Hash (plus Float(f64) raw NaN, which Term wraps in OrderedFloat and Value does not). KEY POINT: the cache key / RequiresEntry.spec / any type-or-spec position only ever holds the STRUCTURAL subset (Term/Node/Entity) -- never a Cell/Closure. So routing type-structure through the runtime Value union was the carrier-split's root. THE HASH STORY UNDER THIS WI: do NOT invent a separate Value structural hash. Once Term<->Value is total+lossless (this WI), DEFINE identity by the mapping: Hash(value) = Hash(its canonical Term), so a Value and its Term share ONE identity (same hash, equal across carriers) -- the single-representation ideal at the identity level. Precondition is exactly the opaque-free subset (no Cell/Closure/Stream -> they have no Term; a mutable cell cannot have a stable canonical term), which the type/spec/cache context satisfies by construction. END-STATE: Value::Node-for-denoted FOLDS INTO Value::Term -- Node existed ONLY because Term could not hold the occurrence; make Term faithful and the carrier collapses, leaving identity = TermId everywhere on the structural subset. resolve_cache/RequiresEntry then key on plain TermIds and 'hash' is never a separate concern; the opaque runtime handles are simply the part of Value that is not KB-structural (and never needs a key). MECHANICAL WALL this resolves: std::hash::Hash cannot take kb, so a hashable value must already CARRY its canonical TermId -- Value::Term does, and this WI makes denoted a Value::Term, so there is no kb-at-hash-time.

### 2026-06-05T06:08:22Z — user

DESIGN REFINEMENTS (user 2026-06-05): (1) The Term<->Value conversion is TOTAL and HONEST via Result, not panic/lossy: value_to_term(v) -> Result<TermId, NotRepresentable>. Ok(term) for the structural subset; Err for a Value carrying a non-representable element (the opaque runtime handles Closure/Stream/Lazy/Map/Cell/Substitution/Requirement). This REPLACES today's panic/silent-loss at the boundary (reify / materialize / build_expr_leaf panic / 'fact_head_named_args else continue'): the Err is a real signal ('this is a runtime value, not type-structure'), so a Cell-in-a-spec/key position surfaces as a typed Err the caller handles, never a panic and never a lossy term. This is the 'if a second representation is needed it must be CORRECT' principle applied to the conversion itself. (2) Occurrences and SPANS fold INTO terms -- 'we will write terms with positions as terms': the term representation gains POSITION, so an occurrence (positioned structure + span) IS a term, and a span is term-representable. This is the lossless mechanism for occurrence_to_term (the span an occurrence carries survives because the term carries it) AND it UNIFIES the two-ish representations that exist today -- (term + side-table span: term_spans/create_occurrence) AND (NodeOccurrence with inline span) -- into ONE: positioned terms. OPEN DESIGN QUESTION to nail in this WI: is the position part of term IDENTITY (hash/eq) or PROVENANCE (carried, ignored for identity)? For the resolve_cache / dispatch, structural identity must IGNORE position (a 'requires' is the same regardless of source location), so position is most likely provenance (the term dedups structurally; the span rides alongside) -- which means the genuinely NON-dedup 'temporary items' are specifically the binder-bearing denoted (scope/alpha-equivalence), not positioned terms in general. Decide this explicitly: it determines whether positioned terms hash-cons (provenance) or stay temporary (identity).

### 2026-06-05T06:13:35Z — user

RESOLVED — position is IDENTITY, not provenance (user): 'Position should be stored in Term, otherwise Modify[c] will be same for different c in different places.' This is a CORRECTNESS argument that overrides the earlier dedup-sharing lean: with position as mere provenance, Modify[a] bound by one callback and Modify[a] bound by another hash-cons to the SAME term despite different binders -> they wrongly unify AND wrongly share a resolve_cache entry. So the term MUST carry it. PRECISE FORM: the load-bearing discriminator is the BINDER IDENTITY the denoted reference resolves to (which c / which binding site), with source position as its carrier -- so two references to the SAME binder stay EQUAL (unify/dedup) regardless of where they appear, and two references to DIFFERENT binders (same surface name, different scope) DIFFER. NATURAL SCOPING that keeps ground dedup intact: a denoted BINDER REFERENCE carries its binder's identity/position -> binder-bearing denoteds are SCOPED (the 'temporary/scoped' store items that don't share the global ground dedup -- exactly the CLAUDE.md 'hash-consing inappropriate for binders' case), while a pure-GROUND term (Foo[Int, 3], no binder) stays position-free and hash-conses globally as before. So 'position in Term' is specifically the binder-reference's binder-identity, not a blanket source-span on every node (a blanket span would over-distinguish same-binder reuse and break unification of two uses of the same a). IMPLEMENTATION CHOICE to pin in this WI: encode the binder identity as the binder's binding-site (position/uid) vs a globally-unique per-binder symbol -- either disambiguates; the binding-site-uid is the 'position' framing.

### 2026-06-05T06:15:14Z — user

CONCRETE ENCODING (user): a special term anthill.reflect.Positioned(pos, internal) -- a Term::Fn, NOT a new Term enum variant, so it reuses hash-consing (two Positioned differ iff pos or internal differ -> different binders/sites -> different terms; internal is the recoverable structure) and is special-cased by the typer/unifier like the other anthill.reflect.* forms (SortView/OperationInfo/TypeExtractor). Realizes the prior points: an occurrence IS Positioned(span_term, structure_term) -> 'occurrence and span become terms'; value_to_term builds it (Err only for opaque runtime handles); the resolve_cache then keys on plain TermIds again. ONE THING TO PIN: what exactly is pos? (a) the BINDER-SCOPE IDENTITY the denoted reference resolves to -> two refs to the SAME binder stay EQUAL (same pos) regardless of source location, different binders differ. This is the CORRECTNESS discriminator for Modify[c]. OR (b) the raw SOURCE SPAN -> over-distinguishes (same binder used twice = two terms -> their unification/dedup breaks), good for provenance/diagnostics but WRONG as the binder discriminator. These can be BOTH carried but only ONE is identity-bearing: pos-for-identity must be the binder-scope (a); the source span rides as provenance. Equivalent framing: if a binder were resolved to a globally-UNIQUE symbol per binding-site, then 'internal' alone distinguishes binders and Positioned's pos is pure provenance; the user's 'pos in identity' assertion means binders are SCOPED (non-unique surface), so pos=binder-scope is the identity. DECIDE in this WI: pos = binder-scope-uid (identity) with source-span as separate provenance, vs unique-binder-symbol (pos -> provenance). Also: granularity -- Positioned wraps the binder-bearing/denoted node (keeping pure-ground terms unwrapped + globally deduped), not every node.

### 2026-06-05T06:17:46Z — user

CORRECTION to the prior note's (a)/(b) framing (it was wrong): binder-scope identity is NOT an alternative to pos -- it CONTAINS pos / is GIVEN BY pos (user: 'binder-scope identity contains pos'). A binder is identified by WHERE it is bound, so pos = the BINDING-SITE position of c, and that position IS the binder identity. There is no 'binder-scope-id vs source-span' choice to make. This also dissolves the over-distinguish worry: pos is the binder's binding-site, NOT the reference's location, so every Modify[c] resolving to the same c carries the SAME pos -> the SAME Positioned(pos, internal) term -> they unify/dedup; only a DIFFERENT binder (different binding site -> different pos) yields a different term. So anthill.reflect.Positioned(pos, internal) with pos = binding-site position is the complete answer; a reference's own source span (for diagnostics) is unrelated provenance, not this pos.

