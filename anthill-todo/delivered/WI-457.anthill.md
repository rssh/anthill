```anthill
fact WorkItem(id: "WI-457", created: "2026-06-14T09:11:46Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-15T06:26:12Z"))

fact Tag(workitem: "WI-457", name: "typing")
```

## description

Typer/WI-401 escape-gate gap: an ABSTRACTING (sealing) return via an IF/MATCH-JOIN body slips abstracting_return_error. Today '-> KVStore = if persistent then diskStore else memStore' (bare-spec return, NO ensures) LOADS: the branch-join (compute_branch_join_type) widens the divergent concrete providers up to the bare spec, so body_ty = the declared return sort and the gate's same_symbol(body_sort, ret_sort) returns None (no error). The DIRECT upcast '-> KVStore = memStore' is correctly rejected (body_sort=MemStore != KVStore). So an abstract member (KVStore.K) can ESCAPE via a join-typed body without an 'ensures' vouching for it, bypassing WI-401's escape-free guarantee for the strict base model. Runtime-sound (the value is a real backend) but over-accepting — the type discipline's escape model is bypassed for join bodies. SURFACED during WI-402 (the existential half); deliberately NOT tightened there (WI-401 scope + cascade risk into delivered functionality). FIX SKETCH: when the body type is a JOIN that widened divergent concrete providers up to the bare/partial spec, treat it like the direct upcast in abstracting_return_error — flag unless ensures vouches (kb.existential_return_ops marker). Must NOT reject a legitimate same-sort join or a fully-manifest-spec join. Verify wi402_existential_return_test + wi401_escape_free_return_test + branch-join tests stay green.

