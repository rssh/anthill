```anthill
fact WorkItem(id: "WI-477", created: "2026-06-15T14:41:03Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-471"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-15T14:40:49Z"))
```

## description

Remove/replace Value::as_term -- the silent Value->TermId downgrade that erases information. as_term() returns None for any non-Term carrier (Node/Tuple/Entity), so callers that read it as content treat None as 'nothing' and drop it -- the binding-erasure class fixed in bind_spec_params_from_carrier (WI-470, d42682c). ~80 non-test call sites: resolve.rs ~42 (the term-based resolver), mod.rs ~18, typing.rs ~13, load.rs ~6, builtins.rs ~1. Triage each into three buckets: (1) FAST-PATH branch where None is genuinely handled -> direct `if let Value::Term(t)` pattern match (already the dominant legit style -- 112 Value::Term( matches in typing.rs alone); (2) CONTENT read -> carrier-agnostic extract_type/TermView, never downgrade; (3) genuine TermId DEMAND (the term-based resolver) -> cached_term(kb) to materialize on demand (the WI-471 bridge), or a LOUD accessor that errors instead of returning None. Migration tactic: make as_term loud (or delete it) and let the compiler/suite flush out every site at once, then triage. This is the WI-470 thesis fully realized: occurrences primary, hash-consing a derived index recovered on demand -- no silent Value->TermId leak. The WI-470 commits (ec8c619 arrow flip, d42682c bind_spec carrier-agnostic read) are the first concrete instances. Deserves its own /code-review pass.

