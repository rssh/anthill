```anthill
fact WorkItem(id: "WI-1073", created: "2026-08-10T07:56:47Z", context: none, acceptance: [ToolPasses(tool: "manual-review", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "codex", at: "2026-08-10T17:47:38Z"))

fact Feedback(workitem: "WI-1073", author: "codex", at: "2026-08-10T17:47:31Z")
```

## description

AUDIT: AN IMPLEMENTED PROPOSAL'S LANGUAGE CHANGE MUST REACH kernel-language.md — measured on one instance, unmeasured on the rest.

CLAUDE.md calls `docs/kernel-language.md` the canonical language spec, "kept in sync with the implementation". WI-1070 found it is not, on the construct it happened to touch: `const` (proposal 039 / WI-084) is IMPLEMENTED — `const_declaration` in `grammar.js`, `parse::ir::Const`, `load_const`, `const_map` in a `provides` block (WI-889) — and the string `const` did not appear ANYWHERE in kernel-language.md. Not a production, not a section, not a mention. WI-1070 added §5.9 and the §11 grammar entry for it.

THAT WAS ONE INSTANCE, FOUND BY ACCIDENT. Nobody has asked the general question: which OTHER implemented proposals changed the language and never reached the reference? The proposals directory runs 001–059+; the ones with a "## Affects" section naming `grammar.js` or a `::=` production are the candidate set, and their delivery status is in the tracker.

WHY IT MATTERS, and it is not tidiness. A reader who needs to know whether `const` may carry a description block, or what its effect row is, or whether `const_map` exists, had exactly one place to look and it was silent — so the answer had to be recovered from the parser. Worse for the DRIFT direction the WI-1070 review caught: the spec's EBNF for `OperationEntry` was CORRECT until the grammar changed under it, and would have been left silently wrong. A reference nobody re-reads at the point of change decays toward fiction, and each decayed entry is one a future ticket will "measure" against and get wrong.

THE POLICY IS ALREADY DECIDED, so this ticket is discovery + application, not a design question. USER, 2026-08-10: implemented proposals' language changes are incorporated INTO kernel-language.md; and data is NOT removed from the proposal to avoid the duplication — the proposal stays the design record, the spec section is what an implementer reads, and the two carry cross-links. WI-1070 did exactly that for 039 (§5.9 + §11 entry; 039 keeps its `Const ::=` production and its description-block sentence, plus a pointer to §5.9).

SCOPE:
  1. Enumerate the proposals whose status is delivered/implemented AND which changed the surface language (a production, a keyword, a clause, a declaration form). Cross-check against `grammar.js` rather than against the proposal's own claim — a proposal can be delivered with its grammar half dropped, and a grammar production can exist with no proposal at all.
  2. For each, say whether kernel-language.md carries it: a section, a production in §11's collected grammar, or nothing. Report the table BEFORE writing any prose — the inventory is the deliverable even if the writing is staged.
  3. Incorporate the missing ones, following WI-1070's shape. Cross-link both ways. Do not delete from the proposal.
  4. Note (do not necessarily fix) any place where the spec is not merely SILENT but WRONG — a production that contradicts `grammar.js`. Those are worse than gaps and may deserve their own ticket.

KNOWN ALREADY, do not re-derive: `const` is DONE (WI-1070). One pre-existing wrongness is already spotted and NOT fixed — §4.1 calls the fact `Description(target, text, index)` while the loader emits `anthill.reflect.DescriptionInfo(target, content, index)`; both the sort and the field name differ.

ACCEPTANCE: the inventory table exists and is derived from `grammar.js` + the tracker, not from the proposals' self-description — state the method and the counts; every gap it names is either closed in kernel-language.md or carries a stated reason for deferral (a gap with a reason has an owner and a ticket number at the site, not silence); no proposal loses text; cross-links go both ways for each section added. Docs-only, so cargo-test cannot observe this — say so rather than recording a gate that passes vacuously (WI-1068), and let the inventory table be the evidence.

REFERENCE: WI-1070 (found it, and is the worked example — kernel-language.md §5.9 + §11, proposal 039 cross-linked), CLAUDE.md ("should be kept in sync with implementation"), WI-1068 (why a wrong acceptance tool is not loud).

## Feedback

### 2026-08-10T17:47:31Z — codex

Completed the proposal/grammar/tracker audit. Inventory: 66 numbered proposal files; 40 surface candidates = 25 implemented, 9 partial, 5 unimplemented, 1 merged; 26 explicitly excluded non-surface proposals. Closed G1-G12 in kernel-language.md, including proposal 060's delivered require[X] channel and the compatibility boundary between implemented [simp]/[unfold] typed patterns and open plain-relational typed heads (WI-742/WI-743). Added bidirectional canonical links to 16 proposal files without deleting proposal text. Corrected stale collected grammar plus the ordinary-denial enforcement contradiction in sections 6.2/8.4. Evidence: full changed-hunk manual review, inventory-count checks, proposal-link/reverse-link checks, no proposal deletions, and git diff --check clean. Docs-only: cargo tests intentionally not run because they cannot observe this acceptance. The repository's /code-review skill is unavailable in this session, so I performed the required manual review directly. Queue monitor: 139 open unblocked items.

