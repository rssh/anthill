## Attributes

- id: WI-369
- created: 2026-06-03T11:15:46Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-18T20:12:56Z

- acceptance: cargo-test

## Description

Loader/resolver: ENFORCE internal visibility (currently parse-only). The internal modifier (grammar.js:802 visibility rule; kernel-language.md section 8.6 'hidden from outside the declaring scope', lines 540/1390) is PARSED but NOT enforced: a cross-scope reference to an internal name -- even fully qualified, e.g. a.b.Sort.mkStack -- resolves and loads CLEAN. Verified in WI-364 Phase 0: a MutableStack with internal entity mkStack(rep: Cell[...]) is constructed from a different namespace (anthill.test.mstack.MutableStack.mkStack(...)) with ZERO load errors -- so internal is decorative today. FIX (kb/load.rs + intern.rs scope resolution): record the internal flag on declared symbols (the parse IR carries the visibility modifier) and reject cross-scope resolution of them -- both the scope-walk path (resolve_in_scope) AND by_qualified_name / selective-import -- so an internal name is visible only within its declaring scope (the sort/namespace) and that scope descendants; emit a load-blocking error (UnresolvedName or a dedicated forbidden-access diagnostic) naming the internal symbol. NEEDED to encapsulate stateful carriers: a MutableStack/MutableCollection representation handle (the rep Cell) must be hidden so external code cannot alias it and break invariants (proposal 002 / WI-364). ACCEPTANCE: cross-scope construction/projection of an internal ctor/field errors at load with the name; same-scope use stays clean; default-visible behavior and the standalone export no-op unchanged; stdlib + full anthill-core green. REF: kernel-language.md section 8.6; grammar.js visibility rule; WI-364 Phase 0 finding. Rust-only (resolver; scaland mirrors only if it grows the same check).

## Changes

### 2026-06-18T20:12:55Z — feedback — claude

DELIVERED 2026-06-18 (not committed). Enforce 'internal' visibility (was parse-only). MECHANISM: SymbolTable.internal_syms HashSet + mark_internal/is_internal/internal_visible_from (walks is_enclosing parent chain from the entry scope to the symbol's declaring scope). record_internal wired at all 4 visibility-bearing definition sites in scan_items_pass1 (Sort/AbstractSort/Entity/Operation + OperationBlock). ENFORCEMENT: (1) name resolution — resolve_in_scope resolves IGNORING internal then POST-FILTERS matched symbols by internal_visible_from(sym, entry_scope) [KEY DESIGN: a per-hop parent.locals filter was UNSOUND — code-review found 2 leaks: transitive (internal reached via a non-enclosing parent's enclosing grandparent) and re-export (internal imported into a descendant then wildcarded out); the post-resolution gate on the matched symbol vs the ENTRY scope fixes both]; (2) by_qualified_name + dotted-probe + multi-segment fallback guarded via internal_visible_from; (3) selective/plain import guarded via forbid_internal_import; (4) field PROJECTION (b.field) — typer ForbiddenInternalField at the DotApply dispatch + field_access_owning_ctor paths, access scope = env.enclosing_sort()'s body scope (kb.alloc(Fn{sort}).raw() == load-time scope_raw via hash-consing). New LoadError::ForbiddenInternalAccess (load-blocking) + TypeError::ForbiddenInternalField. MutableStack carrier 'entity mutableStack(rep)' now 'internal'; its own ops construct+project same-scope (enclosing → clean); external construction/projection blocked. 8 tests (qualified/bare-requires/selective-import construction, projection, same-scope+public-API control, public-sibling selectivity, transitive-leak + re-export-leak regression guards). Full workspace green (60 binaries). /code-review xhigh (3 agents): found the 2 soundness leaks (FIXED) + dead scope_raw_of (REMOVED); regression-safe (filter is a no-op when nothing is internal); free-op projection None-branch is dead-but-defensive (namespace ops get the namespace as enclosing_sort; top-level ops are parse-rejected). Spec §8.6 already described this; impl now matches.

