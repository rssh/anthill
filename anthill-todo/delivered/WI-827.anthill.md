```anthill
fact WorkItem(id: "WI-827", created: "2026-07-23T13:23:33Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-24T19:47:22Z"))
```

## description

INTEGRATE σ into provider-candidate matching — match_candidate_against_goal is σ-blind and spelling/granularity-sensitive exactly where WI-821 taught it to record rigids (/code-review findings 3, 5, 8, 11 — three CONFIRMED by live drives, one PLAUSIBLE). MEASURED DEFECTS: (a) a requires-clause COMPOUND element (requires Desc[T = Wrap[A = HT]]) keeps Ref-SPELLED components — substitute_spec_via_subst deliberately preserves abstract elements as written Refs — and the record arm (matches! Var::Rigid only) SKIPS them, so the conditional sub-goal keeps the raw impl param, the σ-gated FromScope refuses it, resolution dies Cyclic → dict None → runtime '__req_desc not bound'; the semantically identical whole-param spelling (requires Desc[T = WT] at WT := Wrap[A = rigid]) works (measured Ok(52) vs Internal). (b) TWO DISTINCT rigids for one impl param (diagonal provider fact Desc[T = Pair[A = E, B = E]] at per-call Pair[A = rigidGT, B = rigidHT]) are accepted with only the FIRST recorded — measured Ok(11) = half-wrong dict (sound = refusal; both-correct = 15); the type-param early-return never compares against the occupied slot. (c) the stored-rigid-yields-to-concrete arm checks only the stored HEAD: a stored compound-containing-rigid (Wrap[A = rigid]) vs a structurally different incoming compound drops the candidate in BOTH orders (NoMatch → dict None → unbound), while the bare-rigid twins ACCEPT the same structural disagreement (measured Ok(9)/Ok(12)) — the verdict depends on which side of the fact the compound is written, not on the disagreement. (d) PLAUSIBLE: arm (2.5) (self-provides alignment) was not given the rigid-yield symmetry — values_structurally_equal(rigid, concrete) rejects the whole candidate when arm (1) recorded a rigid and (2.5) later threads a concrete into the same param. DEEPER FORM (altitude review): thread the σ ctx (ResolutionScope.sigma is already in resolve_inner's hand at the collect_provides_candidates call) into candidate matching and classify each per-call element ONCE via sigma_class — spelling-neutral: no σ-class → concrete arm; rigid-terminal class → record the canonical rigid (any spelling); global class → unconstraining skip — and define rigid-conflict semantics deliberately (two distinct rigids = refuse or unify-check, compound granularity = component-wise). σ-less consumers (resolve_at_goal etc.) keep today's behavior — verified no divergence exists there (two claims driven and REFUTED on both commits). ACCEPTANCE: the three driven fixtures flip to sound verdicts (compound-requires witness = depth-coded values, not Cyclic; diagonal-rigid = refusal or both-correct; compound-stored yield consistent across spellings); wi817 (15) + wi224/wi227 + full workspace green.

