```anthill
fact WorkItem(id: "WI-873", created: "2026-07-30T16:12:37Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-19T22:54:17Z"))

fact Feedback(workitem: "WI-873", author: "claude", at: "2026-07-30T16:06:59Z")

fact Feedback(workitem: "WI-873", author: "user", at: "2026-08-19T22:54:11Z")
```

## description

`dispatch_origin` KEEPS ONLY ONE DEFER REWRITE PER SPEC OP FOR THE WHOLE IMAGE — a second sort whose `requires` chain names that spec EVICTS the first, silently. MEASURED at WI-858's HEAD, by loading the same stdlib beside two different fixtures and listing `kb.dispatch_origin_iter()` in full: (a) with fixture `sort Wi227Flat { sort T = ?  requires PartialEq[T]  operation use_eq(a: T, b: T) -> Bool = eq(a, b) }` the surviving `anthill.prelude.PartialEq.eq` entry is `var_ref(name = __req_partialeq_14325)` — `anthill.prelude.Pair`'s, whose chain names `PartialEq` twice — and the FIXTURE'S OWN rewrite is ABSENT; (b) with the near-identical `Wi222Box` fixture the surviving entry is the fixture's `__req_partialeq` and Pair's is absent, while in that same load the `Ordered.compare` entry is a `Pair` ordering's `__req_ordered_14331`. TOTAL entries were 11 in every configuration — the same count with and without a fixture that indisputably adds a rewrite, which is the measurement that says entries are LOST rather than merely ordered. WHY IT WAS INVISIBLE UNTIL NOW: the recorded terms carry `args = nil()` — the argument list is EMPTY in the stored rewrite — so the only distinguishing content is the requirement-param name, and before WI-858 no sort in the tree repeated a spec in its chain, so `synth_req_names` never produced a disambiguated `__req_<spec>_<n>` and every sort's rewrite for one spec was the SAME term. Two sorts sharing one entry was indistinguishable from two sorts sharing one shape. THE MECHANISM IS NOT YET IDENTIFIED and must be DRIVEN, not read: the map is `HashMap<TermId, Symbol>` (`kb/mod.rs`, `dispatch_origin.insert(rewritten_apply, spec_op_sym)`) and two rewrites differing in the var_ref name are structurally different, hence hash-cons to different TermIds, hence should BOTH be present — so either the insert happens before the requirements sub-term is filled in (one placeholder TermId reused, last writer wins) or a later pass re-allocates over the entry. Find which by instrumenting the insert site, not by inspection. CONSUMER IMPACT: three tests (wi222_defer_rewrite_test x2, wi227_projection_search_test x1) select 'the rewrite for this spec' from this map and assert the requirement-param name, so they were asserting whichever sort's rewrite happened to survive; they now assert the SPEC of the name via common::assert_req_param_spec, which tolerates the disambiguating suffix, and that helper's doc carries this measurement. Whether any NON-test consumer reads the map for anything but diagnostics is part (a) of the work — if one does, a lost entry is a lost rewrite. ACCEPTANCE: every defer rewrite in a load is present in the map (assert the count grows when a fixture adds a sort with a fresh chain); the three tests above go back to asserting the EXACT synthesized name and assert_req_param_spec is deleted (that reversion IS the acceptance); full workspace green via rustland/scripts/test.sh. REFERENCE: docs/design/operation-call-model.md 'Names model'; rustland/anthill-core/src/kb/mod.rs dispatch_origin; rustland/anthill-core/tests/common/mod.rs assert_req_param_spec.

## Feedback

### 2026-07-30T16:06:59Z — claude

ACCEPTANCE NEEDS ONE MORE CLAUSE, found in review of WI-858. 'The three tests go back to asserting the EXACT synthesized name' is not sufficient and would be FLAKY: `dispatch_origin` is a HashMap, so the moment it correctly holds more than one rewrite per spec op, the tests' current selection — 'take the first (or last) entry for this spec' — becomes nondeterministic and an exact-name assertion fails at random. Fixing the eviction therefore BREAKS those tests unless their SELECTION is fixed in the same change. So the acceptance must also require each test to scope its selection to its OWN fixture's rewrite. NOTE, measured: no per-fixture handle exists today — the stored rewrite carries `args = nil()`, an EMPTY argument list, so nothing in the term names the fixture's binders (a marker search over the term was tried at WI-858 and found nothing). Whatever makes the entries distinguishable is part of this ticket: either the rewrite must retain its args, or `dispatch_origin` must be re-keyed by something that identifies the enclosing operation. Also note there are FOUR call sites, not three: wi222_defer_rewrite_test x3 (deferred_call_rewrites_to_apply_within_with_spec_op_fn, requirement_name_tracks_requires_chain_entry, dispatching_dict_is_caller_direct_requirement_var_ref) and wi227_projection_search_test x1. The fourth was found only by the FULL-workspace run — a `--test wi_tests` run had it passing, because which entry survives varies with interning order, so a per-binary green is not evidence for this class.

### 2026-08-19T22:54:11Z — user

DELIVERED. The mechanism was NEITHER hypothesis in the description — nothing was ever overwritten, the entries were never inserted. `req_insertion::materialize_apply` synthesized the table's key as `apply(fn = Ref(functor), args = nil())`; terms are hash-consed, so that key named the CALLEE and nothing else, every call site of one spec op in the image collapsed onto one TermId, and the recorders' `contains_key` guard — written as idempotence for a pass re-run — dropped every site after the first. DRIVEN by instrumenting the materialization site: `PartialEq.eq` -> TermId(17285) four times, `WeakOrd.compare` -> TermId(17277) six times. The `args = nil()` the description flagged as "why it was invisible" is the same line's doing: `materialize_apply` writes nil deliberately, so nothing in the term distinguished sites. NOTE FOR NEXT TIME: this was already written down one hop away — `wi222_defer_rewrite_test`'s WI-1091 doc says "IDEMPOTENT on a key built from the callee functor ALONE, so every abstract `eq` call in the image collides", in a file this ticket lists as a consumer.

PART (a) ANSWERED: no non-test consumer reads either map, at all. `dispatch_origin_of` and `dispatch_rewrite_of` had ZERO callers repo-wide (Rust and anthill source) and are deleted; the only remaining mention outside the pass is a comment in `load.rs`. So a lost entry was a lost DIAGNOSTIC, not a lost rewrite — the runtime takes `CallClass` off the NodeOccurrence (post-WI-248).

FIX: the two term-keyed maps collapse into one keyed by `CallSite { op, functor, span, nth_at_span }` holding `DispatchRewrite { rewritten, spec_op }`. `op` comes from `op_bodies_iter`'s own key — the occurrence's `owner` is None on every inner expression node (measured, all 47), and `PinNow` records no enclosing scope. `dispatch_origin_iter` is kept (8 test files read it) and is now complete; its doc states it yields one item per SITE, so a TermId can repeat, and points at `dispatch_rewrites_iter` for attribution.

MEASURED, stdlib + anthill-stl: 47 classified call sites, 16 recorded rewrites before; 39 / 40 / 41 after, for zero / one / two deferring fixtures. Before, the total was 16 in all three configurations — the description's own number.

ACCEPTANCE MET: `wi873_dispatch_rewrite_completeness_test` (5 arms); all four call sites revert to EXACT synthesized names, each selected by its OWN enclosing operation via `common::rewrite_in_op`; `assert_req_param_spec` deleted. The feedback's extra clause is what `rewrite_in_op` is for — it filters `dispatch_rewrites_iter` by `site.op` and asserts exactly one hit, so nothing depends on HashMap order. CONTROL, by mutating the key rather than deleting anything: `size_grows_per_fixture` fails with "baseline 16, with fixture 16"; two other arms fail with "got 0"; the negative control passes either way; the four repaired call sites fail while six siblings in those two files pass either way.

FOUND IN REVIEW AND FIXED — THE SAME DEFECT ONE COORDINATE OVER. The first patch keyed on (op, functor, span). A SPAN DOES NOT IDENTIFY A CALL: `simp_rewrite::substitute_to_occurrence` builds every node of a `[simp]` RHS from the single redex occurrence and `synthesized_expr` inherits its span, so `rule both(?a, ?b) <=> and(eq(?a, ?b), eq(?b, ?a)) [simp]` puts two classified `eq` applies at one (op, functor, span). DRIVEN: the fixture tripped the guard, naming drive / PartialEq.eq / span 277..287 — in a debug build a loader panic on a legal program, in release a silently dropped rewrite. `nth_at_span` numbers each call within its (op, functor, span) group, which makes the key injective BY CONSTRUCTION; the distinctness assert was then removed as a near-tautology. Pinned by `a_simp_expansion_with_two_calls_is_two_entries`, whose own back-out (stamp every entry 0) fails at "got 1" while the other four arms pass — two axes, two back-outs.

COST, paired min-of-3 with the rebuild confirmed in each arm (eval_tests, 118 tests): 6.98s -> 7.55s, +8%. That is the ~24 extra `record_apply_*` calls per stdlib load — the rewrites the old key was dropping, several building a projection dictionary — i.e. work that was being skipped, not overhead added around it. The removed distinctness assert accounted for a further 2.5% (7.74s -> 7.55s), a second HashMap over the same ~47 entries re-deriving what the stamping pass already knew. Recorded at `stamp_nth_at_span`.

Two other review findings fixed: `record_apply_rewrite`'s doc still claimed "the post-typing rewrite pass uses that map to substitute the rewritten term into operation bodies bottom-up" (no such pass exists, and it contradicted the field's own doc); `defer_dict_param_name`'s doc promised a single-entry-channel check its body did not perform (the tail check is now real).

Full workspace green via rustland/scripts/test.sh: 35 binaries, 5280 tests, 0 failures.

