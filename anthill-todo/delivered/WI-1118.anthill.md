```anthill
fact WorkItem(id: "WI-1118", created: "2026-08-16T14:50:07Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-17T13:55:36Z"))

fact Tag(workitem: "WI-1118", name: "wi437")

fact Feedback(workitem: "WI-1118", author: "user", at: "2026-08-17T10:21:33Z")

fact Feedback(workitem: "WI-1118", author: "user", at: "2026-08-17T10:25:11Z")

fact Feedback(workitem: "WI-1118", author: "user", at: "2026-08-17T13:30:42Z")

fact Feedback(workitem: "WI-1118", author: "user", at: "2026-08-17T13:54:14Z")
```

## description

anthill-todo backend, INCREMENT 6 of WI-437: MIGRATE. Design: backend-github-coordination.md §11; §14 row 6. Ships this repository's own tracker moving to the coordinated backend -- `anthill-todo migrate --to github-coordinated`, RESUMABLE and IDEMPOTENT, because a migration that files hundreds of issues will be interrupted and must be safe to re-run. Two halves: (1) the on-disk move -- explode workitems.anthill into the §4 state directories, one file per item carrying that item's WorkItem, Feedback and Tag facts together; (2) the FULL BACKFILL -- every item gets a mirror issue, INCLUDING the terminal ones, which are created and then closed, so that open-versus-closed on GitHub reflects the whole tracker rather than only its live tail. A partial backfill would make the issue list a misleading projection of the tracker, which is the one thing the mirror must not be. Existing ids are preserved: they are already allocated, and the backfilled issues record rather than assign them -- so the §6.1 allocator's registry must be seeded consistently with what backfill created (an issue titled `WI-<n>:` for an existing item is a RECORD of an allocation, and reads identically to a stake). Verify the result with increment 5's `sync --check`.

## Feedback

### 2026-08-17T10:21:33Z — user

EDGE ON WI-1117 REMOVED 2026-08-17 -- MIGRATION NO LONGER NEEDS THE MIRROR AT ALL, and this moves the split much closer.

§11's four steps are: (1) explode workitems.anthill into one file per item, (2) create one mirror entry per item, (3) rewrite the ExtentBinding store term and add the Mirror fact, (4) stamp StoreFormat(version: 2). Steps 1, 3 and 4 are PURELY LOCAL. Only step 2 needed a forge, and it needed one because the forge ALLOCATED IDS -- an unmirrored item had no permanent id, so mirroring and migrating were inseparable. With ContentHash (WI-1121) that tie is gone: ids are minted locally, and creating mirror entries is just 'export' (WI-1117), run separately and only if the project wants a mirror. §11 step 2 is now marked optional.

SO MIGRATION IS A LOCAL REWRITE: no network, no forge, no GitHub secondary rate limits, no ~1110 paced issue creations, and nothing to resume across an API. That is a much smaller and much safer commit than the one this ticket was filed for.

WHAT IT STILL WAITS ON, and why:
  WI-1120 (format) -- HARD. §14.1: the live tracker migrates EXACTLY ONCE. Splitting into plain .anthill files first and then landing the markdown format means rewriting every one of ~1112 files a second time: two full-tree diffs in history instead of one.
  WI-1121 (ids)   -- SOFT but wanted. Migration back-dates the created field from git history (§11's amendment), and WI-1121 is what adds that field. Doing it in one pass beats migrating and then backfilling 1112 separate files. NOTE that ids themselves do NOT force the ordering: legacy WI-NNN ids are GRANDFATHERED and never renumbered, so WI-1121 renames no existing file.

WHAT IT NO LONGER WAITS ON: WI-1115 (forge carrier) and WI-1117 (export/import), which are now genuinely optional side-work rather than steps on the path to moving this repo's tracker.

### 2026-08-17T10:25:11Z — user

RESEQUENCED 2026-08-17: THIS GOES FIRST NOW, and both blocking edges are removed. It is §14 row 2a, immediately after WI-1114, not row 6.

WHY THE OLD ORDERING FELL. It waited on WI-1120 (format) and WI-1121 (ids) under §14.1's rule that the tracker migrates EXACTLY ONCE. That rule was written when migration meant CREATING ~1110 ISSUES OVER THE NETWORK, paced under GitHub's secondary rate limits and resumable across an API -- genuinely something you do not want to do twice. §11 step 2 is now OPTIONAL (mirroring is 'export', WI-1117, run separately if wanted at all), so migration is a PURELY LOCAL REWRITE, and repeating a local rewrite costs one mechanical commit that changes no data.

WHY FIRST IS BETTER, not merely allowed. The conflict-free multi-development that WI-437 exists for is NOT REAL until the tracker actually splits. WI-1114 is delivered, tested, and UNUSED; every day until this runs is another day of workitems.anthill conflicts on a 5.2MB single file. And the risk of the split is at its smallest now, because the tracker only grows.

NEITHER SUPPOSED PREREQUISITE IS TECHNICAL:
  FORMAT (WI-1120) -- splitting into plain fact files first means a second full-tree pass when the markdown format lands. TWO mechanical diffs instead of one, against getting the conflict benefit two tickets earlier. That is the whole cost.
  IDS (WI-1121) -- does not apply at all. Legacy WI-NNN ids are GRANDFATHERED and never renumbered, so migration renames nothing whatever new ids look like. The created field can be back-dated from git history in a later pass exactly as easily as in this one.

DE-RISKED BY MEASUREMENT (2026-08-17). The exploded tree was built from this repo's real tracker and run: all 1112 items across 1114 files parse and load in 1.38s against the single file's 1.30s -- about 6% for 1114 extra opens, with raw I/O only 42ms. Splitting does not make the tracker slower. Per-status directory counts came out delivered 912, open 123, pre_opened 21, verified 39, claimed 6, rejected 6, stale 3, proposal_rejected 1.

WHAT STILL APPLIES, and matters MORE now that this runs early: §14.1's rule that this is the ONE commit that moves the live tracker. Rehearse on a copy with -d <tmpdir>, verify by running the CLI against the result, and keep the commit ATOMIC AND ALONE so a single git revert restores workitems.anthill. It is a local rewrite, so that revertibility is the only thing between a bad migration and a lost tracker.

SCOPE NOW: §11 steps 1, 3 and 4 only -- explode the file, rewrite the ExtentBinding store term to ItemPerFileStore, stamp StoreFormat. No network, no forge, no rate limits, nothing to resume.

### 2026-08-17T13:30:42Z — user

PROGRESS 2026-08-17 -- THE TOOL IS BUILT AND TESTED; THE LIVE TRACKER HAS NOT MOVED YET. Committed separately from the migration on purpose (SS14.1: the commit that moves the live tracker is atomic and alone, so one `git revert` restores workitems.anthill).

WHAT LANDED. `anthill-todo migrate --to item-per-file` -- SS11 steps 1 and 3 -- host-served beside `fsck`, in run_anthill_bundle between building the store and handing it to the registry. It is served there for `fsck`'s reason plus one more: it needs a SECOND store, and the bundle cannot construct one (SS8.2.1, WI-1113 measured -- open_store is not expressible against today's spec). Rows are replayed through ItemPerFileStore::persist and ONE flush, so the routing rule (status -> directory, id -> filename, workitem -> its item's file) stays single-homed in the store rather than being restated by the migrator. The binding is a SPLICE over the fact's own span, not a regenerated file, so project.anthill keeps the prose explaining it; a project running on the DEFAULT binding gets one written instead.

THE SPELLING CHANGED WITH THE OPERATION. `--to github-coordinated` is REFUSED with its reason rather than aliased: it named this while migration also created ~1110 issues, and someone typing it today is asking for the mirror, not a local rewrite.

ONE anthill-core CHANGE, AND IT IS LOAD-BEARING HERE. ItemPerFileStore::flush pass 3 now places every PRIMARY row before asking any satellite where it goes -- a stable partition, not a sort. Without it the answer depended on the caller's buffer order: a Feedback persisted ahead of its own WorkItem in one flush failed with "this store holds no file for X", and in this repo's tracker most Feedback rows PRECEDE their item. Control stated at the test site: back the partition out and `a_satellite_buffered_before_its_item_lands_in_the_item_file` is the ONLY one of wi1114's 27 that fails.

SS11 STEP 4 IS WITHDRAWN, NOT FORGOTTEN, and the doc now carries the argument. StoreFormat versions the SCHEMA a row is written in; this changes no schema -- same four entities, same fields, redistributed across files. There is ONE current_store_format() for the binary, so bumping it to 2 makes every project still on IndexedFileStore -- supported, not deprecated (SS5.2) -- warn on every command and point at a `migrate` that would REFUSE the bump ("no data migrator for this bump yet"). Version 2 is better spent on WI-1120's markdown encoding, which really does change how a row is written. `the_data_format_stamp_is_not_bumped` is the control, so a quiet reversal fails loudly.

ORPHANS ARE CARRIED ACROSS, NOT REFUSED -- and my first cut had this WRONG. A satellite whose item has no row cannot go through the store (path_of refuses it, pinned by `a_satellite_naming_no_item_is_refused_at_flush`), so I made migration refuse too. That was backwards: the store deliberately tolerates on READ what it refuses on WRITE, because Feedback is `monotone` (proposal 053) and so CANNOT be retracted -- `delete` leaves an item's feedback behind BY DESIGN, and LayoutFault::OrphanRow is explicitly NON-blocking. Refusing would have locked out every tracker that ever deleted an item that had feedback. Migration now writes them to <root>/orphaned.anthill, names each on stdout, and leaves them to fsck.

MEASURED ON A COPY OF THIS REPO'S OWN TRACKER, repeatedly, via -d <tmpdir>, never the live tree: 1113 items -> 1114 files in 5.3s; every per-status directory count equals what `status` reports; `fsck` says layout ok; claim moves the file carrying its 6 rows (WorkItem + Tag + 4 Feedback); feedback/deliver/add/next/show all round-trip; a re-run reports "already declares" and exits 0.

TWO THINGS THE REHEARSAL FOUND IN THE TRACKER ITSELF, both now handled:
  (1) TWO ORPHANED Feedback ROWS on a WI-237 whose WorkItem had been deleted, with three depends_on edges (WI-240, WI-241, WI-260) still naming it -- invisible for months because every row shared one file. WI-237 RESTORED as a Delivered stub (description reconstructed from its own feedback, which is original and unedited), so the tracker migrates with no orphans at all.
  (2) `delete` LEAKS ROWS BY CONSTRUCTION -- WorkItemStore.forget retracts only the WorkItem. Filed as WI-1123, ticket-sized rather than inline because cascading to Feedback means revisiting WI-666's monotone declaration, not adding a loop.

/code-review AT high FOUND A DATA-LOSS BUG I HAD WRITTEN, and it is the most important line in this note. The "whole file or none" guard compared covered facts against LoadResult::fact_rule_ids -- which counts FACTS and is blind to every other item kind. A file holding a covered row beside a `rule` passed as a store file, had its rows re-emitted, and was then DELETED with the rule inside it: unrecoverable outside git, on a one-way rewrite. Now counts every item recursively plus namespace imports and description blocks (`item_census`), with `a_file_holding_a_rule_beside_its_rows_is_refused` as the control. Four more findings fixed: `migrate --help` never reached MIGRATE_USAGE (the dispatch keyed on --to); a "nothing was written" note that is false when flush fails mid-write-loop, whose debris then makes the retry misdiagnose itself as a parse failure; SS11 prose still saying "step 2 resumes"; and the new test file untracked by git.

MEASURED HAZARD FOR WHOEVER RUNS THE LIVE MIGRATION: an anthill-todo predating WI-1114 does NOT refuse a migrated tree. Reads work (the extent is RESIDENT -- every file is parsed at startup and kb.rules answers reads), but a WRITE silently recreates workitems.anthill and lands there, and nothing says so until you upgrade, at which point it shows up as a non-blocking MisfiledRow. So the live migration must be paired with `cargo install --path rustland/anthill-todo`. Measured against the binary on this machine's PATH (0.1.1, c392503f, 2026-08-15).

WHAT REMAINS ON THIS TICKET: run `migrate --to item-per-file` against anthill-todo/ and commit it ALONE, having first reinstalled the CLI.

SUITE: full workspace, 0 failures. wi1118_migrate_test adds 12 tests; wi1114's store suite goes 27 -> 28.

### 2026-08-17T13:54:14Z — user

DELIVERED 2026-08-17. The live tracker is migrated (commit above): 2559 rows out of one 5.26MB workitems.anthill into 1115 item files across eight state directories, plus store_format.anthill. status byte-identical before and after, every directory count equal to what status reports, fsck layout ok, and no orphaned.anthill because WI-237 was restored first. The CLI on PATH was reinstalled from 172b979b BEFORE the move, because a binary predating WI-1114 does not refuse a migrated tree -- it reads fine and then silently recreates workitems.anthill on the first write. This feedback row is itself the post-migration write test: it was persisted through ItemPerFileStore into claimed/WI-1118.anthill.

