## Attributes

- id: WI-828
- created: 2026-07-23T13:23:33Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-23T16:23:00Z

- acceptance: cargo-test

## Description

DECIDE + surface the σ-refusal of GENUINELY-UNCONSTRAINED requirement elements as a load-time diagnostic — today it lands as silent dict-None → load-clean-then-runtime crash (/code-review findings 4 and 6, both CONFIRMED by driving HEAD vs HEAD~1 in a worktree). MEASURED REGRESSIONS: (a) nullary/return-only element — 'operation make() -> MT' on Maker requires Desc[MT], called from Box requires Desc[CT]: HEAD~1 measured Ok(Int(11)) (the pre-WI-821 sole wildcard cover silently instantiated MT := CT — loose but ran); HEAD loads CLEAN then dies Err(Internal(DeferToRequirement: __req_desc not bound in caller frame)) — the σ-class of unbound MT can never equal the caller's rigid, Strategy 3 goes Ambiguous (2 instances), require_complete → None, ConcreteApplyWithin{dispatch_dict: None} → eval plain apply. The refusal itself is defensible (MT is truly unconstrained — the old forward guessed); the FAILURE MODE is the repo's named anti-pattern (loud-error principle: load-clean-then-eval-crash). (b) eta path — Applier.apply1(bump) with bump on a requires-carrying Maker, expected arrow Int64→Int64 pins nothing: HEAD~1 Ok(Int(4)); HEAD rejects the LOAD with the WI-420 'unsatisfiable requirement' TypeError — loud, but names neither the σ refusal nor the unconstrained element, so the migration story is a riddle. FIX SHAPE: in build_concrete_dispatch_dict, when require_complete yields None for a CROSS-SORT call with a non-empty chain AND a σ was in hand, emit a located load diagnostic naming the dep, the refused covering entry (or Ambiguous provider set), and the unconstrained element — instead of silently classifying dispatch_dict: None. CAUTION: the op-scoped (c)-row pins (op_scoped_single_conditional_level_dies_unbound, op_scoped_recursion_...) pin the SAME runtime-unbound outcome for dict-less flows that must NOT get a new load error under this ticket (their supply channel is the separate WI-822-sphere work) — gate the diagnostic on the σ-refusal path (sort-level chain, disambig was Some) so those pins stay green, and decide explicitly whether a program like (a) should be REJECTED (diagnostic) or given a defined fallback semantics (must NOT resurrect the blind wildcard forward). ACCEPTANCE: the two driven fixtures produce located load-time diagnostics (or the decided semantics) instead of runtime Internal / unexplained WI-420; op-scoped (c) pins unchanged; wi817 (15) + full workspace green.

## Changes

### 2026-07-23T16:23:00Z — feedback — user

DELIVERED. DECIDED: REJECT at load, no fallback semantics — any pick for a genuinely-unconstrained element is a guess (caller-param instantiation was exactly the pre-WI-821 unsound forward; an ambiguous-provider pick is the arbitrariness DispatchAmbiguous already refuses; deferring to eval is the anti-pattern this ticket kills). Both driven fixtures produce located load diagnostics via new TypeError::UnsatisfiableRequirement carrying a RequirementRefusal (dep, unconstrained elements, sigma-refused covers, construction outcome): (a) direct call — the WI-415 classify site surfaces Err instead of dispatch_dict: None; (b) eta — the WI-420 site raises the same variant (eta-flagged); the bare WI-420 message remains only for non-refusal classes. GATE per the CAUTION: refusal = sigma-refused coarse cover (Strategy 1 direct OR Strategy 2 slot-COMPOSED, mirroring S2's own one-level composition) OR S3 Ambiguous; bare NoMatch (WI-415/418 uncovered-abstract gap) and Cyclic (WI-827 sphere) keep the silent no-dict behavior; op-scoped chains never reach the build (parent-SORT chain gate) so the (c) pins are untouched by construction — wi817 15/15 green. MEASUREMENT DEVIATION: fixture (a)'s Strategy 3 does NOT go Ambiguous(2) on HEAD — the sigma-gated candidate match refuses an unbound element against ground heads outright, so construction terminates NoMatch, rendered via ResolutionResult::NoMatch's hint (its FIRST-EVER reader; the eagerly-formatted hint had no consumer since WI-821). The Ambiguous trigger leg is kept but unexercised by the driven fixtures. FIXTURE NOTES: (b) needs the sort-member-import spelling (import ns.Maker.{bump}) — bare/dotted cross-sort refs resolve nothing — and a caller carrying the covering requires (else HEAD~1 would also have refused). A nullary call's SPELLING decides constraint: '-> CT = Maker.make()' loads clean (return-type unification pins MT := CT into a sigma-AGREEING forward); the unconstrained shape is the call in spec-op-argument position. /code-review (4 agents) applied: kernel-language.md spec-sentence CLOBBER restored (my edit ate the Standalone-entity lead-in); explain walk extended to S2 slot-composed covers — the one spelling where load-clean-die-at-eval survived the first cut, now pinned by nested_cover_refusal_is_also_a_load_diagnostic; the duplicate S3 resolve on clean-load silent paths eliminated by threading build_dep_projection's own terminal ResolutionResult out (opt-in out-param; 2x cost gone, drift surface halved); refusal Boxed so the hot Result stays small; dep goal derived once via goal_from_requires_entry; diagnostic strings built only after the trigger decides. RESIDUAL MIRROR (doc-commented at explain_dep_refusal): the S1/S2 cover re-scan must track WI-826/827 walk changes or the diagnostic silently stops firing for that spelling. Spec updated (kernel-language.md requires-declaration section: requirement supply checked at load). 6 new tests; full workspace 3470 passed 0 failed.

