## Attributes

- id: WI-452
- created: 2026-06-13T22:37:21Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-14T04:34:50Z

- acceptance: cargo-test

- depends_on: WI-451

- tags: typing

## Description

Loader: a MARKED structured sort param registers a NON-RIGID BACKING VAR (design path-dependent-types.md §5.4). 'sort ?F' / 'sort [F]' (or '[F[T]]' in the enclosing list) registers the qualified 'CpsMonad.F' as a SortAlias -> Var::Global with its member sub-decls hung off it -- 'CpsMonad.F maps to ?F', EXACTLY as 'sort T = ?' registers 'CpsMonad.T' (so type_params_of_sort / sort_type_params_as_pairs now INCLUDE F with a backing var; today they OMIT the structured param -- the recorded gap). UNMARKED 'sort F {...}' stays a CONCRETE nested sort (NOT a param) -- the marked/unmarked split is load-bearing (distinguish param-variable from concrete nested sort). Def-site treatment: F skolemizes like other sort params so the spec's own bodies + coverage + the WI-270/WI-392 unconstrained-type-param check stay green and the wi383_hk decomposition stays RIGID. Acceptance: marked CpsMonad.F resolves to a Var via sort_type_params_as_pairs; unmarked concrete nested sort unchanged; suite green. Rust-only (load.rs).

