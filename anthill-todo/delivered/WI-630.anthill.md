## Attributes

- id: WI-630
- created: 2026-07-04T14:22:14Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-09T10:29:38Z

- acceptance: cargo-test

- tags: resolver, reflect

## Description

loader-metadata functor hygiene + namespace-level EntityInfo (WI-515 follow-up). WI-515 removed the ONE loader-emitted fact whose head was a user data functor (the entity schema fact), fixing phantom rows in var-quantified queries/constraints — but the bug CLASS remains open: resolve() candidate lookup is sort-blind (discrim tree keys on structure), so any future loader/stage0 emission that heads a user functor re-creates the pollution silently. Two scope items: (1) enforce the invariant loudly — a load-time debug check (or assert_fact-seam guard) that a loader-emitted metadata fact never tops a user entity/rule functor; evaluate the ticket's alternative (sort/domain-scoped guard queries) as the deeper fix. Related residual: emit_operation_equation asserts eq(op(?params), <body>) facts under sort Eq whose head functor eq is a user-queryable prelude relation — shielded today only because eq is BuiltinTag::SemEq (see WI-627 short-name trap for how that shield can slip). (2) everything-is-facts gap: EntityInfo facts are emitted only for SORT-BODY entities (load_sort_with_body); a namespace-level entity's only declaration record is now the Rust-side entity_field_types registry, invisible to anthill-level queries — emit EntityInfo for namespace-level entities too. NOTE the accepted WI-515 behavior changes this WI should NOT revert: ambiguous short-name entity resolution = deterministic min-qualified-name (KB::resolve_entity_functor); find_entity_schema serves value-in-type entities. ACCEPTANCE: invariant guard in place + a test that a same-functor metadata fact is rejected loudly; EntityInfo emitted for namespace-level entities + reflect query test; cargo-test green.

## Changes

### 2026-07-04T14:33:05Z — feedback — user

CAVEAT for scope item 2 (namespace-level EntityInfo), from the WI-515 dialogue: EntityInfo emission is coupled to entity_of SEMANTICS, not just accidentally placed in load_sort_with_body. typing.anthill:26-35 derives the parent sort from the entity's SCOPE ('rule is_entity_of(?A,?B) :- EntityInfo(name: ?A), scope(?A, ?B)'; same for entity_of) — for a namespace-level entity the scope is the NAMESPACE, so naively emitting EntityInfo for it would make entity_of(Account, ?sort) bind the namespace as a bogus parent; typing_test.rs entity_of_standalone_entity_has_no_parent pins that standalone entities have NO parent (0 solutions). Emitting namespace-level EntityInfo therefore requires either a sort-guard conjunct in the typing.anthill rules (e.g. SortInfo(name: ?B)) or a distinct fact shape for parentless entities. That coupling is WHY the gap exists: EntityInfo was built as the entity-subtyping record for sort-body entities; the schema fact accidentally covered the rest of fact space until WI-515 removed it.

