```anthill
fact WorkItem(id: "WI-514", created: "2026-06-19T04:01:48Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-246"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-19T08:16:55Z"))
```

## description

Guard occurrence-leaf path hardening (gated on WI-246 occurrence-goal resolution). Today the loader always builds Value::Term guards, so these are latent: (1) check_all_guards' Gated outcome routes through LoadError::Other which is NOT in is_load_blocking, so in the CLI a gated guard would surface as a non-fatal warning and load with the constraint unenforced (contradicts the stated 'surfaced loudly / load-blocking' intent); (2) lower_logical_query's pattern_query non-term branch is debug_assert!(false)+empty, i.e. silent in release; (3) assert_checked silently skips a gated guard at runtime with no diagnostic. When WI-246 makes the resolver evaluate Value::Node occurrence goals (so guards can carry occurrence leaves), make all three loud/blocking. Surfaced by WI-023 code review.

