```anthill
fact WorkItem(id: "WI-311", created: "2026-05-26T13:40:17Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-14T19:49:59Z"))

fact Feedback(workitem: "WI-311", author: "user", at: "2026-06-14T19:49:55Z")
```

## description

commonTypeExpr grammar unification (STEP 1 toward FULL type/term conversion). FULL-CONVERSION GOAL: the grammar should NOT distinguish type-vs-term — merge the dual type/term productions into shared rules so 'types are terms' holds at the CFG level, and the LOADER classifies (sort_ref vs denoted/value vs application) via SymbolKind. This WI is the first, validated step: the application merge (parameterized_type == instantiation_term -> one 'application' rule) plus the dotted-path/field_access unification. VALIDATED (measured 2026-05-26, prototype reverted, baseline green): the application name MUST be the identifier-only dotted path $.name, NOT field_access — field_access's _atom_term receiver CASCADES type/term conflicts ([_atom_term,application] -> [_atom_term,_type] -> [_atom_term,_effect_type] -> ...). With $.name it generates CLEAN and Eq[T=Int] / a.b.Stack[T=Int] / Modify[c] / Vector[Int,3] all parse. REMAINING WORK: the contained name-vs-field_access overlap (a.b matches both) breaks p.x(?x) and g(a.b). Fix per 'do not distinguish in grammar, distinguish in loader': make a.b ONE path node everywhere — route fn_term names, bare references, and ident-chain projections through the dotted path — and RESTRICT field_access to non-name receivers (?x.y, foo().y); the loader classifies projection-vs-qualified-vs-application. SCOPE: grammar (tree-sitter-anthill/grammar.js: application rule, fn_term/_atom_term/field_access restructure; full conversion later also merges tuple_type==tuple_literal) + converter (anthill-core/src/parse/convert.rs) + corpus rebaseline (~27 trees, mostly node renames) + cargo green. PAYOFF: grammar cleanliness ('types are terms' at CFG level), NOT new capability — the loader already classifies. PREREQ/RELATED: builds on committed _common_type_expr = _type | literals + TypeExpr::Denoted (commit fcb75e6, branch wi302-denoted-value-in-type; WI-302 denoted value-in-type).

## Feedback

### 2026-06-14T19:49:55Z — user

SUBSTANTIALLY DELIVERED 2026-06-14 (closed). The FUNCTIONAL half landed on main: parameterized_type + instantiation_term are merged into one 'application' rule (grammar.js:1184, tagged WI-311), base is the dotted $.name (Eq[T=Int64] / a.b.Stack[T=Int64] / Modify[c] / Vector[Int64,3] all parse), old node names gone, 'npx tree-sitter generate' is conflict-free. The name/field_access overlap for 'a.b' is absorbed by the CONVERTER, not eliminated at the CFG: explicit WI-311 classification (convert.rs:392 + receiver-walk ~885-910) splits a sort/namespace name (qualified-name flattening) from a value receiver (field projection). SMOKE-TEST (2026-06-14, main): the cited breakages are STALE — p.x(?x) parses as fn_term(name: field_access(p,x), ?x); g(a.b) as fn_term(g, field_access(a,b)); Color.red as field_access(Color,red) then classified — all ZERO ERROR nodes, full suite green (Color.red-style access is pervasive in stdlib, so the path is exercised). REMAINING (descoped): only the pure CFG-cleanliness goal — restrict field_access to non-name receivers (?x.y, foo().y) and route a.b through 'name' everywhere so the loader classifies. The WI itself states this is 'NOT new capability — the loader already classifies'; cost is restructuring the precedence-delicate field_access/fn_term/_atom_term + converter rewrite + corpus rebaseline for ZERO capability gain, and CLAUDE.md's representation note has since walked back the 'types are terms at CFG level' motivation. Converter-classifies is an acceptable terminal state. Closing.

