```anthill
fact WorkItem(id: "WI-680", created: "2026-07-11T07:19:42Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-11T07:19:58Z"))

fact Tag(workitem: "WI-680", name: "proof")

fact Feedback(workitem: "WI-680", author: "claude", at: "2026-07-11T07:01:00Z")

fact Feedback(workitem: "WI-680", author: "claude", at: "2026-07-11T07:18:15Z")
```

## description

smt-gen: lower conditional (ite/if) + boolean conditions in EXPRESSION position. anthill-smt-gen's translate_expr (anthill-smt-gen/src/lib.rs) handles arithmetic (add/sub/mul/div, abs/neg) and treats inequalities (gte/lte/gt/lt) ONLY as body-GOAL assertions (map_inequality_op in process_body_goal) — it has NO path for a conditional or a boolean-valued subterm in EXPRESSION position. CONFIRMED GAP: a hand-written 'rule f(?x,?r) :- ?r = ite(gte(?x,0), ?x, 0)' fails today with 'v0: unhandled arithmetic op ite'; the same blocks stdlib's own ite twins (sign/max/min: int64.anthill:35, ordered.anthill:49-50). FIX (downstream emitter only, NO extractor dependency): teach translate_expr to emit '(ite <cond> <then> <else>)' for (a) the Expr::If { condition, then_branch, else_branch } occurrence and (b) the anthill.prelude.Bool.ite(cond, then, else) functor application; add a translate_condition helper lowering a boolean-valued occurrence — the relational ops (map_inequality_op: gte/lte/gt/lt) + eq (is_eq_functor) + the Bool connectives and/or/not (anthill.prelude.Bool.*) — recursing on operands via translate_expr. Loud SmtGenError on any other condition shape (right layer, loud-error principle). INDEPENDENTLY MEANINGFUL: unblocks discharge of ANY ite-bodied defining rule (hand-written or synthesized); stdlib sign/max/min become dischargeable. Prerequisite substrate under WI-669 inc-1b (the body-derived synth seam refolds op_defining_equations arms into a nested Expr::If and needs this lowering). ACCEPTANCE: a hand-written ite-bodied rule (e.g. '?r = ite(gte(?x,0), ?x, 0)' with a clamp(x)>=0 violation-rule) discharges by z3 (unsat); cargo-test green + /code-review. NOTE: if/then/else does NOT parse in rule-body position (only operation bodies), so the hand-written demonstrator uses the ite(...) functor form; Expr::If coverage is exercised via WI-669's synth path + a unit test.

## Feedback

### 2026-07-11T07:01:00Z — claude

DELIVERED (smt-gen conditional lowering; anthill-smt-gen suite 75/0 green). translate_expr now emits (ite cond t e) for BOTH the Expr::If occurrence AND the Bool.ite(cond,then,else) functor; new translate_condition helper lowers a Bool-valued occurrence (map_inequality_op relationals + is_eq_functor eq + new map_bool_connective and/or/not, recursing), loud SmtGenError otherwise. New free helpers is_ite_op + map_bool_connective. Test wi680_ite_lowering_test (4): (ite (>= ..)) appears; and/not connectives lower; z3 unsat for TRUE clamp(x)>=0; z3 SAT (counterexample) for FALSE clamp(x)>0 (guards against vacuous always-unsat).

SCOPE CORRECTION (my filing overclaimed): 'stdlib sign/max/min become dischargeable' is NOT delivered by WI-680 alone. Those are <=> twins stored as EQUATIONS (functor unify/eq with max(?a,?b) as an arg), NOT as function-relations under the op's functor — so a call max(?a,?b,?r) hits 'unhandled body goal functor max' (rules_by_functor(max) has no such rule) and a max(?a,?b) expression hits 'unhandled arithmetic op max' (translate_expr doesn't inline op-calls). Reaching the new ite lowering from a <=> twin needs a SEPARATE <=>-twin->rule lowering path (out of scope; candidate follow-on WI). WI-680's verified reach = any RULE whose body binds a var to an ite/if expression: a hand-written 'rule f(?x,?r) :- ?r = ite(..)' twin, and (the point) the WI-669-synthesized defining rule (head functor = op).

### 2026-07-11T07:18:15Z — claude

/code-review (2 fresh general-purpose finders, xhigh): NO hard correctness/soundness bug. Sort discipline holds — translate_expr yields only Real, translate_condition only Bool; every unsupported shape (bare Bool var, Bool-in-Real slot, nullary ident) is a LOUD SmtGenError, never silent mis-sort. Two findings ADDRESSED: (1) test gap — 'or' had zero coverage, and/not only string-matched; added and/or/not z3-SEMANTICS tests, each a contradiction/tautology whose unsat verdict FLIPS under a miswiring (or<->and, not<->id). (2) latent-but-loud — the free-var scan chained assertions+conclusions but NOT body_smtlib, so a FunctionLike result bound to an ite over an otherwise-unused input (?w = ite(gte(?x,0),?x,0)) emitted an undeclared var_x -> z3 unknown-constant; FIXED by adding body_smtlib to the scan (result var is in local_bindings so never double-declared; free_vars render before body_smtlib) + a regression test (freevar -> sat). Suite now 8 WI-680 tests; anthill-smt-gen 79/0.

NOT fixed (pre-existing / out of scope, all LOUD not silent): (3) render_upper_bound_with never emits free_vars — an ite-over-free-var in OBLIGATION (upper-bound) mode would emit undeclared; WI-680 is wired/tested only through the satisfiability path; candidate follow-on. (4) Expr::Ref/Ident nullary used as a condition/branch emits a bare undeclared identifier (pre-existing translate_expr:917). (5) bare-Bool-var guards + flat n-ary and/or are loud-rejected (by design; the WI-669 refold emits only binary Bool.and/Bool.not). (6) bare short-name match ('ite'/'and'/'or'/'not') can reinterpret an unresolved same-named user op — consistent w/ existing map_arith_op style, and qualified_name_of returns FQN for RESOLVED syms so it only fires on unresolved bare names.

