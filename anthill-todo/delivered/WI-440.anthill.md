## Attributes

- id: WI-440
- created: 2026-06-11T14:52:52Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-11T15:50:33Z

- acceptance: cargo-test

- depends_on: WI-353

## Description

-Modify[binder] CHECKING direction: discharge a lacks-constraint on a callback param at the call site — the slice WI-353 explicitly left out ('pairs with WI-341 for the find-style -Modify(a) CHECKING direction, the lacks-constraint path, separate from this propagation slice'). Given a param typed '(x: Element) -> Bool @ -Modify[x]', a call site passing pred must verify pred's Modify row contains NO Modify on the place(s) fed through x — pred's row from its declared/derived effects (WI-352 flow facts + WI-353 keep_modify classifier output), absence via the WI-328 lacks machinery, label identity via the WI-341 occurrence-aware denoted comparator (all three DELIVERED — this WI is the wiring). PROBE EVIDENCE 2026-06-11 (supersedes the WI-353-era 'Modify[a] fails to LOAD/UnresolvedName' finding): '(x: Int64) -> Bool @ -Modify[x]' LOADS CLEAN and the arrow's own binder x RESOLVES in the effect annotation (differential probe: a bogus place name gets only a WARNING and loads — so today a declared absence is VACUOUS, silently unenforced). SCOPE ALSO: (a) loud-error: unresolved place name in an absence/effect-row position must be an ERROR not a warning (vacuous-constraint footgun, probe p4); (b) DECIDE row-openness of a bare-absence annotation '@ -Modify[x]' — open row {-Modify[x] | rho} (intended reading: any effects EXCEPT modifying x) vs closed; document in 045/spec. ACCEPTANCE: a pred that writes through its argument is REJECTED against a -Modify[x] callback param; a pred with unrelated effects (e.g. reads a Cell) is ACCEPTED under the open-row reading; typo'd place in absence position errors loudly; cargo-test green. Interacts with WI-342 (Value-carried types) but comparator-level deps are delivered.

## Changes

### 2026-06-11T15:50:32Z — feedback — user

Delivered 293b0bd. DECISIONS: (1) row-openness — absence-only '@ -Modify[x]' = CLOSED row + lacks atom, per 045's co-finite rule ('{ ?, -e }' is 'anything but e'); explicit open base required ('@ {?, -Modify[x]}' or '@ {Eff, -Modify[x]}'); implicit fresh tail tried + REVERTED (unnameable var -> a HOF APPLYING the callback surfaces an undeclared ?rho it cannot declare — probe p2). GAPS SURFACED, for WI-441: (a) the arrow annotation's 'Eff' and the op's 'effects Eff' clause lower to DIFFERENT VarIds (boundary check: 'expected declared [?Eff], got undeclared ?Eff'), and (b) a row-valued instantiation of a declared effect var is not decomposed at the boundary ('expected [Beep], got merge[present(Beep), empty_row]') — so a HOF that APPLIES an open-row callback and declares 'effects Eff' is blocked; both are the WI-441 result-row threading work. v1 CONSERVATISMS (documented in fn docs): lambda args skipped (eta op-refs only); open/absent-bearing ACTUAL rows skipped; arity-mismatch alignment skipped (generic WI-385 check owns it). ALTITUDE note (review): binder-normalizing arrow rows at CONSTRUCTION (positional places in the type itself) would subsume the side-validator, the unify path's binder-BLINDNESS (pair_present_labels/label_violates_lacks still compare places structurally), and gap (a) — candidate direction when WI-441/342 touch this; the wi440 tests pin against vacuous-pass regressions meanwhile.

