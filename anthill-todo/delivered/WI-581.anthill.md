## Attributes

- id: WI-581
- created: 2026-06-28T17:36:33Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-29T07:44:45Z

- acceptance: cargo-test

- tags: wi502

## Description

Guardrail (WI-502 sub-item): canonical head/goal functor symbols for the two rule-firing indexes. Both indexes key on the RAW functor Symbol read off the head TermView — the discrimination tree via DiscrimKey::Functor(s)/Ref(s) (the resolution-time matcher, query_view at resolve.rs:905) and rules_by_functor via TermView::head().functor_sym() (kb/mod.rs:995,1013; the enumeration index) — with NO by_qualified_name canonicalization, unlike sort_ops which normalizes via canonical_sort_sym on BOTH insert and lookup. Sound TODAY only because heads and goals carry the resolved canonical symbol by construction. RISK: WI-502 typed-rule-pattern machinery (the ?x: T binder grammar + loader, plus any head rekeying) and the typed-value producers (WI-578) will synthesize or rekey head and goal functor symbols; a non-canonical copy of the same name (a fresh intern, an intern_unique twin per WI-550, a scan-time copy, or a SHORT-name intern) would index or query under a divergent u32 and SILENTLY no-match (no fallback). DECIDED FIX: producer-side canonical-by-construction is PRIMARY — producers resolve the functor to the canonical resolved symbol (the by_qualified_name entry); because both the stored head and the query goal flow from producers, this covers BOTH index sides at once (insert-only canonicalization does NOT, since the goal/query path has no insert choke point). ENFORCE with a loud debug_assert at the shared insert choke point KnowledgeBase::push_value_head_entry, which feeds both rules_by_functor (~kb/mod.rs:1013) and the discrim insert (~:1019) off one head_functor: assert by_qualified_name[qualified_name_of(f)] == Some(f). CAVEAT: canonical_sort_sym (by-QN) only bridges same-FQN copies; a short-name or mis-qualified intern is NOT bridged (falls through to identity), so it MUST be fixed at the producer, never papered over by a canonicalize call. ACCEPTANCE: (1) typed-rule head/goal producers yield the canonical resolved symbol; (2) loud assert at push_value_head_entry catches a drifting producer; (3) caveat documented; (4) a general canonical_sym alias added since canonical_sort_sym now also serves rule head functors; (5) the stale comment at kb/mod.rs:1816 (unindex_functor, which calls rules_by_functor the SLD goal-resolution index) corrected — the resolution index is the discrim tree, rules_by_functor is the enumeration index; cargo-test green. DEFERRED (opt-in): symmetric canonical_sym normalization INSIDE the lookup (query_view top-edge + rules_by_functor()) mirroring sort_ops, only if a non-canonical goal path appears that cannot be fixed at the producer.

## Changes

### 2026-06-29T07:44:35Z — feedback — user

DELIVERED 2026-06-29 (commit 2b6e03b) with a DEVIATION from the prescribed assert, user-approved. The ticket prescribed a STRICT assert by_qualified_name[qn(f)]==Some(f) on the premise 'heads carry the resolved canonical symbol by construction'. A non-fatal probe at push_value_head_entry over the FULL workspace suite FALSIFIED that premise: 1096 DISTINCT head functors are legitimately bare short-name interns (by_qn=None) -- undeclared 'fact foo(...)' predicates (stdlib file_extension/needs_rebuild), datalog-style test facts (ancestor/parent/...), and generated _synth_N memo rules (execute.rs:377 uses intern, not define) -- and ZERO are a genuine same-FQN divergence (by_qn=Some(other)). The strict form would panic ~7317x. A bare ad-hoc predicate is SOUND without being canonical: the fact head and every query goal bare-intern the same string to the same Symbol via intern_map dedup, so the discrim tree matches. SHIPPED FORM (lenient): debug_assert_eq!(canonical_sym(f), f) -- fires only when a QN IS registered but maps to a DIFFERENT symbol, i.e. a same-FQN COPY (scan-time twin under a different u32), which is the realistic WI-502 typed-rule/typed-value producer drift and exactly the canonical_sym-bridgeable case the ticket's DECIDED FIX + CAVEAT center on. Passes the whole suite (2360/0). DOCUMENTED RESIDUAL GAP: a bare-head / FQN-goal asymmetry (head bare-interns a name the goal FQN-resolves) is UNDETECTABLE from the head symbol alone at this choke point -- a benign ad-hoc bare head and a 'should-have-resolved' bare head are indistinguishable, so no choke-point assert can separate them without false-positiving on all 1096. Acceptance: (1) producers enforced via the assert; (2) loud debug_assert added; (3) caveat documented (assert comment + canonical_sym doc); (4) canonical_sym general alias added, canonical_sort_sym delegates; (5) unindex_functor/rules_by_functor doc corrected (enumeration index vs discrim-tree resolution index; SLD drops eqs via is_equation filter). /code-review (2 finders): correctness []=clean (re-ran full suite + cargo check), doc-accuracy verified all 7 behavioral claims TRUE + 1 minor locator nit fixed. STILL DEFERRED (ticket's opt-in): symmetric canonical_sym normalization INSIDE the lookup (query_view top-edge + rules_by_functor()), only if a non-canonical goal path appears that cannot be fixed at the producer.

