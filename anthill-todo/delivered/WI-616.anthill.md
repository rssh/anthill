```anthill
fact WorkItem(id: "WI-616", created: "2026-07-02T16:04:03Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-615"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-03T17:32:02Z"))
```

## description

Proposal 051 Phase 2: make `=`/`eq` genuinely DISPATCH through the carrier's Eq instance (semantic equality), replacing the structural shortcut 049 deferred; add the first non-structural Eq carriers (Set/Map membership equality). `=`/`eq` = the Eq.eq spec op: concrete arg -> carrier-pinned impl (WI-350; Int->IntEq.eq stays an i64 compare so every current ground use keeps its answer), abstract arg -> requirement dictionary (WI-300 Tier B). GATED ON spec-op dispatch at SLD, which does NOT exist today: a rule-body `eq(1,1)` works only because it is builtin_eq; unbinding it needs the resolver to dispatch Int->IntEq.eq at resolution -- the same SLD->eval bridge that gates WI-300 Tier B (WI-483/487 line). Also completes the intent audit: split remaining `=`/`eq` sites into structural (->`===`, done in Phase 1) vs semantic (stay `=`, gain dispatch); `neq` pairs with semantic `=`. Design: docs/proposals/051-structural-vs-semantic-equality.md. NB the SLD->eval op-body dispatch bridge is a prerequisite and should be scoped as its own WI first.

