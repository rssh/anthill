```anthill
fact WorkItem(id: "WI-870", created: "2026-07-30T16:12:37Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-08-06T21:15:03Z"))

fact Tag(workitem: "WI-870", name: "modinst")

fact Feedback(workitem: "WI-870", author: "user", at: "2026-08-06T21:14:53Z")
```

## description

A NAMED REQUIREMENT SLOT BOUND IN A BRACKET VALUE IS VALIDATED AND THEN DROPPED — 058 §3.3's composition leg, DRIVEN for the first time at WI-858 and found unwired. §3.3 states the rule: pinning does not reach into the resolution tree, so steering a witness's own sub-goal is written by binding the witness's NAMED slot in the key's VALUE position — `fold[Monoid = ListM[O = MyEq]]` — and 058-implementation.md §7 recorded the leg as the one composition never measured. MEASURED at HEAD with the shipped `PairByFst` (whose element slots ARE named, `requires OA: Ordered[A]`, `requires OB: Ordered[B]`), two arms: (1) THE KEY IS CHECKED — `Ordered.compare[Ordered = PairByFst[NoSuchSlot = X]](…)` is refused, 'anthill.prelude.PairByFst has no type parameter named NoSuchSlot — it declares type parameter(s) A, B, OA, OB', so the value's bracket list is parsed and validated against the witness; (2) THE VALUE STEERS NOTHING — `[Ordered = PairByFst[OA = PairBySnd]]`, a NONSENSE binding (PairBySnd provides no Ordered[Int64] at all), LOADS CLEAN and computes the unbound answer (-1). A SILENT DROP, against the repo's loud-over-silent principle. Root cause located, not guessed: `selection_witness_sym` (kb/typing.rs) reads `sort_functor_of_view` — the BASE identifies the witness, with the comment already noting 'a witness may carry type arguments, §4.5' — and `seed_op_type_args` pushes one spec-keyed InstanceSelection from that base; the arguments reach the type-parameter half and never become a selection for the SUB-GOAL, whose resolution runs with `selected: &[]` by design (resolve_inner's `at_call_goal = stack.is_empty()` gate). WHY IT IS A DEFECT AND NOT A GAP: `TieRepair::SubGoal` PRINTS this exact spelling as the repair — 'give the conditional provider a NAMED requirement slot and bind it in the value position (f[Spec = W[Slot = Chosen]])' — so an author who follows the diagnostic gets the identical error back. MEASURED: with a rival `Ordered[Int64]` declared, `[Ordered = PairByFst[OA = Descending]]` produces the same SubGoal tie as the bare `[Ordered = PairByFst]` control. WORK: carry the bracket value's own slot bindings into the sub-goal resolution keyed by the SELECTED provider (the WI-857 locality rule's channel is the natural carrier — it already threads `local_provider` one level), validate each against the witness's declared slots the way `check_selection_bindings` validates the outer one, and refuse a binding whose value provides nothing at those bindings. ACCEPTANCE: the nonsense binding is REFUSED naming the slot and the non-providing value; a MEANINGFUL binding resolves the sub-goal tier-3 tie the diagnostic advertises; wi858_pair_orderings_test::a_named_slot_bound_in_a_bracket_value_steers_nothing flips to a positive assertion; wi857's locality pins unchanged; full workspace green via rustland/scripts/test.sh. REFERENCE: 058 §3.3 last paragraph, §5's `SortedSet.empty[…, O = ListOrd[OE = LexFst]]` line; design/058-implementation.md §7 final paragraph.

## Feedback

### 2026-08-06T21:14:53Z — user

DELIVERED. 058 §3.3 composition is wired: an InstanceSelection is now (spec, witness, slots), where slots is what the value bracket bound on the WITNESS own named requirement slots — recursively, so W[S = X[Inner = Y]] composes to any depth. resolve_inner takes a slot_pin for the sub-goal it is resolving; step 0 stays exclusive by construction (a spec key answers the goal the call made, a value slot binding answers a sub-goal of the provider it names). Keyed POSITIONALLY by dictionary-chain index, because 058 own example declares TWO slots of ONE spec whose goals are identical after substitution; the index has one owner (dict_chain_index_of_named_slot) which states the identity — declaration order = SortRequiresInfo fact order = provider_dict_chain prefix — and VERIFIES it, refusing rather than mis-pinning, because a wrong pin computes a wrong answer silently.

THE HALF THE TICKET DID NOT NAME, and 058 §5 writes it on its own second line: there are TWO producers of a selection, and the σ-read one (selections_from_slot_bindings, WI-844) took sort_functor_of and kept the BASE exactly as selection_witness_sym did. A named slot IS a type parameter (§3.4), so SortedSet.empty[T = …, O = ListOrd[OE = LexFst]]() puts the nested binding in the value TYPE and every later bracket-less call carries it as an ARGUMENT. Fixing only the bracket would have re-dropped the pin one call later. Both producers now read one function; backing that half out alone fails exactly one test.

TWO VALIDATION DECISIONS, each with an exclusive control. (1) Only a NAMED requirement slot becomes a selection — a witness plain parameters share the same bracket, so LexFst[A = Int64] binds a type and selects nothing. (2) §3.5 CHECK 3 IS NOT A SUB-SLOT BUSINESS: it refuses naming a CONCRETE provider because at a call site the argument value already directs dispatch, and a witness requirement slot has no value — it is a dictionary slot the typer resolves. Driven with the identical name in both positions: [Ordered = Pair] is refused, [Ordered = LexFst[OA = Pair]] runs; applying check 3 to the sub-slot fails that arm and nothing else. Check 1 IS shared (check_witness_provides_spec, one owner two callers), so the split is structural, not textual; the binding-precise half stays with the resolution and is worded in the AUTHOR vocabulary (the call bound slot OA of W to X) because the outer wording would send them to the bracket KEY.

A GUARD BUILT THEN DELETED, argument kept at slot_pin_at: a binding for a slot this dispatch does not answer (WI-869 Unavailable) is unreachable — a binder is minted only for a sort-level requires, and a sort-level slot conditions EVERY provision, so strict_mask answers true for exactly the slots an author can write. Untestable check logic replaced by a stated invariant.

MEASURED. Baseline: with three Ordered[Int64] suppliers, [Ordered = LexFst] and [Ordered = LexFst[OA = Descending, OB = Ascending]] produced the IDENTICAL SubGoal tie — the diagnostic advertising the repair that did not work. Back-out matrix, one edit at a time: resolve_inner routing → 7 fail; bracket producer → the same 7; σ-read nested read → 1; slot-flavoured wording → 2; check 3 applied to sub-slots → 1. Three arms pass either way by design and say so at their site. wi858 a_named_slot_bound_in_a_bracket_value_steers_nothing flipped to a positive assertion (renamed …steers_its_sub_goal). Full workspace green: 29 binaries, 4276 tests, 0 failed. Corpus-inert — slots is empty for every bare witness. Docs: proposal §3.3/§5 + Status, design/058-implementation.md §7 and new §18.

