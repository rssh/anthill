## Attributes

- id: WI-1001
- created: 2026-08-04T20:13:00Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-27T10:31:14Z

- acceptance: cargo-test

- depends_on: WI-1000, WI-980, WI-895

## Description

Replace 059 R3's BLANKET rule ban in a secondary entry with the NARROW rule the
proposal actually intends. Spec: docs/proposals/059-secondary-entries.md, "The rule a
secondary entry MAY declare, and the two conditions that make it sound".

A rule is a definition, and a definition of something NEW displaces nothing. So a
secondary entry may declare a rule exactly when BOTH hold:
  (1) ITS HEAD INTRODUCES — the head resolves to nothing in the FINISHED program, so no
      existing goal can be about it; and
  (2) ONE ENTRY OWNS THE PREDICATE — every clause of that head is written in that same
      entry, keyed as 059's Definitions individuate one: the main entry, or one file's
      text at that address.

EACH CONDITION ANSWERS A MEASURED FAILURE of the naive "a fresh head is harmless" reading:
  * WITHOUT (2), two entries silently compose one predicate. Two secondary entries IN
    DIFFERENT FILES that each introduce `freshp` do not collide — both clauses join it,
    `freshp(1)` and `freshp(2)` both answering. Neither captured a pre-existing name, so
    R4 is silent, and the predicate ends up assembled by two parties that never agreed on
    it. R4 CANNOT express this, because a predicate legitimately has many clauses; it is
    a rule about where they may be WRITTEN. Two blocks in ONE file are one entry and
    compose freely — deliberately: that is one author writing two paragraphs.
  * WITHOUT (1), the head is not really new. A main entry carrying
    `rule q(0) :- not freshp(1)` answers q(0) once; add `namespace Rec { rule freshp(1) }`
    and it answers ZERO times, though the head was as fresh as heads get — the symbol
    lands at X.freshp, exactly where the main entry would have put it. The REFERENCE
    existed before the DEFINITION did.

WHY IT CANNOT LAND YET — the two gates, both already ticketed:
  * WI-980 (R6). Condition (1) is UNDECIDABLE while text order decides whether a head
    introduces. Measured today, `rule p(1)` then `sort Rec { rule p(2) }` joins both
    clauses to one `p`, while the same two swapped make TWO predicates. R6 says the
    binding is computed against the FINISHED program; until it is, (1) has no stable
    answer to compute.
  * WI-895. Condition (1) is UNSOUND while a body may reference what resolves to nothing.
    Measured, a rule body and a constraint both LOAD while naming a predicate that
    resolves to nothing (an operation body naming one is already refused, `unknown
    functor`). A name that resolves to nothing must stop being referenceable before
    "resolves to nothing" can mean "no one refers to it".

NO FURTHER DESIGN IS NEEDED. Both conditions are decidable at scan time once the gates
close — (2) by grouping a predicate's clauses on (address, SourceId) with the main entry
as its own group, which is the key the declaration ledger already carries.

TWO SHAPES ARE EXCLUDED BY (1) RATHER THAN BY A CLAUSE OF THEIR OWN, which is the sign
the condition is the right one: a DOT RULE and an OPERATOR RULE carry the desugar's
functor (dot_apply, add), and a desugared conclusion introduces nothing — so neither is
ever fresh, and the `[simp]`-fires-in-the-typer hazard cannot arise through them.

ONE HONEST LIMIT, do not let this ticket be read as closing it: this does NOT close the
underlying hazard. A rule extending a predicate declared elsewhere is NON-MONOTONE
wherever it is written — an ordinary nested namespace does the same, and so does the
sort's own body. R3 closes the route 059 opens; the general case belongs with the module
question.

## Changes

### 2026-08-27T10:31:19Z — feedback — user

DELIVERED. 059 R3's blanket rule ban is now the narrow rule: a secondary entry may
declare a rule exactly when (1) its head INTRODUCES and (2) ONE ENTRY OWNS THE PREDICATE.

THE TICKET WAS WRITTEN BEFORE 061 AND HAD AGED IN ONE LOAD-BEARING PLACE. It says
condition (1) is blocked on WI-980's R6 and names the Ownership fixpoint as the
mechanism. That fixpoint is GONE (845G7) and a predicate is now DECLARED (061), so:
 * (1) is the phase-2 ladder answer `denotes`, read against the finished name table
   before any head is minted. No fixpoint is consulted.
 * (2)'s CROSS-FILE half is already enforced by 061's `PredicateHeadsSpanFiles`. What
   remains uniquely (2)'s is the pair 061 cannot see: a clause in the main entry and a
   clause in a secondary entry IN ONE FILE.
 * 061's DECLARATION form forced an arm the ticket does not mention. A body-less
   `rule p(?x)` mints in pass 1, so its head DENOTES — to the symbol IT created — and
   condition (1) alone would report it as "joins a predicate that already exists", the
   opposite of what it does. It is refused with its own reason: a declaration exists to
   let clauses span files and scopes, which is the spread (2) forbids; inside one entry
   the clauses auto-declare the predicate themselves.

WHERE THE VERDICT IS TAKEN. Sub-pass 1b DEFERS every rule (it is the only pass that
knows a rule is in a secondary entry — an ADDRESS question) and sub-pass 3 judges, right
after `denotes` and before the collision / multi-file reports so a rule R3 will not admit
is reported as itself. One message per PREDICATE, at the earliest member.

TWO REPORTS WOULD HAVE PRESCRIBED CONTRADICTORY REPAIRS. 061's multi-file message says
"declare it once, in the scope that owns it" — the declaration R3 refuses in an entry.
MEASURED before the suppression: two entries in two files each introducing `freshp`
reported THREE errors. Now one, R3's, and the 061 one is suppressed per (scope, name).

/code-review AT `high` FOUND ONE REAL HOLE, AND IT WAS 059'S OWN WORKED HARM.
A `fact` IS a clause — since 061 `fact H` is `rule H :- true` — but it is not a
`RuleHeadSite`: the head scan reads `Item::Rule`/`Item::RuleBlock` only, deliberately (a
fact must not MINT, §5.3 "a fact head is unscoped", and must not enter 061's multi-file
report). So condition (2) censused rule heads and missed the fact. DRIVEN, not predicted:
    sort Rec { entity rec(n: Int64); fact freshp(2); rule q(0) :- not freshp(1) }
    namespace Rec { rule freshp(1) :- true }
LOADED CLEAN, `Rec.freshp` held TWO clauses, and `q(0)` answered 1 WITHOUT the entry and
0 WITH it — a statement that was true made false through the one route R3 exists to
close, while the `rule` spelling of the identical main-entry clause was refused one
keyword away. Closed by a SECOND census (`FactHeadSite`), collected on the same walk into
its own list so it reaches condition (2) and nothing else. Scoped, because a fact head is
unscoped: an ENCLOSING namespace's fact does not join (measured — `Rec.freshp` keeps one
clause and `freshp(2)` answers 0) and is not counted; a fact in ANOTHER entry is named as
that entry rather than misattributed to the main entry.

A MEASUREMENT OVERTURNED MY OWN REASONING MID-IMPLEMENTATION. I added a `!denotes` filter
to condition (2)'s site set — "a head that resolved elsewhere is a clause of what it
resolved to" — which is wrong: `load_rule` remaps the head in its SCOPE at LOAD time, by
which point another file's head has minted `(scope, name)` there, and a local symbol beats
an import. Driven on three files: the main entry's clause lands under `Rec.p` beside the
entry's (`Rec.p` 2, `lib.p` 1) though its scan answer was `lib.p`. That divergence is
WI-20260820-JR7BB. Filter reverted; the measurement is at the site and pinned by
`condition_2_counts_a_head_by_where_its_clause_lands`, whose three-file shape is required
(in ONE file the entry sees the main entry's import too — imports are file-local, WI-995 —
both heads denote, and condition (1) refuses instead).

TWO BACK-OUTS, BOTH APPLIED AND RUN, present-but-wrong rather than deleted:
 * A — the narrow rule (refuse beside the push in `defer_rule`): 15 rows fail. Ten
   because an admitted program stops loading, three on an error COUNT. FOUR PASS EITHER
   WAY and all four are REFUSAL rows — with the judge still running, a refused program
   keeps its sentence; what they measure is that the verdict names WHICH condition
   failed, which the single-reason WI-1000 ban could not. My predicted list was wrong
   here and is corrected in the file.
 * B — the fact census alone: EXACTLY 2 rows fail, both the census's own. Nothing else
   moves.

WHAT STAYS REFUSED, each with a reason that is now TRUE rather than borrowed:
 * A rule inside a `provides … language L … end` block, whatever its head:
   `load_provides_block` sets the domain to the block's SPEC, so its clauses are another
   type's predicate by construction — (2) can never hold — and neither condition is even
   askable (no pass descends into a block, so such a rule is never a head site).
 * A `fact`. Its old first clause read "facts are rules, so the ban reaches them", and
   there is no longer a ban to reach; the whole weight now sits on the DISCRIMINATOR
   (`maybe_emit_fact_provides_info` recognises a claim by SHAPE). Nothing is unwritable:
   the rule spelling of the same assertion is admitted, and the message says so.
 * A `constraint`. Same stale reason, replaced: a constraint has NO head, so it can never
   meet the one condition that admits a rule.

TESTS: `wi1001_secondary_entry_rule_test`, 17 rows, every one driving a goal.
`wi1000`'s refused matrix lost its two rule rows (eight now, not ten) and its
ordinary-namespace control moved from a FRESH head — legal at both addresses now, so it
no longer separates them — to a JOINING one, which does. `wi980`'s
`a_rule_in_a_secondary_entry_is_still_refused` became `..._is_order_free`: the spelling
WI-980 originally named finally runs, both orders x both arms, plus a row pinning that
061's "declare it" remedy is unavailable inside an entry. WI-980's header no longer calls
it a seventh channel — a `namespace Rec` at `sort Rec`'s address is the SORT-BODY
channel's edge with different text.

DOCS: kernel-language.md §5.1's table (the rule row, the 061-declaration row, the
provides-block row, and separate fact / constraint rows) plus a §5.3 cross-reference at
the auto-declaration rule; 059's R3 refused list, its condition-(2) paragraph, and the
"what an implementation does today" paragraph, which stated a blanket ban that no longer
exists.

NOT CLOSED, and stated rather than credited to a neighbour:
 * WI-20260820-JR7BB is live and this rule now rests on it — condition (2) counts a head
   by where its clause LANDS, which is the load-time remap, not the scan decision. If
   JR7BB is fixed so the two agree, re-read condition (2)'s site set: the `!denotes`
   filter I removed becomes correct at that point.
 * An imported AUTO-declared predicate is invisible to condition (1): a selective import
   of a rule-introduced name resolves in sub-pass 4, after the ladder answers this
   condition reads. Measured, identical in an ordinary namespace, so neither caused nor
   fixable here — and 061's declared spelling, which is what an author is told to write,
   IS seen.
 * scaland is untouched and needs nothing: it has no R3 classifier and WI-980/059 R6 is
   unported there (WI-20260821-SBZ2A).
 * The underlying hazard is unchanged, as the ticket insists: a rule extending a
   predicate declared elsewhere is non-monotone wherever it is written. R3 closes the
   route 059 opens; the general case belongs with the module question.

