## Attributes

- id: WI-297
- created: 2026-05-23T16:31:09Z

- status: Delivered
- status_agent: claude
- status_at: 2026-05-23T21:28:03Z

- acceptance: cargo-test

## Description

Implement occurrence reflection builtins occurrence_term / occurrence_span / occurrence_owner / sub_occurrences. PROBLEM: these are stubs returning BuiltinResult::Delay (rustland/anthill-core/src/kb/resolve.rs:1518, comment: Occurrence builtins stubs returning Delay until Phase 2). The self-hosted typing relation and any reflection rule depend on occurrence_term to destructure an expression occurrence, e.g. synth(?, ?e, Int, nil) :- occurrence_term(?e, int_lit(value: ?)); with the stub, every reflection rule first goal delays and nothing resolves. TASK: implement at least occurrence_term(occ, term): given a bound occurrence (Value::Node / occurrence handle), bind or unify the second arg with its term structure; building block exists, occurrence_to_term (node_occurrence.rs:762) converts an occurrence to a TermId. Decide the mode (functional: bind term from occ; or relational: match a pattern against the occurrence structure). Implement occurrence_span (-> SourceSpan), occurrence_owner (-> Symbol), sub_occurrences (-> List[Occurrence]) similarly. Delay on unbound occurrence. ACCEPTANCE: synth on a literal occurrence resolves to its sort (int_lit -> sort_ref(Int)) once the opener fix is in; a builtin test that occurrence_term binds the term of a known occurrence; cargo-test green. SCOPE: the Phase 2 the stub references; precondition for WI-079 (runnable typing spec); Rust-only.

