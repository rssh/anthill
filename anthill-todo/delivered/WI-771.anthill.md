## Attributes

- id: WI-771
- created: 2026-07-19T10:30:00Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-22T07:53:07Z

- acceptance: cargo-test

- depends_on: WI-773

- tags: extent-seam

## Description

cpp-gen sibling realization readers head-match with no bodied-rule refusal: WI-770 closed the guard-skip trap for the five functors routed through query_realization_facts, but three sibling readers in the SAME crate read realization heads via kb.rules_by_functor + kb.rule_head with NO is_fact guard: CarrierTable::from_kb reading anthill.realization.Implementation (rustland/anthill-cpp-gen/src/lib.rs:129), OpImplTable::from_kb reading anthill.realization.OperationImpl (lib.rs:254), and generated_targets reading anthill.realization.Generated (lib.rs:1542). A BODIED rule for any of these is silently head-matched with its guard skipped: a guarded Implementation binds carrier/host_type/binding-key unconditionally (wrong C++ types AND includes emitted), a guarded OperationImpl enrolls the operation body for emission unconditionally, and a guarded Generated emits the artifact / selects the profile overlay unconditionally (generated_targets feeds CLI profile selection at anthill-cli/src/main.rs:917 and the target-declaration check at main.rs:1003). Surfaced by the WI-770 review -- three independent finder angles converged on the same three sites; all are the exact silent-wrong-answer class WI-770's assert now refuses one screen away, which users will read as 'guards work here'. FIX DIRECTION: same refusal as WI-770 (assert kb.is_fact naming the rule via render_rule), or route through the WI-773 facts-only accessor if that lands first. ACCEPTANCE: a bodied Implementation / OperationImpl / Generated rule in a test KB is refused loudly, never silently head-matched; cargo-test green via scripts/test.sh.

## Changes

### 2026-07-21T07:21:55Z — feedback — user

REBUILD on the WI-773 accessor + 057 Store API, not standalone is_fact asserts. ALL cpp-gen facts-only realization readers become accessor clients (values-first, Refuse policy): CarrierTable::from_kb (Implementation, lib.rs:129), OpImplTable::from_kb (OperationImpl, :254), generated_targets (Generated, :1542) — the three unguarded siblings — PLUS query_realization_facts (:3789, already is_fact-guarded WI-770) and the op_info readers (:1330/:1915) all collapse to kb.read_facts(functor, selection, Refuse): the placeholder-var pattern build, query_view, is_fact assert, and rule_head extraction ALL disappear into the accessor. Refusal moves from assert! (abort exit 101, no span) to Err rendered through CppCodegenError (error: {msg} exit 1). BOUNDARY vs WI-774: the EffectMapping/LanguageMapping candidate readers (supported_effects ~4100) want the RESOLVE policy (guards honored on the deciding path), NOT Refuse — those stay WI-774's call and pass policy=Resolve to the SAME accessor; WI-771 migrates only the facts-only readers. STORE-API PAYOFF (why route through the accessor, not a local guard): 057 reads over resident+MOUNTED extents, so a project that mounts anthill.realization.TypeMapping on a shared host-mapping store is read transparently — cpp-gen unchanged — and cpp-gen's ground selection (anthill_type) becomes the LookupQuery.bound pushdown so the store indexes instead of scans. DEPENDS WI-773 (resident path suffices; no need to wait on 057 items 0-6). Subsumes the original three-is_fact-guards direction.

### 2026-07-21T07:24:03Z — feedback — user

CORRECTION (design review): WI-771 depends on the COMPLETE WI-773 accessor — which lands with 057 item 1 (ExtentSource read seam + discrim mounts + in-memory reference source), NOT a resident-only interim. No partial interface: cpp-gen migrates ONCE, onto the final resident+mounted shape.

