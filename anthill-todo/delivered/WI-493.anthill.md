```anthill
fact WorkItem(id: "WI-493", created: "2026-06-16T15:25:46Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-16T19:33:30Z"))

fact Tag(workitem: "WI-493", name: "typing")

fact Feedback(workitem: "WI-493", author: "user", at: "2026-06-16T19:12:08Z")

fact Feedback(workitem: "WI-493", author: "user", at: "2026-06-16T19:33:30Z")
```

## description

Canonicalize effect-row shape at the BINDING site, not at decompose. CONTEXT (WI-278): a row's tail can be bound to either a BARE EffectExpression (from a `provides Iterable[E = {}]` fact) or a WRAPPED `effects_rows(empty_row)` (from lambda-effect inference), so a `{E, EffP}` merge mixes both shapes. WI-278 patched the CONSUMER (decompose_effect_row in typing.rs now unwraps a wrapped tail mid-walk) — a 'make it parse' tolerance rather than 'make it well-formed'. The inverse fn effects_rows_to_flat_list already tolerates the same split, and the unify/subtype multi-tail arms special-case it; each new row consumer is a fresh place the mismatch can silently mis-decompose. Deeper fix: route the lambda-effect-inferred tail through the existing canonicalization (build_canonical_effects_rows / make_effects_rows_* / make_row_shaped ~typing.rs:9962) so every binding site emits ONE canonical shape; then delete the consumer tolerance arms. Acceptance: make the lambda-effect bind site emit the same shape as the provides-fact path; remove the WI-278 mid-walk unwrap arm in decompose_effect_row and confirm xs.map(f).filter(p) still type-checks + evaluates; cargo-test green.

## Feedback

### 2026-06-16T19:12:08Z — user

INVESTIGATION (claude, not delivered — reverted to green, stop-and-ask). The ticket's premise of a SINGLE 'lambda-effect bind site' is inaccurate. Empirically (removed the WI-278 mid-walk unwrap arm in decompose_effect_row, ran wi278/wi492): the wrapped tail is 'open[tail = {empty_row}]' i.e. a row-tail var bound to a WRAPPED effects_rows(empty_row) instead of bare empty_row. ROOT: effect-row TYPE-ARGS (Stream[E = {...}]) are uniformly stored WRAPPED as effects_rows(...) by convention, and they get bound onto row-tail vars across MULTIPLE independent paths, NOT one: (1) unify_parameterized_with_sort_ref (WI-375 arm, binds the whole effects_rows term/Node onto the spec alias var); (2) carrier_param_receiver_for_values / WI-393 grounding (binds the ground provider row 'concrete' onto the op's Eff param); (3) the provider-admissibility/subtype path (each combinator's source-effect param binds to the PREVIOUS combinator's whole provided E row → the wrap NESTS: filter's ES ← map's {ES,EF}). I fixed (1)+(2) with a new effects_rows_inner_term unwrap helper; the error just moved deeper (nested open[tail={merge[...]}]) because (3) still wraps. So removing the tolerance requires canonicalizing effect-row type-arg bindings to BARE at EVERY var-binding site (unify + subtype + provider admissibility) — a representation-level change with cascade risk into delivered effect WIs (wi357/375/424/441/492). The WI-278 decompose-site unwrap is actually the single CHOKE POINT where all row tails are walked, so it is arguably the correct place. Needs a human design decision (see options in the stop-and-ask).

### 2026-06-16T19:33:30Z — user

DELIVERED 2026-06-16 (3ebebbc) — CONSOLIDATE option (user AskUserQuestion decision; the ticket's single-bind-site premise was inaccurate, see prior investigation feedback). Extracted ONE shared helper effects_rows_inner<V: TermView>(kb,&v)->Option<Value> (Some(inner) iff the value is the EffectsRows wrapper, matched by QUALIFIED symbol) and routed all four open-coded unwrap sites through it: decompose_effect_row top-level + WI-278 mid-walk arm, row_inner_value (the multi-tail arm), and effects_rows_to_flat_list. BEHAVIOR-PRESERVING: each site does the same unwrap; decompose's malformed-wrapper hard-rejects survive via fall-through (top-level -> bare/tail fallbacks -> empty; mid-walk -> the  unknown-functor ); effects_rows_to_flat_list's unreachable malformed case now flows to list_to_vec instead of a debug_assert (make_effects_rows_* always sets the child, so unreachable). The WI-278 tolerance STAYS (kept, not removed) — now single-sourced so a new row consumer can't drift. /code-review (xhigh) caught + fixed a doc-comment misattribution (the new helper was inserted between decompose's doc and its fn, orphaning decompose's doc); no correctness bugs. Tests: wi493_effect_row_tolerance_test (map.filter chain type-checks + evaluates = the bare+wrapped mixed row); wi278/wi492 broader regression. Full workspace green 56 binaries. NOT done (deliberate, user-decided): the full producer-side canonicalization (bind effect-row type-args bare everywhere + delete the tolerance) — a representation-level change with cascade risk into wi357/375/424/441/492; left as the decompose choke point.

