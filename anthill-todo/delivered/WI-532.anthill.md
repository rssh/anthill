```anthill
fact WorkItem(id: "WI-532", created: "2026-06-21T06:33:36Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-21T07:04:45Z"))
```

## description

Float infinity as a host-supplied term-level constant. anthill currently has NO way to WRITE IEEE +infinity as a Float — it appears only in float.anthill comments (floor/ceil docs). Add 'const infinity: Float' (bodyless, host-supplied) to the prelude (float.anthill), backed by a registered reflect builtin in anthill-stl returning Value::Float(f64::INFINITY); reuses the WI-084 host-const value-source path (force_const reads a registered builtin for a SymbolKind::Const). Consider negative_infinity / nan siblings and an is_finite/is_nan predicate. Built on WI-084 (term-level const, delivered in main). Driver: lf1 webots motor velocity-mode sentinel — 'const VELOCITY_MODE_POSITION: Float = infinity' for Motor.set_position(+inf) — which ALSO needs the const-codegen WI before it reaches generated C++. Generally useful beyond motors (saturation bounds, unbounded ranges).

