## Attributes

- id: WI-20260820-CTD6D-an-effect-row-carried-as-a
- created: 2026-08-20T18:22:55Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-20T19:26:11Z

- acceptance: cargo-test, scaland-sbt-test

## Description

An effect row carried as a `Value::Entity` is not recognized as a row by either of the two sites that turn a walked row VALUE back into a row, so it is carried whole as a single opaque "label" instead of being flattened to its atoms. `wrap_bare_effect_expr_as_row` (kb/typing.rs, added by WI-329) matches `Value::Term` and `Value::Node` and returns `None` for everything else; `explode_incurred_effect_row` has had the identical hole since WI-441, where the same match is written inline. Both callers treat `None` as "not a row" and fall back to their non-row handling, so nothing is DROPPED -- the failure mode is the pre-WI-441 one: a row leaks as one opaque atom, which at an operation boundary renders as `undeclared effect: merge[left = present[...], ...]` (the shape WI-493's test asserts must not leak) and inside a lambda's arrow becomes the malformed `present(label = merge(...))` WI-329 fixed for the other carriers. HONESTY ABOUT THE EVIDENCE: this is a STATED gap, not a measured defect. No program was found that produces an Entity-carried effect row -- the rows reaching these sites in the current corpus are `Value::Term` (hash-consed) or `Value::Node` (occurrence-carried). It is filed because the repo principle is a loud error over a silent skip, and because the classification is by FUNCTOR HEAD (`value_is_bare_row_expr` reads `head(kb).functor_sym()`, which a `Value::Entity` answers), so an Entity-carried `merge(...)` is classified as a row and then silently declassified by the wrapper -- the same source shape producing two different incurred atoms depending purely on carrier. TWO ACCEPTABLE OUTCOMES, and the ticket is done when EITHER is reached with evidence: (a) the carrier is REACHABLE -- write the program that produces it, give the wrapper an Entity constructor (a `make_effects_rows_entity` sibling to `make_effects_rows_type` / `make_effects_rows_occ`), and drive the flatten with a test that fails when the constructor is removed; or (b) the carrier is UNREACHABLE BY CONSTRUCTION -- establish why (which producers can mint an effect-row Value, and that none of them mints an Entity), and replace the silent `None` with a loud error at both sites, so a future producer that does mint one is reported rather than silently mis-flattened. Do not close it by widening the match without deciding which of the two it is. ACCEPTANCE: whichever outcome, the reasoning lands at BOTH sites (they must not drift again -- WI-329 already extracted the shared helper for that reason); full cargo-test green.

## Changes

### 2026-08-20T19:26:00Z — feedback — user

DELIVERED as outcome (b): the carrier is UNREACHABLE BY CONSTRUCTION. Commit 47b04e6e.

THE REASON IS STRONGER THAN THE PRODUCER CENSUS THE TICKET ASKED FOR. Every row-STRUCTURAL position in the occurrence IR is a `TypeChild`, whose two variants are `Ground(TermId)` and `Node(Rc<NodeOccurrence>)`: `TypeNode::EffectsRows{effects_expr}` and all of `EffectExprNode` (`Merge{left,right}` / `Present{label}` / `Guarded{label}` / `Absent{label}` / `Open{tail}`). The one non-`TypeChild` child anywhere in the algebra is `Guarded.guard`, a `List[reflect.Term]`, which is not a row. `type_child_view_item` maps the two variants onto `ViewItem::Term`/`ViewItem::Node` and `view_item_value` onto `Value::Term`/`Value::Node`, so reading a row's child never widens the carrier. The term builders all return `TermId`, the occurrence builders all return `Rc<NodeOccurrence>`, and there is no third constructor. Two sites already asserted the same invariant in their own words: load.rs's `unreachable!("a param type is Term or Node")` over the same `type_expr_to_value` that builds `effect_values`, and `value_to_type_child`'s "a scalar/`Var`/`Entity` is a typer bug here".

THE STRUCTURAL ARGUMENT COVERS ONLY WHAT CAME THROUGH THE STRUCTURE — found by /code-review, not by me. The classifier reads `head(kb).functor_sym()` off arbitrary values, and WI-436 makes a 0-ARY row constructor read as a bare `ViewHead::Ref`. `empty_row` is one, and by volume it is the most common row shape reaching the wrapper (27621 of 97799 probed calls). So `Value::SymbolRef(empty_row)` AND a nullary `Value::Entity{empty_row}` classify as rows without ever having been in the IR. Same verdict by a PRODUCER census: `Value::SymbolRef` has exactly three producers, enumerated at its minter `symbol_value` (eval/builtins.rs) — `Dictionary.impl`, `OpRef.op`, `OpRef.named` — none an `EffectExpression` symbol; the nullary entity is what the EVALUATOR mints for `empty_row()`, and the typer's effect stream is loader/typer-built, never fed from the evaluator. This case now has its own driven row.

WHAT SHIPPED. `wrap_bare_effect_expr_as_row` returns `Value`, not `Option<Value>`. Both callers lose the fallback they each read as "not a row". The third arm is an OR-PATTERN over every remaining `Value` variant with no `_`, so a new carrier is a COMPILE ERROR at the site. It panics where the sibling `value_to_type_child` only `debug_assert!`s, and the doc says why they differ: that site has a meaningful degradation to fall back on (`?ungrounded`), this one does not — the only thing left to do with an unwrappable row is carry it whole, which IS the defect. The whole census lands in the wrapper's doc; both callers point at it.

MEASURED. A probe at the wrapper and at both callers, over the whole workspace suite (5353 tests over 35 binaries), logged 969760 row-position values. Every one was `Value::Term` or `Value::Node`; the gap arm never fired. A corpus run is a LOWER BOUND, which is why the verdict rests on the IR — the run only says nothing reachable contradicts it.

A DEAD ARM THE TICKET DID NOT ASK ABOUT. The same probe found that all 97799 values reaching the wrapper were `Value::Term`: its `Value::Node` arm — the entire reason the function is carrier-preserving, and what WI-341's occurrence preservation depends on — was executed by NO test in the repo. `a_node_carried_bare_row_wraps_and_stays_an_occurrence` now drives it.

TESTS, per back-out (restore the `Option` with `_ => None`, and the callers' `.flatten()`/`?`): 5 rows, 2 FAIL on the back-out (`a_row_shaped_value_on_a_third_carrier_is_refused_loudly`, `a_zero_ary_row_on_the_symbolref_carrier_is_refused_loudly`). The other 3 pass either way BY DESIGN and say so at their sites: `the_classifier_admits_a_third_carrier_the_wrapper_must_refuse` pins the ticket's PREMISE (classification is by functor head, so the wrapper is the only thing between a third-carrier row and the flatten), and the two live-arm rows pin carrier preservation. One guard cannot be driven at all and is stated instead: the no-`_` or-pattern is a compile-time gate.

CORRECTION TO THE TICKET TEXT. It says `explode_incurred_effect_row` has "the same match written inline". WI-329 (commit 70d5bb92, the commit immediately before this ticket was filed) had already extracted the shared helper and replaced that inline match with a call to it. There was one match and two callers, not two matches — which is what made "the reasoning lands at BOTH sites" cheap to satisfy.

ACCEPTANCE: cargo-test 5358 passed / 0 failed across 35 binaries (5353 before, +5 new rows), exit 0. scaland-sbt-test 507 passed / 0 failed / 0 errors, exit 0 — unchanged by this ticket, and expected to be: scaland PARSES effect rows but has no typer counterpart to this wrapper.

THREE REVIEW FINDINGS OUTSIDE THIS TICKET'S SUBJECT, fixed inline rather than filed (each smaller than its own description). (1) `goal_form_proposition` passed a literal `1` to `goal_position_boolean` beside the routing table that already carries an arity column and beside its own `pos_arity != 1` shape gate — now `pos_arity`, so "not is unary" is not spelled a third time. (2) `infer_discharged_row_tails` discarded `bind_row_tail`'s verdict; the refusals it can carry are now named (lacks violation, a denoted extra label deferred as WI-342 P4-B, prelude-less KB, occurs, contradiction) along with the symptom the user actually sees — `check_unconstrained_type_params`' "type parameter 'Rho' is unconstrained", accurate about the state and silent about the cause. (3) The WI-567 `not`/Γ ladder now states what makes its fall-through safe: a Γ hit makes the frame a ChoicePoint so `step_naf` is never reached, which is sound only because `gamma_candidates_for` filters with `views_structurally_equal` and yields `Candidate::Assumption`, which cannot fail deeper — relax that filter and a `not` with a Γ candidate FAILS outright instead of falling back to NAF, a completeness loss.

