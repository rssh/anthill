```anthill
fact WorkItem(id: "WI-824", created: "2026-07-23T10:11:25Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-25T05:54:17Z"))

fact Feedback(workitem: "WI-824", author: "user", at: "2026-07-25T05:53:13Z")

fact Feedback(workitem: "WI-824", author: "user", at: "2026-07-25T05:53:37Z")
```

## description

FIX the abstract-call mis-pin (bonus hazard from WI-817; it refuted the witness's first-cut positive control): with an UNCONDITIONED parametric provider in scope — fact Desc[T = Wrap[A = E]] whose head carries a FREE type var E and NO requires precondition — an ABSTRACT spec-op call (Desc.describe(x) at abstract FT) is silently resolved to that provider at LOAD: var-var unification lets the parametric head match the abstract per-call binding, dispatch returns a bogus Unique, and the WI-325 protection never fires (it guards NoCandidates and NoMatch, not Unique). The program then dies at eval — Err(Internal(field_access: entity has no field 'inner')) on a leaf value — or, with a signature-compatible wrong impl, would compute WRONG VALUES silently. MEASURED, pinned: unconditioned_parametric_fact_mispins_abstract_call (wi817_polyrec_requirement_test.rs). NOTE the asymmetry worth establishing before fixing: in the same witness family the CONDITIONAL parametric candidate (same head WITH requires Desc[T = E]) did NOT mis-pin the abstract call — it produced NoMatch — so the defect is specific to how an UNCONDITIONED parametric head matches an abstract goal; find where the two diverge (collect_provides_candidates / pick_most_specific / the dispatch walk, kb/typing.rs) before choosing the fix shape. SOUND BEHAVIOUR, decide precisely: an abstract per-call binding must not be PINNED via var-var unification — either (i) goal type params are RIGID in candidate matching, so the abstract call flows to NoMatch and into the existing ladder (MissingRequiresForSpecOp when uncovered, WI-562 licensing when covered), or (ii) the match survives but classifies Deferred, never Unique. WATCH: do not break parametric providers at CONCRETE bindings (stdlib provides Stream/FiniteCollection/Iterable with type-param bindings; wi224 conditional resolution; the witness family itself). INDEPENDENT of WI-821/822/823 — this is dispatch candidate matching, not the supply channel. ACCEPTANCE: the mis-pin pin FLIPS to a load-time rejection naming the spec op and abstract param (wi325-style) or to the explicitly chosen Deferred semantics, with the choice and reason recorded; wi224/wi210 dispatch suites and stdlib provides stay green; full workspace green via rustland/scripts/test.sh.

## Feedback

### 2026-07-25T05:53:13Z — user

DELIVERED. DECISION RECORDED: ticket option (i) — goal type params are RIGID in candidate matching — taken AT ITS WORD: with a call-site sigma in hand, EVERY bare type-param element is refused against a STRUCTURED candidate head, not merely those a sigma-class walk can PROVE rigid. Option (ii) Deferred rejected: a skolem is not provably Wrap[A=E] for any E, so there is nothing to defer TO; and Deferred with no supplier loads clean and dies at eval on an unbound __req_*, the failure mode WI-828 exists to convert INTO a load error. Refusing adds no new outcome — the call takes the existing ladder (MissingRequiresForSpecOp uncovered, WI-562 licensing covered). LOCUS: match_candidate_against_goal arm (2)'s non-parametric per-call branch (kb/typing.rs), the only place holding BOTH the sigma context and the (candidate-head, goal-element) PAIR; alternatives priced and rejected at review — goal construction cannot drop the element (resolve_inner needs it to find a FromScope cover), collect_provides_candidates would re-walk the same pairs, pick_most_specific sees only a u32, and resolve_at_goal IS option (ii) and would also refuse WI-507's legitimate Unique. ASYMMETRY ESTABLISHED FIRST as the ticket asked: the CONDITIONAL twin matched its head IDENTICALLY and diverged only AFTER, on its own unresolvable sub-goal — the head match was the defect, and the two now agree. SECOND WITNESS the ticket did not have, found by building the sigma-present path: cross-sort dictionary CONSTRUCTION (caller entry Desc[Wrap[A=AT]] sigma-disagrees with callee goal Desc[BT:=AT], so Strategy 3 constructs) loaded CLEAN and returned 99 from the mis-pinned provider — the ticket's hypothetical 'would compute WRONG VALUES silently', measured. It flips to the WI-828 refusal. MEASUREMENT THAT DECIDED THE SHAPE: the narrow sigma-CLASSIFIED form was written and measured first and is IDENTICAL on the corpus (17678 sigma-present arm-(2) firings, every one classifies rigid, ZERO unpinned); the wide form was chosen because the narrow one's retained leniency cites WI-507, whose unpinned-sibling case actually meets an impl-param REF and is decided by arm (1) — a rationale transplanted across candidate shapes — and because sigma-PRESENCE keying drops a dependency on the classifier, which cannot see an OPERATION's own type param (sort_param_rigids is built from parent-sort params only), returns None on chase-bound exhaustion, and chases a KB-wide canonical global. ACCEPTANCE MET: 3 WI-817 pins flip (mis-pin pin -> load rejection naming the spec op + the missing requires; the two op-param (b) pins keep pinning the SAME gap but reach it via the WI-325 ladder instead of DispatchNoMatch, there being no bogus candidate left for dispatch to fail on); 8 new tests in wi824_abstract_mispin_test.rs with both witnesses DRIVEN HEAD->fix and 6 controls green in BOTH directions; wi210/wi221/wi224/wi228 dispatch suites and stdlib provides green; full workspace 3498 tests, 0 failed.

### 2026-07-25T05:53:37Z — user

REVIEW RESIDUALS (recorded here per the no-ticket-spinoff rule). /simplify (4 agents) APPLIED: flattened the guard; test driver() helper + one MISSING_REQUIRES const per file (the needle carries a U+2026, spelled 4x before); module registration appended in delivery order. Its altitude agent caught the WI-826 failure mode reproduced VERBATIM in my own comment — I attributed 'compat-only' to the sigma-less path after enumerating ONE of its FOUR entries; the omitted three include resolve_bridge_requirements, which builds REAL dictionaries at EVAL and is safe only because it resolves fully-ground goals (its own all_pinned guard, whose rationale I annotated as still-true-because-sigma-less). Comment now enumerates all four. /code-review (12 findings): APPLIED the retained-lenient-branch tightening (see the decision note above — two independent reviewers converged on it being corpus-dead, untested and transplanted), the sole_error_at test helper (every refusal pin asserted needles against the JOIN of all load errors, which stays green if the clauses drift into two diagnostics), and a doc precondition sentence naming the sigma gate + the four sigma-less entries (a port reading the table would otherwise take rows 2-3 as total). NOT REPRODUCED, both DRIVEN: (F1) 'the classifier cannot see an OPERATION's own type param, so the mis-pin reproduces with the element moved to operation a[AT]' — the mechanism claim is TRUE but the consequence is not: every faithful op-scoped spelling dies FIRST on the WI-822 no-supply error (DeferToRequirement: __req_desc not bound), so op-scoped chains never reach construction at all today. The blind spot becomes OBSERVABLE when WI-822 lands a supply channel — recorded there too; the tightening makes the guard spelling-independent, so it is closed in advance. (F2) 'spec_resolves_at_bindings accepts a declared field type on the strength of the lenient match' — probed with and without the parametric provider in scope: the verdict is IDENTICAL (both load clean), so that branch is not what decides it. SKIPPED with reasons: consolidating the rule's three implementations behind one predicate (the altitude review priced it — a blanket hoist above arms (2)/(2.5)/(3) would newly refuse dispatch_values_match's universally-quantified-candidate fast path, i.e. a legitimate 'fact Eq[T]' provider = doc row 4; documented at both emergent sites instead); hoisting the Desc/Leaf/Wrap fixture + load_errs + eval_fresh into tests/common (SEVEN sigma-cluster files carry copies now — doing it for two would create a split convention; same reason WI-826 skipped the with_instances hoist, and this is the second ticket to pay it); resolve_at_goal collecting the candidate set TWICE per goal (pre-existing; the tightening removed the sigma chase from this branch entirely, so the diff no longer adds cost to it).

