## Attributes

- id: WI-858
- created: 2026-07-30T11:56:39Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-30T16:11:59Z

- acceptance: cargo-test

- depends_on: WI-857

- tags: modinst

## Description

Proposal 058 (WI-648) PHASE 7 — the SHIPPED-LIBRARY driver: two lawful orderings of one carrier in the prelude (the WI-844 instruction phase 4 measured impossible for String: the Rust binding's own fact Ordered[T = String] makes any third String provider break every downstream bracket-less compare — brainstorm prelude-multiple-orderings.md). CARRIER: Pair. WORK: (a) Pair gains componentwise provides PartialEq[T = Pair] / provides Eq[T = Pair] in stdlib/anthill/prelude/pair.anthill, conditional on Eq[A]/Eq[B] (the Set precedent — sort-level requires honored via the provides fact; the q4c probe measured this shape loading, on a local Duo stand-in). (b) Two BUNDLED witnesses — PairByFst (lex fst-then-snd) and PairBySnd (lex snd-then-fst) — each providing its OWN PartialOrd[T = Pair[A, B]] AND Ordered[T = Pair[A, B]] with requires Ordered[T = A], Ordered[T = B]: the 058 §3.8 bundle rule (ordered.anthill:44-48 couples a lone witness's compare to the carrier's inherited PartialOrd, so ANY single-floor alternative is unlawful; a compare-fst-only witness is additionally a PREORDER that drops elements — do NOT resurrect it). NO DefaultProvider row for (Ordered, Pair): no canonical pair order exists, silence stays loud (tier 3), correctly. (c) SortedSet-over-Pair driver value tests: construction-site bracket; bracket-less thread; the union-across-orderings type-error twin; the two first-elements swap when the two brackets do. ACCEPTANCE: two orderings in the shipped prelude; bracket-less Ordered.compare on Pair is the tier-3 diagnostic naming both witnesses; String behavior unchanged (control); the binding-free stdlib load stays clean (obstacle B control — the witnesses' Eq/PartialOrd legs discharge from Pair's own provisions, q4c shape). GATED ON WI-857: the bundled parametric witness form LOADS today and DIES at eval on the dictionary-chain split, both routes (probes q4d/q4e, recorded in WI-857's feedback). Full workspace green via rustland/scripts/test.sh. REFERENCE: 058 §3.8, §5; design/058-implementation.md §7, §8 phase 7.

## Changes

### 2026-07-30T14:11:43Z — feedback — user

UNBLOCKED — WI-857 delivered 2026-07-30; the bundled parametric witness form now LOADS AND RUNS on both routes (its q4e reproducer is pinned in wi857_dictionary_layout_test::a_parametric_chain_bearing_witness_threads_both_halves, -1 plus a tie-break arm = 1). TWO THINGS THIS PHASE INHERITS. (1) DRIVE THE UNMEASURED COMPOSITION LEG: 058 §7 records that binding a witness's own NAMED slot inside a bracket value (O = ListOrd[OE = LexFst], proposal §5) was never measured — the pre-restructure §4.5 called it 'the composition of two measured legs' and the q4 series drove only the INFERRED form (O = LexFst, params bound by unification). Eval is restored, so it is drivable now; this phase is the natural place, since PairByFst/PairBySnd over a parameterized element is exactly that shape. (2) THE LOCALITY RULE IS WHAT MAKES TWO COEXISTING BUNDLES WORK, and this phase is its first REAL exercise: once the producer bundles the spec's chain, PartialOrd[Pair] has one candidate inside EACH of the two witnesses' chains and a global search TIES — WI-857 resolves it witness-locally (resolve_inner's local_provider, immediately-enclosing provider only). Its pin today is a synthetic two-witness fixture; the prelude Pair pair is the shipped one. If a bracket-less compare on Pair does NOT reach the tier-3 diagnostic naming both witnesses, suspect locality having been applied too widely (it must key on the SELECTED provider, never on caller scope) before suspecting the classifier.

### 2026-07-30T16:12:22Z — feedback — claude

DELIVERED 2026-07-30. Shipped: prelude/pair.anthill carries PairByFst/PairBySnd (bundled PartialOrd+Ordered, NAMED element slots OA/OB) and Pair provides PartialEq/Eq componentwise; acceptance in wi858_pair_orderings_test (15 tests), full workspace green (3803 passed). Positional type-arg spelling throughout (Ordered[A], Pair[A, B]) — measured identical to the [T = …] form and far more readable.

THE TICKET'S PRESCRIBED CHAIN WAS WRONG AND DRIVING SHOWED IT. (a) said 'conditional on Eq[A]/Eq[B]'. Measured: with an Eq chain, Pair[A = Float, B = Int64] becomes a LOAD ERROR — Float provides NonEq and WI-835's check_use_site_requires_eq refuses a NonEq carrier at a parameter whose sort requires Eq — i.e. Pair stops being a general PRODUCT. The Set precedent the ticket cites does NOT transfer: Set/Map genuinely need Eq on keys, a pair of anything is a pair. Shipped with requires PartialEq[A]/[B] — the weakest chain the componentwise body needs, conditioning PartialEq[Pair] exactly — on the USER's call (they chose the ticket's shape first, then refined to 'unconditional PartialEq, conditional Eq to a follow-up'). Residue recorded not hidden: a sort's requires chain is SHARED by every provision, so Eq[Pair] rides the same chain and OVER-CLAIMS. That is WI-869, now in the plan and in proposal 058 §3.8/§4 as a stated rule with its own production.

THE COMPOSITION LEG (feedback item 1) IS DRIVEN AND UNWIRED — WI-870. Two arms: the value's bracket IS validated against the witness (an unknown slot name is refused, naming A, B, OA, OB) and then DISCARDED — [Ordered = PairByFst[OA = PairBySnd]], whose value provides no Ordered[Int64] at all, loads clean and computes the unbound answer (-1). A silent drop, and a defect rather than a gap because TieRepair::SubGoal PRINTS that exact spelling as the repair.

THE LOCALITY RULE (feedback item 2) HELD, first try, on the shipped pair: a bracket-less Ordered.compare on a Pair reaches the tier-3 diagnostic naming both witnesses — so PartialOrd[Pair], which has one candidate inside EACH bundle, resolved witness-locally rather than tying.

THREE PRE-EXISTING DEFECTS FOUND BY DRIVING, all invisible until a PRELUDE sort repeated a spec in its chain: WI-871 (componentwise provider's second requirement slot read from the first; four-cell matrix, pinned with a failing arm that panics WITH INSTRUCTIONS when fixed), WI-872 (a provision's carrier matched by SHORT NAME at dispatch — a local sort Set collides with the prelude's identically, so never about Pair; broke wi664_composite_eq_test, whose fixture was renamed Pair->Duple, and is now pinned in this phase's suite since the change WIDENS the reserved-name set), WI-873 (dispatch_origin keeps ONE rewrite per spec op for the whole image; four order-dependent call sites now go through common::assert_req_param_spec).

TWO PROCESS LESSONS. A per-binary green is NOT evidence for order-dependent tests: --test wi_tests passed while the full-workspace run failed a fourth wi222 site, because which dispatch_origin entry survives varies with interning order. And a review subagent ran a git checkout mid-review and REVERTED pair.anthill to HEAD — the whole delivery's central file — recovered by rewriting it and confirming by measurement (2312 facts, matching exactly).

### 2026-07-30T16:38:36Z — feedback — claude

REVISED 2026-07-30, SAME DAY, ON REVIEW — the shipped shape is ONE canonical ordering, not two witnesses. The delivered form (PairByFst + PairBySnd in the prelude) satisfied this ticket's LETTER and worked against its PURPOSE. Two things drove the revision, both raised by the user:

(1) OBSTACLE A BITES Pair TOO, which the brainstorm's Options table missed. It reasoned that a Pair witness pair costs nothing because Pair self-provides nothing — true when written, but obstacle B REQUIRES Pair to gain Eq before any prelude ordering of it is admissible, and once Pair is a provider, shipping two rivals hands every downstream bracket-less pair compare the same tier-3 error that rules String out. A standard library may not impose that on consumers who never opted in.

(2) A PAIR HAS A CANONICAL ORDER. Lexicographic fst-then-snd is what Haskell (Ord (a,b)), Rust (Ord for (A,B)), Python tuples and std::pair all give it. 'No canonical pair order exists' — which the delivered pair.anthill asserted — is false against that precedent, and it was the load-bearing premise for shipping two co-equal witnesses and for declining a DefaultProvider row.

SHIPPED NOW: Pair provides PartialOrd/Ordered ITSELF, lexicographic fst-then-snd, beside componentwise PartialEq/Eq. A bracket-less Ordered.compare on a Pair ANSWERS (tier 2) and SortedSet.empty[T = Pair[Int64, Int64]]() needs no ordering bracket — MEASURED: -1 / 1 on tie / 0, and (1,9)(2,1) through a bracket-free SortedSet pipeline. Nothing ordered a Pair before, so this is a capability ADDED, not a choice imposed. An ALTERNATIVE ordering is the PROGRAM's to declare; wi858_pair_orderings_test declares BySnd and ByFstDesc locally and selects them by name, which is 058's coexistence exercised the way a real consumer meets it.

NEW MEASURED LIMIT, pinned: while a rival IS declared, the canonical order is UNREACHABLE through a requires slot. The bare goal is ambiguous, and the repair that error suggests is itself refused — Pair is a CONCRETE provider, so §3.5 check 3 rejects an explicit [Ordered = Pair] (the value decides). That pincer is exactly what rung 2a removes via §3.6's inference rule (the carrier's own provision IS the default), with NO edit to pair.anthill. WI-861 now gates the Pair story as well as the String one; the test's first arm becomes a value assertion when 8c lands.

ALSO NEW: requires Ordered[A], Ordered[B] joins the shared chain, so a SECOND over-claim rides it (every Pair nominally demands ordered components — false for Pair[Term, Term], unenforced since no use-site check exists for Ordered). Same fix as the Eq over-claim: WI-869.

THE LESSON: 'the shipped library must carry two orderings' was a PROXY for 'the mechanism must be exercised outside the suite'. Proxy and goal came apart at the one carrier that has a canonical order — where carrying two imposes on every consumer what one consumer should opt into. Read a phase's instruction for its purpose before satisfying its letter.

