## Attributes

- id: WI-479
- created: 2026-06-15T16:18:42Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-24T04:55:20Z

- acceptance: cargo-test

- depends_on: WI-067

- tags: p048

## Description

Proposal 048 Phase 3 — migrate partial primitives to guarded Error[DivisionByZero]. Change Int64.div/mod/rem/divExact (and Field.div/recip) from unconditional 'effects Error[DivisionByZero]' (WI-066) to '{ Error[DivisionByZero] :- eq(b, 0) }'; update the WI-066 tests so a literal-divisor call (div(a,5)) and a guarded-branch call (if neq(b,0) then div(a,b)) now type PURE, while div(a,b) with unknown b still carries the effect. Decide open question C: keep the existing 'constraint div_nonzero_primary: neq(?b,0) :- div(?_,?b)' as an integrity guard and DECLARE the positive guard on the op (do not derive it from the constraint — neq is not(eq)=NAF). Depends on WI-067 (discharge) which already requires WI-478 (grammar/representation).

## Changes

### 2026-06-24T04:55:21Z — feedback — claude

DELIVERED 2026-06-24 (commit 37cf9af). Migrated Int64.div/mod/rem/divExact + Field.div/recip from unconditional 'effects Error[DivisionByZero]' to guarded '{ Error[DivisionByZero] :- eq(b,0) }' (recip guards eq(a,0), its sole param). WI-067 discharge applies unchanged to the stdlib ops: literal non-zero divisor (div(n,2)) and 'if neq(d,0) then div(n,d)' narrowing now type PURE; symbolic divisor + literal-zero (div(n,0), eq(0,0) holds) keep the effect; declaring it over a symbolic divisor still loads (over-declaration is allowed — the effect check is one-directional incurred-subset-declared, no reverse check). Open-question-C: DECLARED the positive guard on each op; kept div_nonzero_primary/div_nonzero/rem_nonzero/mod_positive constraints unchanged as integrity guards (NOT derived — neq=not(eq)=NAF would route discharge through the polarity hazard). mod's effect guard eq(b,0) is correct (DivisionByZero fires at b=0) even though mod_positive is stricter (b>0) — separate concerns. Rewrote wi066_division_effect_test.rs (6 tests). Full workspace suite green (63 binaries). /code-review (3 finders, max effort) returned [] — verified guard params correct, sound (drop only on constructive refutation), literal-0 error is genuinely undeclared-effect not a constraint violation (op bodies emit no div(...) fact). Field has no concrete instance so Field.div/recip validated only by stdlib load. Phase 4 (head/nth/... partial ops) remains future work.

