## Attributes

- id: WI-378
- created: 2026-06-04T06:24:49Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-17T14:35:18Z

- acceptance: cargo-test

## Description

Unify rule var-collection (renumbered off origin's parallel WI-377/effect-row collision). Kill the 4-way lockstep over an Expr's embedded type-fields (Apply.type_args / ApplyWithin.type_args / Let.type_annotation, holding Values): collect/close/open/substitute each special-case them; close+open share carrier-agnostic close_value_type/open_value_type but COLLECT re-implemented a bespoke Term-only walk held in manual lockstep ('CRUCIAL: must mirror node_to_debruijn'). STEP 1 DELIVERED (a96a55f, behavior-preserving): collect_value_type/collect_type_args_vars/collect_option_value twins of close_*; collect_expr_termid_field_vars delegates -> structural mirror, not hand-maintained. STEP 2 (follow-on): wire gap 1 (WI-342-P3 Type-occurrence var-walk) so collect/close walk INTO a Type occ, then migrate type_args/type_annotation to occurrence-children -> one carrier-agnostic walk replaces the collect_vars_rec(term-head)/collect_occurrence_global_vars_ordered(occ-body) pair; touches the typer + Expr repr; also unblocks denoted value rule heads (WI-373 gap 1). User-chosen 'both sequenced' 2026-06-04. Surfaced reviewing WI-373. Rust-only.

## Changes

### 2026-06-04T06:24:49Z — feedback — user

STEP 1 DELIVERED 2026-06-04 (a96a55f): collect side now structurally mirrors close via collect_value_type (twin close_value_type) + collect_type_args_vars (twin close_type_args; _vars avoids the existing reflect collect_type_args) + collect_option_value (twin close_option_value). Behavior-identical (Value::Term->collect_vars_rec, else nothing); Value::Node skip consistent with close_value_type's node_to_debruijn no-op on a Type occ. node_occurrence.rs only; 1439 green (incl origin WI-375 tests); /code-review 0 findings. STEP 2 = gap 1 (Type-occ walk) + type-field->occurrence-children migration; couples with WI-373 gap 1 + typer.

### 2026-06-04T08:34:40Z — feedback — user

STEP 2 GAP-1 (Type-occurrence var-walk) DELIVERED 2026-06-04 — user-scoped to gap-1 only this session (migration deferred). The 3 rule-var rewriters (node_to_debruijn close / open_debruijn_node / substitute_occurrence) + the var-collector now DESCEND a NodeKind::Type / NodeKind::EffectExpr occurrence (was: opaque early-return on as_expr / debug_assert). Mechanism: a shared structural walk map_type_node / map_effect_node / map_type_child parameterized by trait TypeChildRewrite (term + node leaves), with Close/Open/Subst impls — close/open/sigma cannot drift over the type spine. map_value_type is the SINGLE def of "rewrite a type Value" (Term->term op, Node->recurse node rewriter, Entity/Tuple->recurse children, ptr-eq reuse on no-change); close_value_type/open_value_type/subst_value_type collapsed to one-line delegates (killed the 3-way value-type lockstep — extends STEP 1). collect_value_type is the collect twin (descends Node via collect_type_node_vars/collect_effect_node_vars/collect_type_child + Entity/Tuple). Ground children walk term-side; NamedTuple.fields cons-list (Value::Entity) recurses (matching close_value_head_debruijn / occurs-check / rewrite_ref_value). The 3 for_each_child-driven no-kb collectors KEEP their NodeKind::Type debug_assert — unreachable (for_each_child does not yield type-field children) until the migration routes type fields through for_each_child (which must also thread kb); comments updated. FORWARD SUBSTRATE: no producer mints a var in a Type occ today (denoteds Ref/Const, type-vars stay ground TypeChild::Ground), so behavior-preserving — wired arms run on no-var denoted/value-in-type paths, return unchanged via ptr-eq. 5 substrate tests (collect ground+node, close/open round-trip per-index, no-change identity, NamedTuple field var, sigma). 1449 green (+5). /code-review: 2 finders+sweep caught the NamedTuple.fields cons-list gap -> fixed via map_value_type Entity/Tuple recursion; verifier re-confirmed 0 findings. === REMAINING (STEP 2 migration): type_args/type_annotation -> occurrence-children. SCOPING FINDING (corrected — earlier wording overstated this as a rule conflict): it is a COST TRADEOFF, not a carrier-rule violation. The carrier rule's only HARD direction is the floor (denoted-containment => Value); a ground type-arg MAY be hash-consed Value::Term but is NOT required to be (entity-representation-term-or-value.md sec2 says "may"; hash-consing is an optimization that "earns its keep" for ground/shared structure per its Choosing-a-carrier section + types-need-not-be-hash-consed + the CLAUDE.md representation note). So the migration would either (a) push ground type-args out of their hash-consed form (losing dedup / O(1)-eq / sharing) to get one uniform for_each_child walk, OR (b) keep ground as Term, leaving the term-vs-occurrence dispatch (close_value_type etc.) inherent. A tradeoff to weigh, not a rule that blocks it. Only the loader produces real type values; only the typer reads them (seed_op_type_args, Let typing). Reassess migration scope before committing.

### 2026-06-17T14:35:16Z — feedback — claude

DECISION (with user, 2026-06-17): do NOT do the STEP 2 repr migration (type_args/type_annotation -> for_each_child occurrence-children). WI-378 is satisfied by gap-1 (delivered 2026-06-04). RATIONALE: gap-1 already eliminated the 4-way type-field lockstep -- map_value_type is the single "rewrite a type Value" def with close/open/subst as one-line delegates, collect_value_type its collect twin; nothing can drift over the type spine. The remaining migration would only collapse an already-single, tested side-walk (for_each_child + collect_expr_termid_field_vars -> collect_value_type) and drop 3 debug_asserts that are UNREACHABLE today (for_each_child does not yield type-field children). No live defect. COST that outweighs it: thread kb through the 4 for_each_child walker families OR push var-free ground type-args out of hash-consed Value::Term (losing dedup/O(1)-eq/sharing), plus a new (name,type) child repr that seed_op_type_args relies on, touching load/typer/eval/resolver/DeBruijn. The carrier rule does NOT require it (denoted-containment is the only hard floor; ground MAY stay Term per the CLAUDE.md representation note + types-need-not-be-hash-consed). TERM_CACHE finding (user-raised): cached_term (node_occurrence.rs:2244) memoizes a hash-consed TermId per occurrence, so a Value::Node yields its canonical/dedup'd term on demand -- the dual carrier (Value::Term vs Value::Node; TypeChild::Ground vs TypeChild::Node) is NOT a representational forcing function; no capability gap pushes the migration. IF uniformity is ever wanted (no current driver): the clean path is var-bearing type leaves as TypeChild::Node(Expr::Var) (walkable with NO kb), var-free ground stays Ground(TermId), consumers read the canonical term via the cache -- no hash-consing loss, no kb-threading. RESIDUAL: the lone latent gap (resolve-time delay-check over a Type-spine var, guarded by the WI-322 loud debug_assert in collect_unbound_vars_node) = WI-504; it does NOT depend on this migration. WI-504's WI-378 dependency removed.

