## Attributes

- id: WI-629
- created: 2026-07-03T17:32:03Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-06T08:48:57Z

- acceptance: cargo-test

## Description

Resolver NAF over carrier-neutral (Value-carried) goals: two verdict-honesty gaps (pre-existing; found during WI-616 /code-review, finders A/E). (1) value_is_ground (resolve.rs ~2605) has NO Value::Entity/Tuple arm — falls to '_ => true' — so step_naf's groundness gate reads a not(Value::Entity{...}) wrapper goal (the shape lower_query synthesizes via make_goal_value for not/or) as GROUND even when unbound vars sit inside its children; NAF then runs and can decide/flounder where it should delay-and-rotate (contrast value_has_open_world_ref_inner, which HAS the Entity recursion arm — value_is_ground needs the same; WI-616's value_deep_ground in resolve.rs is a ready-made deep checker to unify with). (2) step_naf's ground/inner-floundered arm (~1735) pops the frame and yields Solution{residual:[not(P)]} WITHOUT checking whether the frame still holds TAIL goals — goals[1..] are silently dropped from the yielded solution, so a conjunction reads satisfied-modulo-residual while a conjunct was never attempted (the Delay branch at ~1666 correctly rotates when tail goals exist). Fix both together: deep groundness for value-carried inners + carry-or-rotate the tail on the floundered yield; add regression tests with not(Entity) wrappers from LogicalQuery paths.

