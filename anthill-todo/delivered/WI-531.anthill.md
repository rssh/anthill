```anthill
fact WorkItem(id: "WI-531", created: "2026-06-20T18:35:02Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-519"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-21T07:30:55Z"))

fact Feedback(workitem: "WI-531", author: "user", at: "2026-06-20T17:26:01Z")

fact Feedback(workitem: "WI-531", author: "user", at: "2026-06-20T17:31:49Z")

fact Feedback(workitem: "WI-531", author: "user", at: "2026-06-20T17:40:12Z")

fact Feedback(workitem: "WI-531", author: "user", at: "2026-06-21T05:16:26Z")

fact Feedback(workitem: "WI-531", author: "user", at: "2026-06-21T07:30:51Z")
```

## description

Reflect faithfully mirrors core query semantics: a reflect query result must carry (subst, residual) exactly like the core Solution, so reflect is a faithful DESCRIPTION of the core (the WI-010 self-hosted typer runs on reflect, so any gap = reflect lies about core). Today eval/mod.rs:850 (stream_split_first / PumpResolver) wraps sol.subst as Value::Substitution and SILENTLY DROPS sol.residual; consumers KB.execute (builtins.rs:724 kb_execute), LogicalStream.splitFirst (665-701 logical_stream_split_first). WI-519 makes the VERDICT honest at the reflect boundary (a floundered residual is not delivered as a definite answer); THIS WI adds the DATA: the reflect query result exposes the residual goals (the pending/undischarged goals) so anthill code can inspect WHICH goals floundered, matching what Rust consumers see from resolve(). Likely shape: a result sort carrying (substitution, residual: List[Term|NodeOccurrence]) OR a stream of (subst, residual) pairs; update stdlib reflect query result sort (reflect.anthill Substitution/query area) + eval PumpResolver + kb_execute/logical_stream. DESIGN NOTE (considered + rejected alt, user 2026-06-20): representing floundering by rigidifying the unfilled vars (Var::Rigid) instead of carrying the residual is NOT equivalent — it loses the pending CONSTRAINT (gt(?x,3)/not(p(?x))/nonvar collapse to 'opaque var'), conflates a floundered answer with a universally-quantified DEFINITE answer (re-introducing the masquerade), and skolemizing free vars to DECIDE a goal is unsound. Residual = first-class 'goal is pending' marker, strictly more expressive. Rust-only + stdlib.

## Feedback

### 2026-06-20T17:26:01Z — user

DESIGN REFINED (user, 2026-06-20): execute is ALREADY 'operation execute(kb, query) -> Stream[T = Substitution, E = Error]' (reflect.anthill:284-285) — the Error channel is the residual's home. So do NOT change the stream element type (it stays Stream[Substitution] of DEFINITE answers, per the existing API). Instead: a floundered solution RAISES Error (via the existing E=Error effect, WI-073) carrying the residual goals as data, rather than silently delivering/dropping it (eval/mod.rs:850 PumpResolver currently drops sol.residual). This is verdict-honest (floundered != a substitution answer), data-faithful (residual inspectable via the Error handler => reflect still describes core), uses the existing effect (no signature change), and is 'loud error over silent skip'. Streams compose: yield N definite substitutions, then raise Error on the floundered branch. SUPERSEDES the earlier 'stream of (subst,residual) pairs' shape — residual rides in the Error, not on every element. Likely needs an Error variant like Floundered(residual: List[Term|NodeOccurrence]). The rigid-var alternative is rejected: floundering is a trichotomy (success/fail/undecided); a substitution only encodes success-with-bindings, and rigidity (a var's unify-behavior, value-like) can't carry 'goal pending' — it collapses undecided into success (masquerade) or fail (wrong verdict, e.g. eq of distinct rigids), which is the very core/reflect divergence to avoid.

### 2026-06-20T17:31:49Z — user

CORRECTION (user, 2026-06-20) — RETRACTS the flounder-as-Error refinement above. Undecided is NOT a runtime Error; it is a THIRD logical outcome carried as residual DATA. (1) The core already represents it as data (resolve() -> Solution.residual, never raises) — raising Error in reflect would re-diverge reflect from core. (2) E=Error on execute is for genuine Error.raise from op bodies; floundering is 'couldn't decide', conflating them is wrong. (3) DECISIVE: the WI-010 self-hosted typer (reflect's reason to exist) must COLLECT residual constraints as data to solve them; if undecided aborted as an error, inference couldn't run. So WI-531 = expose residual as DATA: the streamed answer carries (subst, residual) — i.e. execute's element type becomes a Solution-like sort (subst + residual), NOT bare Substitution; E=Error reserved for genuine errors. Each consumer then DECIDES (prover: residual=not-proved; typer: residual=pending constraints). This is bigger than flounder-as-Error (changes the stream element type / public KB.execute result) but is the faithful, typer-enabling design.

### 2026-06-20T17:40:12Z — user

DECISION (user, 2026-06-20): the honest result type is Either INSIDE the stream — Stream[Either[Undecided, Substitution]] (equivalently Stream[T = Solution] where Solution = definite(subst) | undecided(subst, residual)), NOT Either[Undecided, Stream[Substitution]] (Either outside). Reason: Either-outside forces EAGER flounder-detection (you must know if any branch flounders before choosing Left/Right) which defeats the lazy Stream and collapses the 'one definite + one floundered' case. Either-inside is lazy and exactly faithful to core's Vec<Solution> (a Solution IS Either[Undecided(residual), Substitution] with Right = empty-residual). Each pull yields a definite Substitution OR an Undecided carrying (partial subst, residual goals). Undecided is the Left DATA case in the answer stream, NOT the E=Error channel (E=Error stays for genuine Error.raise). 'There is no stream of residuals' — the residual is per-stuck-branch; the stream is of definite-or-undecided ANSWERS. Final naming/shape (named Solution sort vs Either) is impl detail for WI-531.

### 2026-06-21T05:16:26Z — user

DECISION FINAL (user, 2026-06-21): element type is a NAMED Solution sort, not generic Either — resolving the 'named Solution vs Either' impl-detail the 2026-06-20 decision left open. Shape: sort Solution { entity definite(subst: Substitution); entity undecided(subst: Substitution, residual: List[Term]) }. execute: Stream[T = Solution, E = Error] effects Error. KEY: keep BOTH channels for their proper meanings — undecided (couldn't-decide, floundered) is the DATA case in the stream (definite|undecided); E=Error stays RESERVED for genuine errors only (invalid query, depth limit, Error.raise from op bodies). Rationale recap (chosen over Substitution+Error[NotDecidable] effect): reflect must faithfully describe core, and core's Solution{subst,residual} never raises on flounder (resolve.rs:246, is_definite()==residual.is_empty()) — an execute that raised would re-diverge reflect from core; the WI-010 self-hosted typer must COLLECT residual as pending-constraint data; Either-INSIDE the stream stays lazy/per-branch (interleaves one-definite + one-floundered). The typed Error[NotDecidable] steelman reifies (047 Error↦Result) to exactly this Stream[Solution], so it buys nothing but forcing every caller to reify at the boundary that exists to reify. 3 deliverables: (1) element type Substitution→Solution (undecided arm carries partial subst + residual goals, NOT a bare marker); (2) stop dropping sol.residual at eval/mod.rs:850 PumpResolver, thread through kb_execute (builtins.rs:724) + logical_stream.splitFirst (665-701); (3) fix reflect.anthill:283 comment which currently documents the rejected 'residual captured by E=Error' design.

### 2026-06-21T07:30:51Z — user

DELIVERED 2026-06-21 (uncommitted; agent claude). execute now -> Stream[T = Solution, E = Error] with enum Solution { definite(subst: Substitution) | undecided(subst: Substitution, residual: List[T = Term]) } (reflect.anthill) — the named-sort form of the 2026-06-20 Either-inside decision. Producer = ONE choke point Interpreter::make_solution_value (eval/mod.rs) feeding stream_split_first's resolver branch (the site that was dropping sol.residual). Residual is CARRIER-FAITHFUL: goals stay as their Value (a Value::Node keeps occurrence/span per WI-348 — deliberately NOT reified, which would drop span), surfaced as List[Term]. Field order DECLARED (subst, residual) via sort_named_canonical, not alphabetical, so positional patterns bind right (first non-alphabetical entity). E=Error reserved for genuine errors. Consumers migrated: tests kb_query/wi182/wi077 + NEW wi531_solution_residual_test (proves the undecided arm carries a non-empty residual via a Person(...) :- eq(?a,?b) flounder); anthill-stl split_first_yields_solution_values unit test; anthill-todo bundle main.anthill walk_solutions — made VERDICT-HONEST (skips undecided claimable; exhaustive match forces both arms; golden parity byte-unchanged since claimable is ground). Full workspace 2161 passed / 0 failed / 11 ignored. /code-review (4 finder agents + verify + sweep): applied 3 fixes (verdict-honest walk_solutions; functor-resolve-before-alloc leak-proofing; explicit residual-carrier docs); refuted the arena-leak finding; FILED WI-534 for the host bridge (anthill-stl/src/reflect/bridge.rs) which is a SECOND execute realization still dropping residual (distinct subsystem, no test break). Remaining follow-ups: WI-527 (docs), WI-528 (scaland), WI-534 (bridge). NOT committed (awaiting user).

