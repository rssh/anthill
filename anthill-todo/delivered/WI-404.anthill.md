## Attributes

- id: WI-404
- created: 2026-06-06T03:53:17Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-16T09:58:17Z

- acceptance: cargo-test

- tags: typing

## Description

Typer BUG: a DENOTED / value-in-type parameter binding does not round-trip through the SUBTYPE check, so a denoted-bearing parameterized type fails to conform to ITSELF. A binding like N = 3 in Vec3[T = Int, N = 3] (a value-in-type / denoted parameter, WI-366/WI-390 representation) renders/compares as N = ? inside parameterized_compatible_view (and check_binding_by_variance) in types_compatible -- so returning a Vec3[T=Int, N=3] value where Vec3[T=Int, N=3] is declared is SPURIOUSLY REJECTED (a type is not compatible with itself). REPRO: no alias needed -- reproduces with a plain value-in-type parameterized sort compared against itself via return-conformance. SURFACED 2026-06-06 by the WI-381 /code-review finder agents (a transient probe), confirmed independent of WI-381 (alias resolution): the denoted binding is dropped to a wildcard one level below alias resolution. IMPACT: (a) false-reject of valid code carrying a value-in-type parameter; (b) LATENT SOUNDNESS smell -- if both sides collapse the denoted binding to ? (an inference wildcard), a real mismatch (N=3 vs N=4) could be masked rather than rejected; needs checking which way it fails. CODE: parameterized_compatible_view / check_binding_by_variance (anthill-core/src/kb/typing.rs); the Value::Node / denoted carrier (TypeExtractor::Denoted). RELATES to the WI-381 view-path follow-on (the denoted-bearing-alias test vehicle is blocked by THIS bug). ACCEPTANCE: a denoted-bearing parameterized type conforms to itself (and a differing denoted binding N=3 vs N=4 is correctly rejected); regression test; cargo-test green.

## Changes

### 2026-06-16T09:58:16Z — feedback — user

Delivered in b55f85e (merged 8475023). Two-halves resolution: (1) COMPARES — the denoted-binding subtype comparison was ALREADY correct at HEAD (cumulative value-in-type denoted work, decisively WI-481 re-keying denoted param refs into the synthesized call-return type, plus the (denoted,denoted) subtype arm from WI-342). Verified across all vehicles the ticket names (literal/value-place/alias/member-op) + soundness controls (N=3 vs N=4 and value-place N=c vs N=d both correctly REJECTED — the accept+reject pairs prove the body type carries the concrete denoted, not a wildcard). (2) RENDERS — found the residual 'renders as N = ?' half: denoted_value_display dropped a LITERAL denoted (Expr::Const) to '?', so a mismatch error read 'expected N = ?, got N = ?'. Fixed with a literal_display helper (Expr::Const arm; reuses public write_anthill_string for strings). 6 regression tests; full workspace 1927 green; /code-review run.

