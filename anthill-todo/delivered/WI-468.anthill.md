```anthill
fact WorkItem(id: "WI-468", created: "2026-06-15T06:26:12Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-15T17:11:34Z"))

fact Tag(workitem: "WI-468", name: "typing")
```

## description

Typer/WI-401 escape-gate gap (sibling of WI-457): a LET-binding ANNOTATION launders a provider upcast past the escape gate. 'operation f(m: MemStore) -> KVStore = let s: KVStore = m  ;  s' LOADS today (verified: errs=[]) -- the let annotation widens s to the bare spec KVStore, so the returned tail leaf 's' has type KVStore == ret_sort and abstracting_return_error's same_symbol short-circuit returns None; the abstract member (KVStore.K/.V) escapes with no 'ensures'. INDEPENDENT of joins: it leaks with a direct provider value (no if/match) -- so it is NOT the WI-457 join-body vector (WI-457 walks if/match/let-BODY tail leaves; this is the let-VALUE laundering). The 'let s: KVStore = if p then diskStore else memStore ; s' join-via-let-value form leaks the same way. FIX DIRECTION: at the escape gate, see through a let-bound variable returned in tail position to the binding's VALUE (dataflow: a tail var resolves to its let value's leaves), OR treat an annotated let whose annotation upcasts a concrete provider to a bare/partial spec as itself an abstracting position. Must NOT reject a let-bound value NOT in return position, nor an ensures-vouched / manifest one. Verify wi401/wi402/wi457 stay green. Rust-only (typer). SURFACED by the WI-457 /code-review (deferred at delivery).

