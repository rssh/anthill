```anthill
fact WorkItem(id: "WI-770", created: "2026-07-19T07:58:43Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-19T10:29:40Z"))
```

## description

cpp-gen reads realization facts through HEAD-MATCHING with no bodied-rule refusal: query_realization_facts (rustland/anthill-cpp-gen/src/lib.rs:3778) maps every kb.query_view hit straight to kb.rule_head(rid), so a BODIED realization rule (e.g. rule TypeMapping(...) :- <guard>) would be read with its guard SILENTLY SKIPPED -- the trap WI-760's description recorded ('the head matches but the BODY IS NEVER EVALUATED'), still live for TypeMapping/IncludeMapping/NamingConvention after WI-760 delivered the resolve seam for realizes_effect only. Latent today (stdlib realization entries are all facts) but it is a silent wrong answer waiting: an author following WI-089's addendum direction who writes a guarded overlay rule gets the wrong host type emitted into generated C++ with no diagnostic. Surfaced by the WI-767 review, which fixed the same class in the CLI (a bodied head match must not read as an answer; the CLI now renders such matches as head :- body and resolves by default). FIX DIRECTION: in query_realization_facts, refuse candidates whose body_nodes is non-empty with a loud diagnostic naming the rule and pointing at the WI-760 resolve seam (or route them through that seam). ACCEPTANCE: a bodied TypeMapping rule in a test KB is either resolved (guard honored) or refused loudly, never silently head-matched; cargo-test green via scripts/test.sh.

