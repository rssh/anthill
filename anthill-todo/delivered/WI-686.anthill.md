```anthill
fact WorkItem(id: "WI-686", created: "2026-07-11T14:39:38Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-11T15:35:03Z"))
```

## description

smt-gen close_occ scalar-param value channel: close a body-derived entity constructor whose field is computed from a SCALAR operation parameter (not just entity params). Today anthill-smt-gen/src/lib.rs::close_occ loud-errors on a Var::DeBruijn scalar param inside a constructor ('a scalar operation parameter inside a constructor cannot be closed across the inline boundary'); WI-681 handled only entity-typed params (Vec3 whose fields are other entity params). To derive a bodied op that RETURNS a constructor with a scalar-computed field (e.g. TransponderFollowerState{ upc: ite(...scalar upc...) }), thread the callee scalar bindings (try_inline_rule_call's callee_str string-map) into close_occ so a scalar DeBruijn resolves to its value rather than erroring. ACCEPTANCE: a bodied op like f(x: Int64) -> Wrapper = Wrapper(v: if gte(x,0) then x else 0), inlined at f(?x, ?w) then ?w.v field-access, lowers to the ite over ?x; smt-gen test + cargo-test green. General beyond lf1 (constructor-returning bodies with scalar fields). Blocks the lf1 whole-step derivation follow-up.

