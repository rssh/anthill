## Attributes

- id: WI-281
- created: 2026-05-22T16:50:59Z

- status: Delivered
- status_agent: claude
- status_at: 2026-05-30T19:32:58Z

- acceptance: cargo-test

- depends_on: WI-279

## Description

Requirement-aware dot dispatch for spec-satisfaction methods (WI-278 6.6 / D6). A method coming from a spec the receiver's sort SATISFIES must resolve and thread/check its requires: (3).min(5) -> Ordered.min via fact Ordered[Int]; a.min(b) needs Ord; l.contains(e) needs Eq. find_operation_on_sort currently uses the WI-240 sort_ops table whose pass-2 covers ONLY user 'fact Spec[ImplSort]' impls, NOT builtin satisfaction (Int->Ordered) — verified Int.min -> None, so (3).min(5) does not dispatch (pinned by spec_satisfaction_method_is_a_known_gap test). Needs resolution over the satisfied-spec chain + requires insertion/checking on the produced apply, with an unsatisfied requires reported as a clear error at the dot-call span. Acceptance: (3).min(5) dispatches to Ordered.min; l.contains(e) threads Eq; unsatisfied requires -> clear error at the dot span; flip the known-gap test; cargo-test green.

## Changes

### 2026-05-30T19:30:47Z — feedback — user

LANDED (core + requires-threading). IMPL: new find_spec_op_for_provided_sort(kb, recv_sort, short) in typing.rs (after try_fire_dot_rule) walks SortProvidesInfo for the specs recv_sort provides (mirrors build_sort_ops_table pass-2 snapshot), resolves member on each spec via find_operation_in_scope; chained via .or_else into the DotApply build arm AFTER the WI-279 direct-sort fallback. Synthesized Apply{spec_op,[receiver,...args]} is IDENTICAL to the direct path, so it re-Visits and rides the existing spec-op dispatch + req_insertion — the requirement (Ordered[Int]) is THREADED by that machinery, not re-implemented. Sidesteps the WI-240 sort_ops pass-2 gap (no sort_ops_lookup dependency). TESTS (new wi281_spec_dot_dispatch_test.rs, 5 green): dispatch-via-provided-spec; (3).min(5)-shape via Int->Ordered (loads anthill-stl Int facts); requires-threaded (Comparable requires Nameable, both provided); no-match regression preserved; + a known-gap test. Full anthill-core suite green; /code-review xhigh found no new bug. ACCEPTANCE: (3).min(5)->Ordered.min DONE; requires-threading (l.contains/Eq shape) DONE; cargo-test green DONE. NOT DONE: 'unsatisfied requires -> clear error at the dot span' — a PROVIDER-SIDE unmet spec-level requires (carrier provides Spec but not Spec's own requires) passes SILENTLY today = the WI-343 gap, which owns that diagnostic uniformly for ALL spec-op calls (dot routes through the same dispatch as a plain call), not the dot path's to special-case. Pinned by dot_spec_method_unsatisfied_requires_passes_silently_wi343_gap (flip when WI-343 lands). NOTE: WI text's referenced spec_satisfaction_method_is_a_known_gap test never existed (verified) — added positive tests instead. SHARED PRE-EXISTING LIMITS (with WI-279, not introduced, follow-up): synthesis assumes receiver-typed param is FIRST; named-arg dot calls double-bind arg[0]. Status left Claimed pending the WI-343-gated acceptance item.

