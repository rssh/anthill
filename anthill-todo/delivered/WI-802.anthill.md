## Attributes

- id: WI-802
- created: 2026-07-21T13:51:13Z

- status: Delivered
- status_agent: user
- status_at: 2026-07-21T21:27:56Z

- acceptance: cargo-test

- depends_on: WI-788

## Description

The string literal "anthill.prelude.Function" is hand-compared at THREE independent sites in kb/typing.rs -- ~18226 (the cheap head gate before arrow_parts), ~19068 (inside arrow_parts itself), and ~19398 (function_spec_param_type, added by WI-788) -- with no shared predicate. A rename or namespace move of the stdlib sort fixes two and leaves the third matching nothing, and the failure mode of the third is SILENT: the Function-slot argument check simply stops firing, reverting to the no-check state WI-788 fixed, with no test failing on the recognizer itself. WI-790 set the precedent of giving the _N positional-label convention ONE owner (intern::positional_label / positional_label_index / is_positional_label_at) for exactly this reason. FIX: one is_function_spec(kb, base) -> bool predicate (or a cached Symbol / TypeHead predicate) that all three call. Found by /code-review on WI-788; both the reuse and altitude reviewers rated it low priority but flagged it independently. SMALL. ACCEPTANCE: one owner for the recognizer, all three sites call it; full workspace green via scripts/test.sh.

