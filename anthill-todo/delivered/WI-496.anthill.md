```anthill
fact WorkItem(id: "WI-496", created: "2026-06-16T17:31:16Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-16T21:12:59Z"))

fact Tag(workitem: "WI-496", name: "typing")
```

## description

Transitive dispatch of body-less spec ops + unify provision-transitivity. CONTEXT (WI-495): List keeps a redundant identity 'operation iterator(l: List) -> Stream = l' ONLY because the typer's body-less spec-op DISPATCH is direct-only: explicit 'iterator(xs)' on a concrete List (Iterable.iterator is body-less) needs a direct impl, and dispatch_spec_op_cached / the carrier-keyed provider_spec_view_bindings probe (typing.rs ~4867) match impls via DIRECT provider facts, not transitively. Make that dispatch transitive (reuse transitive_carrier_for_param: List provides Stream provides Iterable -> Stream.iterator) so 'iterator(xs)' resolves to Stream.iterator and List drops its iterator op (completes WI-495). ALTITUDE: provision-transitivity now lives in FOUR independent per-consumer walks — find_spec_op_for_provided_sort BFS, transitive_provision_view, transitive_carrier_for_param, sort_provides_reach. Evaluate ONE load-time transitive closure of SortProvidesInfo facts (synthesize 'C provides Spec2' with composed type-param AND op bindings) so every consumer (incl. body-less dispatch and WI-363 coverage) sees direct facts. compose_provision_views substitutes only BARE type-param refs today (compound kept verbatim -> loud ?_; deep substitution = WI-380 follow-up). Acceptance: List drops 'operation iterator'; 'iterator(xs)' on a List type-checks + evaluates; cargo-test green.

