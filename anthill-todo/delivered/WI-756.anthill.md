## Attributes

- id: WI-756
- created: 2026-07-18T13:47:43Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-17T14:32:24Z

- acceptance: cargo-test

- tags: wi502

## Description

Proof-path coverage for override-aware eq: verify (and test) that the direct prove_from_gamma sites decide eq over a CUSTOM-eq carrier semantically, not structurally. This is WI-573's recorded 'RESIDUAL / review Finding C', filed separately as WI-573 flagged it out of its own guarded-effect-discharge scope. BACKGROUND: WI-573's override gate lives in the typer's conjunct_refuted (typing.rs ~21503) and therefore covers ONLY the guard-refutation route (refute_guard, typing.rs:1156-1158). The other prove_from_gamma call sites do NOT pass through that gate: (1) typing.rs:5471 — WI-538 'proof .. by derivation' (Expr::Proof, tier-A inline proof over Gamma union KB); (2) typing.rs:21871 — WI-539 precondition_proved, the call-site 'requires' obligation; (3) proof_verify.rs:602 — WI-558 contract 'ensures' discharge. NOTE Finding C named only sites (1) and (3) using pre-drift line numbers ('typing.rs ~3669' is now 5471); site (2), precondition_proved, was MISSED and must be included — it is the exact positive dual of precondition_refuted (typing.rs:21893), which DOES route through the gated conjunct_refuted, so the two polarities are asymmetric today and that asymmetry is itself worth asserting on. POLARITY OF THE RISK, which differs from WI-573's: the guarded-effect hazard was an unsound REFUTATION dropping an effect that should stay; here the hazard is an unsound PROOF — a precondition deemed satisfied, or an 'ensures' conjunct deemed derivable, on a structural eq verdict the carrier's own eq would contradict. CURRENT BELIEF (must be confirmed, not assumed): the gap appears to have closed INDIRECTLY when WI-616 made eq/neq semantic (BuiltinTag::SemEq/SemNeq -> sem_eq_core, resolve.rs:4010), since all four sites resolve THROUGH the resolver and so inherit its dispatch/delay discipline rather than a typer gate. If that holds, this ticket is characterization-and-regression work, not a fix. If it does NOT hold on some path, it is a live soundness bug in the WI-538/WI-539/WI-558 proof paths and should be fixed here. THE REAL GAP TODAY IS TEST COVERAGE: nothing exercises a custom-eq carrier on ANY proof path — none of WI-538/WI-539/WI-558 carry acceptance criteria mentioning one, and wi573_eq_override_discharge_test covers only guarded effects. Note also that WI-621 refactored precondition_proved/precondition_refuted/conjunct_refuted/guarded_atom_refuted to thread Value end-to-end but changed no eq semantics. Acceptance: for EACH of the three sites, a test with a carrier whose eq override DISAGREES with structural equality (the CUSTOM_EQ_TRUE_PRELUDE / STRUCTURAL_EQ_PRELUDE fixture pattern in tests/include/wi573_eq_override_discharge_test.rs) asserts the override decides — a 'proof by derivation', a call-site requires, and a contract ensures each resolve per the carrier's eq, NOT structurally; a non-ground or buried-override operand stays UNPROVED (definite_only maps the resolver's Delay to unproven — the obligation default) rather than being proved structurally; any path found deciding structurally is FIXED, not documented; cargo-test green via scripts/test.sh.

## Changes

### 2026-08-17T14:32:14Z — feedback — user

DELIVERED. Site census first: prove_from_gamma has exactly 4 in-crate call sites and refute_guard exactly 1 caller (the WI-573-gated conjunct_refuted); none outside anthill-core. The ticket's list of 3 ungated proof-obligation sites is complete.

BELIEF MEASURED PER SITE (all three probed BEFORE any change):
* Site 2 (WI-539 call-site `requires`, precondition_proved) — HOLDS, already semantic. Four cells over one program varying only the `operation eq` member: `requires eq(c, Red)` at `needy(Green)` LOADS with `Color.eq = true` and is refused on the structural twin; `requires neq(c, Red)` at the same call is REFUSED with the override and LOADS on the structural twin. The override both admits an obligation the structural verdict would refuse AND refuses the one it would prove — the unsound-PROOF direction the ticket names.
* Site 3 (WI-558 contract `ensures`) — HOLDS, already semantic. `proof pick.ensures by derivation`, body `Green`: `ensures eq(result, Red)` Discharged under the override / Failed structurally; `ensures neq(result, Red)` the reverse.
* Site 1 (WI-538 in-body `proof … by derivation`) — DID NOT HOLD, and the defect was neither of the two the ticket predicted: it decided NOTHING over constructor operands. Not structurally either (so no unsound proof), but `conclude eq(Green, Red)` never discharged and neither did the reflexive `conclude eq(Red, Red)`. FIXED.

ROOT CAUSE: site 1 was the only prove_from_gamma site handing the resolver a RAW source occurrence; the other two build their goal from a clause TERM and so already crossed the goal-lowering boundary. Op-body lowering wraps EVERY bare identifier as `var_ref` — parameters and nullary constructors alike — and only that boundary separates them (WI-592: a `var_ref` naming a CONSTRUCTOR is a closed datum ⇒ `Ref`). Off the raw occurrence, resolve.rs's force_delay (value_has_open_world_ref → occurrence_has_var_ref) read `Red` as a runtime binder and delayed; definite_only mapped that to unproved. WI-592's fix recurring one CARRIER over — occurrence_has_var_ref's doc claims symmetry with term_has_var_ref, and the symmetry held only because the term side never SEES a constructor var_ref.

NOT FIXED BY NARROWING THAT GATE: the operands' VIEW would still head as `var_ref`, so eq's dispatch index would miss the carrier and a custom-eq carrier would then be decided STRUCTURALLY — a wrong answer where there had been a conservative non-answer. Recorded at occurrence_has_var_ref and at the WI-592 arm.

THE SECOND HALF, found by /code-review and confirmed: Γ has two sides, and I had changed only the QUERY side. Γ's PRODUCERS still carried raw source occurrences, and the overlay is consulted STRUCTURALLY ahead of the builtin, so a fact spelling `Red` as `var_ref` cannot discharge a goal spelling it `Ref`. MEASURED: this was NOT merely a consequence of the goal change — two of the three producer channels were already broken for constructor operands, in programs with no proof in them at all. With `goal_form` backed out of FlowEnv::assume: `if eq(c, Red) then needy(c)` (needy requires eq(c, Red)) is REJECTED, and `let d = Red; needy(d)` is REJECTED, while the identical `if` over an Int64 literal — the shape WI-539 already tested — loads. The match-arm channel was already correct (its fact builder spells the pattern constructor as a closed datum). FIX: FlowEnv::assume normalizes every fact through the same goal_form (recursing into carrier children, since a `let`/`match` fact carries its operands as children), so producers and consumers share ONE vocabulary by construction rather than by agreement. assume now takes &mut KnowledgeBase.

FIXTURE NOTE: `eq = true` is the ONLY discriminating override shape. sem_eq_values answers REFLEXIVITY before any dispatch (an Eq law), so an override disagreeing on IDENTICAL operands is unobservable; the discriminating shape is DISTINCT operands the carrier calls EQUAL.

ASYMMETRY ASSERTED (the ticket asked; WI-755 owns the fix and is named at the site): a rule-body call `needy(Red)` with `requires neq(c, Red)` is REJECTED on the structural carrier and ACCEPTED verbatim once the carrier gains `operation eq`. precondition_proved declines it semantically either way, but precondition_refuted routes through the WI-573 pre-gate, which suspends every eq-family conjunct over an overriding carrier, so a definite violation reads as an undetermined float.

CONTROLS MEASURED, EACH HALF BACKED OUT ON ITS OWN (26 rows): raw `conclude` goal ⇒ 4 fail (custom_eq_in_body_proof_discharges, structural_carrier_in_body_reflexive_proof_discharges, the discharging half of buried_override_in_body_proof_stays_unproved, an_in_body_proof_reads_the_gamma_its_producers_write); no `goal_form` in assume ⇒ 3 fail (if_premise_over_a_constructor…, let_bound_constructor…, and again an_in_body_proof_reads_the_gamma…, the one row that fails under EITHER because what it measures is the two halves agreeing). Separately: swapping the asserting occurrence_to_term back in makes exactly a_conclude_that_is_not_a_goal_shape_stays_unlowered_and_unproved panic (a `conclude` takes a full `_term`, which admits an args-bearing dot), which is why the total variant is used.

WHAT NOTHING PINS, stated rather than implied: that the typer's Expr::Proof arm CALLS in_body_proof_goal. Reverting that line leaves the workspace green (measured by the reviewer), and no test can close it behaviourally — the site's only effect is assumeing a conclusion into a Γ that already derives it (Γ ⊆ KB ∪ Γ there). An earlier draft of the test header claimed wi538_local_proof_test owned that wiring; it does not, and the header now says so.

ALSO FROM THE REVIEW: the goal is now built INSIDE the `derivation` arm (every other strategy discards it, and building it interns the conclusion's term twin for the KB's lifetime); a `⊥` lowering is treated as "not a goal" like the None arm; and wi733_relation_head_and_tail_decompose_the_stream now reads both drains off ONE interpreter and asserts `tail == all[1..]` positionally — its fixture comment claimed exactly that while the test compared two loads as sets, which a `tail` dropping the LAST row would also have satisfied.

Tests: rustland/anthill-core/tests/include/wi756_proof_path_eq_override_test.rs, 26 rows, registered in wi_tests.rs. Docs: 050 OQ-A ("fact representation") resolved to the one-vocabulary rule; local-proof.md's boundary requirement and its stale "Γ is write-only / refute_guard has no caller" deferral. Full workspace green via scripts/test.sh.

