```anthill
fact WorkItem(id: "WI-866", created: "2026-07-30T14:47:57Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-19T19:12:09Z"))

fact Feedback(workitem: "WI-866", author: "claude", at: "2026-08-19T19:11:58Z")
```

## description

WI-857 FOLLOW-UP — the dictionary layout is COUNTED independently of what the producer PRODUCES, and one of its inputs is derived with a silent fallback. TWO gaps in the invariant WI-857 established. (1) dict_layout(spec, provider).arity() = |direct_requires_chain(spec)| + |direct_requires_chain(provider)|, while the list itself is built by dict_sub_goals (two different functions: provider_requires_subgoals for the spec half, candidate_provider_sub_goals for the provider half). The two are related only by a 1:1 slot discipline neither signature states, bridged by a debug_assert_eq! at the producer — which is RELEASE-OFF, for an invariant whose violation is a wrong-slot read rather than a crash. It is not theoretical: that assert is exactly what caught the EffectsRuntime anchor being dropped from the provider half while the layout counted it (144 dictionaries measured). FIX: have dict_sub_goals RETURN the layout it actually built (from_halves(spec, provider, spec_goals.len(), provider_goals.len())), keeping dict_layout as the 'predict without a goal' face for expand_dispatching_dict and stand_in_requirement, documented as asserted-equal. Then the NUMBER has one source of truth, not just the ORDER. CONSIDER ALSO a producer-side check at build_construct_requirement (kb/typing.rs), through which all four term-side producers funnel — it would need the spec passed alongside the functor, and it would move enforcement off the last consumer in the chain, which is where WI-857's failure lived. (2) expand_dispatching_dict derives the layout's SPEC from impl_parent_of_op(dispatched_from).unwrap_or(provider), commented as covering a namespace-level dispatched_from. impl_parent_of_op is qn.rsplit_once('.') + try_resolve_symbol(parent), so it ALSO returns None for a dot-less top-level op QN (which _global declarations produce) and for any dotted op whose parent segment is not a registered symbol — and in those cases the layout silently degrades to the one-list reading, which is the PRE-WI-857 BUG SHAPE, rather than complaining. FIX: pass Option<Symbol> and make 'no spec' an explicitly asserted case (the caller knows whether it holds a parent-bundle dict), or debug_assert that an unresolvable parent implies dict.functor() is the frame owner. ACCEPTANCE: the layout's count comes from the producer; a synthetic divergence is caught at the producer, not at a frame push; full workspace green via rustland/scripts/test.sh.

## Feedback

### 2026-08-19T19:11:58Z — claude

DELIVERED, with one of the ticket's two premises MEASURED WRONG and one gap found one producer over.

GAP (1) — the layout's count now comes from the producer. `DictLayout::from_halves` builds the layout from the two half-lists `dict_sub_goals` emitted; `dict_layout` stays the predict-without-a-goal face. The bridge is `divergence_from`, which compares BOTH HALVES: the pre-ticket `debug_assert_eq!` compared arities, and a produced (2,3) against a predicted (3,2) passes an arity check AND both of `expand_dispatching_dict`'s own guards while every frame slice reads the other half's slots. The check is ALWAYS ON (the ticket's own objection to release-off) and has one owner, `check_against_prediction`.

`(goals, usize, mask)` became `DictSubGoals`, because that usize answered TWO QUESTIONS: the layout's `spec_len` (which slice an owner reads) and the producer's `provider_half_start` (where the strictness mask and pin `chain_index` start). They differ in the self case — one list, two encodings — and a single name served both by accident of the non-self case being common. `from_halves` folds the self case so the two encodings compare equal.

MECHANISM DECISION, recorded at the site because a reviewer asked: the raise is a `panic!`, not a `ResolutionResult` arm. Grounds: the condition is programmer error, not input (both halves are built 1:1 with the very chains the prediction counts, each KEEPING its slot on the unreadable-head branch), and `ResolutionResult` is a semantic verdict that 27 match arms here plus three test files translate into user prose. Same trade, same settlement, as `term_view`'s reflect-key reads.

GAP (2) — the ticket is WRONG about which shapes the `unwrap_or` served, and wrong that an unresolvable parent should raise. MEASURED:
  * `Holder.cmp` (dotted, parent is a Sort) -> the sort.
  * `wi866.shapes.nsLevel` (dotted, parent is a NAMESPACE) -> the namespace RESOLVES, so this arrived as `Some(namespace)` and NEVER reached the `unwrap_or` — though the comment there claimed it. A namespace was named as a dictionary's spec: identical arity and identical `slots_for` under both readings, wrong only in `DictLayout::describe`, which is the one sentence a reader gets when the arity guard fires.
  * `topLevel` (DOT-LESS canonical name) -> the shape the `unwrap_or` actually served.
  * UNRESOLVABLE PARENT is NOT an inconsistent KB. It is the WI-234 Model 1 dispatch form (`test.wi223.dispatch_form.Spec.foo`, whose `Spec` is deliberately never registered). Raising on it took `wi223 apply_within_with_requirement_dispatch_resolves_via_handle_functor` red. It is now a NAMED arm that still takes the one-list reading; what refuses a genuinely wrong one is downstream and was already loud.
`DispatchSpec` + `or_provider` gives the collapse ONE owner, exhaustively matched, so the two readers (`expand_dispatching_dict`, `requirements_for_value_directed_impl`) cannot drift.

THE GAP THE TICKET POINTED AT AND I ALMOST MISSED — its "CONSIDER ALSO ... the funnel through which all four term-side producers go" was right, and the defect there is LIVE. `build_dispatching_dict_from_chain`'s `None => {}` arm SILENTLY DROPPED a dep that failed to project and kept building, emitting a dictionary SHORTER than its own layout — the exact WI-857 shape, at the producer WI-857 did not touch. MEASURED with a probe: 2434 of 100641 emitted dictionaries were short (2401 EMPTY where 1 slot was wanted — `anthill.prelude.PartialOrd` alone 2101, `SortedSet` 130 — 25 empty where 2 were, and one PARTIAL, `wi817.dsets.BOps` 1 of 2). All on the `require_complete = false` route, whose term is diagnostic-only (`req_insertion.rs` says so), which is why a green suite carried them: an EMPTY bundle is indistinguishable from a legitimately chain-free one, so each entry told a reflection consumer "this call needs no requirements" exactly where none could be built. Fixed by applying the rule the function's own `require_complete` arm already states ("else fall back to no dict") to the path that was exempt, and the check now runs there too.

WHAT NOTHING COVERS. A produced/predicted divergence cannot be driven by any program — both halves walk the chains the prediction counts — so it is driven SYNTHETICALLY against `divergence_from` (`wi866_a_swapped_split_is_a_divergence`), and that is said at the site. Likewise the self case NEVER REACHES `dict_sub_goals`: 480537 reaches across `wi_tests`, ZERO self-provided, so `from_halves`' fold is what keeps the FIRST such dispatch from raising a false divergence rather than something a fixture protects. I predicted the fold's back-out would fell "a large part of the suite" and it fells exactly one row — its own; that prediction was wrong and the number is recorded.

BACK-OUTS, each measured: halves->arity-only fails the swap row alone; the pre-ticket spec derivation makes the nsLevel row answer `wi866.shapes`; restoring `None => {}` fails the population census with `` `wi866.shapes.Holder`: 0 slot(s) emitted, layout wants 2 ``. The end-to-end two-half dispatch passes either way BY DESIGN and says so, and a probe showed it reaches the changed derivation exactly twice, both `Spec(..)` — `nsLevel`/`topLevel` are requires-free and carry no dictionary, so the spec-less arms are covered by the wi223 file, not by any WI-866 fixture. Said at the site rather than credited to a neighbour test.

TESTS: 2 unit rows in `kb::typing::wi866_dict_layout_agreement_tests` (synthetic divergence + the self-case fold), 5 integration rows in `wi866_dictionary_layout_source_test` (the shape table, the namespace-describe claim, the WI-234 form, the live two-half dispatch, and a POPULATION census over every dictionary the load emits — the defect lived in the stdlib, not in a fixture).

Full workspace green: 35 binaries, 5269 tests, 0 failures. `/code-review high` run; all six findings acted on (two mediums: the panic mechanism is now a recorded decision at one owner, and the second producer is covered; four lows fixed).

