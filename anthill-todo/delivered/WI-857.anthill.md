```anthill
fact WorkItem(id: "WI-857", created: "2026-07-30T06:57:01Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-07-30T13:36:42Z"))

fact Feedback(workitem: "WI-857", author: "user", at: "2026-07-30T10:32:35Z")

fact Tag(workitem: "WI-857", name: "modinst")

fact Feedback(workitem: "WI-857", author: "user", at: "2026-07-30T13:36:32Z")

fact Feedback(workitem: "WI-857", author: "user", at: "2026-07-30T14:43:31Z")

fact Feedback(workitem: "WI-857", author: "user", at: "2026-07-30T14:44:04Z")
```

## description

A requirement DICTIONARY built from a CARRIER-KEYED provider bundles NO sub-requirements, so any spec whose OWN `requires` chain is non-empty dies at eval with an arity mismatch. MEASURED 2026-07-30 (`anthill run`, HEAD + WI-844), and reproducible with ZERO proposal-058 vocabulary — no named slot, no second provider, no bracket: `sort Holder { sort T = ?  requires Eq[T]  operation same(a: T, b: T) -> Bool = PartialEq.eq(a, b) }` called as `Holder.same(7, 7)` loads clean and dies `Internal(DeferToRequirement: projection index 0 out of range (requirement `__req_eq` bundles 0 sub-requirement(s)))`. The `Ordered` twin — `requires Ordered[T]`, body `Ordered.compare(a, b)`, called at Int64 — dies `Internal(deferred dispatch frame-push: dispatching dict for anthill.prelude.Ordered.compare has arity 0 but its requires chain has 2 entries)`. CONTROL, so the trigger is the SPEC's chain depth and not the shape: the same Holder over `PartialEq` (whose own requires chain is EMPTY) RUNS, printing `PE-false`. SECOND CONTROL, so it is the PROVIDER KIND and not the spec: with a WITNESS sort supplying the op (`sort Ascending { fact Ordered[T = Int64]  operation compare(a, b) = sub(a, b) }`) the identical program runs — because dispatch then lands on `Ascending.compare`, whose parent's requires chain is empty, so `expand_dispatching_dict` asks for 0 names and gets the 0-arity dict. ROOT CAUSE (read, not inferred): `candidate_sub_goals_owned` (kb/typing.rs) builds a resolved node's sub-goals from `direct_requires_chain(impl_sort)` — the PROVIDER's own chain. For a carrier-keyed `fact Ordered[T = Int64]` the provider is `anthill.prelude.Int64`, whose chain is empty, so `resolve` returns a `Leaf` and `emit_tree_as_projection` emits `construct_requirement(Int64, [])`. But the CONSUMER, `expand_dispatching_dict` (eval/eval.rs), names the frame slots from `synth_req_names(impl_parent_of_op(target))`, and when the target is the spec's OWN op (the builtin `Ordered.compare`, no impl member) that parent is the SPEC — whose chain is `Eq[T], PartialOrd[T]`. Producer walks the provider's chain, consumer walks the spec's; they agree only when the provider IS a witness sort. The information the producer needs already exists and is already checked: `check_provider_requires` (WI-343/WI-356) verifies that every SPEC-level `requires`, instantiated at the provision's bindings, resolves — it just is not put in the dictionary. FIX DIRECTION (settle at pickup, both faces touch the WI-239 'direct requires' arity invariant): either the producer bundles the SPEC's chain at the goal's bindings when the provider contributes none, or the consumer names from the PROVIDER when there is one. Whichever is chosen, the two owners must be made to read ONE chain — that they read two is the defect. IMPACT: today this is why `SortedSet.empty[T = Int64]()` (proposal 058 §5.3's 'omitting the binding still works' spelling) loads, resolves by tier 2, and then dies at eval — pinned in `wi844_sorted_set_driver_test::omitting_the_ordering_is_resolved_but_dies_at_eval`, which asserts the CURRENT failure and its 058-free control so this ticket has a live reproducer. Not caused by 058: it predates the whole arc and needs none of it. SCOPE: Rust — kb/typing.rs `candidate_sub_goals_owned` / `emit_tree_as_projection`, eval/eval.rs `expand_dispatching_dict` / `start_apply_deferred`. ACCEPTANCE: the two Holder reproducers above RUN; the PartialEq and witness-sort controls still run; `SortedSet.empty[T = Int64]()` with no `O` written sorts genuinely (the WI-844 test flips from asserting the failure to asserting the value 3); full workspace green via rustland/scripts/test.sh.

## Feedback

### 2026-07-30T10:32:35Z — user

SCOPE EXTENDED by the 058 third review (2026-07-30; docs/proposals/058-modular-instances.md §4.5 correction, §5.4, §9 phase 6; brainstorm prelude-multiple-orderings.md addendum; probes q4a-q4g, anthill load/run at HEAD). Three additions, each measured. (1) The ticket's SECOND CONTROL ('a witness sort supplying the op runs — the parent's requires chain is empty') is the ONLY witness configuration that runs: a PARAMETRIC, chain-bearing witness dies identically. Minimal reproducer (q4e): enum Duo { sort A = ?, sort B = ?, entity duo(l: A, r: B) } providing PartialEq/Eq[T = Duo] componentwise, plus witness LexFst { sort A = ?, sort B = ?, requires Ordered[T = A], fact PartialOrd[T = Duo[A = A, B = B]], fact Ordered[T = Duo[A = A, B = B]], compare calling Ordered.compare(al, bl) } — LOADS clean (check_provider_requires discharges the Eq leg from the carrier's provides and the PartialOrd leg from the witness's own fact), then dies at eval on BOTH routes (σ-pinned O = LexFst through SortedSet's named slot, and direct searched Ordered.compare on two duos): 'dispatching dict for anthill.prelude.Ordered.compare has arity 0 but its requires chain has 2 entries'. The count STAYS 2 when the witness's own chain is cut to 1 (q4f), so the frame is named from the SPEC's chain even though the provider is a witness — plausibly the op-target lookup misses the PARAMETRIC member and dispatch falls to the spec builtin, whose parent is the spec; read the internals at fix time, the boundary is measured. CONTROLS: the ground, chain-free, slot-free-body bundle (q4g) RUNS (-1); and the SAME parametric+chain shape over a spec whose OWN chain is EMPTY (local Sized, q4a) threads the witness's conditional chain end to end (3+6=9) — so the trigger stays the spec's chain depth, now measured on the witness route too. ACCEPTANCE ADDITION: the q4e reproducer runs. (2) The settlement must include the LOCALITY rule: when constructing provider W's dictionary, a sub-goal that W itself provides resolves to W's OWN provision before global search. Reason: the lawful form of an alternative Ordered witness is a PartialOrd+Ordered BUNDLE (ordered.anthill:44-48 derives the inherited gt/lt from compare, so a lone witness's dictionary contradicts itself — 058 §5.4), and once the producer bundles the SPEC's chain, PartialOrd[carrier] has TWO candidates inside each of two coexisting witnesses' chains; the only right answer is witness-local. The rule depends on the SELECTED provider, never on caller scope, so 058 §4.2's import-coupling objection does not touch it. (3) IMPACT RE-SCOPED: this ticket gates the ENTIRE 058 shipped-library goal — §9 phase 7 (two orderings in the prelude, the WI-844 instruction) and phase 8 (rung 2a: an unselected dispatch routes into carrier-keyed dictionaries) — not merely the omitted-O spelling. 058 §4.5 now records the same finding from the pin side: step 0's ResolvedTree::leaf is right only for a CHAIN-FREE witness; the chain-bearing form must yield the CONDITIONAL node with sub-goals resolved under σ. Probes preserved in the session scratchpad (q4a-q4g); pin as tests at fix time. NOTE: anthill query -p diverges from anthill load (sort-level member imports refused; panics with the bindings dir) — drive reproducers via anthill run + a Main sort, not query.

### 2026-07-30T13:36:32Z — user

DELIVERED 2026-07-30. SETTLEMENT: one layout, one owner — `dict_layout` (kb/typing.rs), stated in design/operation-call-model.md §'Dispatch rule' and CLAUDE.md. A dictionary for spec S supplied by provider P bundles S's own direct requires chain THEN P's; P == S is ONE list (which is also the parent-bundle dict, whose functor IS the frame owner, so `build_dispatching_dict_from_chain` is unchanged). The SPEC half is the PREFIX so `requirement_at_sort`'s projection path — whose k indexes the required spec's own chain — needs no offset and no knowledge of the provider. `expand_dispatching_dict` takes a new `dispatched_from` (the classification's pre-existing `spec_op_sym`) and hands the frame exactly the half `impl_parent_of_op(target)` owns; when that is a THIRD sort (an inherited same-short-name default, a WI-431 binding elsewhere) it gets no slots, loud if it declares any. THREE THINGS THE TICKET DID NOT ANTICIPATE, each forced by measurement. (1) The spec half CANNOT be a hard demand: requiring it to resolve broke 33 TESTS, because a spec-level requires is routinely satisfied only via `check_provider_requires`' abstract fallback (base-level existence) — `FiniteCollection requires Iterable[C = C]` holds for a List only through `List provides Stream provides Iterable`, which no `Iterable[C = List[…]]` provision matches. So an unresolved spec-half goal is RECORDED (`ResolvedRequiresNode::Unavailable` -> an empty bundle over an `anthill.reflect.NoProvider` marker), never dropped (would shift the provider half) and never refused; dispatching through the marker is refused at ONE owner (`resolve_op_target_checked`). Consequence for a fixture: a GROUND unprovided spec half is unreachable — the load check refuses it — so the `Unavailable` pin needs the parametric shape where the abstract fallback fires. (2) EVERY producer must build a layout-valid dict, and the ticket's SCOPE named neither of the two that were not: the host-entry STAND-INS (`seed_entry_requirements`, `call_with_requirements`' `__req_self`) were `cr(functor, [])`, which the layout reads as 'claims functor's whole chain, bundles none'. Found by wi818's value-directed rescue FAILING, not by reading. Now `stand_in_requirement`. (3) The `EffectsRuntime` kind-anchor was SKIPPED by the provider half while `build_dep_projection` kept it and the layout counts it — MEASURED 144 dictionaries across the suite disagreeing (Iterable-over-Stream alone = 140), invisible because none reached a frame push. Both halves now keep the slot as a structural leaf; `effects_runtime_sym` is its owner. A `debug_assert_eq!` at the producer now pins producer == layout; it is what caught this. LOCALITY delivered as review required (`resolve_inner`'s `local_provider`, immediately-enclosing provider only). ACCEPTANCE: all met — both Holder reproducers run (Eq->PartialEq transitively = true/false decided; Ordered.compare at Int64 = 1); the PartialEq and chain-free-witness controls still run; q4e runs on both routes (-1, and a tie-break arm = 1 that a mis-sliced half would silently pass); `SortedSet.empty[T = Int64]()` sorts genuinely (wi844 test flipped to == 3); full workspace green 3777/0. Pinned in `wi857_dictionary_layout_test.rs` (10 tests). REVIEW FOUND, all fixed: `is_effects_runtime` narrowed the qualified-NAME compare it replaced to a raw == (would re-open the same arity gap); the reflect `Dictionary.resolveOp`/`ops` faces bypassed the marker refusal, drifting from `resolve_op_target` in the exact direction its own doc forbids; the spec half still dropped an unreadable-head slot the provider half kept. OPEN / DEFERRED, in priority order: (a) `type_params_of_sort` (kb/mod.rs) is an O(|symbols|) scan called per resolved goal, and this ticket multiplies goals ~5x (the Ordered->Eq/PartialOrd->PartialEq diamond resolves PartialEq twice) — MEASURED suite 219s -> 235s; memoize it on the `sort_param_pairs_cache` pattern, and consider a per-resolve memo keyed on (SortGoal, local_provider) — local_provider MUST be in the key. (b) `Unavailable` discards the failure KIND, so a TIE reports as 'no provider'; carrying it needs the reason in the handle, i.e. an arena-slot variant (`SlotFunctor::NoProvider { spec }`), which would also give the diagnostic the bindings it claims to name. (c) the layout is COUNTED independently of what the producer produces (bridged only by the debug_assert); have `dict_sub_goals` return the layout it built. (d) `alloc_requirement` is the public host constructor and is layout-blind — `call_with_requirements` now validates each supplied dict, but a layout-aware constructor should be exposed and the doc repointed. (e) the σ short-name re-key allocates Strings per resolved goal, now on a per-dispatch path. (f) 058 §7's unmeasured composition leg (`O = ListOrd[OE = LexFst]`) is now drivable — phase 7 should drive it.

### 2026-07-30T14:43:31Z — user

CODE REVIEW (/code-review, post-delivery) FOUND ONE REAL REGRESSION AND FOUR REAL GAPS — all fixed, suite 3778/0. (1) REGRESSION, the one that mattered: an OpRef minted by Dictionary.resolveOp carries op = the RESOLVED impl member while its dict witnesses the SPEC, so the OpRef apply path (which passed (op, op) to expand_dispatching_dict) measured a spec-instance dictionary against the PROVIDER's chain — for a chain-free witness that is 0, so a valid arity-2 Ordered dict was rejected 'wants 0 slot(s)'. Pre-WI-857 it matched by accident (dicts bundled only the provider's chain). FIX: Value::OpRef gained a  field — the op the call NAMED, None for an eta (whose captured dict is its own parent's bundle, so spec == provider). The compiler found all four construction/match sites. Pinned by wi577_dictionary_test::resolve_op_remembers_the_named_spec_op. LESSON: I checked the reflect faces for the marker guard and NOT for the layout — the same file, one line apart. (2) Dictionary.ops' per-element marker refusal could never fire, because a marker's sort_ops_for_impl is EMPTY: a no-provider dictionary answered  = 'this dictionary has no operations'. MY OWN COMMENT SAID SO ('the same silence, one level quieter') and I shipped it anyway — refusal moved up front. (3) reduce_requirement_at_sort_node / eval_requirement_chain_node called RequirementHandle::project with NO bounds check, so projecting into a marker (arity 0) hit the arena's panic! instead of an EvalError; only start_apply_deferred's proj_path loop had the guard, and markers made it newly reachable. FIX: one owner, project_requirement, used by all three. (4) All three refusals were EvalError::Internal, which the SLD bridge and simp_rewrite debug_assert AGAINST — resolve.rs says so explicitly and records that AmbiguousRequirement (WI-855) and AmbiguousSpecOpDispatch (WI-842) got their own variants for exactly this. A bridged rule dispatching through a marker would have ABORTED the test binary instead of delaying. FIX: EvalError::UnpinnedRequirement, the third member of that family. (5) The bindings-free SortGoal I pushed for an unreadable  head is RESOLVABLE, so it could raise a NEW load error in check_provider_requires (which shares the walk) and hard-fail a dispatch in the provider half — my comment justified it as 'harmless, that check does not index', which is the wrong reason (resolving, not indexing). MEASURED the branch ZERO times across the suite; kept the slot for positional exactness and made the guess a debug_assert!(false) so it is loud if it ever becomes reachable. PLUS six comments of mine that had gone FALSE and were corrected — the worst: inserting resolve_op_target_checked between resolve_op_target's doc and its body silently TRANSFERRED the whole doc block, leaving the pub API undocumented and the new fn carrying two unrelated contracts. Also: resolve_bridge_requirements' doc still asserted the EffectsRuntime skip this ticket removed; seed_entry_requirements' doc still described the empty stand-in the same commit made invalid; the FieldSymbols doc claimed ONE identity channel for the marker while the hot path still compares by name; the marker's 'nothing can declare it' claim is true for the wrong reason (intern vs define maps are disjoint; the name is NOT reserved); and the apply_within comment stated a -slot convention that has TWO producers with opposite conventions (safe only because that arm is rebuild-only today — now stated conditionally). Review findings already covered by filed follow-ups: the discarded failure kind (WI-865), the String allocs (WI-864), and the debug-only producer/layout assert (WI-866).

### 2026-07-30T14:44:04Z — user

CORRECTION to the code-review entry above: three backtick-quoted fragments were eaten by shell command substitution when it was written (a footgun: pass feedback text in SINGLE quotes, or the shell evaluates anything in backticks). The three: (1) "Value::OpRef gained a  field" should read "gained a named: Option<Symbol> field"; (2) "a no-provider dictionary answered  = this dictionary has no operations" should read "answered nil ="; (3) "the apply_within comment stated a -slot convention" should read "stated a fn-slot convention". Nothing else was lost.

