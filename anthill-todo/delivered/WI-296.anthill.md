## Attributes

- id: WI-296
- created: 2026-05-23T16:31:09Z

- status: Delivered
- status_agent: user
- status_at: 2026-05-23T16:30:28Z

- acceptance: cargo-test

## Description

Resolver: open_debruijn_node must handle child-bearing control-flow Expr forms at rule-body atom positions (currently panics). PROBLEM: open_debruijn_node (rustland/anthill-core/src/kb/node_occurrence.rs:614) opens Apply/Constructor/Instantiation/HoApply by remapping DeBruijn->Global in their children, but its catch-all arm debug-asserts that any other child-bearing Expr cannot occur at a rule-body atom position (line 651). It CAN: reflection/typing rules legitimately match expression structure as data, e.g. synth(...) :- occurrence_term(?e, lambda(param:.., body:?b)), ... The materializer (visit_fn) elaborates reflect-Expr functors lambda/let_expr/if_expr/match_expr into Expr::Lambda/Let/If/Match, and opening the rule body for resolution (occurrence/typed path, post-WI-283) hits the assert and panics. Reproduced: anthill query of anthill.reflect.typing.synth with --resolve panics (Discriminant 6 = Lambda). TASK: extend open_debruijn_node to open the children of ALL child-bearing Expr variants (Match/If/Let/Lambda/DotApply + the *_within elaboration forms), mirroring the Apply/Constructor arms (open each child position, rebuild only if changed); either explicit per-variant arms or a generic map_children mirroring for_each_child (node_occurrence.rs:531). Drop the catch-all panic for child-bearing forms; keep it only for genuine leaves. ACCEPTANCE: the synth query no longer panics (opens cleanly, even if it then delays on the unimplemented occurrence_term); a test opening a rule whose body matches lambda/match_expr resolves without panic; cargo-test green. SCOPE: precondition for running the self-hosted typing relation (WI-079); Rust-only.

