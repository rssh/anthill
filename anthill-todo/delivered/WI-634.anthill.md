## Attributes

- id: WI-634
- created: 2026-07-04T20:06:44Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-05T11:19:26Z

- acceptance: cargo-test

- tags: typing

## Description

simp/eq-rewrite: instantiate_eq_rhs drops query-var links — the WI-624 twin in the eq-rules path (CONFIRMED by the WI-624 /code-review verify pass; pre-existing, untouched by WI-624's fix pair). instantiate_eq_rhs (resolve.rs ~2453-2470) threads ONLY synthetic u32::MAX-n entries into its RHS rename; a query-var -> Var(DeBruijn n) link entry (recorded when a query var meets an LHS var position, discrim.rs:611-613 Path extraction) is never consumed. Consequences: (a) '[simp] pick(?a,?b) = ?a' firing over redex pick(?q, 7): no synthetic entry exists for DeBruijn 0 in that direction, the opened RHS fresh global stays disconnected, and the rewritten goal SEVERS ?q — whatever solves the rewritten goal never binds the caller's var (reachable: step_init ~940-956 fires eq rewriting on any term goal when candidates are all-eq; no groundness gate — the 2310-2357 gates are tag/requires/typed-bounds only). (b) reify(rhs, &tree_subst) at ~2384 chases a query var through the raw q -> Var(DeBruijn n) entry and leaks an unopened DeBruijn — but only into EqChange.rewritten (the change RECORD, 2386-2390); the goal continuation uses rhs/final_term, so (b) corrupts provenance not resolution. (c) the rename walks iter_terms() only, so a Value-bound synthetic entry (non-Term carrier) is silently skipped — same class as WI-635-to-be / with_fresh_vars gap. NOTE the missing-is_contradiction claim from the same review was REFUTED: query()/query_view already drops contradictory matches (kb/mod.rs:2282-2284) before apply_eq_rules sees them. Fix direction: mirror WI-624's with_fresh_vars pass structure in instantiate_eq_rhs (build the synthetic rename first, then thread query-var links through it — or refuse to fire on redexes whose var links the rename cannot express, a loud gate per the loud-over-silent principle).

## Changes

### 2026-07-05T07:38:15Z — feedback — user

WI-633 review surfaced the load-bearing distinction for this twin: resolve_leaf is shared by SLD resolution (query_resolved_value, unify-on-rebind) AND one-directional matching (query_resolved -> match_view, structural-identity-on-rebind, drives typer simp_rewrite::try_fire + assumed_facts discharge + external rows). WI-633 threaded a unify_rebind flag so only the SLD path unifies. apply_eq_rules is the RESOLVER's simp entry (unify path) and is already guarded against inexpressible query-var/unify-produced entries (the WI-633 loud gate at resolve.rs ~2355). The typer's match_view simp path needs NO guard because matching (unify_rebind=false) already refuses to fire a nonlinear LHS on distinct-but-unifiable subterms. So WI-634's remaining completeness work is narrower than filed: instantiate_eq_rhs need only thread the SYNTHETIC-entry rename it already builds PLUS any query-var link the guard currently skips; the match_view path is out of scope. Regression locked by match_term_nonlinear_is_matching_not_unification (kb/mod.rs) + apply_eq_rules_skips_{severing_var_redex,inexpressible_query_var_links} (kb/resolve.rs).

