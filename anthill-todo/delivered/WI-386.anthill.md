```anthill
fact WorkItem(id: "WI-386", created: "2026-06-04T17:06:41Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-06-04T17:27:44Z"))

fact Feedback(workitem: "WI-386", author: "user", at: "2026-06-04T17:06:20Z")

fact Feedback(workitem: "WI-386", author: "user", at: "2026-06-04T17:27:43Z")

fact Tag(workitem: "WI-386", name: "typing")
```

## description

Typer+loader: written effect rows (E = {}) as first-class type-args in ALL binding positions (provider facts), + cross-sort provider-view E admissibility -- the PREREQUISITE for WI-380's written-E producer signatures. Surfaced implementing WI-380: 'iterator[Elem](l: List[T=Elem]) -> Stream[T=Elem, E={}] = l' is blocked by TWO gaps. GAP 1 (loader): WI-375 admitted a written row (E={} / E={Modify[c]}) in OPERATION-RETURN type position, but NOT in a FACT-HEAD SortBinding -- 'fact Stream[T = T, E = {}]' fails to load with "unresolved name '{}'" (span 0..0). The WI-302 binding-value classifier (load.rs, the remap_symbol_strict path ~4374/4390) stringifies the parsed TypeExpr::EffectRow to a name '{}' instead of lowering it via the existing effects_rows machinery (build_instantiation_term EffectRow arm load.rs:1187; lower_effect_row :6140; build_canonical_effects_rows). FIX 1: handle TypeExpr::EffectRow in the fact-head/SortBinding value lowering (route to the effects_rows lowering, as op-return does); disambiguate row-vs-set_literal by the bound param's kind (WI-375 note). GAP 2 (typer): parameterized_compatible_view (typing.rs ~9382) rejects a CROSS-SORT actual against an expected carrying a param the actual lacks ('None => return false' ~9449), instead of consulting the provider fact to supply it. So 'List[Elem]' is NOT <: 'Stream[T=Elem, E={}]' even when List provides 'fact Stream[T=T, E={}]' -- T matches only because List and Stream share the short symbol T; E is dropped. FIX 2: in the cross-sort None arm (actual_base != expected_base AND actual provides expected), consult provider_spec_view_bindings(actual_base, expected_base) for the missing expected param (matched by short name) and check its supplied value against the expected binding. LOOSENING change (cross-sort only; can't newly-reject). Both together let a pure carrier WRITE 'E = {}' on its Stream provision+return so the row is IN the type (the WI-380 written-row discipline / sound for effectful carriers too). ACCEPTANCE: 'fact Spec[..., E = {}]' loads; a carrier providing 'Spec[E={}]' conforms when its body is returned as 'Spec[E={}]' (cross-sort); the WI-375 op-return forms still load; full anthill-core green. UNBLOCKS WI-380 (write E on iterator/collect producers -> length(collect(List.iterator(xs))) pure, WI-368). DEPENDS WI-375 (delivered), WI-379 (delivered). BLOCKS WI-380. Rust-only (typer+loader).

## Feedback

### 2026-06-04T17:06:20Z — user

REFINED DIAGNOSIS after implementation attempt (reverted to clean; WI-379 baseline green). The 'written E on a cross-sort carrier' needs THREE interacting changes, and the naive path CASCADES into delivered WIs: (1) FIX 2 cross-sort provider-view in parameterized_compatible_view (typing.rs ~9449 None-arm: consult provider_spec_view_bindings for an expected param the cross-sort actual lacks) -- IMPLEMENTED + ISOLATED-INNOCENT (wi357 all 4 + wi210 all 20 pass with FIX 2 kept and the List change reverted). (2) The fact-head {} loader gap (FIX 1) can be ROUTED AROUND: a 'provides Stream[T=T, E={}]' CLAUSE loads the {} fine (load_provides_clause -> sort_inst_to_value -> type_expr_to_value handles EffectRow), unlike a 'fact' head (term path -> Term::Ref('{}') -> remap_symbol_strict -> unresolved). So use 'provides', not 'fact', for a written-row provision. (3) NEW GAP (FIX 3): writing E={} on List's Stream provision makes the WI-325/356 abstract/requires-coverage check DEMAND 'requires Stream[E=…]' for Stream spec ops consumed on a List -> REGRESSES wi357 (element threading) + wi210 (dispatch) with 'expected requires Stream[…] covering abstract type parameter, got missing requires Stream[E=…] on enclosing sort'. The check must treat a PROVIDED concrete E (from the SortProvidesInfo) as COVERING Stream.E, not as an uncovered abstract param. VERIFIED that WI-368's acceptance WORKS in isolation (provides + FIX 2 + iterator[Elem](l:List[T=Elem])->Stream[T=Elem,E={}]: wi368_iterator_threading_test both cases pass) -- the ONLY blocker to landing is FIX 3 (the abstract-check cascade). wi368_iterator_threading_test.rs is on disk (untracked, dormant) as the acceptance anchor. So WI-386 scope = FIX 2 (done) + the provides-surface decision + FIX 3 (abstract-check E coverage, WI-325/356 area, sensitive). WI-380 then = the stdlib producer rewrites (List.iterator first) once WI-386 lands.

### 2026-06-04T17:27:43Z — user

DESIGN DELIVERED: docs/design/effect-rows-on-cross-sort-carriers.md captures the full plan -- FIX 2 (cross-sort provider-view, implemented+isolated-innocent), the provides-clause route around the fact-head {} loader gap, and FIX 3 (the blocker: abstract/requires-coverage must treat a provided concrete E as covering Stream.E). WI-368 acceptance proven achievable in isolation. IMPLEMENTATION is WI-387 (depends WI-386, blocks WI-380); wi368_iterator_threading_test wired #[ignore]'d as the acceptance anchor. This WI delivered as DESIGN-only.

