## Attributes

- id: WI-462
- created: 2026-06-14T10:43:02Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-15T11:15:36Z

- acceptance: cargo-test

- tags: typing

## Description

Typer: TUPLE-LITERAL element threading parity with the Pair-CONSTRUCTOR (bidirectional expected->component push). REPRO: operation split(xs: List) -> Option[(xs.T, List[T=xs.T])] = match xs case nil->none case cons(h,t)->some((h,t)) -> 'match.rule: expected Option[(_1: xs.T, _2: List[T=xs.T])], got Option[(_1: ?_, _2: List)]'. The Pair-CONSTRUCTOR pair(h,t) vs Pair[A=xs.T,B=List[T=xs.T]] THREADS (verified probe B1); the positional TUPLE literal (h,t) vs (xs.T, List[T=xs.T]) does NOT (head h stays ?_, tail t stays bare List). DIAGNOSIS: the WI-427 bidirectional expected->argument push reaches named constructor args but not positional tuple-literal components. FIX DIRECTION: push expected tuple component types into the tuple-literal elements (h<-xs.T; t:List expands to List[T=xs.T]). BLOCKS WI-447 split. TEST: bare split typechecks; tuple-component threading test.

## Changes

### 2026-06-15T11:27:40Z — feedback — claude

DELIVERED (merge 83cdc18 + review-cleanup follow-on). The positional tuple (h,t) lowers to Constructor{TupleLiteral} (NOT Expr::TupleLit) -- fix threads expected components in check_tuple_literal_constructor (unify+walk via thread_expected_tuple_field) + pushes the ctor's expected into a tuple-literal field value (tuple_field_expected_from_ctor); parity with pair(h,t). /code-review (3 agents): 2 correctness/soundness clean (no-op for concrete-matching components, unify-failure ignored so concrete mismatch still REJECTS, scratch/local substs no leak, parity with pair accept-set). Cleanup applied: removed dead Expr::TupleLit arm from is_tuple_lit (surface tuples are all Constructor{TupleLiteral}; the Expr::TupleLit build frame discards expected); added named-tuple threading test. DEFERRED/NON-BLOCKING: (1) ALTITUDE -- root cause is that an unbound flexible Var::Global is NOT a wildcard in types_compatible (only TypeExtractor.TypeVar is); a general fix (treat flexible unbound vars as wildcards) would subsume tuples + arrows + all containers and delete the per-shape plumbing, BUT it's a soundness-sensitive change to a SUBTYPE check (bind-vs-accept, flexible-vs-rigid skolem distinction) = cascade risk, NOT for the loop to guess. Record as intended convergence so per-literal threading (tuple, future records/variants) doesn't accrete. (2) DIRECT-return tuple (non-Option) is semantically impossible for a total list fn (nil has no head) -- the some-wrapped form is THE form and works; the direct tuple BODY path does thread (verified mkpair).

