## Attributes

- id: WI-027
- created: 2026-03-25T07:00:47Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-24T09:40:38Z

- acceptance: cargo-test

- depends_on: WI-029

## Description

forall quantifier in rule bodies: forall ?x in list: P(?x) eliminates recursive list walking. Extend grammar and resolver to support quantified goals in rule bodies, not just constraints.

## Changes

### 2026-06-24T09:40:33Z — feedback — user

DELIVERED. Collection-bounded quantification in rule bodies: (forall ?x in xs: P(?x)) = finite conjunction (nil=vacuous true); (some ?x in xs: P(?x)) = finite disjunction (nil=fail). Distinct from the Harrop forall_impl (WI-105/108) which skolemises; this iterates a concrete list's elements, eliminating hand-written recursive list walks. LAYERS: (1) grammar.js bounded_quantification = '(' (forall|some) ?x 'in' <term> ':' rule_body ')', parenthesised so the comma-body doesn't bleed into the enclosing conjunction; wired into _non_name_atom_term. (2) convert.rs convert_bounded_quantification lowers to forall_in/some_in(binder, collection, tuple(body)); binder shares VarId with body uses (get_or_create_var); anonymous '?' binder REJECTED loudly (would bind nothing). (3) print.rs round-trips both occurrence + term printer paths. (4) resolve.rs: marker dispatch (step_init) -> bounded_quant_kind -> step_bounded_quant: forall = flatten per-element bodies into a conjunction + frame-replace (like step_forall_impl); some = ChoicePoint with one Continuation per element. Binder substituted STRUCTURALLY per element via subst_globals (NOT bound in sigma), so a free ?y in the body threads across elements through sigma; non-ground collection -> delay_goal -> floundered residual (WI-519, never silently decided). bounded_list_elements walks cons/nil/ListLiteral by SHORT NAME (resolve_sym, since a value list and the resolver carry distinct cons/nil Symbols sharing the name), handles nil-as-Ref AND nil-as-Fn, with a cycle guard (non-occurs-checked sigma can carry a cyclic spine -> bail to delay vs hang). (5) node_occurrence.rs: FIXED a pre-existing silent-data-loss bug the feature exposed — the 'ListLiteral' term->occurrence build used collect_list_visits (a cons/nil walker) which returned EMPTY for a ListLiteral (elements live in pos_args); now reads pos_args. Added a try_occurrence_to_term ListLit arm reifying to the ListLiteral term twin. KEY DEBUG FINDINGS (root causes, in order): elements lost = ListLiteral build via cons/nil walker; ?y not threaded = item() test helper used make_name_term (nullary Fn) vs facts' Ref -> use Term::Ref; variable collection None = cons matched by Symbol-equality vs a 2nd 'cons' symbol -> match by name; nil collection None = nil rides as Ref not Fn. TESTS: tests/include/bounded_quant_test.rs (11: parse/load, round-trip, forall all-hold/one-fail/nil-vacuous/var-bound/free-var-threaded, some one-holds/none/nil-fail, non-ground->undecided) + 3 tree-sitter corpus cases. SPEC: kernel-language.md §5.3 bounded-quantification subsection + binder-hygiene caveat (no per-quantifier scoping; binder must be a fresh name). CODE REVIEW (high, 3 finders): no new correctness bug in the feature; cyclic-list hang FIXED; anon-binder diagnostic ADDED; some-witness over-dedup REFUTED (goal_fingerprint reifies body through sigma so distinct enclosing bindings differentiate). FOLLOW-UP WI-559: SetLiteral/TupleLiteral have the IDENTICAL pre-existing build bug + try_occurrence_to_term lacks SetLit/TupleLit reify arms + ListLiteral [h|t] tail dropped at occurrence-build. anthill-core 1907/0; tree-sitter 172/0; full workspace green.

