## Attributes

- id: WI-910
- created: 2026-07-31T21:41:42Z

- status: Delivered
- status_agent: claude
- status_at: 2026-09-04T19:36:44Z

- acceptance: cargo-test

## Description

NOTHING ENFORCES THAT THE IMPLICIT VOCAB'S SHORT NAMES ARE DISTINCT, so a duplicate would make one entry DEAD SILENTLY. `implicit_qualified` (rustland/anthill-core/src/kb/load.rs) resolves by LAST DOT-SEGMENT — `kernel_vocab_qualified(name).or_else(prelude_qualified(name))`, each a linear `find` over its `&[&str]` — so if two entries ever share a short name (within a list, or across the two), the first wins and the second can never be reached by any of the four consumers. Checked at filing: all 61 entries (28 KERNEL_VOCAB_QUALIFIED + 33 PRELUDE_QUALIFIED) have distinct short names today, so this is a guard against rot, not a live bug. It is the second way these tables can go quietly wrong; WI-900 closed the first (an entry whose TARGET is not declared, now pinned by `implicit_target_orphans` + `wi900_implicit_tier_agreement_test::every_implicit_target_is_declared_by_the_standard_load`). Deliberately NOT folded into WI-900: it is a different invariant about the same table, and the ticket-spinoff rule says a surviving follow-up becomes its own ticket. ACCEPTANCE: a test (or a const-time check) fails when two implicit entries share a short name, naming both; the check lives beside the tables like `implicit_target_orphans` does; cargo-test green via scripts/test.sh.

## Changes

### 2026-09-04T19:36:16Z — feedback — user

CLOSED ONTO A GUARD, not lapsed. Both tables this ticket names are gone -- KERNEL_VOCAB_QUALIFIED with WI-20260825-5W3RJ (the converter names its target outright now, parse::desugar_target), PRELUDE_QUALIFIED with WI-909 -- and with them the linear `find` by LAST DOT-SEGMENT that made a duplicate short name silently unreachable. The ticket as WRITTEN has no subject.

BUT SHORT-NAME LOOKUP DID NOT GO WITH THE TABLES, and an earlier draft of this note said it did. What WI-909 deleted is the lowest-precedence fallback RUNG. Ordinary scope resolution still resolves a short name against a scope's locals, and register_stdlib_scopes still DEFINES nine of these addresses under `dt::short(X)`. The currency this ticket is about is still live. Readers keying on a last segment today: `dt::is`'s third arm; `expr_form_key`, the dispatch key of the hand-written match arms (EIGHT of dt::ALL, not twelve -- field_access / ho_apply / cut / find_dictionary have no arm); register_stdlib_scopes; kb::resolve's ho_apply gate and `Some("ListLiteral")` in bounded_list_elements; kb::load's ho_apply intern fallback and type_expr_base_name; and node_occurrence's `debug_assert!(!dot_chain || key == "field_access")`, whose own comment says the safety is one collision away.

DELIVERED: `wi040_reserved_vocab_test::{no_two_minted_addresses_share_a_short_name, the_short_name_collision_detector_finds_what_it_is_given}`. Sited in the file that already builds the UNION domain, which is what the ticket's own acceptance asks about ("within a list, or ACROSS the two"): dt::ALL chained with pratt's SPEC_OP / CONNECTIVE / EQUALITY_FAMILY tables, 30 addresses. That domain was extracted into `every_minted_address()` so the orphan row and the distinctness row cannot drift apart. The detector keys on the ADDRESS, because EQ_FUNCTOR is in the domain TWICE by construction and a short-name-only check reports it colliding with itself.

TWO ROWS, AND THE SECOND IS THE CONTROL: a four-element probe colliding at positions 1 and 3, with the twin DERIVED as `format!("..anthill.reflect.Pattern.{}", short(MATCH_EXPR))` rather than spelled out. Separate #[test] fns on purpose -- assert_eq! panics, so folding them together would make the control unreachable at exactly the moment a real collision reds the first. A derived length assertion catches a dropped `.chain`, which would otherwise leave the sweep returning None over a shrunken domain.

WHAT IT DOES NOT COVER, so the census is not read as complete: the dispatch KEY SPACE is wider than this domain -- visit_fn matches ~20 strings and register_stdlib_scopes hand-writes apply / constructor / var_ref / the *_lit family into the same scopes. expr_form_key is namespace-blind, so is_reflect_form_functor ALREADY answers true for anthill.reflect.Substitution.apply and anthill.prelude.Function.apply. That is a live, pre-existing defect, older than this ticket and not repaired by it. Also unchanged: the same-scope half is already LOUD, because SymbolTable::define merges and leaves the second qualified address unregistered, which the orphan row above reports as a named orphan.

PROCESS NOTE WORTH KEEPING. The first attempt put the guard in desugar_target.rs over dt::ALL alone and routed it through expr_form_key. /code-review found 15 issues, including an inverted scope-nesting claim in the sentence advertising itself as precise, a domain too narrow to catch the hazard it named, and a control that pinned expr_form_key's namespace-blindness as REQUIRED behaviour -- so fixing that pre-existing defect would have red a test in an unrelated file. It was reverted whole rather than patched. Suite: 36 binaries, 6404 passed, 0 failed.

