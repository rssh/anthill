## Attributes

- id: WI-1034
- created: 2026-08-07T03:09:51Z

- status: Delivered
- status_agent: user
- status_at: 2026-08-07T20:34:10Z

- acceptance: cargo-test

## Description

A rule-body goal whose functor names NOTHING answers nothing, silently. MEASURED on WI-1026's fixture: 'rule answer(?r) :- describe(leaf(), ?r)' loads CLEAN and answers [] even with ONE unambiguous supplier present — 'describe' is not in scope at the namespace top level, so it interns to a symbol with no clause, no operation record and no builtin, and the goal can never match. This is NOT a dispatch question (WI-1026's own ordering note predicted it: 'a resolution failure prior to any 058 question'), which is why WI-1026 left it and PINNED the current behaviour in 'a_bare_goal_naming_nothing_still_answers_nothing'. WHY IT IS NOT INLINE: the check is corpus-wide and at least two of its categories are LEGITIMATE, so a naive 'no clauses => refuse' rule is wrong. MEASURED over stdlib+bindings / +testcases / +examples / +anthill-todo, counting GOAL positions only (top-level body atoms plus the arguments of not/or/push_choice — a whole-subtree walk over-counts every constructor in an argument position and reports ~313 instead): 20 dangling goals in stdlib alone, 25 with examples, over 5 distinct names in three categories — (a) 'forall_impl', a RESOLVER SCOPING MARKER that has no clauses by design and is classified specially by the resolver; (b) 'anthill.reflect.typing.DefaultProvider', an ENTITY whose facts the loader asserts after this check would run; (c) genuinely dangling names in examples ('distance_at_step', 'observed_pose_at', 'anthill.examples.lf1.safety.common.RealVelocity'). WORK: decide the predicate (it must know about resolver markers and about functors that acquire facts later), triage the 5 corpus names, then make the rest loud. ACCEPTANCE: a rule-body goal that can never match is refused at load naming it and its span; the three categories above still load; the WI-1026 test above flips from asserting [] to asserting the refusal. REFERENCE: WI-1026; the probe that produced these counts is described in the header of wi1026_rule_body_spec_op_dispatch_test.rs.

