```anthill
fact WorkItem(id: "WI-615", created: "2026-07-02T16:04:03Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-02T16:02:11Z"))
```

## description

Proposal 051 Phase 1: un-park the `===` structural-equality-test operator. 049 parked `===` and left the resolver's structural shortcut (`builtin_eq`->`views_structurally_equal`, wired mod.rs:4171 as BuiltinTag::Eq on Eq.eq) in place under the Invariant 'valid while every carrier's equality is structural'. That condition is now VIOLATED: Set is a non-structural-equality carrier (set.anthill:25 makes insert commutative, so two equal sets are structurally-distinct terms), so any Eq[Set]/Eq[Map] is membership-based and `=`/`eq` give the wrong answer. SCOPE (decision-free, no SLD-dispatch dep): add `===` as a single grammar token (tree-sitter-anthill/grammar.js + scaland fastparse, lexed greedy-longest before `==`/`=`) -> loader maps `===` to the EXISTING structural comparator (BuiltinTag::Eq / builtin_eq) -- a relabel, not new behaviour; `===` is a total, carrier-agnostic, dispatch-free TEST needing no Eq instance (opaque carriers compare by handle identity per WI-486). KEEP `=`/`eq` as the structural shortcut for now (backward-compatible; the semantic flip is Phase 2/WI-616). Migrate the obvious structural-intent internal/reflect comparison sites (Term/Symbol/reflected-structure identity) from `=`/`eq` to `===` so they are already off `=` before Phase 2 flips it. Orthogonal to WI-300 (guard tier). Design: docs/proposals/051-structural-vs-semantic-equality.md.

