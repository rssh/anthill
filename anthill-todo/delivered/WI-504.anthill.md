```anthill
fact WorkItem(id: "WI-504", created: "2026-06-17T13:17:46Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-18T17:02:40Z"))

fact Feedback(workitem: "WI-504", author: "claude", at: "2026-06-17T14:35:34Z")

fact Feedback(workitem: "WI-504", author: "user", at: "2026-06-18T17:02:33Z")
```

## description

Resolve-time subst-aware var collection for Type/EffectExpr-kind Value::Node type-arg spines in the rule-delay pre-check (WI-322 residual). WI-322 made body_builtins_delay_on_caller_vars_nodes (resolve.rs) detect caller vars in op type positions (Apply/ApplyWithin.type_args, Let.type_annotation): collect_type_value_unbound_vars routes a Value::Node type-arg spine through collect_unbound_vars_node (resolve.rs:3289). That catches Expr/Denoted leaves subst-aware, but collect_unbound_vars_node (resolve.rs:3192) has a `None => {}` arm (resolve.rs:3228) that SILENTLY skips Type/EffectExpr-kind occurrences (as_expr/as_pattern both None). So a caller var living in a Type-spine sibling of a Node type-arg — e.g. a TypeChild::Ground(Term::Var(Global)) inside a denoted/parameterized type that with_fresh_vars opened from a DeBruijn var — is NOT collected, and a type-dispatching builtin whose first arg is f[T = «Node Type-spine holding ?caller_var»](v) does NOT propagate rule delay (latent UNDER-delay; the exact failure-class WI-322 closed, but for the Type/EffectExpr-kind Node carrier only). The loader load-time twin collect_value_type (node_occurrence.rs:1968) DOES descend such spines via collect_type_or_expr_node_vars (node_occurrence.rs:1987) -> collect_type_node_vars (2018) / collect_effect_node_vars (2047), but those collect ALL vars (no substitution), so they are not directly usable for the resolve-time unbound-only pre-check (and the var must be subst-walked to its final var for the caller-var membership test). SCOPE: provide a subst-aware Type/EffectExpr-spine var collector — the resolve-time twin of collect_type_or_expr_node_vars — reachable from collect_unbound_vars_node`s None arm (or collect_type_value_unbound_vars`s Value::Node arm), so a caller var inside a Type-kind Node type-arg spine is detected (chase Ground TermId children via collect_unbound_vars; resolve Global occurrence vars via the same subst-aware logic). This rides WI-378 step 2 — the kb-threading type-field->child migration that lets collect/close/open walk INTO a Type occurrence; the pending boundary is marked by debug_assert!(false, "type-field migration must thread kb here") at node_occurrence.rs:1812/1855/1921, the same wall that blocks this. Once occurrence Type-spine children are walkable with kb threaded, the resolve-time subst filter follows. LATENT today: no builtin first-arg currently carries a var-bearing Type/EffectExpr-kind Node type-arg; WI-322 already covers Value::Term, Denoted-Expr Value::Node, and tuple/named-tuple carriers. ACCEPTANCE: a delayed builtin whose first arg is f[T = «Value::Node Type-spine holding ?caller_var»](v) triggers rule delay (caller var detected through the Type-spine, not silently dropped by the None arm); cargo-test green.

## Feedback

### 2026-06-17T14:35:34Z — claude

Dependency on WI-378 removed (2026-06-17): WI-378's STEP 2 repr migration was DECLINED (see WI-378 feedback) and WI-378 delivered via gap-1. This residual does NOT require that migration. Narrow fix path: the resolve-time delay pre-check collect_unbound_vars_node (resolve.rs) descends a Value::Node type-arg, but its catch-all arm cannot walk a Type/EffectExpr-kind spine (a TypeChild::Ground(Term::Var) needs kb to see the var, and that walker has no kb). Fix by threading kb into that descent, or by reusing collect_value_type (node_occurrence.rs, which already has kb and descends Type spines via collect_type_node_vars/collect_effect_node_vars) under a subst filter. Currently guarded by a loud debug_assert added in WI-322; latent (no builtin first-arg carries a var-bearing Type-kind Node type-arg today).

### 2026-06-18T17:02:33Z — user

DELIVERED 2026-06-18. Fix: resolve-time subst-aware Type/EffectExpr-spine var collector wired into collect_unbound_vars_node's None arm (resolve.rs). New collect_type_node_unbound_vars / collect_effect_node_unbound_vars / collect_type_child_unbound_vars are the resolve-time, subst-aware twins of the loader's collect_type_node_vars / collect_effect_node_vars / collect_type_child (node_occurrence.rs): same arm-for-arm child walk, but TypeChild::Ground chases through the subst-aware collect_unbound_vars (not all-vars collect_vars_rec) and TypeChild::Node recurses through collect_unbound_vars_node (whose None arm re-dispatches a nested Type/EffectExpr spine). The WI-322 debug_assert!(false) guard removed now that the case is handled. NOTE: collect_unbound_vars_node is a &self method, so it already had kb via self — no kb-threading needed (the feedback's threading concern applied to the loader's free-function for_each_child walkers, which retain their asserts and are out of scope). 2 tests added (delay_propagates_on_caller_var_in_type_node_spine = acceptance, NodeKind::Type carrier; collect_type_node_spine_is_subst_aware = direct, proves σ-bound var excluded + EffectExpr/Denoted-Node coverage). Full workspace green (60 binaries, 0 failed). /code-review (xhigh, 2 agents): clean, no findings.

