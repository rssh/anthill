## Attributes

- id: WI-524
- created: 2026-06-20T09:39:58Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-20T10:31:59Z

- acceptance: cargo-test

- depends_on: WI-522, WI-523

## Description

Proposal 049 build step 4 (let sugar). Desugar `let ?v = expr` to `?v <=> expr` in the loader (one primitive, directed surface). `:=` is RESERVED for Cell.set, not a let surface (proposal 037). Rust-only.

## Changes

### 2026-06-20T10:31:51Z — feedback — claude

SUBSUMED by WI-522 (delivered 2026-06-20). The `let ?v = expr` -> `unify(?v, expr)` desugar landed in the PARSE layer (parse/convert.rs convert_let_binding), NOT the loader — because that is exactly where every operator desugars (pratt.rs lowers `<=>` and all infix ops in parse/). Grammar `let_binding` + shared `_goal` rule; converter lowers body `let` to unify and rejects `let` in rule heads (loud). Verified: parse_test let/<=>->unify + corpus tests + full anthill-core suite green. No separate loader work needed; closing as Delivered.

