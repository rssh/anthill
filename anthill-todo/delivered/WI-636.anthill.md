## Attributes

- id: WI-636
- created: 2026-07-04T20:06:44Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-07T18:44:48Z

- acceptance: cargo-test

- tags: typing

## Description

with_fresh_vars: non-Term Value-carrier tree_subst entries are silently dropped (LATENT — PLAUSIBLE-no-live-producer per the WI-624 /code-review verify pass; parity/loud-over-silent hardening). Both WI-624 passes walk tree_subst.iter_terms() (Value::Term-only, subst.rs:560-565): (a) a SYNTHETIC u32::MAX-n entry bound to a non-Term Value (scalar/Entity via BindValue::Value, term_view.rs ~1135) never enters body_rename — for a nonlinear head the query-var link then dangles on a never-bound fresh var, the WI-624 symptom on the Value carrier; (b) non-Term query-var LINK entries never reach answer_links at all, while the fact fast-path preserves them via bind_waking (resolve.rs ~1900-1908, proposal 026.1 lineage) — a bind_waking-parity gap for the bodyless arity>0 rules WI-624 rerouted. WHY LATENT: the top-of-function normalization rescues Value::Node (the realistic carrier — occurrence goals convert sigma-bound scalars to Expr::Const Node leaves, node_occurrence.rs ~3579, and panic LOUDLY on Entity); Term goals stay term-land (Path extraction vs a Term head always yields TermId); scalar Value children reaching a DeBruijn head match need a Value-carrier rule head with vars — none constructible today (bodyless value heads are always arity 0 per push_value_head_entry kb/mod.rs:1062; REFUTED-reachability in the review). ACTION when it becomes reachable (WI-342-P3 value-head-with-vars territory, or any new Value-carrier producer): extend the normalization to reify/route ALL non-Term entries or fail LOUD on the unhandled carrier instead of the silent iter_terms skip; also give answer_links a bind_waking-parity path in the resolver rule branch. Related: instantiate_eq_rhs has the same iter_terms skip (noted in WI-634 (c)).

## Changes

### 2026-07-07T18:44:45Z — feedback — claude

Ticket's LATENT/no-live-producer premise DISPROVEN during /code-review: the WI-625 eval->SLD eq/neq bridge feeds raw ground Value::Entity{ctor,[Value::Tuple...]} operands into a rule-backed eq's head match, reaching with_fresh_vars. A first-cut panic on the un-reifiable carrier would abort the PROCESS on legit user code -> pivoted to graceful candidate-drop (contradiction=true), so eq falls back to its structural verdict. FIX: top normalization (precedes arity split) routes EVERY non-Term tree_subst carrier via value_to_term -- reify structural subset (Node/Entity-with-scalars/scalar/Var; Entity-with-scalars now FIRES the rule, was silently dropped), drop the whole candidate on the un-reifiable residue (Tuple/opaque/Unit/Entity-carrying-one). answer_links is Term-only BY CONSTRUCTION (all .bind->Value::Term); the resolver debug_assert is a construction tripwire, NOT a WI-636 completeness guard (a dropped link passes all(is_term) vacuously). WI-634(c)'s instantiate_eq_rhs ref is STALE (deleted -> fire_simp_equation/match_view); refreshed 3 comment refs. Deeper fix = reify eq operands at the producer (eq-bridge) = WI-625 follow-up, not a new ticket.

