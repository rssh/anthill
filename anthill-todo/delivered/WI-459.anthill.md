```anthill
fact WorkItem(id: "WI-459", created: "2026-06-14T10:43:02Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-14T19:46:36Z"))

fact Tag(workitem: "WI-459", name: "typing")

fact Feedback(workitem: "WI-459", author: "user", at: "2026-06-14T19:46:30Z")
```

## description

Typer: projection neutrals must compare by DEFINITIONAL (delta) EQUALITY, not syntactic identity -- a recursive/consuming body's projected member off a LOCAL whose type BINDS that member must delta-reduce to the bound value, rather than survive as a fresh neutral that then fails an identity check. MINIMAL REPRO (/tmp/probeD): operation sfd(xs: List) -> Option[Pair[A=xs.T, B=List[T=xs.T]]] = match xs case nil->none case cons(h,t)->some(pair(h,t)); operation collectd(xs: List) -> List[T=xs.T] = match sfd(xs) case none->nil case some(pair(h,rest))->cons(head:h, tail:collectd(rest)) -> 'match.rule: expected List[T=xs.T], got List[T=xs.T]' (SAME print, but rest.T and xs.T are DISTINCT neutrals). collectd(rest) is List[rest.T]; rest:List[T=xs.T] so rest.T is DEFINITIONALLY EQUAL to xs.T (delta: project T off rest's type binding) but NOT syntactically identical. DIAGNOSIS -- it is a MISSING REDUCTION, not a too-strict check: expr_carried_zeta (typing.rs ~11459) compares two ExprCarried by same-member AND receiver structural_eq -- correct BY DESIGN (non-decomposing; ExprCarried is non-injective per WI-400, must NOT infer rest=xs from rest.T=xs.T) and is only the FALLBACK for genuinely-rigid neutrals (abstract receiver, no type to reduce against). The delta-reduction lives at the ELIMINATION site (project_type_member via check_apply_iter), which has the typing ENV to look up rest's type; unify_types/the zeta arm has only kb+subst, NOT the env, so delta cannot simply move there. BUG: at the recursive call rest's type IS known (List[T=xs.T]) yet the return projection leaks to zeta as rest.T instead of delta-reducing to the canonical xs.T. Same on the EFFECT dual (count: 'undeclared effect: s.E', body's s.E = rest.E). DISTINCT from WI-413 (delivered; that was the UNBOUND ??_ close-didn't-run case) -- here the neutral is BOUND but UN-REDUCED. FIX DIRECTION: make the projection delta-reduce at the recursive/consuming call (receiver type known -> rest.T reduces to canonical xs.T) so it never reaches the zeta arm; KEEP expr_carried_zeta non-decomposing (WI-400 soundness). DECIDE (scope): the deferred-receiver case -- receiver type fixed only AFTER the projection forms (rest a fresh var bound later) -- eager elimination cannot catch and needs delta-aware comparison OR a DELAYED projection that re-reduces when the receiver binds; handle now or record as a follow-on. DOMINANT WI-447 blocker (collect/takeN/count/nth/foldLeft/foldRight). TEST: probeD typechecks; full stdlib + wi357/365/368/376/396/398/424 green; WI-400 negatives (neutral vs concrete, two distinct rigid neutrals) stay rejected.

## Feedback

### 2026-06-14T19:46:30Z — user

DELIVERED (dd00198). Root cause was NOT a too-strict zeta check but a NEUTRAL RECEIVER LEAK: at call-site elimination a projection off a callee formal that stayed Neutral (arg type didn't bind the member, e.g. sfd(xs) with bare xs:List) kept the CALLEE's receiver symbol (sfd.xs.T) when it leaked into the caller — so rest:List[T=sfd.xs.T] then failed the non-decomposing zeta vs the declared collectd.xs.T (same print, distinct symbol). FIX: re-key a surviving neutral's receiver formal->arg (param_to_arg_sym) SURGICALLY in rewrite_term_projections' Neutral branch (new arg_syms param threaded through eliminate_type_projections; call-sites pass Some, body-sites None). Grounded delta-reduction (recursive collectd(rest)) is untouched (precedes the re-key) — a blanket substitute_ref_syms_value would corrupt it (self-recursion: callee formal == enclosing param). EFFECT DUAL (count s.E): the pre-existing blanket effect re-key at 4187 had the SAME over-application bug (s.E->rest.E 'undeclared effect'); gated it to skip projection-bearing effects, keeping only ground Modify[c] re-key. /code-review (3 finders + sweep): no soundness bug; fixed a vacuous negative test (was rejected by cons.type_args clash, not the return gate). FOLLOW-ONS (NOT needed for WI-447 — stdlib threads only single-Ref receivers s/xs/rest): (1) compound-receiver a.b.T neutral is NOT re-keyed (Node arm; loud sound over-reject, documented at eliminate_type_projections Node arm); (2) deferred-receiver case (non-value-ref arg) keeps callee-keyed neutral; (3) PRE-EXISTING sibling: dispatched_impl_effects (~8916) still blanket-substitutes without the projection guard.

