## Attributes

- id: WI-328
- created: 2026-05-28T20:44:49Z

- status: Delivered
- status_agent: claude
- status_at: 2026-05-29T09:01:54Z

- acceptance: cargo-test

- depends_on: WI-307

## Description

WI-307 v1b: lacks-constraints on row tails (proposal 045 §5.5, §7.1, §Phase 3). Adds '-e' (absent) semantics to row unification: a row containing 'absent(e)' (= 'lacks e') must NOT have 'e' in its extension. SUBSTRATE TODAY: v1a's decompose_effect_row (typing.rs ~5260, commit 1d62e7a) already collects 'absent(label)' nodes into the third tuple slot (_a_absent), but unify_effect_rows ignores them — v1a is presence-only. PIECES: (a) attach a 'lacks set' to each row-variable Var binding (a side-table keyed by VarId; cannot live in the EffectExpression term because the constraint is on the unbound tail, not on the row's labels); (b) at unify_effect_rows' tail-binding sites (bind_row_tail in typing.rs ~5310), check the new binding against each existing lacks constraint — if a present-label in the binding matches a lacked-label, the row unification fails; (c) on bind_row_tail's open(fresh) path, propagate the union of both sides' lacks constraints onto the fresh shared tail; (d) reject at parse time when a row has both 'present(e)' and 'absent(e)' for the same label (already a hard error per proposal §7.2); (e) tests covering: '-Modify' propagation through map/fold callbacks, '{Modify[c]} unifies with {-Error | ρ}' → ρ acquires '-Error' lacks, and proven impossibility ('{Error | ρ}' with ρ-lacks-Error fails). ACCEPTANCE: lacks-constraint tests pass; v1a row tests still green; decompose_effect_row's _a_absent slot is now consumed; full cargo-test green. Depends on WI-307 v1a having shipped unify_effect_rows (it has). Code-review #4 from WI-307 substrate (commit 1f92799) — present(Var)/open(Var) schema mismatch — overlaps here; if v1b lifts row vars to a dedicated schema-conformant shape (type_var-wrapped), this WI can close it too. Otherwise file separately.

