```anthill
fact WorkItem(id: "WI-640", created: "2026-07-05T15:07:08Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-05T15:05:55Z"))

fact Tag(workitem: "WI-640", name: "rust")
```

## description

Simp firing convergence: make apply_eq_rules carrier-neutral (&Value -> (Value, changes)) and fire it via a corrected ONE-WAY matcher, so `anthill prove` simplifies Value::Node occurrence goals (was silently term-goal-only) and the WI-624/633/634 query-var-threading apparatus (instantiate_eq_rhs, the query-link classification, the linearity gate) is DELETED. Root cause fixed: match_view was WRONG for a redex carrying flex-Global query vars — it ran the discrim QUERY path which self-binds a flex-Global TARGET var and scrambles the match. Added a match_mode to the discrim query so a target flex-Global var is inert (one-way); kept match_view WILDCARD (assumed-fact/reflect discharge relies on target-var binding, resolve.rs:1023) and added match_view_oneway used only by fire_simp_equation. /code-review (high, 8 angles) found+fixed 2 correctness bugs (the match_view hypothesis-discharge regression from flipping it globally one-way — A/C/G converged; and a value-headed-equation rule_head panic) + 2 efficiency regressions (WI-595 guard-result cache, unified functor pre-filter); regression test match_view_binds_target_var_but_oneway_does_not. Phase 1 of the convergence; Phase 2 (driver merge) is the follow-up. Full workspace 2523/0. Related WI-283 (converge the two simp sites, delivered), WI-634.

