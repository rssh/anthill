## Attributes

- id: WI-413
- created: 2026-06-07T00:30:01Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-07T01:42:53Z

- acceptance: cargo-test

## Description

Typer: effect-row threading through a CARRIER-IMPL SELF-RECURSION (the effect dual of WI-357's element threading). A provider's own splitFirst impl that self-recurses on a RECONSTRUCTED carrier value leaves the recursive call's incurred effect an unbound ??_ that won't close to the declared effect row. MINIMAL REPRO: sort Rec { sort T=?; effects E=?; entity recw(source: Stream[T=T,E=E]); provides Stream[T=T,E=E]; operation splitFirst(r: Rec) -> Option[T=Pair[A=T,B=Rec]] effects E = match r case recw(src) -> match Stream.splitFirst(src) case none()->none case some(pair(h,rest)) -> splitFirst(recw(rest)) } -> 'splitFirst.effects: expected declared [E], got undeclared effect: ??_'. WITHOUT the self-recursion (else->none) it is 0 errors; the element T threads fine (post-WI-357); ONLY the effect leaks. Parameterizing splitFirst[Elem,Eff](r: Rec[T=Elem,E=Eff]) does NOT fix it. DIAGNOSIS: the WI-357/WI-365 effect-CLOSE (typing.rs ~3302-3320, bind_spec_params_from_carrier + filter effect_is_unresolved_var) applies to a SPEC op (Stream.collect) consumed on a concrete carrier (List), but NOT to a carrier's OWN impl (Rec.splitFirst) self-recursing on a constructed carrier value — Rec is not a spec sort so self_receiver_spec_sort returns None and the close never runs. Contrast: the eager combinators (fold_left/find) recurse on a Stream 'rest' directly typed Stream[E=Eff] and DO thread the effect. FIX DIRECTION: extend the effect-close/threading to a carrier-impl self-recursion whose receiver is a constructed carrier (thread/close E like the body-less self-receiver path). This BLOCKS WI-410 (lazy filter). An EAGER filter (Stream op -> List, like find) works TODAY without this. TEST: the minimal Rec repro typechecks; the lazy FilteredStream filter typechecks+evals; WI-357/365 + full stdlib stay green.

## Changes

### 2026-06-07T00:38:25Z — feedback — user

ATTEMPTED 2026-06-07 — traced to a MULTI-LAYER gap, not a single-point fix. Debug instrumentation (WI413_DBG in the typing.rs effect-close block) on the minimal Rec repro showed: at the self-recursion splitFirst(recw(rest)), the RECEIVER recw(rest) types as Rec[T=??_, E=??_] (BOTH params unbound, not just the effect), and receiver_carrier returns Abstract because base(Rec)==spec_sort(Rec) — the 'base != spec_canon' guard at typing.rs:6448 treats a carrier-impl self-recursion as an abstract spec value, so the effect-close (3302-3320) never fires. But closing alone would be WRONG anyway (it drops the effect → a pure filter over an effectful stream); the effect must be BOUND to the carrier's E, not dropped. ROOT CHAIN (all must thread for the lazy carrier): (1) pattern destructure 'match r case recw(src)' on r: Rec[T=Elem,E=Eff] must bind src: Stream[T=Elem,E=Eff] (WI-357 claimed pattern threading works for the HEAD element A, but here even src/element comes out ??_ for the parameterized carrier — needs re-verification); (2) Stream.splitFirst(src) must thread T/E into the NESTED stream B of its Pair return (WI-357 fixed the HEAD A; the TAIL B threading is the extension); (3) the reconstructing constructor recw(rest) must carry T/E from rest into the built Rec type; (4) receiver_carrier must classify a carrier-impl self-recursion (base==spec_sort AND sort_has_constructors) as Concrete so the bind/close fires. Contrast: the EAGER combinators (fold_left/find) recurse on a Stream 'rest' DIRECTLY (no reconstruction) so they sidestep (1)/(3) — that is why an eager filter (Stream->List) works today. This is substantial effect+element threading work across pattern/spec-op-return/constructor/dispatch-classification in the delicate WI-357/365/393 cluster (soundness-sensitive). RECOMMEND a dedicated focused session; an EAGER filter unblocks WI-410's functionality meanwhile.

### 2026-06-07T01:42:53Z — feedback — user

DELIVERED 2026-06-07 (903a238). ROOT CAUSE was NOT the effect-close/receiver_carrier (the WI's layer-4 diagnosis) but the PATTERN DESTRUCTURE (layer 1): extend_env_from_pattern read constructor field types through the SHALLOW walk_type_value, which resolves only a TOP-LEVEL type-param var — a parameterized field type (source: Stream[T=T,E=E], vars NESTED in the Fn named_args) was left unsubstituted, so 'case recw(src)' left src:Stream[?_,?_] and rest/recw(rest)/the recursive effect all followed to ??_. FIX: walk_pattern_field_type_deep (deep-walk field types into Fn children, preserving top-level Value::Node surfacing). With the carrier op declaring its OWN [Elem,Eff] params (the whole combinator library convention), the destructure threads AND the recursive call's Eff binds from its reconstructed arg by ordinary op-type-param unification -> closes to the declared row. CORRECTION: the WI's 'parameterizing splitFirst[Elem,Eff] does NOT fix it' was true ONLY before this fix; AFTER it the parameterized form WORKS and is the delivered form. The BARE self-receiver (r: Rec) stays unsupported (no op-param machinery to carry element/effect — same reason every eager combinator uses explicit params); known boundary, not a regression. Tests: wi413_effect_self_recursion_test (Rec carrier typechecks) + eval_test wi413_lazy_filter (filter typechecks+evals). Full workspace suite green (1540). /code-review high: no real findings.

