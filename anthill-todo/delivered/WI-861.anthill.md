## Attributes

- id: WI-861
- created: 2026-07-30T11:56:39Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-13T16:45:56Z

- acceptance: cargo-test

- depends_on: WI-860, WI-857

- tags: modinst

## Description

Proposal 058 PHASE 8c — RUNG 2a: an unselected dispatch takes the DEFAULT among the tied most-specific candidates (058 §3.2). WORK: the classifier's tie (pick_most_specific returning none / DispatchOutcome::Ambiguous) consults phase 8b's index — if exactly one tied candidate answers default_provider(spec, carrier, ?W), take it; otherwise the tier-3 refusal stands unchanged. SPECIFICITY RANKS FIRST: a strictly-more-specific unmarked candidate beats a less-specific default (the Rust default-impl reading; boundary 3's pinned silent-pick widening untouched). THE FLIP INVENTORY, written before implementation because a refusal that MOVES must enumerate its consumers — each asserted as a test: (1) two_string_orderings_make_a_bare_compare_ambiguous FLIPS into this rung's acceptance — bracket-less compare answers HOST order beside two loadable witnesses, each still opt-in by bracket; (2) WI-855's AmbiguousRequirement on the self-provider-plus-rival pair RESOLVES to the self-provider, while the witness-only tie KEEPS the diagnostic (control); (3) the value-directed AmbiguousSpecOpDispatch likewise takes the default where one candidate is the carrier's own, witness-only control stays loud; (4) the Eq family is UNTOUCHED — coherent stays load-refused, no rung consulted (control); (5) a DefaultProvider-driven flip: a marked witness for a carrier with NO self-provision answers bracket-less (the Money shape), and unmarking it restores the tier-3 refusal. GATED ON WI-857: the rung routes bracket-less dispatch INTO carrier-keyed dictionaries — today's measured eval death. ACCEPTANCE: the five flips above; full workspace green via rustland/scripts/test.sh. REFERENCE: 058 §3.2, §3.6; design/058-implementation.md §3, §8 phase 8.

## Changes

### 2026-07-30T16:38:52Z — feedback — claude

NOW GATES `Pair` AS WELL AS `String` — WI-858's revision made rung 2a load-bearing for a SHIPPED library type, not only for the String scenario the brainstorm framed it around. (This entry replaces an earlier one whose backtick-quoted identifiers were eaten by shell interpolation.) MEASURED at WI-858's HEAD: `anthill.prelude.Pair` provides its own canonical `Ordered` (lexicographic fst-then-snd), and a program that declares an ALTERNATIVE pair ordering can no longer reach the canonical one through a `requires` slot. Two errors form a pincer: (a) the bracket-less goal is ambiguous — 'constructing Ordered[T = Pair[A = Int64, B = Int64]] is ambiguous among providers: anthill.prelude.Pair, <the rival>'; and (b) the repair (a) suggests is REFUSED, because `Pair` is a CONCRETE provider and §3.5 check 3 rejects an explicit `[Ordered = Pair]` ('its values carry their own sort, so the dispatch is already directed by the value'). So there is no spelling that gets the canonical order back. §3.6's INFERENCE rule closes it exactly — default_provider(?S, ?C, ?C) :- self_provides(?C, ?S) makes the carrier's own provision the default, so silence takes `Pair` and the rival stays opt-in — WITH NO EDIT TO pair.anthill, which is the property worth preserving when 8c is implemented. ADD TO THE FLIP INVENTORY: wi858_pair_orderings_test::the_canonical_order_is_unreachable_beside_a_rival — its first arm asserts the ambiguity TODAY and its message says so; when 2a lands it becomes a VALUE assertion expecting the canonical (1,9)(2,1). Its second arm (the concrete-provider refusal) should NOT flip: naming a concrete provider stays refused, and 2a is what makes naming it unnecessary.

### 2026-08-13T13:37:45Z — feedback — user

WI-1091 NOW WAITS ON THIS TICKET, and its measurement is a second, independent driver for the flip inventory — worth having before claiming, because it says which of the five flips already have a program that reaches them.

MEASURED (WI-1091 session, c2920cf8 + wi1091-partial.patch, full workspace 29 binaries): widening the op-scoped deferral so a body reads the frame slot its own requires names costs 11 tests, and SIX of them are this ticket's flips rather than that ticket's regressions. They fail as DeferToRequirement, requirement param not bound — because build_dep_projection cannot construct a dictionary for a goal whose providers TIE, so the call site leaves the slot absent and the body's read has nothing:
  * wi842 a_two_provider_value_directed_dispatch_names_both_candidates and a_rule_body_delays_on_the_tie_instead_of_first_matching
  * wi843 a_rule_body_over_two_coexisting_witnesses_does_not_first_match
  * wi855 tie_through_value_directed_dispatch_names_the_requirement_and_both_providers, load_coherence_admits_the_tie_either_way, other_unresolvable_causes_still_enter_unsupplied
The first five are pairs of (carrier's own provision, witness rival) — flip inventory items (2) and (3) verbatim. Under rung 2a the tie resolves to the carrier's own provision, the dictionary BUILDS, and those calls stop being ties at all.

SO THE INVENTORY GAINS A SIXTH CONSUMER TO CHECK, and it is not a dispatch site: the REQUIREMENT-DICTIONARY CONSTRUCTION path (build_dep_projection Strategy 3, and resolve_bridge_requirements op half) reads the same resolution and today answers None / skips the slot on Ambiguous. If rung 2a is wired only at the classifier's tie and not at the construction, a program whose tie has a default will still fail to get a dictionary — silently, since both those producers are best-effort. Worth an assertion in this ticket rather than in WI-1091: build a requirement whose goal ties with a default present, and assert the DICTIONARY is built (a value), not merely that a dispatch chose.

wi855 other_unresolvable_causes_still_enter_unsupplied is NOT one of your flips — Desc[T = Box[Mystery]] resolves to NO provider, which no default answers. It is WI-1091's, and its sort-level twin was measured this session to behave identically (loads, raises at the read), so it re-aims there to assert that agreement.

### 2026-08-13T16:45:59Z — feedback — user

DELIVERED 2026-08-13. 058 §3.2's rung 2a is one predicate — `defaults::default_among(kb, spec, carrier, providers)` — asked by the two candidate populations a bracket-less dispatch can tie over. PROVISIONS at `resolve_inner`'s `pick_most_specific` tie (`default_among_candidates`, a candidate's provider IS its `impl_sort`), which covers the typer's `UnselectedInstance`, `build_dep_projection` Strategy 3 (the requirement DICTIONARY — your sixth consumer) and `resolve_bridge_requirements` in one edit because all three read that one arbitration. SUPPLIERS at all three faces of `AmbiguousSpecOpDispatch` (`default_among_suppliers`: eval's value-directed read, WI-1027's body-less guard, WI-1012's defaulted arm and the dot), which had to be wired together or a program would be refused at load and answered at run time. `SpecOpSupplier::provider` maps `Witness(w)` to `w` and `Own`/`Fact` to the CARRIER, and that collapse is load-bearing: a carrier's own member beside its own instance fact is ONE provider saying two things, so the rung sees two hits and declines. MEASURED — relaxing `default_among` to take the FIRST hit fails 32 tests (wi859/1032/1035/1036/1040/1043/1044/1076), i.e. every "one provider, two texts" refusal in the tree. The clause reads like caution and is the load-bearing half.

THE FLIP INVENTORY WAS RIGHT ABOUT WHAT IT NAMED AND THE MEASUREMENT FOUND MORE. It named eight existing tests; the suite moved ELEVEN. Six predicted flips landed (wi844's bare compare, wi858's bracket-less Pair compare, wi855 x2, wi842 x2). Two predicted flips did NOT move and are controls rather than misses — wi843's tie is WITNESS-ONLY (no default names either candidate) and wi855's `other_unresolvable_causes_still_enter_unsupplied` resolves to NO provider, exactly as your note said. Five were unlisted: two flipped (wi1027 and wi1035 had each built their tie out of *the carrier's own provision beside a witness*, which is precisely the shape §3.6 infers a default for — each keeps its exclusion on an all-witness fixture and gains a sibling asserting the new value), and three are the finding below.

THE FINDING, and it is the one place a default must NOT answer: A NAMED REQUIREMENT SLOT WHOSE BINDING THE CALLER ERASED. §3.4 says omitting a named slot in type position means ANY — the value flowing in already chose, and its choice is part of its type — so a default there does not fill silence, it overrides a decision made elsewhere. MEASURED (`anthill run`, scratch probe): a `SortedSet[T = Int64, O = Descending]` inserted into through `Any.add(s: SortedSet[T = Int64], x: Int64)` reads back 3 where the slot-KEEPING route reads 7; the erased route built its dictionary from `Int64`'s own ascending `Ord`. Before this ticket that program was REFUSED by the tier-3 tie. Ungated, rung 2a turns that loud refusal into a silent wrong answer, which is the worst direction a change can move — so it is withheld there (`typing::DefaultRung`, `rung_for_dep`, matched BY SPEC and not by chain index, WI-857's dual lesson). SECOND MEASUREMENT that says the hazard is pre-existing and not mine to widen: with exactly ONE provider for the element the identical erasing program loads clean today and constructs silently.

THE GATE HAS A COST AND WI-1094 OWNS IT. §3.4's other half ("omitting it at a call leaves it to inference") is consequently unimplemented, and so is the SECOND FACE of WI-858's pincer: `SortedSet.empty[T = Pair[Int64, Int64]]()` beside two rival pair orderings still refuses. Pinned as still-open in `wi858_pair_orderings_test::a_bracketless_pair_set_still_needs_its_bracket`, whose flip is WI-1094's headline acceptance. A dictionary-only default cannot deliver it: it writes nothing into the TYPE, so `empty` might infer `O` while the very next `insert(s, x)` sees an unbound `O` again — and a value whose type says "any O" while its dictionary says `Pair` is exactly the mismatch measured above. The `Pair` DISPATCH face IS closed here, which is the half §3.2 actually legislates.

ONE PREREQUISITE THE TICKET DID NOT NAME: THE INDEX HAD TO BE BUILT TWICE. `build_default_provider_index` must run after `eq_derive::run` (the last pass to assert provisions), which is ~70 lines of pipeline AFTER `type_check_sorts` — where the rung's first consumer lives. Measured: with only the late build every typer-site flip stayed refused and it read as "not implemented". `seed_default_provider_index` runs the same derivation before the typer and its verdict is DISCARDED, which is not a silent skip: provisions are only ever ADDED between the two calls, so the late error set is a superset, and collecting early would be the unsound direction (check 1 would refuse a mark on a provider whose only provision `eq_derive` has not asserted yet). What the typer therefore cannot see is a row inferred from an `eq_derive` derivation — the coherent `Eq` family, whose ties are load-refused before any rung is reached, which is flip (4).

ACCEPTANCE: all six flips driven as VALUES in `wi861_rung2a_default_dispatch_test` (13 tests) plus the flipped arms in wi842/wi844/wi855/wi858/wi1027/wi1035; the witness-only and Eq-family controls stay loud; the DICTIONARY assertion your feedback asked for is `a_tied_sub_goal_with_a_default_builds_the_dictionary` (12 = 10*1+2 through `WrapDesc`'s own slot, with a marked-rival twin answering 72, so it measures the slot's CONTENT and not merely that a dispatch chose). BACK-OUT MATRIX, each reverted alone over `wi_tests`: provision-site consult -> 9 fail; supplier-site consult -> 8; `seed_default_provider_index` -> 10 (the widest, every LOAD-time consumer); `DefaultRung::Withhold` -> 4 (wi844's erased-slot refusal, wi870's two controls, wi858's still-open pin); `default_among`'s *exactly one* -> 32. Full workspace green (29 binaries, 4666 tests). Docs: proposal §3.2/§3.4/§3.6 amended (the named-slot rule is a RULE and is stated there); design/058-implementation.md §3, §8 phase 8c and new §26.

THE REVIEW FOUND FIVE, AND ONE WAS A DEFECT IN THIS TICKET'S OWN MECHANISM. (1) The BODY-LESS dot consulted the rung and threw the answer away: the guard learned to decline when the rung names a supplier, but its dot caller still returned `DotMember::Take` — calling the very member the default did NOT name. Driven by the review: one program answered `Desc.describe(x)` = 7 and `x.describe()` = 1, where before this ticket it was refused at load, i.e. the spelling-keyed silence WI-1035 exists to close, re-created by its successor, because the DEFAULTED half got the reroute and the body-less half did not. Both now read one owner, `dot_takes_or_reroutes`. (2) That reroute also over-fired on a SOLE supplier, contradicting its own comment — `chosen` is Some for one supplier too, and the interpretability filter makes "one supplier whose target is not the dot's member" reachable; gated on len>=2, and the back-out answers the witness. (3) `declared_op_with_no_definition` made a CLOSED-WORLD verdict backend-dependent: it composed the rust-only `op_is_interpretable`, so a cpp-`operation_map`ped operation answered "declared and nowhere defined" and the resolver returned Undefined where the rust build refutes — two existing docs (`PredicateProof::Undefined`, `op_is_executable`) had already legislated the other way, and `op_is_executable` is now what is asked. (4) `start_apply_deferred` and `record_apply_within_rewrite` demanded `enclosing_sort` ahead of the `enclosing_op` arm WI-822 added; relocated, and recorded as UNDRIVEN at the site — the namespace-level op-scoped program was built and RUNS, so no reachable behaviour changed. (5) wi842's `RIVAL` went unused when the tie pins moved; re-pointed at an arm asserting what that program does now, since an orphaned fixture is the shape a lost control takes. Findings 3 and 4 are against WI-1092/WI-822 code and were fixed inline rather than filed, per the repo rule.

FINAL: full workspace green, 29 binaries, 4670 tests.

