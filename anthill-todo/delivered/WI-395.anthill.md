## Attributes

- id: WI-395
- created: 2026-06-05T14:53:30Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-19T16:47:08Z

- acceptance: cargo-test

## Description

anthill-todo CLI: let `update` CHANGE a work item's status (or add an `unclaim`/`reopen` subcommand). Today the only status transitions are claim -> deliver -> verify; there is NO way to revert Claimed -> Open (un-claim) or otherwise correct a status, and `update` explicitly PRESERVES status (only edits description/acceptance). Surfaced 2026-06-05 resequencing WI-374: it was Claimed, then deferred behind WI-376, leaving it Claimed-but-blocked with no CLI way to reopen -> had to hand-edit anthill-todo/workitems.anthill (status: Claimed(...) -> Open). FIX: add `update --status <Open|Claimed|...>` and/or an `unclaim`/`reopen` command; preserve id/depends_on/tags; set/clear agent+timestamp consistently with claim/deliver. Bundle lives in rustland/anthill-todo (anthill bundle + src/main.rs shim).

## Changes

### 2026-06-19T16:47:00Z — feedback — claude

DELIVERED 2026-06-19. unclaim/preopen/promote already existed (WI-310 + follow-up); the remaining scope was the GENERAL correction route: 'update --status <Status> [--reason R]' (case-insensitive) sets ANY WorkStatus in one atomic replace, stamping agent+now like claim/deliver (Claimed/Delivered), now only (Verified), or nothing (Open/PreOpened/Draft, clearing the claimant); Rejected/ProposalRejected/Stale carry (--reason, now) and REQUIRE --reason. id/depends_on/tags preserved. Loud errors (exit 2, no write) on unknown status, missing --reason, and (per /code-review) a stray --reason on a non-reason/absent status. Classification centralized in status_for -> UpdateStatus result sort (status_ok/needs_reason/reason_unused/unknown) — no string-fallback, no dup. GOTCHA: a locally-defined sort field typed WorkStatus needs 'import anthill.stage0.{WorkStatus}' (the type), not just its constructors — unresolved field type is only a WARNING but silently broke the store atomic replace (append-without-retract). Files: rustland/anthill-todo/anthill/main.anthill + tests/cmd_update_status_test.rs (11 tests). Full workspace green (2122).

