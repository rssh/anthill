## Attributes

- id: WI-529
- created: 2026-06-20T10:00:22Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-20T13:02:44Z

- acceptance: cargo-test

## Description

Split the boolean-operator / logic-primitive conflation (not/or/and) + route prefix-neg; deferred from WI-521. DECISION + detail in docs/design/kernel-vocab-provenance.md section C.1 — no new proposal (boolean-family analogue of proposal 049 (= vs <=>): dispatched value op vs resolver primitive). Bool.not/and/or ALREADY EXIST (bool.anthill + bool_not/and/or builtins) so MOST is RESOLUTION ROUTING. DISAMBIGUATE BY SYNTACTIC POSITION: op-body value -> Bool.X; rule-body goal -> resolver primitive. (1) not -> Bool.not vs reflect.not (NAF); (2) or -> Bool.or vs kernel.or (disjunction); (3) and -> Bool.and (goal conjunction is the comma); (4) neg (prefix -) -> Numeric.neg. ONLY NEW STDLIB: Numeric.neg op + default neg(?a)=sub(zero-val,?a). See WI-521 feedback + design doc C.1.

## Changes

### 2026-06-20T13:02:42Z — feedback — claude

DELIVERED (commits 8cc3897 + review-fix 908579a; full workspace green, merged with proposal-049 WI-522/523/525). MECHANISM (op-body vs resolver): op body EVALUATED -> not/or/and = dispatched Bool VALUE ops (eval builtins); rule body RESOLVED -> not/or stay reflect.not NAF / kernel.or disjunction (no eval builtin, so a Bool op can never be an SLD goal — why neq/NAF/disjunction MUST stay primitives). Loader.in_op_body_value (convert_expr_term, entry debug_assert) + POST-RESOLUTION redirect in remap_name_str (reflect.not->Bool.not, kernel.or->Bool.or AFTER resolution, so it beats an explicit import anthill.reflect.{not} Found-shadow; user own not/or = different symbol, untouched). Bool.and + Numeric.neg in PRELUDE_QUALIFIED. numeric.anthill: Numeric.neg + neg_def; numeric_neg eval builtin. Repaired pre-existing broken op-body and/not (cli/parse.anthill, lf1). TEST wi529_boolean_operator_split_test.rs (4). REVIEW (high; 3 finders+verify): import-shadow CONFIRMED+FIXED via the redirect; rest refuted/hardening. DEFERRED: prefix `-x` on NON-literals — `-` in tree-sitter _prefix_op COLLIDES with negative-literal lexing (/-?[0-9]+/), broke eq(state.seq,-1) in lf1, generate did NOT flag it -> REVERTED; use neg(x) + negative literals; a real `-x` op needs unifying with negative-literal lexing (broad blast radius), separate grammar task. GOTCHA: grammar.js change leaves a stale tree-sitter rlib in workspace cargo test -> cargo clean -p tree-sitter-anthill. scaland NOT mirrored.

