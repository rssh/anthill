## Attributes

- id: WI-681
- created: 2026-07-11T07:19:42Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-11T10:13:48Z

- acceptance: cargo-test

- depends_on: WI-669

## Description

Apply WI-669 body-derivation to the lf1 GPS safety proof — derive the formation geometry (and approach law, backend permitting) from the follower_gps controller body, shrinking the trust surface to the inner-loop velocity envelope only.

CONTEXT: examples/webots-modelling/lf1/. The GPS safety proof (safety_gps.anthill + safety_common.anthill) is DECOUPLED from the actual controller code (follower_gps.anthill): the bridge is the hand-asserted velocity envelope (KinematicAssumptions + velocity_envelope/step_distance_lemma 'by trust' axioms) and the follower's nominal separation is hand-planted as the literal 4.0 in real_pose_at(0, Follower, ...). The controller IS an anthill body (compute_controls + desired_position), so WI-669 defining equations CAN feed this proof. Three tiers:

  1. FORMATION GEOMETRY (this ticket) — desired_position(leader, offset) = leader.pos + R(leader.yaw)*offset; a 2-D rotation preserves norm so |desired_position - leader.pos| = |offset| independent of yaw. FULLY body-derivable (only nonlinear fact is cos^2+sin^2 = 1, QF_NRA-decidable, NOT a trust axiom). Derive the leader<->follower-target distance from the desired_position body via WI-669's op_defining_equations, retiring the hand-planted 4.0. desired_position is single-arm, let-free -> works with the WI-669 inc-1a engine as-is.

  2. APPROACH LAW — compute_controls (atan2 yaw + log10 pitch) governs HOW the follower closes on the target (proof abstracts this into step_distance_bound). Body-derivable but transcendentals push discharge to a dReal-class backend, not Z3 LRA/NRA. BACKEND limitation, lower priority.

  3. INNER-LOOP VELOCITY ENVELOPE — commanded Controls -> actual velocity is mavic_base.cpp (Quoted('cpp'), NOT modeled). STAYS a trusted axiom unless the inner loop is modeled/empirically bounded. Irreducible trust surface.

SCOPE: tier 1 — wire desired_position's body-derived equation into the GPS proof so |offset| comes from the body, not a constant. Tiers 2/3 documented as follow-on / surviving trust.

DEPENDS: WI-669 (defining-equation engine; inc 1a delivered, 1b = SMT seam pending). Reuses KnowledgeBase::op_defining_equations. Sibling consumer: the transponder ranking ticket (LIA/integer-state-machine case; this one is the QF_NRA geometry case).

ACCEPTANCE: GPS safety proof reads the follower-target separation from a desired_position body-derived equation (no hand-written <=> twin, no hand-planted 4.0 for the geometry); trust surface reduced to tier 3 (+ tier 2 backend-gated); anthill check + discharge.sh green; cargo-test green.

