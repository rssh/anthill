```anthill
fact WorkItem(id: "WI-373", created: "2026-06-03T21:48:55Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-14T20:12:29Z"))

fact Feedback(workitem: "WI-373", author: "user", at: "2026-06-03T21:48:45Z")

fact Feedback(workitem: "WI-373", author: "user", at: "2026-06-03T22:28:15Z")

fact Feedback(workitem: "WI-373", author: "user", at: "2026-06-04T06:51:36Z")

fact Feedback(workitem: "WI-373", author: "user", at: "2026-06-04T07:33:30Z")

fact Feedback(workitem: "WI-373", author: "user", at: "2026-06-14T20:12:25Z")
```

## description

WI-348 Phase C: value rule heads — make a rule HEAD carrier-agnostic (carry Value::Node), not just facts. Today RuleEntry.head is already a Value (Phase B) but the rule-assertion entries (assert_rule_nodes / assert_rule_debruijn_with_nodes / _in_frame / finalize_rule_debruijn_nodes) take head: TermId and wrap Value::Term; assert_fact_value already proves carrier-agnostic head storage+indexing (incref_value_ground, TermView::head functor, skip-dedup-for-Node, insert_pattern via Value). SLICE 1 (assertion/storage/closure): head param -> impl Into<Value> (+ impl From<TermId> for Value so TermId callers are unchanged); carrier-agnostic var-collect (collect_vars_rec | collect_occurrence_global_vars_ordered), DeBruijn-close (term_to_debruijn | node_to_debruijn), converge assert_rule_nodes storage/indexing with assert_fact_value; behaviour-identical for term heads; driving test = assert Value::Node-headed rule, read via rule_head_value, find via query_view. SLICE 2 (resolver): with_fresh_vars opening a value head = De-Bruijn-over-occurrence (the documented Phase C gap, mod.rs ~2358); + rule_head term-only readers migrate reactively. NO consumer yet (forward-correct substrate, like the rest of WI-348). sort stays TermId (nominal bucket key, by_sort/by_domain/fact_dedup; hash-cons-appropriate per the representation note; even assert_fact_value keeps sort: TermId). Follows WI-348 (delivered) + WI-372.

## Feedback

### 2026-06-03T21:48:45Z — user

SLICE 1 DELIVERED 2026-06-03: carrier-agnostic rule-head STORAGE. impl From<TermId> for Value (TermId = universal Value::Term carrier); assert_rule_nodes head: TermId -> impl Into<Value>; new shared push_value_head_entry (incref_value_ground + functor-via-TermView + push RuleEntry + by_sort/by_domain/rules_by_functor + discrim insert_pattern via Value) CONVERGES assert_rule_nodes + assert_fact_value into one storage epilogue (was duplicated — /code-review finding). Ground value RULE heads now storable/indexable/readable (test value_rule_head_node_stored_and_read_back: denoted head via assert_rule_nodes, Rc::ptr_eq identity). Behaviour-identical for term heads: incref_value_ground(Value::Term(t)) == terms.incref(t) (collect_value_ground_terms_into pushes [t] not leaves; symmetric w/ retract release_value_ground); functor/discrim/fact_dedup all equivalent (/code-review verified all 6, 0 bugs). 1435 green. === KEY DISCOVERY: VAR-BEARING value heads (the DeBruijn-CLOSURE path = the asked-about assert_rule_debruijn_with_nodes_in_frame) are blocked on THREE unimplemented Phase C gaps, found empirically: (1) WI-342 P3 Type-occurrence var-walk — denoted occs are NodeKind::Type; collect_occurrence_global_vars_ordered + node_to_debruijn + open_debruijn_node + substitute_occurrence all debug_assert 'not wired until P3'; (2) discrim DeBruijn-value-head keying — discrim.rs panics 'value-fact keying unimplemented (WI-348 Phase B/C)' on a DeBruijn var in a value head; (3) with_fresh_vars opening a value head (rule_head term-only). So the DeBruijn entries KEEP head: TermId (carrier-agnostic param there is premature/fake until 1+2+3 land) — I tried impl Into<Value> there + reverted. SLICE 2 = wire (1)+(2)+(3) together, then flip the DeBruijn-entry head params. sort stays TermId (nominal bucket key; even assert_fact_value keeps it).

### 2026-06-03T22:28:15Z — user

GAP 2 (discrim DeBruijn-value-head keying) DELIVERED 2026-06-03 (committed 9b3a8e3): 'make Value/Occurrence vars the same form as term'. The trait doc already prescribed it (index_var keys a stored-pattern var of ANY kind; Value carriers override to also surface Rigid/DeBruijn) — TermIdView did (Term::Var(v)=>Some(v)) but the occurrence carriers were Global-only, so a DeBruijn/Rigid var inside an occurrence read Opaque and PANICKED at discrim insert. FIX: occ_index_var(occ)=match occ.as_expr(){Some(Expr::Var(v))=>Some(v),_=>None} wired into Value::Node + ViewItem::Node + Rc<NodeOccurrence> override. A value rule head's DeBruijn var now indexes a var-edge like a term head's. /code-review 0 bugs: insert/remove symmetric (both via index_var -> same Var::DeBruijn(n) edge); query/goal path reads head() only so goal-side unchanged; old behavior was a panic => zero regression. Test value_head_debruijn_var_in_occurrence_indexes_like_term. 1436 green (rebased onto origin 0e55935 / WI-374-376). REMAINING for end-to-end var-bearing value rule heads: gap 1 (WI-342 P3 Type-occ var-walk, DENOTED heads) + gap 3 (with_fresh_vars opening, RESOLUTION) + re-add carrier-agnostic head closure on DeBruijn entries (now unblocked for Expr-Node heads by gap 2).

### 2026-06-04T06:51:36Z — user

CLOSURE RE-ADD DELIVERED 2026-06-04: carrier-agnostic rule-head closure restored (after WI-378 pause). assert_rule_debruijn_with_nodes/_in_frame/finalize_rule_debruijn_nodes take head: impl Into<Value>; collect_value_head_vars + close_value_head_debruijn (Term/Node/Entity/Tuple recurse, scalars no-op, exotic carriers debug_assert per /code-review). A var-bearing value rule head now ASSERTS (collect the var inside an Expr Node, close to De Bruijn) + INDEXES (gap 2). Behavior-identical for term heads (Value::Term->collect_vars_rec/term_to_debruijn). Test value_rule_head_with_var_asserts_closes_and_indexes. 1440 green; /code-review 0 correctness (1 future-facing Tuple/Var silent-drop finding APPLIED: loud debug_assert). === GAP 3 IS DEEPER THAN 'wire with_fresh_vars' (key finding): RESOLVING a value rule head needs the DISCRIM BINDING-EXTRACTION, not just with_fresh_vars. Empirically: query vf(g(?y)) vs stored vf(Node(g(DeBruijn0))) FIRES but yields EMPTY tree_subst -> ?y unconstrained (silent wrong answer). ROOT: VarPath is single-level (Root|Arg) and recorded only for top-level args (bind_paths); a query var matched at a NESTED position inside the value head's Node records no path, and extract_value_at_path (persist_subst.rs:92) does not descend into a Node child. So with_fresh_vars KEEPS the LOUD rule_head panic guard (value-head resolution fails loudly, not silently mis-resolves) until the binding-extraction is built: (a) VarPath nesting/chaining, (b) extract_value_at_path descends into Node children, (c) bind_paths at nested levels. with_fresh_vars's arity>0 path reads-but-never-uses head (the head-match is in tree_subst), so once (a)-(c) land the panic-guard removal is trivial. This is the real remaining half of gap 3.

### 2026-06-04T07:33:30Z — user

GAP 3 BINDING-EXTRACTION DELIVERED 2026-06-04 (SLICE 2 resolver half): a query/goal var matched at a NESTED position now records its full path and binds carrier-faithfully. (a) VarPath: enum{Root,Arg(ArgPos)} -> newtype over SmallVec<[ArgPos;4]> (empty=root, multi-step=nested; shallow paths stay inline). (b) extract_at_path + extract_value_at_path walk the chain step-by-step, descending into Term/Value/Node children (value side via TermView, the indexed surface). (c) discrim query traversal: replaced pos_offset+bind_paths with a single prefix:&VarPath threaded through query_args/query_arg_value/follow_key_then_continue/skip_subtree_then_continue; descending a nested compound passes arg_path as the child container prefix, so vars at ANY depth record a path (was: nested recorded None and silently dropped). === KEY DISCOVERY: this was a LATENT CORRECTNESS BUG for plain TERM facts too, not just value heads. The discrim tree query DIRECTLY produces the answer subst (no separate unify step; proven at resolve.rs fact-answer path + query_view), so a goal holds(state(?x)) against ground fact holds(state(active)) FOUND the fact but left ?x UNBOUND (reified to itself) — a silent wrong answer. Now fixed; regression tests nested_var_binds_to_fact_subterm (discrim) + nested_var_goal_binds_against_ground_fact (resolve). === PANIC GUARD REMOVED: with_fresh_vars moved `let head=rule_head(id)` into the arity==0 legacy branch (its sole reader); the arity>0 path (where var-bearing value rule heads land) never reads head — match is fully in tree_subst — so a value Node/Entity head no longer panics. arity==0 value head still loud-guards (gap 1 territory). End-to-end test value_rule_head_with_var_resolves_and_binds_nested: vf(g(?y)):-thing(?y) + thing(active) => vf(g(?y)) binds ?y=active; vf(g(active)) holds; vf(g(missing)) fails. === SOUNDNESS BONUS: nonlinear nested patterns f(g(?x),h(?x)) vs f(g(1),h(2)) now correctly contradict (drop) instead of matching with ?x unbound. /code-review max effort, 2 finders + sweep, 0 correctness bugs; 1 robustness finding applied (silent invalid-path fallback -> debug_assert per loud-error principle). 1444 green (full workspace; +4 tests). REMAINING: gap 1 (WI-342-P3 Type-occurrence var-walk for DENOTED value rule heads) still open, tracked under WI-378 step 2 — distinct from this Expr-Node binding-extraction.

### 2026-06-14T20:12:25Z — user

DELIVERED 2026-06-14 (done-but-not-closed sweep; spot-verified GREEN). Phase C value rule heads (carrier-agnostic rule HEAD) delivered across 4 commits: 1969ff1 (slice 1 storage), 9b3a8e3 (gap 2 discrim keying), 6004d07 (closure re-add), 0d3bcda (gap 3 nested binding-extraction). Code present in tree: mod.rs:809,2162,2225 (assert_rule_*_nodes head: impl Into<Value>, var-collect/De-Bruijn-close), discrim.rs:595,987, persist_subst.rs:30,123,420 (VarPath nested descent), resolve.rs:5120 (arity>0 panic-guard removed). 6 acceptance tests exist+match: value_rule_head_node_stored_and_read_back, value_head_debruijn_var_in_occurrence_indexes_like_term, value_rule_head_with_var_asserts_closes_and_indexes, value_rule_head_with_var_resolves_and_binds_nested, + regressions nested_var_binds_to_fact_subterm, nested_var_goal_binds_against_ground_fact. Residual gap-1 carved to WI-378. VERIFIED 2026-06-14: anthill-core --lib -- value_rule_head = 3 passed / 0 failed. Tracking-only close.

