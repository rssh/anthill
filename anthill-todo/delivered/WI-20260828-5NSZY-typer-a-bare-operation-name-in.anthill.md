## Attributes

- id: WI-20260828-5NSZY-typer-a-bare-operation-name-in
- created: 2026-08-28T22:12:39Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-29T04:37:38Z

- acceptance: cargo-test, scaland-sbt-test

- tags: typing

## Description

typer: a BARE OPERATION NAME in a POLYMORPHIC slot cannot reach the arrow that would let it lift, so it is REFUSED where it could be accepted. MEASURED: `operation apply_it(o: Option[T = Function[A = Int64, B = Int64]], v: Int64)` fed `apply_it(some(inc), 41)` is a load error at the reference — `some(value: T)` is not callable by head, so no constructor-argument hint is computed, the name reaches `check_bare_ref` with `expected = None`, and WI-20260828-2TMB5 refuses a lift with no arrow to lift against. THE REFUSAL IS CORRECT AS IT STANDS and must not simply be relaxed: with no arrow, `attach_eta_dispatch_dict` pins neither the requirement dictionary nor the argument-spread labels, and the first cut of 2TMB5 — which lifted anyway against the operation's own arrow — made `via_option(some(sub2))` return -7 where its arrow-slot twin returned 7. The fix is to make the ARROW REACH THE REFERENCE, not to lift without one. WHERE TO LOOK, and what has already been REFUTED: `ctor_field_expected` (typing.rs) already walks a declared field type through the constructor's own `expected`, and a hint built on it (gated `type_head_is_callable` on the WALKED type, the exact peer of `variant_field_expected_from_ctor`) was written and MEASURED NOT TO FIRE — because the constructor itself has `expected = None`. The chain breaks ONE LEVEL UP: `one_arg_hint` pushes an operation's declared parameter type into a CONSTRUCTOR-APPLICATION argument only when `variant_slot_arg_hint` accepts it, i.e. when the parameter type names an ENTITY; `Option[T = ...]` / `List[T = ...]` name a SORT, so nothing is pushed. A second probe that pushed the parameter type down whenever the constructor argument carried a bare operation name ALSO did not suffice on its own. So this needs BOTH halves: the param type reaching the constructor, and the walk from there to the field. CONTAINMENT IS THE RISK, not the mechanism — pushing an expected type into constructor arguments that took none changes the WI-20260826-JSFHG classification decision (`expected_names_an_entity`) and the expected-seed for every such build, so it needs its own census of what newly receives a hint. ACCEPTANCE: `apply_it(some(inc), 41)` evaluates to 42, DRIVEN; a 2-parameter operation through the same polymorphic slot spreads BY LABEL (the `via_option(some(sub2))` / `direct(sub2)` pair in wi_2tmb5_bare_op_name_zero_arg_reading_test must BOTH return 7); a `requires`-carrying operation through that slot mints a dictionary rather than dying in a foreign apply frame; and every row of wi_2tmb5_bare_op_name_zero_arg_reading_test that is about a NON-callable slot stays refused.

## Changes

### 2026-08-29T04:37:32Z — feedback — user

DELIVERED. The arrow an author pins ONE LEVEL OUT now reaches a bare operation name nested inside a constructor argument, so `apply_it(o: Option[T = Function[A = Int64, B = Int64]], v: Int64)` fed `apply_it(some(inc), 41)` returns 42 instead of being refused.

IT TOOK BOTH HALVES, exactly as the ticket predicted, and each was measured alone and found to change nothing. `arrow_field_expected_from_ctor` walks the field's declared type through the CONSTRUCTOR's own `expected` (`some`'s `value: T` becomes `Function[…]` once `T` is pinned) — alone it fires NOWHERE, because the constructor had no `expected` to walk. `ctor_arg_unlocks_an_arrow_for_a_bare_name` supplies that `expected` — alone it changes nothing, because the hint it enables is the first one. Backing out either turns every capability row red and leaves both controls green.

CONTAINMENT WAS THE STATED RISK, AND THE CENSUS CAUGHT REAL OVER-FIRING. Pushing an expected type into a constructor is not neutral: `check_constructor_iter` reads it for the §8.2 classification (`expected_names_an_entity`) and seeds it into the build. My first gate ("the walk reaches a callable") fired on FOUR stdlib sites -- `Stream.splitFirst`, `Iterable.iterator`, `FiniteCollection.collect` -- plus `wi8q0q5`'s carrier, every one an already-arrow field that `arrow_slot_arg_hint` (WI-20260828-8Q0Q5) already serves and which needs no `expected` at all. Adding the second condition -- the field's DECLARED type must not be callable already -- reduced the firing set to nothing outside the new rows. Re-censused with a probe on both halves over the whole workspace: no stdlib call site receives a hint it did not receive before.

/code-review FOUND FOUR MORE ROUTES, three of them fixed inline and all four reproduced first. (1) An EPONYMOUS PARAMETRIC sort was refused while the `Option` spelling of the identical program returned 42 and a lambda in the same slot returned 41 -- one arrow, three verdicts by spelling. Cause: `ctor_field_expected` read `strict_parent_sort`, which subtracts exactly the reflexive case. WI-946 had left that one strict reader in place and written down why -- "no probe made the difference observable ... converting is still the right move the moment one exists" -- and WI-20260828-2TMB5 created that probe by making a missing hint a REFUSAL rather than a worse inference. Converted, with the row that forced it. (2) A constructor nested in a CONSTRUCTOR FIELD never threaded: the supplier was wired into `one_arg_hint` only, so the constructor-field chains had the reader with nothing to supply their `expected`. Same supplier, both routes. (3) The body-less diagnostic's trailing "Write a lambda that calls it instead" is now conditioned on the slot actually declaring an arrow -- where none does, a lambda is equally inadmissible. (4) The gate now tests the cheap bare-name predicate before allocating `entity_field_types(..).to_vec()`, and reads the PROJECTION-ELIMINATED parameter type its siblings read (WI-485); measured to change no verdict, so it is agreement rather than repair.

TWO ROUTES DELIBERATELY NOT REACHED, recorded at the test file rather than papered over. A LIST/SET LITERAL (`head_apply([inc], 41)`) stays refused where its desugared twin returns 42, and the hint is WITHHELD rather than missing: `TypeBuildFrame::ListLit` takes `element_hint` as the element type UNCONDITIONALLY and never consults what the elements typed as, so a hint there overwrites rather than checks (`operation mk() -> List[T = Int64] = ["x"]` loads clean today). Supplying one would trade a correct refusal for a silent accept. That is WI-20260826-7JDWY, now marked as depending on this item. And a CALLEE TYPE PARAMETER inside the arrow (`Function[A = X, B = Int64]`) leaves the spread labels unpinned so the callback spreads by SOURCE ORDER -- measured -7 -- but this is PRE-EXISTING and not about the nesting: the direct arrow-slot twin answers -7 too. So the rows asserting a label ordering use GROUND arrows, and the entity-chain row (whose callee carries an `X`) asserts only that the arrow ARRIVES, which is what that shape can honestly witness.

WI-20260828-2TMB5'S RULE IS UNWEAKENED. A slot that pins no arrow anywhere still refuses the bare name, and a body-less operation still has no function-value form; those rows are green throughout, and `a_slot_that_pins_no_arrow_still_refuses` is the control that keeps every capability row above from being satisfied by a fix that pushes every parameter type into every constructor argument. 2TMB5's own polymorphic row, which asserted that slot was refused, is now the driven agreement row: the arrow slot and the polymorphic slot must BOTH return 7, since 7 and -7 are the two orderings of the same subtraction and only the pair separates "labels pinned" from "labels happen to match source order".

ONE HONEST ASYMMETRY: the hint is gated on the argument being a bare OPERATION NAME, so an inline lambda in the same nested slot still receives no expected type and `lambda x -> x + 1` there is refused `ambiguous dispatch of Additive.add`. Measured identically with the change on and backed out, so it is pre-existing and unchanged; the lambda control uses an identity lambda and says at its site why.

Tests: rustland/scripts/test.sh green -- 36 binaries, 6000 passed, 0 failures. scaland sbt test green -- 544 passed, 0 failures. rustfmt introduces no new complaint (89 at HEAD, 89 now).

### 2026-09-07T17:00:11Z — feedback — claude

THE LIST/SET-LITERAL RESIDUAL IS CLOSED. `head_apply([inc], 41)` against `List[T = Function[A = Int64, B = Int64]]` now returns 42, as its desugared twin `head_apply(cons(inc, nil()), 41)` always did — one program, one answer, whichever way the list is written. The set surface loads too. Rows: `a_literal_spelling_reaches_the_nested_name` (driven, both spellings) and `the_set_literal_spelling_reaches_it_too`; `typer_capability_matrix_test`'s routes 6 and 7 are the cells that carried it.

IT WAS RED FOR TWO DIFFERENT REASONS IN TURN, and that sequence is the thing worth keeping. This ticket WITHHELD the hint and said why: a hinted literal took `element_hint` as its element type UNCONDITIONALLY, so pushing one would have traded a correct refusal for a silent accept. WI-20260826-7JDWY removed that — a declared element type is now CHECKED — and the route STAYED refused for a narrower reason nobody had written down until the capability matrix forced it: 7JDWY restored the argument-slot hint only where the element type MENTIONS AN ENTITY (WI-20260826-JSFHG's containment gate), and `Function[A = …, B = …]` names a spec. Widening that gate to admit a CALLABLE element is what finally closed it. "The blocker was closed" is not "the cell is fixed".

THE HINT IS ITS OWN KIND NOW. It had been living inside `variant_slot_arg_hint`, which is about §8.2's classification, because the variant case was the first that needed it. The two ask different questions of different types — that one whether the SLOT mentions an entity, this one what the ELEMENT is — so a reader looking for "why does my list literal get an expected type" was reading a function named for variants. `seq_slot_arg_hint` reads the declared element through `declared_element_type`, which also gives it the own-surface rule for free: a `[…]` in a `Set[T = X]` slot is a shape disagreement and gets nothing. `type_head_is_callable` is borrowed from `arrow_slot_arg_hint` so "is this slot callable" has one answer wherever it is asked.

TWO GATES, NOT ONE GATE WIDENED TWICE — measured, and the sets are DISJOINT. Dropping the whole hint from `one_arg_hint`'s chain fails 6; dropping only the `type_mentions_an_entity` arm fails 3 (all of 7JDWY's variant rows, none of these); dropping only the `type_head_is_callable` arm fails 3 (these two plus the matrix's bare-name row). The union of the two arms is exactly the whole-hint set.

WHAT ELSE THIS PASS FIXED, because `/code-review` found it and it was the more serious defect: `join_types` returned the BARE BASE SORT when two same-base parameterized types had a binding it could not combine — `Option[T = Int64] ⊔ Option[T = String] = Option` — on the recorded reasoning that this is "the conservative common supertype". It is an upper bound and NOT a usable one: `types_compatible` treats bare-vs-parameterized as compatible in BOTH directions, so `Option` is also BELOW every instantiation, and a "least upper bound" that is simultaneously a lower bound launders the bindings. MEASURED: `takeOpts([some(1), some("x")])` against `List[T = Option[T = Int64]]` loaded, putting a `String` in an `Int64` slot — and so did the `if` spelling. That arm answers NO JOIN now. It overturns WI-464's and WI-769's recorded decision; the three unit tests that pinned it are rewritten to state the new one and why, and nothing else in the workspace depended on it.

AND `join_types` GAINED A HASH-CONSED FAST PATH, which is where the O(n) question landed. `join(a, a) = a` was reached only after `types_compatible` in both directions (two `Substitution` allocations) plus `more_general_type`; `TermId` equality answers it in one comparison, sound because the store is hash-consed by structure. Measured over the whole `wi_tests` corpus, 449,660 calls: 88.3% take the fast path, 11.7% take the walk, and 0.0% involve a non-interned carrier — which settles the question of whether `Value` needs a computed hash to generalize it. There is nothing here for one to serve, and the zero is recorded at the site so a future producer routing a `Value::Node` there is not silently uncovered. Backing the fast path out fails NOTHING, which is the correct outcome for a performance change and is its correctness evidence.

VERIFICATION. Full workspace 6589 passed / 0 failed. Five back-outs, each a run: the erasing-join arm 5 fail; the fast path 0; the literal join 9; join-vs-subtype 1; the value carrier 1. scaland untouched.

