```anthill
fact WorkItem(id: "WI-1063", created: "2026-08-08T23:33:10Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-08-09T02:37:09Z"))

fact Tag(workitem: "WI-1063", name: "typing")

fact Feedback(workitem: "WI-1063", author: "user", at: "2026-08-08T23:44:51Z")

fact Feedback(workitem: "WI-1063", author: "user", at: "2026-08-08T23:52:01Z")

fact Feedback(workitem: "WI-1063", author: "user", at: "2026-08-09T00:08:07Z")

fact Feedback(workitem: "WI-1063", author: "user", at: "2026-08-09T00:16:30Z")

fact Feedback(workitem: "WI-1063", author: "user", at: "2026-08-09T02:36:58Z")
```

## description

AN UNWRITTEN SORT PARAMETER IN A RETURN IS EXISTENTIAL: SKOLEMIZE IT AT THE CALL, NOT IN THE BODY (typing; WI-1061 residue, driven end-to-end 2026-08-08/09). THE HOLE, still reachable and pinned by wi1061_unwritten_slot_positions_test::a_return_position_unwritten_row_is_not_yet_rigid and ::an_effectful_stream_still_reaches_a_pure_slot_through_a_return: 'operation takes_pure(s: Stream[T = Int64, E = {}]) -> Int64' plus 'operation widen(s: Stream[T = Int64, E = {Error}]) -> Stream[T = Int64] = s' plus 'operation exploit(s: Stream[T = Int64, E = {Error}]) -> Int64 = takes_pure(widen(s))' loads clean, and an effectful stream reaches a slot that declared E = {}. A SECOND WITNESS, inside a DELIVERED feature: 'operation openOne(m: MemStore) -> C ensures KVStore[C] = m' with 'MemStore provides KVStore[K = String, V = String]', then 'needs_int_key(openOne(m))' where 'needs_int_key(s: KVStore[K = Int64, V = String])' -- also loads, K = String satisfying a demand for K = Int64. So this is NOT confined to bare/partial returns and a fix confined to them would leave the ensures form leaking. THE RULE. Read the quantifier off the POLARITY of the arrow. A parameter's unwritten slot is negative-position and UNIVERSAL: the caller instantiates it, so at a call it is flexible and binds from the argument -- that is section 8.1's 'threads bindings' feature and WI-1059's body rule, one thing from two ends. A RETURN's unwritten slot is positive-position and EXISTENTIAL: 'operation widen(...) -> Stream[T = Int64]' declares 'exists E. Stream[T = Int64, E]'. The BODY PACKS -- it exhibits a witness -- so widen is CORRECT AS WRITTEN and must keep loading; 'the inferred type is more specific than the declared type' is existential introduction, not a defect. EACH CALL OPENS -- it mints a FRESH Skolem constant per opening, so 'widen(s)' has type 'Stream[T = Int64, E = rho]' with rho rigid, and 'takes_pure(widen(s))' is refused because rho is not {}. Freshness PER OPENING is load-bearing: two calls may genuinely return different rows, so one shared constant would be unsound. WHERE THE SKOLEM IS MINTED IS THE WHOLE DESIGN. Not in the declaration and NOT in the body check. Minting it in the body check is the rejected option below: same word, opposite site, opposite verdict -- it makes the BODY satisfy the rigid and refuses widen, which is the wrong quantifier. Minting it at the CALL leaves widen alone and refuses exploit. IMPLEMENTATION SITE: check_apply_iter (typing.rs, the 'proj_return_type' computation around line 10562 and the unify against 'expected' below it), where a callee's declared return type becomes the call's result type. There is NO existing opening machinery to copy -- the WI-402 carrier case gets its abstraction for free because 'strip_spec_carrier' (load.rs build_existential_return) DROPS the carrier so the declared return literally IS the abstract spec, and 'needs_mem(openOne(m))' then fails by ordinary subtyping at 'expected MemStore, got KVStore'. The carrier supplies the semantics, not code. WHAT MUST STILL HOLD. (1) An escaping skolem is the avoidance problem, which is exactly what WI-401's gate already names ('the abstract member would escape its scope'); that gate must cover skolems minted this way. (2) A row-polymorphic consumer must keep working -- 'f[E](s: Stream[T = Int64, E = E])' accepts a skolem, because a universal instantiates to anything including a rigid. (3) THE RISK TO MEASURE: code that currently relies on a row threading THROUGH a return, the WI-368/WI-374 'collect(iterator(xs))' shape. WI-380 rewrote the stdlib to explicit forms so it may survive; that is the measurement, not an assumption. REJECTED, with the measurement that rejected it: rigidify the return IN THE BODY CHECK (the first thing WI-1061 built). It works and refuses widen at 'widen.return: expected Stream[E = ?E, T = Int64]', but it demands the WRONG QUANTIFIER -- universal in a positive position -- and costs 40 tests across thirteen delivered tickets (WI-353/374/401/402/405/457/480/488/491/734/762/776/839), mostly correct programs refused for failing to be universally polymorphic where only a witness was asked. It also silently retires the WI-401/402/457/480/488/491 escape gate: abstracting_return_error sits in the else of !conforms, so a non-conformable return means it never runs and its whole diagnostic family disappears. Do not revive it without defending the universal reading on its merits. ALSO REJECTED: requiring the explicit 'ensures' spelling. Driven -- 'operation widen(s: Stream[T = Int64, E = {Error}]) -> C ensures Stream[C, T = Int64] = s' loads AND so does takes_pure(widen(s)); the explicit form leaks identically, because the ensures rewrite reduces the return to a bare spec and unbound MEMBERS are the un-enforced cell. ACCEPTANCE (each test says which case fails when backed out): widen still LOADS and exploit is REFUSED, at the call rather than at widen's declaration; the openOne/needs_int_key chain is refused the same way, and wi402_existential_return_test grows a member-opening row (it has none today -- every row checks admission, not what a caller may do with the result); two calls to one existential-returning op get UNRELATED skolems, driven, not assumed; a row-polymorphic consumer 'f[E](...)' still accepts one; an escaping skolem is refused by the WI-401 gate with its own diagnostic; the two pins in wi1061_unwritten_slot_positions_test flip to assert the new verdicts (they are the control -- both pass today); wi1059_unwritten_param_rigid_test and wi1056::all_four_spellings_of_one_type_conform_alike stay green; the corpus loads clean at all SEVEN tiers (stdlib, rustland/anthill-stl/anthill, rustland/anthill-todo/anthill, rustland/anthill-cpp-gen/anthill, examples/, anthill-todo/, anthill-testcases/) measured through 'anthill load' per tier, whose control is that it DOES report the WI-1059/WI-1061 parameter-position refusals; and BOTH docs state the decided rule with neither contradicting. NOTE: 'anthill-todo/' at the repo root holds WORK ITEMS; the CLI's own anthill program is 'rustland/anthill-todo/anthill'. REFERENCE: WI-1061 (parameter positions delivered: top level, nested, callback result, tuple component; and the block comment at the return site in check_operation_bodies); WI-1059 (the parameter rule and its projection filler); WI-402 + docs/design/path-dependent-types.md section 5 (the explicit existential, 'ensures Spec[C]', and the four-cell gap: carrier enforced both halves, members neither); WI-401 (the escape gate and the avoidance problem); WI-374 + docs/design/type-parameter-scoping.md sections 4-5 (erasure, which IS this existential informally stated); docs/kernel-language.md section 'Expansion during unification'.

## Feedback

### 2026-08-08T23:44:51Z — user

CORRECTION to this ticket's own framing, found by re-reading the spec paragraph in full instead of quoting it from memory. THE TICKET SAYS 'two readings, both written down'. That is WRONG, and the difference decides where the burden sits. docs/kernel-language.md section 'Expansion during unification' states the EXPANSION at every sort application -- a return type included -- and its own statement of effect is about the CALL: 'a parameter declared s: Stream, unified against an argument of type Stream[T = Int64, E = {}], binds the expanded T and E, so both the element type and the access effect ground AT THE CALL instead of silently dropping'. The BODY OBLIGATION is in the NEXT paragraph, which WI-1059 added (commit 6d94d327): 'An unwritten parameter is fresh at each call, so the BODY must hold for every instantiation of it.' Every example in it is a PARAMETER, the refusal is at feed's parameter, and 'at a call it is flexible again, and binds from the argument' is about the argument side. IT NEVER EXTENDS THE OBLIGATION TO A RETURN. So option (a) is an EXTRAPOLATION from the parameter rule, not a competing written rule; docs/design/type-parameter-scoping.md section 5 is the only text that speaks to the return, and it says ERASED. One reading stated, one unstated -- the burden is on (a) to be written down, not merely inherited. TWO DELIVERED TESTS ALREADY BET ON THE STATED READING, which the ticket did not record: wi374_expansion_test::foreign_bare_return_op_loads_and_narrows pins 'makeList() -> List = cons(head: 1, tail: nil)' as LOADING and says at its site that the return is deliberately NOT expanded; and bare_value_stays_unusable refuses the CONSUMER of an erased value, which is exactly where section 5 puts the fault -- so option (b)'s enforcement point is not a new invention, it is the same principle with its remaining hole. THAT HOLE, stated precisely, is what option (b) has to close: an absent slot expands to a FLEXIBLE variable at the call, so erasure bites for T (a visible element mismatch downstream) but not for E (a fresh row var closes to {} and satisfies any demand). ALSO CORRECTED: the cost. 40 tests across THIRTEEN delivered tickets (WI-353/374/401/402/405/457/480/488/491/734/762/776/839), not 32 across nine -- re-measured on the delivered tree by re-enabling the return walk; 42 failures of which 2 are this ticket's own pins. WI-488 and WI-762 are reached only once WI-1061's arrow-result and tuple-component arms are in place, which the first count predated. Both figures are fixed in kernel-language.md, type-parameter-scoping.md, the typing.rs block comment and the test header.

### 2026-08-08T23:52:01Z — user

THE ASYMMETRY THAT MAKES OPTION (b) COHERENT, and the concrete experiment to run FIRST. State it in the language's own vocabulary -- every mismatch prints 'expected X, got Y', where EXPECTED is the type written on the thing being fed and GOT is the type of the value feeding it. Every check unifies one of each, and an unwritten slot behaves differently depending on which side it is on. THE FEATURE (section 'Expansion during unification' says this is what expansion is FOR): the missing slot is on the EXPECTED side. 'operation takes_any(s: Stream) -> Int64' called as 'takes_any(mk_int())' where 'mk_int() -> Stream[T = Int64, E = {}]' -- expected is bare Stream (T and E missing), got is fully concrete, so the fresh vars bind FROM got and the signature LEARNS Int64 and {}. Verified real and not ignored: 'takes_int(mk_str())' with 'takes_int(s: Stream[T = Int64])' is REFUSED at 'expected Stream[T = Int64], got Stream[T = String, E = {empty_row}]', so the argument's slots genuinely reach the demand. THE LEAK: the missing slot is on the GOT side. Three checks run in the widen/exploit program and only the third leaks. (1) widen's OWN BODY CHECK -- expected = its declared return 'Stream[T = Int64]' (E MISSING), got = the body 's : Stream[T = Int64, E = {Error}]'. Missing on EXPECTED; it expands flexible and swallows {Error}. This is the site option (a) would fix. (2) the inner call 'widen(s)' -- both sides complete, nothing happens. (3) the outer call 'takes_pure(widen(s))' -- expected = 'Stream[T = Int64, E = {}]' (demands empty), got = widen's declared return 'Stream[T = Int64]' (E MISSING). Missing on GOT; expansion mints a fresh var for it and binds it to {}, so a value that really carries {Error} is TOLD it is pure. This is the site option (b) would fix. SO OPTION (b) IS NOT 'undo expansion'. Case 1's missing slot is on EXPECTED and case 3's is on GOT -- same mechanism, opposite sides. The rule to try is: AN UNWRITTEN SLOT ON THE GOT SIDE IS OPAQUE, NOT UNIVERSALLY ACCOMMODATING -- it may not bind to satisfy a concrete demand; an unwritten slot on the EXPECTED side still binds from the argument exactly as today. That is precisely section 5's 'a consumer may rely only on what the type carries', and it leaves section 8.1's stated purpose ('a signature written against a bare sort still threads bindings') untouched, because that purpose is the EXPECTED-side case. THIS ASYMMETRY IS WRITTEN DOWN IN NEITHER DOCUMENT and is NOT MEASURED -- I have driven only that the two cases exist and behave as described (demo file: bare-expected call loads, wrong-element call refused, widen/exploit chain loads). RUN THIS FIRST: implement the got-side cut and measure the corpus at all seven tiers plus the full suite. If it survives, option (b) wins outright -- it is the authorized reading, it is cheaper than (a)'s 40 tests, and it leaves the WI-401/402/457/480/488/491 escape gate's precondition intact because the return type is never materialized. If it does NOT survive, the failures say what legitimate threading depends on a got-side absent slot binding, and THAT list is what decides between (a) and (c).

### 2026-08-09T00:08:07Z — user

THE RETURN SLOT IS EXISTENTIAL, NOT UNIVERSAL -- user's reading, 2026-08-09, and it invalidates option (a) rather than merely leaving it unauthorized. READ THE POLARITY OFF THE ARROW. A parameter's unwritten slot is in NEGATIVE position: universally quantified, the CALLER instantiates it, so at a call it is flexible and binds from the argument. That is WI-1059's rule and the section 8.1 'threads bindings' feature, one thing seen from two ends. A RETURN's unwritten slot is in POSITIVE position: EXISTENTIALLY quantified. 'operation widen(...) -> Stream[T = Int64]' declares 'exists E. Stream[T = Int64, E]'. The BODY PACKS -- it supplies a witness -- so widen is CORRECT as written, and 'the inferred type Stream[T = Int64, E = {Error}] is more specific than the declared type' is exactly existential introduction, not a defect. The CONSUMER OPENS -- it gets a fresh RIGID constant -- so 'takes_pure(widen(s))' is refused because that constant is not {}. WHAT THIS INVALIDATES. WI-1061's description says 'the RETURN flips the quantification -- the body must PRODUCE a value good for every instantiation, not consume one', and option (a) implements exactly that. It is not a flip: it keeps FOR-ALL and moves it to the produce side, which is the strongest reading available and is precisely why it costs 40 tests across 13 tickets. A real flip gives THERE-EXISTS, under which widen must load. So option (a) does not merely lack a written authorisation -- it demands the WRONG QUANTIFIER, and its 40 failures are mostly correct programs being refused for failing to be universally polymorphic in a position that only asked for a witness. Downgrade (a) accordingly. WHAT IT EXPLAINS. The expected/got asymmetry recorded in the previous note is the SYMPTOM; this is the cause. 'An unwritten slot on the got side is opaque' is just what OPENING an existential looks like at the unification boundary, and 'an unwritten slot on the expected side binds from the argument' is what INSTANTIATING a universal looks like. One rule, two polarities. It also puts a precise semantics under type-parameter-scoping.md section 5's informal 'ERASED': erasure IS the existential -- 'no consumer-side mechanism can soundly recover l's element or effect' is 'opening yields an opaque constant', and wi374_expansion_test::bare_value_stays_unusable is that opening, already delivered and already green. THE LANGUAGE ALREADY HAS THIS, EXPLICITLY: WI-402 / docs/design/path-dependent-types.md section 5, 'ensures Spec[C] -- an interface-rooted existential: an abstract C usable only through Spec... the body witnesses it with a concrete provider, the caller sees the spec with the carrier abstract'. Delivered, with tests (wi402_existential_return_test). So the question is no longer 'universal or erased' but: IS A BARE OR PARTIAL RETURN THE IMPLICIT SPELLING OF THAT SAME EXISTENTIAL? THE ONE REAL DIFFERENCE, and it is likely the ticket's actual content: WI-402 argues its existential is SAFE because the 'ensures' clause NAMES C in the signature, so the caller can speak about what it opened ('c.M is a neutral rooted at the op interface, not a hidden local'). A bare return names nothing -- which is exactly section 5's complaint. So the options should be re-cut as: (b') an UNNAMED existential is admissible -- open it to an anonymous rigid at the consumer, refuse exploit, leave widen alone (this is the previous note's got-side cut, now with a reason); or (d) an unnamed existential is NOT admissible -- require the explicit 'ensures' form and refuse the bare return at its declaration, which is a DIFFERENT refusal from (a) (it asks the author to name the witness, not to be universally polymorphic) and needs its own cost measurement. Option (a) as written should be retired unless someone defends the universal reading on its merits. UNMEASURED: everything above about (b') and (d). The driving so far only establishes the three checks and their current verdicts.

### 2026-08-09T00:16:30Z — user

DRIVEN 2026-08-09: THE EXISTENTIAL MACHINERY ALREADY EXISTS AND IS ENFORCED -- FOR THE CARRIER ONLY. This gives the ticket a working template and kills one of its options. THE DELIVERED EXPLICIT FORM (WI-402): 'sort KVStore { sort K = ?; sort V = ? }', 'sort MemStore provides KVStore[K = String, V = String]', 'operation openOne(m: MemStore) -> C ensures KVStore[C] = m'. BOTH HALVES OF THE EXISTENTIAL ARE ENFORCED FOR C. Declaration side: the same body written '-> KVStore' with NO ensures is REFUSED, and the diagnostic is this ticket's own complaint word for word -- 'expected an interface-expressible return (concrete, input-rooted, or an ensures manifest), got an abstracting return: the body provides KVStore only by an upcast that leaves its member(s) K, V UNBOUND, so the abstract member would escape its scope (the avoidance problem)'. Consumer side: 'needs_mem(openOne(m))' with 'needs_mem(m: MemStore)' is REFUSED at 'expected MemStore, got KVStore' -- the witness does not escape, the caller really does see the abstract carrier. So the language already implements pack-at-the-body / open-at-the-consumer. WHERE IT STOPS, and this is the finding: the MEMBERS are not opened. 'operation needs_int_key(s: KVStore[K = Int64, V = String]) -> String' fed 'openOne(m)' LOADS -- openOne's real K is String (from MemStore's provides) and it satisfies a demand for K = Int64. Mechanism: the loader rewrites '-> C ensures KVStore[C]' to a bare '-> KVStore', so the members ride the ordinary expansion path -- missing slot on the GOT side, binds from the demand -- exactly the previous note's asymmetry. Only the carrier gets existential treatment. THE FOUR CELLS. Carrier: declaration ENFORCED (WI-401 gate), consumer ENFORCED (opened). Member/sort-parameter: declaration NOT enforced (widen admitted), consumer NOT enforced (exploit and launder admitted). WI-1063 is therefore not 'invent a semantics for the return' but 'do for a parameter slot what WI-401/402 already do for a carrier', and the carrier's consumer-side opening is a worked reference implementation to copy. OPTION (d) IS DEAD AS STATED. 'Require the explicit ensures form' does not help: 'operation widen(s: Stream[T = Int64, E = {Error}]) -> C ensures Stream[C, T = Int64] = s' LOADS, and so does 'takes_pure(widen(s))' -- the explicit spelling leaks identically, because E is left unbound in the ensures application and unbound members are the un-enforced cell. Writing ensures buys nothing here. OPTION (b') IS THE ONE WITH A TEMPLATE and should be tried first, as the previous note said, now with a reference implementation rather than only a rule to invent. THE TICKET IS BIGGER THAN BARE RETURNS. 'launder' above is the same unsoundness INSIDE A DELIVERED FEATURE, with ensures written and the WI-401 gate satisfied -- a second witness this ticket did not know about, and one that says a fix confined to bare/partial returns would leave the ensures form leaking. Whatever WI-1063 decides must cover both, and wi402_existential_return_test should grow a member-opening row (there is none today: its rows check admission, not what a caller may do with the result). ALL FIVE PROGRAMS ABOVE WERE RUN against the delivered tree; none of the enforcement claims is read off the source.

### 2026-08-09T02:36:58Z — user

DELIVERED 2026-08-09, commit 0611dcbd. OPTION (b') SHIPPED and it is CHEAP: full workspace 4424 passed / 0 failed against a main baseline of 4409/0, all seven tiers zero errors (control: 'anthill load' still reports the WI-1059 parameter refusal). Total cost THREE delivered tests, against option (a)'s 40 across thirteen tickets.

WHAT THE TICKET GOT RIGHT: the polarity reading, the call as the mint site, freshness per opening, and that a fix confined to bare returns would leave the ensures form leaking. Both witnesses close by the same walk: 'rigidify_unwritten_sort_params' now takes a SlotPosition (Body vs CallResult) and is called from both polarities.

FOUR THINGS THE TICKET DID NOT PREDICT, all found by driving or by review.

(1) EVERY USE IS FOUR SITES, NOT ONE. The ticket names check_apply_iter and I wrote 'there is exactly one site where a declared return becomes a call's result type'. That was FALSE and it was a live hole, not a doc nit: review drove 'takes_pure(mk)' -- the bare nullary name, no parentheses -- loading clean while 'takes_pure(mk())' was refused, i.e. the headline exploit surviving a two-character edit. Also check_apply_iter's Path-3 fallback, and the ETA lift ('apply_it(widen, s)' laundered {Error} into a slot declaring E = {}). All four now open.

(2) THE ETA SITE OPENS ONCE PER LIFT, and refusing the lift instead is REFUTED, measured. That site already refuses a type-parameterized op because 'its arrow would carry unfreshened type-param vars that alias across multiple eta-lifts', so widening that clause looked like the principled answer. It is not: 'Function' declares an effect-row parameter, so the ordinary 'build(seed: Int64) -> Function[A = Int64, B = Bool]' is caught too, falls through to the zero-arg-call reading, and wi420_eta_of_curried_requires_op_is_loud_type_error turns from a loud load error into a clean load that crashes at eval. Opening gives one skolem per lift -- sound for the monomorphic arrow that site builds, and stated as a limit rather than sold as complete.

(3) 'UNWRITTEN' MEANS SOMETHING DIFFERENT AT A CALL, and the corpus decided it. The body walk treats any still-FLEX var as an author's '?' (WI-1056's four spellings), because by then WI-392/WI-942 have rigidified everything else. At a CALL nothing has been rigidified: the op's own [Elem], the enclosing sort's parameters and an already-eliminated s.T are all flexible Globals and are exactly the slots the call is about to BIND. Carrying the body's answer over re-minted FilteredStream.splitFirst's own Pair[A = Elem] as an unrelated Pair[A = ?A], and 'LogicalStream.empty() -> LogicalStream[?A]' writes a NAMED variable in a return on purpose. So at a call only an ANONYMOUS slot opens. The fourth spelling ('E = ?') is closed too, via a shared value_is_anonymous_wildcard that reads BOTH carriers.

(4) THE ESCAPE GATE IS NOT WHERE THE ESCAPING SKOLEM IS CAUGHT. Acceptance says 'an escaping skolem is refused by the WI-401 gate with its own diagnostic'. It IS refused -- 'escape(m) -> KVStore[K = String, V = String] = openOne(m)' went from loading clean to refused at escape.return -- but by ordinary return CONFORMANCE, because abstracting_return_error sits in the else of !conforms and a non-conforming return never reaches it. That is the ticket's own observation about option (a), applied here. The gate keeps its own population (a bare abstract-spec return conforming by provider upcast) and still fires byte-identically; both are driven as separate rows.

ALSO: WI-374's foreign_bare_return_op_loads_and_narrows was pinning the ACCEPTING side of this hole, which the ticket's cost estimate did not know. Driven on the delivered tree, 'makeList() -> List = cons(head: "x", tail: nil)' -- a list of Strings -- bound to 'let l : List[T = Int64]' LOADED CLEAN. Its own doc said 'the annotation-as-bound-type is what carries the element here'; the type carried nothing and the annotation fabricated it. Renamed to foreign_bare_return_op_loads_but_no_longer_narrows.

THE CORPUS REACHES THE OPENING ZERO TIMES, counted at the mint over all seven tiers. So tier cleanliness is evidence nothing broke and NOT evidence anything works -- the same shape as WI-1061's Anonymous arm. wi1063_existential_return_test (14 rows) is the only coverage, with a back-out table measured one revert each: the opening = 15 rows; the self gate = the PRELUDE stops loading plus 1590 tests; the anonymity narrowing = combinators.anthill:121. Three rows pass under every revert BY DESIGN and say so at their sites.

SELF SLOTS ARE NOT OPENED: a reference to the callee's OWN sort in its return is the type-parameter-scoping section 3 tie, pinned by the call's arguments, so it rides the canonical channel exactly as expand_foreign_sort_application leaves it on the parameter side. That gate is now impl_parent_sort_of_op (has_kind) and is computed ONCE and shared by both polarities -- the parameter side had been spelling it with kind_of, which WI-956 exists to replace.

DOCS: kernel-language.md section 'Expansion during unification' states both polarities and says 'each USE opens'; type-parameter-scoping.md section 5 says erasure IS the existential and that writing the result type is now load-bearing rather than cosmetic; path-dependent-types.md section 5 records that an ensures existential's MEMBERS are opened at the consumer too. Neither contradicts the others.

REVIEW: /code-review high, two rounds. Round 1 found the bare-name hole and the concrete_override_threaded mis-keying (the WI-606 fallback returns the OVERRIDE's declared return, so keying the self gate on fn_sym would have read MappedStream.splitFirst's own carrier as foreign); round 2 found the eta lift. All applied.

