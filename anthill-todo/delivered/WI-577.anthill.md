## Attributes

- id: WI-577
- created: 2026-06-27T17:38:46Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-02T14:02:10Z

- acceptance: cargo-test

## Description

First-class Dictionaries + OpRefs -- the RUNTIME dispatch values Value::Requirement (the requirement DICTIONARY: functor + sub-requirements) and Value::OpRef (op symbol + optional dispatch dict) given a first-class anthill face. These are RUNTIME sorts in anthill.realization.runtime -- Dictionary[S] / OpRef[A], the runtime dual of realization Obligation/Implementation (a dictionary discharges a requires by carrying its resolved Implementation) -- NOT reflect objects. SCOPE (the ready unit): the two runtime VIEW sorts + native builtins over the RequirementArena + the carrier-type mapping Value::Requirement -> anthill.realization.runtime.Dictionary. Value::Requirement STAYS ViewHead::Opaque -- NO de-opaquing (builtins match the handle in Rust and read the arena; de-opaquing was only for a dropped structural-match face; SUPERSEDES the earlier DEP-NOTE that WI-300 needs Value::Requirement de-opaqued). Dictionary[S] ops: impl/sub/arity/resolveOp/ops. OpRef[A] ops: op/dict. TWO-TYPE SPLIT via TYPE PARAMETER: the denoted type IS the parameter (S = the spec instance Eq[Int]; A = the op arrow), tracked STATICALLY by the typer -- NOT read off the opaque value (dissolves store-the-spec: the runtime slot keeps only (functor, sub-handles), the spec lives in the type; the functor is a multi-spec carrier so it cannot recover the spec). Bare Dictionary = the existential (S unknown). Payoffs: uniform typing (WI-502 resolves OpRef/Requirement by reading the type) + introspectable dispatch. SEPARATE follow-on (NOT this WI, gated on a metaprogramming consumer): the reflect Term layer -- reflect_expr(Expr, pos) -> NodeOccurrence (the occurrence dual of reflect(TermRepr)->Term) + anthill constructors genRef/genApply/genConstruct/findDictionary/mkApply over the Expr enum; find_dictionary Expr form is scoped to WI-300. Design: docs/design/requirement-dictionaries.md sec 2 (runtime sorts) / 2.5 (two-type split) / 2.6 (reflect Term layer) / 4 (phasing). Acceptance: the two runtime sorts + native builtins + carrier mapping; cargo-test green.

## Changes

### 2026-07-01T20:14:18Z — feedback — user

DEP NOTE (2026-07-01): WI-300 (rule-body requirement goals) now depends on this, added this session. Reason: under the settled WI-300 semantics, a rule-body requires(X) weaves + dispatches through the requirement dictionary (Value::Requirement) and must reason about it, so WI-300 needs Value::Requirement DE-OPAQUED -- today it is ViewHead::Opaque (term_view.rs:1081) and unlowerable (execute.rs:331 UnsupportedVariant Requirement). WI-577 structural view + two-type split (reflect-type = it IS a Dictionary; denoted-type = the spec instance it witnesses, e.g. Eq[T]) is exactly what the rule-body weave consumes. WHAT WI-300 ACTUALLY NEEDS is the DESIGN ANSWER (de-opaque Value::Requirement into a structural view + the two-type split), more than the full reflect.Dictionary/reflect.OpRef sorts + bridge plumbing -- that plumbing can trail. Note the dictionary shape WI-577 lifts (functor + sub-requirements) is already exactly the ConstructRequirement{impl_functor, requirements} occurrence in node_occurrence.rs.

### 2026-07-01T20:28:41Z — feedback — user

DESIGN DOC: docs/design/requirement-dictionaries.md (2026-07-01) -- covers WI-577 (substrate) + WI-300 (consumer) in one doc; see esp. section 2 (first-class dictionaries in reflect: how visible from anthill, the Substitution builtin wiring, accessor-ops vs structural-match faces, OpRef, two-type split) and section 4 (phasing, WI-577 -> WI-300).

### 2026-07-02T12:27:47Z — feedback — user

SCOPE REFRAMED (2026-07-02 session, doc-aligned): description above rewritten to match docs/design/requirement-dictionaries.md. Key shifts from the original: (1) Dictionary[S]/OpRef[A] are RUNTIME sorts in anthill.realization.runtime (runtime dual of Obligation/Implementation), NOT reflect objects -- reflect only hosts the separate Term-construction layer. (2) Value::Requirement STAYS ViewHead::Opaque -- NO de-opaquing (supersedes the DEP NOTE). (3) two-type split via TYPE PARAMETER (Dictionary[S]/OpRef[A]), tracked statically -- denoted type is the param, not read off the value; dissolves the store-the-spec problem (functor is a multi-spec carrier). (4) WI-577 = the two runtime view sorts + builtins + carrier mapping (the ready unit); reflect_expr + the genRef/genApply/genConstruct/findDictionary constructors are a SEPARATE follow-on WI gated on a metaprogramming consumer.

