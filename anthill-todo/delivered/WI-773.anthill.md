```anthill
fact WorkItem(id: "WI-773", created: "2026-07-19T10:30:00Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-797"]), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-07-21T22:39:41Z"))

fact Feedback(workitem: "WI-773", author: "user", at: "2026-07-19T12:10:24Z")

fact Feedback(workitem: "WI-773", author: "user", at: "2026-07-19T17:03:45Z")

fact Feedback(workitem: "WI-773", author: "user", at: "2026-07-21T07:20:18Z")

fact Feedback(workitem: "WI-773", author: "user", at: "2026-07-21T07:24:03Z")

fact Tag(workitem: "WI-773", name: "extent-seam")
```

## description

kb layer lacks a facts-only query accessor, so every facts-reader hand-rolls the bodied-candidate policy -- and three divergent policies now coexist in the workspace: silent `if !kb.is_fact(rid) { continue; }` (~20 sites: anthill-cli check.rs x4 / run.rs / prove.rs, anthill-smt-gen policy.rs:69, cache/key.rs, core op_info/typing/eq_derive), a compiled-out debug_assert (cpp-gen cpp_method_name, lib.rs ~4186), and WI-770's hard assert (query_realization_facts). kb exposes query_rules -- the BODIED-only filter (kb/mod.rs:3260) -- but no facts-only dual with a decided refusal policy. Surfaced by the WI-770 review's altitude angle; the concrete demonstration that helper-local refusal is too shallow: cpp-gen itself still has three unguarded realization readers (WI-771), and anthill-todo's collect_workitems (rustland/anthill-todo/src/main.rs:331) head-matches WorkItem rules unguarded, so a bodied WorkItem rule would list/claim/deliver as a phantom item. FIX DIRECTION: add a kb accessor with an explicit bodied-rule policy -- e.g. query_facts_view returning facts plus a LOUD channel (Err or diagnostic callback) for bodied candidates -- then migrate the hand-rolled sites (including anthill-todo:331 and the WI-770/WI-771/WI-772 sites) onto it; while designing, weigh Result-over-panic so CLI paths can render the refusal through their error channels (the WI-770 assert bypasses CppCodegenError: process abort exit 101 with no source span, vs run_codegen_cpp's rendered `error: {message}` + exit 1 at anthill-cli/src/main.rs:938-942). Also consider promoting the `head :- body` rule renderer into anthill-core (TermPrinter) -- the WI-770 review counted three hand-rolled copies (anthill-cli print_rule_results main.rs:1594 + print_query_results main.rs:1624, cpp-gen render_rule) that are divergent at birth on value-head handling. ACCEPTANCE: the accessor exists with a pinned bodied-rule-policy test; the enumerated hand-rolled sites read through it (or are individually justified in comments); cargo-test green via scripts/test.sh.

## Feedback

### 2026-07-19T12:10:24Z — user

WI-772 review outcome + 2026-07-19 design discussion: (1) TermPrinter::print_rule now EXISTS in anthill-core (persistence/print.rs) and cpp-gen render_rule is deleted -- the renderer bullet is half done; remaining copies are the two anthill-cli solution printers, whose heads go through the cli-local render_value (Value renderer), so full promotion needs that renderer too. (2) Consider reframing the accessor as THE single-valued/table read primitive with a pluggable bodied-rule policy (facts-only-refuse today; WI-774 may graduate specific functors to resolve+ordering) rather than facts-only per se. (3) Closing phase should be a visibility RATCHET: after migration, demote raw head-as-answer enumeration (rules_by_functor + rule_head used as answers) to pub(crate) or a deliberately-named browse API so the WI-770 class becomes unrepresentable outside kb. Legitimate rule-aware consumers keep scoped access: CLI --match browser, prove lift (rule_ids_by_qn), smt-gen collect_rule, and write-side store addressing (WI-780). is_fact itself stays public -- hiding the predicate would push worse hacks.

### 2026-07-19T17:03:45Z — user

2026-07-19 (WI-780 EDB/IDB follow-through): design the accessor VALUES-FIRST -- the public read shape must not return or require RuleId, or it bakes in full residence, exactly what WI-780 virtualized-extent design forbids (a store-backed functor has no resident RuleId to hand out). Fact-address stays a store-layer concern behind the seam. Reads answer with values/rows; the loud bodied-rule channel reports the offending rule by rendered text (print_rule), not by id. WI-780 design output should be consulted while shaping this API.

### 2026-07-21T07:20:18Z — user

057 (extent sources) landed the read-path design that SHAPES this accessor — build to that contract so cpp-gen/CLI migrate onto the final shape, not an interim one. (1) VALUES-FIRST: returns row Values, never RuleId (057 Read path); a RuleId in the public read shape bakes in full residence that 057 forbids (a store-mounted functor has no resident RuleId). (2) POLICY PARAMETER, not hardcoded refuse: read_facts(functor, selection, policy) where policy in {Refuse = facts-only, bodied candidate -> loud Err rendering the rule via TermPrinter::print_rule; Resolve = WI-774 per-functor graduation}. The WI-774 EffectMapping/TypeMapping resolve-vs-refuse decision plugs in HERE, per functor — that is why the accessor MUST carry a policy param. (3) SELECTION = 057 lookup-contract LookupQuery.bound: ground field bindings pushed down (query_type_mappings already grounds anthill_type — that IS the selection); empty selection = enumeration. (4) RESIDENT + MOUNTED, uniform: reads resident discrim AND (once 057 item-1 mounts land) store-owned extents. CRUCIAL SEQUENCING: the RESIDENT path is buildable NOW, so WI-773 + the cpp-gen migration (WI-771) land ahead of 057 items 0-6 on resident-only, and gain store-mounted extents later with ZERO caller change. Error is Result-over-panic so CLI/cpp-gen render through their channels (replaces WI-770 assert-abort). This is 057 retirement stage R1; keep the accessor RuleId-free so the R4 visibility ratchet can privatize the raw walk.

### 2026-07-21T07:24:03Z — user

CORRECTION to the prior feedback's sequencing (design review): NO partial interface — the accessor is adopted whole or not. DROP the 'resident-only accessor now, gain mounted extents later' idea: a caller (cpp-gen) must migrate onto the FINAL contract (values-first, resident AND mounted, full lookup-contract), never a resident-only stub whose read semantics change under it when mounts land. So WI-773 lands TOGETHER WITH the ExtentSource read seam + discrim mounts + the shipped in-memory reference source (057 item 1) — COMPLETE. 'Ready when used.' v1 stays small not by shipping a partial interface but by LOUD-REFUSING unsupported capability behind the complete one (volatile/oracle = loud registration error; external writes = NotWritable). See 057 Phasing note.

