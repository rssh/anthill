## Attributes

- id: WI-1056
- created: 2026-08-08T12:42:17Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-08T14:24:01Z

- acceptance: cargo-test

## Description

BUG + CAPABILITY (058; WI-1043 residue, MEASURED not predicted): a rule body is NEVER TYPE-CHECKED, and the two populations that follow from it. (1) THE GENERAL Expr::Apply: call_dispatch_shape admits a dot (WI-282) and a spec-op call of either half (WI-1026 defaulted, WI-1043 body-less); every OTHER rule-body call stays untyped, so an arity error, a type mismatch or an unknown functor in a rule body is found by nobody. (2) THE FAILURES WI-1043 DELIBERATELY DOES NOT REPORT: it types body-less spec-op atoms for their dispatch verdict and reports only the two 058 ties (CallDispatch::reports_every_failure), because reporting the rest refuses the corpus. THE MEASUREMENT, taken at the widening (2026-08-08, stdlib + host bindings + examples/ + anthill-todo/ + anthill-testcases/): 231 newly decided call sites; the failures are 41 leaf errors at 7 sites — 27 DotDispatchNoMatch (a nested dot the per-node tolerated arm swallows, typed as part of its enclosing atom), 8 TypeMismatch, 6 UnknownApplyFunctor — and ZERO dispatch verdicts. The 7 sites, each a program that loads and runs today: stdlib/anthill/prelude/bigint.anthill:39 (?P(sub(?n, 1)) in the induction rule — the literal 1 is Int64 where BigInt is expected); stdlib/anthill/prelude/logical_stream.anthill:73 (splitFirst(?a) = some(pair(?first, ?rest)) — the two sides differ by an effect row E = s.E); examples/webots-modelling/lf1/safety_common.anthill:236,277,278,279 and safety_gps.anthill:154 (entity constructors and dots inside = goals). WORK: decide each of the 7 (a source fix, a typer gap, or a stated exemption), then widen — either the error policy at the body-less shape or the gate to the general Expr::Apply, in that order, since the first is the smaller half of the same question. ACCEPTANCE (each test says which case fails when backed out): a rule body with a genuine arity/type error is REFUSED at load naming it (control: today it loads clean); the 7 corpus sites either load clean for a stated reason or are fixed at their source; the WI-1043 suite stays green; the corpus loads clean at every tier. REFERENCE: WI-1043; WI-1026 (which first narrowed the gate for this reason); WI-282; docs/design/058-implementation.md sec 23.

## Changes

### 2026-08-08T12:30:58Z — feedback — user

MEASUREMENT CORRECTED (the ticket's own numbers were taken from a CUMULATIVE 4-tier probe, so every count was inflated ~4x). Re-measured per LOAD with counters at the acting arm: 133 newly decided body-less call sites on a whole-corpus load (stdlib + host bindings + examples/ + anthill-todo/ + anthill-testcases/), 28 of them on stdlib + bindings alone; their failures are 19 LEAF ERRORS — 15 DotDispatchNoMatch, 2 TypeMismatch, 2 UnknownApplyFunctor — and ZERO dispatch verdicts. Reported (i.e. with WI-1043's error policy off) those render as 7 LOAD ERRORS in 4 files, which is the list the ticket names and which is unchanged: stdlib bigint.anthill:39, stdlib logical_stream.anthill:73, examples/webots-modelling/lf1/safety_common.anthill:236/277/278/279, safety_gps.anthill:154. Read 133/19 as the population and 7-in-4-files as the work.

### 2026-08-08T14:24:51Z — feedback — user

DELIVERED. The 7 sites split ONE typer gap / TWO source errors / TWO exemptions -- not the three-way split the ticket predicted, because one of its two 'typer gaps' was refuted by building it. (1) THE TYPER GAP: check_apply_iter's materializer-fallback gate asked is_constructor_symbol where the question is is_entity_constructor; the loader's ApplyOrConstructor drain has always asked the latter, which is why an operation body never noticed and a rule body -- which does not take that drain -- did. (2) SOURCE ERRORS, all invisible because nothing checked them: bigint's induction schema (bare 0/1 where BigInt is declared, all three sites corrected); logical_stream's interleave (LogicalStream[?A] bound T alone and left the WI-714 row to the default -- now shares one row variable across both inputs and the result); and, found only by widening, anthill.kernel.unify(a: T, b: T) written against a NAMESPACE-level 'sort T = ?', so every '<=>' in every program shared ONE symbol as its argument type -- it now carries [T] as struct_eq beside it already did, and the dead sort is deleted. (3) EXEMPTIONS, both 'cannot decide', not 'decline to report': the WI-282 unresolved dot receiver (the whole of what the corpus would newly refuse -- 7 reports at 4 sites, every one an untyped rule-head var), and an equation-introduced functor applied in a rule body, where WI-898 had gated its own diagnostic !in_rule_body and then let a worse one through; its boundary is the IMPORT (an unresolved bare 'ite' is still refused). THE TICKET'S SECOND 'TYPER GAP' WAS WRONG AND I BUILT IT BEFORE FINDING OUT. logical_stream:73 read as a subtype gap -- a same-base PARTIAL type application is refused where the BARE form is accepted, though kernel-language 'Expansion during unification' says both mean the same. Loosening it is UNSOUND: driven, an effectful Stream[T = Int64] then reached a slot declared E = {}. That section governs UNIFICATION (unify_parameterized_view says so in the same comment: 'the subtype twin is the direction that rejects'). Backed out; the site is fixed at source; the asymmetry is WI-1059 with BOTH directions' costs measured (the sound one is 33 stdlib errors, and only after patching both carrier arms -- WI-1016). FOUND BY THE /code-review, not by me: that unsoundness, and the dedup keyed against the whole error accumulator rather than this atom's own recursion (two unlocated errors in different rules could collapse). MY PREDICTED BACK-OUT MATRIX WAS WRONG IN NINE CELLS. Driving it found (a) my shared fixture spelled its sort eponymously, so four rows that say nothing about constructors moved when the ctor gate was reverted, and (b) the rows I marked 'passes both ways by design' FAIL when the exemptions are backed out -- the STDLIB itself stops loading, which is the one failure mode small fixtures cannot see. The table now marks that cascade distinctly. FOUR FIXTURES WERE ILL-TYPED PROGRAMS, not regressions: wi894/wi896 (the kernel.unify shape), wi927 (a bracketed type application in a slot that expects no Type -- refused in an OPERATION body too, always was), wi680 (a bare un-imported 'ite'). THE SECOND HALF IS WI-1058 AND ITS SIZE IS MEASURED: the general Expr::Apply widened reports 468 errors (173 distinct) on stdlib + bindings ALONE against 2 for the body-less shape, and they are two readings the typer lacks -- a rule NAME applied to arguments is a relational subgoal (WI-714's arm is gated !in_rule_body for exactly that reason), and reflect/?P(...) arguments land in List[T = Term] positions. NOT CLAIMED: BigInt.induction is still not driven end-to-end by anything (nothing resolves it; the prove pipeline's phase gamma would need z3), and the test that pins the source fix says so at its site rather than pretending otherwise. Back-out matrix driven, four reverts. Corpus clean at every tier. Full workspace: 29 binaries, 4396 passed, 0 failed.

