```anthill
fact WorkItem(id: "WI-444", created: "2026-06-11T21:02:47Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-16T13:01:36Z"))

fact Tag(workitem: "WI-444", name: "typing")
```

## description

Typer+eval: carrier override of a DEFAULTED spec op — the carrier's own member must win over the spec's default body at the spec call site (typeclass default-method semantics: defaults fill gaps, they do not shadow). TODAY a spec op WITH a default body (the WI-362/424 members: Stream.takeN/collect/find/foldLeft/foldRight/isEmpty/count, Iterable.find/map/filter/isEmpty/size/folds) cannot be overridden by a carrier: lookup_spec_op_dispatch (typing.rs ~5209) gates on operation_has_no_body, so the WI-210 rewrite-to-impl never fires; at eval, step 3 (cached_operation_body, eval.rs ~1004) finds the SPEC's own body and runs it — the step-3b receiver-sort dispatch (WI-350) is body-less-only and never consulted. PROBED 2026-06-11 (wi424 pinning test iterable_size_carrier_override_currently_shadowed): 'sort Counted provides Iterable' + its own 'size' returning a stored count 99 — the Iterable-imported size(counted([1,2,3], 99)) returns 3 (the derived walk); the override is SILENTLY ignored (silent-wrong-answer class, worse than loud). Motivating case: a counted/keyed carrier with O(1) size (cf. Map.size) under the new Iterable.size default. FIX DIRECTION: (typer) extend the WI-210/218 PinNow classification beyond operation_has_no_body — when the receiver's carrier classifies Concrete and the carrier sort declares a member backing the spec op, rewrite to the carrier op even though the spec op has a default body; (eval) for un-rewritten abstract calls, prefer resolve_spec_op_target_by_value's carrier impl over the spec's own default body when the receiver's runtime sort defines the member. ACCEPTANCE: the pinning test flips to 99 (update it to assert the override); a counted-carrier O(1) size override typechecks + EVALS through the Iterable-imported call site; all existing WI-362/424 defaulted-member tests stay green (defaults still fire for carriers WITHOUT an override, e.g. BoxColl).

