```anthill
fact WorkItem(id: "WI-934", created: "2026-08-02T11:43:41Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-02T14:03:25Z"))

fact Feedback(workitem: "WI-934", author: "claude", at: "2026-08-02T14:01:43Z")
```

## description

Relocate the SQL store sketch out of the standard library — it declares a backend no realization implements.

WHAT IT IS. stdlib/anthill/persistence/sql.anthill declares SqlStore(connection, schema, dialect), the SqlDialect enum (Postgresql/Mysql/Sqlite/Duckdb), QueryBinding and ColumnDef. MEASURED (WI-931): NO host implements a SQL backend in any form. rustland/anthill-core/src/persistence/ holds file_store.rs and indexed_file_store.rs and nothing else, so retract / update / retrieve against a SqlStore value reach no registered mirror. WI-931 removed the file's two provision facts for exactly that reason; what remains is a SHAPE with no realization, which is an example, not a standard library.

USER CALL (2026-08-02): move it to examples/.

WHY IT IS NOT A FILE MOVE — three live consumers point at the stdlib path, and each needs a decision:
 (1) rustland/anthill-stl/build.rs maps ('persistence/sql.anthill', 'sql.rs') and rustland/anthill-stl/src/persistence/sql.rs include!s the generated output, so the generated SqlStore / SqlDialect / QueryBinding / ColumnDef structs are COMPILED INTO the anthill-stl crate today. Decide whether they are still wanted: if yes the build script must read from the new location (a build script reaching into examples/ is itself a smell — consider whether these structs have any Rust consumer at all, which should be measured before either answer); if no, drop the entry and the include, and the crate loses four unused types.
 (2) rustland/anthill-core/examples/gen_store.rs names the same path.
 (3) docs/rust-forward-mapping.md uses SqlStore as THE worked example, in two sections. Whatever moves, the doc's paths must move with it. Note WI-933 also touches this doc — the bracket-less 'fact QueryableStore' it documents is one of the SqlStore lines — so sequence the two or make both edits in one pass.
Also: rustland/anthill-stl/src/stdlib.rs embeds the file in SOURCES (the CLI's bundled stdlib) and tests/stdlib_drift_test reconciles that list against the on-disk tree, so both must be updated or the drift test fires.

SCOPE NOTE. The abstract store algebra (stdlib/anthill/persistence/store.anthill: Store / NonMonotonicStore / QueryableStore / BulkStore) STAYS in the stdlib — it is the language-agnostic spec every backend is written against, and the filesystem backends realize it. Only the SQL sketch moves.

ACCEPTANCE: anthill.persistence.sql no longer ships in the standard library or its embedded bundle; every consumer above is updated or deliberately removed with the reason recorded; the drift test passes; docs/rust-forward-mapping.md's paths are correct; cargo-test green via rustland/scripts/test.sh.

SOURCE: measured while delivering WI-931 (2026-08-02).

## Feedback

### 2026-08-02T14:01:43Z — claude

DELIVERED. stdlib/anthill/persistence/sql.anthill -> examples/sql-store/ (sql.anthill + a new demo.anthill + README.md), namespace anthill.persistence.sql -> anthill.examples.persistence.sql (the anthill.examples.* convention webots/lf1 already uses). Full workspace: 4023 passed, 0 failed, 95 binaries. scaland core compiles.

THE CONSUMER LIST IN THIS TICKET WAS INCOMPLETE — two more, one of which would have broken a build:
 * scaland/core/src/main/scala/anthill/load/EmbeddedStdlib.scala listed 'anthill.persistence.sql'. Its resolver returns Left on a missing file and ParserIntegrationTest asserts errs.isEmpty, so leaving it would have failed the Scala suite. (That suite has a PRE-EXISTING failure — 7 tests, all 'stdlib anthill.realization.cpp_std: Parse error at 9975', a scaland parser gap in a file this ticket does not touch. Confirmed pre-existing by running the same suite with HEAD's EmbeddedStdlib restored: same failure, and zero occurrences of 'sql' anywhere in the log.)
 * docs/cpp-forward-mapping.md section 7.1 and the comment above the unsupported_runtime_profile call in anthill-cpp-gen/src/lib.rs both named SqlStore as a member of the cpp17-stl refusal set. That set is keyed on the string prefix 'anthill.persistence.', so the rename removed SqlStore from it and both sites became false. Corrected to IndexedFileStore. The gate's behaviour change is benign — SqlStore/SqlDialect/ColumnDef are plain scalars and are lowerable; QueryBinding's three Term fields are refused anyway via the anthill.reflect. arm — and coverage was NOT reduced: anthill-cpp-gen/tests/unsupported_test.rs drives both arms, the persistence one on anthill.persistence.Store, which stayed.

Also updated: docs/kernel-language.md 8.8's routing example (it illustrated routing with SqlStore, a sort the stdlib no longer ships — now IndexedFileStore/FileStore), and docs/proposals/007 par.99, which taught 'fact NonMonotonicStore[SqlStore]' as THE example of a provision — the exact spelling WI-931 refuses.

(1) THE anthill-stl BUILD ENTRY: DROPPED, not re-pointed, and the ticket asked for this to be measured first. Measured: 'grep -rn persistence::sql' over the workspace matches NOTHING outside the include itself, so the four generated structs had no Rust consumer. build.rs entry and src/persistence/sql.rs both deleted. collect_trait_sorts is unaffected — sql.anthill declared only entities and an enum, so it contributed zero trait names.
(2) gen_store.rs: entry dropped.
(3) rust-forward-mapping.md par.2.13 / par.6.1: namespaces updated; the WI-933 bracket-less question is left to WI-933 and cited, not pre-empted.

THE RULE NOW HAS ONE OWNER. 'A shape no host realizes is an example, not a standard library' was, in my first cut, stated in full at ~14 sites — CLAUDE.md's own 'Localized invariants — not restated here' failure mode, and /code-review caught it from three angles at once. It is stated once in docs/proposals/038-builtin-sorts.md, 'What the stdlib carries', beside the backing rule it follows from; the other sites are one-line citations.

TESTS. sql_store_example_test.rs (6, top-level binary like classic_mini_test/github_todo_test since it drives an example): the shape left the stdlib, it resolves at its new home, and four the_demo_* tests DRIVE each field kind to an answer — a String, the Postgresql variant (asserted by QUALIFIED name; resolve_sym returns the SHORT name, so the .rsplit('.') I first wrote was a no-op), a Quoted SQL fragment, and the List[T = ColumnDef]. wi931's two SqlStore tests now load the example explicitly and keep their claims. stdlib_drift_test gained an assertion over the DIRECTORY (persistence/ holds exactly store.anthill + filesystem.anthill) rather than a name filter — re-adding the sketch as persistence/database.anthill would walk past a name filter.

CONTROL MEASURED, NOT ASSERTED, AND IT CAUGHT THE TEST BEING WEAKER THAN IT READ. Copying sql.anthill back into stdlib/anthill/persistence/ PASSED the_sql_store_shape_left_the_stdlib on the first run: I had asserted only the OLD name absent, and the move renamed the namespace too, so the old name stays absent however the file is filed. Both spellings are named now and the backout fails as it should — it fails that test plus the two stdlib_drift_test tests, and nothing else.

TWO OF MY OWN CLAIMS WERE FALSE AND DRIVING KILLED THEM. (a) I wrote that QueryBinding could not be instantiated because 'quoted terms have no surface syntax' — I had grepped grammar.js for 'quoted' and found nothing. Quoted("sql", "...") is an ordinary application and LOADS CLEAN; the demo now builds a real QueryBinding with five Quoted fragments, which is a materially better example. (b) 'anthill-core/src/persistence/ holds the two file stores and nothing else' — it also holds mod.rs, print.rs, term_ser.rs. Written twice before /code-review caught it.

A REAL LOADER BUG, FOUND BY MY OWN CLEANUP -> WI-936. /code-review asked load_example to WALK examples/sql-store/ rather than name its two files, so a third file could not be silently skipped. That is right in general and it BROKE THE EXAMPLE: the walk is alphabetical, so demo.anthill loaded before sql.anthill, and that order LOADS CLEAN while silently costing the columns destructuring. Measured both ways, same helper, only the order swapped: sql,demo -> account_column_type answers 1; demo,sql -> 0, no diagnostic. QueryBinding.columns' declared List[T = ColumnDef] is what desugars the literal to a cons spine, and sibling rules reading columns as a bare variable are UNAFFECTED, which is what makes the loss quiet. Local fix: an EXAMPLE_FILES constant in dependency order, reconciled against the directory (the SOURCES + stdlib_drift_test shape). The loader half is WI-936.

FOLLOW-UPS I FILED AND THEN WITHDREW, on the user's rule that a follow-up is for a BUG:
 * cpp-gen's refusal keyed on a namespace prefix — I claimed 'no test on this gate at all'. FALSE: unsupported_test.rs drives both arms. Deleted.
 * rustland/anthill-core/examples/gen_store.rs has no consumer (content grep for 'gen_store' matches nothing repo-wide), writes into a gitignored dir (.gitignore:3), and has no completeness or removal guard — the only one of the four parallel stdlib file lists with neither. Had this ticket missed it, nothing would have failed. That is dead code, not a bug; recorded here, delete/keep is the user's call.

