## Attributes

- id: WI-846
- created: 2026-07-28T11:03:40Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-29T08:20:59Z

- acceptance: cargo-test

## Description

SILENT SKIP (cpp-gen, introduced by WI-833 0f979eac): a ZERO-result cpp NamingConvention read now returns identity-cased method names with NO diagnostic, so an unloaded or misconfigured realization profile emits non-compilable C++ against the vendor header and cpp-gen says nothing. CODE-READ at HEAD 2cdfde3e (rustland/anthill-cpp-gen/src/lib.rs, cpp_method_name): after unique_or_ambiguous, `let Some((source_case, method_case)) = convention else { return Ok(source.to_string()) }` — zero conventions falls straight through to identity. Every carrier dispatch then renders `self->get_gps()` where the vendor header declares `getGps()`. VERIFIED REGRESSION, not a pre-existing gap: the parent commit (0f979eac~1:lib.rs:4357-4366) guarded BOTH ends with `debug_assert!(facts.len() == 1, "expected exactly one cpp NamingConvention fact, found {}", facts.len())`, whose own comment read "Zero (misconfigured or unloaded profile) or >1 ... is a profile bug: fail loudly in dev/test per the repo's 'loud error over silent skip'". WI-833 re-established only the >1 half (as a hard CppCodegenError via unique_or_ambiguous) and replaced the zero half with a NEW and CONTRADICTORY rationale in the comment — "an absent one is an ordinary miss, not an error (a project may emit without loading a naming profile)". Those two comments cannot both be right, and the ticket must DECIDE which, not split the difference: this is a policy question, so if 'a project may legitimately emit without a naming profile' is the intended rule, then the requirement is a DIAGNOSTIC (a warning naming the absent profile), not silence — the repo rule is 'prefer a loud error over a silent skip ... Silent skips hide bugs and read as handled when they arent' (CLAUDE.md, Development principles). NOT YET DRIVEN, and the reason is itself part of the work: the cpp-gen test harness (anthill-cpp-gen/tests/common/mod.rs, load_kb_with_extras) ALWAYS loads STDLIB_PARSED + CPP_BINDINGS_PARSED, and the stdlib always ships exactly one cpp NamingConvention (snake_case -> camelCase), so NO existing test can reach the zero branch at all — which is why the branch's behaviour change went unnoticed. ACCEPTANCE: (1) a driven repro that reaches the zero branch — a KB loaded WITHOUT anthill.realization.cpp_std — showing what is emitted today, so the decision is made against a measurement rather than a reading; (2) the decided policy implemented — loud error, or a diagnostic that names the absent profile — never silence; (3) a test that reaches the zero branch, which requires the harness to gain a no-realization-profile load path (state it in the test if the harness change is the bulk of the work); (4) full workspace green via rustland/scripts/test.sh. SOURCE: /code-review of the WI-833 merge, 2026-07-28. SCOPE: rustland/anthill-cpp-gen.

