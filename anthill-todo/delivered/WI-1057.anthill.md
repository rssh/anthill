## Attributes

- id: WI-1057
- created: 2026-08-08T12:42:17Z

- status: Delivered
- status_agent: user
- status_at: 2026-08-09T11:02:13Z

- acceptance: cargo-test

## Description

BUG (058; WI-1043 residue, CONFIRMED BY DRIVING + one wrong fix already measured): an implementation supplied ONLY by a WI-431 INSTANCE FACT is unreachable from a RULE BODY, on both spellings, while an operation body reaches it. DRIVEN on the wi1043/wi1035 body-less fixture with the fact as the sole supplier (Leaf declares nothing; fact Desc[T = Leaf, describe = otherDescribe]): 'rule answer(?r) :- Desc.describe(leaf(), ?r)' answers [] and so does 'leaf().describe(?r)', while 'operation probe() = Desc.describe(leaf())' answers 9. Pinned by wi1043_bodyless_rule_body_test::a_fact_route_supplier_is_still_unreachable_from_a_rule_body, which FAILS when this is closed — the ticket comes back there. MECHANISM, two early returns and BOTH on the SPELLED functor's missing body: dispatch_spec_op_cached does not read instance-fact op-bindings (WI-431 inc 2), so no typer pin exists and the implementation is discoverable BY VALUE alone; then functional_relation_arity answers None (op_body_node(f) is none), so the goal f(a…, ?r) never becomes the call f(a…); and reduce_op_value would return before bridge_op_to_eval for the same reason. WI-1043 gave the goal shape the PIN (dispatched_relation_arity) — this route has no pin to give it. THE NAIVE FIX IS WORSE AND WAS MEASURED: admitting a body-less spec op to the functional-relation view by its signature arity and bridging to eval bound ?r to the UN-REDUCED CALL and reported it DEFINITE — a wrong answer where there was a missing one, because the WI-938 hook's 'rule-less BODIED op' gate is what guarantees the rewrite reduces. So the work is to make the goal reach eval's value-directed dispatch WITHOUT rewriting to unify() on a call that may not reduce: either resolve the supplier at the goal (a typer pin from the fact binding, which changes operation bodies too) or a bridge whose failure leaves the goal un-rewritten. ACCEPTANCE (each test says which case fails when backed out): the fact-only fixture answers 9 from a rule body in BOTH spellings (control: today [] in both); a body-less spec-op goal whose args are not ground still delays rather than binding a residual (the trap above, driven); the two-supplier tie still REFUSES at load (WI-1043 suite green); wi1027/wi1035 suites green. REFERENCE: WI-1043; WI-1027; WI-1010; WI-431; WI-938; WI-842 (the eval reader that resolves it by value); docs/design/058-implementation.md sec 23.

## Changes

### 2026-08-09T11:02:05Z — feedback — user

DELIVERED. Four early returns, not the two the ticket predicted, and a fifth piece the ticket could not have known about.

THE PIECE THE TICKET MISSED — THE HOST ENTRY. The ticket named two resolver early returns; both were real, but fixing them is not enough. `bridge_op_to_eval` calls `Interpreter::call_op_bridged` -> `invoke_op_with_requirements`, and that HOST ENTRY had no value-directed step at all: it went `cached_operation_body` -> `eq_bridge_target` -> `unrunnable_target_error`. Eval's IN-BODY path has had one since WI-350/WI-842 (step 3b of `dispatch_resolved_operation`), which is exactly why an operation body reached the fact-route supplier and a rule body could not. CONTROL: back this out alone and `a_fact_route_supplier_answers_from_a_rule_body` FAILS with the other three pieces present.

THE FOUR PIECES (each backed out separately, wi1043 suite re-driven per revert):
  goal shape  `body_less_relation_arity` (resolve.rs)  — admit on the SPELLED functor, since there is no pin to redirect to. Kept OFF `functional_relation_arity`: that is also WI-1040's weave gate, whose doc measures that weaving a body-less spec op takes `require[PartialEq[T]], eq(?x,?y)` from ONE solution to ZERO.
  reduction   `reduce_op_value` — stop bailing on a body-less op; run the eval bridge instead.
  host entry  `invoke_op_with_requirements` — the escape above.
  hook guard  `reduction_left_body_less_call` — the trap the ticket named, closed.

THE TICKET'S 'NAIVE FIX IS WORSE' WAS RIGHT AND ALSO INCOMPLETE. The trap is real and `an_unground_body_less_goal_binds_no_residual` drives it. But the ticket suggested the guard belongs with the existing 'is this un-reduced' question. IT DOES NOT, AND I MEASURED IT: folding it into `is_unreduced_op_call` broke 5 wi616_semantic_eq_test cases, each turning a definite FAILURE into a residual success (eq({1,2},{1,3}) and even the STRUCTURAL {1,2} === {2,1} answered 1 where they must answer 0). That predicate decides the DOMAIN of eq/cmp, and anthill.prelude.Set's insert/empty ARE body-less spec ops — symbolic ALGEBRA, data in all but declaration, the terms the membership rules resolve over. 'Has a body to fold' and 'did the reduction decide it' are two questions; they now have two predicates, and `op_call_as_occ` keeps the bodied one.

THE FINDING THE TICKET COULD NOT PREDICT — BRIDGE_REENTRY_CAP WAS LANDING ON THE STACK, NOT BELOW IT. The host entry sits on the eval<->SLD bridge path, whose crossings nest real Rust frames. MEASURED with RUST_MIN_STACK against wi625_eval_semantic_eq_test::eval_undecidable_instance_fact_eq_is_loud_not_false (the circular-eq fixture built to EXHAUST that cap): at 32 the chain already needed just under 2 MiB — essentially ALL of libtest's default 2 MiB thread stack, zero margin. The cap was not bounding the recursion below the native stack; it was landing on it. My escape deepened each crossing ~30% and the whole wi_tests BINARY died: 'fatal runtime error: stack overflow', SIGABRT, 2463 tests reported as NOTHING, test.sh exiting 0 and a failure-grep finding nothing. Two fixes: write the escape to REBIND sym/requirements and fall through to the existing frame push rather than recurse (one frame per crossing), and 32 -> 16. Numbers on record: 32/no-escape in (1.5,2.0] MiB, 32/escape in (2.0,2.6], 16/escape in (1.0,1.5].

WHAT /code-review THEN NARROWED (4 findings, all acted on, full suite re-run and controls re-driven):
  * THE REDUCTION WAS TOO WIDE. `reduce_op_value` is shared with eq/cmp/arith/unify via `reduce_operand`, and the first cut widened it for all of them — so a user sort supplying Set.insert would have had an eq OPERAND dispatched (the wi616 failure through the other door), and every eq over a Node-carried Set literal paid `sort_is_parametric` (51us, O(|symbols|)) plus a full `run_in_bridge_interp` per operand. Now behind a flag only `reduce_dispatched_goal_call` sets — the hook's own entry, the one site that BUILT the call it is deciding.
  * THE RULE-LESS CLAUSE was checked only at the goal shape; moved into `body_less_dispatchable` so the admitting and deciding halves cannot disagree about 'rules win while both exist'.
  * THE HOST ENTRY WAS SILENCING WI-822 LEG 2. `requirements_for_value_directed_impl` passes a non-empty incoming channel through, on the premise that a caller who built one 'knew the callee'. FALSE at a host entry: `call_op_sym` seeds __req_self for any op with a parent sort and `call_op_bridged` resolves the SPEC op's chain — both keyed to the sort we dispatch AWAY from. The channel is now discarded so leg 2 actually runs. Unreachable in the fixture (Desc has no requires chain), found by reading.
  * THE WOVEN BYPASS (`!reqs.is_empty()`) routed regardless of the new guard; narrowed so 'never unify on an undecided call' is TOTAL. WI-1040's reason survives (an unresolved call comes back as an ApplyWithin, which the predicate does not match, so it still routes and still delays). NOT DRIVEN and the code says so: reaching it needs a woven call — hence a BODIED functor, since collect_covered_calls weaves only what functional_relation_arity admits — whose dictionary selects a body-less member of a CONCRETE provider, which WI-818's backing check refuses.

ONE DEVIATION FROM THE ACCEPTANCE TEXT, stated rather than papered over: it asks that an un-ground body-less goal 'still delays'. It answers NOTHING — the pre-WI-1057 outcome. Making that case delay is WI-938's own standing open half, recorded at its site; the test pins only that no residual is bound.

ACCEPTANCE, all driven: fact-only fixture answers 9 from a rule body in BOTH spellings (control: [] in both) · un-ground body-less goal binds no residual · the two-supplier tie still REFUSES at load · wi1027/wi1035/wi1043 suites green. Full workspace green twice post-review: 29 binaries, 4434 tests, 0 failed. Back-out matrix: 5 reverts, 5 cells, re-driven after the review moved three of the pieces — same five cells.

DOCS: docs/design/058-implementation.md gains Sec 24 and Sec 23's row now points there; wi1043_bodyless_rule_body_test's header carries the four-piece list and WI-1057's own back-out table.

