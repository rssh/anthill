```anthill
fact WorkItem(id: "WI-411", created: "2026-06-06T19:43:02Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-16T11:34:17Z"))

fact Tag(workitem: "WI-411", name: "typing")

fact Feedback(workitem: "WI-411", author: "user", at: "2026-06-16T11:34:16Z")
```

## description

Typer: an UNQUALIFIED self-named spec-op call on an ABSTRACT receiver inside a provider's own impl statically self-dispatches to that impl instead of staying abstract (runtime value-dispatched). Surfaced building WI-064's lazy map carrier: MappedStream.splitFirst's body calls splitFirst(src) where src is the abstract source Stream; unqualified, it self-dispatched to MappedStream.splitFirst (so a List source's cons hit MappedStream's 'case mapped' -> MatchFailed; in general unbounded self-recursion). Worked around in combinators.anthill with the QUALIFIED Stream.splitFirst(src). Typecheck-only verification (the original fragment) did not catch it — only eval did. The typer should detect the abstract receiver and NOT bind the call to the enclosing sort's same-short-name impl. Acceptance: an unqualified nested abstract spec-op call in a carrier value-dispatches on the receiver's runtime carrier; the Stream. qualifier in combinators.anthill MappedStream.splitFirst can be dropped.

## Feedback

### 2026-06-16T11:34:16Z — user

Delivered in f053c53 (merged d715450). Root: an unqualified self-named spec-op call inside a provider's OWN impl (splitFirst(src) in MappedStream.splitFirst) was bound at LOAD by the scope chain to the enclosing member, shadowing the spec op Stream.splitFirst — so the typer saw a direct concrete call and never dispatched (eval hit 'case mapped' on a List cons -> MatchFailed). Design (user-steered, grounded in Scala/Rust/Haskell method-resolution research): the callee comes from the receiver's STATIC type, resolved in the typer — the dot form src.splitFirst already does this via find_spec_op_for_provided_sort; this brings the function-call form into line. Fix (typer, tree-producing): in the Apply build frame, when the functor is the enclosing sort's own provision of a spec op AND the receiver's static type is NOT the enclosing carrier, synthesize the spec-op Apply and re-Visit (like the dot form / fire_simp) so the STORED occurrence carries the spec-op functor; the existing dispatch then PinNows a concrete receiver (FilteredStream self-recursion stays static, preserving WI-424/WI-413) and value-directs an abstract Stream receiver. Combinators de-qualified (Stream.splitFirst(src) -> splitFirst(src)) = the acceptance. Regression wi411_unqualified_spec_op_dispatch_test (eval; typecheck alone missed it); wi064/wi413/wi439 now exercise the unqualified form. Full workspace 1938 green; /code-review (no correctness bugs / no over-fire / genuine guard) + 3 fixes applied.

