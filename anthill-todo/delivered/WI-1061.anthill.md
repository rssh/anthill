```anthill
fact WorkItem(id: "WI-1061", created: "2026-08-08T19:31:42Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-08T23:13:21Z"))

fact Feedback(workitem: "WI-1061", author: "user", at: "2026-08-08T23:13:19Z")
```

## description

BUG (typing; WI-1059 residue, DRIVEN end-to-end on the delivered tree): the unwritten-parameter rule is enforced ONLY in a PARAMETER's type and ONLY at its TOP LEVEL, so the same soundness hole is still reachable through the other two positions. WI-1059 made an unwritten sort parameter RIGID inside the operation body by materializing the slot as the projection off the value that carries it (`s: Stream[T = Int64]` is checked as `s: Stream[T = Int64, E = s.E]`), which refuses `feed(s: Stream[T = Int64]) = takes_pure(s)` at feed's own declaration. Two positions were deliberately left out and BOTH still load: (1) RETURN POSITION -- 'operation widen(s: Stream[T = Int64, E = {Error}]) -> Stream[T = Int64] = s' plus 'operation exploit(s: Stream[T = Int64, E = {Error}]) -> Int64 = takes_pure(widen(s))' loads clean, and an effectful stream reaches a slot declared 'E = {}' through the unwritten row in the RETURN; (2) NESTED POSITION -- 'takes_pure(l: List[T = Stream[T = Int64, E = {}]])' with 'feed(l: List[T = Stream[T = Int64]]) = takes_pure(l)' and 'caller(l: List[T = Stream[T = Int64, E = {Error}]]) = feed(l)' loads clean for the whole chain. WHY EACH WAS EXCLUDED, and what makes each its own measurement rather than a line to add: the RETURN flips the quantification -- the body must PRODUCE a value good for every instantiation, not consume one -- and there is no carrier VALUE in a return type to project off, so the filler there can only be a fresh anonymous rigid; WI-1059 measured that exact filler in the PARAMETER position at 27 stdlib load errors (all of the shape 'expected List[T = s.T], got List[T = ?T]', none of them wrong programs) and at 82 workspace failures on a later retry, so a fresh rigid needs its own cost measurement before it is used anywhere. A NESTED slot has NO path that names it at all ('s: List[T = Stream]' -- nothing spells the inner row), so it too can only take a fresh rigid, and a return type spelling the same nested bare sort would mint a SECOND unrelated one and desynchronize exactly as the parameter-position experiment did. ACCEPTANCE (each test says which case fails when backed out): the two programs above are REFUSED, each at the declaration that is wrong rather than at its exploiting caller (control: both load clean today); a body that genuinely holds for every instantiation still loads in both positions; wi1059_unwritten_param_rigid_test and wi1056_rule_body_type_check_test::all_four_spellings_of_one_type_conform_alike stay green; the corpus loads clean at every tier -- stdlib, host bindings, examples/, anthill-todo/, anthill-testcases/ -- measured through 'anthill load' per tier, whose control is that it DOES report the WI-1059 refusals. REFERENCE: WI-1059 (the delivered parameter-position half, and the two scope statements at rigidify_unwritten_sort_params that this ticket owns); WI-1056 (the four spellings); WI-942/WI-392 (the two rigid families that came before); docs/kernel-language.md section 'Expansion during unification', whose 'How the slot is named' paragraph states this ticket's scope boundary.

## Feedback

### 2026-08-08T23:13:19Z — user

DELIVERED, but SPLIT: the ticket's two positions turned out to be one implementation and one language DECISION, and only the first is here. NESTED IS CLOSED, and wider than the ticket described. rigidify_unwritten_sort_params now recurses through a parameter type's whole structure, not only its top level: a slot nested inside a sort-application binding (the ticket's 'feed(l: List[T = Stream[T = Int64]])'), inside a callback's RESULT ('feed(f: (x: Int64) -> Stream[T = Int64]) = takes_pure(f(1))'), and inside a TUPLE COMPONENT ('feed(p: (s: Stream[T = Int64], n: Int64))'). The last two were NOT in the ticket -- they were found by /code-review on the first cut, which descended through Parameterized bindings only and left every other type carrier falling to '_ => None'; both chains loaded clean and both are the WI-1059 hole verbatim one carrier further in. Each nested slot takes a fresh anonymous Var::Rigid (nothing NAMES a nested slot, so the WI-1059 projection is unavailable), except a SELF-sort reference, which takes the enclosing sort's rigid at every depth. THREE CHILDREN ARE EXCLUDED, each measured, not assumed: an effect-ROW binding and an arrow's effects child (a row's labels are compared by structural identity against the effects a body incurs -- descending into 'Stream[T = Solution, E = Error]' materialized Error's own T, broke rustland/anthill-todo/anthill at 'expected declared: [Error], got undeclared effect: Error[T = ?T]' and took 193 tests with it); and a callback's own PARAMETER, where the quantification faces the OTHER way -- 'f: (a: Cell) -> Unit' says f accepts EVERY instantiation, so the body may pass what it has; rigidifying it failed 9 tests at 'each(l: List[T = Cell], f: (a: Cell) -> Unit)' and 'foldCell', all refusing a body for handing a good value to a callback that declared it takes any. THE RETURN IS NOT CLOSED, and that is a decision, not a line: WI-1063 owns it, with the whole measurement. The walk works there (one more call to the same function; widen is refused at 'widen.return: expected E = ?E') but it contradicts docs/design/type-parameter-scoping.md sections 4-5, which read a bare return as ERASED rather than universally quantified, and it costs 40 tests across thirteen delivered tickets including the WI-401/402/457/480/491 escape gate, whose mechanism REQUIRES a bare abstract-spec return to conform by provider upcast before being refused. The user chose 'ship nested; hand the return to WI-374's scope'. The hole stays reachable and is PINNED by two tests so it cannot be forgotten. THE DECLARED EFFECTS CLAUSE IS NOT A FURTHER POSITION -- measured: the corpus's effect atoms are row vars, projections, Modify[v] denotations and concrete labels, and where a parametric sort IS written there the effects check is already strict in the SAFE direction ('effects Box' against an incurred 'Box[T = Int64]' is refused today). No ticket needed. EVIDENCE. Tests: wi1061_unwritten_slot_positions_test (7), with a four-column back-out matrix in its header -- every cell RUN, one revert each, and each column fails exactly ONE row because the corpus reaches none of these mechanisms. One earlier draft of that matrix was wrong and is recorded at its site. wi1059_unwritten_param_rigid_test and wi1056::all_four_spellings_of_one_type_conform_alike stay green. Full workspace: 29 binaries, 4409 tests, 0 failures. Corpus: all SEVEN tiers byte-identical to baseline (stdlib, rustland/anthill-stl/anthill, rustland/anthill-todo/anthill, rustland/anthill-cpp-gen/anthill, examples/, anthill-todo/, anthill-testcases/) with the control that 'anthill load' DOES report the WI-1059 refusal. NOTE for whoever measures a tier next: 'anthill-todo/' at the repo root holds WORK ITEMS; the todo CLI's own anthill program is 'rustland/anthill-todo/anthill', a different directory -- measuring the first and calling it the tier missed a real 2-error regression here. Docs: kernel-language.md section 'Expansion during unification' (the enforced positions, the by-depth filler rule, the three exclusions, and the undecided return); type-parameter-scoping.md section 5 now names its disagreement with the spec and points at WI-1063.

