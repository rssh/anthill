```anthill
fact WorkItem(id: "WI-1094", created: "2026-08-13T16:45:33Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none), ToolPasses(tool: "scaland-sbt-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-13T19:22:04Z"))

fact Tag(workitem: "WI-1094", name: "modinst")
```

## description

A NAMED requirement slot whose binding the caller ERASED gets a CONSTRUCTED dictionary, which may not be the one the incoming VALUE carries — 058 §3.4's "omitting a named slot in type position means ANY" is not implemented, it is merely unenforced. MEASURED (WI-861 session, scratchpad probe erase3.anthill, `anthill run`): a `SortedSet[T = Int64, O = Descending]` built at one site, then inserted into through `Any.add(s: SortedSet[T = Int64], x: Int64) = SortedSet.insert(s, x)` — a signature that omits O — reads back `3` where the same inserts through a slot-KEEPING route read back `7`. The erased route built a dictionary from `Int64`'s own ascending Ord; the set's own comparator was never consulted. SECOND MEASUREMENT, and it is why this is not WI-861's to fix: with exactly ONE Ord provider for the element the identical erasing program LOADS CLEAN today (probe erase1.anthill) and constructs silently — so the hazard is pre-existing and count-independent; the multi-provider case was only ever loud by accident of the tier-3 tie.

WI-861 THEREFORE WITHHOLDS 058 §3.2's rung 2a AT A NAMED SLOT (`typing::DefaultRung`, `rung_for_dep`), because letting a default answer there converts an existing loud refusal into exactly the silent wrong answer measured above. That gate is a holding action with a cost this ticket owns: §3.4's OTHER half — "omitting it at a call leaves it to inference (the ladder, §3.2)" — is consequently unimplemented, and so is the SECOND FACE of the pincer WI-858 recorded for `Pair`. Pinned as still-open in `wi858_pair_orderings_test::a_bracketless_pair_set_still_needs_its_bracket`, whose flip is this ticket's headline acceptance: `SortedSet.empty[T = Pair[Int64, Int64]]()` with two rival pair orderings declared must load and render `(1,9)(2,1)` — the prelude's canonical order — with no `O =` written anywhere in the pipeline.

WHY A DICTIONARY DEFAULT CANNOT DELIVER IT, stated so the increment is not re-attempted the cheap way: rung 2a fills a dictionary and writes nothing into the TYPE. A `SortedSet[T = Pair]` value whose type says "any O" and whose dictionary says `Pair` is the exact mismatch the erase3 measurement produced, and it does not compose — `empty` might infer O, but the very next `insert(s, x)` sees an unbound O again and re-infers (or refuses) independently. What is needed is INFERENCE INTO THE TYPE: the ladder's answer for an unbound named slot must BIND that slot's parameter in the call's result type, so the choice travels with the value and every downstream bracket-less call reads it back through the existing `selections_from_slot_bindings` (σ) channel, which is already tier 1.

TWO SUB-QUESTIONS TO SETTLE AT PICKUP, both visible in the measurements above. (a) A slot appearing only in the RESULT (`SortedSet.empty() -> SortedSet[T, O]`) is a genuine construction and inference is sound; a slot appearing in a PARAMETER (`insert(s: SortedSet[T, O], …)`) means a value already chose, and inferring there is what produced `erased=3` — so the parameter case must either forward the value's own dictionary (058 §3.9's "run time copies it along") or be REFUSED, and today it is neither. (b) The one-provider silent construction is the same defect with the tie removed; closing (a) closes it, and a fix that only handles the tie leaves it.

SCOPE: Rust — `kb/typing.rs` (`build_dispatching_dict_from_chain` / `build_dep_projection` Strategy 3, `selections_from_slot_bindings`, `check_unconstrained_type_params`), and whatever writes the inferred binding back into the call's resolved type. ACCEPTANCE: the wi858 arm above flips to the value; the erase3 shape either forwards the value's comparator (reads `7`) or is refused, DRIVEN as a value/diagnostic and not "it loads clean"; WI-861's `DefaultRung::Withhold` gate is re-examined and either deleted or re-justified in one sentence at its site; each test says which case fails when the change is backed out; full workspace green via rustland/scripts/test.sh. REFERENCE: 058 §3.2, §3.4, §3.9; design/058-implementation.md §3, §8 phase 8c.

