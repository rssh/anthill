```anthill
fact WorkItem(id: "WI-717", created: "2026-07-13T17:51:23Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-716"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-14T22:43:27Z"))

fact Feedback(workitem: "WI-717", author: "user", at: "2026-07-14T22:43:30Z")
```

## description

Domain rules match Option fields by their some/nil/cons SHAPE but have no `none()` case, so after WI-716 a WorkItem fact that OMITS such an optional (now stored as `none()`, not a wildcard var) matches NO rule and falls through. This is the domain-side consequence of the WI-716 soundness fix.

CONCRETELY (rustland/anthill-todo/anthill/rules.anthill): `depends_on: Option[List[String]]` and `description: Option[Term]` are matched structurally —
  :25  all_deps_satisfied(?wi) :- WorkItem(id: ?wi, depends_on: nil())
  :28  all_deps_satisfied(?wi) :- WorkItem(id: ?wi, depends_on: cons(head: ?dep, tail: ?rest)), ...
  :53/:58/:63/:68  ... WorkItem(..., description: some(?desc))
(the WI-408 some-coercion wraps the bare nil()/cons() patterns to some(nil())/some(cons(...))). The example examples/github-todo/rules.anthill has the same shapes.

BEFORE WI-716 an omitted optional was a wildcard var that unified ALL of these (unsoundly — exactly the bug WI-716 fixed: an omitted depends_on matched BOTH the nil() and the cons() rule). AFTER WI-716 an omitted optional is `none()`, which unifies NONE of them. EFFECT: a WorkItem that omits `depends_on` is classified as neither unblocked (no nil() match) nor has-deps (no cons() match) — it drops out of claimable/ready/blocked; one that omits `description` vanishes from open_item/claimable/needs_review.

CURRENTLY LATENT: all workitems.anthill facts write depends_on and description explicitly, so nothing omits them today. Surfaces the moment a hand-written or tool-written fact omits one of these optionals.

FIX (decide the domain semantics first — is "omitted depends_on" = "no deps" or "unspecified"?). Most likely: an omitted optional list should read as empty/no-deps, so add a `none()` case beside the nil() case, e.g.:
  all_deps_satisfied(?wi) :- WorkItem(id: ?wi, depends_on: none())
and, for description, decide whether an item with no description is still open/claimable (probably yes — match `description: none()` too, or drop the description guard from those rules). Alternatively normalize on the writer side so omitted optionals are persisted with an explicit empty value (`depends_on: []`), but the loud/robust fix is handling `none()` in the rules.

ACCEPTANCE: a WorkItem fact that OMITS depends_on is classified as unblocked/claimable (not dropped); same for an omitted description where the domain intends the item to remain listed; an integration/todo test loading such a fact and asserting claimable/ready; cargo-test green. Driver: surfaced by WI-716 (omitted optional now none(), not a wildcard).

## Feedback

### 2026-07-14T22:43:30Z — user

DELIVERED (098beb75). Domain semantics decided as the WI suggested: omitted depends_on = no deps (matches CLI normalize_deps); omitted description = item stays listed. Mechanism: (a) all_deps_satisfied/all_deps_verified gain a 'depends_on: none()' case beside nil() — safe because the WI-408 coercion skips Option.none-headed patterns, so the written none() matches the WI-716 fill exactly; (b) description_view(some(?d),?d)/(none(),none()) helper joined into open_item/claimable/blocked/needs_review — a description-less item surfaces with ?desc = none (deliberately heterogeneous: term when present, none atom when absent; CLI extract_string_field falls back to '' so next prints 'WI-x — '). needs_review added to the EXAMPLE rules (bundle parity) so it has coverage. Tests: 4 wi717 tests in github_todo_test with EXACT sorted-id sets (these also pin the WI-716 cross-file none()-fill: a var-fill would double-match description_view and duplicate solutions) + both description_view cases asserted + CLI next/list test vs bundled rules. Docs: kernel-language.md omission-semantics callout (value=none()/pattern=wildcard/reflect-Term=pattern), building.md walkthrough refreshed. NOTES: (1) verification via 'anthill query -p stdlib -p <proj>' is INERT for this fix — anthill-cli globally sorts files across -p dirs so project files convert before option.anthill registers ctors (WI-697 order-dependence); filed WI-719; test-harness and anthill-todo bundle load stdlib-first and are correct. (2) A hand-written var slot (description: ?) in a store fact still double-matches — pathological input, pre-existing class, accepted. (3) description_view is duplicated in the two intentionally-parallel rule files; hoisting a generic option-view to the prelude is a possible future cleanup.

