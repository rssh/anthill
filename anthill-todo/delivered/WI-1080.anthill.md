## Attributes

- id: WI-1080
- created: 2026-08-11T16:45:09Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-11T16:48:09Z

- acceptance: scaland-sbt-test

## Description

scala-gen Bootstrap: a refusal takes the whole FILE, so effects.anthill's two reflect-typed sorts take nine reflect-free siblings with them — Modifiable among them, dangling MutableCollection's supertrait as the LAST errors in the prelude closure.

MEASURED (2026-08-11, WI-1065's peel re-measurement): the whole-closure ladder is now 3 -> clean at 36 files, and all three errors are 'type Modifiable is not a member of anthill.prelude' in MutableCollection.scala. effects.anthill declares ELEVEN sorts in namespace anthill.prelude (Effect, Modifiable, Modify, ModifyRuntime, Error, Suspension, Branch, MatchFailed, DivisionByZero, EmptyStream, RelationFloundered); exactly TWO import anthill.reflect — MatchFailed (:101-103, fields typed NodeOccurrence/Term) and RelationFloundered (:152-154, a Term field) — and the per-file refusal ('sort MatchFailed ... imported from anthill.reflect') takes the other nine out with them.

THE BLAST RADIUS IS EXACTLY ONE CONSUMER, measured before filing: no emitted prelude signature names MatchFailed or RelationFloundered — every prelude occurrence is inside an Error[...] effect row, and rows ERASE (§2.8a, WI-1062; the one other mention is a comment in relation.anthill). So sort-scoped refusal would leave NOTHING dangling, while file-scoped refusal dangles MutableCollection extends Modifiable[C].

ARMS, with costs:
1. PER-DECLARATION granularity (recommended): generate catches BootstrapError per top-level declaration — the SORT is refused, its siblings emit. The refusal-set test already pins (declaration, construct) per file; it reshapes to allow multiple refused declarations per file (effects.anthill gains its second: RelationFloundered, currently shadowed by the first throw). DESIGN CONSEQUENCE to state at the WI-1055 'breaks the promise loudly' comment: emittedTypes' caller-side promises are already per-declaration, so a refused sort's promise now breaks NARROWLY — a consumer's closure compile reports the one missing _root_ type instead of the whole file vanishing. Same loud failure mode, smaller blast radius. refuseColliding runs over the surviving set. This deliberately revisits WI-1055's abort-the-file choice; record why at its site. Note the standalone-compile floor (>= 16) may move UP.
2. STDLIB SPLIT: move MatchFailed + RelationFloundered to a reflect-adjacent prelude file. Fixes the granularity for every backend at once, but touches both implementations' loadable lists and the rust eval that raises match_failed — cross-impl coordination for the same closure outcome.
3. MAP the reflect types (type_map/CarrierBinding entries for NodeOccurrence/Term): the real 'unmapped reflect types' family from WI-1020's partial-delivery measurement (also meta.anthill's Meta, relation.anthill's guarded_of). Largest, KB-gen-scale per WI-1065's option-3 discussion, and NOT needed to unblock the closure.

RELATION TO WI-1020 (soft ordering, deliberately no --depends edge — an edge blocks claiming, and WI-1020 is deliverable either way): with arm 1 or 2 delivered first, WI-1020's whole-prelude closure test needs NO exclusions beyond the refusal set itself; delivered after, WI-1020 names mutable_collection.anthill as an exclusion pointing HERE, removed when this lands.

ACCEPTANCE: effects.anthill's nine reflect-free sorts are emitted and the whole-closure peel reaches clean WITH mutable_collection.anthill in the set (re-run the ladder and record it); the refusal-set test pins BOTH of effects.anthill's refused declarations with their constructs; a same-file consumer of a refused sort's type is DRIVEN by a fixture (sibling sort with a field typed by the refused sort) and the pinned outcome stated — the promise breaks at the closure compile naming the one missing type; say at each test site which assertion fails when the granularity is backed out. sbt test green.

