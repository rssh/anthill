## Attributes

- id: WI-20260913-J38VE-s7-of-wi-20260908-vvm1r
- created: 2026-09-13T12:36:11Z

- status: Delivered
- status_agent: claude
- status_at: 2026-09-13T14:04:32Z

- acceptance: cargo-test, scaland-sbt-test

- depends_on: WI-20260909-QMFC5-s2-of-wi-20260908-vvm1r

- tags: vvm1r

## Description

S7 of WI-20260908-VVM1R (umbrella): THE FETCH MUST SEE THE WRITTEN BRACKET -- no MULTI-PARAMETER SPEC grounds through the typed-head anchor, and today that fails SILENTLY as a clean load plus a residual.

MEASURED 2026-09-13, eight arms on one fixture (a CARRIER parameter and a CONTENT parameter at DIFFERENT types, with a receiving operation on the carrier):

  BASELINE, the identical shape at ONE parameter     Int(7), DEFINITE
  2 params, carrier declared 1st, both written       residual
  2 params, carrier declared 2nd                     residual
  2 params, only the carrier binding written         residual
  2 params, the other carrier                        residual
  2 params, NO receiving op                          REFUSED, located (correct)
  2 params, receives on the WRONG param              REFUSED, names P (correct)
  CONTROL, no require at all                         no rows

THE BASELINE IS THE POINT. Without it "two parameters delay" is equally consistent with "this fixture shape delays", and the first attempt at this measurement had exactly that hole -- it bound BOTH parameters to the same sort, which cannot tell which one the dictionary keyed on. Parameters at DIFFERENT types plus the one-parameter twin is what makes this a PARAMETER-COUNT finding.

ROOT CAUSE, ALREADY NAMED AND NOT A HYPOTHESIS. fetch_dictionary takes (spec_sort, op_functor, arg_vals) and NEVER SEES SLOT 0, so the author's written `P = Int64` is replaced by WI-20260830-X9PB4's synthesized wildcard -- and a wildcard is REFUSED against a provider's CONCRETE binding. No provider answers, the fetch reports Undecided, the goal delays. 060-implementation sec 8.6 states this and says closing it "is a resolver signature change, which is why it is recorded here instead of grown into this ticket".

WHAT THIS TICKET CORRECTS BESIDES BUILDING IT: sec 8.6 recorded the gap as a CORNER -- "a self-representing spec whose provider pins a sibling concretely" -- and pinned one row in that shape. It is not a corner. The fixture above is NOT self-representing, its carrier parameter IS identifiable (rung 1 names it from crecv(x: C)), its second gate AGREES with the provision, and it delays anyway; declaration order makes no difference and omitting the content binding makes none either. The carrier-IDENTIFICATION machinery is fine -- both rungs sec 8.2 calls "genuinely absent" refuse with their own located messages. The FETCH is what fails, one question later.

THE CONSUMER EXISTS AND IS WAITING. WI-20260909-51W18 RETAINS the written bracket on the goal's slot 0 and make_witness now pushes the instance whole, so `require[Desc[T = Leaf]]` and `require[Desc]` are already distinguishable on the stored form. This would be that retention's real first reader on the FETCH side -- WI-20260909-QMFC5 delivered the first on the GUARD side (anchor_grounding's written-binding disagreement refusal).

ACCEPTANCE, and it has FAILING CONTROLS FROM DAY ONE because both are already pinned as delays: a_multi_parameter_spec_delays_even_when_its_carrier_parameter_is_identifiable (wi_qmfc5_typed_head_anchor_test.rs) must answer 7, and a_self_representing_spec_whose_provider_pins_a_sibling_concretely_delays must stop delaying -- each asserted BY VALUE, not by a clean load. Its one-parameter control (the_control_the_same_shape_at_ONE_parameter_answers) must keep answering 7 and must be stated at its site as passing either way by design.

CONTROLS THE FIX MUST NOT BREAK, each named at its test site with the rows it moves: a WRITTEN binding that DISAGREES with the derived concrete type stays a located refusal (a_written_binding_that_disagrees_with_the_bound_is_refused -- once the fetch reads slot 0, that check and this read are two readers of ONE bracket and must not answer differently); the two "genuinely absent" rungs keep their located refusals; and the WITNESS path's existing rows keep their answers, since slot 0 reaches the fetch on BOTH paths once the signature changes. Say which rows fail per back-out and run each back-out with a patch that ASSERTS it applied. cargo-test green via scripts/test.sh.

NOT IN SCOPE: the ATTRIBUTION by projection root (WI-20260909-S8CBV's own acceptance), and sec 8.9's undecided question about whether a rule USES a requirement written on its enclosing or callee declaration.

## Changes

### 2026-09-13T14:04:27Z — feedback — claude

DELIVERED. `fetch_dictionary` reads the goal's slot 0, and the elements a `require`
bracket NAMES now select the provider row. Both pinned delay rows answer `7` DEFINITELY.

WHAT WAS BUILT. `builtin_find_dictionary` already walked slot 0 twice-over (the spec base,
and since S8CBV the projected member); it now reads the instance ONCE into a
`RequirementBracket` — the projected member and the written bindings off one
`extract_type` — and hands it down BOTH the guard path (which uses only the member) and
the fetch path. The shared wildcard tail `sort_goal_with_wildcards`, which both
`anchor_sort_goal` and `witness_sort_goal` end in, consults the bracket before minting: an
element the author NAMED is pinned from what they wrote, and only an element nobody named
becomes a wildcard. An element the CARRIER already pinned is untouched — the written-vs-
derived disagreement is the load site's refusal, not a precedence question at the fetch,
so that bracket still has exactly ONE reader.

Only a NAMED TYPE pins, as a positive test (`SortRef` / `Parameterized`) rather than a
list of exclusions, because a bracket's value kinds are a growing set and a new one must
default to the pre-ticket behaviour. The two shapes it turns away — a projection, and a
value naming a type parameter — could NOT be driven to a different ANSWER, and that is
stated at the site rather than implied: a projected element reaches the tail only where
the producer did not already pin it, and every such spelling is REFUSED AT LOAD first
(a projection needs a typed head root, and a typed head takes the anchor route); a written
type-parameter name is dropped upstream by 51W18's own rule, and a head-introduced type
variable resolves to its guard-given BOUND. Both lines are written for the value they
carry if those upstream rules move.

ACCEPTANCE, both asserted BY VALUE.
  a_multi_parameter_spec_answers_when_its_carrier_parameter_is_identifiable   7, DEFINITE
  a_self_representing_spec_whose_provider_pins_a_sibling_concretely_answers   7, DEFINITE
Both were renamed (they were named `…_delays`) and the file header's "two boundaries"
section now records the first as closed. Its one-parameter control keeps answering 7 and
says at its site that it passes either way BY DESIGN — its sole parameter IS the carrier,
so the tail this ticket changed has no element left to fill.

ONE THING THE TICKET DID NOT PREDICT, and it is a regression this ticket introduced and
then closed. `WitnessGoal::synthesized` is what makes a resolution TIE read as "cannot
decide" rather than as a coherence Defect — a `debug_assert!(false, …)`. A first cut let a
WRITTEN element leave that flag intact, on the reading that an element the author named
makes the goal fully determined. MEASURED on X9PB4's own tie fixture,
`require[Spec[C = Carrier, Note = Int64]]` ABORTED every debug build:

  find_dictionary: two providers answer `Spec[C = Carrier, Note = Int64]`
  at run time: MidA, MidB

And the reading was wrong on its own terms — the tie is on `C`, which both providers match
through subtyping, and has nothing to do with `Note`. Pinning an element does not convert
an unrelated tie into overlap. A written element therefore clears the flag exactly as a
minted wildcard does; the flag is renamed `from_carried_types` and inverted, because that
is the question it actually asks: did anything but a carried type decide this goal. Kept
as a ROW (a_tie_a_written_element_does_not_cause_stays_a_delay) with the bare spelling as
its control, not as a comment.

/code-review (high) FOUND A SECOND FIRST-CUT REGRESSION, and it was the worse kind — a
WRONG ANSWER, not a missing one. `written_element` lowered the bracket's value with
`type_value_as_term`, which returns a term id only for a `Value::Term` and otherwise falls
to `sort_functor_of_view(…)` — the bare SORT HEAD, arguments discarded. MEASURED: a
written binding ALWAYS arrives as a `Value::Node`, so EVERY applied element took that
path. `Box[E = Leaf]` became `Box`, and both faces were wrong:

  * the AGREEING spelling DELAYED — `require[Sp[…P = Box[E = Leaf]]]` against
    `provides Sp[…P = Box[E = Leaf]]` found nothing, so this ticket's headline capability
    did not hold for applied elements at all;
  * the DISAGREEING one ANSWERED — `require[Sp[…P = Box[E = Other]]]` selected the
    `Box[E = Leaf]` row, because both had been reduced to `Box`.

FIXED by lowering through `value_to_term` — WI-390's "faithful, total Value → Term
boundary", whose own doc names this exact use ("the one converter to use where a
value-in-type may ride, e.g. a `requires`/`provides` spec"). Driven by a new row asserting
BOTH faces. The CARRIER sites (`anchor_sort_goal`, `witness_sort_goal`) still lower
through `type_value_as_term` and are deliberately NOT moved: their value is a carried type
read off a runtime value, not an author-written one, and changing it changes which rows
every existing anchor selects. Recorded at the site rather than folded in.

The review's other finding (High — "the tie verdict is cleared only on the wildcard arm")
was an ARTIFACT of my own back-out harness racing the review: it read the tree mid-matrix,
captured the `flag` back-out as the baseline, and faithfully restored to that. Verified
against the snapshot — the shipped code sets the flag unconditionally at one site, and
`written_element` runs once per element, so the secondary "runs twice / allocates" note
does not apply either. My fault for running the matrix concurrently with the review.

BACK-OUTS — six axes, one per CALL SITE, each applied with a patch that asserts its anchor
occurs exactly once, one restore between each, re-measured after the final refactor:

  THE PREFERENCE (`written_element` → None at entry)                4 rows
  the WITNESS call site (`witness_sort_goal` passes &[])            1 row
  the ANCHOR's PARAMETER branch (passes &[])                        2 rows
  the ANCHOR's SELF-REPRESENTING branch (passes &[])                1 row
  the FLAG CLEARING (only a MINT clears it)                         1 row, and it ABORTS
  the FAITHFUL LOWERING (back to `type_value_as_term`)              1 row, BOTH its asserts

The two anchor branches are two call sites and move SEPARATELY — that is why they are
backed out separately rather than as one switch. The in-fixture controls (every spelling
that writes nothing for the element: `Sp[C = Red]`, `Sp`, `Sp[C = p.E]` alone) pass under
all five BY DESIGN, and are what say the WRITTEN BINDING is doing the work and not the
fixture.

NEW ROWS this ticket adds beyond its acceptance, in
`wi_j38ve_written_bracket_fetch_test.rs`:
  * the WITNESS route — the acceptance rows are both ANCHOR rows, and the fix threads slot
    0 into both producers, so the witness half would otherwise be unmeasured;
  * the SUM with S8CBV gate (1) — `require[Sp[C = p.E, P = Int64]]`, a projected carrier
    AND a written content, which needs both tickets and neither alone. It is also what
    would expose the two readers of slot 0 disagreeing;
  * the tie row above;
  * the APPLIED element row, both faces.

A REASON THAT EXPIRED, corrected rather than inherited. `requirement_projection_member`'s
doc justified S8CBV's carrier-parameter rule with "MEASURED: on a two-parameter spec every
admissible spelling RESIDUALIZES, so the two rules cannot be told apart". This ticket is
what made that false. RE-MEASURED on the disagreeing shape built for it (a concrete carrier
binding with a projected sibling, at a provider binding the sibling abstractly AND at one
binding it concretely): both rules answer nothing on both, and the whole wi_s8cbv /
wi_qmfc5 / wi_j38ve set is green under the naive rule. The conclusion survives; its reason
is now structural — a projection at a non-carrier element does not survive to a fetch at
all.

DOCS: 060-implementation §8.6 (the STILL UNDECIDED paragraph, the AS BUILT record, the
controls), the §0 owner table and the §8.10 build sequence; requirement-channel.md §5's
wildcard bullet gets its complement; kernel-language.md gets the language-visible rule —
every element the bracket names selects the instance, and an element it leaves unwritten
does not discriminate.

TESTS: full workspace green via scripts/test.sh — exit 0, 6992 passed / 0 failed, of
which 4622 wi_tests (was 4618; +4 new rows). scaland
`sbt test` green, 573 + 35 — nothing to port: scaland has no dictionary machinery at all, which
its own `Loader` comment records ("no `DictLayout` and no dispatch resolution").
NOT IN SCOPE, untouched: S8CBV's attribution by projection root, and §8.9's question about
whether a rule USES a requirement written on its enclosing or callee declaration.

