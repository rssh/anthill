```anthill
fact WorkItem(id: "WI-819", created: "2026-07-22T14:50:20Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-07-22T16:07:32Z"))

fact Feedback(workitem: "WI-819", author: "user", at: "2026-07-22T16:08:22Z")
```

## description

ONE type-annotation channel, on the PATTERN: lift `type_ann` from `Pattern::Var` to every `Pattern` variant and DELETE `Expr::Let.type_annotation`. PROBLEM -- a `let` annotation has TWO homes, and which one it lands in depends on the PATTERN'S SHAPE, which is not something the author chose. (1) INNER: `Pattern::Var { name, type_ann: Option<Rc<NodeOccurrence>> }` (node_occurrence.rs), the per-binder `: T` -- `let (a: Int, b) = pair`, `lambda (y: T) -> …` (WI-517). (2) OUTER: `Expr::Let { pattern, type_annotation: Option<Value>, value, body }`, whose own doc calls it "the OUTER `: T` annotation from WI-185 (`let p: T = …`)". The outer channel exists ONLY because `type_ann` is declared on `Pattern::Var` ALONE -- `Wildcard`, `Literal`, `Constructor` and `Tuple` have no such slot, so `let (a, b): (Int, String) = pair` has nowhere on the pattern to put it. So `let x: T` and `let (a, b): T` are the SAME SYNTAX landing in DIFFERENT SLOTS. WHY IT IS NOT MERELY UNTIDY -- the two channels are ALREADY OUT OF SYNC, and the drift is silent. The INNER annotation is lowered onto the pattern's TERM twin: `load_pattern_var` (load.rs) calls `node_occurrence::value_to_term` and wraps it `build_some`/`build_none` into `var_pattern(name, type_ann)`, with a comment stating the case -- "`value_to_term` is the total Value→Term boundary (WI-390) … a `denoted`-bearing type lowers losslessly". The OUTER annotation is on NEITHER carrier: WI-342 T8 dropped the term-side `type_name` slot as write-only, and WI-814 finished the deletion (reflect field + `visit_fn` reader). CONSEQUENCE, MEASURED as part of WI-814: with `Expr::Let` now reading as a structural `Functor{let_expr, 0, 3}`, the outer annotation is invisible to `views_structurally_equal` AND absent from `goal_fingerprint`, so `let x: Int64 = 1` and `let x: String = 1` compare EQUAL and produce the SAME `GoalKey` -- and because no `StructToken::Opaque` is emitted any more, that key is now CACHEABLE, so the resolver's per-query cache and `seen_goals` treat two different programs as one. The inner channel has none of these problems, because it rides the pattern twin. NOTE THE FALSE JUSTIFICATION, so it is not repeated: the reason recorded for making the outer annotation `Value`-only was that a denoted-bearing type (`: Modify[c]`) cannot be hash-consed. That is FALSE since WI-390 (`value_to_term` is total; `Value::Node` lowers via `occurrence_to_term`), and it is falsified IN PRACTICE three functions away by `load_pattern_var`, which has been lowering exactly such types all along. PROPOSAL -- delete the second channel rather than repair it. Move `type_ann: Option<…>` from `Pattern::Var` up to EVERY `Pattern` variant (or to a wrapper carrying pattern + optional annotation, whichever the implementer finds less invasive), and remove `Expr::Let.type_annotation` entirely. Then: `let`, `lambda` and match branches annotate UNIFORMLY; there is ONE reader instead of two; the annotation is already on the term (the pattern twin's existing `value_to_term` path, no new slot on `let_expr`); the view already exposes it (`var_pattern(name, type_ann)` is in WI-814's `pattern_shape`), so comparison and fingerprinting become correct with NO new key and NO new mechanism; and the WI-814 conflation above disappears without restoring anything. DECIDE, do not assume: whether EVERY variant gets the slot or only the ones that can bind. `let _: T = e` is meaningful (it asserts the value's type and binds nothing); `let 5: Int64 = e` is odd but harmless. State the rule chosen. BLAST RADIUS, to be measured before starting rather than discovered: the reflect surface grows a slot on `tuple_pattern` / `constructor_pattern` / `literal_pattern` / `wildcard` (`stdlib/anthill/reflect/reflect.anthill`), `pattern_to_term` and `term_to_pattern_occurrence` gain it on every arm, and `for_each_pattern_child` / `drain_pattern_children` / `reassemble_pattern` must visit it uniformly. On the deletion side, `Expr::Let.type_annotation` is walked by the DeBruijn open/close pair, `substitute_occurrence`, the `*_value_type` helpers and `simp_rewrite::reassemble` (node_occurrence.rs), and is READ by the typer in several places that must be repointed at the pattern: the `Expr::Let` arm's annotation handling (WI-399 projection elimination, WI-400 let-alias canonicalization), the value's expected type, the `LetAfterValue` conformance check (WI-379), and `unroll_annotation_with_inferred` (WI-374 / spec §8.1). The binder-side reader (`bind_and_label_pattern` / `extract_pattern_type_ann`, plus WI-794's contradicting-binder-annotation check) is the one that SURVIVES and absorbs the others. DO TOGETHER WITH WI-803's REMAINING GAP: `Pattern::Tuple.labels` is dropped by `pattern_to_term` ("KNOWN LOSSY AND UNCOVERED"), so `Pattern::Tuple` is touched by both concerns; doing them separately means editing the same arms and the same reflect entity twice. ACCEPTANCE: exactly one annotation channel remains and it is on the pattern; `Expr::Let.type_annotation` no longer exists; a test pins that `let x: Int64 = 1` and `let x: String = 1` are NOT structurally equal and do NOT share a `GoalKey`; a test pins that `let (a, b): (Int64, String) = p` annotates through the SAME channel as `let x: Int64 = 1` (the shape-dependence is gone); the existing let-annotation behaviours stay green -- WI-379 conformance rejection, WI-399 projection elimination, WI-374 partial-parametric unrolling, WI-400 alias identity, WI-517 typed lambda binders, WI-794 binder contradiction; the rule chosen for non-binding patterns is stated; full workspace green via rustland/scripts/test.sh.

## Feedback

### 2026-07-22T16:08:22Z — user

DELIVERED. ONE annotation channel, on the pattern occurrence; `Expr::Let.type_annotation` deleted. Full workspace green (3424 tests / 85 binaries / exit 0) via rustland/scripts/test.sh.

REPRESENTATION CHOSEN, and the rule for non-binding patterns. `type_ann` hangs on `NodeKind::Pattern { pattern, type_ann }` -- the OCCURRENCE wrapper -- not on each of the five variants. 23 `NodeKind::Pattern` sites vs ~90 variant-construction sites, but the real reason is that an annotation is written ABOUT a pattern occurrence (`let p: T = e`), so no variant should own it. THE RULE: EVERY pattern may be annotated, including ones that bind nothing. Hanging the slot on the occurrence DISSOLVES the "which variants get it" question rather than answering it -- there is no variant to exclude. `let _: T = e` asserts the value's type and binds nothing (tested; a false assertion is rejected); `let 5: Int64 = e` is odd but harmless. Grammar already admitted both.

TERM SURFACE. `type_ann` declared on ALL SIX reflect pattern entities (including `named_tuple_pattern`, which has no producer -- declared anyway so the enum has NO annotation-less variant, since a shape that cannot carry `: T` IS the WI-819 defect), emitted ONLY when present. Keeps `wildcard` NULLARY, which `KnowledgeBase::alloc`'s WI-511 collapse and WI-436's cross-carrier head identity rely on -- pinned by `an_unannotated_wildcard_stays_nullary`. TRADEOFF STATED IN THE FILE rather than papered over: a declared `Term` that is sometimes absent is less type-honest than an always-present `Option[Term]`; the latter was rejected because it makes `wildcard` unary. NOTE A FALSE PRECEDENT I WROTE AND THEN REMOVED: I cited `proof_stmt.strategy` as a surface precedent for conditional keys. `proof_stmt` has NO reflect entity declaration -- it appeared in that file exactly ONCE, in my own comment. Same failure mode as WI-776 (a claim the file itself refutes), caught by review, not by me.

WI-803's REMAINING GAP CLOSED on the same arms: `tuple_pattern` gained `labels` (emitted when non-empty), so `pattern_to_term` no longer drops them and a round-tripped pattern stops reverting to reading components BY SLOT (the WI-788 wrong answer on a permuted value).

THE DEEP FIX, which was NOT where I first looked. A first cut passed `Value::Node` straight into the slot to keep a COMPOUND type projection (`s.cell.T`) on the carrier `eliminate_type_projections` understood. That was fixing the wrong end, and it introduced a REGRESSION: a `Type`-kind occurrence in an ordinary occurrence-child slot breaks collect/close LOCKSTEP -- `node_to_debruijn` closes it via `rewrite_type_occurrence` while `collect_occurrence_global_vars_ordered` asserts on it and collects NOTHING (stray `Global` in a stored rule body, undercounted arity). Pre-WI-819 that path worked via `collect_option_value` -> `collect_value_type`. MEASURED with a witness, not reasoned. Root cause was one altitude lower: `resolve_receiver_path_type` matched `Value::Node` + `Expr::DotApply` STRUCTURALLY, so a compound receiver resolved on ONE CARRIER ONLY -- and `eliminate_expr_carried_projection` rejected it on the other with "receiver is not a simple value reference". WI-814 had already given `Expr::DotApply` and its `dot_apply` term twin ONE view head with the same three keys, so the fix is to read the receiver through `TermView`. Carrier-neutral now; the verdict no longer depends on which carrier a type is written on (WI-425).

WHAT THAT DELETED, all at once: the `Value -> Term -> Occurrence -> Term -> Value` roundtrip; `ann_override` / `term_to_param_occurrence_annotated` / `BuildFrame::Let.type_annotation`; the double lowering in `annotate_let_pattern`; and the let-vs-lambda fidelity asymmetry (both spellings now reach the slot identically). The TERM is the single source -- `term_to_param_occurrence` reads the annotation back out of it -- so the two carriers hold one value BY CONSTRUCTION rather than by two writes that must agree. Also retired: four `*_option_value` walker helpers, three explicit `Expr::Let` walker arms, `extract_named_arg` (became dead), and node_occurrence's private `build_some`/`build_none` (duplicates of load.rs's).

A FALSE JUSTIFICATION I WROTE, recorded so it is not repeated. `for_each_pattern_child`'s doc claimed the split `(pat, type_ann, f)` signature "is a compile error until every caller passes the annotation along". That is INVERTED: `for_each_pattern_child(pat, None, f)` compiles, so the split PERMITTED the very silent skip it advertised against, while taxing all twelve call sites. It now takes the OCCURRENCE, which makes omission unrepresentable; `for_each_subpattern` is the named opt-out for the two callers that legitimately supply the annotation themselves.

TESTS, and TWO VACUOUS DRAFTS the controls caught. `wi819_annotation_channel_test.rs` (10 tests). The identity claim is HAND-BUILT with one shared binder symbol, and the file records why: a loader-driven pair CANNOT isolate the annotation, because WI-550 mints every binder Symbol per SOURCE SITE, so two `let`s in two operations differ in their binder name regardless. Draft 1 also differed in its VALUE. BOTH DRAFTS STAYED GREEN with `pattern_shape` deliberately blinded to `type_ann` -- the control that caught them. Every claim is now control-verified: blinding the view, dropping the loader annotation, dropping the labels, and re-introducing the Node passthrough each turn the relevant test RED. What the loader actually does is pinned separately (`the_loader_attaches_distinct_annotations_to_distinct_lets`, `the_annotation_rides_the_pattern_term_not_the_let_term`).

DIAGNOSTICS. The two new loader errors are SPANNED variants (`PatternAnnotatedTwice`, `LetAnnotationOnNonPattern`), not `LoadError::Other`, which carries no span and renders with no line:col -- WI-745 made file identity part of every load error's contract, and the double-annotation case is user-reachable. A test asserts the location is present, and fails when the error is downgraded to `Other`.

SPEC. `docs/kernel-language.md` gained a `let`-annotation paragraph -- the form was NEVER documented despite shipping in WI-185. Every claim in it is backed by a passing test, including `let (a: A, b): T` (sub-binder and whole-pattern annotations coexist: different slots on different patterns). An illustrative `Map.empty()` example was dropped rather than ship one I had not verified exists.

STALE COMMENTS SWEPT: seven sites in node_occurrence.rs / resolve.rs still described `Let.type_annotation` as a live TermId-typed field the walkers descend; term_view.rs's "two lets differing only in annotation compare EQUAL / the annotation CAN be restored to the let_expr term" block (now false, and the restoration it invited is what `NodeKind::Pattern` forbids); and the typer's lambda arm claiming "the grammar's `pattern_var` doesn't surface [an annotation] today", false since WI-517.

NOT RUN: the `/code-review` skill (bug-hunting). `/simplify` was run (4 agents: reuse / simplification / efficiency / altitude) and all findings are applied or recorded above. `/code-review` remains outstanding per the repo rule.

