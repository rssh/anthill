```anthill
fact WorkItem(id: "WI-314", created: "2026-05-27T12:22:12Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-307"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-05-27T15:43:21Z"))

fact Feedback(workitem: "WI-314", author: "user", at: "2026-05-27T08:32:17Z")

fact Feedback(workitem: "WI-314", author: "user", at: "2026-05-27T08:50:00Z")
```

## description

Modify region/escape analysis: make Modify[result] non-viral so constructors can declare it. SUCCESS CRITERION: stdlib cell.anthill operation `new(v: V) -> Cell` declares `effects Modify[result]` and the full suite is green — in particular wi205_cell_test passes with no "undeclared effect Modify[T = result]" failures.

BACKGROUND (045/046 review, 2026-05-27): Alloc was eliminated in favor of Modify[result] — a constructor's effect is { Modify[result] } (it initializes the region it returns; result flows out so the label is well-scoped). Proposals 045 §5.5 and 046's case table now document this. But adding `effects Modify[result]` to Cell.new TODAY breaks 6 wi205_cell_test: effect-checking is enforced and there is NO masking, so the effect goes VIRAL — every cell-allocating op (make/chain/make_and_read) must itself declare Modify[result].

MECHANISM NEEDED: at a call `c = f(...)` where f : Modify[result], rewrite Modify[result] -> Modify[c] (callee result -> caller binding), then (a) MASK it if c does not escape the caller (never reaches the caller's own result), or (b) re-key as the caller's Modify[result] and propagate if it does. The decision "does this binding reach the operation's result?" is escape / result-reachability analysis — the narrow slice of 046's region analysis (provenance: input / fresh-output / local; masking: Modify[input]->kept, Modify[fresh-output]->masked or handed off, Modify[local-non-escaping]->dropped). Full provenance+aliasing is the superset; the constructor case needs only result-reachability.

NOTE: the trivial alternative (unconditionally discharge Modify[result] at the call boundary) needs no analysis but makes Modify[result] semantically identical to {} — so a MEANINGFUL Modify[result] requires this analysis. (If the answer turns out to be "{} is enough", that is the alternative resolution and this item closes by documenting it.)

SCOPE: typer effect-checking (typing.rs) — a masking pass over derived effect rows keyed on result-reachability; possibly load.rs for result-keyed Modify handling. REFS: proposals 045 (§5.5; effect_derive default+dispatch §5.2.1), 046 (provenance/masking, the deferred body of effect_derive); builds on effect-checking (WI-307); WI-302 name-case (Modify[result] denotes, landed 7ada265); related but separate: WI-309 ([feeds:] metadata for the higher-order/foreach cases). ACCEPTANCE: cargo-test green WITH cell.anthill `new` declaring `effects Modify[result]`.

## Feedback

### 2026-05-27T08:32:17Z — user

CORRECTION (2026-05-27): the "{} is enough" alternative in this WI's NOTE is REJECTED. A no-effect Cell.new reads as PURE, which would let CSE / memoization merge distinct new() calls — but each must yield a distinct cell (new(); new() != reuse). So a non-empty effect on constructors is MANDATORY as the "not pure / not CSE-able" marker; the ONLY resolution is to build the analysis. CSE-safety AND non-virality both hold with Modify[result] + boundary masking: each new() stays effectful at its CALL SITE (CSE-safe), while masking happens only at the enclosing OPERATION BOUNDARY (so ops with non-escaping locals don't go viral).

EXISTING SUBSTRATE (extend, don't build from scratch): the typer ALREADY masks Modify on let/match-bound local resources at the op boundary. external_effects() (typing.rs:486; called at 1712 and 6773) drops effects whose resource symbol is a local resource; TypingEnv.local_resources + declare_local_resource (449) are populated for let bindings (1579) and match-case patterns (4558); extract_effect_resource_sym (499) already sees through WI-302 denoted. GAP to close for WI-314: (a) at a call `c = f(...)` re-key the callee's Modify[result] -> Modify[c] (callee result -> caller binding); (b) make the local-resource drop ESCAPE-AWARE — today external_effects drops let/match-bound-local effects UNCONDITIONALLY, which becomes unsound once such a local ESCAPES via return: then it must be re-keyed as the op's own Modify[result] and propagated, not dropped.

### 2026-05-27T08:50:00Z — user

ORGANIZATION DECISION (2026-05-27; see docs/brainstorms/region-analysis-organization.md): implement WI-314 as OPTION 3' — a factored, separately-testable region/effect MODULE (grown from the existing external_effects / local_resources / extract_effect_resource_sym functions in typing.rs) that the typer calls at its existing boundary frames. NOT inline-scattered (option 3), NOT a post-typer pass (option 2), NOT a generic plugin-engine yet (option 1). Keep it PLUGIN-READY — a clear "node + env + child results -> masked/re-keyed effect row" interface — so it promotes cleanly to the Option-1 fused plugin-engine when a 2nd mini-phase arrives. That promotion is tracked as WI-315 (so it is not forgotten).

