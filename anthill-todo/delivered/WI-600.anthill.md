## Attributes

- id: WI-600
- created: 2026-06-29T16:21:20Z

- status: Delivered
- status_agent: opus-wi600
- status_at: 2026-07-01T19:13:49Z

- acceptance: cargo-test

## Description

Typer: normalize type-parameter representation to ONE canonical form (the sort's Var::Global param var) so carrier-grounding matches by IDENTITY, retiring the short-name workaround. ROOT CAUSE: the same logical type-param appears in ≥4 term shapes across the three sources carrier-grounding joins — the loader interns a bare param name as a nullary Fn{param} (make_name_term) in a provider spec-view (provides FiniteCollection[Element=Pair[A=K,B=V]]), as a Ref/Ident elsewhere, and as the sort's canonical Var::Global param var — under different Symbol/TermId intern copies, so Symbol/TermId identity matching MISSES. The current workaround keys on SHORT NAME (typaram_ref_short_name collapses Ref/Ident/nullary-Fn/Var→short name; parameterized_short_bindings keys recv bindings by short name; bind_spec_params_from_carrier/_param and the WI-593 substitute_carrier_params compare carrier↔spec params by short name). Short-name matching is non-hygienic (two unrelated sorts' T collide in principle; safe today only because the compare is scoped per spec↔carrier triad) and papers over names that should have been resolved to their canonical symbol in the scan/resolve pipeline. FIX: canonicalize every type-param occurrence to the sort's Var::Global (or a single chosen rep) at load/resolve time (where make_name_term currently emits nullary Fn{param}), then replace the short-name helpers with identity-keyed lookups; delete/inline typaram_ref_short_name. Surfaced by the WI-357/WI-424/WI-593 carrier-grounding cluster. ACCEPTANCE: type-param matching in bind_spec_params_from_carrier(_param)/substitute_carrier_params keys on canonical identity (no short_name compares for params); cargo-test green (incl. wi357/wi424/wi495/wi585/wi593 carrier-grounding tests).

## Changes

### 2026-06-29T16:06:42Z — feedback — user

WI-593 added one more site to subsume: substitute_carrier_params now ALSO rebuilds a reflect.SortView wrapper into a plain Fn{base,named} during grounding (the provider fact stores compound bindings as SortView(Pair,A=K,B=V), not Pair[...]). The canonical-identity normalization should make provider-view compound bindings carry the plain parameterized term directly, removing both the short-name keying AND this SortView->Fn rebuild.

