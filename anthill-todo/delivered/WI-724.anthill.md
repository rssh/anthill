## Attributes

- id: WI-724
- created: 2026-07-15T19:19:54Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-15T19:47:37Z

- acceptance: cargo-test

## Description

Typer/stdlib: eq on Int64 fails impl-coherence in a value context (multiple impls match) — blocks WI-714 where/join over Int64 columns. Diagnosed in the WI-714 where increment: eq(c, 30) with c : Int64 fails to load with: type mismatch in anthill.prelude.PartialEq.eq.dispatch, expected unique impl for per-call bindings, got multiple impls match (coherence rule). Multiple PartialEq[Int64] impls (or overlapping provides via the numeric hierarchy Eq/Ordered/Numeric) match, so eq per-call impl dispatch is ambiguous. Observed for Int64 in the scalar diagnostic; String may be unaffected (unconfirmed — the named-tuple path failed earlier on the r.T binder gap, WI-723). Acceptance: eq(30, 30) and eq(c, 30) (c : Int64) type-check with a unique impl. Determine whether it is a stdlib provides-overlap (fix the numeric eq provisions) or a typer coherence-resolution gap. Rust typer/stdlib.

## Changes

### 2026-07-15T19:47:20Z — feedback — user

RESOLVED BY WI-723 (no new impl needed) — root cause was NOT a stdlib provides-overlap (the ticket's guess), verified empirically in an isolated worktree. There is exactly ONE fact PartialEq[T=Int64]; the numeric hierarchy (Eq/Ordered/Numeric requires ... PartialEq) is resolved by the requires-recursion in resolve_inner, NOT collected as extra candidates. The real cause was an ABSTRACT row-lambda binder: pre-WI-723 c typed as an unresolved projection var, and dispatch_values_match/types_lesseq accepts an abstract per-call value against EVERY concrete PartialEq provider (Int64/String/Bool/BigInt/Set/Map/TotalFloat) -> 'multiple impls match'. WI-723 (concrete-schema binder) makes c:Int64 concrete, so eq's per-call value matches only the Int64 provider -> unique. PROOF: at a73d2297 (WI-723 parent) the exact scenario 'ages.where(lambda c -> eq(c,30))' yields the exact ticket error 'PartialEq.eq.dispatch ... multiple impls match (coherence rule)'; on WI-723 (main) it loads clean. Suite-wide sweep: ZERO multi-candidate eq dispatches across 1180+ tests. Acceptance (eq(30,30) and eq(c,30) c:Int64 type-check with a unique impl) now HOLDS. Guarded by new regression tests include/wi724_eq_int64_coherence_test.rs (3 cases: scalar literal, single-column row binder = exact scenario, valid Int64 column) — the case set the WI-723 suite omitted (it tests only String + the mismatching eq(c.age,'thirty')).

