```anthill
fact WorkItem(id: "WI-201", created: "2026-05-08T08:35:11Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-428", "WI-429", "WI-430"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-16T07:13:13Z"))

fact Tag(workitem: "WI-201", name: "typing")

fact Feedback(workitem: "WI-201", author: "user", at: "2026-06-16T07:12:51Z")

fact Feedback(workitem: "WI-201", author: "user", at: "2026-06-16T07:13:11Z")
```

## description

Abstract type members in spec sorts — bare-spec `Spec.AssociatedType` sugar (e.g. `WorkItemStore.State`). RE-POINTED 2026-06-15 onto the delivered RigidTypeProjection machinery: the path-dependent projection cluster — WI-376/WI-400 (value receiver `s.State`), WI-428 (type-receiver `P.Key` / RigidTypeProjection), WI-429 (loud unresolved-type-name), WI-430 (carrier-precise requires) — already ships the two SOUND receiver forms: a value (`s.State`) and a `requires`-bounded type parameter (`P.State`), per wi428/wi430 tests. This ticket is now SUGAR-ONLY: the one remaining surface form is the BARE spec-sort reference with NO carrier in scope — `operation cmd_add(a: AddArgs, s: WorkItemStore.State) -> Int effects ...` desugaring to the explicit `s: ?S requires WorkItemStore[?S]` (which typechecks today). What makes it sound — and distinguishes it from the carrier-CONFLATION reading WI-428/429 deliberately turned into a loud error — is SINGLE-BINDING-PER-DECLARATION scoping: every occurrence of `Spec.Member` within ONE declaration binds to the SAME fresh existential carrier `?P`; distinct declarations mint distinct `?P` (NOT the global Scala-2 `T#K` nominal). MECHANISM (all prerequisites exist): at classification, a bare `Spec.CarrierMember` in a signature type position lowers to a fresh type-param `?P` + a synthesized `requires Spec[?P]` clause (fill-as-requirement-discharge, WI-383), deduped across the declaration, then rides the existing RigidTypeProjection `P.Member` path. HOOK SITE: anthill-core/src/kb/typing.rs ~11708 — the `ProjResult::Neutral` arm for a bare spec-sort subject currently emits the 'would conflate distinct carriers' error (reproduced 2026-06-15: `operation use_sugar(s: Store.State) -> Int` is rejected there); bare-spec desugaring replaces that arm with the fresh-carrier mint, keeping the loud error only for the genuinely-unsynthesizable cases. DISAMBIGUATION (both deferred in WI-428, in scope here): inside an impl sort that already binds State via `fact WorkItemStore[WIS]`, `WorkItemStore.State` resolves to the bound WIS (the unique-carrier-in-scope desugar); free-standing call sites resolve via caller-supplied arg types (existing spec-op dispatch). Work: (a) loader/typer classification of bare `Spec.Member` in signature type positions; (b) fresh-`?P` + synthesized-`requires` elaboration with per-declaration dedup; (c) tests — `WorkItemStore.State` typechecks identically to `?S requires WorkItemStore[?S]`; two refs in one signature share `?S`; two signatures get distinct `?S`; the carrier-in-scope impl case narrows to the bound type; ambiguous / zero-binding stays loud; (d) rewrite ONE stdlib spec (Eq / Ordered / WorkItemStore) to use the sugar. Out of scope: full VALUE-level path-dependent types (already delivered via `s.State`). Driver: proposal 036 — bundle commands drop the requires-clause noise and read like ordinary code.

## Feedback

### 2026-06-16T07:12:51Z — user

DESIGN DECISION (user-confirmed 2026-06-16): desugar CARRIER-DIRECT (Reading A), NOT the ticket's 'rides RigidTypeProjection P.Member' prose. Empirically verified: Reading B (s: ?P.Member projection) is BROKEN at every call site for WorkItemStore.State — State IS WorkItemStore's sole param/carrier, so an arg typed ?P.State can't infer ?P (non-injective: 'expected a type for P, got unconstrained'). Reading A (s: ?P + synthesized 'requires Spec[Member=?P]', no projection) loads clean, infers ?P at concrete calls, and matches the acceptance's literal 'WorkItemStore.State == ?S requires WorkItemStore[?S]'. wi428/wi383 RigidTypeProjection handles NON-carrier associated members (Storage.Key/Resource.V off a separate carrier C); WorkItemStore.State is the degenerate carrier-IS-member case.

### 2026-06-16T07:13:11Z — user

DELIVERED (all 5 acceptance scenarios + review hardening), commit on main. Impl = loader-side carrier-direct desugar (anthill-core/src/kb/load.rs): BareSpecSugar accumulator armed only during op-signature conversion; try_rigid_type_projection bare-spec arm mints fresh op type-param ?P (kb.fresh_var, no symbol registration — extract_type_params reads vid.name()), deduped per (spec,member)/op, drained into OperationInfo.type_params + a 'Spec[Member=?P]' requires clause rebuilt from each entry. FIRES ONLY for a SPEC (Sort with no constructors, via sort_has_constructors) — a parameterized DATA sort (Option/List/enum) keeps the loud conflation error (code-review fix). Carrier-in-scope NARROWING via order-independent pre-scan of the enclosing sort's provides/fact bindings (scan_sort_carrier_bindings + current_sort_carrier_bindings): inside an impl binding Store[State=WIS], Store.State==WIS (named+positional forms); narrows ONLY to a CONCRETE, UNAMBIGUOUS carrier — a logic-var or conflicting binding falls back to the fresh existential (code-review fixes). Tests: wi201_bare_spec_member_sugar_test.rs (13). wi428 bare_spec_subject_rejected repointed to an entity-field position (op-sig case is now the sugar). Task (d) 'rewrite ONE stdlib spec': NO clean production candidate (Eq/Ordered use in-scope carrier T; the bundle Main sort's SORT-LEVEL shared 'requires WorkItemStore[State]' is cleaner than per-op sugar) — sugar is forward-looking, demonstrated in the fixture not forced as a regression. Sugar typechecks IDENTICALLY to the explicit form including pre-existing op-requires limits (orthogonal WI-383-era, not WI-201 regressions). Full workspace green.

