```anthill
fact WorkItem(id: "WI-320", created: "2026-05-28T14:33:57Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-05-28T18:07:12Z"))

fact Feedback(workitem: "WI-320", author: "user", at: "2026-05-28T15:19:23Z")

fact Feedback(workitem: "WI-320", author: "user", at: "2026-05-28T16:27:15Z")
```

## description

Variant-7 substrate for proposal 045 — Phase 0: add Type::effects_rows variant + EffectsRuntime kind-anchor sort + loader bridge rule + auto-requires inference + 'effects' keyword sugar. Implements the variant-7 foundation 045 adopted 2026-05-28 (see docs/brainstorms/effect-sets.md §'Variant 7' marked ADOPTED 2026-05-28; docs/proposals/045-effect-sets-and-expressions.md §1/§2/§6/Next-steps phase 0). PIECES: (1) Type enum gets a new variant 'effects_rows(effects_expr: EffectExpression)' in stdlib/anthill/prelude/sort.anthill, parallel to existing 'denoted(value: NodeOccurrence)'; EffectExpression (prelude/effect-expression.anthill) stays its own reflect sort and becomes the wrapped payload — the variant is purely the bridge into Type position. (2) New carrier sort 'EffectsRuntime { sort Effects = ? }' in stdlib/anthill/prelude/ (pure kind anchor — no entities, no operations in scope A; the handler-bundle role is a captured follow-on, NOT in scope here). (3) Loader emits the bridge rule once: 'rule type_of(?occ, EffectsRuntime[Effects = effects_rows(?expr)]) :- is_entity_of(?occ, effects_rows(?expr))' — any Type binding to EffectsRuntime.Effects that is not effects_rows(...)-shape fails the requires at binding-site. (4) Loader auto-requires inference pass: walk each operation's 'effects <expr>' clause, collect free variables, emit 'requires EffectsRuntime[Effects = E_i]' per free variable into the operation's requires list. Examples: 'effects E' -> one requires; 'effects merge(E1, E2)' -> two requires; 'effects { E, -Modify[kb] }' -> one requires; 'effects { Modify[c] }' (closed, no free vars) -> none. (5) Grammar: add 'effects' keyword at sort-item position to tree-sitter-anthill/grammar.js, parsing 'effects E', 'effects E = ?', 'effects E = X' — desugars at load time to 'sort E = ? requires EffectsRuntime[Effects = E]'. Disambiguated by position from the existing post-signature 'effects (...)' clause. (6) Migrate existing declarations: Function.E (prelude/function.anthill) and Stream.E (prelude/stream.anthill) currently use 'sort E = ?' (mis-kinded); migrate to 'effects E'. The 'effects Effect' placeholders in prelude/collection.anthill and prelude/iteration.anthill (the Effect kind-marker standing in for a missing row variable) migrate to a real bound variable at the same time. NO ROW CHECKING YET — this is the substrate the row checking will build against. GATES WI-307 (which wires arrow.effects -> row and implements unify_arrow's effects_rows(...) <-> effects_rows(...) match arm — the actual row-rewrite algorithm). CLOSES WI-318 on adoption (the operation effect-var binding question dissolves under auto-requires). The handler-bundle direction (capability-passing dispatch via an EffectsRuntime 'perform' op, collapsing 027/027.1/037 into one witness) is captured in docs/brainstorms/effect-sets.md §'Addendum — EffectsRuntime as the handler-bundle witness' as a strictly-additive follow-on; explicitly OUT OF SCOPE for this WI. WI-301's prior design (add 'entity effect_set(effects: List[Type])' to Type enum) is superseded by variant 7's 'effects_rows(EffectExpression)' shape — WI-301 should be updated / closed once this substrate lands. SCOPE: shared grammar (tree-sitter) + Rust loader + stdlib .anthill files. ACCEPTANCE: (a) Type enum has effects_rows variant; (b) EffectsRuntime sort exists with the documented bridge rule; (c) 'effects E' / 'effects E = ?' / 'effects E = X' parse and convert to the right IR; (d) loader auto-requires inference fires on operation effects clauses (test: an op declared 'effects E' has 'requires EffectsRuntime[E]' in its OperationInfo.requires); (e) Function/Stream/Collection/Iteration migrated; (f) full cargo-test green; (g) typing/checking still works for existing (closed-row) effects without regression.

## Feedback

### 2026-05-28T15:19:23Z — user

Reflect-side substrate landed on branch wi320-variant-7-substrate (commits 2827071 + 4ecef86). Done: (1) Type::effects_rows(EffectExpression) variant in stdlib/anthill/prelude/sort.anthill, parallel to denoted(NodeOccurrence); (2) sort EffectsRuntime { sort Effects = ? } in stdlib/anthill/prelude/effects-runtime.anthill (pure kind anchor — no entities, no operations; the addendum's handler-bundle role stays out of scope); (3) Bridge fact emission from Rust — register_prelude (load.rs) pre-registers EffectsRuntime sort symbol + emits 'EffectsRuntime[Effects = effects_rows(effects_expr = ?expr)]' via assert_rule_debruijn. Bridge has to live in Rust, not stdlib, because surface _type admits only simple_type / parameterized_type / variable_term — an entity-construction term 'effects_rows(?)' in type-arg position does not parse (parse error tested). Full anthill-core test suite green at this checkpoint. Remaining (next session): (4) tree-sitter grammar — 'effects' keyword at sort-item position (effects E / effects E = ? / effects E = X); (5) convert.rs desugaring of the new sort-item rule to 'sort E = ? requires EffectsRuntime[Effects = E]'; (6) auto-requires inference loader pass — walk each operation's effects-clause free variables and auto-emit 'requires EffectsRuntime[Effects = E_i]' into OperationInfo.requires; (7) stdlib migrations (Function.E / Stream.E / collection.anthill / iteration.anthill) — must land WITH the auto-requires inference, not before, or existing 'sort E = ?' ops will fail the new kind constraint. Tasks 5–7 are tightly coupled (a fresh session).

### 2026-05-28T16:27:15Z — user

Substrate-half MERGED to main (2026-05-28, fast-forward c4fb24c..9ed183d). 6 commits landed: substrate ingredients (Type::effects_rows variant, EffectsRuntime kind anchor, bridge fact emitted from Rust), idempotency fix (code-review #1, guard against duplicate emission across the register_prelude-twice pattern in tests/load_all), five typing.rs arms recognizing effects_rows (#2/#3/#4/#5/#11), Type ↔ EffectExpression colocation (#14, eliminating cross-file cycle), fail-loud on missing bootstrap symbols (#9, replaced silent return with .expect()), doc/comment fixes (#6/#7/#10/#12/#13). Full anthill-core suite green throughout (972 tests, 0 failures). Filed WI-321 for the general cross-file mutual-recursion loader-policy question that #14 surfaced. WI-320 STILL CLAIMED — substrate is in, grammar + converter + auto-requires + stdlib migration (Function/Stream/Collection/Iteration to effects keyword) remain. Those four are tightly coupled (the migration breaks until the auto-requires inference fires correctly) and belong in a single follow-on session, ideally on a fresh branch wi320-grammar-and-migration or similar.

