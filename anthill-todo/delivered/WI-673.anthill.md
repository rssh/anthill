```anthill
fact WorkItem(id: "WI-673", created: "2026-07-10T14:48:42Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-07-10T04:39:10Z"))

fact Tag(workitem: "WI-673", name: "carrier-agnostic")

fact Feedback(workitem: "WI-673", author: "user", at: "2026-07-10T04:39:12Z")
```

## description

PERF / carrier-agnostic bucket-1 (WI-661 child, sibling of WI-656/659/660): SortInfo reflect facts are scanned unindexed via kb.rules_by_functor(SortInfo) (fresh Vec<RuleId> per call) at 4 PER-QUERY keyed lookup sites, each of which finds the ONE SortInfo fact whose name field matches a given sort and reads its constructors/operations. Worst case is find_sort_info (typing.rs:24403), called once PER SORT in the type_check_sorts_typed loop -> O(sorts x SortInfo facts) = O(sorts^2). The 4 keyed sites: operations_of_sort (op_requirements.rs:184, name via raw ==), collect_sort_operations (load.rs:4891, name Ref-only via raw ==), sort_operation_names (typing.rs:22786, name via same_symbol), find_sort_info (typing.rs:24403, name via same_symbol; the quadratic per-sort caller). FIX: a build-once SortInfoIndex { by_name: HashMap<String, Vec<RuleId>> } keyed on the SHORT-NAME (last segment) of each fact's name field, mirroring ProvidesIndex.by_carrier. CANONICALIZATION SPLIT is load-bearing: 2 sites compare name via raw == (exact), 2 via same_symbol (bridges bare<->qualified) -- since (a==b) => same_symbol(a,b) => same last segment, the short-name bucket is a valid SUPERSET for BOTH, and each consumer KEEPS ITS OWN EXACT re-filter (raw == or same_symbol) over the candidates (per-site re-filter as in WI-660). Add a sort_info_rids_by_name(kb, sym) fast-path-or-scan helper (Some(ix) -> bucket by short-name of query; None -> live rules_by_functor scan) so the pre-typecheck load-time callers fall back correctly. LEAVE the 4 enumerate-all builders as direct scans (no per-query key -> a keyed index does not serve them; same call WI-660 made for its 4 coherence passes; they belong to the separate rules_by_functor impl-Iterator-variant tail): build_base_substitutions (load.rs:3426), register_induction_axiom_witnesses (load.rs:3944), sorts_and_own_ops (load.rs:4420), sorts_with_constructors (load.rs:4465). LIFECYCLE (simpler than provides): SortInfo is frozen at the end of the file-loading loop (sole producer emit_sort_info/assert_metadata_fact; never retracted/re-asserted by typer/eq_derive/eval), so reset kb.sort_info_index=None at load_phase_inner start (load.rs:3164, next to provides_index reset; covers incremental loads) and build_sort_info_index(kb) at type_check_sorts_typed start (typing.rs:24245, next to build_provides_index). NO eq_derive null/rebuild pair (eq_derive never touches SortInfo) and NO 'constant' runtime guard needed (not read at runtime; and never mutates post-load so a persisted index cannot go stale anyway). Acceptance: full suite green; behaviour identical (canonicalization parity per-site verified); /code-review high. DEFER the SortRequiresInfo scan (only 1 keyed site direct_requires, already memoized in requires_tree_cache -> marginal; the other 2 sites are an enumerate-all mutator + witness pass) to a later bucket-1 child. Files: kb/{typing,mod,load}.rs.

## Feedback

### 2026-07-10T04:39:12Z — user

DELIVERED 2026-07-10. SortInfoIndex { by_name: HashMap<String,Vec<RuleId>> } keyed on the SHORT-NAME of each SortInfo fact's name field (mod.rs field + typing.rs struct/build_sort_info_index/sort_info_rids_by_name helper). 4 keyed consumers migrated to sort_info_rids_by_name (fast-path bucket else live-scan): operations_of_sort, collect_sort_operations, sort_operation_names, find_sort_info (the O(sorts^2) per-sort caller). The 4 enumerate-all builders LEFT as direct scans (WI-660 precedent; belong to the iterator-variant tail). LIFECYCLE: reset=None at load_phase_inner start; build at type_check_sorts_typed start (beside build_provides_index). SortInfo is frozen after the file-loading loop (sole producer emit_sort_info always emits a Term::Ref name; nothing re-asserts), so NO eq_derive null/rebuild pair and NO 'constant' runtime guard needed (not read at runtime, cannot go stale). CANONICALIZATION: short-name bucket is a sound SUPERSET for BOTH the 2 raw-== consumers and the 2 same_symbol consumers (a==b => same_symbol => equal last segment); each consumer KEEPS its own exact re-filter unchanged. /code-review high (8 angles/4 finders + producer check): 0 correctness/removed-behavior/cross-file/lifecycle findings; ONE cleanup applied -- the builder's name-extract helper had a dead Term::Ident arm (no consumer accepts Ident; emit_sort_info only emits Ref) and an over-claiming doc, so inlined a minimal {Ref,Fn} match and deleted the single-use helper (declined reusing sort_ref_functor: it is SortProvidesInfo.sort_ref-specific, coupling would be worse). Full suite 69 bins/2686 passed/0 fail (green before AND after the cleanup). PERF: architectural O(sorts^2)->O(sorts) for find_sort_info; A/B at 115 sorts (github-todo load) within run-to-run noise (~100-200ms type_check_sorts either way) -- the win scales with sort count, sub-noise at this size, which is why acceptance was correctness/parity not a perf number. Files: kb/{typing,mod,load,op_requirements}.rs. Remaining WI-661 bucket-1: SortRequiresInfo scan (DEFERRED -- 1 keyed site direct_requires already memoized, marginal) + rules_by_functor impl-Iterator variant for the surviving enumerators.

