## Attributes

- id: WI-689
- created: 2026-07-11T15:46:38Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-12T08:11:59Z

- acceptance: cargo-test

- depends_on: WI-685

- tags: carrier-neutral

## Description

Unify the sem-eq / groundness gate walks into ONE generic <T: TermView> fold, retiring the per-carrier + per-gate twin split. WI-685 review ALTITUDE finding (3 finders flagged it independently): 8 helpers hand-roll the SAME skeleton -- match head(): ViewHead::Var(Global) chase sigma via subst.resolve_as_value; Var(_) => leaf verdict; ViewHead::Functor => recurse pos (0..pos_arity via pos_arg) + named (named_keys -> named_arg); non-var leaf => default. THE SET: value_deep_ground / term_deep_ground / occurrence_deep_ground (resolve.rs), value_reaches_eq_override / term_reaches_eq_override / occurrence_reaches_eq_override (resolve.rs), value_reaches_partial_carrier / view_fields_reach_partial_carrier (eq_derive.rs). They differ ONLY in (a) leaf default (ground=true vs reaches=false), (b) combine (all vs any), (c) an optional head-level check (reaches_eq_override: eq_dispatch_target(functor); partial_carrier: Const(Float) + field_wise_noneq_carriers membership), (d) whether sigma-Globals chase. COST of the status quo: each gate carries explicit Term/Var/Entity/Tuple/Node arms, so adding a 6th value carrier means editing ~5 gates, and an OMITTED arm silently falls to `_ => true`/`_ => false` -- a wrong verdict, NOT a compile error (a soundness footgun). Since Term/Value/Node/Entity/Tuple all implement TermView (head/pos_arg/named_keys/named_arg), one view-generic fold subsumes them -- the ONE-view analog of the EXISTING two-view generic views_structurally_equal (term_view.rs, recurses over any two TermView carriers) and same_shape_child_pairs (eq_derive.rs). DESIGN: a single fold_view<T: TermView, A>(&self, v, subst, ...) (or a small Visitor trait) parameterized by leaf-default + combine + optional head-check, that all gates reduce to thin predicates over. CONSTRAINTS / SOUNDNESS: (1) preserve EXACT semantics -- the WI-685 adversarial tests (revert-a-gate-and-a-specific-test-breaks: eq(red,blue), set-eq negatives, the op-body bridge tests) MUST stay green; (2) the term carrier reads sigma via self.walk() first (path compression) where occurrence/value reads chase Global via resolve_as_value -- the fold must keep this per-carrier read distinction; (3) the 256 depth cap in *_reaches_eq_override must be preserved; (4) &self, not &mut; (5) a head the view reports but cannot resolve stays a LOUD debug_assert (WI-685 hardening), never a silent skip. ACCEPTANCE: the 8 named helpers collapse to one generic fold + per-gate predicates; a hypothetical new value carrier needs NO new per-gate arm; anthill-core + full workspace suites green (esp. wi616_semantic_eq_test, wi625_*, wi664_composite_eq_test); cargo-test green. Rust-only (resolver + eq_derive). Refs: WI-685 (introduced the occurrence_* twins + this review finding), WI-486 (views_structurally_equal = the two-view fold to mirror), WI-348 (carrier-neutral direction).

## Changes

### 2026-07-11T21:19:39Z — feedback — user

NEW MEMBER of the fold set (from WI-690 inc2): resolve.rs value_has_bodied_op_call is another hand-rolled copy of the TermView recursion skeleton (ViewHead::Functor -> walk pos_arg + named). Differs by: leaf-default=false, combine=any, head-check='functor is a bodied non-builtin op' (builtins.get(f).is_none() && op_body_node(f).is_some()), NO sigma chase (structural over the shallow-walked OTHER operand), and OPAQUE=>true (conservative decline). Fold it into the generic <T: TermView> fold; the head-check + opaque-default are the per-gate params.

### 2026-07-12T08:12:24Z — feedback — user

DELIVERED 2026-07-12 (9a2f1c2b). 9 hand-rolled TermView-recursion helpers collapsed into ONE view-generic KnowledgeBase::fold_gate + fold_gate_children (resolve.rs) driven by a Copy GateSpec { combine: {All,Any}, chase_sigma, head_check: HeadCheck::{AlwaysRecurse,EqOverride,BodiedOpCall,PartialCarrier}, opaque, depth_cap } + 4 const specs (DEEP_GROUND/REACHES_EQ_OVERRIDE/HAS_BODIED_OP_CALL/REACHES_PARTIAL_CARRIER). value_reaches_partial_carrier (eq_derive.rs) routes cross-module to it. Deleted the 5 pure-internal twins (term_/occurrence_ deep_ground, term_/occurrence_ reaches_eq_override, view_fields_reach_partial_carrier); 4 external entries stay thin one-liners. The one-view analog of views_structurally_equal -- a NEW value carrier needs NO per-gate arm, closing the footgun where an omitted per-carrier arm fell silently to a wrong _ => true/false. CRUX preserved (exact-semantics, all suites green): (1) per-carrier sigma-read -- Term path-compresses via walk() BEFORE head-read (the 'Value::Term + Some(s) if chase_sigma' arm), value/occurrence chase Var(Global) via resolve_as_value. (2) 256 depth cap via depth_cap=Some((256,true)); depth++ on child + sigma-chase, NOT on walk/term-entry. (3) opaque is an explicit per-gate field = combine-identity for 3 gates but TRUE for HAS_BODIED_OP_CALL (the one gate where Opaque != identity). (4) var-leaf => false UNIFORMLY (for DEEP_GROUND this is the non-ground verdict, NOT the All-identity true). (5) BodiedOpCall reads RAW Functor{Some(f)} (Ref excluded) vs EqOverride/PartialCarrier use functor_sym() (Ref included). (6) PartialCarrier Stop(contains(f)) for functor_sym==Some so a classified sort does NOT re-descend (O(1)); Recurse only for functor-less aggregates. (7) loud debug_assert on reported-but-unresolvable child now UNIFORM in fold_gate_children (hardens 2 former silent-skip gates). occurs_in_value is a 10th copy DELIBERATELY OUT OF SCOPE (runtime occurs-target vid + unify_flex_var self-ref detection -- doesn't fit &self-free const-spec). /code-review high (3 finder angles incl. removed-behavior auditor): 0 correctness findings; 2 cleanups APPLIED -- fold_gate takes subst: Option<&Substitution> (non-sigma gates pass None, removed dead per-call Substitution allocs incl. a PRE-EXISTING one in value_has_buried_eq_override), dropped dead depth param. Full workspace green: 73 binaries, 2735 passed, 0 failed.

