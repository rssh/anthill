## Attributes

- id: WI-446
- created: 2026-06-12T17:26:14Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-16T22:15:08Z

- acceptance: cargo-test

## Description

Grammar/parser: DANGLING-CASE attachment hazard -- a trailing 'case' arm written after a NESTED match attaches to the INNER match, silently removing the arm the author wrote for the OUTER one (match term_field(..) / case some(st) -> match ... case none -> '' / case none -> '' : the last arm lands on the inner match; the outer loses its none arm entirely). Discovered 2026-06-12 when WI-374's exhaustiveness coverage (sort_functor_of_view at the match scrutinee, replacing the bare-ref-only read that SKIPPED parameterized scrutinees) un-hid three live instances: anthill-todo store.anthill status_short + find_term_by_id and main.anthill collect_feedback_set -- all fixed by reordering the leaf arm before the nested-match arm (commit note in each). Parenthesized inner matches (lambda bodies) are delimited and safe. CANDIDATE FIXES: (a) tree-sitter grammar: make case attachment indentation-aware (external scanner / indent tokens) so a shallower case closes the inner match; (b) loader lint: reject or warn on a case whose indentation is shallower than its match's previous arm (cheap, catches the hazard without grammar surgery); (c) at minimum a corpus test pinning today's attachment so a future grammar change is deliberate. Repro anchor: the reordered sites in store.anthill carry a NOTE comment; wi374_expansion_test::annotated_let_match_exhaustiveness_kept exercises the coverage that detects the symptom.

## Changes

### 2026-06-16T22:15:11Z — feedback — user

DELIVERED 2026-06-17 (uncommitted at write time). Implemented candidates (b) loud detection + (c) corpus pin; rejected (a) indentation-aware grammar as overkill. (b) convert.rs check_dangling_case: hard parse error when a match branch's leading-whitespace indentation is a PROPER PREFIX of the match's first branch (visually shallower => written for an enclosing match but stolen by this nested one). Compared by whitespace-prefix not byte column (tree-sitter column is a byte offset; a tab is 1 byte and would falsely read as shallower than space-indented siblings) — tab/space-mix safe + conservative on incomparable indentation. Single-line nested matches carry no indentation signal => skipped (there the parse matches the visual reading). (c) corpus test 'Dangling case attaches to inner match' pins today's grammar attachment (outer keeps 1 branch; trailing case lands on inner's 2). FOUND+FIXED two REAL latent hazards the guard exposed in docs/proposals/typing_pass_spec.anthill: 'match ar' silently stole 8 outer arms (if_expr..TupleLiteral); 'match operation_body' stole the 'case _' fallback — both fixed by let-binding the nested match (trailing ref terminates its branch list), order+semantics preserved. 5 new parse tests (rejected/let-bound-ok/single-line-ok/mixed-indent-ok + spec parses), corpus 158/158, full rustland suite green x2, codebase scan 0/111 flagged. /code-review xhigh caught the byte-column tab false-positive (fixed). scaland: no change (already braced-only; the converter guard is rust-only).

