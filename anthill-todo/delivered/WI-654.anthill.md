## Attributes

- id: WI-654
- created: 2026-07-07T13:50:31Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-07T13:50:31Z

- acceptance: cargo-test

## Description

Carrier-aware transitive op-requires coverage (DELIVERED). op_requires_covers licensed an abstract spec-op call when the enclosing op's declared requires TRANSITIVELY reached the call's spec, but compared only spec SYMBOLS -- wrongly licensing e.g. a Bar-op over the sibling param of requires Foo[A,B] whose Foo requires Bar[B]. Fix (consumer-side, no storage change): the carrier link IS present in the stored op-requires (held positionally), so thread it through the requires graph -- op_requires_entry_carrier_map + compose_reached_carrier_map + reached_carrier_matches_call -- composing then sigma-aligning the reached carrier against the deferred call's carrier. NOTE: this is the work committed as 'WI-653' on the wi-644-partial-total-eq-ordering branch (commits 6eccc6e1, 28da049e); that branch-local WI-653 label COLLIDED with main's WI-653 (anthill-todo perf regression) at merge time, so re-filed here as a fresh id. Code + commit messages retain the WI-653 label. Unblocks WI-625.

