## Attributes

- id: WI-472
- created: 2026-06-15T10:46:26Z

- status: Delivered
- status_agent: user
- status_at: 2026-07-12T06:15:35Z

- acceptance: cargo-test

- depends_on: WI-471

## Description

Wire the WI-471 NodeOccurrence term cache into fact_dedup so Node/Entity-headed ground facts are de-duplicated (close the Node-head fact-dedup-miss). Today assert_fact_value (kb/mod.rs:1273-1286) stores a Node/Entity head directly and SKIPS fact_dedup ('a dedup-miss, not unsound'), so two structurally-identical Node-headed ground facts both insert. Use occ.cached_term(kb) (WI-471) as the dedup key for a Value::Node head; for a Value::Entity head materialize the whole head via value_to_term (or stash the key TermId in RuleEntry at assert). Extend retract (kb/mod.rs:1398-1408) to remove these Node/Entity-head fact_dedup entries, mirroring the existing Term-head removal (rid-guarded). RECLAMATION of the cache's owned +1: v1 = pin-for-lifetime (retract removes the entry but leaves the cached term alive; freed wholesale at KB teardown; bounded, benign, sharing-safe). Optional upgrade (split into its own ticket if the pin ever matters): a process-wide deferred-release queue keyed by (KbId, TermId) -- Drop enqueues at last-Rc-drop (sharing handled automatically, no unique-ownership gating; node stays KB-decoupled via Copy tags), KB force-drains at op boundaries (terms.release), KnowledgeBase::Drop purges its own orphan KbId entries. NOTE: any owned +1 breaks the WI-246 invariant that body occurrences hold no term-store refcount -- update the retract comment (kb/mod.rs:1417-1418). VERIFY: whether fact HEAD occurrences are ever Rc-shared (decides if reclamation gating is needed -- the deferred queue handles it regardless); Entity-head dedup-key storage. TEST: two structurally-identical Node-headed ground facts dedup to one RuleEntry; assert/retract/re-assert behaves; existing Term-head dedup + value_fact_dedup_keeps_distinct_node_answers stay green.

## Changes

### 2026-06-15T10:43:36Z — feedback — user

Carry-forward from WI-471's /code-review (3 finder angles): (1) DRAIN MUST RELEASE PER-CELL, not per-unique-TermId. Two distinct occurrences with identical structure each take their OWN +1 on the same hash-consed TermId (alloc increments on the existing-term hit), so the term's refcount = number of populated cells. The deferred-release queue must release once per cached CELL entry (matching the per-cell +1) -- NOT once per unique TermId -- else it under-releases (leak) or, combined with any stray caller release, over-releases (free/dangle). (2) cached_term returns a BORROWED TermId (the cache owns the pinned +1); demand sites here (fact_dedup key) must use it WITHOUT releasing -- consistent with the existing non-owning bind_term model. (3) cached_term ALREADY guards the read by KbId (returns the memoized id only when id == kb.id, else re-materializes against the current store), so the queue's per-KB drain can trust the KbId stamp. (4) REUSE opportunity (separate perf, not required for WI-472): the existing occurrence_to_term caller at kb/mod.rs:2603 (rule-body Value::Node binding normalization) re-walks each binding spine per firing and could call cached_term to memoize (same-KB, safe).

### 2026-06-15T10:56:07Z — feedback — user

The deferred-release-queue 'optional upgrade' described above is now EXTRACTED as its own ticket (the just-filed deferred-release queue WI). WI-472 v1 stays pin-for-lifetime (retract removes the fact_dedup entry but does not release the cached +1); adopt the queue ticket later only if the pin's leak matters. Neither ticket gates the other.

