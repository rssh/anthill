## Attributes

- id: WI-436
- created: 2026-06-11T13:32:13Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-18T13:00:38Z

- acceptance: cargo-test

## Description

Kernel decision: IDENTIFY Ref(c) with the nullary application Fn{c} for CONSTRUCTOR symbols at unification/matching -- the runtime twin of WI-391's extractability criterion and the principled end-state behind TWO shipped local fixes: WI-428's normalize_spec_binding_type (type positions) and WI-009's TermPrinter list literals (cons/nil spines). ROOT DISEASE: a nullary Fn prints BARE (indistinguishable from a Ref), so print->parse does not round-trip; the system survives on the CONVENTION that rule patterns use the bare-ref spelling for enum-like constructors (status: Open) -- but the convention is unenforced and the failure mode is the 9fa0da6 class: facts silently invisible to rules. LIVE INSTANCES (probe-verified in review): the store's replace path persists 'context: none, generates: none, params: none' -- bare nullary constructor writes surviving only by convention; any rule matching none() against them silently fails. DIRECTIONS: (a) unifier/discrim bridge Ref(c) ~ Fn{c} when c is a registered constructor (makes the bare print canonical); (b) loader normalizes bare constructor refs in fact/pattern positions to applications (one-time form churn); (c) printer prints all nullary constructors as name() (breaks every bare-ref pattern -- rejected). Decide with WI-391 (same Ref-vs-nullary-Fn divergence, fact-binding side). Also home for the unwrap_list_spine short-name corruption edge (user cons/nil sorts): a symbol-identity check becomes possible once the identification fixes which symbol is THE list cons.

## Changes

### 2026-06-18T13:00:36Z — feedback — claude

DELIVERED 2026-06-18 (5e98878; full workspace 2070 passed / 0 failed; /code-review xhigh, 3 reviewers, 1 finding fixed in-pass, 0 new bugs). Identifies Ref(c) === nullary Fn{c} for CONSTRUCTORS at the VIEW/matching layer (a bridge; single-storage ELIMINATION = WI-511). MECHANISM: TermView::head() canonicalizes a 0-ary registered constructor to ViewHead::Ref(c) across all 5 carrier head-sites (Term::Fn x2, Expr::Apply, Expr::Constructor, Value::Entity) via new functor_view_head, gated on is_constructor_symbol. SOUND + kind-isolated: ops are Value::OpRef (never Term::Ref), sorts/params are not constructors, so the WI-391 Ref=wildcard / Fn=concrete TYPE-dispatch distinction is untouched. THE HARD PART = the meta-world uses 0-ary constructors too (empty_row under EffectExpression, Nothing under TypeExtractor) decomposed STRUCTURALLY via head() -- a naive all-constructors view-flip broke 448 tests. Fix: new ViewHead::functor_sym() reads the symbol off EITHER Ref or Functor, routed through the 4 readers that key on a constructor symbol: resolved_functor_name, explode_incurred_effect_row, value_is_bare_effect_expr, and type_head (restructured to classify by functor SYMBOL first so Nothing is not mis-read as a bare SortRef; Nothing is the ONLY 0-ary TypeExtractor variant). REVIEW FIX (reviewer 1): rules_by_functor indexing (mod.rs assert ~874 + retract ~1414) also routes through functor_sym so a nullary-constructor FACT stays indexed under its symbol, mirroring the discrim tree. VERIFIED SAFE (reviewer 3): codegen never touches head(); printer reads raw Terms (both forms print bare, WI-009 round-trip holds); reify is IMPROVED (a reified Fn{c} now views as Ref(c)); the pre-existing Ref convention (resolve.rs:2250 nullary none = Expr::Ref) confirms Ref is the intended canonical form. The 21-site Functor-head sweep confirmed only these readers see a 0-ary constructor; the rest key on sorts/ops/non-nullary functors (EffectsRuntime is a SORT, List.cons/EffectsRows are non-nullary). TESTS: term_view::wi436_tests (cross-carrier view identity across Term/Entity/occurrence + non-constructor Functor control) + discrim::wi436 (cross-form Fn<->Ref discrim match, both directions). KNOWN BOUNDARY (= WI-511): bridges at the view; storage keeps dual TermIds and the Value::Entity vs Value::Term(Ref) carrier, so Value::structural_eq (backing eq/neq + SetLiteral dedup) stays carrier-blind for 0-ary constructors (pre-existing, latent, unexercised). USER STEER: prefer ELIMINATION over bridging (one representation = less complexity = less risk); WI-511 carries the concrete storage+comparison elimination plan.

