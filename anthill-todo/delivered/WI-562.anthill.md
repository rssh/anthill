```anthill
fact WorkItem(id: "WI-562", created: "2026-06-25T05:05:24Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-25T05:05:13Z"))

fact Tag(workitem: "WI-562", name: "prop025_1")

fact Feedback(workitem: "WI-562", author: "user", at: "2026-06-24T23:36:44Z")

fact Feedback(workitem: "WI-562", author: "user", at: "2026-06-25T04:15:39Z")

fact Feedback(workitem: "WI-562", author: "user", at: "2026-06-25T04:48:13Z")
```

## description

Fix IndexedSeq.nth dispatch regression: spec-op dispatch via a carrier wrongly imposed the carrier's UNRELATED self-requires. `List requires Eq[T]` (added 2026-05-29 by WI-325 so the abstract `member` body's `eq(head,x)` type-checks) interacted with the WI-224 dispatch sub-goal check (candidate_sub_goals_owned recurses the impl sort's full requires chain), so dispatching ANY spec op through List demanded Eq on the element. `nth(pts, i)` on a `List[Waypoint]` (Waypoint not Eq) in examples/webots-modelling/lf1/leader.anthill failed with 'IndexedSeq.nth.dispatch: no impl matches', aborting the whole lf1 load and breaking discharge.sh. A dispatch-layer fix (filter the carrier's self-requires to those the dispatched spec itself requires) is WRONG: it can't distinguish an incidental self-requires (List's Eq, for member) from a conditional-provider's requires (EqList: `requires Eq[A]` guarding `fact Eq[List[A]]`) — structurally identical. Correct fix: (1) stdlib — move Eq from the List sort to the `member` op as an op-scoped requires over an op-type-param (`member[E](x: E, l: List[T=E]) -> Bool requires Eq[E]`, WI-448), so List[NonEq] is well-formed for element-agnostic ops (nth/length/append/IndexedSeq/Stream/Iterable/PersistentCollection) and member's Eq is checked at its call site via E inference; (2) typer — kb/typing.rs NoCandidates arm now lets an operation's OWN op-scoped requires cover its body's abstract spec-op calls (op-scoped dual of the sort-level defer-to-requirement). Tests: wi562_indexed_seq_nth_dispatch_test (nth on List[NonEq] dispatches; sort-param no-requires still rejected). NOT CAUGHT because no cargo test type-checks the lf1 example end-to-end (only discharge.sh, a shell script not in cargo test) and lf1_real_spec_test.rs discards load errors (let _ = load_all) — it only emits SMT for specific safety rules, never type-checking leader.anthill's operation bodies.

## Feedback

### 2026-06-24T23:36:44Z — user

FINAL APPROACH CORRECTION (supersedes the "dispatch-layer fix is WRONG" line in the description): the fix IS a dispatch-layer filter in candidate_sub_goals_owned, made sound by a keep-condition refinement I initially missed. Keep a carrier self-requires entry iff its spec is RELEVANT to the dispatched spec: (a) it equals the dispatched spec_sort — the conditional self-provision case (EqList provides Eq[List[A]] requires Eq[A]: a list is Eq iff its elements are, so Eq[A] IS the provision condition and must be checked), OR (b) it is in the dispatched spec's transitive requires_chain (a genuine dependency, e.g. Ordered requires Eq). An UNRELATED self-requires (List's Eq, for member, while dispatching IndexedSeq) is dropped. The naive filter (keep only requires_chain(spec_sort)) was wrong because requires_chain(Eq) is empty and dropped EqList's same-spec Eq; the "== spec_sort" clause fixes that. NO stdlib change needed (List keeps its sort-level requires Eq[T]). An earlier op-scoped-member detour (member[E] requires Eq[E] + a typer op-requires-coverage guard) was REVERTED because it broke WI-420 eta-dict capture (which keys on sort-requires) and was far more invasive. Final diff: ~25 lines in candidate_sub_goals_owned (add spec_sort param + relevant-set filter) plus tests. Tests: wi562_indexed_seq_nth_dispatch_test (nth on List[NonEq] dispatches; conditional Eq-provider still requires element Eq).

### 2026-06-25T04:15:39Z — user

FINAL APPROACH (supersedes BOTH earlier notes — the dispatch-filter idea AND the op-type-param-member idea). Two dead ends ruled it out: (1) a dispatch-layer filter that drops a carrier's "unrelated" self-requires is IMPOSSIBLE — a carrier's genuine provision requirement (CarrierB requires DiamondA, providing DiamondB) is structurally identical to an incidental one (List requires Eq, for member), and wi224 diamond-coherence pins that a carrier's sort-requires ARE threaded at every dispatch; (2) moving member's element to an OP-TYPE-PARAM (member[E] requires Eq[E]) breaks eta-expansion (a bare `member` value typed as Bool, not the arrow) and the WI-420 eta-dict path.

WHAT SHIPPED: keep member on the SORT param T but move its Eq need from the LIST SORT to an OP-SCOPED requires (WI-448): `operation member(x: T, l: List) -> Bool requires Eq[T]`. List drops its sort-level `requires Eq[T]`. Typer (kb/typing.rs): TypingEnv gains an `op_requires` snapshot (set per op-body in check_operation_bodies from op_requires_entries); check_apply, BEFORE provider dispatch, licenses a spec-op call whose spec the enclosing op's own op-requires covers (leaving it as the spec op for value-directed eval). NO change to candidate_sub_goals_owned (diamond coherence intact). NO change to attach_eta_dispatch_dict needed — member still eta-expands as a sort-param op, and its `eq` value-directs at eval (member is only ever evaluated on concrete lists), so no requirement dict is threaded. Diff: ~30 lines in typing.rs + the 1-line stdlib member requires + tests. Tests: wi562 (nth on List[NonEq] dispatches; op-scoped requires licenses abstract eq in a body; no-requires still rejected). Full anthill-core suite green; member eval (wi431, eval_test) + eta (wi420) + diamond (wi224) all pass.

### 2026-06-25T04:48:13Z — user

PLACEMENT REFINEMENT (post code-review): the op-requires coverage guard lives in the FAILED dispatch arms (DispatchOutcome::NoMatch and the abstract NoCandidates arm), NOT pre-dispatch. Helper op_requires_covers(kb, env.op_requires(), spec_sort). Rationale (code-review finding): a pre-dispatch guard also short-circuited CONCRETE calls of an op-required spec, bypassing the WI-365 impl effect-grounding (an effectful override would surface as ?_ instead of the named effect). Placing it in the failure arms means only a genuinely-abstract call (member's eq on the abstract element, which fails dispatch — NoCandidates in plain scope, NoMatch when a parameterized provider like fact Eq[List[A]] false-matches) is licensed; a concrete call resolves Unique and keeps effect-grounding + precise return. wi365 + full anthill-core suite green.

