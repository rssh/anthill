## Attributes

- id: WI-408
- created: 2026-06-06T13:17:26Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-12T10:41:54Z

- acceptance: cargo-test

- depends_on: WI-385

- tags: typing

## Description

Typer/eval: some-coercion INSERTION pass — when a value of type T flows into an Option[T] slot (field or arg), INSERT some(...) so the value is properly Option-typed (sound; lets normalize_deps drop its reflect-launder). Classify-during-typing + rewrite-pass (à la WI-231 req_insertion) for op-body constructors; loader path for on-disk facts (asserted before the typing pass); migrate the on-disk fact format to explicit some(...)/none() (user decision 2026-06-06). REPLACES WI-385's bare-T-vs-Option[T] FIELD lenient-accept INTERIM. The first concrete slice of the deferred implicit-conversion / 'into' framework (general framework still deferred). Acceptance: cargo-test green; normalize_deps launder removed; anthill-todo fact round-trip uses some()/none().

## Changes

### 2026-06-12T10:41:52Z — feedback — claude

DELIVERED 2026-06-12 (cb82116 + review-fix commit, pushed; suite full-workspace green). THREE LEGS. (1) TYPER (op bodies): validate_arg_against_param -> tri-state ArgValidation (Ok / WrapSome{declared} / Fail); check_apply_iter (ARGS — previously strict) + check_constructor_iter (fields) record WrapSome during the WI-385 validation and rebuild the apply/constructor via wrap_some_children + synthesize_some_wrap — a synthesized NAMED some(value: child) Constructor occurrence (canonical shape), stamped with the resolved declared Option type; the rebuild runs BEFORE any annotation write (classification / resolved_type_args land on the fresh node — reassemble starts empty cells). Tree reassembly in ALL Build arms is now UNCONDITIONAL (reassemble's ChildCursor ptr-eq short-circuit keeps the no-rewrite case allocation-free); simp_enabled gates only rule FIRING — required so a typer-inserted rewrite reaches the stored body via set_op_body_node in KBs with no [simp] rules (in real stdlib loads simp was always on, so no practical perf change). (2) LOADER (term world): wrap_bare_option_value in convert_term_with_expected's entity-arg loops wraps a bare value in an Option-typed field via build_some — on-disk FACTS and rule-body entity ATOMS, so bare rule patterns (depends_on: cons(...)) auto-wrap and keep matching wrapped facts; a Var binds the WHOLE Option (never wrapped); some/none heads skipped. Validation order: types_compatible FIRST, then Option-peel — so Option[T] into Option[Option[T]] takes exactly ONE outer wrap, and a doubly-bare value under nested Option is rejected loudly (no guessed double-wrap). (3) CANON + MIGRATION: canonical in-KB some form = NAMED some(value: v) (build_some + value->term reflection); source-written positional some(v) is CANONICALIZED at load (term-world unification does not bridge positional<->named — a positional wrap never matched the CLI-persisted named facts); an explicit some(payload) threads the PEELED element hint into its payload (without this a persisted depends_on: some([...]) literal lost the List hint and never desugared — surfaced as 'pattern match failed on Term(ListLiteral)' in cmd_tag_test). anthill-todo: normalize_deps reflect-launder REMOVED (acceptance); description readers (print_description_w / row_of_w) peel some/none; workflow rules bind description: some(?desc) (absent-description facts still match via the loader's fresh-var stuffing); unused as_term/term_as_entity imports dropped. Fact ROUND-TRIP now writes explicit some(value: ...)/none (acceptance; verified live — WI-445's own fact + probe project); legacy bare data keeps loading via the wrap, so workitems.anthill migrates INCREMENTALLY per touched fact (no bulk rewrite — avoids the WI-436 TermPrinter round-trip bug class on 3400 lines). Spec: kernel-language.md gained a some-coercion paragraph after the Option sort (sec 4.2 area). TESTS: wi408_some_coercion_test (5: field+arg eval coercion proving runtime some-shape, Option[T=Term] bare string, nested-Option rejection + single-wrap acceptance, fact-wrap + explicit/bare rule-pattern alignment + none preservation); wi385 test comments de-interim'd; cmd_add/tag/update assert the explicit persisted format; golden parity green. BOUNDARIES (recorded, not blocking): (a) coercion gated on GROUND declared+actual (same gate as WI-385 validation) — a still-polymorphic Option slot stays unwrapped; (b) RETURN-position coercion not in scope ('field or arg') — op declared -> Option[T] with bare-T body still mismatches; (c) general entity positional<->named canonicalization NOT done (only some) — hand-written positional patterns vs named persisted facts (ToolPasses("x") vs ToolPasses(tool: ...)) remain un-unifiable in term world, a PRE-EXISTING silent-mismatch class worth its own ticket if it bites; (d) op-body match PATTERNS in Option slots are not coerced (patterns aren't typed) — a bare sub-pattern there needs explicit some(...); (e) eval value world records the supplied constructor-arg shape, hence the named wrapper prints some(value: ...) on persist. FILED: WI-445 — pre-existing typer gap found by probe (named constructor sub-patterns don't bind: 'case Box(v: some(x))' UnresolvedName while positional binds; plus TermId-keyed span side-table misattributes diagnostics to another file's span).

