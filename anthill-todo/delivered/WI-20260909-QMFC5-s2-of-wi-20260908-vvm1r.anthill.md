## Attributes

- id: WI-20260909-QMFC5-s2-of-wi-20260908-vvm1r
- created: 2026-09-09T05:59:29Z

- status: Delivered
- status_agent: user
- status_at: 2026-09-09T10:41:42Z

- acceptance: cargo-test, scaland-sbt-test

- depends_on: WI-20260909-51W18-s1-of-wi-20260908-vvm1r

- tags: vvm1r

## Description

S2 of WI-20260908-VVM1R (umbrella): THE TYPED-HEAD ANCHOR itself -- proposal 060 sec 3's second anchor, bind tier, PLUS the check tier resolved at compile time (folded in: it is a load-time verdict riding on steps 1-2, smaller than its own ticket). Design, with every measurement: docs/design/060-implementation.md sec 8.1-8.8. Read it; this restates only what scopes the work.

WHAT IS REFUSED TODAY (measured 2026-09-09): rule anchored(?x: Leaf, ?d) :- ?d = require[Desc[T = Leaf]], seed(?x) reports 'expected a body call to one of Desc's operations ... got no such call in the rule body' -- the SAME message its untyped twin reports, so the annotation buys nothing. Same for the introducer spelling with a bounding guard (rule anchored[A](?x: A, ?d) :- Desc[A], ?d = require[Desc[T = A]], seed(?x)), and same for the check tier requires(Desc[T = Leaf]).

THE ATTRIBUTION IS NOT MISSING -- this corrects the umbrella ticket's own text, which says a carrier-to-parameter map 'does not come from an op' and must be invented. spec_carrier_param_or_sole (typing.rs:34990, WI-1102 over WI-1076) answers it op-INDEPENDENTLY on two rungs (the param some declared operation receives on, first in the sort's declared order; else a SOLE param gated on not-self-representing), memoized, measured over stdlib + host bindings at its own site. It is a DIFFERENT question from param_is_spec_carrier (WI-596), which classifies a call's ARGUMENTS and answers a SET. A NEW CONSUMER OWES THE SECOND GATE the predicate's own doc demands ('it answers which parameter an operation takes, and that is not by itself which parameter names the carrier' -- measured there: touch(c: Spec, x: P) answers P): here the gate is agreement with the bound's own provision row, used as a CHECK and never as the producer, which would be circular.

THE BOUND IS TWO DIFFERENT THINGS, and that is the discriminator, not a complication. CONCRETE (?x: Leaf) records the CARRIER -- test sort_provides(bound, spec); this is also the delivered C666A typeclass-method shape. INTRODUCER (rule p[A](?x: A) :- Desc[A]) records THE SPEC, because rule_head_bound_alias substitutes A -> Desc before make_sort_ref (load.rs:20262-20268) -- test bound == spec. Two non-overlapping tests, one channel (rule_type_bounds).

THE BOUND IS NOT THE CARRIER VALUE. bare_sort_compatible admits the sort, its sort_sym_compatible relatives, AND any sort that PROVIDES it, so the bound is an UPPER bound: it picks WHICH head variable anchors the spec and never stands in for the carrier. Measured per shape: a SPEC bound differs from the carrier by design (row k: bound Desc, carrier Leaf -- the clause is polymorphic over Desc providers); a DATA-sort bound is structurally closed (a provides onto a constructor-declaring sort is refused loudly: 'nothing is-a a data sort'); a bare PARAMETERIZED bound admits every instantiation. Hence step 4 below reads typeof(?x) at RUN TIME, exactly as the witness path does.

THE TRANSFORM, at record_find_dictionary_grounding, for a clause with non-empty rule_type_bounds whose witness scans all miss. (1) p = spec_carrier_param_or_sole + the gate above; None + self-representing => no param to pin (witness_sort_goal's self-representing branch already builds GoalCarrier from the value's type and args); None otherwise => located refusal. (2) Pick the anchoring head variable by the two tests; ZERO => a located refusal naming sort, spec and the missing provides -- REQUIRED, because today the non-providing case is refused by the GROUNDING message, so the acceptance control does not discriminate until this exists; MORE THAN ONE => refuse for now, lifted by S4. (3) Emit find_dictionary(spec_base, <carrier>, ?x, out: ?d), the carrier slot replacing op_functor. (4) Run time reads typeof(?x) via the same value_type_term the witness path uses, pins p, and X9PB4's wildcard loop fills the rest; simp_guard_holds_core collapses to a direct sort_provides -- the op-keyed consumer is BYPASSED, not generalized.

A COMPILED PROJECTION, NOT A COMPILE-TIME DICTIONARY (channel doc sec 2.1). Splicing a constant is sound only when ALL THREE hold -- the bound is a data sort, fully applied, and its provision unconditional -- which is narrow enough to be recorded as a boundary rather than built; taking it would also have to answer WI-860, whose agreement check lives in read_dictionary_into and which a spliced constant never reaches. NOT monomorphization either: conditional provisions make the family unbounded (Pair provides Eq[Pair] :- Eq[A], Eq[B]) and it changes the clause population the discrim tree indexes.

CHECK TIER, FOLDED IN: requires(X) under a typed-head anchor is resolved at LOAD, not lowered to a runtime goal; the witness-grounded requires(X) is untouched. Sound in both shapes because no runtime question remains -- a data-sort bound cannot be widened so the carrier IS the bound and sort_provides decides at load; a spec bound has the annotation's own generated domain(?x, Desc) goal, which IS the provides-check. Holds => the goal is inert; fails => the same located refusal step 2 needs. This keeps requires(X) and require[X] ONE relation (WI-1040's 'one form, one owner': requires(X) IS the no-out case) rather than admitting one spelling and refusing its twin.

ACCEPTANCE. Both anchored surfaces LOAD and THREAD -- the covered call dispatches through the dictionary the annotation grounded, asserted BY VALUE. THE FIXTURE IS THE SHARPEST DECISION HERE and it must be built deliberately: measured, a bodied Desc.describe call is SIMULTANEOUSLY the witness and the covered call, so the obvious fixture passes entirely on the existing witness path and never exercises the anchor. Use a spec op that is COVERED but cannot be a WITNESS (op_has_spec_carrier_param false -- a nullary / all-content op). NOTE the umbrella ticket's acceptance spells the surface p(?x: T, ?y) :- ?d = require[Eq[T]], f(?x, ?y, ?d): THAT CLAUSE DOES NOT LOAD, and for an unrelated reason (WI-582: rule type-variable T has no bounding guard) -- use the introducer form with a written bounding guard.

CONTROLS, each stated at its test site with the rows it moves: the UNTYPED twin stays refused (what says the ANNOTATION grounds it; passes either way under the WI-742 half alone); a bound that does NOT provide is refused with its OWN located message, not the grounding one; a clause with BOTH a typed head and a covered witness call keeps answering exactly as today (measured: 7) -- the witness path is not displaced. Say which rows fail when the anchor recognition is backed out, which when the check-tier arm is, and which pass either way by design. cargo-test green via scripts/test.sh.

## Changes

### 2026-09-09T10:20:00Z — feedback — claude

DELIVERED. The anchor works in BOTH tiers, asserted by value; 39 rows across the two files (18 qmfc5 + 21 51w18 after S1's grew). What follows is what MOVED from the ticket's own plan, and every count is written from a run.

WHAT THE TICKET SAID TO BUILD, AND DID GET BUILT: spec_carrier_param_or_sole answers the attribution op-independently; the two bound shapes discriminate by the two tests (sort_provides for a CONCRETE bound, bound == spec for the INTRODUCER); the goal is find_dictionary(<written instance>, <spec base>, ?x[, out: ?d]) with the spec base in slot 1 as the form discriminant; simp_guard_holds_core is BYPASSED, not generalized; the acceptance fixture uses Desc.tag(), nullary and op_has_spec_carrier_param false, so it is COVERED and can never be a WITNESS -- the_control_a_nullary_call_folds_the_spec_default measures it answering 1 with no require at all.

THE CHECK TIER IS NOT RESOLVED AT LOAD. This ticket specified "resolved at LOAD, not lowered to a runtime goal", and I built that. It is wrong, and /code-review drove three counterexamples, each a program where the load-time verdict said SATISFIED and the identical clause one spelling apart left a residual: a CONDITIONAL provision (Wrap provides Sh[T = Wrap] :- Sh[A], carrier wrap(bad()) -- check 1 definite, bind residual); SORT_REFINES (sort Sub { requires Leaf } providing nothing -- check 1 with one solution, bind no solutions); and a WITNESS-SUPPLIED provision (sort Rival provides Desc[T = Leaf] -- refused at load outright). The ticket's soundness argument -- "nothing widens to a data sort so the carrier IS the bound" -- is TRUE about the sort HEAD and the conclusion does not follow: sort_provides is base-level and type-argument-blind, so Wrap[A = Good] and Wrap[A = Bad] are both Wrap and provide differently. Each hole has a different single-site repair and every one leaves the other two open, which is the tell that the SHAPE was wrong rather than the predicate. It now emits the same goal the bind tier does, minus out:. Back-out: 4 rows.

AND THAT ARM COULD EMPTY A RULE BODY. rule fe: f(?x: Leaf) <=> 1 :- requires(Desc[T = Leaf]) [simp] has that goal as its ONLY body atom, so dropping it handed set_rule_body_nodes an empty body and tripped its fact-ness assert; with debug_assertions off is_equation flips false->true and a GUARDED REWRITE BECOMES AN UNCONDITIONAL LAW. Every anchored fixture in the file was relational, and a relational typed head gets a prepended domain(?x, Leaf) goal that keeps the body non-empty -- so the suite shipped green over it. equation_headed_anchor_keeps_its_body is now that row.

THE >1 COLLAPSE WAS BUILT AND THEN REMOVED. A first review round found ?x: Leaf, ?y: Leaf refused as "two dictionaries" and I collapsed two anchors of one DATA sort, on the argument that nothing widens to a constructor-declaring sort so both carriers select the same row. Three counterexamples: a PARAMETERIZED data sort collapses on the head symbol alone and its conditional sub-dictionary differs -- and that shape SHIPS (pair, list, option); two BARE byte-identical Wrap bounds diverge under a conditional provision, 7 one way and a residual the other, decided by WHICH HEAD VARIABLE WAS WRITTEN FIRST (so comparing full bound TERMS does not rescue it either -- the predicate needed is "the carriers are equal AT RUN TIME"); and the gate itself read kind_of and was source-order dependent. The refusal stands and that spelling is refused with it -- a real cost, taken because a refusal is loud and names 96ZTM where the collapse silently threaded one carrier's dictionary into the other's call. Back-out: 2 rows.

THE SECOND GATE EXISTS NOW, and my recorded reason for omitting it was wrong. I had written "no independent second gate on spec_carrier_param_or_sole -- the shape its doc warns about takes the self-representing branch". It does not: sort Sp { sort C; sort P; operation touch(x: P); operation tag() } with Leaf provides Sp[C = Leaf, P = Int64] is NOT self-representing, answers P (the RECEIVED param) while the provisions carry the carrier in C, and loaded clean and answered a residual -- the silent non-grounding this ticket's acceptance forbids in as many words. The gate is agreement with the bound's OWN PROVISION ROW, used as a CHECK and never as the producer, exactly as the ticket specified. It is gated on !spec_is_self_representing to MIRROR anchor_sort_goal's branch order; asking unconditionally refused a shape the emitter handles by the carrier discriminant. Back-out: 1 row.

THE WRITTEN BRACKET NOW HAS A READER. 060-implementation sec 8.6 said a written binding disagreeing with a derived concrete type "is a new refusal ... nothing decides that today because nothing can". Something can: anchor_grounding holds both. require[Desc[T = Other]] under ?x: Leaf loaded clean and answered 7 -- Leaf's dictionary, ignoring the author. Located refusal now, with the agreeing spelling as its control. Back-out: 1 row.

S1 REGRESSED THREE SURFACES AND I MISSED IT ON A GREEN-SUITE ARGUMENT. I deleted the term-side lowering because "the full binary passes with it disabled". Constraint bodies, operation ensures bodies and aggregation-constraint conditions are stored as TERMS, not occurrences; the corpus has zero BRACKETED requires( outside a rule body, so the binary could not have measured it. constraint c :- requires(Eq[T]) -- the canonical stdlib typeclass spelling -- became "unresolved name 'T'". The term walk has the arm now; five surfaces are driven, with the bare spelling as the control. Back-out: 1 row.

THE LOADER PAIRING HAD THREE MORE FACES. (a) check_sort_type_args was fed the SURVIVING arguments, so the drop rule it was added to police made over-application invisible -- Desc[Leaf, Leaf] refused and Desc[T, Leaf] loaded, two verdicts for one mistake. It counts what the AUTHOR WROTE now. (b) The NAMED loop dropped silently and without recording its claim, so Desc[T = Zork, Leaf] re-indexed Leaf onto T -- the same defect S1 already fixed for positionals, arriving from the other side. (c) The drop rule was STATED for names and APPLIED to every carrier, so a literal, an entity constructor, a rule name, a logical variable in type position and a tuple type all vanished with no diagnostic. It is now stated about NAMES: a spec's OWN declared parameter is the wildcard, anything else is reported. A corpus census says that costs nothing -- every free-name binding that ships spells the spec's own parameter (Eq[T] x24, Desc[T] x23, PartialEq[T] x15, ...). Back-outs: 1, 1, 2 rows.

PRE-EXISTING, FIXED INLINE BECAUSE THE ARGUMENTS DEPEND ON IT: sort_has_constructors read kind_of, the FIRST-DECLARED category, so a sort whose entity red is preceded by an operation red() stopped being a DATA sort -- which flips ProvidesNamesDataSort, the "nothing is-a a data sort" refusal that several typer soundness arguments cite. has_kind now. Symbol categories are a SET; this is the same desync WI-Q0093 found once already.

TESTS THAT MEASURED NOTHING, FIXED: both_spellings_of_one_slot_key_alike compared local_name_of, which renders BOTH keys as "T" -- it passed with its own fix backed out. It reads the QUALIFIED name now and goes red under back-out. a_head_introduced_type_variable_resolves_inside_the_bracket said "loads and THREADS" and asserted only a stored SHAPE; it asserts 7 by value now. The nested-base ladder had no driving row (the corpus's only nested fixture has an ordinary sort as its nested head, which remap_symbol_strict resolves too); a_nested_base_resolves_through_the_ladder_too uses a head-introduced A and is the one row that back-out reddens. And the file header's back-out counts were written from a prediction ("8 of 11" when the file held 14 and 11 failed) -- every count in it is now from a run.

MEASURED BUT NOT CHANGED, each recorded at its site: (i) the dotted-base-naming-no-sort case was predicted to report "a body call to one of field_access's operations" -- it does NOT, it reports unresolved name 'a.b.Zork' correctly; (ii) require[A] (a bare introducer at top level) is refused where require[Desc[T = A]] works -- routing (false, Some) to Ref(sym) would fix the spelling and change the bare-name path for EVERY require[Spec] in the corpus, so it is a boundary with its measurement rather than a widening on a guess; (iii) a POSITIONAL effect row does not PARSE, so the asymmetry predicted for it does not exist at the surface -- the arm added to the positional loop is a backstop and a_positional_effect_row_does_not_parse_at_all says so; (iv) collect_covered_calls is CARRIER-BLIND, an open axis the >1 refusal cannot see because it counts BOUNDS; nobody could drive it to a wrong value, so it is recorded, not claimed fixed. 96ZTM owns it.

TWO BOUNDARIES STAND: a self-representing spec whose provider pins a sibling CONCRETELY still delays -- the written bracket would decide it and S1 retains it, but fetch_dictionary takes (spec_sort, op_functor, arg_vals) and never sees slot 0, so closing it is a resolver signature change; and TWO anchors are refused rather than threaded (96ZTM).

CORRECTION TO S1's OWN NOTE: it says the duplicate-spec-base refusal "names QMFC5" as its reader. QMFC5 performs no such lift -- two requires on one spec are two DICTIONARIES and need the same carrier-directed weave the >1 anchor refusal waits on. Both the comment and the diagnostic string now name WI-20260909-96ZTM; the string also still said "Tier B", a delivered ticket's tier.

NOT COMMITTED -- left in the working tree for review.
