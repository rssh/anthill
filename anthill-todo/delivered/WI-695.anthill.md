## Attributes

- id: WI-695
- created: 2026-07-12T09:31:21Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-12T09:31:21Z

- acceptance: cargo-test

- tags: carrier-neutral

## Description

typing.rs type-annotation carrier preservation (WI-348; reify-audit follow-up). extend_env_from_pattern (typing.rs:16946) and the Expr::Lambda ann_type (3717) ground a binder's :Type annotation OCCURRENCE via Value::term(occurrence_to_term(kb, ann)) -- a reify-then-rewrap that CONTRADICTS the adjacent WI-342 comment ('the env binds a carrier-agnostic Value, so a Value::Node component type is preserved, not re-grounded'). Carry the occurrence instead: Value::Node(Rc::clone(ann)). Safe because (a) extract_pattern_type_ann returns &Rc<NodeOccurrence>; (b) env.bind_var stores a Value and lookups unify carrier-neutrally; (c) the priority scrutinee_type branch already feeds Value::Node into the SAME ty slot; (d) make_arrow_value->value_to_type_child (typing.rs:1458) has a Value::Node=>TypeChild::Node arm. Strictly more faithful for a denoted-bearing annotation (drops reliance on WI-390 lossless lowering). 16946 is a live WI-517 typed-lambda-binder path; 3717 is normally-None (grammar's pattern_var surfaces no type_ann today). Acceptance: full cargo-test green (esp. WI-517 typed-lambda-binder + WI-342 carrier tests). NOTE: sibling audit item typing.rs:19208 (guard-list Node arm via value_list_to_vec) is a SEPARATE ticket.

## Changes

### 2026-07-12T09:31:21Z — feedback — user

NEGATIVE RESULT — NOT removable, NO code change (reverted). Reclassified GENUINE BOUNDARY. Carrying the binder :Type annotation as Value::Node(Rc::clone(ann)) instead of Value::term(occurrence_to_term(kb,ann)) FAILED 3/4 wi517_typed_lambda_binder tests (single_typed_binder_pins_param_without_expected_context, typed_tuple_binders_pin_elements_without_expected_context, body_contradicting_annotation_is_rejected; the 4th passes because it uses the scrutinee_type priority path, not the annotation fallback). ROOT CAUSE: occurrence_to_term for a type-annotation occurrence is NOT a carrier round-trip -- try_occurrence_to_term routes a type-kind occ via occ.as_type() -> type_node_to_term, i.e. it INTERPRETS the Expr occurrence AS a type and RESOLVES it (Int64 name -> resolved type term). env.bind_var then stores the resolved type, which body dispatch (e.g.  -> Numeric.add) needs to see Int64. A raw Value::Node keeps the un-interpreted Expr occurrence, so the param type is unresolved and  is dispatch-ambiguous. The audit's Cluster-B 'REMOVABLE' verdict traced the consumers as carrier-neutral but missed that the reify does type-resolution; live-test verification refuted it. A true carrier-neutral form would need the type/dispatch machinery to resolve a raw type-annotation occurrence on demand (read a Node annotation as a resolved type at dispatch) -- a much larger effort, not this ticket. LESSON: independently RUN the live tests, do not trust an audit agent's 'removable' verdict for reify sites that may hide resolution/interpretation.

