## Attributes

- id: WI-20260921-R10KC-decide-whether-a-requirement
- created: 2026-09-21T15:25:33Z

- status: Delivered
- status_agent: claude
- status_at: 2026-09-21T21:34:13Z

- acceptance: cargo-test

## Description

A SPEC DEFAULT BODY MUST RECEIVE ITS PROVISION'S DICTIONARY, and must resolve a body-less sibling member by PROJECTION out of it rather than by value-directed dispatch. Today it receives nothing, falls back to the value, and then tries to RECONSTRUCT the requirement from the argument data - which cannot work for a slot that leaves no trace in the data.

THE MEASURED PROGRAM (~70 lines, scratchpad `valuedict.anthill`, reconstructible from this paragraph; re-measured on the merged tree at ccf530b5). `MySet` is a carrier with `requires O: WeakOrd[T]` whose `contains` reads the slot through `WeakOrd.compare`. `Searchable` is a spec with a body-less member `contains(c: C, x: Element)` and ONE shared default body `containsAny(c, xs)` that calls it. Two rival `WeakOrd[String]` are in scope, so nothing below is explained by there being one thing to pick. `anthill load` is CLEAN (4293 facts, 432 rules); `anthill run` dies:

  error: cannot dispatch `anthill.prelude.WeakOrd.compare`: ... reached by dispatching on a
  VALUE, which carries its sort but none of its type parameters - so the provider the value's
  construction chose for the slot cannot be recovered here, and more than one could have been.

THE FOUR STEPS, and the information is present at three of them. (1) `MySet.empty[T = String, O = ByLength]()` - the typer builds MySet's dictionary from the bracket, puts it in `empty`'s frame; the frame pops and the returned `nothing()` keeps none of it. (2) `MySet.insert(…, "zz")` - built AGAIN from the type; runs. (3) `Searchable.containsAny(s, …)` - the typer knows `s : MySet[T = String, O = ByLength]` and resolves the provision, but the frame it enters is the SPEC's, and eval.rs says what that channel holds: "They used to enter the impl's frame with the SPEC call's own channel, EMPTY for the ordinary abstract call." There is no slot to hand it to. (4) `contains(c, h)` in the body - `c : C`, and the same doc states the consequence: "Both arms resolve the impl from a runtime RECEIVER VALUE, precisely because the typer could not pin it - so no call site built the impl a dictionary and no caller slot names it." The runtime then rebuilds from `node(elem: "zz", rest: nothing())`, which mentions no ordering.

WHY PROJECTION IS THE RIGHT ROUTE AND NOT A NEW MECHANISM: a body-less spec operation IS a dictionary entry, and the projection already exists. `stdlib/anthill/realization/runtime.anthill`: `operation resolveOp(d: Dictionary, specOp: Symbol) -> OpRef` - "the impl op plus THIS DICT AS ITS DISPATCH ENVIRONMENT. Keyed by the SPEC OP SYMBOL - the same key the interpreter dispatches on. The reflect face of `dispatch_via_sort_ops_table`." An `OpRef`'s `dict` is installed into the callee frame at apply (WI-420) rather than forwarding the caller's.

AND ONE DICTIONARY IS SUFFICIENT, because `dict_layout` already reserves the space. For spec = Searchable, provider = MySet the layout is the spec half (EMPTY - Searchable declares no `requires`) then the provider half (MySet's own sort-level `requires O`). So the dictionary is

  Dictionary( sub0 = Dictionary(impl: ByLength),  impl: MySet )

which carries BOTH halves of what the body needs: `impl` says which implementation `contains` resolves to, and `sub0` is the evidence that implementation reads. THE ONE-SENTENCE STATEMENT OF THE DEFECT: both routes find the same implementation, and only one of them brings the evidence.

WHAT THIS TICKET IS NO LONGER ABOUT. It was filed as "may a requirement dictionary be stored in a VALUE", with five obstacles about where a field would live in the tree. That framing is RETIRED and the correction is recorded in this ticket's Changes. `s` never needed to carry anything; the dictionary is built three times in the program above and simply never reaches the frame that reads it. A value-carried dictionary survives only for the routes where there is genuinely NO static type to build one from - a host `interp.call` whose value was built in Rust (`seed_entry_requirements` has nothing to read), the SLD bridge calling from a rule body, and a true existential (`List[T = MySet[T = String, O = ?]]`), which Anthill does not have. If this ticket lands, whoever takes it states which of those three still refuse and whether `NamedSlotNotCarried` survives for them.

NEGATIVE RESULT, MEASURED, so it is not retried: the slot cannot be declared on the SPEC. `Searchable requires OE: WeakOrd[Element]` with `OE = O` written in the provision head is refused - "'MySet' provides 'Searchable', which requires 'WeakOrd', but 'MySet' does not provide 'WeakOrd'". A `requires` on a spec is an obligation on the PROVIDER, so that spelling demands MySet itself be an ordering. The evidence must travel as the provider's own condition, which is exactly the half `dict_layout` reserves.

PRIOR ATTEMPT AND THE LIKELY PREREQUISITE. Frame inheritance was tried during WI-456 (2026-09-20) and backed out because "a dictionary does not record which spec it witnesses"; it broke `wi435_iterable_op_on_map_handle_value_dispatches`. CONFIRMED at the representation: `eval/dictionary.rs` is `Dictionary(sub0 … subn-1, impl: S)` - the PROVIDER is recorded, the spec is not, and `alloc_dictionary(spec, provider, subs)` takes the spec only to validate against `dict_layout` and then drops it. So making a dictionary self-describing may be this ticket's first step rather than a separate one; whoever takes it decides and says which.

BLAST RADIUS, and it is the reason this is not a SortedSet patch. EVERY spec default body calling a body-less sibling is on the value-directed route today. Most of them work, and it is worth being exact about why: the rebuild SUCCEEDS wherever the reconstructed goal has a unique answer - one provider for `Eq[T = String]`, say - not because evidence travelled. It fails exactly where the construction site made a SELECTION that the data does not record, which is what a named slot is. `wi456_sorted_set_collection_test::single_ordering_control_*` passes either way by design and pins that boundary.

ACCEPTANCE:
 - the probe program RUNS through the DEFAULT BODY and answers yes;
 - and the ordering decides it there: one polymorphic body, `O = ByLength` and `O = Alphabetical` giving DIFFERENT answers for the same query, so one answer twice fails the test;
 - `wi456_sorted_set_collection_test::a_default_body_reading_the_slot_by_value_is_refused_naming_it` INVERTS rather than being deleted, and its `direct` row is FIRST strengthened to that split - today it answers 1 under either ordering, so it proves the typed call RUNS and not that the comparator DECIDED;
 - the programs that work today by unique-answer rebuild still work, named rather than assumed: `single_ordering_control_*`, `wi435_iterable_op_on_map_handle_value_dispatches`, and `op_scoped_relay_chain_correct_via_value_direction` still computes 551;
 - the three no-static-type routes are each stated - refusing or running - and `NamedSlotNotCarried`'s remaining population is named at its site;
 - full workspace green via rustland/scripts/test.sh.

RELATED: WI-456, where this was measured (its §33 in docs/design/058-implementation.md is the layout this reads); WI-20260921-159S9, the other residue of that measurement and a neighbour here, since it is also about which channel reaches a body; WI-857, whose "chain-free-provider accident" is why the blast radius is wide. NOT WI-402, which is Delivered and covers `ensures Spec[C]`, the RETURN position.

NOTE ON THE ID: the slug reads "decide-whether-a-requirement" from the original framing. Ids are never renumbered, so it stays.

## Changes

### 2026-09-21T18:08:09Z — feedback — user

DESCRIPTION REWRITTEN 2026-09-21, SAME DAY IT WAS FILED, because the first framing asked the question at the wrong altitude and would have sent a claimer at the wrong target. Recorded rather than silently replaced.

WHAT IT SAID. "DECIDE WHETHER A REQUIREMENT DICTIONARY MAY BE STORED IN A VALUE", followed by five obstacles - `tip` is nullary so an empty set has nowhere to put one; `bin` IS the set with no root wrapper, so a per-node field duplicates it n times; `eq` is extensional and would have to exclude the field; the discrimination tree indexes structure; SMT terms are data. Every one of those is a cost of ONE MECHANISM - a field on the carrier's own constructors - and I let the mechanism into the problem statement. The five were answers to a question nobody had established was the right one.

HOW IT CAME APART, in the user's questions and in this order. (1) "What can be the reason it CAN'T be stored in a Value?" - there is none. A `Value` is a tree, a `Dictionary` is already a `Value::Entity`, and trees nest; the five were consequences dressed as impossibilities. (2) "Why do you think we have erasure?" - I had imported the word from Java and it fits badly. `Value::Entity` has exactly `{functor, pos, named}` and `Term::Fn` matches it, which is all the refusal message means; but `T = String` IS recoverable from the elements, so the gap is not that type arguments are erased, it is that a WITNESS slot is the one type argument with no footprint in the data. And `Value::OpRef` already carries a `dict`, with WI-420's reason - "an eta'd OpRef escapes to a foreign apply frame, so it cannot inherit" - so the precedent for a value carrying evidence was already in the tree. (3) "But at compile time `s` always has a type" - correct, and decisive: the type is known at EVERY concrete site, and what is shared is the BODY. So the failure is a missing parameter pass, not a missing fact. (4) "`contains` is without body, it means contains is in dictionary" - the one that lands it. A body-less spec operation IS a dictionary entry, `Dictionary.resolveOp` already projects it and hands back an `OpRef` carrying that dict as its dispatch environment, and `dict_layout`'s provider half already holds the comparator. Both routes find the same implementation; only one brings the evidence.

WHAT SURVIVED THE REWRITE: the measured program, the refusal text, the ByLength / Alphabetical split as the acceptance shape, the negative result about declaring the slot on the spec, and the observation that the committed test's `direct` row proves the typed call RUNS and not that the comparator DECIDED.

WHAT DID NOT: the five obstacles, the word "erasure", and the premise that a datum must hold a dictionary. That premise is demoted to the three routes where there is genuinely no static type to build one from - host entry, the SLD bridge, and a true existential - and the new acceptance asks whoever takes this to state which of them still refuse.

### 2026-09-21T21:33:54Z — feedback — user

DELIVERED. The dictionary was never missing; it was resolved and then DROPPED, on one line.

WHERE. The typer's defaulted fall-through arm (`check_apply_iter`, the WI-1091 arm) already resolved `Searchable` AT `MySet` and handed the tree to `classify_pin_or_apply_within` — CONFIRMED by instrumenting that arm on the probe: `fallthrough fn=Searchable.containsAny tree=Some(MySet)`. That function then chose `PinNow`, which carries no requirements channel at all, because its `needs_reqs` test is `op_reads_requirement_slots(impl_op)` — `op_owner_dict_entries`, i.e. the SPEC half of `dict_layout` — and `Searchable` declares no `requires`. The instance, whose PROVIDER half held `ByLength`, went in the bin. Traced frames, before: `MySet.empty` and `MySet.insert` each got `__req_self => D(D(…; impl=ByLength); impl=MySet)`; `Searchable.containsAny` got `reqs=[]`.

THE FIX, TWO EDITS, AND BACKING OUT EITHER ALONE RESTORES THE EXACT REFUSAL (measured, three configurations: typer-out, eval-out, both-out — the same four rows fail in all three and the same rows pass in all three):
 * `classify_pin_or_apply_within` gains `threads_instance`: a resolved tree pinning a PROVIDER the callee's own parent is not makes the dictionary the callee's DISPATCH ENVIRONMENT, threaded even where the callee reads no named slot of its own. Gated by `dictionary_covers_target`, the same question `expand_dispatching_dict` asks at the frame push, so a member inherited from a third sort keeps the `PinNow` it has today.
 * `Interpreter::spec_instance_for_sibling_call` (eval): a call to a member of the RUNNING operation's own sort fills its empty channel from that frame's `__req_self`. Four gates, ordered cheapest-first because this sits on every dispatch — the frame `__req_self` read excludes nearly all of them before any name lookup. (1) sibling; (2) the dict's provider is NOT that sort, so it is an INSTANCE and not a WI-415 parent bundle; (3) it is the shape `dict_layout` lays out for the pair AND covers `target`; (4) it covers the op that will ACTUALLY RUN after value-direction redirects, because `requirements_for_value_directed_impl` passes a non-covering channel through unchanged and the pre-R10KC alternative was the value REBUILD.

NO NEW MECHANISM WAS BUILT, which is what the reframing predicted. `dispatch_via_sort_ops_table` + `expand_dispatching_dict` ARE `Dictionary.resolveOp`, and both already served the `DeferToRequirement` route; the projection the ticket asked for is those two, called from one more place.

THE DICTIONARY IS STILL NOT SELF-DESCRIBING — the ticket asked whoever took it to decide, and the decision is NO, not now. The spec is established from the FRAME instead (gate 1: the running op's parent), which is what WI-456's abandoned frame-inheritance lacked; gates 3 and 4 are what make a wrong reading fall back rather than raise. Making the VALUE carry its spec stays available and is not needed for this.

ACCEPTANCE, ITEM BY ITEM.
 * The probe RUNS through the default body and answers yes.
 * THE ORDERING DECIDES: one polymorphic body, one query, `O = ByLength` answers yes and `O = Alphabetical` answers no — `wi_r10kc_spec_default_body_dictionary_test::a_spec_default_body_reads_the_carriers_named_slot_and_the_slot_decides`, plus `the_dictionary_survives_the_default_bodys_own_recursion`, which drives the BODIED sibling (a different eval arm: step 3 rather than step 3b) by putting the deciding element second.
 * `a_default_body_reading_the_slot_by_value_is_refused_naming_it` INVERTED, renamed `a_default_body_reads_the_named_slot_through_its_provisions_dictionary`, and its `direct` row was FIRST strengthened to the split: `"zz"` against `"a"` agreed under both orderings, so its `1` proved the typed call RAN and not that the comparator DECIDED. `"aaa"` against `"zz"` splits them 2 vs 3.
 * THE UNIQUE-ANSWER REBUILD still works, named: `single_ordering_control_*`, `wi435_iterable_op_on_map_handle_value_dispatches`, `op_scoped_relay_chain_correct_via_value_direction` (551), `wi1093_*` all pass. One of this ticket's OWN controls was WRONG and the back-out caught it: a "single provider" fixture that removed `Alphabetical` still had TWO, because the stdlib's `String` provides `WeakOrd[String]`. Rewritten at `T = Int64`. A control that fails on the back-out is not a control.
 * THE THREE NO-STATIC-TYPE ROUTES, each measured, and TWO OF THE TICKET'S OWN CLAIMS ABOUT THEM WERE WRONG.
     - EXISTENTIAL RETURN — the ticket said "which Anthill does not have". IT HAS: §"In a RETURN the quantifier flips to ∃" (WI-1063). `operation mk() -> MySet[T = String]` packing `O := ByLength` is exactly the shape, loads, and is THE surviving population of `NamedSlotNotCarried`. The two halves refuse one PHASE apart — direct is a LOAD error (WI-1094's), through the spec is a run-time refusal.
     - HOST ENTRY — does NOT reach `NamedSlotNotCarried` at all. `seed_entry_requirements` installs a self-rooted STAND-IN (WI-868's decision, three measurements at `stand_in_requirement`), the read falls to value-direction, and it answers from the ARGUMENTS: `WeakOrd.compare("aa","zz")` lands on `String`'s alphabetical order and a `ByLength` set answers FALSE where the right answer is true. SILENTLY WRONG, and PINNED as such by `the_host_entry_route_answers_by_value_direction` so the eventual fix fails the test and reads its doc. Closing it is what WI-868 names as the thing that "would re-open" it — a change to the dictionary VALUE.
     - SLD BRIDGE — `NamedSlotTies::Raise`, so it suspends and residualizes. Not driven: the rule shape that would reach it residualizes one goal EARLIER, with one provider in scope as well as two, so an assertion would not be about the slot.
   `NamedSlotNotCarried`'s remaining population is named at the variant's own site.
 * Full workspace green via rustland/scripts/test.sh.

A THIRD TICKET CLAIM CORRECTED, raised in review: `List[T = MySet[T = String, O = ?]]` is NOT unspellable. It is spellable and a generic consumer over it RUNS once the consumer declares and forwards the slot (`a_generic_consumer_over_a_list_keeps_each_elements_ordering`, four levels between the binding and the read). What cannot be BUILT is a HETEROGENEOUS list, and the reason is RANK-1-ness rather than a missing ∃: an unwritten nested slot is one fresh rigid PER SLOT (WI-1061) and a list has ONE element type. `cons` refuses the second ordering by name.

DOCS SYNCED, because the spec named this defect's shape as an EXAMPLE of a rule that no longer holds. `kernel-language.md`'s "At run time a named slot is never recovered from a value" kept its general claim and lost its example — a spec's default body is no longer such a dispatch — and now names the three surviving routes. `docs/design/path-dependent-types.md` gains §5.5: the four sources of rigids, the two distinct reasons elimination can fail, and the correction that §5.3's "runtime `Type` values stay ground" fence holds only for a projection whose member the value's own DATA determines. §5.3 carries a pointer to it.

WHAT /code-review CHANGED, because the first cut of the eval gate was UNSOUND and the finding was DRIVEN. It inferred the dictionary's SPEC from the running frame's `op`; `enter_closure` deliberately pairs the CALLER's `op` with the LAMBDA's captured `requirements` ("the closure inherits its caller's `op` for error-reporting purposes"), so on any frame running a lambda the two describe different things and a foreign `__req_self` was installed. The licence is now asked of the dictionary directly - `sort_provides(provider, spec)`, the invariant the prose already claimed - which also closes the namespace-parent reading, a namespace never being `provided`. Six more, each accepted with its own repair:
 * `!requirements.is_empty()` was the wrong test for "carries no `__req_self`" - a channel built by `start_apply_with_op_slots` holds OP-SCOPED slots and no self slot, so every callee with a `requires` of its own was silently skipped. Now keyed on `find_requirement(.., __req_self)` and MERGED rather than replaced, which is the reading WI-1091 already gave the mirror site.
 * The gate sat BELOW the builtin arm, which seeds `builtin_dispatch_dict` from the pre-gate channel and returns - so a sibling that happens to be HOST-backed reached its builtin with the dictionary unset, and for a nullary one (`TypeValue.type_value()`) the dictionary is the whole answer. Moved above it: one source construct must not behave two ways on body-vs-builtin.
 * A RECEIVER-LESS sibling was not served at all - value-direction has no argument to classify, so the call fell through every arm to `unrunnable_target_error` with the dictionary unread. `dictionary_resolved_sibling` is `Dictionary.resolveOp`'s other half, strictly additive: it runs only where the call raised a moment ago.
 * `dictionary_covers_target(parent, provider, impl_op)` was a TAUTOLOGY - that function's own first line re-derives `owner` as the same symbol, so `slots_for` never answers `None`, and its `names.is_empty()` early return is the very reader `needs_reqs` already consulted. The guard the comment described did not exist. It now asks the question of the op eval ENTERS, with the dispatched spec, which also narrows the promotion at WI-1093's supplier-pinned arm, whose own doc records that pairing as unguarded.
 * `Err(_)` swallowed a tie step 3's `None` path would never re-raise. The gate now asks exactly the resolver the selected arm will ask - chosen by `cached_operation_body`, which decides the arm - and propagates.
 * The promotion is DECLINED where the callee would inherit the caller's frame anyway (`inherits`): `dispatch_dict` is `None` there, so promoting would not thread the instance, it would reinstate the unguarded frame inheritance WI-456 backed out, by a class change instead of a frame read.

TWO FINDINGS DECLINED, each with the reason at its site rather than in this note alone. The SLD woven-dispatch route hand-copies the typer's predicate under a comment asserting the two agree; that claim is now false and the comment says so, and the consequence is named - a RULE BODY reaching this shape is still on the value route. Widening it there is a behaviour change on the resolver's own dispatch with no driver in this ticket's fixtures, and it moves `reduce_op_value`'s folding too. And `stdlib/anthill/prelude/sortedset.anthill`'s enforcement comment stated the OPPOSITE rule and called this ticket open; it is rewritten, which is the site CLAUDE.md's localized-invariant rule says a reader greps. Four anchor defects of my own went with it: `*_through_the_default_body` grepped to nothing, wi456's BACKED-OUT CONTROLS block still named the deleted test, the list row was presented as a driver when it runs the pre-existing `DeferToRequirement` route and passes either way, and "measured and driven" over-claimed a route this file itself calls not driven.

FOLLOW-UP FILED, and it is the question this ticket's measurements raised rather than answered: WI-20260921-EE0EP — pass the ARGUMENT's own dictionary so `s.O` is a channel and not only a name. Measured here: `operation has(s: MySet[T = String], …)` is refused, and DEPTH IS NOT THE AXIS — a top-level slot, which WI-1059 says IS the projection `s.O`, gets the same refusal as a nameless nested one, word for word. The evidence is already where it would have to be: `dict_layout`'s provider half means a carrier's dictionary already NESTS its named slots' dictionaries (`__req_self = D(sub0 = <ByLength>, impl: MySet)`, and `__req_weakord` IS that sub0). It is WI-1094's other branch, whose own sub-question (a) put the two outcomes side by side and shipped the refusal for a good reason that does not apply to forwarding the value's own.

