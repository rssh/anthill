## Attributes

- id: WI-555
- created: 2026-06-24T06:11:08Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-01T13:16:04Z

- acceptance: cargo-test

- depends_on: WI-551

- tags: wi541

## Description

Consolidate the reflect TERM-REIFIER pair (reify/reflect), the residual of WI-551 part (a). WI-551 shared the introspection RECORD ops (sorts/operations/constructors/fields/rules/descriptions/facts_of + leaf helpers) via anthill-stl/src/reflect/reader.rs, but left the two TERM reifiers per-realization: interpreter reify_term_to_value/reflect_value_to_term (builtins.rs, TermId<->Value::Entity tree over anthill.reflect.TermRepr/LiteralRepr ctors) vs bridge reify_view/reflect (bridge.rs, TermView<->generated TermRepr enum). These are parallel recursive walks over the same Const/Var/Ref/Fn/Bottom structure and HAVE drifted (WI-543 BigIntLiteral synced in BOTH; bridge uses index_var for Rigid/DeBruijn while interpreter handles them inline). WRINKLE found during WI-551: the two pick DIFFERENT in-band name carriers -- the interpreter builds RefRepr/FnRepr name as a Value::Term(Ref(sym)) (a Ref TERM, so its inverse reflect_value_to_term reads it back as Ref/Ident), while the bridge/generated TermRepr types name as Symbol (ReflectSymbol). A shared walk cannot just emit one TermRepr both consume: consolidating needs to either (a) route the interpreter through the generated TermRepr enum + a TermRepr->Value::Entity mapper (reconciling the name carrier, watching kb_reflect_roundtrips_a_ref_repr), or (b) extract a carrier-callback visitor over the term structure each realization parameterizes. Acceptance: one shared term-structure walk behind both reify realizations (no parallel Const/Var/Ref/Fn recursion in builtins.rs vs bridge.rs); reify roundtrip + bigint tests green; cargo-test green.

