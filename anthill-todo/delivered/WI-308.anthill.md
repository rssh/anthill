```anthill
fact WorkItem(id: "WI-308", created: "2026-05-25T09:07:01Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-05-25T09:06:45Z"))
```

## description

Fix syntax-mixing in typing_pass_spec.anthill: the reference algorithm (Part II) builds Type VALUES in expression position using the type-annotation instantiation sugar Name[bindings], which conflates two syntaxes. Three sites: line 497 and 505 (Function[A = param_type, B = result_type(br), E = result_effects(br)]) and line 557 (List[T = TypingEnv.apply_subst(env3, elem_type)]). These are not type annotations; they construct a Type value where one binding is computed by an operation call returning Type. Rewrite them to the explicit Type constructors (already imported in the spec): parameterized(base: sort_ref(name: Function), bindings: [TypeBinding(param: A, value: param_type), TypeBinding(param: B, value: result_type(br)), TypeBinding(param: E, value: result_effects(br))]) and parameterized(base: sort_ref(name: List), bindings: [TypeBinding(param: T, value: TypingEnv.apply_subst(env3, elem_type))]). Verified: the explicit form parses with 0 errors on the unmodified grammar (no grammar change, no denoted needed). This is the LAST remaining parse blocker for typing_pass_spec (lambda was fixed in WI-306); after it, un-ignore typing_pass_spec_parses_and_loads (WI-303) if it also loads clean. SCOPE: spec doc + the #[ignore] test. NOT related to WI-302 (value-in-type/denoted) or WI-307 (effect poly).

