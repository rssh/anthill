## Attributes

- id: WI-682
- created: 2026-07-11T09:11:34Z

- status: Delivered
- status_agent: user
- status_at: 2026-07-11T23:27:45Z

- acceptance: cargo-test

- tags: carrier-neutral

## Description

antill-core: resolve.rs: Why occurrence_arg_and_pattern return TermId as pattern?

## Changes

### 2026-07-11T21:19:38Z — feedback — user

NOT delivered as of 2026-07-11 (code still reifies): occurrence_arg_and_pattern (resolve.rs:3404) returns (Rc<NodeOccurrence>, TermId), reifying the Node pattern via try_occurrence_to_term (L3432-3440). ENABLER NOW EXISTS: WI-683 delivered match_view_value_pattern (mod.rs:2758), the carrier-neutral peer of match_view accepting a Value pattern of any carrier. CLEANUP: return the pattern as a Value (Node stays Node); route the 4 reflect builtins (occurrence_term/_span/_owner/sub_occurrences) through match_view_value_pattern for the Node case, keeping match_view for a genuine Term pattern. GOTCHA: the try_occurrence_to_term None=>Failure arm (L3428-3431) is DELIBERATE -- a child-bearing reflect pattern (if_expr/let_expr/lambda/match_expr) has no goal-term shape and MUST fail clean. Small, independent of WI-690.

### 2026-07-11T23:28:21Z — feedback — user

DELIVERED (commit 3685032b). occurrence_arg_and_pattern now returns OccPattern{Term(TermId),Node(Value)}; Node arg1 STAYS a Node, σ-applied via reify_value (substitute_occurrence, identity/span preserved) and matched via match_view_value_pattern (WI-683). Term arg1 keeps fast match_view. New match_occ_pattern dispatches. GOTCHA honored via view_is_indexable pre-check: a child-bearing/Opaque form fails clean (no match) instead of tripping insert_pattern's Opaque panic; whole-structure check also catches NESTED such forms the old top-level-only reify silently lowered to bottom. Extra (code-review): arg1 reader extracted to shared occ_arg1_pattern, so builtin_operation_body (WI-305) deduped onto it (its 'mirror' comment was stale) and gains the same carrier-faithful+fail-clean behavior. Test wi682_node_pattern_binds_reflectable_child. anthill-core 2293/0, workspace 2734/0.

