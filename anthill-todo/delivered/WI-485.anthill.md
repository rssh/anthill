```anthill
fact WorkItem(id: "WI-485", created: "2026-06-16T05:08:24Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-460", "WI-469"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-16T05:28:01Z"))

fact Tag(workitem: "WI-485", name: "typing")
```

## description

Typer (WI-447 bare-form prereq, Gap I): a `find` DOT-CALL on a List does not thread the callback's `s.T` ELEMENT when stream.anthill find is in BARE form. With `find[EffP](s: Stream, pred: (x: s.T) -> Bool @ {EffP, -Modify[x]}) -> Option[s.T] effects {s.E, EffP}`, a call `xs.find(is_big)` (xs: List[Int64], is_big(n: Int64)->Bool) leaves the callback param at the neutral `s.T`, so the concrete callback clashes — 'type mismatch in gt.b: expected s.T, got Int64' (is_big's body retyped against the un-grounded expected param). PRE-EXISTING bare-form gap, INDEPENDENT of effects: reproduces with the Gap-G+H effect-projection fix REVERTED (and that fix actually REDUCES the wi443 failure count 3→1, so it is orthogonal, not causal). Repro: wi443 identifier_dot_call_evals_like_plain_find / _needs_no_import / _let_bound_receiver fail once stream.anthill find is bare. The `s.T` sits inside a DENOTED arrow (the `-Modify[x]` lacks binder), so this is WI-460-adjacent (projection in a denoted-bearing arrow) but on the DOT-CALL DISPATCH + named-callback path: the receiver's element must ground the callback param projection before the callback arg is checked. Likely the dot-call dispatches to the bare Stream.find (s.T) rather than/alongside Iterable.find (named Element), and the element does not thread into the denoted-arrow callback param. FIX DIRECTION: thread the dispatched/dot-call receiver's element type into the callback param's `s.T` projection (eliminate it against the receiver) before validating the callback arg; OR resolve the find dispatch to the element-threading form. CASCADE: touches dot-call dispatch + denoted-arrow callback validation (WI-460/WI-469 area). BLOCKS WI-447. ACCEPTANCE: wi443 green with the bare Stream.find form; cargo-test.

