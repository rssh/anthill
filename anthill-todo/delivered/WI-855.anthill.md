```anthill
fact WorkItem(id: "WI-855", created: "2026-07-29T11:35:33Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-07-29T12:48:33Z"))

fact Tag(workitem: "WI-855", name: "modinst")

fact Feedback(workitem: "WI-855", author: "user", at: "2026-07-29T12:48:50Z")
```

## description

The eval bridge silently DROPS an AMBIGUOUS requirement-dictionary verdict: `resolve_bridge_requirements` (rustland/anthill-core/src/kb/typing.rs, `BridgeRequirements`) folds "no provider", "AMBIGUOUS provider", "cyclic provider" and "under-determined carrier" into ONE `Unresolvable { detail }` bucket, and its eval consumer (rustland/anthill-core/src/eval/eval.rs:1785) converts that into a silently UNSUPPLIED frame — trace-only, printed just under `ANTHILL_TRACE_REQ`. So when two providers genuinely tie for one requirement, the ONE place that DETECTED the tie throws the detection away and the call proceeds with no dictionary; the eventual failure (an unbound requirement further in, or a wrong answer) names neither the tie nor the candidates. Recorded by docs/proposals/058-modular-instances.md §4.9's consumer table as a 'genuinely independent blind spot / candidate ticket', beside the first-fact-wins one WI-837 retired for the sem-eq index. NOT re-litigating WI-822: that ticket MEASURED that raising loudly at dispatch for EVERY `Unresolvable` breaks 29 tests, and deliberately chose trace + loud-at-the-read; the un-relitigated question is the AMBIGUOUS sub-case specifically, which is a coherence violation rather than an unpinnable-at-these-types situation and has no legitimate 'proceed unsupplied' reading. ACCEPTANCE: `BridgeRequirements` distinguishes an AMBIGUOUS verdict from the other unresolvable causes (a distinct variant or a tagged cause), carrying the tied candidates; the eval consumer surfaces it as a named error/diagnostic naming the requirement and both candidates rather than entering unsupplied; the other unresolvable causes keep WI-822's measured trace-and-continue behaviour (its 29 tests stay green); a test drives a genuine two-provider tie through value-directed dispatch and asserts the diagnostic identity; full workspace green via rustland/scripts/test.sh.

## Feedback

### 2026-07-29T12:48:50Z — user

DELIVERED with two corrections to the ticket's own framing, both measured. (1) The ticket says the eventual failure 'names neither the tie nor the candidates' — MEASURED, it was Internal(DeferToRequirement: __req_desc not bound, running WrapDesc.describe): the failure was LOUD, just attributed to a MISSING dictionary rather than to the tie. The defect is mis-attribution, not silence. (2) OPEN, recorded not filed: the reason a tie reaches the runtime at all is that WI-838's per-(spec,carrier) coherence grouping admits a candidate only as an instance FACT (binds an op) or as a WITNESS (provider != carrier), so an ordinary SELF-PROVIDER (sort Leaf { fact Desc[T=Leaf]; operation describe }) is a candidate of NEITHER kind and can never be one half of a refused pair. Adding a self-provider candidate kind would move this class to load time; that is a design increment on 058's grouping, recorded in 058 section 4.9. My first explanation blamed the concrete-provider exemption and was REFUTED by driving an abstract-rival twin (still loads clean) — the test pins both spellings. Also NOT split: ResolutionResult::Cyclic is a program-level defect too and stays in the Unresolvable bucket, unmeasured; splitting it is the same edit one variant over.

