```anthill
fact WorkItem(id: "WI-641", created: "2026-07-05T15:07:08Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-640"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-05T18:43:30Z"))

fact Tag(workitem: "WI-641", name: "rust")
```

## description

Simp rewriter convergence PHASE 2: merge the two [simp] drivers into ONE carrier-neutral engine. After WI-640 the FIRING is converged (typer simp_rewrite::try_fire and resolver fire_simp_equation both match one-way via match_view_oneway + share the guard/bounds helpers) but the DRIVERS still coexist: the typer's ITERATIVE bottom-up simp_rewrite::rewrite/visit_node/build_node (work-stack, made iterative by WI-278 to survive deep occurrence trees) vs the resolver's apply_eq_rules_occurrence RECURSIVE walk (added in WI-640). Tasks: (1) route the resolver's occurrence redex through ONE iterative driver (parameterize its firing over fire_simp_equation) so a deep Node prove-goal can't overflow the host stack; (2) share the opener — open_equation returns fresh so fire_simp_equation stops re-inlining it and try_fire reuses it; (3) make the requires-guard carrier-neutral (fire_simp_equation reifies a Node redex to a term for simp_requires_guard_holds(TermId), discarding the occurrence's WI-578 carried type + interning a transient term — the typer uses occurrence-native simp_fire_guard_holds(&occ)); (4) align the descend gate (apply_eq_rules_occurrence uses ViewHead::Functor vs the typer's is_rewritable Expr whitelist). Branch base: wi640-simp-rewriter-convergence. Related WI-640 (Phase 1), WI-283, WI-278. ACCEPTANCE: one shared iterative occurrence driver + one opener; requires-guard carrier-neutral; cargo test green; a deeply-nested Node prove-goal that would overflow the recursive walk now simplifies.

