## Attributes

- id: WI-676
- created: 2026-07-11T05:20:14Z

- status: Delivered
- status_agent: user
- status_at: 2026-07-11T05:19:03Z

- acceptance: cargo-test

- tags: carrier-agnostic

## Description

CARRIER-AGNOSTIC bucket-1 index consolidation (WI-661 child; a WI-672 /code-review deferral). The per-fact reflect indexes ProvidesIndex (WI-660) and SortInfoIndex (WI-673) are the 3rd/4th hand-rolled copy of the same Symbol->Vec<RuleId> build/lookup/fallback pattern. Introduce ONE shared SymbolKeyedFactIndex (a HashMap<Symbol,Vec<RuleId>> bucket map; canonical-key get(); a rules_or_scan fallback helper that fast-paths the built index else does the live rules_by_functor scan). ProvidesIndex becomes two of them (by_spec_base + by_carrier); SortInfoIndex collapses to a bare SymbolKeyedFactIndex. Folds the 3 near-duplicate accessors (providers_of_spec/carrier_candidates/candidates) and the 3 X_rids_by_Y wrapper helpers into the shared struct. WI-656 op_records (rich per-op record) and WI-659 SortAliasIndex (TermId targets, string/parent keys) deliberately NOT folded in -- neither is a Symbol->Vec<RuleId> bucket. No behavior change; identity stays canonical (WI-672, spec 8.6). Acceptance: full cargo-test green + /code-review.

