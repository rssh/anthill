## Attributes

- id: WI-1023
- created: 2026-08-05T21:15:40Z

- status: Delivered
- status_agent: user
- status_at: 2026-08-06T04:17:45Z

- acceptance: cargo-test

## Description

"Which carriers are structurally equivalent to their term twin" is answered by four hand-written per-site lists, and they have drifted. Found by /code-review on WI-1016, which added a fifth arm to one of them. Each site asks the same question and answers it with its own `matches!`, so a new structural carrier needs N edits and gets fewer. THREE MEASURED-OR-NAMED GAPS, all fail-open today (nothing goes red), each with the site that owns it. (A) is_duplicate_projection (kb/resolve.rs) allows Value::Term | Node | SymbolRef. Value::Requirement and Value::OpRef became STRUCTURAL in WI-1019 -- the commit immediately before WI-1016 -- and Value::Var fingerprints as a Var token, yet all three still set has_value_binding and switch answer dedup OFF. Reachable by exactly the route wi1016_symbol_carrier_test drives: finish_result_value binds a projected child in its own carrier, and Dictionary.sub(d, i) yields a Value::Requirement. Effect: duplicate answers survive. CAUTION -- fixing it widens in the answer-DROPPING direction, which is why it is a ticket and not an arm: it needs a control per added carrier, and the domain argument at the site (this scan is over ALL of sigma; the fingerprint is over goal-reachable vars only) must survive the change. (B) fn_value (kb/mod.rs) accepts Value::Term | SymbolRef as leaves. alloc_from_value ALSO lowers Int/Str/Bool/Var/Entity losslessly, so the WI-815 disjoint-key-space argument stated at that site applies verbatim to a scalar child: f(Value::Int(1)) takes the Value::Entity path and keys value_fact_dedup, while f(1) built as a term keys fact_dedup -- two facts for one logical fact. MEASURED as unreachable rather than assumed harmless: all four assert_fact_carrier callers (eq_derive.rs:281, load.rs:14991, typing.rs:40273, assert_metadata_fact_carrier) pass Value::term(..) children. Widening RE-ROUTES existing heads between the two key spaces, so it needs the same instrumentation WI-815 used -- and note WI-815's lesson, that a count over a population of zero cannot see a wrong key. (C) MapKey::try_from_value (eval/map_arena.rs) canonicalizes Value::SymbolRef and Term::Ref into MapKey::Ref, deliberately declining Term::Ident (declining is CORRECT -- widening would merge two distinct keys and let one put overwrite another) but ALSO declining a non-canonicalized nullary constructor Fn{c,[],[]}, which resolve_qualified_name_term mints by bypassing alloc's canon. That one IS the same name as Ref(c), so declining it accepts a key SPLIT rather than avoiding a merge. Narrow today (that mint has few callers, none feeding a Map). WORK: decide whether one predicate can serve all four -- the honest answer may be NO, since (A)'s domain is all of sigma while (B) additionally needs 'lowers WITHOUT losing identity' (a Value::Node must never lower) and (C) needs 'is the same NAME' -- in which case deliver named predicates with the distinction written down, not one blurred one. Whatever the shape, each carrier admitted needs its own control, and (A) and (B) both move in a direction where being wrong LOSES data.

