## Attributes

- id: WI-584
- created: 2026-06-29T05:13:21Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-29T08:19:38Z

- acceptance: cargo-test

- tags: wi502

## Description

Resolver equational [simp] fallback (apply_eq_rules) instantiates VARIABLE-RHS rules correctly. Today the query + reify(result_var) shortcut (resolve.rs ~2244-2289) returns the rule UNOPENED DeBruijn RHS var, so only GROUND-RHS identities rewrite: a var-RHS rule pick(?a,?b)=?a makes simplify(pick(5,7)) yield Var(DeBruijn(1)) instead of 5 (probe-verified; GENERAL -- reproduces with a NON-requires-guarded sort, so pre-existing rewriter debt, not type-directed). The discrim match tree_subst already carries the LHS bindings as SYNTHETIC VarId(u32::MAX - n) entries (one per DeBruijn n); with_fresh_vars (mod.rs ~2848, body_rename loop ~2929-2937) opens a rule head+body via term_from_debruijn + those bindings. Fire the equation by instantiating the eq head RHS template the same way (open its DeBruijn vars to fresh globals, bind them to the synthetic LHS-match values, reify) instead of reifying the raw result_var. CASCADE RISK: any var-RHS equation now instantiates -- run the full suite. Acceptance: a non-requires-guarded var-RHS [simp] rule fires to its substituted RHS via simplify; cargo-test green. Discovered while implementing WI-292 (type-directed [simp] firing), which depends on this.

## Changes

### 2026-06-29T08:19:28Z — feedback — user

DELIVERED 2026-06-29 (commit 7db93b1, suite 2364). apply_eq_rules matched eq via query(eq(current,?result)) then reify(result_var), which for a DeBruijn rule (loaded [simp] form, arity>0) returns the UNOPENED DeBruijn RHS template -> only ground-RHS identities rewrote; pick(?a,?b)=?a made simplify(pick(5,7)) yield Var(DeBruijn(0)) not 5. (Arity-0 Global-var equations via assert_fact never hit it -- LHS match binds the Global directly, reify resolves.) FIX: new instantiate_eq_rhs opens the RHS template DeBruijn vars to fresh globals (term_from_debruijn) + substitutes the synthetic VarId(u32::MAX-n) LHS-match values in tree_subst -- the SAME instantiation with_fresh_vars does for a body. Ground RHS / non-term-RHS fallback behavior-preserving. CORE ASSUMPTION verified (code-review finder 3, traced discrim.rs/persist_subst.rs/walk_view): the query var binds to the raw DeBruijn(0) term and reify never chases a DeBruijn through the synthetic encoding, so the RHS var and the LHS-matched var are independent subst entries -- the fix bridges them; test passes for the right reason. /code-review (3 finders, max effort): correctness []=clean (6 concerns traced: synthetic-detection byte-identical to with_fresh_vars, term_from_debruijn bounds safe since RHS indices<arity, fresh vars strictly newer than tree_subst keys so 2nd reify cannot capture, fuel-bounded); cascade []=clean (activation reaches ONLY the proof-verify simplify pass -- config.simplify default false, sole prod setter discharge_by_derivation; dangerous Set/Map commutative/idempotent rules stay requires-guarded=skipped, validated by resolver_skips_requires_guarded_equation; activated requires-free rules Pair/List/Int64/Float/Eq/String are sound terminating identities; no cross-sort collision per WI-581 qualified-functor guardrail); 1 maintainability finding APPLIED -- the u32::MAX-n synthetic-VarId decode (was inline in BOTH with_fresh_vars and instantiate_eq_rhs) centralized in new Var::synthetic_debruijn_index next to its encoder Var::as_vid, eliminating the 3-file silent-desync risk. Test: apply_eq_rules_instantiates_var_rhs. UNBLOCKS WI-292 (now its only remaining blocker is cleared).

