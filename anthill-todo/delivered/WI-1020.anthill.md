```anthill
fact WorkItem(id: "WI-1020", created: "2026-08-05T12:34:08Z", context: none, acceptance: [ToolPasses(tool: "scaland-sbt-test", params: none)], depends_on: some(value: ["WI-1055", "WI-1054"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-11T18:55:13Z"))

fact Feedback(workitem: "WI-1020", author: "user", at: "2026-08-08T11:58:03Z")

fact Feedback(workitem: "WI-1020", author: "user", at: "2026-08-08T12:05:47Z")

fact Feedback(workitem: "WI-1020", author: "user", at: "2026-08-08T12:13:30Z")

fact Feedback(workitem: "WI-1020", author: "user", at: "2026-08-08T14:45:17Z")

fact Feedback(workitem: "WI-1020", author: "user", at: "2026-08-08T17:12:00Z")
```

## description

docs/scala-forward-mapping.md §2.3 promises "The generated file compiles as-is" and NOTHING verifies it -- every BootstrapTest assertion is a substring match on the emitted text, which is exactly how the two defects in WI-1021 and WI-1022 survived in stdlib output.\n\nMEASURED (2026-08-05, delivering WI-940). Bootstrap.generate on stdlib/anthill/prelude/pair.anthill emits, into src/main/scala/anthill/prelude/Pair.scala:\n\n  package anthill.prelude\n\n  enum Pair[A, B] extends PartialEq[A], PartialEq[B]:\n    case Pair(fst: A, snd: B)\n\n  trait PairOps[A, B]:\n    def fst(p: Tuple2): A\n    def snd(p: Tuple2): B\n    def eq(a: Tuple2, b: Tuple2): Boolean\n\nFed to `scala-cli compile --scala 3.6.3` (with a PartialEq trait as eq.anthill's own emission defines it) this produces SIX errors from two independent mechanisms: four "Missing type parameter for [T1, T2] =>> (T1, T2)" (WI-1021) and "trait PartialEq is extended twice" plus "class Pair cannot be instantiated since it has conflicting base types" (WI-1022). The existing BootstrapTest for this file passes -- it asserts `enum Pair`, `case Pair(fst: A, snd: B)`, `trait PairOps`, `def fst`, `def snd`, and every one of those substrings IS present in a file that does not compile.\n\nWHY THIS IS THE ROOT AND NOT A THIRD SYMPTOM. The repo's own rule is that a test for a capability must DRIVE the capability -- "it loads clean" is not evidence. A codegen backend's capability is EMITTING COMPILABLE SOURCE, so its driving test compiles the source. String-matching measures that a renderer produced the characters the test author expected, which is a test of the test author. Every future Bootstrap fix (WI-1021, WI-1022, and the WI-940 record collapse itself) is currently pinned the weak way, and each new mapping rule adds another chance for a plausible-looking string to be uncompilable Scala.\n\nSCOPE: a test that takes Bootstrap.generate's output for a set of stdlib files, writes the tree, and COMPILES it, failing with the compiler's own diagnostics. Open questions for whoever takes it, all real:\n  * HOW to invoke the compiler. scala-cli is on the developer's PATH but is not an sbt dependency; `scala3-compiler` as a Test dependency and a programmatic `dotty.tools.dotc.Driver` invocation keeps it hermetic and is the likelier answer. Check what the JVM/Scala version matrix in scaland/build.sbt allows.\n  * WHICH files. The emitted tree has cross-file references (Pair.scala names PartialEq, declared by eq.anthill), so a single-file compile is not enough -- compile a CLOSURE, and say which one.\n  * WHAT to do about files that legitimately cannot compile yet. Bootstrap emits abstract `<Sort>Ops` traits with no implementations, which is fine; but a `TypeExpr.Variable` renders as `?` and Denoted/EffectRow render as `Any` placeholders (TypeGen.scala:18-39), and `?` is not a Scala type. Decide whether the harness covers the files that hit those paths or names them as an explicit, listed exclusion -- an unlisted skip would recreate the blindness this ticket closes.\n\nACCEPTANCE: a test that compiles Bootstrap's emitted output for at least the prelude closure and fails on a compiler error, with the compiler's message in the failure text. State at the test site which currently-green string-match test it SUBSUMES (if any) and which it does not. Any file excluded from compilation must be named in the test with its reason -- no silent skips. sbt test green.\n\nSOURCE: measured while delivering WI-940, whose /simplify-style review pass dumped the actual emitted stdlib output for the first time.

## Feedback

### 2026-08-08T11:58:03Z — user

PARTIAL DELIVERY 2026-08-08, commit 0ebec357. The harness EXISTS and works; the ticket's "at least the prelude closure" acceptance is NOT met. Left open deliberately.

DELIVERED: `ScalaCompile` (scaland/core/src/test/.../ScalaCompile.scala) runs `dotc.Driver` in-process and fails with the compiler's own diagnostics. Answers to the ticket's three open questions:
  * HOW: `scala3-compiler % Test` + programmatic Driver, not scala-cli. Hermetic, pinned to `scala3Version`, cannot be absent — so there is no skip-when-unavailable branch, which the ticket specifically warned against. Classpath comes from a `Test / resourceGenerators` file, NOT `java.class.path`: sbt runs tests unforked, so that property is the launcher's classpath and carries no scala3-library.
  * WHICH: the `eq` closure (PartialEq/Eq/NonEq), compiled as ONE invocation because Eq and NonEq name PartialEq.
  * EXCLUSIONS: not yet answered, because the closure is not yet wired — see below.

FOUND AND FIXED, a THIRD defect independent of WI-1021 and WI-1022: a sort declaring no operations emitted `trait Eq[T] extends PartialEq[T]:` over a lone `// (no operations)` comment. Scala requires a `:`-opened block to contain a definition, so stdlib's Eq.scala has been shipping as `indented definitions expected, eof found`. The existing WI-170/WI-644 string test was green on it. Fixed in `renderMainSort`'s Algebra branch.

THE CONTROL IS THE TICKET'S OWN ARGUMENT: back the one-line fix out and the compile test fails naming the compiler's error, while the other 18 tests — including the string-match test over that same file — stay green.

MEASURED, and it resizes the remaining work: compiling each prelude file STANDALONE, 16 of 44 compile and 27 do not. Cross-file refs inflate that (Eq/PartialEq/Ord "not found" would resolve in a closure compile), but the real defect families are much wider than WI-1021+WI-1022:
  * `Missing type parameter for [T1, T2] =>> (T1, T2)` — pair.anthill (WI-1021)
  * `cannot determine type argument for enum parent` — List, Option (WI-1022 family)
  * `Too many type arguments for LazyList[A]` / `Iterable[A]` — Stream, Iterable, collection, finite_collection, mutable_collection, combinators
  * a sort's OWN name unparameterised — Cell, FiniteStream, Relation, LogicalStream
  * unmapped reflect types (`Term`, `NodeOccurrence`, `Type`) — effects, meta, sort
  * unbound higher-kinded params (`Not found: type A`/`type M`) — Monad, DelayMonad
  * a SYNTAX error, `'=' expected, but identifier found` — Numeric.scala:8

WHAT REMAINS FOR THIS TICKET: wire the closure compile over the prelude and name every excluded file with its reason. That cannot land before the defects above are fixed, or the test cannot pass — so the closure ships WITH the fixes, not ahead of them. WI-1021/WI-1022 are now unblocked (they depend on this ticket for the harness, which exists). The Numeric.scala syntax error and the own-name-unparameterised family look like they need their own tickets; the latter may be the same root as WI-1021 (a sort's own name reaching TypeGen without its parameters).

NOTE for whoever finishes it: the harness catches "does not compile", NOT "compiles and means the wrong thing". Measured: `enum Pair[A, B]: case Pair(...)` — the nested Pair.Pair that §6.3 rules out — compiles clean. Said at the ScalaCompile site.

### 2026-08-08T12:05:47Z — user

ORDERING, so nobody wires it backwards: WI-1054 (hyphenated identifiers) and WI-1055 (fix-or-refuse) are NOT blocked on this ticket. The harness they need is already committed (0ebec357); what is left here is the prelude CLOSURE test, and that cannot be written until WI-1055 supplies the refusals that give the closure a named exclusion set. So the edge runs the other way in practice: finish WI-1054/WI-1055, then close this. Deliberately left as description text rather than a --depends edge, since an edge blocks claiming.

### 2026-08-08T12:13:30Z — user

CORRECTION to the ORDERING note above: the ordering is now a real --depends edge, WI-1020 -> {WI-1054, WI-1055}, not description text. The earlier note undersold it. It IS a technical prerequisite, not soft ordering: with the fix-or-refuse policy decided (WI-1055), the closure test gets its named exclusion set from the refusals, and hyphenated identifiers are in that policy Group A (FIX, not refuse), so numeric.anthill has to genuinely compile before the closure can be green. The direction that would deadlock is the reverse one — WI-1054/WI-1055 must NOT depend on this ticket, since the harness they need is already committed at 0ebec357.

One overstatement in that note, corrected: "cannot be written until WI-1055 supplies the refusals" is too strong in the abstract — a test-side exclusion list would also satisfy the "no silent skips" wording. It is the DECISION to refuse rather than exclude that makes WI-1055 a prerequisite, not a logical impossibility.

### 2026-08-08T14:45:17Z — user

WI-1055 delivered (2026-08-08, commit c5f23975). The REFUSAL SET this ticket needs now exists, and the closure is close but NOT yet green — here is exactly what is left, measured.

THE NAMED EXCLUSION LIST is live and asserted, in BootstrapTest 'WI-1055: the prelude refusal set is a NAMED list, and the compiling count is a floor'. Thirteen prelude files, each paired with the construct that defeated the emitter, so adding a refusal is an edit there rather than a quiet shrinking of the output. Consume that set: what this ticket compiles is its complement.

MEASURED, prelude: 18 -> 24 files compile standalone; 0 -> 13 refused; the whole-closure compile went 415 errors -> 11.

THE 11 REMAINING, all owned, none of them WI-1055's to fix:
 1. Numeric.scala:8 '=' expected — `zero-val` is not a Scala identifier. WI-1054.
 2. 7 errors: PersistentCollection and MutableCollection extend `anthill.prelude.Iterable`, which nothing emits because iterable.anthill is REFUSED on the `Stream[Element, E]` -> one-parameter `LazyList` arity. WI-1021.
 3. 3 errors: MutableCollection names `anthill.prelude.Modifiable`, declared in effects.anthill, which is REFUSED because it imports `NodeOccurrence`/`Term` from `anthill.reflect` — a package OUTSIDE the prelude closure. Either widen the closure to include stdlib/anthill/reflect.anthill, or keep effects.anthill in the exclusion list and accept that its dependents go with it. That is a scoping decision for THIS ticket.

A CASCADE IS NOT SELF-DETECTING, and it is the one gap to plan for: 2 and 3 are files that would compile if the file DECLARING their sibling had not been refused. Bootstrap is per-file (proposal 034) so it cannot see this; only this ticket, which holds the whole set, can. Expect to compute the transitive closure of the refusal set rather than assuming 'not refused' means 'compiles'.

WHAT MAKES THE MISSING NAMES LOUD: an unplaced same-package name is emitted FULLY QUALIFIED (`anthill.prelude.Iterable`), not bare. A bare mention resolves against Scala's root imports, so an absent sibling CAPTURES instead of failing — measured, field.anthill's bare `Numeric` compiled green against `scala.math.Numeric`. So every cascade now surfaces as 'type X is not a member of anthill.prelude' naming the exact missing declaration, instead of an arity error against the wrong type.

ALSO NOTE the floor assertion (>= 16 compiling standalone) already lives in BootstrapTest and is deliberately `>=`, not `==`: WI-1054 and WI-1021 each raise it, and a pinned number would fail on being improved.

### 2026-08-08T17:12:00Z — user

CORRECTION after WI-1021 delivered (2026-08-08). The previous note attributed 7 of the 11 remaining closure errors to WI-1021 ("iterable.anthill is REFUSED on the `Stream[Element, E]` -> one-parameter `LazyList` arity"). WI-1021 fixed that entry and the cascade DID NOT MOVE -- measured, still 13 refusals and still 11 closure errors.

WHAT ACTUALLY HAPPENED: the arity refusal was hiding a second one in the same file. `iterable.anthill`'s refusal moved from `operation iterator` (line 21) to `operation map` (line 61), which writes `Stream[Dst, {E, EffP}]` -- an effect ROW in a type-argument slot, the WI-1055 B2 refusal. Same for combinators.anthill. So the owner of the `Iterable` cascade is now WI-1062 (what an effect PARAMETER erases to under scala_std, or that it cannot), not WI-1021.

THE EXCLUSION LIST THIS TICKET CONSUMES CHANGED ACCORDINGLY: `combinators.anthill` and `iterable.anthill` are now recorded in BootstrapTest's named refusal set with reason "effect row", not "`Stream` maps to Scala `LazyList`". The set membership is unchanged -- thirteen files.

WHAT IS LEFT FOR THIS TICKET, unchanged in substance and now correctly attributed:
 1. Numeric.scala:8 `zero-val` is not a Scala identifier -- WI-1054.
 2. 6 errors: PersistentCollection/MutableCollection name `anthill.prelude.Iterable`, which nothing emits -- WI-1062.
 3. 3 errors: MutableCollection names `anthill.prelude.Modifiable` from the refused effects.anthill (`anthill.reflect` import) -- still THIS ticket's scoping decision (widen the closure to include stdlib/anthill/reflect.anthill, or keep the exclusion and accept the dependents go with it).
 4. 1 error: MutableCollection "is extended twice", downstream of 2 and 3.

ALSO NOTE for the floor assertion: standalone-compiling prelude files went 24 -> 17 under WI-1021, and that is not a regression -- all seven stopped CAPTURING host types (`scala.Option`, `scala.List`, `Tuple2`) and now honestly name their prelude siblings. The `>= 16` floor holds and the reason is recorded at its test site. The closure error count, which is the number that means something here, is unchanged.

