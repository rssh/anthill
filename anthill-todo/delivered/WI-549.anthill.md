## Attributes

- id: WI-549
- created: 2026-06-22T03:29:15Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-23T22:22:40Z

- acceptance: cargo-test

- tags: wi541

## Description

Reflect bridge: implement KB.add_guard via a LogicalQuery-enum -> structured guard-Value reifier (split from WI-546). bridge.rs add_guard still panics (WI-549). It must convert the generated reflect LogicalQuery ENUM (negation/conjunction/pattern_query/sort_query/forall_q/no_q/... ~18 variants) into a KB Value built from the anthill.reflect.LogicalQuery.* constructors, so the KB's add_guard_labeled + collect_trigger_sorts (which walk pattern_query/negation/quantifier nodes via LogicalQuerySymbols) + evaluate_guard recognize it. This is DISTINCT from the bridge's query_to_goals, which FLATTENS a LogicalQuery to resolver goals (unwrapping pattern_query) — wrong for guards. Key facts (from WI-546 investigation): only QUANTIFIED/negation guards are enforced (a bare pattern_query/conjunction vacuously holds, evaluate_guard kb/mod.rs); guard registration needs the reflect stdlib loaded (LogicalQuerySymbols); ConstraintId is a generated unit struct so add_guard just returns ConstraintId. Scope: a reflect-LogicalQuery -> Value reifier in bridge.rs (resolve the LogicalQuery.* ctor syms once), then kb.add_guard_labeled(value, None). Acceptance: bridge.add_guard(negation/no_q over a pattern) registers a guard whose subsequent violating bridge.assert returns None; unit test; cargo-test green. WI-546 already delivered assert + kb() and proved the assert<->guard path via a source constraint.

## Changes

### 2026-06-23T22:22:37Z — feedback — user

DELIVERED. Bridge KB.add_guard implemented: reify_logical_query converts the generated LogicalQuery enum into a carrier-faithful Value::Entity over anthill.reflect.LogicalQuery.* ctors (mirrors interpreter eval-side builders + WI-513 carrier-agnostic lower_query_with/collect_trigger_sorts read it via TermView like the loader's hash-consed form), then kb.add_guard_labeled(value, None). Reify is total over all 18 variants; quantifiers (forall/some/one/lone/no) delegate to reify_quantifier (var=Symbol ref, inert to eval), conjunction/disjunction to reify_binary, sort_name=Value::Str (matches both readers), pattern term rides verbatim (Node-faithful). lq_ctor/reify_string_list panic loudly if reflect/prelude stdlib absent. code-review (xhigh, 2 finders) caught 1 REAL bug: add_guard accepted aggregation variants (count_q/sum_q/min_q/max_q) which the engine can't lower -> a DELAYED misleading panic at the next matching assert (assert_checked assumes load-time check_all_guards ran, which never does on the bridge path; loader avoids it via build_logical_query->None for Aggregation). FIX: reject aggregations loudly+early at reify (not boolean constraints). 3 tests: negation-rejects-violating-assert, no_q-rejects-2-cycle (quantifier path, shared ?x), aggregation-rejected-early (#[should_panic]). anthill-stl 36/0, full workspace suite exit 0 green.

