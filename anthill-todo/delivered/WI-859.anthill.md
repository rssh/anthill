## Attributes

- id: WI-859
- created: 2026-07-30T11:56:39Z

- status: Delivered
- status_agent: claude
- status_at: 2026-08-05T05:44:52Z

- acceptance: cargo-test

- tags: modinst

## Description

Proposal 058 PHASE 8a — the coherence grouping's MISSING CANDIDATE KIND: a SELF-PROVIDER is a candidate of neither kind (measured at WI-855's delivery: WI-838's one-grouping-per-(spec, dispatch carrier) admits a candidate only as an instance FACT — provision_binds_any_op — or as a WITNESS — witness_dispatch_carrier, which returns None exactly when the provider IS the carrier). So sort Leaf { provides Desc[T = Leaf] } plus an abstract rival forms a group of ONE and the pair loads, tying only at dispatch (the_tie_is_invisible_to_load_coherence_either_way pins it). WORK: add the third candidate kind — a provision of an op-bearing spec whose carrier IS the provider, its ops the provider's own members — to the WI-838 grouping, with is_nameable answered (a self-provider is nameable by the carrier's own name). WHY: it is the PREREQUISITE of phase 8b's one_default load check (the inferred default row is keyed on exactly this candidate), and it independently closes the recorded load-time blind spot. GUARDRAIL — enumerate the verdict matrix and MEASURE each cell before believing it (the WI-805 lesson): self+witness must KEEP loading (tier-3 coexistence with a nameable rival is the point; the runtime tie is phase 8c's to retire via the default rung); self+instance-fact — decide by measurement, minding that both have sort_ref = carrier so WI-842's ConflictingProvisionBindings bucketing may already own the within-carrier conflict; witness+fact stays MixedProviderKinds. This phase changes VISIBILITY, not verdicts, unless a measured cell says otherwise — record any verdict change explicitly. ACCEPTANCE: the WI-855 pin still loads and still ties at dispatch (unchanged behavior, new visibility); a probe shows the group counting 2 candidates for self+rival; the verdict matrix documented at the grouping; full workspace green via rustland/scripts/test.sh. REFERENCE: 058 §3.6; design/058-implementation.md §3, §6; the pre-restructure §4.9's recorded-not-filed increment.

## Changes

### 2026-08-05T05:44:49Z — feedback — user

DELIVERED. `Provider::SelfProvider` is the third candidate kind, keyed at `canonical_sort_sym(provider)` — the carrier `provision_supplier` (dispatch) already keys it at, so the load check and the dispatch reader cannot answer different carriers. Criterion is `witness_dispatch_carrier == None`, one owner, which also folds in the bare / own-param-bound provision (`provides Spec`, `provides Spec[T = OwnParam]`) since dispatch keys those at the provider too. `is_nameable` = TRUE: the carrier's own name is the bracket that selects it. The op-less-spec exemption is shared with the witness leg; the CONCRETE-provider exemption is deliberately NOT applied — a self-provider is not a rival backend but the carrier the group is keyed ON, and applying it would leave the kind empty (WI-855's `Leaf` is concrete).

VERDICT MATRIX — measured cell by cell over the stdlib and the WHOLE corpus in one instrumented suite run, BEFORE any arm was written (group observations): (0,>=1,0) ADMIT 37; (0,>=1,1) ADMIT 40; (1,0,1) ADMIT 2; (>=2,*,*) AmbiguousInstanceFact 4; (1,>=1,0) MixedProviderKinds 4; (1,>=1,1) absent from the corpus. NO VERDICT CHANGED — 40 groups that were groups of ONE now hold two, and 2 more reach the new admission arm.

THE CELL THAT HAD TO BE DECIDED was (1 fact, 0 witnesses, 1 self), and the measurement decided it AGAINST the naive nameability reading (which says: a group holding an unnameable candidate is refused). The corpus's two instances are the two OPPOSITE shapes. `test.wi837.hidden.Pebble` is a type-only self-provision COMPLETED by a retroactive `fact PartialEq[T = Pebble, eq = pebbleEq]` — ONE dictionary written in two places, the WI-431 shape, which loads clean and whose `eq` ANSWERS. `test.wi837.ownplusfact.Pebble` is the carrier's OWN `eq` member RIVALLED by a fact binding another — already refused, by `AmbiguousEqDispatch`. A group cannot tell them apart, because the question is per-OP and a group is per-SPEC. Refusing the composition would have taken the first with the second. So the cell is ADMITTED (`fact_beside_self_provider_is_one_carrier`) and the RIVAL half stays with the readers that can count per op (058 3.7) — EqDispatchIndex at load for the Eq family, `spec_op_suppliers_for_carrier` at the value-directed read, `UnselectedInstance` at a classified call. Each is DRIVEN in the test, so "someone else refuses it" is measured, not assumed (the rival fixture's actual refusal was measured too: tier 3's `UnselectedInstance`, not eval).

THE COMPOSITION SPACE IS TILED AND ENFORCED, not argued: a `debug_assert` fires when a group is admitted by neither arm and named by neither diagnostic. DRIVEN — deleting the second admission arm makes it fire on both wi837 `Pebble` fixtures, which is what keeps that arm from being decorative rather than a comment with an `if` around it.

ACCEPTANCE. WI-855's pin still loads and still ties at dispatch — but its test was RENAMED (`the_tie_is_invisible_to_load_coherence_either_way` -> `load_coherence_admits_the_tie_either_way`): "invisible" became FALSE for the abstract arm, which now forms a group of two admitted by nameability, while the concrete arm still forms a group of one via the witness exemption. Two reasons, one verdict. Three docs that asserted the retired claim were corrected in the same commit (wi855's fixture docs, wi842's RIVAL doc, typing.rs's unit-test SRC doc) — the claim was load-bearing prose in each. The probe is `provider_coherence_candidates`, rendered `kind:qualified-name`, and it shows the (Desc, Leaf) group counting 2 for self+rival with the sole-provider control at 1. Matrix documented at the grouping and in design/058-implementation.md 11. Full workspace green.

BOUNDARY RECORDED, NOT TAKEN (review finding). Kind 3's criterion is one notch coarser than its dispatch twin: `SelfProvider` is recorded from the PROVISION, while `SupplyRoute::Own` is derived from `carrier_own_op` — "the carrier actually has a member". Gating kind 3 on the carrier owning at least one of the spec's ops would split the (1,0,1) cell BY CONSTRUCTION (completion -> group of one; rival -> group of two, seen) and delete the admission arm — but the rival half would then need a VERDICT of its own, a NEW LOAD REFUSAL, which is the one thing this phase scoped itself out of. Phase 8b meets the same fork when `self_provides` has to say whether a carrier owning no member self-provides at all.

FOR 8b (WI-860), stated so it is not inherited by accident: do NOT read this grouping for `self_provides`. It carries coherence POLICY — the op-less-spec exemption drops a self-provision whose spec declares no ops, and the concrete exemption reshapes the witness leg — and a defaults relation must inherit none of it. The policy-free classifier is `witness_dispatch_carrier` over `provisions_of_spec`.

SPUN OFF: WI-1010 — a spec op's DEFAULT BODY silently shadows a retroactive instance fact's op binding, so a WI-431 instance of a carrier whose spec op defaults is INERT. Measured three ways while looking for a non-eq driver for the completion cell (answers 1 with a self-provision AND without one; the carrier's own member correctly answers 7). WI-444's "defaults fill gaps, they do not shadow" is wired to route 1 and never to route 2, and WI-876's default bodies on the ordering family gave that collision real carriers.

