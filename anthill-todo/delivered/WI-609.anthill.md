## Attributes

- id: WI-609
- created: 2026-07-01T04:41:13Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-01T23:24:27Z

- acceptance: cargo-test

- tags: finiteness

## Description

Typer (WI-357/594 effect-threading family, CALL-RESULT location): thread the RESULT effect of an abstract spec-op called on a spec-typed field receiver. Reproducer: a thin finite combinator whose collect body does mapElems(collect(src), fn) where src: FiniteCollection[C=SrcC, Element=Src, E=ES] (a destructured carrier field). collect(src) does NOT thread ES into collect's result effect -> 'collect.effects: expected [ES,EF], got undeclared ?_'. The receiver's written type-arg E=ES must flow into the abstract spec-op's declared 'effects E' at the call result. Distinct from gap-1 (WI-599 carrier_arg_provision_projection, which is a CONSTRUCTOR-ARG threading). Prereq for the WI-599 thin stdlib rewrite. ACCEPTANCE: the collect body over an abstract FiniteCollection field type-checks with the source effect threaded; a focused typer test + cargo-test green.

## Changes

### 2026-07-01T23:24:27Z — feedback — claude

DELIVERED (code 69f0280). ROOT: collect(src) on src:FiniteCollection dispatches FiniteCollection.collect -> spec_sort==carrier_sym==FiniteCollection (REFLEXIVE; a spec doesn't provide itself). transitive_provision_view (provides-only) + WI-608 abstract_spec_required_view (requires-entries-only) both returned None -> collect's declared 'effects E' never grounded to recv's E=ES -> 'collect.effects: expected [ES], got undeclared ?_'. FIX: the reflexive case needs NO provider view — the receiver's own written type-args ARE the spec's params (same canonical VarIds, since WI-600's parameterized_vid_bindings keys by carrier_sym==spec_sort). Added (1) a reflexive branch in bind_spec_params_from_carrier_param that binds each recv_binding directly (skipping carrier_pvid==C, which binds by arg unification), and (2) a reflexive branch in carrier_param_receiver's .or_else returning an EMPTY view + transitive=true to engage that path + the carrier_is_abstract_spec deferral gate. Sibling of WI-608's requires-view. Grounding Element/E ALSO discharges the WI-599 gap-2 requires-coverage error (the dispatch goal is no longer abstract-uncovered) — so both the free-op (op-level requires) and sort-member (sort-level requires) shapes load. Gated on carrier_is_abstract_spec -> concrete self-collect (List.collect on List) unaffected. VERIFICATION: 5 correctness concerns cleared (carrier_pvid==recv C vid via resolve_sort_alias; parameterized_vid_bindings filters to ground Value::Term; empty-view consumers all safe; transitive_provision_view never yields carrier==spec so carrier==spec<=>empty-view). Full suite 2410/0. /code-review reviewer HUNG on infra (2h+, killed) — relied on manual+empirical verification instead. Rebased context: builds on WI-600 canonical-VarId matching + WI-608. UNBLOCKS the Claimed WI-599 thin stdlib rewrite (last typer prereq). Repro: wi609_collect_effect_over_finite_collection_test.

