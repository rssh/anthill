```anthill
fact WorkItem(id: "WI-498", created: "2026-06-17T04:05:41Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-17T06:06:08Z"))

fact Feedback(workitem: "WI-498", author: "user", at: "2026-06-17T06:06:07Z")
```

## description

Route the persistence/deserialization entity-term rebuilders through KB::make_entity_term so they canonicalize named args to DECLARED field order (the WI-299 funnel), not Symbol::index() interning order. PROBLEM (same hazard class as WI-299, found by its code-review, but in the persistence layer rather than the make_* term builders): three sites in persistence/term_ser.rs hand-build entity Term::Fn sorting named_args by sym.index() only — entity_to_term deserialize (term_ser.rs:307, arbitrary user-entity facts), list_to_term (term_ser.rs:415, cons cells), json_object_to_ctor_term (term_ser.rs:470, named-constructor / enum-variant terms). The loader canonicalizes source-loaded facts to declared field order via entity_field_names (load.rs), and the discrim matcher descends named keys POSITIONALLY, so a fact RELOADED from a persisted store (index-order) would silently fail to hash-cons / discrim-match the SAME fact loaded from .anthill source (declared-order) for any entity whose field interning order != declaration order. Coincidentally safe today (field interning usually happens in declared order), exactly the latent fragility WI-299 eliminated for the builders. SCOPE: these are &mut KnowledgeBase contexts already calling kb.alloc(Term::Fn), so replace each ad-hoc sort+alloc with kb.make_entity_term(functor, pos, named). SEPARATE/NUANCED (evaluate, do not blind-reroute): load.rs:7901 assemble_sort_view_value sorts SortView bindings by sym.index() DELIBERATELY for WI-449 spec byte-identity — its named args are TYPE-PARAM bindings (not declared SortView fields), single-field today; routing through sort_named_canonical would fall back to index() and be equivalent now, but the intent differs, so handle with care or leave with a clarifying note. ACCEPTANCE: term_ser deserialize round-trips an entity whose declared field order != interning order and the reloaded term hash-cons-matches the source-loaded form; cargo-test green.

## Feedback

### 2026-06-17T06:06:07Z — user

DELIVERED 2026-06-17 (uncommitted at write time). Routed the 3 persistence entity-term builders in persistence/term_ser.rs through kb.make_entity_term (the WI-299 canonicalizing funnel) instead of ad-hoc sort_by_key(sym.index())+alloc(Term::Fn): (1) load_entry entity deserialize, (2) array_to_list_term cons cells, (3) build_constructor_term named-ctor/enum-variant arm. make_entity_term -> sort_named_canonical sorts to DECLARED field order when entity_field_names is registered, else falls back to index() (== old behavior), so it's a strict consistency improvement matching the loader (convert_term canonicalizes fact heads to declared order) and the discrim matcher (descends named keys positionally). SortView site (load.rs assemble_sort_view_value) LEFT AS-IS with a clarifying note: its index sort is deliberate (WI-449 byte-identity); its named keys are the viewed spec's TYPE-PARAM bindings (T/E), NOT declared SortView fields, so routing through the funnel would mis-canonicalize them against SortView's own field schema. Single-field shorthand + positional arms intentionally not routed (1 named arg is trivially canonical; positional has no named args). TEST: wi498_deserialized_entity_matches_source_loaded_form (toml_ser_test.rs) — top-level entities (Pre interns aafield first => lower index than zzfield; Rec declares zzfield first) so declared order [zzfield,aafield] REVERSES index order — same shape as the real WorkItem entity (id declared first, interned after description/status). Asserts the TOML-reloaded fact hash-cons-matches the source-loaded fact head; precondition asserts index!=declared so it can't pass vacuously. VERIFIED the test catches the bug: temporarily reverting site 1 to index-sort makes it FAIL. Full workspace green (56 binaries, 2022 tests, 0 fail) via scripts/test.sh; /code-review (parallel correctness + cleanup/altitude/conventions) clean, 0 findings. NOTE: an adjacent pre-existing gap (serialize skips none()-valued Option fields + deserialize doesn't fresh-var-fill absent fields, so absent-Option facts don't round-trip-match) is orthogonal to this ordering fix — not addressed here.

