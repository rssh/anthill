## Attributes

- id: WI-077
- created: 2026-04-23T19:38:56Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-20T16:56:49Z

- acceptance: cargo-test

## Description

Long-stream stress test for execute_logical_query / SearchStream: build a KB with N (>=1000) matching facts (or a recursive rule yielding N solutions), wrap the resulting SearchStream as StreamSource::Resolver, drive splitFirst to exhaustion, and assert (a) all N solutions surface, (b) stream arena slot is reclaimed (stream_arena_live_count returns to 0), (c) memory/perf does not blow up. Current coverage tops out at 3 solutions on Resolver streams (eval_m4_test.rs); infinite-stream tests only exercise StreamSource::Native, not the resolver path. Should also gate disjunction work (WI-075) so the same harness can verify MPlus over long branches.

## Changes

### 2026-06-20T16:56:43Z — feedback — claude

DELIVERED: added anthill-core/tests/include/wi077_long_stream_stress_test.rs (registered in wi_tests.rs). Two tests drive the StreamSource::Resolver arena path at N=1000. (1) wi077_resolver_long_stream_surfaces_all_n_solutions: 1000 facts item(group:0, key:0..999), query item(group:0, key:?k) -> asserts count==N (declaration fact excluded because literal Int head can't unify with sort_ref(Int64), per kb_query_test pattern), stream_arena_live_count returns to 0 after handle drop, subst_arena_live_count stays 0 (per-yield Value::Substitution freed each iteration => no O(N) blowup). (2) wi077_mplus_over_long_branches_surfaces_all_solutions (WI-075 gate): MPlus over two 1000-fact resolver branches -> count==2N, all arena slots reclaimed via cascade. Full anthill-core suite green (810 passed/0 failed). /code-review max-effort: 0 correctness bugs (count, refcount traces, subst reclaim, declaration-fact exclusion, non-vacuity all independently verified); 1 cleanup applied (raw-string header instead of \x20 escapes). Tests run in 0.38s -> perf does not blow up.

