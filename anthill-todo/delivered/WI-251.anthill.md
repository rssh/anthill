## Attributes

- id: WI-251
- created: 2026-05-15T21:05:46Z

- status: Delivered
- status_agent: claude
- status_at: 2026-05-16T10:28:35Z

- acceptance: cargo-test

- depends_on: WI-249

## Description

Wave 4 — final cleanup of NodeOccurrence migration. Once typer (WI-247), eval (WI-248), cpp-gen (WI-249) migrated: (a) reflection ops body_of/head_of/args_of/sub_occurrences produce Value::Node(Rc<NodeOccurrence>) instead of Term-encoded results; replace the materialize_from_handle helper with native NodeOccurrence access (loader will build NodeOccurrence trees directly in convert_expr_term). (b) Delete OccurrenceStore, OccurrenceId, ExprOccurrenceId, HandleKind::Occurrence (and its construction site in load.rs::convert_expr_child), split_handle and resolve_handle in typing.rs, kb.occurrences field on KnowledgeBase. (c) OperationInfo.body field deleted from the OperationInfo fact entity (replaced by kb.op_bodies lookup). OpInfoRecord.body and body_occ fields deleted; body_node remains. (d) Rename stdlib reflect 'sort Occurrence' to 'sort NodeOccurrence' (stdlib/anthill/reflect/reflect.anthill); update all consumers. (e) Eliminate the double tree walk (deferred from WI-242 simplify pass) by building NodeOccurrence trees directly in convert_expr_term instead of post-walking via materialize_from_handle. Depends on WI-247, WI-248, WI-249 (all consumers migrated). Acceptance: 'cargo test' green; rg 'HandleKind::Occurrence' returns no matches; rg 'OccurrenceStore' returns no matches outside the deleted occurrence.rs.

## Changes

### 2026-05-16T10:28:55Z — feedback — user

DELIVERED. Phase 1 commit 95db037 + Phase 2 commit 4e22524. Acceptance met: 0 matches for HandleKind::Occurrence; 0 matches for OccurrenceStore outside kb/occurrence.rs (now 30 lines holding just PassId). Workspace tests via scripts/test.sh: 45 binaries green, ~2600s. Span infrastructure migrated to kb.term_spans/functor_spans side-tables. OccurrenceStore family deleted; Handle wrapping in convert_expr_child deleted; materialize_from_occurrence deleted; split_handle/resolve_handle deleted; OpInfoRecord.body/body_occ deleted. req_insertion walks kb.op_bodies via visit_classifications. typing_pass_spec_parses_and_loads runs with a 32MiB spawned-thread stack to fit deep NodeOccurrence materialization. Stdlib reflect 'sort Occurrence' rename deferred (cascades through ~26 reflect.anthill sites). Diff -540 LOC net. CLAUDE.md updated to prefer scripts/test.sh.

