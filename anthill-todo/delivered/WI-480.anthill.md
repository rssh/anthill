```anthill
fact WorkItem(id: "WI-480", created: "2026-06-15T17:11:39Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-468"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-16T08:51:48Z"))

fact Tag(workitem: "WI-480", name: "typing")
```

## description

Typer/WI-401 escape-gate gap (sibling of WI-468): a DESTRUCTURING-pattern let annotation launders a provider upcast. Confirmed false-accepts today: tuple 'let (s,_): (KVStore,Bool) = (m,true) ; s', constructor 'let boxed(s): Box = boxed(m) ; s', and OPAQUE-value 'let (s,_) = mkpair(m) ; s' all LOAD clean (verified via CLI; the direct Pattern::Var case correctly errors post-WI-468). ROOT CAUSE: branch_leaf_abstracting_return_error's let_bound_var_name returns None for any non-Pattern::Var binder, so destructured names are NOT tracked in LeafScope; the returned component var falls back to its laundered annotation-component type and slips same_symbol. FIX DIRECTIONS: (a) pattern-directed value-component projection -- extend the LeafScope tracking so a Pattern::Tuple/Constructor binder maps each destructured name to the corresponding LITERAL value-component node (tuple element / constructor field, recursively for nested patterns); the see-through then reaches the concrete provider. (b) the OPAQUE-value case (value is a call returning the tuple/entity, no literal element node to project) CANNOT be seen through structurally -- it needs the ticket-WI-468 alternative 'annotation-as-abstracting position' approach: treat a RETURN-position destructured var whose annotation-component upcasts a concrete provider to a bare/partial spec as itself an abstracting position. Must NOT reject a destructured value NOT in return position, nor same-sort/manifest/ensures cases. Verify wi401/wi402/wi457/wi468 stay green. Rust-only (typer).

