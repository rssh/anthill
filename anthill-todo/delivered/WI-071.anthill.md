## Attributes

- id: WI-071
- created: 2026-04-20T06:54:22Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-17T18:18:15Z

- acceptance: cargo-test

## Description

Grammar: add bare-Type positional SortBinding. Spec (kernel-language.md:481-483) declares three SortBinding forms: Name=Type (named), Type (positional), VariableTerm. The tree-sitter grammar (tree-sitter-anthill/grammar.js:121) only implements Name[=Type] and variable_term. This blocks bracketed-type positional bindings like Effect[Output[?]] or List[Option[Int]], which must be written in named form today (Effect[T = Output[?]]). Fix: extend sort_binding choice 2 from variable_term to a general _type, updating the converter to emit positional bindings. Update existing stdlib opportunistically. Acceptance: 'effects Output[Console]' works for bare names (already parses) AND nested like 'fact Effect[Output[?]]' parses and lowers correctly; existing uses unchanged.

## Changes

### 2026-06-17T18:18:13Z — feedback — user

Already implemented incidentally by the WI-302/WI-375 sort_binding generalization: grammar sort_binding choice 2 is now the general _common_type_expr (grammar.js:136-139), convert_sort_binding handles all three spec forms (convert.rs:571-596), and type_expr_to_term maps positional bindings to declaration-order params via type_params_of_sort. Acceptance pinned by tests/include/wi071_positional_sort_binding_test.rs (3 tests, all green). Stdlib already relies on nested positional bracketed bindings (fact PersistentCollection[List[T], T]; Option[Pair[?A, LogicalStream[?A]]]). No remaining code work; delivered as tracker reconciliation.

