## Attributes

- id: WI-595
- created: 2026-06-29T09:27:08Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-01T03:05:23Z

- acceptance: cargo-test

- depends_on: WI-292

- tags: wi502

## Description

WI-292 PHASE 3 -- efficiency / carry-sites (correctness already shipped on-demand). (1) Stamp typed() at the apply_eq_rules/simplify ENTRY so the redex carries its ty (compute-once) instead of value_type_term recomputing structurally per candidate. (2) The 4 WI-578 carry-sites (reify_value, close_value_head_debruijn, rewrite_ref_value, walk_value_to_resolved) must carry+sigma-refine ty so the stamp survives reconstruction. (3) Cache the sort_provides SortProvidesInfo edge-set (rebuilt per call today; now driven from the resolver loop). (4) Thread the resolver frame subst into apply_eq_rules so a constraint-store-typed UNBOUND carrier var is decidable (today &Substitution::new() -> headless -> conservative suspend; sound but incomplete -- C1). Also fold the D1/C4 double lookup_spec_op_dispatch (gate + core) into one by passing spec_sort into simp_guard_holds_core.

## Changes

### 2026-06-29T15:05:18Z — feedback — claude

PARTIAL (2026-06-29, commits 69db1a3 part5 + 27be6d3/merge 5601894 part4+refine; full suite green). DELIVERED the TYPE-READ COMPLETENESS half: (part 5) folded the double lookup_spec_op_dispatch -- simp_guard_holds_core now takes the resolved spec_sort, each caller (typer/resolver) keeps its own non-spec-op default; (part 4) apply_eq_rules now takes the live frame subst (the resolver loop threads self.stack.last().subst, an O(1) imbl clone; simplify+tests pass empty), so a constraint-store-typed carrier var in the redex is decidable (C1); (NEW, via design dialogue) var_type_term refines a BOUND var: meets the bound value's own type with the carrier var's store constraint(s) over the resolved chain. /code-review (focused) caught a DORMANT soundness defect -- meet_types returns the CONSTRAINT when it is a strict SUBSORT ((false,true)=>b), so a List value + NonEmptyList bound would move the head and wrong-fire; FIXED by PINNING the refine to value_ty's authoritative sort head (apply only meets that keep the head = sharpen type-params; never change it; headless stays headless; deterministic chain order). 2 tests (wi578: refines nil+List[Int64]->List[Int64]; ignores Int64+bogus-List[Int64]). KEY FINDINGS for the REMAINING parts: the Value::ty CARRY machinery is INERT in prod (typed() has ZERO prod callers -> value_type_term always recomputes structurally; the carry-sites drop None->None as no-ops); the refine only sharpens type-PARAMS (sort head invariant) so it is inert for the head-reading simp consumer but correct for param-readers; reify's fn_value COLLAPSES an all-Term redex back to a Value::Term (not an Entity), so part 1 must build the typed Entity EXPLICITLY (not via reify) to get per-arg ty; the carry-sites are NOT on the apply_eq_rules path (part 1's typed Entity is used locally), so they are separate infra. REMAINING (pure efficiency, no behavior change): (1) compute-once typed Entity at the apply_eq_rules entry so per-arg carrier sorts compute once across candidates; (2) the 4 carry-sites (reify/fn_value, close_value_head_debruijn, rewrite_ref_value, walk_value_to_resolved) sigma-REFINE ty (user chose sigma-refine over verbatim; helpers close_value_type/subst_value_type/open_value_type exist); (3) cache the sort_provides SortProvidesInfo edge-set (rebuilt per call today). Still Claimed/in-progress.

### 2026-06-29T16:03:30Z — feedback — claude

PART 1 DELIVERED 2026-06-29 (commit cc5ecfd; suite 2386 green; /code-review high, 4 finders): apply_eq_rules computes the requires-guard (simp_requires_guard_holds) at most ONCE per redex (lazy memo over the candidate loop) instead of per requires-guarded candidate -- the decision reads only current+subst (rid never enters it), so behavior-identical (all finders + suite confirm). Parts 2+3 DEFERRED after review. (2) carry-sites sigma-refine is INERT: typed()/Value::typed_term have ZERO prod callers, so ty is always None and the 4 reconstruction sites (reify_value, close_value_head_debruijn, rewrite_ref_value, walk_value_to_resolved) carry None->None; sigma-refine = untestable dead code, revived only by a future ty-stamping producer. (3) sort_provides edge-set memo was implemented (count-keyed) then REVERTED: 3 independent finders confirmed a latent ABA soundness hole -- a balanced RUNTIME retract+assert of DISTINCT SortProvidesInfo facts (Store.retract/insert builtins) restores the active provider-rule count, so a count-keyed memo serves a STALE edge-set; also O(providers)-per-hit (not O(1)) and the invalidate_resolve_cache backstop is dead in prod. A correct cache needs a monotonic fact-mutation EPOCH at the assert/retract choke-point (or invalidation wired at the provides sites) -- a central-fact-path change not justified by the UNMEASURED per-call rebuild cost. OPEN DECISION (user): pursue the part-3 epoch (touches core path; unmeasured gain) OR close WI-595 with part-3 as a future profiling-driven ticket. Recommendation: close (part-2 inert; part-3 deferred-pending-measurement). Builds on parts 4+5+refine (69db1a3/27be6d3).

### 2026-07-01T03:05:23Z — feedback — claude

PART-3 DECIDED: CLOSE (measure-first, per user 2026-07-01). Instrumented sort_provides (edge-rebuild loop timed via atomic counters, reverted after) over representative workloads: (a) full stdlib+github-todo LOAD = 87 calls, 3741 edges scanned, ~20us/call, 43 edges/call, rebuild=1.7ms total (84% of sort_provides time), total sort_provides=2.0ms; (b) real github-todo RESOLVE (open_item+claimable) = 0 calls (simp path not reached); (c) stdlib has ZERO [simp]/[unfold] rules so the resolver directional-rewrite gate fires for no production rule; (d) synthetic worst case (500 [simp] redexes in one program) = 1187 calls, rebuild=37ms (77%). Verdict: the epoch cache saves ~1.7ms on a real load and 0 on resolution; only synthetic hundreds-of-simp-redex programs reach tens of ms (~13.5k redexes needed for ~1s). That benefit does not justify a soundness-sensitive change to the central fact-mutation path (assert/retract/unindex + runtime Store.retract/insert). Filed WI-607 (profiling-gated) to revisit ONLY if a real workload surfaces sort_provides as a hot spot. WI-595 delivered: part 1 (once-per-redex guard memo), parts 4+5 (frame-subst threading + folded spec_op lookup), refine (head-pinned var_type_term) all shipped; part 2 inert (no ty producer); part 3 measured+deferred=WI-607.

