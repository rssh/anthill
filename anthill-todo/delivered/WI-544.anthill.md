## Attributes

- id: WI-544
- created: 2026-06-21T16:56:49Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-21T17:43:33Z

- acceptance: cargo-test

- depends_on: WI-540

- tags: wi541

## Description

Reflect Substitution.compose full union via a binding-enumeration op (WI-541 gap 1). bridge.rs SubstBridge::compose is PARTIAL: through a &dyn Substitution it applies s2 to self's range but cannot merge s2's STANDALONE bindings (the spec trait exposes only apply/compose/lookup — no way to enumerate s2's bindings). The interpreter (anthill-stl builtins.rs subst_compose) does the full union because it holds concrete s2.bindings. Fix: add a binding-enumeration operation to sort Substitution in reflect.anthill (e.g. operation bindings(s: Substitution) -> List[T = pair-of-name-and-Term], or an entries form), regen, implement it in the bridge (SubstBridge has concrete inner) AND in the interpreter builtin, then complete compose's second half (extend with s2's bindings for vars absent in self). Acceptance: compose(s1,s2) merges s2's standalone bindings; unit test; cargo-test green. Note compose is currently unused by the bridge — this is parity/correctness, low live impact.

## Changes

### 2026-06-21T17:43:30Z — feedback — user

DELIVERED (uncommitted, branch main). Added operation bindings(s: Substitution) -> List[Pair[Term,Term]] to reflect.anthill (the dual of lookup — variable rides as a var Term so identity is recoverable across the &dyn Substitution boundary). Rewrote SubstBridge::compose to the FULL union: first-half apply s2 to self's range, second-half merge s2's standalone bindings via bindings()+vid_of, keyed by VarId (self wins on overlap) — mirrors interpreter subst_compose. Implemented bindings in BOTH realizations: bridge SubstBridge::bindings + interpreter subst_bindings (ReflectSyms.pair/f_fst/f_snd). 2 new tests (bridge compose union + interpreter bindings); parity confirmed by code-review. anthill-stl 27/0, full suite 2220/0. code-review surfaced a PRE-EXISTING shared limitation (compose first-half doesn't chase a bare Value::Var binding through s2, same in interpreter) -> loud-documented in compose + filed WI-547.

