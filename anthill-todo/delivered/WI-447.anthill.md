```anthill
fact WorkItem(id: "WI-447", created: "2026-06-12T20:19:51Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-374", "WI-459", "WI-460", "WI-461", "WI-462", "WI-474", "WI-475", "WI-484", "WI-485"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-16T05:34:12Z"))

fact Tag(workitem: "WI-447", name: "typing")

fact Feedback(workitem: "WI-447", author: "user", at: "2026-06-14T10:35:58Z")

fact Feedback(workitem: "WI-447", author: "user", at: "2026-06-14T10:43:02Z")

fact Feedback(workitem: "WI-447", author: "user", at: "2026-06-15T20:13:06Z")
```

## description

Stdlib: revert to the SHORT bare typing forms -- the recorded WI-374 PLAN, now its own ticket. Revert the WI-380 explicit [Elem, Eff] operation-type-parameter signatures (stream.anthill splitFirst/head/tail/takeN/collect/foldLeft/foldRight/find/isEmpty/count; list.anthill iterator/splitFirst/nth/split) BACK to short bare member forms: bare self-sort params (s: Stream -- the sec.3-bullet-1 canonical tie, now ENFORCED per WI-374) with the threading WRITTEN via WI-376 projections (-> List[T = s.T] effects s.E; s.Sort for whole-sort returns) and WI-375 written effect rows where the producer must state E (iterator -> Stream[T = l.T, E = {}]). Bare RETURNS stay forbidden by the type-not-provenance boundary (sec.5: a bare return is ERASED; expansion supplies variables, never values) -- every return writes its relationship via projection, written row, or stays explicit. Mechanisms all delivered: WI-376/396-401 projections, WI-374 site-scoped expansion + enforced member tie + foreign per-occurrence vars, WI-375 rows, WI-424 same-sort rigid seeding for member bodies. ACCEPTANCE: reverted stdlib loads; full workspace suite green; wi368 (length(collect(List.iterator(xs))) pure + element threaded) stays green; an abstract-carrier walk (operation walk(c: C) effects E where C provides Iterable = collect(iterator(c))) stays effect-polymorphic; wi357/wi375/wi424 green. WATCH: effect-rows-on-cross-sort-carriers sensitivity (typing-build-loop.md gotchas) -- verify wi357 + wi210; consumer-side bare params rely on the canonical channel, so the WI-374 enforcement semantics (refinement re-unify, rigid exemption) are load-bearing here.

## Feedback

### 2026-06-14T10:35:58Z — user

BLOCKED — premise 'Mechanisms all delivered' is FALSE. Verified: bare-projection DECLARATIONS load (head/tail/splitFirst-decl/isEmpty, List.splitFirst body via B=List[T=xs.T]), but the consumer/recursive BODIES fail on 4 distinct typer gaps. (A) DOMINANT — projection-neutral identity through call-sites/recursion: a recursive consumer (collect/takeN/count/nth/foldLeft/foldRight) reads rest.T/rest.E off the recursion var (typed Stream[T=s.T,E=s.E] or List[T=xs.T] from splitFirst's explicit-param tail); the projection yields a neutral NOT identified with the enclosing s.T/s.E, so result/effect won't reconcile with the projection return. Isolated repro /tmp/probeD.anthill: recursive collectd(rest) -> 'expected List[T=xs.T], got List[T=xs.T]' (same print, distinct identity). (B) find's callback (x: s.T) -> Bool @ {EffP, -Modify[x]} nests s.T in a denoted-bearing arrow = documented-unsupported WI-376 follow-on (wi376 projection_in_denoted_node_is_rejected). (C) iterator(l: List) -> Stream[T=l.T,E={}] = l: body l:List not admissible-as-Stream (bare element not pinned to l.T for provider-admissibility). (D) split's some((h,t)) vs (xs.T, List[T=xs.T]) -> (?_, List): tuple-LITERAL doesn't thread (Pair-CONSTRUCTOR pair(h,t) DOES). Note: B=s.Sort/xs.Sort is WRONG for recursion-fed tails (opaque neutral won't decompose); B=Stream[T=s.T,E=s.E] / List[T=xs.T] is the right shape but exposes gap A. STOPPED for user decision: build typer prereqs (esp. A, core unify/projection change w/ cascade risk) vs keep explicit [Elem,Eff] forms (work today) vs re-scope. Tree reverted to green; WI-447 left claimed.

### 2026-06-14T10:43:02Z — user

DECISION (user 2026-06-14): BUILD TYPER PREREQS. Filed WI-459 (Gap A — projection-neutral identity through recursion, DOMINANT), WI-460 (Gap B — projection in denoted-bearing arrow / find), WI-461 (Gap C — self-admissibility for '= l' identity body / iterator), WI-462 (Gap D — tuple-literal threading / split); WI-447 now depends on all 4 = BLOCKED. Do NOT resume WI-447 until they deliver. (Stays 'Claimed' only because the CLI has no un-claim verb — treat as Blocked; the sequence's <- next is WI-450, then the WI-459-462 prereq cluster.)

### 2026-06-15T20:13:06Z — user

RE-ATTEMPTED THE REVERT 2026-06-15 (all deps WI-459/460/461/462/474/475 delivered). Stdlib loads in bare form, BUT the full suite surfaces TWO further typer gaps (5 failures: wi357 dispatched_stream_splitfirst/head; eval wi362_collect / wi362_take_n / wi064 combinators). Tree reverted to green; WI-447 stays Claimed/blocked. The gaps both root in the cross-sort EFFECT projection `s.E`:

GAP H (root, soundness-sensitive): an `effects s.E` projection on a CONCRETE List receiver errors 'type anthill.prelude.List has no member E' — collect/takeN/foldLeft/foldRight/find all return `List[T = s.T] effects s.E`, and when consumed on a List (admissible-as-Stream) the s.E projection is NOT resolved through `List provides Stream[T, {}]` to `{}`. This is WI-376's DELIBERATE exclusion: 'EFFECT members EXCLUDED (WI-396): l.E stays loud, never {} via Stream provides — skip an EffectsRows-headed binding.' So the bare consumption forms collide head-on with a delivered, on-purpose design choice motivated by WI-396 effect-row soundness. WI-447's own plan ('threading WRITTEN via s.E') presumes cross-sort `s.E` works; it was excluded.

GAP G (dispatch; likely the same root cascading): a DISPATCHED `Stream.splitFirst(xs)`/`head(xs)` on xs:List[Int64] whose abstract signature carries an `effects s.E` clause leaves the s.T ELEMENT projection at `?_` ('expected Int64, got ?_'), so wi357 dispatched_* fail. BISECTION on a minimal wi474-style Strm/Lst fixture (+ `import anthill.prelude.EffectsRuntime` so E resolves cleanly): the `effects E = ?` MEMBER alone PASSES; provides `E = {}` PASSES; `E = s.E` nested in the Pair B PASSES; ONLY the op-level `effects s.E` CLAUSE flips it to FAIL. So the s.E effect-projection on the dispatched op preempts/cascades into the element-projection elimination. WI-474 declared cross-sort s.T 'satisfied by WI-461' but its regression fixture (wi474_dispatched_projection_test) has NO effect dimension — no `effects E=?` member, no `effects s.E` clause — so this interaction was never exercised; the stdlib-based wi357 dispatched tests passed only because the stdlib was still EXPLICIT.

DECISION NEEDED (core-model / cascade into delivered WI-376/WI-396 — stop-and-ask, not loop-guessable): lifting WI-376's cross-sort effect-member exclusion is the natural prereq, but it reopens the WI-396 effect-row soundness reasoning. Options: (a) file a prereq to make cross-sort `s.E` eliminate to the written provision effect (lift the WI-376 exclusion soundly; fixes Gap H and likely Gap G); (b) re-scope WI-447 to bare-the-ELEMENT-only and keep the effect dimension EXPLICIT ([Eff] param or written row, no `s.E` projection); (c) other. The bare element forms (splitFirst/nth/split/iterator `s.T`/`xs.T`) work; the EFFECT dimension is the sole blocker.

