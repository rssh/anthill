```anthill
fact WorkItem(id: "WI-423", created: "2026-06-07T16:49:16Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-17T18:37:08Z"))

fact Feedback(workitem: "WI-423", author: "user", at: "2026-06-17T18:37:06Z")
```

## description

Loader/resolution: close the requires-induced ambiguity leak for the remaining kernel GLOBAL LOCALS. WI-422 (65da79b) fixed the reported case — the loader-internal reflection FACT FUNCTORS (member/meta/SortAlias) were bare global `locals` via `define()`, and a `requires Spec[T]` link opens a scope path (sort -> spec -> prelude -> _global) that BYPASSES a namespace import alias (which short-circuits only along the ENCLOSING chain) and resurfaces the global symbol as a phantom rival to the user's alias -> `ambiguous symbol` -> hard `unknown functor` cascade. WI-422 moved those 3 functors to the new `SymbolTable::define_qualified_only` (registered in `by_qualified_name` ONLY, never any scope's `locals`). STRUCTURALLY-IDENTICAL LEAK STILL OPEN: `KERNEL_META_SORTS` (Sort/Entity/Fact/Rule/Operation/Namespace/Requirement/Description/Constraint/Member, load.rs ~1365) are STILL bare global `locals` via `define(name,name,Sort,global_raw)` in register_prelude (~1407). TRIGGER: a user sort whose name collides with a kernel meta-sort (e.g. a user `sort Member` or `sort Constraint`), selectively imported into a requires-bearing sort and referenced bare in a nested scope, hits the identical ambiguity. Lower-risk than WI-422 (PascalCase kernel names are rarely user-shadowed; the trigger is more contrived than the real, commonly-imported `List.member` operation case), which is why it was kept out of WI-422 scope. TWO CANDIDATE FIXES, pick one: (a) DELOCALIZE the meta-sorts too — same treatment via `define_qualified_only`. SAFE FOR FACT INDEXING (member facts are keyed by `make_name_term("Member")`, which INTERNS into intern_map, a DIFFERENT symbol than the global-local Sort — verified during WI-422), but FIRST verify no legitimate bare scope-resolution of these meta-sort names exists in stdlib/reflect .anthill or in the Rust loader/typer (grep resolve_in_scope / resolve_name_in_global / remap_* for these names; WI-422 verified this only for the 3 functors, NOT the meta-sorts). If any meta-sort IS legitimately referenced bare as a type, option (a) is unsafe for that one. (b) THE DEEPER / higher-altitude fix — change `resolve_in_scope_recursive` (intern.rs ~300-352) from its current 'collect ALL matches across reachable parents, then flag >1 distinct as Ambiguous' policy to PREFER a nearer match (local/import-alias on the enclosing chain) over a symbol reached only via a `requires`-induced parent link to a farther/global scope. This closes the entire class (functors, meta-sorts, AND any future global symbol) at the resolution policy itself rather than per-symbol, but is a broader behavioral change to core name resolution — needs careful blast-radius review (every ambiguity-warning site) and may legitimately change which symbol wins in other multi-candidate cases. Both /code-review reviewers (WI-422 max-effort pass) independently flagged this; (b) is the principled close but (a) is the low-risk targeted one. ACCEPTANCE: a user sort named the same as a kernel meta-sort (e.g. `sort Member`), selectively imported into a requires-bearing sort and referenced bare in an op body, resolves to the USER's symbol without an ambiguity warning or unresolved cascade; a regression test pins it; cargo-test green.

## Feedback

### 2026-06-17T18:37:06Z — user

DELIVERED via option (a) — delocalize the 10 KERNEL_META_SORTS (Sort/Entity/Fact/Rule/Operation/Namespace/Requirement/Description/Constraint/Member) via define_qualified_only in register_prelude (load.rs), exactly mirroring WI-422's functor fix. Chose (a) over the deeper (b) resolve_in_scope_recursive prefer-nearer policy because the gating safety check came back clean: an Explore sweep confirmed NO .anthill source or Rust resolver resolves these names by scope — all reflection facts are emitted/queried via make_name_term()->intern() (a separate intern-map symbol, never scope-resolved), and none are declared in .anthill files. So delocalizing closes the leak with zero capability loss; (b)'s broad core-resolution blast radius is unwarranted. Repro confirmed pre-fix (bare 'Member' in a requires-bearing sort -> 'ambiguous symbol Member [Member, user.Member]') and clean post-fix. Regression test wi423_user_sort_shadowing_kernel_meta_sort_resolves (eval_test.rs) pins both halves: try_load_kb_with(src).is_ok() (no ambiguity) + eval run()->7 (user Member threads through). Full workspace suite 2040 green; /code-review (2 finders, correctness + test-validity/altitude/conventions) clean. This closes the WI-415..423 requires-dispatch cluster.

