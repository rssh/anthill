## Attributes

- id: WI-607
- created: 2026-07-01T03:06:23Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-09T06:32:55Z

- acceptance: cargo-test

## Description

sort_provides edge-set cache via a monotonic fact-mutation epoch (WI-595 part-3, deferred). PROFILING-GATED: measurement (2026-07-01) shows sort_provides rebuild costs only ~1.7ms across a full stdlib+github-todo load (87 calls, ~20us/call, 43 edges) and 0 calls on real resolution; stdlib has zero [simp] rules so the resolver simp path is inert. Only a synthetic 500-[simp]-redex program reaches ~37ms. Revisit ONLY if a real workload surfaces sort_provides as a hot spot. Correct cache needs an epoch counter at the assert/retract/unindex_functor choke-points (a count-keyed memo is ABA-unsound: 3 finders confirmed a balanced runtime retract+insert of distinct SortProvidesInfo facts restores the provider count and serves a stale edge-set). Do NOT re-attempt count-keying.

## Changes

### 2026-07-09T06:32:52Z — feedback — user

CLOSED AS SUPERSEDED (2026-07-09). The epoch-counter cache this WI proposed is obsoleted by the per-functor mutability declaration (proposal 053). SortProvidesInfo is marked constant() (reflect.anthill: rule fact_monotonicity(SortProvidesInfo)=constant() [simp], WI-665), and the WI-666 eval guard makes any runtime Store.persist/retract of a constant functor a LOUD error. So the sort_provides edge set never mutates at runtime => the ABA hole that killed count-keying does not exist, and NO epoch counter is needed. The build-once cache itself is already DELIVERED by WI-660 (typing.rs ProvidesIndex; serves sort_provides -> sort_provides_reach -> provides_out_edges, incl. the resolver simp-guard read this WI targeted; scan fallback pre-build; nulled around eq_derive's load-time read-while-asserting). Zero invalidation code. Epoch/ABA is the wrong frame under 053: forbid the mutation (constant) or tolerate the add via scan-fallback (like op_records, if ever moved to monotone for runtime provider registration); retract stays forbidden unless non_monotone. Residual same-class unindexed scans (SortInfo/RequiresInfo) are separate WIs, not this one.

