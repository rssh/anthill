```anthill
fact WorkItem(id: "WI-385", created: "2026-06-04T16:20:13Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-406", "WI-407"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-06T13:17:18Z"))

fact Tag(workitem: "WI-385", name: "typing")

fact Feedback(workitem: "WI-385", author: "claude", at: "2026-06-06T05:29:03Z")

fact Feedback(workitem: "WI-385", author: "user", at: "2026-06-06T06:52:17Z")

fact Feedback(workitem: "WI-385", author: "user", at: "2026-06-06T11:13:19Z")

fact Feedback(workitem: "WI-385", author: "claude", at: "2026-06-06T13:17:18Z")
```

## description

Typer: operation ARGUMENTS and fact/constructor FIELD VALUES are never type-checked against their declared parameter/field types -- a broad soundness hole orthogonal to WI-379. The arg-unify booleans in check_apply_iter (kb/typing.rs ~2915/2930) and the field-unify booleans in check_constructor_iter are DISCARDED: they drive type-param INFERENCE only, never VALIDATION. Only return-type conformance (check_operation_bodies) and -- since WI-379 -- let-annotation conformance (LetAfterValue) are enforced. PROVEN (recon + WI-379 code-review + bigint_probe): 'operation f(x: Int) -> Int = x' called as 'f("hello")' loads CLEAN (String accepted where Int required); 'fact Counter(n: "hello")' with 'entity Counter(n: Int)' loads CLEAN (the String-in-Int-field control that SHOULD error does not). A caller can pass an argument/field of any type and the typer stays silent. FIX DIRECTION: after the arg/field unify loops, check types_compatible(arg.ty, param_type) (and field.ty vs declared field type) and emit a TypeMismatch on failure (new context variant OperationArgument{op,param}; EntityField already exists for fields). RISK (recon warned 'budget for triage; do not assume a clean run'): enabling this surfaces a WAVE of pre-existing latent mismatches the silent path masked -- the Modify get(target:T)->T conflation and BigInt-in-Int-field were instances. Likely needs staged rollout: emit as a WARNING first (warnings channel WI-345) to size the blast radius, then promote to error once the stdlib is clean. RELATES WI-379 (made the RETURN direction authoritative; this is the ARG/FIELD direction), WI-384 (constructor build robustness, same function). ACCEPTANCE: arg/field type mismatches are diagnosed; full anthill-core green after triage. Rust-only (typer).

## Feedback

### 2026-06-06T05:29:03Z — claude

WI-406 (explicit value<->Term boundary) delivered as the prerequisite. Recon for WI-385 (investigated this session, probe stashed + design in memory anthill-typing-build-sequence): the naive types_compatible-after-unify validation produces 174 FALSE POSITIVES (polymorphic positions T/?_); a GROUNDNESS GATE (walk both sides through subst, validate only when BOTH resolved types are type_value_is_ground -- skips spec-op T params, inference ?_, free params) collapses that to 11, all in wi_tests. Those 11 had TWO root causes: (a) value<->Term reflect-boundary crossings -- NOW FIXED by WI-406 (explicit as_term / typed fresh_var, so persist+query sites type-check ground-vs-ground); (b) PROVIDER-TRANSITIVITY: IndexedFileStore provides QueryableStore/BulkStore which provide Store, so IndexedFileStore<:Store is a 2-hop provider chain, but sort_provides (typing.rs:11977) is SINGLE-HOP, so types_compatible(IndexedFileStore, Store)=false and retract(b,..)/flush(b,..) in store.anthill false-positive. (b) is the REMAINING blocker, a separate types_compatible gap unrelated to the reflect boundary -- either make sort_provides/sort_provides_admissibly transitive (recursion + cycle guard; cascades into all provider-subtyping callers = a core-relation change, stop+ask) or have WI-385 validation conservatively skip when a transitive provider path exists. IMPLEMENTATION: arg-validation in check_apply_iter after the two unify loops (~2987); field-validation in check_constructor_iter after the field-unify loops (~7073); shared helper validate_arg_against_param (walk_view both sides -> resolved_type_is_ground gate -> types_compatible) + new TypeErrorContext::OperationArgument{op_name,param}; EntityField already exists. PRE-EXISTING (unrelated): anthill-todo bundle is RED on main -- 'type mismatch in match.rule: List[String] vs List[List[String]]' at a non-reflect op; reproduces on baseline; NOT in the -p anthill-core gate; worth its own ticket.

### 2026-06-06T06:52:17Z — user

CORRECTED DIAGNOSIS (supersedes the provider-transitivity feedback) — the prior premise was WRONG. Applied the stashed validation on current HEAD (post WI-406): exactly 9 failures remain, ALL 'expected Store, got IndexedFileStore' on persist/retract/flush(b) in rustland/anthill-todo/anthill/store.anthill, where b:IndexedFileStore (from 'entity wis(backend: IndexedFileStore,..)' matched as wis(b,_)) is passed to ops declared 'store: Store'. WI-406 fully fixed the reflect-boundary root cause (a). The remaining blocker is NOT transitivity: instrumented sort_provides to dump the live SortProvidesInfo edge set + probe every subtype channel. RESULT: IndexedFileStore provides ONLY Modifiable. There is NO IndexedFileStore->BulkStore, NO BulkStore->Store, NO QueryableStore->Store edge anywhere. All three channels return false for the whole hierarchy: IndexedFileStore->BulkStore = sort_provides=F sort_sym_compatible=F sort_refines=F; BulkStore->Store = F/F/F; QueryableStore->Store = F/F/F. IndexedFileStore is an Entity with parent_sort=None; Store/BulkStore/QueryableStore are Sorts with parent_sort=None. ROOT CAUSE: Store/BulkStore/QueryableStore are NON-PARAMETRIC spec sorts. The is-a is declared via 'sort QueryableStore { fact Store }' and top-level 'fact BulkStore[IndexedFileStore]', but maybe_emit_fact_provides_info (load.rs ~7276) early-returns on spec_params.is_empty(), so NON-parametric specs emit NO SortProvidesInfo at all. So the declared is-a is invisible to subtyping; it was never checked pre-WI-385 (arg-unify booleans discarded). This is the 'wave of pre-existing latent mismatches the silent path masked' the ticket predicted. The user chose 'transitive everywhere' (implemented: sort_provides recursion+cycle guard) but it has no edges to chase. WIP stash@{0} = validation + transitive sort_provides (compiles; 9 red). DECISION NEEDED (stop+ask): (A) fix provider-representation = emit zero-binding SortProvidesInfo for non-parametric 'fact <Spec>' [sort-body carrier=enclosing sort; top-level carrier=the positional] + the transitive sort_provides -> makes the declared is-a real; BIG blast radius (every non-parametric fact X across stdlib becomes a provider edge, affects dispatch/requires-coverage); likely its OWN inserted prerequisite ticket since it's a pre-existing gap not WI-385 scope. (B) warning-first per the ticket: route WI-385 mismatches through the now-DELIVERED LoadWarning channel (WI-345); suite stays green, sizes blast radius, promote to error after the gap is fixed (typer doesn't emit warnings yet, needs wiring). (C) fix the stdlib modeling to express Store is-a via a recognized mechanism. (D) narrow WI-385: hard-error ground primitive/structural mismatches now (catches the proven String-in-Int holes), skip nominal-sort-vs-nominal-sort pending the provider-repr fix.

### 2026-06-06T11:13:19Z — user

PREREQUISITE WI-407 DELIVERED (4154382, pushed): non-parametric spec 'fact <Spec>' now emits SortProvidesInfo + sort_provides is transitive, so IndexedFileStore<:Store (and the whole non-parametric Store hierarchy) is recognized. VERIFIED: applying this validation on top of WI-407 turns the original 9 anthill-core IndexedFileStore<:Store failures GREEN; full anthill-core suite (298 wi_tests) passes WITH the validation. Validation WIP now in stash@{0} (validation-only, applies cleanly on WI-407 HEAD, NO sort_provides conflict). REMAINING SCOPE = the anthill-todo blast radius the ticket predicted (full workspace: 6 anthill-todo targets fail with validation on; anthill-todo is GREEN without it, so this is WI-385's own scope, not WI-407). Categorized mismatches: (1) value<->Term REFLECT BOUNDARY false-positives -- 'Feedback.content: expected Term, got String', 'WorkItem.description: expected Option[T=Term], got String'. Per memory term-not-top-reflect-reify-conversion, WI-385 MUST treat value<->Term as a CONVERSION (total reflect / partial reify), NOT a mismatch. validate_arg_against_param currently does naive types_compatible -> flags String-vs-Term. NOT YET IMPLEMENTED -- this is the main remaining WI-385 work. (2) OPTION-WRAPPING: 'WorkItem.depends_on: expected Option[T=List[String]], got List[String]', 'WorkItem.description: expected Option[T=Term], got String' -- on-disk facts store a bare T where the field is Option[T] (the unwrapped-on-disk representation, cf. 5e4b635 normalize_deps). Validation must accept bare T where Option[T] declared (some()-coercion / on-disk-unwrap), OR loader wraps. DESIGN CHOICE. (3) 'walk_solutions.stream: expected LogicalStream, got Stream[T=Substitution,E=Error]' -- LogicalStream PROVIDES Stream, so passing a Stream where LogicalStream is expected is a DOWNCAST (abstract->concrete); triage whether the param is declared too-concrete (real bug) or a variance/binding gap. (4) 'match.rule: expected String got ?_' / 'expected ?_ got String' -- OperationMatch context (compute_branch_join_type), NOT the WI-385 arg/field check; triage whether pre-existing-masked or an interaction. IMPLEMENTATION PLAN for next session: (a) apply stash@{0}; (b) make validate_arg_against_param treat the Term boundary as conversion (String/any-value vs reflect Term = OK) and accept bare T vs Option[T]; (c) re-run full workspace, triage (3)/(4) -- likely a stop+ask if (3) is a real downcast bug or (4) is an interaction; (d) /code-review the validation (soundness-sensitive); deliver. Ticket's staged-rollout note (warning-first via WI-345) remains a fallback if the blast radius can't be cleanly resolved.

### 2026-06-06T13:17:18Z — claude

DELIVERED 2026-06-06 (978d7c8, pushed). validate_arg_against_param in check_apply_iter (op args, strict) + check_constructor_iter (fields). Gated on resolved_type_is_ground. Accepts 3 boundary conversions: (1) value->Term reflection (total, both positions; Term is NOT top, so in the validation not types_compatible); (2) bare-T-vs-Option[T] FIELD INTERIM (peel + re-check element; bare Option accepts any) -- value stays bare, normalize_deps launder KEPT, on-disk unchanged; the SOUND some-insertion is WI-408 (user decision: typer inserts some, on-disk migrates to some()/none(), general implicit-conversion/into framework deferred); (3) provider admissibility -- a concrete carrier conforms to a BARE spec it provides (List->Stream param/field), found by /code-review (max effort, 2 agents CONFIRMED a false-reject regression: types_compatible confines provider-admissibility to its bare<->bare arm). Blast radius: walk_solutions widened from concrete LogicalStream to abstract Stream[Substitution,Error] (what execute returns; runtime value is a LogicalStream which provides Stream so splitFirst dispatches) + declared the Error effect. Tests: anthill-core/tests/include/wi385_arg_field_validation_test.rs (8). Full workspace green. KNOWN BOUNDARY (pre-existing pass-reach, NOT a logic gap): free-standing namespace-level entity top-level facts are not visited by type_check_sorts, so their fields stay unvalidated -- op-args, op-body constructors, and IN-SORT-entity facts ARE validated.

