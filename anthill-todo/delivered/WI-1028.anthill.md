```anthill
fact WorkItem(id: "WI-1028", created: "2026-08-06T11:20:51Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-06T14:02:16Z"))

fact Tag(workitem: "WI-1028", name: "rustland")

fact Feedback(workitem: "WI-1028", author: "user", at: "2026-08-06T14:02:15Z")
```

## description

RUSTLAND: CONVERT THE SCOPE-FINDER RETURN TYPES AND THE ScopePass SPINE, so `KnowledgeBase::scope_id_of(TermId)` has no caller left. NOTE THE ACCEPTANCE BELOW IS NOT A CALLER COUNT — the first version of this ticket said 'scope_id_of is deleted', which is the shape WI-983's feedback warns against ('a site can serve two roles at once, so how many callers remain is the wrong shape for a criterion; no caller is in role X, and the rest are all role Y is the right one'). WI-984 quoted that warning and then wrote the defect into this ticket one commit later. WHAT WI-984 ALREADY DID, inline, after that was noticed: every PURE-SCOPE-ROLE site converted — `scan_rule`, `scan_rule_goal`, `scan_operation_params`, `process_imports`, `ensure_intermediate_namespaces` (return type too) and the whole `register_stdlib_scopes` bootstrap, which minted 21 nullary terms whose only consumer was the projection back to their own functor. 89 derivations -> 29, all 29 at genuine two-role sites, and the criterion is written at `scope_id_of` itself. WHAT IS LEFT, and why it is not a sweep: the remaining 29 hand the SAME value to `is_sort_scope`, `walk_scopes`, `register_prelude_constructor`, `enter_sort_with_body` or `assert_fact` AS A TERM. Removing them means changing `resolve_name_to_scope` / `find_scope_by_name` / `lookup_scope` / `resolve_declared_scope` to return `ScopeId`, and `ScopePass::{enter_scope,exit_scope,at_item}` + `ScopeSite.enclosing` (3 methods, 5 impls) plus `Loader.current_scope`; the term-consumers then re-derive with `make_name_term_from_sym(scope.owner())`. THE REASON TO DO IT is not throughput — measured at ~0.5% of a debug load, immaterial. It is that the derivation HID A DECISION: `load_provides_block` assigned an APPLIED spec term as the scope and the question 'what scope does that name?' only surfaced when `name_term_sym` aborted, behind 22 reads instead of at the one write. TERM-ID STABILITY IS NOT A CONSTRAINT — WI-984 measured this rather than assuming it: persistence writes no numeric ids (all name-keyed), and deleting two allocation sites plus a 141-line walk shifted symbol ranks with the full suite green, including the golden CLI transcript. So an `alloc` may be dropped when its term has no consumer; do not repeat the 'term ids are observable' reasoning WI-984 first wrote and then refuted. DO NOT FOLD IN the term-level readers `is_sort_scope` / `eponymous_sort_symbol` (WI-983's feedback rules those out) — but note their caller may hand them a term re-derived from a ScopeId, which is a behaviour question to answer, not assume. ACCEPTANCE: no caller of `scope_id_of` remains in a TWO-ROLE position either — i.e. every scope-carrying value in the loader is a `ScopeId` and the few sites needing a term derive it explicitly; cargo-test green; say at the site which test fails when the change is backed out. REFERENCE: WI-984; WI-983; WI-1004.

## Feedback

### 2026-08-06T14:02:15Z — user

IMPLEMENTED, workspace green 4228/0 plus 4 doctests. /code-review NOT RUN — user-invocable only; /simplify run instead (4 agents, ~30 findings, 11 applied).

WHAT LANDED: every scope-carrying value in the loader is a `ScopeId`. `ScopePass::{enter_scope,exit_scope,at_item}`, `ScopeSite.enclosing`, `walk_scopes`, `lookup_scope`, `resolve_declared_scope`, `find_scope_by_name`, `Loader::current_scope`, `current_domain`, `enter_namespace`, `enter/exit_sort_with_body`, `load_operation`'s `op_scope`, `check_op_type_param_shadowing`'s `enclosing`, `scan_operation_params`, and the prelude bootstrap's last two term round trips. `KnowledgeBase::scope_id_of` has no caller in any role and is DELETED. `current_scope_id()` became the identity across 21 reads and is deleted too.

THREE SITES DERIVE A TERM, all genuine term positions: the namespace fact's subject, the sort's description-fact subject, and `MemberInfo.parent`. (A fourth, `is_sort_scope`, went away with WI-1029.)

THE BEHAVIOUR QUESTION THE TICKET FLAGGED, MEASURED BEFORE CONVERTING, NOT ASSUMED. (1) `held == make_name_term_from_sym(name_term_sym(held))` asserted at every scope site the walk reaches: 4223 tests, never fired; inverted to `assert_ne!` it fires on the first file, so the probe was REACHED. Re-deriving the term is TermId-identical. (2) `resolve_declared_scope`'s two derivations (`lookup_defined` as guard, `remap_name` as answer) agree over the whole corpus — that sentence is now a `debug_assert_eq!` rather than prose, because a sentence is not a guard: `remap_name` cannot fail (bare-interns) and `scope_id` only range-checks, so a divergence would mint a scope over a symbol owning none.

THE CONTROL, SAID PLAINLY: this is a REPRESENTATION change with no intended behaviour change, so 4225 of the tests pass either way BY DESIGN — they are the control for "nothing moved", not evidence anything is new. Three new tests in `wi1028_scope_id_spine_tests`; two fail by NOT COMPILING against a `TermId` spine. `a_clause_is_filed_under_the_owner_of_the_scope_it_is_written_in` is the discriminating one — MEASURED: file every clause under the walk root instead and it fails while the walk test still passes, which is why both exist.

A TEST OF MINE THAT MEASURED NOTHING, caught by driving its control: the first resolve assertion was one-sided (`Red` resolves from Colour's scope) and PASSES with the descent crossed, because `Red` answers `Found` from `_global` too. It is two-sided now on a sort-private OPERATION, and the NEGATIVE half is what fires.

FROM /simplify, applied: reverted my own `emit_member_fact` change (it put a caller-side pair invariant on 10 sites AND made both callers re-derive a term they held — flagged by all four angles); `resolve_declared_scope` now shares `lookup_scope`; `process_imports` had become literally `A && A` / `A || A` and is one `has_qualified_name`; the Entity arm decided "is this a sort body" three times in two spellings, now once; `lookup_scope` lost a `&mut` it no longer needs; two dead `_global` term round trips removed from `anthill-cli/src/prove.rs` and `kb/body_specialize.rs` (the shape WI-984 deleted 21 of).

TWO CLAIMS OF MY OWN THAT WERE WRONG. I listed "a `SortAlias` subject" among the term-needing sites in two doc comments — there is no such site; I fabricated it. And I wrote that `&mut` on the `is_sort_scope_id` predicate was "the honest price": `TermStore::find` exists for callers that only want to NAME a term, and `alloc` bumps a refcount on a hash-cons hit. Verified in `kb/term.rs`. That fed WI-1029.

ALSO: a comment cited `wi984_param_provides_test`, a test name that does not exist; corrected to `parameterized_provides_body_resolves_a_spec_scoped_name`.

