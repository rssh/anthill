```anthill
fact WorkItem(id: "WI-376", created: "2026-06-03T21:31:35Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-379", "WI-398", "WI-399", "WI-400"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-06-09T13:48:28Z"))

fact Feedback(workitem: "WI-376", author: "user", at: "2026-06-04T12:38:05Z")

fact Tag(workitem: "WI-376", name: "typing")

fact Feedback(workitem: "WI-376", author: "claude", at: "2026-06-05T15:07:13Z")

fact Feedback(workitem: "WI-376", author: "user", at: "2026-06-05T16:31:37Z")

fact Feedback(workitem: "WI-376", author: "claude", at: "2026-06-05T16:49:38Z")

fact Feedback(workitem: "WI-376", author: "claude", at: "2026-06-05T21:29:45Z")

fact Feedback(workitem: "WI-376", author: "claude", at: "2026-06-05T22:10:41Z")

fact Feedback(workitem: "WI-376", author: "user", at: "2026-06-09T13:48:05Z")
```

## description

Typer: path-dependent PROJECTION TYPES as eliminated TypeExtractor/TypeHead forms -- the closely-coupled extension of WI-374 (expansion during unification). Design: docs/design/expansion-during-unification.md sec.2 (trigger table), sec.4 (cases 2-3), sec.5 (Threading T). Add two Type heads recognized by type_head/extract_type (typing.rs:10280/10342) and ELIMINATED at the unify boundary (the sort_ref/projection arms of unify_types ~7400+, which hold the &mut subst needed to mint fresh vars + add constraints; extract_type is a pure classifier so minting cannot live there). CASE 2 -- EXPRESSION-CARRIED x.A (path-dependent, Scala-style): mint fresh Ti, constrain unify(Ti, typeof(x).A) -- resolve x's static type, project member A, unify with Ti. Well-formedness: typeof(x) MUST declare member A (loud type error, not a silent fresh var, per the project's loud-error principle). This lets a producer write iterator(l: List) -> Stream[l.T] instead of threading a shared ?t; iterator(xs: List[Int]) then has type Stream[T=Int] -- element IN the type -- threading through composition (collect(s: Stream) -> List[s.T] on Stream[Int] => List[Int]) and resolving the concrete/abstract split with ONE signature (Stream[c.T] => Stream[Int] for concrete c:List[Int]; stays polymorphic for abstract c:C via c's declared interface member). It is the type-MEMBER sibling of the existing Denoted value-in-type form (WI-302), which STAYS rather than eliminates. CASE 3 -- SORT-CARRIED X.L: mint fresh Ti, alias/replace X.L with Ti (trivial case both unbound ?), AND add X[L=Ti] to the enclosing requirements, then expand X[L=Ti] further by the same rules (the F-omega / Scala X#L abstract-type-member -> fresh-var+bound encoding; terminates on finite type structure). RELATIONSHIP TO WI-374: same unify-boundary elimination hook; WI-374 ships case 1 (bare/partial sort application -> fresh per unbound param). The EXPRESSION-carried half (x.A) is the part that PAYS for WI-374 -- it makes Stream[l.T] work and replaces the bare iterator(l: List) -> Stream (list.anthill:67) signature that DELETES the List<->Stream element relationship -- so it is a strong CO-DELIVERY candidate (possibly same PR as WI-374). The SORT-carried half (X.L) drags in the requires plumbing and is the natural FOLLOW-ON seam. NOTE the effect row E is NOT solved by projection: List has no effect member to project, and Stream[E={}] is ungrammatical (library/002:132); E stays the WI-307 row-unification residue, closed by the consumer's context, NEVER defaulted to pure (design doc sec.5). VERIFY before coding: whether x.A / X.L already PARSE in type position (the WI-302 / Denoted work suggests projections do) -- if so the new cost is semantic elimination, not grammar. ACCEPTANCE: a producer signature iterator(l: List) -> Stream[l.T] threads the element through collect/length composition (no fresh ?_ element); the concrete/abstract receiver split holds via one signature; full anthill-core green. RELATED WI-302 (Denoted value-in-type sibling), WI-307 (effect-row residue). Rust-only (typer; scaland has no typer).

## Feedback

### 2026-06-04T12:38:05Z — user

REFRAME 2026-06-04 (design dialogue). The two projection forms now have DECIDED DESUGARINGS; the surface (this WI) is the elimination, but the MEANING is anchored elsewhere:

1. SORT-CARRIED X.L (case 3) = SUGAR for an operation type parameter + a requirement: f(p: X.L) == f[Ti](p: Ti) requires X[L=Ti] (N members: X.L1,X.L2 == [Ti1,Ti2] requires X[L1=Ti1,L2=Ti2]). The synthesized Ti is FREE (op polymorphic in it) and rides the 042 op-type-param channel; the requirement materializes via construct_requirement (operation-call-model). So it adds NO new machinery -- it desugars onto 042 + requirements. NOW DOCUMENTED in docs/proposals/042 sec.'Type projections (s.T, s.Sort, X.L)'.

2. EXPRESSION-CARRIED s.T / s.Sort (case 2) = a SYNTHESIS-TIME CONSTRAINT, not an op type param: fresh Ti + constraint Ti = typeof(s).T, discharged when s is synthesized (WI-379 bidirectional). Ti is DETERMINED by s (not free). ADD s.Sort to the family = the whole parameterized sort of s (= Stream[s.T,s.E]); captures ALL params (wide-sort safe); e.g. splitFirst(s: Stream) -> Option[Pair[A=s.T, B=s.Sort]]. (s.type/s.Self rejected -- singleton/receiver baggage.)

3. DEPENDENCY CORRECTION: this WI currently 'depends on WI-374', but WI-374 is DEMOTED to a convenience (bare-ref expansion; by the type-not-provenance Boundary it CANNOT thread on its own). The projection forms instead depend on: WI-379 (bidirectional synthesis -- discharges the expression-carried constraint) and proposal 042 (the sort-carried desugaring target). NOT WI-374.

4. STRICTNESS (cross-parameter dependencies -- the projection's definitional content, must be loud errors): build the dependency graph (edge p->q when q's type mentions p via projection); resolve in TOPOLOGICAL = bidirectional-synthesis order (a projection's receiver is synthesized before the projection is read); REJECT cycles at load (signature well-formedness); missing member (s.T where s's type declares no T) -> type error, never silent fresh var; ABSTRACT receiver (c: C, C provides Iterable) -> c.T projects against C's DECLARED interface member, stays polymorphic, resolves at the concrete call; no such member -> error.

5. DOC HOMES: docs/proposals/042 (sort-carried desugaring + the s.T/s.Sort/X.L taxonomy); docs/design/type-parameter-scoping.md (value-projection rules s.T/s.Sort); docs/design/expansion-during-unification.md sec.4 cases 2-3 (the elimination mechanics this WI implements).

### 2026-06-05T15:07:13Z — claude

IMPLEMENTATION PLAN (investigated 2026-06-05; code map). Parse IR ALREADY preserves dotted segments: Name{segments: SmallVec} (ir.rs:119); a field_access type name -> TypeExpr::Simple(Name) with multi-segment (convert.rs:346-426). NO grammar/parse change. SCOPE THIS WI to the EXPRESSION-CARRIED half (s.T/s.Sort/s.E) -- the load-bearing fluent threading; SORT-CARRIED X.L (desugar [Ti]+requires X[L=Ti]) is the follow-on seam, overlaps WI-383, defer. SLICES (green checkpoints): (1) SUBSTRATE -- add TypeNode::ExprCarried{value: Rc<NodeOccurrence>, member: Symbol} (node_occurrence.rs:787) + occ builder make_expr_carried_occ + term builder/functor anthill.prelude.TypeExtractor.ExprCarried; add TypeExtractor::ExprCarried{value,member} + TypeHead::ExprCarried + arms in type_head/extract_type/type_dispatch_name_view (typing.rs:10628-10775). Purely additive -> suite stays green (nothing emits it yet). (2) LOADER CLASSIFY (load.rs type_expr_to_child ~6088, the WI-302 is_value SymbolKind site) -- for a MULTI-SEGMENT Simple/Parameterized name, branch on the HEAD segment's SymbolKind: value head (Param/Field/LocalLet/OpResult/...) -> ExprCarried(head-value-occ, last-segment member); CRITICAL do NOT misclassify a QUALIFIED SORT REF (anthill.prelude.List, head=namespace) -- only projection when head resolves to a value (or a parametric sort for the deferred X.L). Verify FULL suite green (qualified refs unbroken). (3) ELIMINATION (the synthesis-time constraint, discharged when s is synthesized -- WI-379 bidirectional): in check_apply_iter, resolve ExprCarried(s,M) by finding the arg bound to param s (param_to_arg_sym/pos_results), take its inferred type, project member M via extract_type_param(kb,&argty,M) (s.Sort = the whole arg type), substitute into the return/effect position; mint fresh Ti + unify if needed. ABSTRACT receiver (c: C) -> project against C's declared interface member, stays polymorphic. MISSING member -> loud TypeError (project principle), never silent fresh var. (4) STRICTNESS -- param-projection dep graph (edge p->q when q's type projects p), topological/synthesis order, REJECT cycles at load, missing-member/no-such-interface loud. (5) TESTS wi376: collect(s: Stream[T=A,E=Ef]) -> List[T = s.T] effects s.E threads element+effect; splitFirst(s) -> Option[Pair[A=s.T, B=s.Sort]]; s.Sort captures all params; concrete (List[Int]->Int) vs abstract (c:C stays poly); NEGATIVE missing-member + cycle rejected. Hooks: unify_view_structural arms (typing.rs:7697) hold &mut subst for fresh-var mint (make_type_var). Deps: WI-379 (done), 042 (done). Docs: 042 sec.Type projections; type-parameter-scoping.md sec.1; expansion-during-unification.md sec.4 case 2.

### 2026-06-05T16:31:37Z — user

PROGRESS 2026-06-05 (delivered the sound CONCRETE half; pushed 862d0a2/9f6b12a/2ea5252, full anthill-core green 1203). DELIVERED: expression-carried type projections s.T / s.Sort threaded through call-site composition — substrate (TypeExtractor::ExprCarried + make_expr_carried, ground-ref term form), loader classify (multi-segment value-head name -> ExprCarried, gated on the design CAPITALIZATION rule: member must be Capitalized, so Modify[result.a] stays a denoted place), eliminator in check_apply_iter (project the member off the ARGUMENT inferred type; one mechanism covers iterator(l:List)->Stream[l.T] and collect(s:Stream[T=A,E=Ef])->List[s.T]). Concrete member -> projected type; missing member -> loud error; s.Sort -> whole arg type. 8 wi376 tests incl headline gather(to_stream(xs)). /code-review fixed 2 confirmed bugs (Modify[result.a] regression via capitalization; Value::Node projection leak -> loud error) + a SOUNDNESS hole (bare/abstract receiver minted a flexible ?proj var absorbing any demand -> peek(l) usable as both Int and String; now loud error). REMAINING (acceptance 'concrete/abstract split via ONE signature' only HALF met — abstract side ERRORS soundly, not stays-poly): (1) ABSTRACT-RECEIVER stays-poly via the receiver DECLARED-INTERFACE member = rigid type tied to receiver (WI-393 carrier/provider machinery; semantic-model change); (2) cross-PARAMETER projection f(a:List,b:a.T) + load-time cycle/topological graph (slice4 strictness); (3) effects s.E does NOT load (effect names route through a separate lowering, never classified ExprCarried); (4) compound receiver a.b.T / (expr).T = TypeNode::ExprCarried Node carrier; (5) cross-sort provider DIVERGENT member name (project off declared param sort+provider, not arg raw sort); (6) ALTITUDE: eliminate at unify_types projection arms (let/rule/HO sites), not only check_apply_iter.

### 2026-06-05T16:49:38Z — claude

SPLIT 2026-06-05 (user-authorized: "keep 376; spin out 4 seams"). The 4 mechanical/architectural follow-on seams from the PROGRESS note are now their own typing tickets: WI-396 (effects s.E -- effect-position projection), WI-397 (compound receiver a.b.T -- Node carrier), WI-398 (cross-param projection + load-time strictness/cycle graph), WI-399 (altitude -- eliminate at the unify_types projection arms, not only check_apply_iter). Each depends on WI-379 and builds on the delivered ExprCarried substrate; deliverable independently of the abstract decision below.

WI-376 RETAINS ITS OWN ACCEPTANCE = the abstract-receiver stays-poly half (remaining items 1 + 5): the concrete/abstract receiver split via ONE signature, where an ABSTRACT receiver c:C projecting c.T resolves against C declared-interface member as a rigid receiver-tied type that STAYS POLYMORPHIC to the concrete call (today it errors soundly instead) -- plus the cross-sort provider DIVERGENT member name (project off declared param sort + provider, not the arg raw sort). This is a SEMANTIC-MODEL change (WI-393 carrier/provider machinery) and is PARKED pending a USER DESIGN DECISION.

WI-376 now depends_on WI-398 + WI-399 so the abstract-stays-poly model is decided/implemented against the COMPLETE projection substrate (synthesis-order strictness + full-altitude unify-boundary elimination). It stays Claimed but BLOCKED until those land; the loop should deliver WI-396..399 first, then resume WI-376 and surface the semantic-model design question to the user (do NOT auto-guess the abstract model).

### 2026-06-05T21:29:45Z — claude

DESIGN DRAFTED 2026-06-05 (docs/design/path-dependent-types.md). The parked abstract-receiver stays-poly keystone crystallized, via WI-397 design dialogue (the DataProvider use-case), into a full PATH-DEPENDENT TYPES model. Key decisions captured: (1) identity is SEMANTIC over the receiver VALUE = unification of the receiver expression up to the substitution (let y=z => y.T==z.T), NOT nominal-over-the-syntactic-path (the divergence from Scala AND ML translucent sums); (2) the two rules of the spine -- GROUNDING = track the expression type through control flow (flow-typing; Scala tracked is the static analogue), IDENTITY = unify expression types; (3) equality is the dependent-type-theory DEFINITIONAL CONVERSION restricted to three reductions -- zeta (receiver-unify) + delta (manifest member grounds) + eta (s.Sort reifies the whole type) -- over a NON-INJECTIVE ExprCarried head (never decompose p.M=?=q.M into p=?=q; rides WI-370 custom-unification-at-head); (4) inference = collect constraints, defer maximally, solve at end, DELAY-never-reject, with the soundness invariant = loud-error-over-silent-skip lifted to constraints (every deferred constraint confirmed/refuted/surfaced, never silently dropped) + 011 three-level residual accounting (ill / well-with-obligations / universally-typed). One fully-worked PARAMETRIC example (State[P] holding a provider) needs NO new term representation -- the occurrence type-slot carries it; PLAIN-field (refinement, Scala tracked-style) and (expr).M (grammar + stability) are deferred. The model SUBSUMES the seams as facets: WI-397 (compound receiver) + WI-398 (cross-param + synthesis order) + WI-399 (eliminate at unify boundary) + WI-384 (constructor infers P) + WI-370 (non-injective head). NOT YET RATIFIED -- this is the session design draft; the abstract half stays parked until the user confirms the model and the build order off it.

### 2026-06-05T22:10:41Z — claude

DESIGN ADOPTED 2026-06-05 (user: "WI-376 can use this design"). docs/design/path-dependent-types.md IS now WI-376 design: the abstract-receiver stays-poly half = path-dependent types per the doc (value-unification identity; grounding by control-flow type-tracking; zeta/delta/eta conversion over a NON-INJECTIVE ExprCarried head; abstraction = a static CALL-SITE CONTRACT via requires/ensures; base model forbids the abstracting return so it is escape-free). No longer "not yet ratified" -- design decided. Implementation needs beyond the existing seams (WI-384/397/398/399, +WI-370 as a dependency) are being mapped; new WIs to be filed for the gaps (equality non-injective head+delay; no-sealing/interface-expressible-return check; ensures-manifest translucent returns [deferred]; realization to host [deferred]).

### 2026-06-09T13:48:05Z — user

DELIVERED 2026-06-09 (the cross-sort provider divergent-member half; abstract-stays-poly already co-delivered by WI-400 inc. B). PROBE PASS first (verify-before-coding): scoped what WI-400 left -- abstract receiver stays-poly works for BOTH the DataProvider/requires same-name shape AND the Iterable spec-member shape (an abstract carrier c:C with requires Iterable keeps c.Element a neutral); the SOLE gap was item 5: a DIVERGENT member at a CONCRETE receiver (l.Element on a List, where List provides Iterable[List[T], T] so Iterable's Element maps to List's T) errored 'List has no member Element' because project_type_member only checked List's own type-params. FIX: project_via_provided_spec in project_type_member's concrete arm (after the direct-type-param check, before the no-member error) -- enumerate the receiver sort's provided specs (provided_spec_base_syms, mirroring find_spec_op_for_provided_sort), for a spec that declares the member read the provides binding via the EXISTING provider_spec_view_bindings (Iterable.Element -> List.T term), then GROUND it against the receiver's type-args (build_pattern_subst + walk_pattern_field_type_deep, same as field-type resolution). EFFECT members EXCLUDED (WI-396: l.E stays a loud error, never silently {} via 'List provides Stream[T, {}]' -- guard skips an EffectsRows-headed binding). /code-review high (1 finder, 6 angles) confirmed the single-ref case sound; FIXED its top finding -- the Grounded/Neutral decision now uses the DEEP resolved_type_is_ground (was head-only is_type_param_value), so a structured binding still resting on an unbound carrier param (Element=Pair[A,B] on a bare receiver) stays NEUTRAL instead of a Grounded type that would absorb demand. Tests (wi376_projection_test): divergent_provider_member_grounds_concrete, _wrong_return_rejected, abstract_carrier_spec_member_stays_poly. Full workspace GREEN 1589. WI-376 acceptance MET (concrete l.T threading + concrete/abstract split via the spec interface); deps WI-379/398/399/400 all delivered; the 4 spun-out seams WI-396/397/398/399 all delivered. DEFERRED latent gaps (review, NONE triggered by current stdlib, all documented): (a) an effect PASSTHROUGH-var provides binding (provides Spec[E=E]) on a sort with no own E declaration slips the EffectsRows guard -> c.E becomes a neutral instead of the WI-396 loud error (needs effect-param classification machinery that doesn't exist); (b) first-provided-spec-wins across two specs declaring the same member with divergent mappings (left to a later disambiguation pass, per the comment); (c) provided_spec_base_syms uses same_symbol while provider_spec_view_bindings uses canonical_sort_sym -- a disagreement is a false-REJECT (sound, over-rejects), the two snapshots of the same facts should share one carrier predicate.

