## Attributes

- id: WI-503
- created: 2026-06-17T07:37:39Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-18T16:48:23Z

- acceptance: cargo-test

## Description

Persistence (term_ser): residual lossy/asymmetric round-trip gaps left by WI-501's type-directed reload (all NARROW / not in the WorkItem schema; surfaced by WI-501 /code-review). (1) NESTED-OPTION COLLAPSE: serialize flattens some(none()) -> inner none -> JSON null -> reload Option+null -> none(), so some(value: none()) (present-but-empty) round-trips to none() (absent) for Option[T=Option[U]] — information loss. (2) SERIALIZE SHORT-NAME DISPATCH ASYMMETRY: term_to_value detects Option/List by SHORT name (name==some/none/cons/nil) while WI-501 made the DESERIALIZE side resolve by QUALIFIED name; a user entity literally named some/none/cons/nil in another namespace is mis-serialized. Harden term_to_value to gate those branches on qualified_name_of(functor)==anthill.prelude.* (the none-Ref serialize check WI-501 added is already qualified). (3) POSITIONAL CTOR FALLBACK (WI-500 class): build_constructor_term_typed _ arm, for a multi-field ctor given a NON-object scalar/array (foreign/hand-authored store only — our serializer never emits this), builds a positional Term::Fn{pos_args} that silently never discrim-matches the named pattern (and drops extra array elems). Make it a loud error when fields.len()>1 and value isn't an object (vs schema-less fields.len()==0 positional fallback, which can't do better). NOTE: gap (1) is inherent to the flattened TOML/JSON format (the tagged/self-describing redesign was declined for WI-501); WI-501 already converted the sibling none-inside-a-list TOML corruption to a LOUD serialize error (json_to_toml null -> Err) vs the old silent null->empty-string. ACCEPTANCE: nested-Option round-trips faithfully OR errors loudly; a user entity named some/cons serializes correctly; foreign multi-field positional payload errors loudly; cargo-test green.

## Changes

### 2026-06-18T16:48:21Z — feedback — claude

DELIVERED 2026-06-18 (49f5e5c). Closed all three residual WI-501 term_ser round-trip gaps. (1) NESTED-OPTION COLLAPSE: serializing some^k(none) k>=1 for an Option[T=Option[U]] field is now a loud SerError::InvalidValue, not a silent round-trip to none() (flattened TOML/JSON cannot represent present-but-empty; self-describing redesign declined for WI-501). USER DECIDED loud-error over accepting the many-to-one collapse: towers ending in a real value (some(some(5))) still round-trip faithfully; only the some^k(none) family would collapse. (2) SHORT-NAME DISPATCH ASYMMETRY: term_to_value + cons_to_json_array detected Option/List by SHORT name while deserialize resolved by QUALIFIED name, so a user entity named some/none/cons/nil in another namespace was mis-flattened (a user cons serialized to []). Fix: new is_prelude(kb,sym,qualified) gates on the qualified prelude name; the 4 prelude qualified names hoisted to module consts (OPTION_SOME/NONE, LIST_CONS/NIL) as single source of truth; pre-existing inline Ref(none) check routed through is_prelude too. (3) POSITIONAL CTOR FALLBACK: build_constructor_term_typed now errors loudly when a multi-field ctor (fields.len>1) gets a non-object scalar/array payload (would build a never-discrim-matching positional Fn); schema-less fields.len==0 positional fallback preserved. TESTS: 3 new regression tests in toml_ser_test (one per gap). Full workspace green (60 binaries). /code-review xhigh multi-agent: correctness [] + removed-behavior [] (qualified-name narrowing confined to a test-only serialize API with no prod caller); acted on the 2 reuse/altitude findings (consts + uniform is_prelude). Declined 3 lower findings with rationale (stdlib path matches build_test_kb convention; manual test term construction faithful for non-nullary ctors; cpp-gen bare-name acceptance is a separate codegen context).

