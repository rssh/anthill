## Attributes

- id: WI-291
- created: 2026-05-23T07:03:53Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-16T19:50:27Z

- acceptance: cargo-test

## Description

Grammar cleanup: remove the now-redundant 'export' statement + 'export' visibility prefix

Follow-up to the Model C visibility migration (visible-by-default; 'internal' is the only hide gate). After that lands, the standalone 'export' statement (grammar.js:139) and the 'export' choice in the visibility prefix (grammar.js:720) are no-ops kept only for back-compat. This WI removes them:
- tree-sitter grammar: drop 'export' statement rule + 'export' from visibility choice; regenerate parser.
- scaland + rustland parsers/IR: drop Export from Visibility enum and the export-list fields on Namespace/SortWithBody.
- Verify stdlib (already export-statement-free post Model C cleanup) and all tests still pass on both impls.
Low priority / mechanical; do only once Model C is stable.

## Changes

### 2026-05-23T07:00:10Z — feedback — user

Superseded/subsumed by proposal 044 (docs/proposals/044-unified-name-resolution.md) — this grammar removal is step 7 of that proposal, do it last.

### 2026-06-16T19:50:48Z — feedback — user

Delivered (uncommitted). Removed export everywhere: tree-sitter grammar (export_clause rule + 'export' from visibility choice + 3 content lists; regenerated parser, 157/157 grammar tests), rustland (Visibility::Export, Namespace/SortWithBody.exports fields, convert.rs handling, codegen arm), scaland (Visibility.Export, exports fields, exportClause, body parser 3-tuple->2-tuple), kernel-language.md canonical spec + forward-mapping docs. Swept 394 standalone 'export X' statement lines across 114 .anthill/.rs/.scala test-fixture+snippet files (stdlib was already export-free). rustland 1987/0 green; /code-review clean.

KEY FINDING: 'export' was NOT a pure no-op as proposal 044 assumed. Two test breakages exposed it: (1) prove_hint_test relied on 'export bound_d, target' to disambiguate a GREEDY rule_block parse — the brace-less form 'rule <entry>* end' (grammar.js rule_block) greedily swallows a following 'proof ... end', treating the proof's 'end' as the block terminator; the leading export statement incidentally steered GLR toward the single rule_declaration parse. Fixed the test to use a braced 'rule { ... }' block (unambiguous). This is a PRE-EXISTING latent grammar fragility, reproduces on clean main with the export line removed. (2) typing_pass_spec.anthill (docs/, not in sweep ROOTS) still had export statements -> removed.

scaland: 6 PRE-EXISTING ParserIntegrationTest failures (AmbiguousSymbol(not,[anthill.reflect.not, anthill.reflect.not])) — confirmed identical on clean main, the known 'missing is_new gate on scaland variant-exposure parent link' parity gap, NOT introduced here. Not committed yet.

