```anthill
fact WorkItem(id: "WI-1024", created: "2026-08-06T04:18:22Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-08-06T05:19:46Z"))
```

## description

eval::value_functor is the fifth by-carrier list: Entity | Term{Fn|Ref} | SymbolRef answers 'what functor does this value name', which is exactly ViewHead::functor_sym(). Found by /code-review on WI-1023, which converted the other four. SAME DRIFT CLASS: Value::OpRef and Value::Requirement gained structural heads in WI-1019 and Value::Node has had one since WI-276, so all three name a functor the view reads and this list answers None for. NOT converted inline with WI-1023 because the direction is OPPOSITE to that ticket's three: those all fail OPEN (a duplicate answer survives, one logical fact stores twice), while widening this one makes values START naming a carrier sort that previously named none -- eval/eval.rs's carrier_sort route and dynamic spec-op dispatch both read it, and anthill-stl's reflect bridge (bridge.rs:205/269/740) reads it across the crate boundary. So each carrier admitted needs a control asserting WHERE it now dispatches, not merely that nothing broke; a green suite is not evidence (WI-979). WORK: decide per carrier whether functor_sym's answer is the wanted one -- note Value::Tuple heads Functor{None,..} and must stay None, which functor_sym already gives -- then convert and drive. Sites: eval/eval.rs value_functor; callers kb/execute.rs:862, anthill-stl bridge.rs:205/269/740, eval/pattern.rs:328.

