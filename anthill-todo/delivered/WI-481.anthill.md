## Attributes

- id: WI-481
- created: 2026-06-15T19:32:54Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-16T09:25:26Z

- acceptance: cargo-test

- tags: typing

## Description

Typer: a value-in-type (denoted) Modify-bearing effect row fails effect-row EQUALITY across a call -- a correctly-DECLARED 'effects {Modify[p]}' is rejected as UNDECLARED when an op incurs Modify[p]. Repro (PRE-EXISTING, reproduces with the EXPLICIT [Elem,Eff] form too, so NOT a projection issue): given 'operation eff_stream(p: Producer) -> Strm[T = Int64, E = {Modify[p]}]' and a Strm op that incurs its observation row (bare 'effects s.E' OR explicit 'effects Eff'), the consumer 'operation okeff(p: Producer) -> Bool effects {Modify[p]} = obsEmpty(eff_stream(p))' errors 'type mismatch in okeff.effects: expected declared: [Modify[T = p]], got undeclared effect: Modify[T = p]' -- the declared and incurred Modify[T=p] (a denoted/value-in-type effect, Modify parameterized by the VALUE p) are structurally identical yet the undeclared-effect check (check_operation_bodies effect comparison / effects_subset over the incurred vs declared rows) treats them as distinct. This is the WI-460-feedback 'ORTHOGONAL: Modify[c] denoted effect-ROW equality fails across a call EVEN WITH NO PROJECTION (WI-459-class)' note + the WI-475-referenced 'gap #2', now reproduced as its own ticket. The pure-context REJECTION (declared []) and pure-stream ACCEPT (E={}) work (WI-475); only the DECLARED-Modify acceptance direction fails. Root: denoted (value-in-type) effect equality -- Modify[p] vs Modify[p] -- must compare the embedded value p by identity/sort-unifier (cf. WI-382 'denoted equality = the embedded value sort's unifier'); the effect-subset check likely compares the Modify rows by a path that drops/mismatches the value-in-type argument. Cascade-sensitive (effect-row equality over Modify-bearing rows). ACCEPTANCE: okeff-style declared-Modify acceptance loads clean while a WRONG/absent declaration still rejects; wi375/wi441/wi440/effects suites stay green (cargo-test). Rust-only (typer).

