```anthill
fact WorkItem(id: "WI-597", created: "2026-06-29T13:08:21Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-07-01T14:56:23Z"))
```

## description

anthill-todo `list --tag` next-marker should skip non-claimable statuses. The `<- next` marker (cmd_list_tagged -> print_tagged_rows, is_next at main.anthill:1535-1540) currently fires on the first item that is NOT is_satisfied (i.e. not Delivered/Verified) with all deps met. But PreOpened (backlog/deferred), Draft, and the terminal off-ramps (Rejected/ProposalRejected/Stale) are all 'not satisfied' too, so a parked PreOpened ticket wrongly grabs `<- next` ahead of a genuinely-actionable Open ticket. Symptom: wi502 tag marks the deferred WI-574 (Step 6 generative labeling, PreOpened) as `<- next` instead of the live Open WI-573. Blocked items are ALREADY excluded (is_next requires unmet deps = nil). Fix: gate is_next on a positive claimable predicate (status == Open; decide whether Claimed should also qualify given the loop resumes Claimed separately) instead of the negative is_satisfied test; introduce is_claimable_next(status) helper. Mirror in native run_list_tagged if any logic survives the WI-009 cutover. Acceptance: cargo-test green via scripts/test.sh, plus a regression test asserting `<- next` skips a deps-met PreOpened row and lands on the next Open row.

