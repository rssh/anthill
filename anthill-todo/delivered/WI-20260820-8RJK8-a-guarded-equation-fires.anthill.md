## Attributes

- id: WI-20260820-8RJK8-a-guarded-equation-fires
- created: 2026-08-20T12:57:54Z

- status: Delivered
- status_agent: claude
- status_at: 2026-09-08T17:52:49Z

- acceptance: cargo-test, scaland-sbt-test

## Description

A GUARDED EQUATION FIRES NOWHERE — `lhs <=> rhs :- guard` is accepted, indexed when tagged, and read by no firing site. Split out of WI-292's "SIBLING GAP" note, which is the only place in the tracker that owned this and which is DELIVERED, so the gap had no live owner. Surfaced again by WI-888, whose refusal message now tells authors "a guarded equation is read by no firing site" and points here.

DRIVEN, NOT CODE-READ, with a control (2026-08-20). `rule pick(?a, ?b) <=> ?a :- Int64.gt(?a, ?b) [simp]` with the guard TRUE at the redex (`pick(9, 2)`): `OperationBodyMissing`. The IDENTICAL rule unguarded: `Ok(Int(9))`. So the guard alone is what kills it, and the fixture is sound.

WI-292's DIAGNOSIS IS WRONG ON THE TAGGED CASE, corrected here rather than carried forward. It said guarded equations "are not even indexed, because is_equation requires an EMPTY body", conflating two mechanisms. Measured, counting `rules_by_functor(anthill.kernel.unify)` over four (body x tag) combinations against a rule-free base of 20:
  guarded  [simp]  -> 21  INDEXED
  guarded untagged -> 20  not indexed
  bodyless [simp]  -> 21  INDEXED
  bodyless untagged-> 20  not indexed
INDEXING TRACKS THE TAG ALONE (WI-139 unindexes untagged equational HEADS by shape, body irrelevant). So a `[simp]`-guarded equation IS in the bucket and IS reachable by `simp_equation_rids`; selection is not the blocker. The blocker is that every firing site gates on `KnowledgeBase::is_equation`, whose FIRST clause is `entry.body_nodes.is_empty()` — `is_directional_equation` (resolve.rs, what `fire_simp_equation` and `apply_eq_rules` gate on) and `is_simp_equation` (simp_rewrite.rs, what the typer's `try_fire` selects by) both build on it. Nothing anywhere evaluates a matched equation's body.

THE STDLIB POPULATION IS 15, AND NONE OF THEM IS TAGGED — the number that decides how this must be accepted. field.anthill:25-27 (mul/div/recip guarded by `neq(?b, 0)`), indexed_seq.anthill:25-26 (`nth` out-of-bounds, lo and hi), list.anthill:257 (`nth` recursive step), logical_stream.anthill:84/86 (`interleave`, both splitFirst cases), map.anthill:119 (`get` past a different key — sitting directly beneath its `<=>` [simp] siblings), stream.anthill:114/115/118/119/120/121 (headOption/head/tail/isEmpty). Every one is UNTAGGED, so each is dead for TWO independent reasons: no `[simp]` (unindexed, not directional) and a non-empty body (not `is_equation`). CONSEQUENCE FOR ACCEPTANCE: relaxing the body clause alone changes NOTHING for any of the 15 — a suite that goes green on that fix has measured nothing. Either the stdlib rules are tagged as part of this, or the ticket decides that a guarded equation does not need the tag, and says why against WI-881 ("`[simp]` is the enablement").

WHAT MUST BE DECIDED, not just implemented.
(1) IS `[simp]` THE ENABLEMENT HERE TOO? Fifteen authors wrote these untagged, which reads as "this is the definition, not a normalization hint". WI-881/WI-888 say the tag is what enables. Pick one and state it at the site; if the tag stays required, the 15 need it added and that is a behaviour change to the stdlib, not a relabel.
(2) WHO PROVES THE GUARD, and WHEN. Post-match, against the match substitution. The resolver has SLD; the typer's `try_fire` runs at type-check time over occurrences and has no prover in the same sense. The two firing sites may not be able to answer alike, and if only one can, say which and what the other does.
(3) THE UNDECIDED GUARD IS THE SOUNDNESS CASE — the WI-067 hazard WI-292 was built around: a guard that is neither proved nor refuted must SUSPEND (do not fire), never NAF-decide. `splitFirst(?s) = none` over an abstract stream is exactly that shape, and six of the fifteen are it.
(4) TERMINATION. `list.anthill:257` and `logical_stream.anthill:84` are RECURSIVE guarded rewrites; firing them makes the normalizer recurse under a guard for the first time. Bound it or state the bound that already applies.

NOT A DUPLICATE OF WI-292, which is delivered and was the TYPE-directed half (read the redex's carried type to discharge a SORT-LEVEL `requires`, the Set/Map dormancy). This is the VALUE-guard half: an explicit `:- guard` on the rule itself. WI-292's own note says so ("NOT type-directed -- a separate firing concern ... Likely its own WI"). Prerequisites it named are all Delivered: WI-584 (var-RHS instantiation), WI-578 (typed-value substrate), WI-502 (umbrella), WI-292 itself.

LIKELY CONSUMER, worth checking at claim time rather than assumed: WI-567 (open) is blocked on "a guard discharges iff its predicate bottoms out in NATIVE scalar builtins ... isEmpty and Eq[List].eq are anthill-RULE-defined, so refuting them needs the rules FIRED on the prove path". `Stream.isEmpty` IS one of the fifteen above (stream.anthill:120/121), so firing guarded equations is at least part of what WI-567 needs. WI-566 cites the same wall. Neither lists this in `depends_on` today.

ACCEPTANCE: a guarded equation whose guard HOLDS at the redex fires and the test asserts the VALUE (`pick(9, 2)` -> 9), with the guard-FALSE row asserting it does not fire and an UNDER-DETERMINED row asserting it suspends rather than deciding; the control is the same rule unguarded, which fires either way, kept in its own fixture; at least one of the fifteen stdlib rules is driven to a value end-to-end, so "it loads" is not the evidence; the decision on (1) is written at the firing site and in kernel-language.md 5.3 beside the equational-rules paragraph, which currently says only that a guarded head keeps its `=` spelling; full workspace green via rustland/scripts/test.sh.

REFERENCE: WI-292 (delivered, the SIBLING GAP note this is split from, and the wrong indexing claim corrected above), WI-881 (`[simp]` is the enablement), WI-888 (the refusal message that points here), WI-139 (unindexing by head shape), WI-067 (never NAF-decide an undetermined obligation), WI-584/WI-578/WI-502 (delivered prerequisites), kb/mod.rs `is_equation`, kb/resolve.rs `is_directional_equation` / `apply_eq_rules`, kb/simp_rewrite.rs `is_simp_equation` / `fire_simp_equation`, docs/design/constrained-term-substrate.md "Conditional rewrite rules".

## Changes

### 2026-09-08T15:19:13Z — feedback — user

DOWNSTREAM CONSUMER, found 2026-09-08 from WI-20260908-PW9A0: TYPED PATTERNS. A `?x: T` bound on a guarded equation is refused at load (TypedPatternNotEnforced::NotARewrite). PW9A0 tried to LIFT that refusal, on the ground that WI-742's `install_typed_head_domain_goals` skips only `is_directional_equation` and so hands a guarded equation a generated `domain(?x, T)` goal. It does — the bound installs and the body carries the extra goal, both observable — but the goal is INERT for exactly this ticket's reason: nothing evaluates a matched equation's body. DRIVEN with an UNSATISFIABLE bound, three rows over one program whose parameters are Int64: `pk: pick(?x: Bool, ?y) = ?y :- src(?x, ?y)`, the same with `?x: Int64`, and the same with no annotation — `kb.simplify(pick(1, 5))` is BYTE-IDENTICAL in all three. The annotation is decorative; nothing would notice if it were absurd. The lift was REVERTED and PW9A0 ships without it.

WHAT THAT COSTS THIS TICKET, and it is a gain: when a guarded equation fires, its typed bound acquires a reader and the refusal can be narrowed. But NOT on `is_equation` alone — WI-903's own test (`typed_bound_on_a_guarded_equation_is_refused`) catches that, and correctly: a `[simp]`-TAGGED guarded equation is a broken PROMISE (the tag asks for a rewrite the body prevents) and must stay refused however this ticket lands. The narrowing predicate is "does anything this rule promises fail to materialize", not "is this an equation". Decision (1) above — is `[simp]` the enablement — decides which side of that line an untagged guarded equation ends on, so the two questions are the same question.

PINNED MEANWHILE: `a_guarded_equation_keeps_its_refusal` in wi_pw9a0_rule_tvar_in_bound_test.rs asserts BOTH guarded spellings stay refused and carries the unsatisfiable-bound measurement as the reason, so the next person to make PW9A0's argument finds the counter-evidence attached to it.

ON THE SIX stream.anthill LAWS IN THIS TICKET'S POPULATION: they were annotated `?s: Stream` as an experiment and reverted. Worth knowing before decision (1): annotating them adds nothing even after this lands, because each operation ALREADY declares it (`operation headOption(s: Stream) -> Option[s.T]`, and likewise head/tail/isEmpty/splitFirst). A typed pattern earns its place only where it NARROWS below the declared signature — WI-582's `keep(?x: Summable, ?y) <=> ?x [simp]`, where the signature says `x: A` and the bound says "only where A provides Summable". So "fire guarded equations" and "type them" are independent wins; this ticket does not need the second.

### 2026-09-08T17:52:43Z — feedback — user

DELIVERED. A guarded equation fires; the four decisions the ticket demanded are made and written at the sites.

(1) `[simp]` IS THE ENABLEMENT HERE TOO — the tag, not the guard, decides whether a rule fires. Written at `simp_rewrite::is_simp_equation`, `resolve::is_directional_equation`, kernel-language.md 5.3 ("The tag still decides, and the guard does not substitute for it"), and at each stdlib site. THE REASON IS `field.anthill`: `div(?a, ?b) = mul(?a, recip(?b)) :- neq(?b, 0)` is TRUE under its guard and, oriented, would rewrite every division in the program into a reciprocal multiply. The guard says WHEN the equation holds; the tag says WHETHER TO ORIENT it, and they are different questions.
CONSEQUENCE, exactly as the ticket required: relaxing the body clause alone changes nothing for any of the fifteen, so three were TAGGED — `indexed_seq.anthill`'s `nth_oob_lo` / `nth_oob_hi` and `map.anthill`'s `get(put(m,k2,v),k)` law, whose own comment named this ticket as the reason it was untagged. The other twelve stay untagged with the reason written at each: `Field`'s three and `euclid_div` do not reduce (whether the algebra library wants a normal form is its decision, not a consequence of a firing gap closing), `logical_stream`'s two are RECURSIVE rewrites on a possibly-infinite carrier, and `stream`'s six carry a `splitFirst(?s) = …` guard that needs the carrier's dictionary to run — WI-567's wall. The population is still 15; its composition has drifted since the ticket was written (`list.anthill:257`'s `nth` is an operation with a body now; `division.anthill:105`'s `euclid_div` joined).

(2) WHO PROVES THE GUARD, AND WHEN — post-match, against the match substitution, by an ordinary nested SLD search (`simp_rewrite::guard_verdict`). ALL THREE firing sites, through ONE evaluator, so a tagged rule means the same thing at a resolution goal, in the operation body that spells the same call, and at a method call. (Three, not two — see the review findings below; the first count was a census of the sites I edited rather than of the readers of the predicate I widened.) The typer CAN answer alike, and the reason it can is checked rather than assumed: `type_check_sorts` runs ONCE after every file has loaded, so the guard sees the complete KB and there is no file-order hazard. At the typer the redex is compile-time syntax, so most guards are simply under-determined and decline; the ones that fire are already decidable from the program text, which is constant folding.

(3) THE UNDECIDED GUARD SUSPENDS — the search runs `definite_only`, so a FLOUNDERED answer yields nothing and the redex is left standing. Driven with `not(p_flounder(?a))` over an undecidable inner goal — the shape that laundered an undecidable guard into a fire — with two CONTROL rows sharing the fixture: `not(p_def(999))` refutes definitely and FIRES, `not(p_def(1))` holds and does not. Backing `definite_only` out makes exactly the suspend row red and leaves both controls green.

(4) TERMINATION — only reducing rules were tagged (see (1)), so no new unbounded recursion enters the normalizer; the recursive stdlib laws are left untagged with the reason at the site. The guard recursion itself is bounded: `SIMP_GUARD_MAX_DEPTH = 1`, and the nested search runs `simplify: false` so the ordinary goal-time rewrite path is out of the loop. NOTHING COVERS THE EXCEEDED ARM, and that is said at the constant rather than left to be inferred from a green suite.

WHAT ELSE MOVED, found rather than predicted.
* WI-903's typed-pattern refusal NARROWED WITH NO EDIT AT THE LOADER, because that site asks `is_directional_equation` rather than restating it: a `[simp]`-tagged guarded equation now HAS the enforcer the refusal said it lacked. `typed_bound_on_a_guarded_equation_is_refused` became `…_follows_the_tag` (tagged: bound kept; untagged: still refused, over one program text), and PW9A0's `a_guarded_equation_keeps_its_refusal` became `an_untagged_guarded_equation_keeps_its_refusal`. PW9A0's measurement — an UNSATISFIABLE bound changing nothing — is kept at that test as the record of what "the mechanism ran" is worth without a firing site behind it.
* THE LOADER WAS NOT INSTALLING A GUARDED EQUATION'S WRITTEN RHS OCCURRENCE (WI-FCZ3N's channel). It gated on `body_nodes.is_empty()`, and `parse_equation_rhs` narrowed further to `<=>` (`is_equation_functor` has exactly one member since WI-888), so a guarded `=` law fell to the term-derived RHS builder and lost the author's spans. Both widened; the parse-side reader now asks only the SHAPE question and the KB's `is_equality_connective_functor` stays the one authority on meaning. FOUND, not reasoned about: FCZ3N's own population census, once widened to `has_equational_head`, named all three newly-tagged stdlib laws — every one `=`-spelled.
* `is_equation` KEEPS its empty-body clause and its own readers (the resolver's candidate triage, WI-888's `=`-spelling refusal): a guarded equation IS a Horn clause with a body and has always been one of those candidates. The firing sites moved to `has_equational_head`, which is that same shape test with the body clause dropped. One name did not acquire two questions.

NOT DONE, and named rather than left to be discovered. `parse_equation_lhs` was deliberately NOT widened with its RHS sibling: whether a guarded `=` head should also MINT `SymbolKind::EquationFunctor` for its subject is a name-resolution question with its own population, and every guarded `=` rule in the corpus writes a subject that some `operation` already declares, so nothing is currently withheld by it. Stated at the function.

WHAT `/code-review` FOUND, all three real, all three DRIVEN before the fix went in — none was a code-read, and each is now a row with its own back-out.
* A THIRD SELECTION SITE. `typing::try_fire_dot_rule` selects through `fires_as_dot_rule` → `is_simp_equation`, the predicate this ticket widened, so a `[simp]`-tagged GUARDED dot rule reached it and would have fired with its precondition discarded. The spec sentence "both firing sites answer alike" was written from a census of TWO. Fixed at the site; the spec now says three. NOTHING IN THE CORPUS DRIVES IT (no guarded dot rule is written anywhere), and that is stated at the call rather than left for a green suite to imply.
* THE GUARD'S WITNESS WAS THROWN AWAY, and this was a WRONG VALUE, not an absent rewrite. MEASURED: `rule h: pick(?a) <=> wrap(v: ?p) :- src(?a, ?p) [simp]` over `fact src(1, 42)` fired to `wrap(v: <unbound Global>)`. A variable bound only by the guard and used in the RHS is the standard conditional-rewrite idiom — `stream.anthill` writes six — so `guard_verdict` now returns the proof's bindings for the RHS variables the head match left open. Because the rewrite's RESULT then depends on the witness, the guard must DETERMINE it: two different witnesses for an RHS variable is not a decision and the rule declines (the same WI-067 answer an under-determined guard gets).
* A GUARD OVER AN OPEN-WORLD OPERAND WAS DECIDED STRUCTURALLY. At the typer a parameter reaches a guard as a `var_ref`, which a scalar builtin reads as a ground constant; `step_init` has this guard (WI-537/WI-067) but only under a Γ overlay, which a guard search does not carry. MEASURED BOTH WAYS on a HETEROGENEOUS fixture: without the check, `pick(?a, ?b) <=> ?a :- neq(?a, ?b) [simp]` rewrote a body `pick(p, q)` over two `Int64` PARAMETERS to `var_ref(p)`, while the ground `pick(1, 2)` beside it folded to `1` either way — so "the symbolic one did not fire" is a measurement and not an observation that nothing happened.

TESTS. `wi_8rjk8_guarded_equation_fires_test`, 11 rows, with a RUN back-out table in its header — seven axes, each naming which rows go red and which stay green, measured rather than predicted. Two of them (`is_directional_equation` and `is_simp_equation`) are separate CALL SITES with separate coverage, so neither is credited to the other's work. `stdlib_indexed_seq_out_of_bounds_law_fires_to_a_value` drives a real stdlib law end to end (`IndexedSeq.nth(nil, -1)` → `none`, with the guard-false row beside it), which is the ticket's "not just it loads".

ACCEPTANCE. Rust workspace green via `rustland/scripts/test.sh`. `scaland` `sbt test` has TWO PRE-EXISTING failures in `BootstrapTest` (`field.anthill:128:12`, "cannot emit the type `Ring`") — MEASURED with every stdlib change of this ticket stashed: same two failures, same reason, same refusal-set diff. They are not this ticket's.

