## Attributes

- id: WI-826
- created: 2026-07-23T13:23:33Z

- status: Delivered
- status_agent: user
- status_at: 2026-07-24T20:29:15Z

- acceptance: cargo-test

## Description

FIX the cover-walk ORIENTATION holes that let bindingless/partially-bound requires spellings bypass or over-refuse the WI-821 σ-gate (/code-review findings 2, 9, 10 — finding 2 CONFIRMED by live drive, 9/10 code-traced PLAUSIBLE). (1) MEASURED: a bare bindingless 'requires Desc' (no brackets — legal, loads clean, zero stdlib/test occurrences) scope entry covers ANY goal: requires_entry_covers_goal iterates only ENTRY bindings and returns true on empty BEFORE the σ mode, so Strategy 3's FromScope forwards the middle sort's dict over a CONCRETE downstream goal — 3-hop drive (AOps requires Desc[AT] → MOps bare requires Desc → BOps at concrete Pebble) measured 1010001 where 5010001 is correct. The empty early-return is literally redundant (the loop falls through to true) — the defect is ORIENTATION: the walk never examines goal keys the entry lacks. (2) The same orientation asymmetry means a PARTIALLY-bound entry (multi-param spec, requires Desc[T = X] with U omitted) is refused by Strategy 1 (dep-driven walk, missing key → false) yet accepted by Strategy 3's scope cover (entry-driven walk checks only T) — resurrecting the exact var_ref forward the gate refused, falsifying the ResolutionScope.sigma doc's guarantee; latent until a multi-param-spec fixture exists. (3) Strategy 2's composition is an EMPTY-MAP no-op for a bindingless slot entry (build_child_subst_map finds no bindings), leaving the sub-entry in slot-space where — per the hunk's own comment — a correct nested forward always σ-disagrees: nested forwarding under a bare-requires slot is refused where the coarse mode forwards requirement_at_sort correctly. DECIDE + FIX: under σ, a bindingless or partially-bound cover should be NO cover (fall through to construction) or a deliberate, documented wildcard — and whichever rule wins, BOTH walks must examine the UNION of binding keys so entry-vs-entry and entry-vs-goal agree (the pair verdict is already shared via binding_pair_covers; this ticket shares the KEY ITERATION). ACCEPTANCE: a wi821 fixture for (1) measures 5010001; a multi-param-spec fixture pins (2); a bindingless-slot nested-forward fixture pins (3); wi817 (15) + forwarding suites + full workspace green.

## Changes

### 2026-07-24T20:14:40Z — feedback — user

DISCOVERED WHILE BUILDING THE FINDING-10 FIXTURE, adjacent but OUT OF SCOPE (present on HEAD, not introduced by this ticket): Strategy 2's nested forward emits requirement_at_sort(slot, k) where k indexes the SPEC's static direct_requires_chain, but the RUNTIME bundle is built from the resolution tree's sub_resolutions, which come from the CARRIER's requires. So a carrier providing a spec UNCONDITIONALLY while the SPEC itself carries requires resolves to ResolvedRequiresNode::Leaf, whose emit_tree_as_projection builds construct_requirement(impl, NIL) - an EMPTY bundle the static index still points into. Repro: 'sort Mid { sort MT2 = ?; requires Desc[T = MT2]; operation midop(x: MT2) -> Int64 }' + 'sort Gem { entity gem; fact Desc[T = Gem]; fact Mid[MT2 = Gem]; operation describe(x: Gem) = 5; operation midop(x: Gem) = 50 }' + 'sort Outer { requires Mid[MT2 = Gem]; operation o(n) = BOps.b(gem()) }'. Loads CLEAN (Strategy 2 finds the composed cover Desc[T = Gem] and forwards), then panics at eval: 'requirement_at_sort: index 0 out of range (slot has 0 sub-requirements)' (eval/requirement_arena.rs project). Loud rather than silently wrong, but the load-time verdict and the runtime bundle disagree about a slot's shape - the Leaf arm should either discharge the SPEC's own requires into the bundle, or the load should refuse the forward. Consequence for this ticket: wi826's bound-slot fixture (bound_slot_nested_forward_is_found_at_load) is pinned at LOAD only, with the reason recorded in its doc comment.

### 2026-07-24T20:14:56Z — feedback — user

FINDING 9 (partially-bound entry) REFUTED BY DRIVING - its premise is false. The ticket predicted a partially-bound 'requires Desc[T = X]' (U omitted) would be key-MISSING, refused by Strategy 1's dep-driven walk and accepted by Strategy 3's entry-driven one. It is never key-missing: a BRACKETED requires is stored as a SortView whose omitted parameters are FILLED with the spec's own param ref, so 'requires Pair[P = MT]' carries 'Q = Pair.Q'. Driven: the 3-sort fixture (Holder requires Pair[P=Leaf,Q=Leaf] -> Partial requires Pair[P=MT] -> Callee requires Pair[P=CP,Q=CQ]) does not even load on HEAD - WI-828 refuses it with 'element Q = Pair.Q is unconstrained at this call site'. BOTH walks see Q and both hand it to binding_pair_covers, so there is no orientation asymmetry to fix. Only the BRACKET-LESS form (no SortView at all, hence genuinely zero bindings) produces the missing key - i.e. finding 2 is the whole of the defect. Pinned as wi826's partially_bound_entry_fills_the_omitted_param_and_is_refused_at_load so the refutation stays refuted. FINDING 2 reproduced EXACTLY as measured (1010001 pre-fix, 5010001 post-fix). FINDING 10's sigma refusal was already the behavior on HEAD; the fix makes it principled (a bare-requires slot's composition map is empty, so its sub-entry cannot denote anything in caller scope) and it is now pinned.

### 2026-07-24T20:28:33Z — feedback — user

/simplify (4 agents) APPLIED. Reuse+Simplification converged on the same locus: the hoisted key iteration was still the pre-existing hand-rolled find(same_label), which the binding_for_param doc explicitly forbids ('any bindings-list lookup by param key goes through binding_for_param', WI-726/764/768/769/825) - enrolled BOTH legs, so the identity-first pass is gained and one function can no longer decide 'entry names goal key' and 'goal names entry key' by two different rules; binding_for_param's 'keep this list honest' enrollment ledger updated. Altitude found the REAL doc defect: my leg-2 justification ('the demand is a DERIVED goal that sort_goal_from_subst drops keys from') is CALLER-SPECIFIC and false for the Strategy-3 caller, whose goal is goal_from_requires_entry(dep) - the very same WRITTEN dep Strategy 1 just handed entries_cover. Re-justified by the QUESTION each walk answers instead: entries_cover asks 'does this dictionary SATISFY that slot' (a slot naming nothing takes any dictionary - the callee said nothing to contradict it), requires_entry_covers_goal asks 'IS this THE dictionary for this goal' and a yes SHORT-CIRCUITS construction, so an unnamed element must answer 'cannot tell'. Altitude also named the resulting DUAL asymmetry (entry names a key, goal names none: the two walks give OPPOSITE verdicts; Strategy 1 returns first so entries_cover's is the reachable one) - documented as deliberate + currently unobservable, NOT driven. Orientation is now TYPE-ENFORCED (newtype Supply/Demand around the two same-typed slices) so a transposed call - literally this ticket's defect - is a compile error. Also applied: entries_cover's stale doc updated for the delegation (the WI-802 stale-neighbour failure mode); explain_dep_refusal's mirror note trimmed to the actionable half; spec now states the NON-symmetric converse (fewer elements weakens a SUPPLY and widens a DEMAND); one-line alloc win in type_param_sym_of_binding (resolve_sym borrows, no to_string needed - 15 call sites, and this diff doubled its frequency); test file: BOPS const hoisted (3 verbatim copies), the rot-prone WI-828 prose substring dropped from the refutation assert (only the rendered element witnesses the fill), the bound-slot doc trimmed to the one bit it actually asserts, sync note added to the shared Desc/Leaf block. SKIPPED with reasons: hoisting with_instances into tests/common (5th copy, but touches 4 sibling files - already tracked as a WI-829 residual); a debug_assert on sort_goal_from_subst's three STRUCTURAL drops (out of diff, and the re-justification no longer depends on that distinction); efficiency's bitmask/spec_qn-laziness (its own verdict: 'micro- to sub-microsecond against a 400ms load, nothing justifies restructuring'); sharing stdlib loads across the 5 tests (needs a cached-KB helper in common/, pays off suite-wide not here); a should_panic companion for the Leaf-empty-bundle gap (pins an incidental panic string; the feedback record is the right home).

### 2026-07-24T20:28:49Z — feedback — user

FOLLOW-UP CANDIDATE (no ticket, recorded here): refuse a BRACKET-LESS 'requires Spec' at LOAD when Spec has type parameters. Corpus impact is ZERO - all 18 bracket-less requires in .anthill sources target parameter-less specs (anthill.cli.Main, Logic.Minimal, Logic.Constructive, and the test-only Base/Pingable) - and it turns a spelling that loads clean and silently means 'wildcard' into an early named error, per CLAUDE.md 'prefer a loud error over a silent skip'. It would also retire the reachable half of the dual asymmetry documented at requires_entry_covers_goal. NOT a replacement for the walk fix, and the altitude review gave the decisive reason: unwrap_spec_view_value returns (sym, EMPTY) for ANY spec value that is not a SortView application - the ViewHead::Ref/Ident arms and the non-SortView functor arm - so a denoted (Value::Node) spec for a parameterized spec arrives keyless no matter how it was written. A parser-level guard closes the syntactic door only; the cover walk is where the invariant is CONSUMED and is total over every producer. Also REJECTED at review: normalizing bracket-less into a filled SortView. The loader's fill loop (load.rs ~4067-4090) walks sort_base_subst, which carries OPERATION slots as well as type params and auto-binds them via find_operation_in_scope, so normalizing would change the op-binding shape of all 18 existing sites and the stored SortRequiresInfo fact shape that direct_requires/spec_base_functor, the flatten key, the resolve_cache key and the nominal-subtype chain all read - large blast radius, and it still would not cover the Value::Node producer.

