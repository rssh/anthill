```anthill
fact WorkItem(id: "WI-908", created: "2026-07-31T21:41:42Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "user", at: "2026-07-31T23:54:19Z"))

fact Feedback(workitem: "WI-908", author: "user", at: "2026-07-31T23:54:50Z")
```

## description

`KnowledgeBase::resolve_name_in_global` (rustland/anthill-core/src/kb/mod.rs) IS STILL PRE-WI-752: it ranks the ABSOLUTE `by_qualified_name` lookup FIRST, ahead of scope resolution, and carries no head-qualification rung at all — the exact inversion WI-752 removed from `resolve_name_in_kb_opt`, whose ticket recorded it as the reason `anthill query` could bind the same dotted text to a DIFFERENT symbol than the program it queries. It is `pub`; live callers are kb/extent.rs (three sites) and the anthill-stl reflect bridge's SortQuery. WI-900 routed its IMPLICIT tier through the shared `resolve_implicit`, so that rung now agrees with the other three — the surrounding ladder does not. NOT DRIVEN: raised by /code-review's altitude pass on the WI-900 diff, code-read only; WI-752's own suite (wi752_dotted_ladder_test) covers the loader and query positions, not this one. FIRST STEP: drive the divergence — a name whose absolute reading and whose scope reading differ, resolved through an extent/reflect-bridge path, asserting which symbol it binds. ACCEPTANCE: this position reads `resolve_dotted_in_kb` in the same order as every other, or its deviation is a stated and tested decision naming why the reflect bridge needs a different one; wi752 tests stay green; cargo-test green via scripts/test.sh.

## Feedback

### 2026-07-31T23:54:50Z — user

DELIVERED 2026-07-31 (commit 2d17e0b9, branch wi908-global-name-ladder). Full workspace 3929/0 green; wi752_dotted_ladder_test and both extent suites green.

THE FIX: resolve_name_in_global delegates to load::resolve_name_in_kb_opt at `_global` (that fn went pub(crate)). 9 lines -> 2.

THE TICKET WAS WRONG TWICE, both checked not assumed. (1) Its caller list names the anthill-stl reflect bridge's SortQuery; the bridge has not called this since WI-632 (a10105ec, 2026-07-09), which moved sort_query to carry the sort BY REFERENCE with 'zero name-string resolution'. So the acceptance clause 'or its deviation is a stated and tested decision naming why the reflect bridge needs a different one' has no subject — three callers remain, all kb/extent.rs. (2) It frames the defect as the order inversion alone; there were THREE.

DRIVEN through register_extent_owner (the live caller), pre-fix measured by restoring the old body:
- ORDER: with a top-level `import wi908.lib`, mounting `Member` bound the loader's QUALIFIED-ONLY kernel meta-sort, not `wi908.lib.Member`. A DIFFERENT SYMBOL — and exactly WI-423's delocalized phantom, resurrected by the absolute rung.
- MISSING RUNG: `Box.thing` (reachable only by qualifying an imported head) resolved to NOTHING.
- VISIBILITY: `wi908.priv.Vault.secret` mounted despite `internal`.
Four tests fail pre-fix, each at its own claim line; the fifth passes BOTH sides — the control that the alignment costs the implicit tier nothing.

WHY THE PURELY-SOURCE ORDER INVERSION IS UNREACHABLE, since the fixture looks contrived and is not: at `_global` a resolvable head's qualified name EQUALS its own spelling for locals, and locals beat imports, so head-qualification and absolute coincide. The only way the two readings differ is a name that is a _global local/import AND some symbol's whole qualified name — which needs a qualified-only registration, the loader's own idiom for KERNEL_META_SORTS/KERNEL_FUNCTORS. That is why `Member` is the subject: it is the mechanism in use, not an invention.

ONE CAPABILITY DROPPED, pinned as the decision the acceptance asked for: the absolute rung is dotted-only (WI-476), so a short host name must be in scope. `Sort`/`Fact`/`Member` (qualified-only kernel registrations) no longer mount bare.

REVIEW CAUGHT A FALSE CLAIM OF MINE IN THREE PLACES AT ONCE — the biggest win. I wrote, in the doc comment, in kernel-language.md and in a test NAME, that a short name 'now resolves only if it is in scope'. FALSE: the ladder ends in `.or_else(resolve_implicit)`, which is SHORT-NAME KEYED, so `SortInfo`/`OperationInfo`/`and`/`not`/`cons` still resolve bare with no scope presence (measured with the stdlib loaded; the pre-fix comment I deleted had stated this correctly). Corrected in all three, and pinned by a fifth test. Its subject is `SortView` not `SortInfo` because SortInfo carries 119 resident facts and the mount is then refused as ResidentCollision — which is the single-owner rule, not the ladder; noted at the site.

ALSO FROM REVIEW: ~20 lines of doc prose retelling this ticket at three sites, deleted (the WI-900 lesson, committed again and caught again); `resolve_implicit`'s pub(crate) had become vestigial and is private; the local `fn sym` was the 6th copy of `kb.resolve_symbol`; an implied assertion dropped; an unused fixture entity dropped. Efficiency review: neutral, measured — all three callers are cold (registration / per-mutation write; the READ path already takes a Symbol).

FOLLOW-UPS FILED, all code-read not driven, all saying so: WI-911 (the mount takes the ladder's VisibleOnly half but NOT its diagnostic half, so a hidden name reports as absent — the misleading-diagnostic case WI-752 split the ladder to avoid), WI-912 (persistence/term_ser.rs, the CLOSEST twin — still carries the WI-476 bare short-name global fallback, plus two local re-spellings of dotted_by_head), WI-913 (four more absolute-only host-name positions; reflect/builtins.rs lookup_symbol_op's MESSAGE and CODE disagree), WI-914 (anthill query --mode listing modes disagree with --mode pattern — a stated decision to re-examine, not an assumed bug), WI-915 (`_global` re-derived at ~12 sites; caching it makes resolve_name_in_global `&self`), WI-916 (one question, three failure shapes, and External FactRefs re-resolving a name the mount already resolved).

