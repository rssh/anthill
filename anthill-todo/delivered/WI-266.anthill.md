## Attributes

- id: WI-266
- created: 2026-05-18T04:42:34Z

- status: Delivered
- status_agent: user
- status_at: 2026-08-15T10:25:39Z

- acceptance: cargo-test

## Description

Brainstorm session: HOF inheritance — sharpness vs simplicity. Decide whether to keep 027.1's conservative-union HOF rule indefinitely or add annotations enabling sharper analysis. Same session resolves the closure-capture appendix (same conservative-vs-sharp question redirected). DEFERRED until a concrete driver appears: a stdlib op where over-reporting blocks effect-row inference; a rustus-pattern Rust codegen case producing unidiomatic signatures; an agent/integration workflow where false-positive effect rows trigger noisy refactors; or a pluggable-backend HOF where over-reporting breaks interchangeability. Full plan with inputs to gather, topics to cover (three candidate annotation families: result-aliasing markers, closure-disposition markers Fn/FnMut/FnOnce-style, region polymorphism Tofte-Talpin), cross-language fit, migration path, typer cost, and decision outputs in docs/brainstorms/hof-inheritance-sharpness.md. On completion: update 027.1 §Discharge through higher-order combinators (or fork 027.2 if scope warrants) and collapse 027.1 OQ1. (Originally filed locally as WI-264; renumbered to WI-266 when origin/main landed a different WI-264 for the typer Result<_,TypeError> refactor before this commit was pushed.)

## Changes

### 2026-08-15T10:25:34Z — feedback — user

RESOLVED BY CONSTRUCTION — the session was never held and no longer needs to be. The deferral gate was 'a concrete driver where the conservative-union over-report blocks useful code'; no driver could ever fire, because the conservative rule was never implemented. While this WI sat deferred, proposals 045 (effect rows / row polymorphism) and 046 (effect_derive's input: the feed-relationship) landed the SHARP analysis, and today's stdlib is written against it: 'operation map[Dst, EffP](c: C, f: (x: Element) -> Dst @ {EffP, -Modify[x]}) -> Stream[Dst, {E, EffP}]' (iterable.anthill:61) — a per-callback row PARAMETER, a binder-scoped lacks constraint, and a precise result row. Same shape at iterable.anthill:41/67, list.anthill:87, combinators.anthill:66/125, finite_collection.anthill:46/68/72, monad.anthill:23-24, stream.anthill:88. ANSWER TO THE THREE CANDIDATE FAMILIES: (1) result-aliasing markers NOT adopted — the feed-relationship is derived from the operation's BODY (WI-352 flow-derivation pass -> flow facts over anthill.reflect.feed), and a body-less op declares it as [feeds: ...] metadata (046 s4.2) whose carrier landed with WI-087/WI-309; (2) closure-disposition markers ADOPTED but in effect-row currency — the 045 lacks constraint -Modify[x] on the callback binder, call-site checking WI-440, first stdlib consumers filter/find/map WI-441 — NOT an anthill Fn/FnMut/FnOnce; (3) region polymorphism NOT adopted — Tofte-Talpin SUBSTITUTION is what op_boundary_effects performs (region.rs:248, WI-314 result-region arm + WI-353 callback-param arm, keep/drop by provenance via keep_modify), but signatures stayed region-monomorphic, so the months of typer work this plan priced were not spent. The closure-capture appendix (the redirected half of OQ1) needs no separate machinery: a captured target is a place like any other and keep/drop comes from provenance. EVIDENCE THE PIPELINE RUNS FROM SOURCE, not paper: wi341_callback_modify_test::callback_element_modify_surfaces_on_list_from_source (binder -> place -> body row Modify[f.a] -> WI-352 flow -> WI-353 re-key -> reported undeclared Modify[l]); wi440_callback_lacks_test + wi441_iterable_arrow_pred_test drive the checking direction. DOCS UPDATED (the WI's own stated deliverable), commit 6fb42a7d rebased onto origin/main: (a) 027.1 s'Discharge through higher-order combinators' rewritten to the shipped rule — the three-pattern table replaced; direct rebind survives as the Modify[result] -> Modify[<op>.result] re-key, container coarsening survives as result_type_admits_region (sort set sourced from Modifiable facts, region.rs:52, so not stdlib-privileged), conservative union is GONE; the section now also states coarsening's stated narrowness (a region reachable only through a returned NAMED SORT'S FIELD is not seen — an unsound drop, region.rs:141-152, unreachable in today's stdlib) and the v1 conservatism at region.rs:342; (b) 027.1 OQ1 collapsed — 'Open questions' now reads 'None outstanding' with a dated resolved-question record; (c) 027.1 header gained a 'Superseded in part by 045/046' line; (d) docs/brainstorms/hof-inheritance-sharpness.md marked RESOLVED BY CONSTRUCTION with an Outcome section (family-by-family verdict table) and the historical plan kept below. NOT CLOSED, recorded in the resolved entry rather than swept: (i) dependent-effect abstraction — a callback whose effect denotes its OWN bound parameter (take_and_zero(acc,c) effects Modify[c] passed to foldLeft) cannot bind the caller-side EffP without escaping c from the arrow scope, refused with unconstrained-EffP; pinned with its flip condition by wi424_iterable_members_test::iterable_fold_mutating_callback_not_lacks_rejected_but_row_gated — note the direction is UNDER-support of the sharp reading, not the over-report OQ1 was watching for; (ii) codegen does not carry the sharpened rows — Rust lowers an arrow to a bare fn pointer and erases the effect row (codegen/rust.rs:616-638), C++ discards the arrow's effect child and REFUSES a denoted-bearing arrow parameter, which every -Modify[x]-carrying stdlib HOF signature now has (anthill-cpp-gen/src/lib.rs:2174-2186, :4845-4858), so Iterable.map/find/filter cannot be emitted to C++ as written. Also confirmed adjacent-but-separate and already tracked: WI-1050 (a [simp] rule's rows checked at the declaration, so an effect-POLYMORPHIC op's row variable blocks valid laws) and WI-317 (inferred effect rows). Diff is docs-only (2 files, +79/-16); no runtime code touched, so cargo-test is unaffected — /code-review run at low effort returned no findings (docs-only diff).

