## Attributes

- id: WI-705
- created: 2026-07-12T19:13:29Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-13T07:51:23Z

- acceptance: cargo-test

- tags: wi698

## Description

Effect-row self-contradiction at SIGNATURE altitude (WI-700 sibling / altitude follow-up, found by WI-700 /code-review). WI-700 rejects a self-contradictory instantiated CALLBACK-PARAM row (shield[EffP={Outside}](poke) -> {Outside,-Outside}) but only for an eta'd OP-REF callback arg, inside the per-arg validate_callback_effect_row. Two shapes of the SAME uninhabitable-row bug slip through (both verified LOADING on HEAD af269943): (a) an op's OWN instantiated row -- operation g[E]() -> Int64 effects {E, -Outside} called g[E={Outside}]() loads with an uninhabitable {Outside,-Outside} signature (no callback param, so validate_callback_effect_row never inspects it); (b) a LAMBDA callback with a self-contradictory instantiated declared row -- shield[EffP={Outside}](lambda () -> 5) loads (validate_callback_effect_row bails at the arg_op_sym extraction for non-var-ref args). FIX: hoist the self-contradiction check to a per-call signature validation right after seed_op_type_args (typing.rs), decomposing the op's OWN declared effect row AND each arrow/Function-typed param's row (after subst) via decompose_effect_row_raw + the shared row_self_contradiction predicate -- catching all callback shapes and non-callback rows uniformly, at the right altitude. SUBSUMES WI-700's eta-scoped self-contradiction reject (can then be removed). ACCEPTANCE: g[E={X}] own-row + lambda self-contradiction both reject with a targeted diagnostic; full cargo-test green.

## Changes

### 2026-07-13T07:51:45Z — feedback — claude

DELIVERED (code daa72975). Replaced WI-700's eta-scoped per-arg self-contradiction reject (in validate_callback_effect_row) with ONE per-call check_signature_self_contradiction over the op's OWN effect row + every arrow-typed param row (actual-agnostic). PLACEMENT is the crux and DEVIATES from the ticket's 'right after seed_op_type_args': that point sees only EXPLICIT [E={..}] args, but WI-700's per-arg reject ALSO ran POST-unify and thus caught an INFERENCE-bound row param (multi-arg op where one param infers EffP={X} and a -X-lacks callback param inherits it) -- placing the check only after seed would SILENTLY REGRESS that. So it runs AFTER the arg-unify loops (subst carries full instantiation, explicit+inferred), before validate_callback_effect_row (reverted to the FILTERED decompose, now owns only actual-vs-declared conformance). /code-review (high, 3 finder angles) FLAGGED the regression; the correctness finder THEORIZED it impossible (bind_row_tail lacks-gate), but an EMPIRICAL probe proved it REAL (HEAD rejects f(src,src); ticket-placement loads) -- the lacked label flows via a DIFFERENT param's row (no lacks registered) then the lacks-bearing row inherits it, bypassing the tail gate. LESSON: verify a suspected regression empirically, don't trust the static theory. Gate = any op type param BOUND (explicit or inferred), subst-based (not occ.kind -> no drift with seed). GUARDED false-positive (also /code-review-found + break-verified): guarded(X,g) present must NOT hard-clash with -X (WI-067 discharge may drop it) -- MULTISET-counted so a literal X alongside a guarded X still rejects. FOUR break-verified mechanisms (own-row branch, param branch, post-unify placement, guarded exclusion -- each toggled RED). Full suite 2778/0, then rebased over WI-707 (also touched typing.rs; clean, re-tested 2783/0). KNOWN LIMITATION (documented, NOT a WI-700 regression): return-position + nested/curried inner arrow rows not decomposed. WI-706 (lambda-callback CONFORMANCE) remains -- distinct mechanism.

