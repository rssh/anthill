## Attributes

- id: WI-432
- created: 2026-06-11T06:15:27Z

- status: Delivered
- status_agent: claude
- status_at: 2026-07-11T19:40:39Z

- acceptance: cargo-test

## Description

anthill-todo CLI: fail LOUDLY on missing work item / mistargeted store (loud-error principle; probed 2026-06-11 during WI-428 delivery). (a) 'deliver WI-NNN' on an id absent from the store prints 'error: work item ... not found' but EXITS 0, so shell '&&' chains continue past a failed delivery -- ALL mutating subcommands must exit nonzero on their error path. (b) 'feedback WI-NNN' APPENDS the Feedback fact WITHOUT validating the work item exists (two facts landed in a store containing no WI-428) -- validate existence before append. (c) The -d footgun that surfaced both: rustland/anthill-todo/ is the CRATE directory and contains a STALE legacy workitems.anthill (last touched WI-237-era, May; crate tests build their own temp stores so it appears unreferenced), so '-d rustland' silently targets it and even parses Cargo.toml as a data file (warning-only). Mitigations: verify+delete or relocate the stale crate-local file; have every MUTATING command print which store file it is writing; consider refusing to operate when the resolved store dir lacks a parseable workitems.anthill.

