## Attributes

- id: WI-588
- created: 2026-06-29T06:20:31Z

- status: Delivered
- status_agent: claude
- status_at: 2026-06-29T13:25:26Z

- acceptance: cargo-test

- depends_on: WI-585, WI-594

- tags: finiteness

## Description

Finiteness Phase B (proposal library/003): finite-preserving combinators + dispatch coherence. Add FiniteCollection.map/filter returning FiniteStream (new finite mapped/filtered carriers whose source FIELD is typed FiniteStream, so they unconditionally provide FiniteStream — no conditional instance). Dispatch coherence (kernel dep 2, VERIFIED mechanism): the kernel selects among same-named ops from different specs by PROVISION-GRAPH DISTANCE (find_spec_op_for_provided_sort typing.rs:2942-2993, BFS direct-before-transitive, first match) — NOT a requires/sub-spec ranking (pick_most_specific typing.rs:8912 is impls-of-SAME-spec by ground-binding count). So shape the provides graph so FiniteCollection.map is strictly CLOSER than Iterable.map on a List (FiniteCollection depth 1 via FiniteStream; Iterable depth 2 via Stream->Iterable; Stream defines no map) and ADD A TEST pinning it — equal distance is order-dependent / ambiguity error. Doc gap: kernel-language.md §8.7 is silent on the two-different-specs-same-op case. Rewire wi492_transitive_provision_test to the finite path (xs.map(inc).size() => List->FiniteStream->size). Additive: Stream/Iterable consumers still present, suite stays green. Depends on WI-585 (Phase A).

## Changes

### 2026-06-29T08:41:09Z — feedback — user

DEFERRED 2026-06-29 (depends WI-594). Phase B stdlib drafted then REVERTED: the finite-preserving combinators need effect-row threading the typer lacks. Approach that works once WI-594 lands: map/filter on FiniteCollection (List provides it DIRECTLY → coherence beats transitive Iterable.map), effect = FiniteCollection's SORT param E (grounds via provision, no projection), body wraps a finite-stream view of the receiver. New sorts needed: FiniteStream (provides Stream covariant-return + FiniteCollection), FiniteMappedStream/FiniteFilteredStream carriers (source field typed FiniteStream → unconditional provision). Covariant-return provision VERIFIED working (MappedStream precedent). The finiteIterator(c)->FiniteStream[Element,E] indirection was a workaround for WI-594 but cascaded backing-burden onto every carrier — drop it once WI-594 lets fmapped(s,f) over a bare receiver thread the effect. Reproducer of the blocker: wi594 test (bare-receiver map over MappedStream).

### 2026-06-29T13:25:26Z — feedback — user

DELIVERED 2026-06-29 (finiteness Phase B, proposal library/003). Finite-preserving map/filter on FiniteCollection returning FiniteStream, so a finite pipeline stays consumable. NEW: prelude/finite_stream.anthill (FiniteStream provides Stream covariant-tail + FiniteCollection) + prelude/finite_combinators.anthill (FiniteMappedStream/FiniteFilteredStream, mirror the lazy carriers; recursive fmapped(rest,fn) bare re-wrap = the WI-594 case). FiniteCollection gains finiteIterator (finite dual of iterator) + map/filter = fmapped/ffiltered(finiteIterator(c),.). List provides FiniteStream + finiteIterator=l; Map finiteIterator=entries. Registered in anthill-stl/src/stdlib.rs AND scaland EmbeddedStdlib.scala. COHERENCE (the load-bearing piece): map/filter live on FiniteCollection so a Map (finite non-stream) gets them too, but a Map provides Iterable AND FiniteCollection BOTH DIRECTLY (equal provision-graph distance) so dot-dispatch m.filter(p) tied to the LAZY Iterable.filter. FIXED by a resolver tie-break in find_spec_op_for_provided_sort (typing.rs): keep nearest-distance definers, then on a tie prefer the requires-REFINEMENT (FiniteCollection requires Iterable -> wins); kernel-language.md 8.7 documents both stages. (The stdlib alternative -- FiniteCollection PROVIDES Iterable -- was tried and rejected: a concrete sub-spec iterator entangles Iterable's abstract bodies, breaking pure-Iterable carriers, wi495.) Verified: full rust 897/0/3, scaland 187/0, /code-review xhigh (caught the Map dot gap; typer tie-break reviewed clean). Tests: wi588 (List map/filter/fold eval; dispatch coherence; Map dot map/filter finite + eval); wi492 rewired to finite + added a qualified-Iterable lazy-carrier test; wi278 rewired to finite (consume via FiniteCollection.foldLeft); wi345 expects 5 finite shadows (3 transitional size/foldL/foldR + 2 PERMANENT map/filter); wi585 FiniteBag backs finiteIterator. GOTCHAS: collect(xs.map(f)) -- direct body-less primitive on the dot-chain's abstract FiniteStream -- fails dispatch (use size/foldLeft); -> WI-598. Follow-ups: WI-599 (thin no-finiteIterator design: needs WI-594 generalized to FiniteCollection fields + WI-593), WI-598.

