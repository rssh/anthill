## Attributes

- id: WI-272
- created: 2026-05-19T05:51:11Z

- status: Delivered
- status_agent: claude
- status_at: 2026-05-19T20:15:53Z

- acceptance: cargo-test

- depends_on: WI-269, WI-273

## Description

WI-269 Phase E: thread operation type-args through eval frame per docs/design/operation-call-model.md §'Operation type arguments'. (1) apply_within gains a positional type_args channel (Expr::ApplyWithin field, parse/load/typer support). The typer fills in resolved type-arg term values (sort_ref / parameterized) per declared type-param, in declaration order; named-form bindings are reordered to positional during elaboration. (2) Frame gains either type_args: SmallVec<[(Symbol, TermId); 2]> (separate channel — cleaner) or frame.requirements is extended to hold both RequirementHandle and Value::Type entries (per the design doc's single-map model — needs a polymorphic value type). Sort-level entries occupy the leading positions; type-arg entries follow in declaration order. (3) Body interpretation: a body's var_ref(T) where T matches a declared type-param resolves against the frame's type-arg channel — same lookup mechanism as a __req_* read, just hitting the type-arg side. (4) Closures: lambda_within captures both channels via the existing requirements-map mechanism — no new closure machinery. (5) Erasure decisions deferred to codegen targets (proposal 029 forward mapping erases most op-level type-args; SMT-LIB and type-driven dispatch backends preserve them). (6) Acceptance fixture (operation-call-model.md §test fixture): operation foo[T](x: T) -> T = x is called as foo[Int](42); frame inspection asserts (a) sort-level entries occupy leading positions, (b) an entry keyed T holds the type-value for Int, (c) order is sort-level first then T, (d) a second call foo[String]('hi') produces a fresh frame whose T holds String (per-call binding, not shared). Negative case: foo(42) (inferred) produces a frame whose T equals Int — frame contents identical to the explicit call. Out of scope: codegen erasure decisions (file separately per backend); higher-rank type params. Acceptance: cargo-test green; the frame-inspection fixture from operation-call-model.md §'Operation type arguments' passes; eval.rs no longer has  ignoring the channel on Expr::Apply.

