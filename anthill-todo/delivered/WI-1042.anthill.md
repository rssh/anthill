```anthill
fact WorkItem(id: "WI-1042", created: "2026-08-07T10:57:40Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: nil), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-07T15:55:23Z"))

fact Feedback(workitem: "WI-1042", author: "user", at: "2026-08-07T10:52:53Z")

fact Feedback(workitem: "WI-1042", author: "user", at: "2026-08-07T15:55:16Z")
```

## description

CLEANUP WITH TEETH (WI-1035/1036 family; /code-review 2026-08-07 findings 5/6/12/14 + the owner stamps of finding 13): ONE OWNER for the defaulted-dispatch gate. (1) typing.rs:41504 is_defaulted_spec_op is a prose-coupled THIRD spelling of the WI-444 gate (check_apply_iter: lookup_spec_op_dispatch().is_none() && spec_op_parent_sort().is_some()) — the equivalence is held only by a doc comment 30k lines away, so when the gate gains a clause the rule-body fire population silently diverges from the op-body pin/refusal population and no test moves (the corpus population is zero). ALSO its two-HashMap-probes-SHIELD claim is wrong for the MAJORITY population: a NON-operation functor (every relational atom head) misses kb.op_record and falls into lookup_operation_info O(N_ops) linear scan over all OperationInfo facts (op_info.rs:197-209), paid in the pre-scan AND the dispatch walk (~745 predicate calls per load measured by the WI-1026 diff instrumentation, which measured only the third clause). FIX: one shared defaulted_spec_op_parent(kb, f) -> Option<Symbol> used by all three sites, with a cheap non-operation early-out; a test that FAILS if the two spellings drift. (2) typing.rs:13027 — dot_member_dispatch_decision defaulted leg is an inline copy of the WI-1012 tie condition with a self-admitted prose-only coupling (nothing enforces that but this sentence); the stated reason not to extract — the pin would walk the suppliers twice — does NOT hold: a helper taking the ALREADY-COMPUTED candidate slice (the exact shape supplier_tie_error / refuse_unarbitrated_supplier_tie already use) serves both sites in one walk. Without it, when check_apply_iter gains a clause the dot spelling silently diverges from the qualified spelling on the same program — the exact spelling-keyed silence WI-1035 was opened to close. (3) typing.rs:12974 — the spec_op_parent_sort-miss early-out silently degrades to Ok(DotMember::Take), skipping the entire WI-1035 tie guard, with no statement of what a None means there and no ticket, unlike every other early-out in the function; it is reached only after find_spec_op_for_provided_sort answered Some (the member DOES back a provided spec), so the miss is an unexpected shape. FIX: loud error, or a recorded impossibility argument at the site — never a silent fallback (CLAUDE.md: prefer a loud error over a silent skip). (4) typing.rs:12838 — the ~100-line contract for dot_member_dispatch_decision (measured table, TIER reasoning, the must-be-written-HERE-too warning) is one contiguous doc block that runs straight into enum DotMember, so rustdoc attaches ALL of it to the enum and the function renders undocumented — the WI-887 comment-placement failure mode reproduced in the file that recorded it. FIX: attach the essay to the function. (5) STAMP LIVE OWNERS at the four unowned gap sites (finding 13; the repo rule: a gap with a reason has no owner — ticket number at the site): the body-less rule-body residue at typing.rs:41398 currently citing Delivered WI-1027; the general-Apply narrowing in dispatch_calls_in_occ (no ticket); the carrier_own_op-disagreement fallback citing Delivered WI-616 at typing.rs:13016 (stamp THIS ticket); load.rs:12050/12081 WI-246 later-work sentence + the loader-synthesized 1:1 residual (stamp WI-1041). The body-less/general-Apply numbers are in this ticket feedback. ACCEPTANCE: one gate helper shared by all three sites; both tie sites read one condition helper; the Take fallback is loud or argued at the site; rustdoc attaches the contract to the function; all four stamps in place; behaviour-neutral — full suite green via rustland/scripts/test.sh, plus the drift test above. REFERENCE: /code-review 2026-08-07; WI-1035; WI-1012; WI-444; WI-653; feedback-typer-no-nongeneric-code.

## Feedback

### 2026-08-07T10:52:53Z — user

STAMP NUMBERS for item (5): the body-less rule-body residue at typing.rs:41398 and the general-Apply narrowing at dispatch_calls_in_occ both stamp WI-1043 (fired beside this ticket; it owns that population and records the general narrowing as its residue). The other two stamps are as written: carrier_own_op fallback -> this ticket; load.rs WI-246 sentence + loader-synthesized 1:1 -> WI-1041.

### 2026-08-07T15:55:16Z — user

DELIVERED with two of the ticket's own claims REFUTED BY DRIVING, both recorded at their sites. (1) Item (3) asked for a loud error or an impossibility argument at the dot site's spec_op_parent_sort miss: the shape is REACHABLE and LEGAL — 'sort Leaf provides Plain' where Plain declares no 'sort X = ?' loads clean and reaches it, so Take is the ANSWER and raising would refuse a legal program; as a panic! it fired on that one fixture and on nothing else in 4321 tests. Kept as a driver (a_non_parametric_provided_sort_takes_the_member), not prose. (2) Item (1)'s two spellings are NOT equivalent, and the first justification written for the unification was VACUOUS — 'driven over every operation in the corpus' walks all_operation_params, one entry per OperationInfo FACT, exactly the population where '!operation_has_no_body' and 'op_has_runnable_body' cannot differ. Found by this ticket's own /code-review. The real measurement: with the old body test in the shared gate, 15 tests across 5 suites fail (wi936, wi618, parameterized_provides_block, sql_store_example, wi1042), 14 through is_defaulted_spec_op's rule-body walk type-checking ENTITY-CONSTRUCTOR calls — re-conjoining the strict test there alone restores exactly those 14. The strict read is required, not stronger. The other two sites are safe by a stated precondition each (check_apply_iter sits inside the lookup_operation_info_full bind; the dot site is reached only via an OperationInfo walk). A FOURTH reader was found by the same review: WI-1040's defaulted_spec_op_witness_grounds_soundly asked the gate's question by name while reading the body-AGNOSTIC parent and leaning on caller scan order; it now asks the gate — not observable, stated as such, the change is to the failure mode. COST claim also corrected: the check_apply_iter frame went 5 -> 4 reaches of type_params_of_sort, not 2 -> 1 (lookup_spec_op_dispatch twice + self_receiver_spec_sort remain); collapsing the rest is WI-1011's memo, not a gate question. Gate leg reach MEASURED (stdlib+bindings): 523 calls, name split answers 244, 279 reach the body probe, 55 the parametric leg; old order 466/80. Full workspace 4321 passed / 0 failed.

