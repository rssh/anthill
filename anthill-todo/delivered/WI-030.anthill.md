```anthill
fact WorkItem(id: "WI-030", created: "2026-03-26T12:02:57Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-029"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-05-04T19:55:05Z"))

fact Feedback(workitem: "WI-030", author: "user", at: "2026-05-04T19:55:12Z")
```

## description

SLD O(n²) optimization: lazy substitution in resolve.rs. Replace eager apply_subst_each on remaining goals after each match with lazy walk — apply_subst goal[0] through frame.subst at selection time in step_init, skip apply_subst_each in step_choice_point. Reduces O(n²) to O(n × goal_size).

## Feedback

### 2026-05-04T19:55:12Z — user

Already implemented in resolve.rs (verified 2026-05-04). Lazy walk lives at step_init lines 358-374 with the [WI-030] tag: σ is applied to goals[0] only at selection time, memoized back into goals[0]; skipped when subst is empty. The corresponding eager apply_subst_each calls were dropped from step_choice_point (see [WI-030] comments at :1077 and :1109). A lazy_walk_calls stats counter and test assertion at :4595 verify the lazy walk is exercised; comment at :4601 records that the historical apply_subst_each_goals metric scaled n²/2 before the change.

