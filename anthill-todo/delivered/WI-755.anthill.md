```anthill
fact WorkItem(id: "WI-755", created: "2026-07-18T13:47:43Z", context: none, acceptance: [ToolPasses(tool: "cargo-test", params: none)], depends_on: some(value: ["WI-756"]), generates: none, requires_capability: none, status: Delivered(agent: "claude", at: "2026-08-17T17:14:18Z"))

fact Tag(workitem: "WI-755", name: "wi502")
```

## description

Retire the now-STALE WI-573 typer pre-gate so guarded-effect discharge reaches semantic-eq dispatch (WI-573's deferred 'option (a)'). WI-573 shipped a SOUND FLOOR on 2026-06-29: guard_conjunct_overrides_structural_eq (typing.rs, called from the shared per-conjunct core conjunct_refuted ~21503) returns false — SUSPEND, keep the effect — whenever an eq/neq operand's carrier, at top level or at any nested element/field, has a custom eq/neq member. That was correct AT THE TIME because anthill.prelude.PartialEq.eq was a purely STRUCTURAL builtin that never dispatched an override, so any refutation it produced was unsound. THAT PREMISE HAS SINCE EXPIRED. WI-616 (delivered 2026-07-03, with WI-625/WI-628/WI-664) made eq/neq SEMANTIC: they register as BuiltinTag::SemEq/SemNeq (kb/mod.rs:5435,5437), dispatch at resolve.rs:3159 into sem_eq_core (resolve.rs:4010) -> sem_eq_values (4029), whose ordered outcomes (4059-4081) are exactly the discipline WI-573 wanted: (3) no override in the KB -> structural; (4) head-carrier override over DEEP-GROUND operands -> sem_eq_dispatch (resolve.rs:4168) proves <carrier>.eq(a,b) by bounded sub-resolution, routing a bodied override through the eval bridge and a rules-backed one through prove_rule_predicate; (5) BURIED override -> delay rather than mis-decide (its doc at resolve.rs:4002 cites 'the WI-573 reasoning' by name); (6) otherwise structural. The load-time dispatch index (load.rs:4680-4726) is keyed on every constructor of a carrier whose carrier_own_op(.., eq) exists — i.e. precisely the WI-573 case. SO THE TYPER PRE-GATE NOW SHADOWS THE VERY DISPATCH IT WAS WRITTEN TO WAIT FOR: it short-circuits before refute_guard -> prove_from_gamma -> resolver, so the guard path can never reach SemEq. The in-code deferral comments still say 'until runtime monomorphization can dispatch the override' (typing.rs:21500-21501) and 'the deferred half (the ticket's option (a))' (21663-21665). SCOPE: narrow or remove guard_conjunct_overrides_structural_eq and let SemEq/SemNeq decide. SOUNDNESS ARGUMENT TO PRESERVE: the resolver returns Delay for non-deep-ground operands and for a buried override, and prove_from_gamma's definite_only maps a delay to UNPROVEN — the conservative direction — so the floor WI-573 established is kept by the resolver rather than by the typer. Verify that mapping holds before deleting the gate; if any path turns a delay into a definite verdict, narrow the gate to just that path instead of removing it. WATCH: WI-648 (modular typeclasses, scoped/non-canonical instances) would change WHICH override is canonical and thus interacts with a dispatch-based decision. THIS IS THE DISCHARGE CAPABILITY WI-566 AND WI-567 ACTUALLY NEED — both currently list WI-573 as satisfied and so read as unblocked, but the capability they depend on is this ticket, not the delivered floor. Acceptance: the existing characterization test wi573_eq_override_discharge_test::custom_eq_override_suspends_rather_than_dispatches (tests/include/wi573_eq_override_discharge_test.rs:176) FLIPS to assert the DROP — its own comment already says 'If this ever starts dropping, the dispatch half (option (a)) has landed and this test should move to assert the drop'; a custom eq override returning true still KEEPS the effect (custom_eq_override_keeps_effect, :98) and the nested-carrier keeps (:228, :271) stay green; native/structural carriers still discharge (:124, :317); a non-ground or buried-override operand still SUSPENDS rather than deciding; cargo-test green via scripts/test.sh.

