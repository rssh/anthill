
 anthill-todo with workitems repository works well in one-developer project, but with multiole devvelopers we have git conflict in workitems.

What to-do
  - add to project files (which is reconfigurable by project) the type of data sources, it can be one of
     - local (local files, as now)
     - github  (workitems are github issues)
     - api    (future direction, will have a standartized API for remote server.)


